---
phase: "03-realtime-and-still-input-slice"
plan: "03-04"
status: "completed"
type: "execute"
wave: 4
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - "BeautyDemo/BeautyDemo.xcodeproj/project.pbxproj"
  - "BeautyDemo/BeautyDemoTests/InputPipelinePrivacyTests.swift"
  - "BeautyDemo/BeautyDemoTests/BeautyDemoImportBoundaryTests.swift"
  - "BeautyDemo/BeautyDemoTests/BeautyDemoViewStateTests.swift"
  - "FRONTEND.md"
  - "SECURITY.md"
  - "RELIABILITY.md"
  - "PRODUCT_SENSE.md"
  - "QUALITY_SCORE.md"
  - "PLANS.md"
autonomous: true
requirements: ["PIPE-01", "PIPE-02", "PIPE-03", "PIPE-04", "PIPE-06", "PIPE-08", "DEMO-01"]
requirements_addressed: ["PIPE-01", "PIPE-02", "PIPE-03", "PIPE-04", "PIPE-06", "PIPE-08", "DEMO-01"]
must_haves:
  truths:
    - "D-01 through D-04 remain true after all input slices: the first screen is `EditorShellView`, Camera/Photo are enabled switches, permission is tap-driven, and preview card is the single changing surface."
    - "D-05 through D-09 remain true after all input slices: Photo has system picker plus fixture path, denied/unavailable/cancel/failure states preserve shell visuals, and purpose strings are local-first."
    - "D-10 through D-14 remain true after all input slices: compare is shared and display-only, loading/errors preserve previous visuals, copy is friendly, and pipelines use latest snapshots with stale-work handling."
    - "PIPE-01 through PIPE-04 and PIPE-06 are backed by focused Demo XCTest coverage."
    - "PIPE-08 is backed by generated Info.plist purpose strings, no-upload scans, raw-error copy scans, and local-first docs."
    - "DEMO-01 is backed by view-state tests proving the main flow offers Camera and still-image editing modes."
  artifacts:
    - "`InputPipelinePrivacyTests` or equivalent deterministic project/source scan coverage for purpose strings, no internal SDK imports, no upload path, no raw error/path copy, and no realtime `UIImage` conversion."
    - "Updated root contract docs recording the delivered Camera/Photo behavior without duplicating business rules across owners."
    - "Updated quality and plan ledger evidence for Phase 3 verification."
  key_links:
    - "Generated Info.plist keys -> `SECURITY.md` protected-resource requirements -> `InputPipelinePrivacyTests`."
    - "Camera/Photo source files -> static no-upload/no-internal-import/no-UIImage scans."
    - "Phase 3 tests -> `QUALITY_SCORE.md` and `PLANS.md` verification evidence."
  scope_guard:
    - "This plan intentionally reaches the file-count warning threshold because AGENTS.md requires updating owning root docs and PLANS.md when durable UI, security, reliability, product, and quality evidence changes."
    - "Do not broaden 03-04 beyond final privacy/static tests, exact Phase 3 verification evidence, and narrowly scoped owning-doc updates."
---

# Plan 03-04: Add Purpose Strings, Privacy Checks, and Input Pipeline Tests

## Phase Goal

**As a** Demo user, **I want to** use Camera and Photo with honest local-first permission and error behavior, **so that** the input pipeline is safe to validate before later effect work.

<objective>
Close the Phase 3 privacy and verification gate by confirming purpose strings, adding deterministic privacy/import/static-scan tests, synchronizing root docs that own new behavior, and running the focused input pipeline checks across Camera, realtime processing, Photo, compare, and local-first boundaries.

Purpose: This plan turns the implementation slices into auditable Phase 3 evidence for PIPE-01, PIPE-02, PIPE-03, PIPE-04, PIPE-06, PIPE-08, and DEMO-01.
Output: Privacy test coverage, final static scans, contract-doc sync, and recorded verification evidence.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@ARCHITECTURE.md
@DESIGN.md
@FRONTEND.md
@SECURITY.md
@RELIABILITY.md
@PRODUCT_SENSE.md
@QUALITY_SCORE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-realtime-and-still-input-slice/03-CONTEXT.md
@.planning/phases/03-realtime-and-still-input-slice/03-UI-SPEC.md
@.planning/phases/03-realtime-and-still-input-slice/03-RESEARCH.md
@.planning/phases/03-realtime-and-still-input-slice/03-VALIDATION.md
@.planning/phases/03-realtime-and-still-input-slice/03-01-SUMMARY.md
@.planning/phases/03-realtime-and-still-input-slice/03-02-SUMMARY.md
@.planning/phases/03-realtime-and-still-input-slice/03-03-SUMMARY.md
@BeautyDemo/BeautyDemo.xcodeproj/project.pbxproj
@BeautyDemo/BeautyDemoTests/BeautyDemoImportBoundaryTests.swift
@BeautyDemo/BeautyDemoTests/BeautyDemoViewStateTests.swift
</context>

## Artifacts this phase produces

## Scope Guard

Plan 03-04 is at the normal file-count warning threshold by design. It is the final evidence and contract-sync slice, so it includes privacy/static tests plus only the root documents that own newly implemented Phase 3 behavior. Executors must keep doc changes limited to observed Phase 3 evidence and must not perform unrelated roadmap, quality-score, or historical documentation cleanup.

- `InputPipelinePrivacyTests.swift`: deterministic XCTest or source-scan helper tests for purpose strings, local-first/no-upload boundaries, friendly copy, and no internal SDK target imports.
- Updated `BeautyDemoImportBoundaryTests.swift`: expanded import-boundary coverage for Camera, Editor, and Demo tests.
- Updated `BeautyDemoViewStateTests.swift`: final state matrix coverage for Camera/Photo modes, permission, unavailable, loading, error, compare, and accessibility-relevant labels/hints.
- Confirmed generated Info.plist keys `INFOPLIST_KEY_NSCameraUsageDescription` and `INFOPLIST_KEY_NSPhotoLibraryUsageDescription`.
- Root doc sync in `FRONTEND.md`, `SECURITY.md`, `RELIABILITY.md`, and `PRODUCT_SENSE.md` only where the implemented behavior creates durable contract evidence.
- Updated `QUALITY_SCORE.md` and `PLANS.md` with exact Phase 3 verification evidence and any honest environment limitations.

<tasks>

<task id="03-04-W0" type="auto" tdd="true">
  <name>Wave 0: add privacy and source-boundary tests</name>
  <files>
    BeautyDemo/BeautyDemoTests/InputPipelinePrivacyTests.swift
    BeautyDemo/BeautyDemoTests/BeautyDemoImportBoundaryTests.swift
    BeautyDemo/BeautyDemoTests/BeautyDemoViewStateTests.swift
  </files>
  <read_first>
    .planning/phases/03-realtime-and-still-input-slice/03-VALIDATION.md
    .planning/phases/03-realtime-and-still-input-slice/03-UI-SPEC.md
    SECURITY.md
    QUALITY_SCORE.md
    BeautyDemo/BeautyDemoTests/BeautyDemoImportBoundaryTests.swift
  </read_first>
  <behavior>
    - Test or scan that generated Info.plist settings contain the exact Camera and Photo purpose strings per D-09 and PIPE-08.
    - Test or scan that Demo and Demo tests import `BeautySDK` only, never internal SDK targets.
    - Test or scan that Camera/Photo source contains no `URLSession`, `http`, `https`, upload wording, raw path copy, or raw framework error domains.
    - Test or scan that realtime Camera source contains no `UIImage` conversion per PIPE-02.
    - Test final view-state coverage for Camera and Photo enabled modes, permission/unavailable/loading/error/compare copy, and previous-visual preservation expectations.
  </behavior>
  <action>Create `InputPipelinePrivacyTests.swift` using deterministic file/project inspection from the test bundle or test source location. Expand existing import-boundary and view-state tests rather than adding broad simulator UI automation. Keep all test assertions on local files, value state, and UI-SPEC strings so they are reliable under simulator permission variability.</action>
  <acceptance_criteria>
    - `InputPipelinePrivacyTests.swift` exists and cites `PIPE-08`, D-09, D-13, and the local-first no-upload boundary.
    - Internal SDK import scan covers `BeautyDemo/BeautyDemo` and `BeautyDemo/BeautyDemoTests`.
    - Static privacy tests reject upload/network references in Phase 3 input pipeline source.
    - Static `UIImage` test distinguishes realtime Camera path and fails on camera conversion usage.
  </acceptance_criteria>
  <verify>
    <automated>xcrun simctl list devices available</automated>
    <automated>xcodebuild -project BeautyDemo/BeautyDemo.xcodeproj -scheme BeautyDemo -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:BeautyDemoTests/InputPipelinePrivacyTests -only-testing:BeautyDemoTests/BeautyDemoImportBoundaryTests -only-testing:BeautyDemoTests/BeautyDemoViewStateTests test</automated>
    <automated>rg -n "NSCameraUsageDescription|NSPhotoLibraryUsageDescription|Use the camera to preview beauty processing on this device|Select photos to preview beauty processing on this device" BeautyDemo/BeautyDemo.xcodeproj/project.pbxproj</automated>
    <fallback>If simulator discovery or XCTest fails for environment reasons, record the exact failure and still run all static scans in this task.</fallback>
  </verify>
  <done>The privacy and source-boundary test file exists and covers the Phase 3 protected-resource and local-first gates.</done>
</task>

<task id="03-04-01" type="auto" tdd="true">
  <name>Finalize purpose strings and local-first source checks</name>
  <files>
    BeautyDemo/BeautyDemo.xcodeproj/project.pbxproj
    BeautyDemo/BeautyDemoTests/InputPipelinePrivacyTests.swift
    BeautyDemo/BeautyDemoTests/BeautyDemoImportBoundaryTests.swift
  </files>
  <read_first>
    BeautyDemo/BeautyDemo.xcodeproj/project.pbxproj
    .planning/phases/03-realtime-and-still-input-slice/03-UI-SPEC.md
    SECURITY.md
    ARCHITECTURE.md
  </read_first>
  <action>Confirm the Debug and Release generated Info.plist settings contain the exact Phase 3 camera and photo purpose strings. If either key is missing from either app build configuration, add it beside the existing generated Info.plist keys without unrelated Xcode project churn. Ensure the privacy/import tests fail if Demo source imports internal SDK targets, contains upload/network calls in Camera or Editor input paths, exposes raw file paths or framework error domains in user-facing copy, or removes the local-first purpose strings.</action>
  <acceptance_criteria>
    - Both app build configurations contain exact local-first Camera and Photo purpose strings per D-09.
    - Source scans show no `URLSession`, `http`, `https`, upload copy, raw `/private/var` path copy, or raw framework error copy in Phase 3 input paths.
    - Demo app and tests import only the public `BeautySDK` facade.
    - No third-party registries or package installs are introduced.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "INFOPLIST_KEY_NSCameraUsageDescription = Use the camera to preview beauty processing on this device\\.|INFOPLIST_KEY_NSPhotoLibraryUsageDescription = Select photos to preview beauty processing on this device\\." BeautyDemo/BeautyDemo.xcodeproj/project.pbxproj</automated>
    <automated>! rg -n "import Beauty(Core|Render|Detection|Effects|Resources)" BeautyDemo BeautyDemo/BeautyDemoTests 2>/dev/null</automated>
    <automated>! rg -n "URLSession|http://|https://|upload|/private/var|NSError|AVError" BeautyDemo/BeautyDemo/Camera BeautyDemo/BeautyDemo/Editor BeautyDemo/BeautyDemo/Support 2>/dev/null</automated>
    <fallback>The internal-import and network/raw-error scans must return no matches. If a scan finds only comments in a test intentionally documenting a forbidden token, move that token to a regex-built string so grep gates remain meaningful.</fallback>
  </verify>
  <done>Protected-resource purpose strings and local-first source boundaries are machine-checkable and pass static scans.</done>
</task>

<task id="03-04-02" type="auto" tdd="true">
  <name>Run final input pipeline verification and sync owning docs</name>
  <files>
    FRONTEND.md
    SECURITY.md
    RELIABILITY.md
    PRODUCT_SENSE.md
    QUALITY_SCORE.md
    PLANS.md
  </files>
  <read_first>
    AGENTS.md
    PLANS.md
    FRONTEND.md
    SECURITY.md
    RELIABILITY.md
    PRODUCT_SENSE.md
    QUALITY_SCORE.md
    .planning/phases/03-realtime-and-still-input-slice/03-VALIDATION.md
  </read_first>
  <action>Run the focused Phase 3 verification set, then update only the owning root docs whose contracts now have implementation evidence. `FRONTEND.md` owns Camera/Photo UI states, compare, and accessibility checks; `SECURITY.md` owns purpose strings and local-first protected-resource behavior; `RELIABILITY.md` owns backpressure, stale-frame drops, loading, and recoverable error preservation; `PRODUCT_SENSE.md` owns acceptance evidence for realtime and still-image journeys. Update `QUALITY_SCORE.md` and `PLANS.md` with exact commands, pass/fail results, simulator fallback details, and remaining risks. Do not edit unrelated dirty documentation outside these files.</action>
  <acceptance_criteria>
    - Every Phase 3 requirement ID appears in automated test names, comments, static scans, or verification evidence.
    - Root docs record only durable behavior and avoid duplicating the same rule across multiple owners.
    - `PLANS.md` records any simulator or SwiftPM environment failure honestly if full verification cannot run.
    - `QUALITY_SCORE.md` raises or leaves scores based only on observed evidence, not planned behavior.
  </acceptance_criteria>
  <verify>
    <automated>xcrun simctl list devices available</automated>
    <automated>xcodebuild -project BeautyDemo/BeautyDemo.xcodeproj -scheme BeautyDemo -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' test</automated>
    <automated>swift test --package-path BeautySDK</automated>
    <automated>! rg -n "UIImage" BeautySDK/Sources BeautyDemo/BeautyDemo 2>/dev/null</automated>
    <automated>! rg -n "import Beauty(Core|Render|Detection|Effects|Resources)" BeautyDemo BeautyDemo/BeautyDemoTests 2>/dev/null</automated>
    <automated>rg -n "PIPE-01|PIPE-02|PIPE-03|PIPE-04|PIPE-06|PIPE-08|DEMO-01" BeautyDemo/BeautyDemoTests QUALITY_SCORE.md PLANS.md</automated>
    <fallback>If simulator or SwiftPM commands fail because of environment availability, record the exact command, exit status, and relevant stderr in `PLANS.md` and the plan SUMMARY. Static scans still must run.</fallback>
  </verify>
  <done>Phase 3 has final automated evidence, local-first privacy checks, and synced owning docs for the input pipeline behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Protected-resource purpose strings -> OS prompt | User-facing OS copy must accurately describe local Camera/Photo use. |
| Demo source -> network/logging boundary | Camera/photo pixels and paths must remain local and not appear in upload or raw-log paths. |
| Phase implementation -> quality evidence | Claims must be backed by tests, static scans, or recorded environment failures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Information Disclosure | Camera/Photo protected resources | mitigate | Verify exact local-first purpose strings and no upload/network references in input paths. |
| T-03-04-02 | Information Disclosure | error and status copy | mitigate | Static tests reject raw path/framework error copy and require UI-SPEC friendly strings. |
| T-03-04-03 | Denial of Service | realtime queue behavior | mitigate | Keep Camera pipeline tests and final full test command covering bounded in-flight and stale drops. |
| T-03-04-04 | Tampering | unsupported pixel formats | mitigate | Preserve SDK unsupported-format test coverage and Camera pipeline error mapping evidence. |
| T-03-04-05 | Elevation of Privilege | SDK boundary | mitigate | Static scans reject direct Demo imports of internal SDK targets. |
| T-03-SC | Tampering | package installs | mitigate | No package manager installs; no third-party registry or dependency additions in Phase 3. |
</threat_model>

<verification>
Overall plan verification:
- `xcrun simctl list devices available`
- `xcodebuild -project BeautyDemo/BeautyDemo.xcodeproj -scheme BeautyDemo -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' test`
- `swift test --package-path BeautySDK`
- `rg -n "INFOPLIST_KEY_NSCameraUsageDescription = Use the camera to preview beauty processing on this device\\.|INFOPLIST_KEY_NSPhotoLibraryUsageDescription = Select photos to preview beauty processing on this device\\." BeautyDemo/BeautyDemo.xcodeproj/project.pbxproj`
- `! rg -n "UIImage" BeautySDK/Sources BeautyDemo/BeautyDemo 2>/dev/null`
- `! rg -n "import Beauty(Core|Render|Detection|Effects|Resources)" BeautyDemo BeautyDemo/BeautyDemoTests 2>/dev/null`
- `! rg -n "URLSession|http://|https://|upload|/private/var|NSError|AVError" BeautyDemo/BeautyDemo/Camera BeautyDemo/BeautyDemo/Editor BeautyDemo/BeautyDemo/Support 2>/dev/null`
</verification>

<success_criteria>
Plan 03-04 succeeds when purpose strings, local-first privacy behavior, facade-only imports, no realtime `UIImage`, bounded pipeline tests, Photo/compare tests, and root-doc evidence all pass automated verification or have exact environment failures recorded.
</success_criteria>

<output>
Create `.planning/phases/03-realtime-and-still-input-slice/03-04-SUMMARY.md` when done.
</output>
