---
phase: 50-independent-eyebrow-geometry-and-pipeline-integration
plan: "03"
type: execute
wave: 3
depends_on: ["50-02"]
files_modified:
  - BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectDomain.swift
  - BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift
  - BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
autonomous: true
requirements: [GEOM-01, GEOM-02, GEOM-03, GEOM-04, GEOM-05, GEOM-06, GEOM-07, PIPE-01, PIPE-02]
must_haves:
  truths:
    - "D-09: normalized public eyebrow values enter seven exact effective fields, receive explicit provisional 0.25 caps once, and trigger face geometry without changing Phase 49 storage semantics."
    - "D-08/D-12: fresh eligible work stays full, reused eligible eyebrow work receives exact 0.5 once in the resolver, stale/no-face work becomes zero, and each provider-empty field is removed before conflict accounting."
    - "D-10: the resolver's retained baseline inserts EyebrowWarpProvider in Face→Chin→Eye→Eyebrow→Nose→Mouth order, uses exactly one `0..<44` monotone loop, and never carries scaled values forward or revives removals."
    - "D-02/D-15: final eyebrow activity is represented only by aggregate `.eyebrows`, fixed `eyebrow_inputs_missing`, `beauty.effects.skippedEyebrowDomains`, and total point count without side/coordinate/provider details."
  artifacts:
    - path: BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectDomain.swift
      provides: "Dedicated aggregate `.eyebrows` effect domain"
    - path: BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift
      provides: "Seven explicitly provisional exact 0.25 eyebrow caps"
    - path: BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
      provides: "Geometry trigger, cap/freshness lifecycle, provider preflight, 44-pass convergence, and final aggregate accounting"
    - path: BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
      provides: "Zero neutrality, cap, field-local eligibility, freshness, domain, warning, and metric tests"
  key_links:
    - from: BeautyParameters eyebrow fields
      to: BeautyEffectiveStrengths eyebrow fields
      via: "normalized input and exactly-once BeautySafetyCaps application"
      pattern: "BeautySafetyCaps.eyebrow"
    - from: BeautyEffectResolver
      to: EyebrowWarpProvider
      via: "preflight and retained-baseline final emission evaluation"
      pattern: "EyebrowWarpProvider"
    - from: final eyebrow emissions
      to: activeDomains, skippedDomains, warnings, and metrics
      via: "aggregate final point ownership only"
      pattern: "eyebrows|skippedEyebrowDomains"
---

<objective>
Route all seven named eyebrow emissions through effective strengths, freshness, resolver eligibility, and aggregate facade-plan evidence.

Purpose: Replace Phase 49 runtime inertness with a single provider-authoritative resolver lifecycle while preserving local degradation and privacy.
Output: Dedicated eyebrow domain, provisional caps, exact 44-pass provider convergence insertion, and focused resolver evidence.
</objective>

## Artifacts this phase produces

- New public enum case `BeautyEffectDomain.eyebrows`, carrying only an aggregate domain label.
- New internal constants `BeautySafetyCaps.eyebrowYPosition`, `.eyebrowThickness`, `.eyebrowLength`, `.eyebrowSpacing`, `.eyebrowHeadSpacing`, `.eyebrowTilt`, and `.eyebrowPeakDefinition`, each exactly `0.25` and documented provisional.
- Resolver helpers/predicates for requested eyebrow work, zeroing, exact reuse scaling, stale/no-face handling, provider preflight, final activity, fixed warning, skipped metric, and `0..<44` monotone convergence.
- Replacement of `testBROW02SevenNonzeroEyebrowFieldsRemainRuntimeInert` with explicit-zero neutrality plus positive routing/cap/provider/domain evidence.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<read_first>
AGENTS.md; PLANS.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-CONTEXT.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-RESEARCH.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-PATTERNS.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-VALIDATION.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-02-SUMMARY.md; BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
</read_first>

<context>
@.planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-CONTEXT.md
@.planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-RESEARCH.md
@.planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-PATTERNS.md
@.planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-02-SUMMARY.md
@BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift

<interfaces>
Plan 02 provides seven `BeautyEffectiveStrengths` fields and `EyebrowWarpProvider.fieldEmissions(...).sanitizing(_:)`. `BeautyEffectResolver` currently owns `requiresFaceGeometry`, cap construction, reusable/stale/no-face helpers, requested/final domain accounting, provider preflight, and a retained-baseline loop that is exactly `0..<37`. Existing non-eye geometry applies exact reuse factor `0.5`; eye geometry has a different skip policy. Phase 50 explicitly adopts the non-eye exact-0.5 policy for eyebrow work.
</interfaces>
</context>

## Decision trace and adopted research assumptions

- D-08 through D-13: local sanitization, seven caps/effective fields, exact 44 loop, one scale/dispatch evidence, conflict symmetry, and exact tests.
- D-14 through D-18: provisional-only, no expanded surfaces, downstream evidence/promotion fences, discretion, and compiled-routing scope.
- A1 adopted: add aggregate public `.eyebrows`, which exposes no geometry and avoids semantic aliasing.
- A2 adopted: use seven provisional exact `0.25` caps; Plan 04 therefore expects eyebrow subtotal `1.75` and combined total `13.45`.
- A6 adopted: reused eligible eyebrow geometry scales by exact `0.5` once in the resolver; the provider remains stateless.

## Plan-local source coverage

| Source | Coverage |
| --- | --- |
| GOAL | Connects provider geometry to the existing resolver/public-plan path. |
| REQ | GEOM fields retain exact semantics through caps; PIPE-01 gets routing/degradation/accounting; PIPE-02 gets the 44-loop insertion. |
| RESEARCH | Implements A1/A2/A6, stable provider order, final-emission authority, and aggregate diagnostics. |
| CONTEXT | D-08..18 are implemented or explicitly fenced. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 50-03-01: Add aggregate eyebrow domain and provisional effective-strength routing</name>
  <files>BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectDomain.swift, BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift, BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift</files>
  <read_first>AGENTS.md; PLANS.md; ARCHITECTURE.md; DESIGN.md; SECURITY.md; RELIABILITY.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-CONTEXT.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-RESEARCH.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-PATTERNS.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-02-SUMMARY.md; BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectDomain.swift; BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift; BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift; BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift</read_first>
  <behavior>
    - Explicit zero eyebrow values leave requiresFaceGeometry and the complete shipped plan equal to the Phase 49 zero baseline.
    - Any one normalized nonzero eyebrow value requires face geometry and maps to its same-named effective field with exact provisional cap `±0.25` for signed fields and `0.25` for positive-only peak.
    - Seven isolated eligible requests activate `.eyebrows`, produce nonempty aggregate geometry evidence, and do not activate `.eyes` or `.faceShape` merely for brow work.
  </behavior>
  <action>Per D-02/D-09/D-14/D-17, add `.eyebrows` beside `.eyes` in `BeautyEffectDomain` and seven explicitly provisional `0.25` cap constants. Replace the Phase 49 runtime-inert test with explicit-zero plan equality and table-driven positive routing tests. Extend `requiresFaceGeometry`, normalized-to-capped strength construction, cap count/warning behavior, requested-value predicates, and helper arrays for all seven exact names. Apply signed caps without sign loss to the first six and positive-only semantics to peak; do not alter `BeautyParameters`, presets, or normalized storage. At this task boundary, verify isolated fresh eligible support only; lifecycle/provider-empty behavior lands in Task 02.</action>
  <acceptance_criteria>Zero remains neutral; each nonzero field triggers geometry and reaches only its same-named effective field at exact provisional cap; `.eyebrows` is independent; cappedCount increments exactly per finite overflow and no storage/preset behavior changes.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --disable-sandbox --jobs 1 --filter BeautyEffectsTests.BeautyEffectResolverTests</automated></verify>
  <done>All seven public intents enter explicit provisional effective routing without aliases or compatibility drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 50-03-02: Enforce freshness, provider preflight, monotone resolver convergence, and aggregate accounting</name>
  <files>BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift</files>
  <read_first>AGENTS.md; PLANS.md; ARCHITECTURE.md; DESIGN.md; SECURITY.md; RELIABILITY.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-CONTEXT.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-RESEARCH.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-PATTERNS.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-02-SUMMARY.md; BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift; BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift</read_first>
  <behavior>
    - Fresh eligible eyebrow strengths retain the capped value; reused eligible strengths equal fresh × 0.5 before conflict; stale/no-face strengths are zero with no stale carryover.
    - Preflight named emissions remove only fields whose own work is empty; left/right, pair, chord, and apex eligibility match the provider contract.
    - The retained loop is exactly `0..<44`, evaluates providers at conflict-scaled values, sanitizes the unscaled retained baseline in Face→Chin→Eye→Eyebrow→Nose→Mouth order, never re-adds fields, and returns one final resolution.
    - Final `.eyebrows` activity, `eyebrow_inputs_missing`, `beauty.effects.skippedEyebrowDomains`, and geometry point count derive from final emissions and disclose no side or coordinates.
  </behavior>
  <action>Per D-05 through D-13/D-15, add eyebrow requested/reusable predicates, exact-once 0.5 reuse scaling, no-face/stale zero helper, provider preflight, and requested-versus-final bookkeeping. Instantiate one `EyebrowWarpProvider`; insert its sanitization after eye and before nose both before the first conflict total and in every retained-baseline pass; change the exact bound from 37 to 44. Providers must evaluate `resolution.strengths` but sanitize `retainedBaseline`; do not carry scaled strengths forward or reconstruct removed names. Compute final emissions once; activate `.eyebrows` and add their exact point count when any final work remains, otherwise skip the requested domain with fixed `eyebrow_inputs_missing` and aggregate `beauty.effects.skippedEyebrowDomains = 1`. Include `.eyebrows` in no-face aggregate skipped-domain counting. Never emit field/side/support-derived warning or metric detail.</action>
  <acceptance_criteria>Fresh/reused/stale/no-face/provider-empty rows pass; only dependent names disappear; exact `0..<44` and provider order are source-locked; final active/skipped/warning/metric evidence matches final emissions; unrelated safe domains remain equal.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --disable-sandbox --jobs 1 --filter BeautyEffectsTests.BeautyEffectResolverTests &amp;&amp; rg -n "0\.\.<44|EyebrowWarpProvider|skippedEyebrowDomains" BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift &amp;&amp; git diff --check</automated></verify>
  <done>The resolver owns one privacy-safe, field-local eyebrow lifecycle and one monotone 44-name retained baseline.</done>
</task>

</tasks>

<threat_model>
<blocking_rule>ASVS L1 with block-on-high: any unresolved HIGH finding, double freshness/cap scaling, removed-field re-entry, raw diagnostic disclosure, or focused-test failure blocks completion; HIGH threats may not be accepted or transferred.</blocking_rule>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Public normalized scalar → capped effective strength | Wrong sign, nonfinite, overflow, or duplicate cap application must not reach provider work. |
| Named provider output → resolver evidence | Empty work cannot count as active or influence unrelated conflict scale. |
| Private support state → warnings/metrics/domain | Only fixed aggregate labels and total counts may escape. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
| --- | --- | ---: | --- | --- | --- |
| T-50-07 | Tampering | high | cap/freshness lifecycle | mitigate | Exact per-field cap tests, exact reuse 0.5 tests, stateless provider, and stale/no-face zero tests. |
| T-50-08 | Tampering / DoS | high | retained-baseline convergence | mitigate | Exact 44 bound, monotone mask, stable provider order, no re-entry/double scale, and focused source/test gates. |
| T-50-09 | Information Disclosure | high | resolver diagnostics | mitigate | Fixed warning/domain/aggregate metric only; no side, point, apex, axis, displacement, or provider detail. |
| T-50-SC | Tampering | medium | package supply chain | mitigate | No installation or manifest/resource change is planned. |
</threat_model>

<verification>
- Resolver focused suite passes zero, caps, seven routes, freshness, field-local empty, domain, warning, metric, and unrelated-domain assertions.
- Source contains exactly one 44-bound retained loop and one eyebrow insertion in the stable provider order.
- Provider suite remains green; checker self-tests remain green; diff hygiene passes.
</verification>

<success_criteria>
- PIPE-01 has complete resolver/facade-plan routing, local eligibility, and aggregate final evidence.
- PIPE-02 has one exact 44-entry monotone resolver loop ready for exact conflict arithmetic in Plan 04.
- Phase 49 privacy/non-substitution and Phase 51/52 nonclaims remain intact.
</success_criteria>

<output>
Create `.planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-03-SUMMARY.md` when done.
</output>
