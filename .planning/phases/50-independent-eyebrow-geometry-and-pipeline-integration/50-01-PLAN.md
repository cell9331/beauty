---
phase: 50-independent-eyebrow-geometry-and-pipeline-integration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/check_eyebrow_geometry_boundaries.py
  - BeautySDK/Tests/BeautyEffectsTests/EyebrowWarpProviderTests.swift
  - BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift
autonomous: true
requirements: [GEOM-01, GEOM-02, GEOM-03, GEOM-04, GEOM-05, GEOM-06, GEOM-07, PIPE-01, PIPE-02]
must_haves:
  truths:
    - "D-01 through D-08: RED named-emission contracts cover seven distinct canonical-trace transforms, their exact per-side/pair/chord/apex prerequisites, and field-local empty-output behavior before production implementation."
    - "D-03/D-04: fixtures consume sealed Phase 49 inner-to-outer traces without sorting, remapping, closing, synthesis, persistence, or mutation of shipped face, eye, nose, and mouth arrays."
    - "D-10 through D-16: a fail-closed checker makes exact 44-field convergence, one dispatch, aggregate-only privacy, package stability, and Phase 51/52 scope fences executable."
    - "Wave 0 leaves EyebrowWarpProviderTests deliberately RED for the absent named provider while existing suites continue to compile through deterministic request-local testing fixtures."
  artifacts:
    - path: .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/check_eyebrow_geometry_boundaries.py
      provides: "Adversarial self-test, pre-implementation, and live privacy/scope/convergence gate"
    - path: BeautySDK/Tests/BeautyEffectsTests/EyebrowWarpProviderTests.swift
      provides: "Asymmetric paired, partial, degenerate, mirrored, canonical-order, provider-empty, and sibling-stability RED contracts"
    - path: BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift
      provides: "Deterministic valid, left-only, right-only, missing, and malformed observed-eyebrow facade fixtures"
  key_links:
    - from: EyebrowWarpProviderTests.swift
      to: BeautyEyebrowSemanticSupport
      via: "direct named-array fixtures over Phase 49 canonical traces"
      pattern: "fieldEmissions|observedEyebrowSupport"
    - from: BeautyEngineTestingSupport.swift
      to: VisionDetectionObservation
      via: "immutable per-request raw support attached to the existing selected-face fixture"
      pattern: "SDKTestingFaceDetectionFixture"
    - from: check_eyebrow_geometry_boundaries.py
      to: BeautySDK, BeautyDemo, renderer/gallery inventories, product ledger, and repository diff
      via: "classified subprocess/path/hash checks whose missing or unclassified state fails"
      pattern: "--self-test|--pre-implementation"
  flagged_assumptions:
    - requirement_id: GEOM-01
      probe_category: empty_geometry
      status: unresolved
      acceptance_predicate: "An empty semantic support emits no eyebrowYPosition work, zeros only eyebrowYPosition during sanitization, and leaves every unrelated retained field unchanged."
    - requirement_id: GEOM-01
      probe_category: single_side
      status: unresolved
      acceptance_predicate: "Exactly one valid canonical side emits whole-trace signed vertical work for that side only; a missing sibling does not erase it."
    - requirement_id: GEOM-02
      probe_category: single_point
      status: unresolved
      acceptance_predicate: "A trace with no valid adjacent tangent emits no thickness work for that side, without manufacturing a normal or suppressing a valid sibling."
    - requirement_id: GEOM-02
      probe_category: degenerate_adjacency
      status: unresolved
      acceptance_predicate: "Coincident adjacent samples make only the affected thickness contribution empty; finite balanced normal-strip work on other eligible samples/sides remains canonical and bounded."
    - requirement_id: GEOM-03
      probe_category: degenerate_chord
      status: unresolved
      acceptance_predicate: "A zero-length inner-to-outer chord emits no length work for that side; the provider never substitutes a sorted endpoint or eye-derived axis."
    - requirement_id: GEOM-04
      probe_category: incomplete_pair
      status: unresolved
      acceptance_predicate: "Single-side, duplicate-side, or zero pair-axis support emits no whole-brow spacing while every independently eligible per-side field continues."
    - requirement_id: GEOM-05
      probe_category: inner_neighborhood
      status: unresolved
      acceptance_predicate: "A valid canonical inner endpoint uses only the first two inner-to-outer samples with weights 1 and 0.5; insufficient or degenerate local geometry fails that side only."
    - requirement_id: GEOM-06
      probe_category: degenerate_chord
      status: unresolved
      acceptance_predicate: "A nonfinite or epsilon-degenerate canonical chord emits no tilt for that side while a valid sibling retains sign-preserving local rotation."
    - requirement_id: GEOM-07
      probe_category: missing_apex
      status: unresolved
      acceptance_predicate: "Nil, endpoint, ambiguous, or chord-degenerate apex evidence emits no peak work for that side and never triggers midpoint or extremum fallback."
    - requirement_id: GEOM-01
      probe_category: canonical_order
      status: unresolved
      acceptance_predicate: "The provider consumes the Phase 49 inner-to-outer order byte-for-byte and never sorts, reverses, remaps, closes, or reconstructs the trace."
    - requirement_id: GEOM-06
      probe_category: mirrored_order
      status: unresolved
      acceptance_predicate: "Canonical chord-derived tilt sign remains stable for mirrored/oriented fixtures without changing stored point order or side identity."
    - requirement_id: PIPE-01
      probe_category: request_isolation
      status: unresolved
      acceptance_predicate: "Repeated valid/missing/valid and alternating left/right requests derive output only from immutable request-local support, with no stale provider state."
    - requirement_id: PIPE-01
      probe_category: concurrency_isolation
      status: unresolved
      acceptance_predicate: "Parallel deterministic requests retain their own eyebrow support, freshness, strengths, warnings, and aggregate counts without cross-request mutation or caching."
    - requirement_id: PIPE-02
      probe_category: adjacency_conflict
      status: unresolved
      acceptance_predicate: "Overlapping local eyebrow control points remain ordinary named provider work under the one shared geometry conflict resolver; no adjacency-specific scale or second pass is introduced."
    - requirement_id: PIPE-02
      probe_category: monotone_removal
      status: unresolved
      acceptance_predicate: "One retained-baseline loop bounded at 44 may remove fields only; removed work never re-enters and scaled strengths never become the next unscaled baseline."
    - requirement_id: PIPE-01
      probe_category: provider_empty
      status: unresolved
      acceptance_predicate: "A requested field whose final named array is empty contributes zero times to strengths, totals, active domains, warnings, metrics, point counts, and dispatch."
    - requirement_id: PIPE-01
      probe_category: unrelated_continuation
      status: unresolved
      acceptance_predicate: "Missing, malformed, stale, or ineligible eyebrow support does not weaken or suppress independently safe skin, color, filter, face, eye, nose, mouth, or eligible eyebrow-sibling work."
  prohibitions:
    - statement: "Raw or derived eyebrow support MUST NOT become identity, recognition, authentication, or biometric-profiling data."
      status: unverified
    - statement: "Eye contours, synthetic traces, generated points, or other facial geometry MUST NOT be presented or consumed as observed eyebrow support."
      status: unverified
    - statement: "Phase 51 output/gallery evidence, Phase 52 final-cap/promotion behavior, or v1.14-v1.16/UI/device/commercial/release scope MUST NOT be silently enabled through a provider, model, resource, dependency, network path, facade, or status edit."
      status: unverified
---

<objective>
Create the Phase 50 Wave 0 contracts and fail-closed boundary before eyebrow production geometry lands.

Purpose: Turn all seventeen specless edge rows, provider-source ownership, privacy, exact convergence, and downstream scope fences into executable inputs rather than implicit assumptions.
Output: One phase-local checker, one deliberately RED provider suite, and deterministic facade fixtures for later routing tests.
</objective>

## Artifacts this phase produces

- New file `.planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/check_eyebrow_geometry_boundaries.py` with `BoundaryCheckFailure`, checked subprocess/path helpers, `self_test`, `pre_implementation_checks`, `live_checks`, and `main`.
- New file `BeautySDK/Tests/BeautyEffectsTests/EyebrowWarpProviderTests.swift` with asymmetric paired/left/right support builders and direct RED tests `testGEOM01VerticalPositionNamedEmission`, `testGEOM02ThicknessNamedEmission`, `testGEOM03LengthNamedEmission`, `testGEOM04WholeSpacingNamedEmission`, `testGEOM05HeadSpacingNamedEmission`, `testGEOM06TiltNamedEmission`, and `testGEOM07PeakNamedEmission`.
- New SPI testing-fixture cases in `SDKTestingFaceDetectionFixture` for valid paired, left-only, right-only, missing, and malformed observed eyebrow support; no raw support is added to any result type.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<read_first>
AGENTS.md; PLANS.md; SECURITY.md; .planning/ROADMAP.md; .planning/REQUIREMENTS.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-CONTEXT.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-RESEARCH.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-PATTERNS.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-VALIDATION.md; .planning/phases/49-public-contract-and-observed-eyebrow-support/49-VERIFICATION.md
</read_first>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-CONTEXT.md
@.planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-RESEARCH.md
@.planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-PATTERNS.md
@.planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-VALIDATION.md
@.planning/phases/49-public-contract-and-observed-eyebrow-support/49-VERIFICATION.md

<interfaces>
`BeautyEyebrowSemanticTrace(side:points:innerEndpoint:outerEndpoint:center:apexIndex:)` and `BeautyEyebrowSemanticSupport(left:right:)` already exist in `WarpControlPoint.swift`; `pairedEligible` requires two distinct non-nil sides. `FaceGeometry.observedEyebrowSupport` is immutable and default-nil. The future provider must mirror `EyeWarpFieldEmissions` named-array/sanitizing structure but must not copy its legacy fallback. `SDKTestingFaceDetectionFixture` already supplies one locked/sequential `VisionDetectionObservation` through the existing public-facade testing route.
</interfaces>
</context>

## Context decision numbering

- D-01: exactly seven field-owned transforms with the semantics stated in CONTEXT.
- D-02: six signed fields and one positive-only peak remain distinct and never alias eye/face values.
- D-03: providers consume canonical traces directly without sort/remap/close/synthesis/persistence.
- D-04: work stays brow-local and shipped face/eye/nose/mouth arrays remain byte-equal under eyebrow-only input.
- D-05: vertical, thickness, and length are independently eligible per side.
- D-06: whole spacing is pair-only; head spacing is independently per side.
- D-07: tilt requires a nondegenerate chord; peak additionally requires the stored unique interior apex.
- D-08: provider-empty/missing/malformed/no-face/stale work fails field-locally with no fallback while safe siblings continue.
- D-09: seven effective strengths/emissions apply provisional caps exactly once.
- D-10: exactly 44 retained names use one monotone removal loop and one final scale across all evidence.
- D-11: eyebrow points enter the existing unified warp exactly once with no special route or pass.
- D-12: conflict symmetry and exact-once reuse scaling preserve unrelated domains.
- D-13: focused/full automated evidence locks exact names, totals, counts, scale, removals, and dispatch.
- D-14: Phase 50 caps are internal and provisional; Phase 52 owns final calibration.
- D-15: no dependency/model/resource/UI/Demo/network/persistence/commercial/public-SPI raw-geometry expansion.
- D-16: Phase 51 owns decoded output/gallery; Phase 52 owns exhaustive safety and promotion.
- D-17: helper names, decomposition, provisional constants, locality functions, and test partitioning follow established patterns at the agent's discretion.
- D-18: this phase claims compiled provider/routing behavior only.

## Specless probe audit

The supplied probe returned exactly seventeen unresolved rows. The frontmatter preserves exactly seventeen `flagged_assumptions`, resolves their acceptance predicates only from CONTEXT/RESEARCH, and does not turn any row into a silent dismissal. The three recalled repository-specific values/safety prohibitions remain descriptor-less `unverified` inputs; checker evidence may detect concrete manifestations but must not invent `check_*` fields or auto-green them.

## Plan-local source coverage

| Source | Coverage |
| --- | --- |
| GOAL | Establishes executable safeguards and fixtures for the seven-provider/44-field route. |
| REQ | GEOM-01..07 receive direct RED named-array predicates; PIPE-01/02 receive privacy, scope, convergence, and dispatch gates. |
| RESEARCH | Covers Wave 0 provider suite, facade fixture gap, Phase 50 checker, provisional-only wording, and all seven assumptions A1-A7 as flagged downstream choices. |
| CONTEXT | D-01..18 are represented by tests, checker rules, explicit nonclaims, or downstream task contracts. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 50-01-01: Build the fail-closed Phase 50 boundary checker</name>
  <files>.planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/check_eyebrow_geometry_boundaries.py</files>
  <read_first>AGENTS.md; PLANS.md; SECURITY.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-CONTEXT.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-RESEARCH.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-VALIDATION.md; .planning/phases/49-public-contract-and-observed-eyebrow-support/check_eyebrow_support_boundaries.py; BeautySDK/Package.swift; docs/meitu-function-blueprint/SHAPE_FEATURE_LEDGER.md</read_first>
  <behavior>
    - `--self-test` mutates an isolated corpus for every live rule; missing paths, repository escapes, subprocess errors, unexpected `rg` statuses, unclassified matches, and absent required markers fail.
    - `--pre-implementation` passes only while the Phase 49 support/privacy contract and package/fixture boundaries are intact and no production eyebrow provider exists.
    - Live mode requires seven named emissions, one `0..<44` retained loop, stable Face→Chin→Eye→Eyebrow→Nose→Mouth order, dedicated aggregate eyebrow routing, and no forbidden scope drift.
  </behavior>
  <action>Per D-03/D-08/D-10/D-11/D-14/D-15/D-16, model the Phase 49 checker without modifying it. Pin `BeautySDK/Package.swift` to git-object hash `6f03b078816ad1f7a426e3f70d4f57503f3152e9` and the Phase 49 checker to `7a8716c810d1b81119d14338cb9ce037c8ebb40b`. Implement checked subprocess/path/hash handling and adversarial self-tests. Reject public/SPI raw support or control-point exposure, Codable/persistence/cache/shared state, coordinate-bearing diagnostics/reflection, eye/synthetic substitution, package target/dependency/resource/model/network drift, Demo/SwiftUI or example-renderer/gallery changes, tracked/staged/non-ignored generated images, product-row/branch promotion, final-cap wording, and v1.14-v1.16 claims. In live mode require exact seven field names, provider-local sanitization, exact `0..<44`, one eyebrow provider in unified dispatch, aggregate-only warning/metric markers, and unchanged historical checker/package hashes. Keep the three frontmatter prohibitions flagged-only: never fabricate descriptor or `check_*` fields.</action>
  <acceptance_criteria>The checker exposes only self-test, pre-implementation, and live modes; each rule family has a mutation that fails; current pre-implementation mode passes; live mode cannot pass before production implementation; tool/path/hash errors block.</acceptance_criteria>
  <verify><automated>python3 .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/check_eyebrow_geometry_boundaries.py --self-test &amp;&amp; python3 .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/check_eyebrow_geometry_boundaries.py --pre-implementation &amp;&amp; git diff --check</automated></verify>
  <done>Phase 50 privacy, exact-accounting, and scope claims have an adversarial fail-closed gate before production edits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 50-01-02: Write seventeen-edge named provider contracts and leave them RED</name>
  <files>BeautySDK/Tests/BeautyEffectsTests/EyebrowWarpProviderTests.swift</files>
  <read_first>AGENTS.md; PLANS.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-CONTEXT.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-RESEARCH.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-PATTERNS.md; BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift; BeautySDK/Sources/BeautyEffects/Warp/EyeWarpProvider.swift; BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift</read_first>
  <behavior>
    - Direct tests name all seven emission arrays and assert sources, targets, sign, locality, radius/falloff, finite/unit bounds, and field-local sanitization rather than aggregate point counts.
    - Asymmetric paired, left-only, right-only, empty, single/degenerate adjacency, pair-invalid, nil-apex, degenerate-chord, mirrored, repeated, and concurrent fixtures encode all seventeen frontmatter predicates.
    - Every eyebrow-only row captures and compares all shipped face/chin, eye, nose, and mouth arrays byte-for-byte.
  </behavior>
  <action>Per D-01 through D-08/D-13/D-17, create deterministic `FaceGeometry` fixture builders using Phase 49 semantic trace initializers directly. Add the seven exact test names listed in Artifacts for whole-trace signed Y translation; balanced normal-strip thickness; outer-only length; pair-only equal/opposite spacing; inner-only head spacing; canonical-chord signed tilt; and stored-apex-only peak, plus field-local `sanitizing`, stable concatenation order, and non-eyebrow provider equality. Include every one of the seventeen flagged predicates, including immutable request/concurrency fixture data and monotone-resolver expectations as direct test vocabulary for later plans. Do not add production provider/types or weaken assertions. Run the focused suite and record the missing provider/named-emission surface as the expected RED result.</action>
  <acceptance_criteria>The new file contains seven direct named-array test families plus all seventeen edge predicates; no raw/eye/synthetic fallback fixture exists; the focused test is demonstrably RED because `EyebrowWarpProvider`/`EyebrowWarpFieldEmissions` are absent.</acceptance_criteria>
  <verify><automated>/bin/zsh -lc 'set -u; test -f BeautySDK/Tests/BeautyEffectsTests/EyebrowWarpProviderTests.swift &amp;&amp; rg -q "testGEOM01" BeautySDK/Tests/BeautyEffectsTests/EyebrowWarpProviderTests.swift &amp;&amp; ! test -f BeautySDK/Sources/BeautyEffects/Warp/EyebrowWarpProvider.swift || exit 1; log=$(mktemp); trap "rm -f $log" EXIT; swift test --package-path BeautySDK --disable-sandbox --jobs 1 --filter BeautyEffectsTests.EyebrowWarpProviderTests &gt;$log 2&gt;&amp;1; status=$?; test $status -ne 0 || { cat $log; exit 1; }; rg -q "error:.*(EyebrowWarpProvider|EyebrowWarpFieldEmissions|eyebrow(YPosition|Thickness|Length|Spacing|HeadSpacing|Tilt|PeakDefinition))" $log || { cat $log; exit 1; }; ! rg -qi "(failed to clone|could not resolve host|network.*(failed|unreachable)|timed out|no space left|permission denied|unable to load standard library|package resolution failed|invalid manifest|internal compiler error|signal 9|killed)" $log || { cat $log; exit 1; }; errors=$(mktemp); trap "rm -f $log $errors" EXIT; rg "error:" $log &gt;$errors || exit 1; ! rg -v "(EyebrowWarpProvider|EyebrowWarpFieldEmissions|eyebrow(YPosition|Thickness|Length|Spacing|HeadSpacing|Tilt|PeakDefinition)|cannot infer contextual base|type of expression is ambiguous without a type annotation)" $errors &gt;/dev/null || { cat $log; exit 1; }'</automated></verify>
  <done>All unresolved edges and seven semantic owners are RED-first and executable before the provider exists.</done>
</task>

<task type="auto">
  <name>Task 50-01-03: Add deterministic request-local eyebrow facade fixtures</name>
  <files>BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift</files>
  <read_first>AGENTS.md; PLANS.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-CONTEXT.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-PATTERNS.md; BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift; BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift; BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift</read_first>
  <action>Per D-03/D-05 through D-08/D-15, extend the SPI-only `SDKTestingFaceDetectionFixture` with deterministic paired-valid, left-only, right-only, missing, and malformed eyebrow cases. Use finite raw inner-to-outer arrays whose accepted path is the same existing `VisionDetectionObservation`/selected-face adapter route; do not construct semantic support inside BeautySDK, expose points in an SPI/public result, add shared mutable storage, or change the lock/sequential provider behavior. Preserve all current fixture cases and their observed face/eye/nose/mouth support byte-for-byte.</action>
  <acceptance_criteria>The BeautySDK target compiles despite the deliberately RED separate test target; every new fixture is request-local and distinguishable; existing facade source remains unchanged; no point/side/axis/apex data enters public result descriptions, warnings, or metrics.</acceptance_criteria>
  <verify><automated>swift build --package-path BeautySDK --disable-sandbox --target BeautySDK &amp;&amp; git diff --check</automated></verify>
  <done>Later facade tests can exercise honest valid/partial/malformed eyebrow support through the existing detector route without new public geometry.</done>
</task>

</tasks>

<threat_model>
<blocking_rule>ASVS L1 with block-on-high: any unresolved HIGH finding, unclassified checker state, source-provenance substitution, raw-coordinate disclosure, required-command failure, or scope-fence violation blocks this plan and every dependent plan; HIGH threats may not be accepted or transferred.</blocking_rule>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Repository/tool output → boundary verdict | Paths, hashes, subprocess statuses, and matches are untrusted and fail closed. |
| Phase 49 support → provider test contract | Biometric-adjacent canonical points authorize only matching eyebrow work. |
| SPI testing fixture → public facade result | Raw support stays inside one immutable request and only aggregate evidence may escape. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
| --- | --- | ---: | --- | --- | --- |
| T-50-01 | Spoofing / Tampering | high | provider RED fixtures | mitigate | Exact canonical sources, eye/synthetic negative cases, all seventeen edge predicates, and shipped-array equality. |
| T-50-02 | Information Disclosure | high | checker and testing support | mitigate | Aggregate-only diagnostics, no raw result carrier, classified scans, pinned hashes, and adversarial self-tests. |
| T-50-03 | Tampering / Elevation of Privilege | high | downstream phase/product scope | mitigate | Renderer/gallery/final-cap/promotion/future-row/Demo/network/dependency gates. |
| T-50-SC | Tampering | medium | package supply chain | mitigate | No install task; package hash is pinned and dependency/target/resource drift blocks. |
</threat_model>

<verification>
- Checker self-test and pre-implementation modes pass; live mode remains unavailable before implementation.
- Provider suite fails for the absent planned named provider, not for fixture syntax or unrelated compilation.
- Existing facade tests pass with the new unused deterministic fixtures.
- Only the three declared files change and `git diff --check` passes.
</verification>

<success_criteria>
- Exactly seventeen unresolved probe rows and three unverified prohibitions remain visible and executable without silent resolution.
- Every GEOM requirement has a direct RED named-emission contract and PIPE requirements have fail-closed convergence/dispatch/privacy gates.
- Phase 51/52 and UI/device/commercial/release work remains mechanically absent.
</success_criteria>

<output>
Create `.planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-01-SUMMARY.md` when done.
</output>
