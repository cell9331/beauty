---
phase: 50-independent-eyebrow-geometry-and-pipeline-integration
plan: "02"
type: execute
wave: 2
depends_on: ["50-01"]
files_modified:
  - BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift
  - BeautySDK/Sources/BeautyEffects/Warp/EyebrowWarpProvider.swift
  - BeautySDK/Tests/BeautyEffectsTests/EyebrowWarpProviderTests.swift
autonomous: true
requirements: [GEOM-01, GEOM-02, GEOM-03, GEOM-04, GEOM-05, GEOM-06, GEOM-07, PIPE-01]
must_haves:
  truths:
    - "D-01/D-02/D-09: seven exact BeautyEffectiveStrengths names feed seven immutable EyebrowWarpFieldEmissions arrays with stable concatenation and independent sanitization."
    - "D-03 through D-07: each provider helper consumes canonical Phase 49 support directly and implements its locked per-side, pair, chord, or apex prerequisite without sorting, fallback, or semantic aliasing."
    - "D-04/D-08: invalid or provider-empty work removes only its own named contribution; eyebrow-only requests leave every shipped provider array byte-for-byte unchanged."
    - "D-14/D-16/D-18: displacement/radius/falloff values are explicitly provisional provider mechanics, not decoded-output, naturalness, final-cap, safety-closeout, or promotion evidence."
  artifacts:
    - path: BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift
      provides: "Seven zero-default effective eyebrow strength fields"
    - path: BeautySDK/Sources/BeautyEffects/Warp/EyebrowWarpProvider.swift
      provides: "EyebrowWarpFieldEmissions and EyebrowWarpProvider with seven canonical-trace transforms"
    - path: BeautySDK/Tests/BeautyEffectsTests/EyebrowWarpProviderTests.swift
      provides: "Green source/direction/locality/eligibility/non-alias/provider-empty evidence"
  key_links:
    - from: BeautyEffectiveStrengths eyebrow fields
      to: EyebrowWarpFieldEmissions
      via: "fieldEmissions(face:strengths:) evaluates one named array per field"
      pattern: "eyebrowYPosition|eyebrowPeakDefinition"
    - from: EyebrowWarpProvider
      to: FaceGeometry.observedEyebrowSupport
      via: "direct read of sealed canonical semantic traces"
      pattern: "observedEyebrowSupport"
    - from: EyebrowWarpFieldEmissions
      to: BeautyEffectiveStrengths
      via: "sanitizing(_:) zeros only requested fields whose own arrays are empty"
      pattern: "sanitizing"
---

<objective>
Implement all seven eyebrow geometry semantics as one provider-owned named-emission subsystem.

Purpose: Give every public eyebrow intent distinct, testable canonical-trace work before resolver and conflict integration.
Output: Seven effective strength fields, seven named emission arrays, finite/local provider math, and a green focused provider suite.
</objective>

## Artifacts this phase produces

- New public-plan fields `BeautyEffectiveStrengths.eyebrowYPosition`, `.eyebrowThickness`, `.eyebrowLength`, `.eyebrowSpacing`, `.eyebrowHeadSpacing`, `.eyebrowTilt`, and `.eyebrowPeakDefinition`, each defaulting to zero.
- New file and symbols `EyebrowWarpFieldEmissions`, `EyebrowWarpProvider`, `fieldEmissions(face:strengths:)`, `makeControlPoints(face:strengths:)`, stable `.points`, and field-local `sanitizing(_:)`.
- Provider-private finite vector/axis/normal/rotation/apex helpers using the provisional Phase 50 constants and no new public/SPI geometry surface.
- Green tests for all seven GEOM requirements, all seventeen edge predicates introduced by Plan 01, stable canonical order, and unchanged shipped provider arrays.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<read_first>
AGENTS.md; PLANS.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-CONTEXT.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-RESEARCH.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-PATTERNS.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-VALIDATION.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-01-SUMMARY.md; BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift; BeautySDK/Sources/BeautyEffects/Warp/EyeWarpProvider.swift
</read_first>

<context>
@.planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-CONTEXT.md
@.planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-RESEARCH.md
@.planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-PATTERNS.md
@.planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-01-SUMMARY.md
@BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift
@BeautySDK/Sources/BeautyEffects/Warp/EyeWarpProvider.swift

<interfaces>
`BeautyEyebrowSemanticTrace` provides immutable `side`, canonical inner-to-outer `points`, `innerEndpoint`, `outerEndpoint`, `center`, and optional stored `apexIndex`. `BeautyEyebrowSemanticSupport` provides independently optional `left`/`right` traces plus `pairedEligible`. Follow `EyeWarpFieldEmissions` for immutable named arrays, stable `.points`, and `sanitizing(_:)`; follow `WarpControlPointResult(points:skipReason:)` for aggregate provider return. Unlike the eye provider, no compatibility fallback is authorized.
</interfaces>
</context>

## Decision trace

- D-01/D-02: exact seven transforms; six signed plus positive-only peak; no aliases.
- D-03/D-04: direct canonical support, brow-local only, shipped provider stability.
- D-05/D-06/D-07: per-side, pair-only, chord, and apex prerequisites.
- D-08/D-09: field-local empty removal and seven exact effective/emission names.
- D-14/D-16/D-18: provisional compiled behavior only; later output/final/promotion scopes stay absent.
- D-17: adopt RESEARCH geometry constants and helper decomposition as the permitted implementation choice.

## Plan-local source coverage

| Source | Coverage |
| --- | --- |
| GOAL | Creates the distinct provider-owned geometry required by the phase outcome. |
| REQ | GEOM-01..07 are implemented directly; PIPE-01 receives named emissions and field-local sanitization. |
| RESEARCH | Adopts A3-A5 geometry formulas/constants and the established eye-provider carrier without its fallback. |
| CONTEXT | D-01..09, D-14, D-16..18 are implemented or explicitly fenced. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 50-02-01: Implement named vertical, thickness, and length emissions</name>
  <files>BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift, BeautySDK/Sources/BeautyEffects/Warp/EyebrowWarpProvider.swift, BeautySDK/Tests/BeautyEffectsTests/EyebrowWarpProviderTests.swift</files>
  <read_first>AGENTS.md; PLANS.md; DESIGN.md; SECURITY.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-CONTEXT.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-RESEARCH.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-PATTERNS.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-01-SUMMARY.md; BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift; BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift; BeautySDK/Sources/BeautyEffects/Warp/EyeWarpProvider.swift; BeautySDK/Tests/BeautyEffectsTests/EyebrowWarpProviderTests.swift</read_first>
  <behavior>
    - `eyebrowYPosition` translates every canonical point of each valid side by signed image Y, with provisional max `0.025 * face.bounds.height`, radius `0.08 * face.bounds.width`, and falloff 2 at full capped strength.
    - `eyebrowThickness` keeps the trace centerline fixed and emits balanced plus/minus normal-strip samples using base half-strip `0.012 * height`, provisional change `0.006 * height`, radius `0.055 * width`, and falloff 2; invalid tangents fail locally.
    - `eyebrowLength` uses only canonical outer endpoint plus its immediate predecessor with weights 1/0.5 along the inner-to-outer chord, provisional max `0.025 * width`, radius `0.07 * width`, and falloff 2.
  </behavior>
  <action>Per D-01 through D-05/D-08/D-09/D-17, add the seven zero-default `Float` fields to `BeautyEffectiveStrengths` in eyebrow group order between eye and nose. Create `EyebrowWarpFieldEmissions` with all seven arrays, stable concatenation, and independent sanitization, plus `EyebrowWarpProvider.makeControlPoints` using fixed `eyebrow_inputs_missing` only when requested aggregate work is empty. Implement vertical, thickness, and length exactly as the behaviors above. Validate finite strength, source, target, axis/normal, displacement, radius, closed-unit bounds, and nonzero work before constructing points; return `[]` rather than clamp invalid work. Consume `observedEyebrowSupport` directly, iterate sides independently, retain canonical order, and never borrow face/eye geometry. Make the corresponding Plan 01 tests green while keeping the other four RED until Task 02.</action>
  <acceptance_criteria>Three named arrays implement both signs and per-side survival; thickness is balanced rather than translational; length never emits inner-head sources; sanitization is field-local/idempotent; shipped provider arrays remain equal.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --disable-sandbox --jobs 1 --filter BeautyEffectsTests.EyebrowWarpProviderTests.testGEOM01VerticalPositionNamedEmission &amp;&amp; swift test --package-path BeautySDK --disable-sandbox --jobs 1 --filter BeautyEffectsTests.EyebrowWarpProviderTests.testGEOM02ThicknessNamedEmission &amp;&amp; swift test --package-path BeautySDK --disable-sandbox --jobs 1 --filter BeautyEffectsTests.EyebrowWarpProviderTests.testGEOM03LengthNamedEmission</automated></verify>
  <done>Vertical, thickness, and length have distinct finite canonical-trace emissions under one named provider contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 50-02-02: Implement spacing, head spacing, tilt, and peak emissions</name>
  <files>BeautySDK/Sources/BeautyEffects/Warp/EyebrowWarpProvider.swift, BeautySDK/Tests/BeautyEffectsTests/EyebrowWarpProviderTests.swift</files>
  <read_first>AGENTS.md; PLANS.md; DESIGN.md; SECURITY.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-CONTEXT.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-RESEARCH.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-PATTERNS.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-01-SUMMARY.md; BeautySDK/Sources/BeautyEffects/Warp/EyebrowWarpProvider.swift; BeautySDK/Tests/BeautyEffectsTests/EyebrowWarpProviderTests.swift</read_first>
  <behavior>
    - `eyebrowSpacing` requires distinct paired support and moves both complete traces equal/opposite along the normalized center-to-center axis, max `0.025 * width`, radius `0.08 * width`, falloff 2.
    - `eyebrowHeadSpacing` survives per side and moves only canonical inner endpoint plus its next sample with weights 1/0.5, max `0.020 * width`, radius `0.06 * width`, falloff 2.
    - `eyebrowTilt` requires a nondegenerate canonical chord, rotates noncentral samples about stored center with max angle `0.12` radians and chord-derived sign, radius `0.075 * width`, falloff 2.
    - `eyebrowPeakDefinition` is positive-only, requires the stored unique interior apex, and moves apex-neighbor weights 0.5/1/0.5 along chord-to-apex normal, max `0.012 * height`, radius `0.055 * width`, falloff 2.
  </behavior>
  <action>Per D-01 through D-08/D-17, implement the remaining four named arrays exactly as the behavior contract. Whole spacing is the sole pair-gated field; head spacing remains per-side and its source set must differ from whole spacing. Derive tilt sign from the canonical inner-to-outer chord so both sides and mirrored/oriented fixtures preserve product direction; do not reorder points. Peak consumes only the stored interior `apexIndex`, rejects nil/endpoint/degenerate chord-to-apex evidence, and never recomputes an extremum or midpoint. Apply the same pre-construction finite/unit/radius/target validation as Task 01, keep the provider stateless, and make the full seven-field/17-edge focused suite green.</action>
  <acceptance_criteria>All seven arrays are non-aliasing on the asymmetric fixture; pair/apex/chord failures remove only dependent work; signed direction and mirrored ordering stay stable; no fallback/sort/remap/close/synthesis appears.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --disable-sandbox --jobs 1 --filter BeautyEffectsTests.EyebrowWarpProviderTests &amp;&amp; python3 .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/check_eyebrow_geometry_boundaries.py --self-test &amp;&amp; git diff --check</automated></verify>
  <done>All seven GEOM semantics are independently named, eligible, finite, local, and provider-owned.</done>
</task>

</tasks>

<threat_model>
<blocking_rule>ASVS L1 with block-on-high: any unresolved HIGH finding, nonfinite/out-of-bounds work, alternate support provenance, shipped-array mutation, or focused-test failure blocks completion; HIGH threats may not be accepted or transferred.</blocking_rule>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Canonical support → derived vectors | Points are biometric-adjacent untrusted numeric input; every derived axis/normal/target/radius must validate before emission. |
| Named emissions → later resolver | Empty arrays must remove only their matching strength and must not masquerade as active work. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
| --- | --- | ---: | --- | --- | --- |
| T-50-04 | Spoofing | high | EyebrowWarpProvider support source | mitigate | Direct `observedEyebrowSupport` consumption, no fallback/sort/remap/synthesis, and negative proxy tests. |
| T-50-05 | Tampering / DoS | high | provider vector helpers | mitigate | Pre-construction finite/nondegenerate/unit/bounds checks, Phase 49 16-point ceiling, O(n) helpers, and field-local empty returns. |
| T-50-06 | Information Disclosure | high | provider result | mitigate | Package-internal arrays and fixed aggregate skip reason only; no raw descriptions/metrics/public carrier. |
| T-50-SC | Tampering | medium | package supply chain | mitigate | No install, target, model, resource, or dependency change is planned. |
</threat_model>

<verification>
- `EyebrowWarpProviderTests` passes every named semantic, edge, and shipped-array regression.
- Checker self-tests remain green and package/diff boundaries remain intact.
- No resolver, conflict, pipeline, facade, renderer, gallery, product ledger, or owner document is changed in this plan.
</verification>

<success_criteria>
- GEOM-01..07 have distinct direct provider evidence with correct signed/positive-only behavior and prerequisites.
- PIPE-01 has seven named emissions and field-local provider-empty sanitization.
- Geometry constants remain provisional and Phase 51/52 claims remain absent.
</success_criteria>

<output>
Create `.planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-02-SUMMARY.md` when done.
</output>
