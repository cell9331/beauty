---
phase: 50-independent-eyebrow-geometry-and-pipeline-integration
plan: "05"
type: execute
wave: 4
depends_on: ["50-03", "50-04"]
files_modified:
  - BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
  - BeautySDK/Sources/BeautyEffects/Render/BeautyGeometryEffectPipeline.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautyGeometryEffectPipelineTests.swift
  - BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift
autonomous: true
requirements: [GEOM-01, GEOM-02, GEOM-03, GEOM-04, GEOM-05, GEOM-06, GEOM-07, PIPE-01, PIPE-02]
must_haves:
  truths:
    - "D-05 through D-08: missing left/right/both, invalid pair, degenerate chord, nil apex, provider-empty, no-face, stale, reused, repeated, and concurrent cases remove only dependent eyebrow work while eligible siblings and unrelated safe domains continue."
    - "D-10/D-12/D-13: one exact 44-field provider-eligible retained baseline yields identical final strengths, totals, count, scale, weakened count, final named emissions, warnings, metrics, and dispatch arrays."
    - "D-11: `BeautyGeometryEffectPipeline` guards `.eyebrows` and concatenates one EyebrowWarpProvider exactly once between eye and nose; no renderer, pre-warp, second scale, or public special route exists."
    - "D-09/D-15: seven isolated public requests use the existing `BeautyEngine.processResult` path, invoke detection once, preserve image extent, report aggregate nonzero geometry evidence, and expose no raw support."
  artifacts:
    - path: BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift
      provides: "Exact 44-field final-mask/provider/metric/dispatch agreement and no-reentry evidence"
    - path: BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
      provides: "Field-local side/pair/chord/apex/provider-empty/freshness/sibling continuation matrix"
    - path: BeautySDK/Sources/BeautyEffects/Render/BeautyGeometryEffectPipeline.swift
      provides: "Exactly-once eyebrow insertion into the existing unified warp control-point list"
    - path: BeautySDK/Tests/BeautyEffectsTests/BeautyGeometryEffectPipelineTests.swift
      provides: "Eyebrow-only and all-provider stable-order dispatch evidence"
    - path: BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift
      provides: "Final deterministic raw eyebrow fixture wiring through the existing detector seam"
    - path: BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift
      provides: "Seven isolated aggregate/redacted public-facade routing tests"
  key_links:
    - from: final named provider emissions
      to: BeautyGeometryEffectPipeline.controlPoints
      via: "exact array equality in Face→Chin→Eye→Eyebrow→Nose→Mouth order"
      pattern: "EyebrowWarpProvider"
    - from: BeautyEffectPlan.effectiveStrengths
      to: metrics and unified control points
      via: "one final provider-eligible retained mask and point-count equality"
      pattern: "geometryPointCount"
    - from: BeautyEngine.processResult
      to: observed eyebrow support
      via: "one existing geometry-triggered detector request and internal adapter route"
      pattern: "SDKTestingFaceDetectionFixture"
---

<objective>
Prove combined monotone convergence, field-local degradation, exactly-once unified dispatch, and redacted public-facade routing end to end.

Purpose: Make all resolver, conflict, provider, pipeline, and facade views agree on the same final seven-field eyebrow work without broadening output evidence or public geometry.
Output: Exact 44-field combined tests, representative degradation matrix, one pipeline insertion, and seven isolated facade route tests.
</objective>

## Artifacts this phase produces

- Exact combined fixture helpers for 44 unique field names, subgroup totals `3.35/4.10/1.75/1.80/2.45`, total `13.45`, scale `1/13.45`, count/weakenedCount `44`, and final total `1.0`.
- Final provider concatenation oracle in stable order `FaceShapeWarpProvider + ChinWarpProvider + EyeWarpProvider + EyebrowWarpProvider + NoseWarpProvider + MouthWarpProvider`.
- New `.eyebrows` pipeline guard and exactly one `EyebrowWarpProvider` call in `BeautyGeometryEffectPipeline.controlPoints`.
- Representative field-local degradation rows for each side/pair/chord/apex prerequisite, provider-empty-after-scaling, fresh/reused/stale/no-face, valid-invalid-valid, and unrelated safe-domain continuation.
- Seven isolated `BeautyEngineGeometryFacadeTests` rows for `eyebrowYPosition`, `eyebrowThickness`, `eyebrowLength`, `eyebrowSpacing`, `eyebrowHeadSpacing`, `eyebrowTilt`, and `eyebrowPeakDefinition`, with aggregate-only detection/domain/point evidence.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<read_first>
AGENTS.md; PLANS.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-CONTEXT.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-RESEARCH.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-PATTERNS.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-VALIDATION.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-03-SUMMARY.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-04-SUMMARY.md; BeautySDK/Sources/BeautyEffects/Render/BeautyGeometryEffectPipeline.swift
</read_first>

<context>
@.planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-CONTEXT.md
@.planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-RESEARCH.md
@.planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-PATTERNS.md
@.planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-03-SUMMARY.md
@.planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-04-SUMMARY.md
@BeautySDK/Sources/BeautyEffects/Render/BeautyGeometryEffectPipeline.swift

<interfaces>
Plans 02-04 provide seven named emissions, seven effective fields, `.eyebrows`, exact provisional caps, resolver preflight/final accounting, one `0..<44` retained loop, and exact 44-field conflict arithmetic. `BeautyGeometryEffectPipeline.controlPoints(for:face:)` currently guards face/eyes/nose/mouth and concatenates five provider groups; this plan inserts eyebrows once. `BeautyEngine.processResult(image:metadata:parameters:)` remains the sole public route, and `SDKTestingFaceDetectionFixture` supplies deterministic request-local observations without exposing raw geometry.
</interfaces>
</context>

## Decision trace

- D-05..08: exact local degradation matrix, no fallback, siblings continue.
- D-09..13: seven final strengths/emissions, exact 44 convergence, one dispatch, reuse/conflict symmetry, exact automated evidence.
- D-15/D-18: aggregate facade evidence only; no raw public/SPI geometry.
- D-14/D-16: no final-cap, decoded output/gallery, exhaustive closeout, or promotion claim.

## Plan-local source coverage

| Source | Coverage |
| --- | --- |
| GOAL | Completes resolver→conflict→warp→public-facade integration. |
| REQ | All GEOM requirements receive route/non-alias evidence; PIPE-01/02 receive degradation, exact convergence, dispatch, and facade proof. |
| RESEARCH | Covers final-emission authority, exact 44 fixture, provider-empty-after-scale, stable dispatch, facade route, and request isolation. |
| CONTEXT | D-05..16/D-18 are implemented or explicitly fenced. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 50-05-01: Prove field-local degradation and exact final-mask convergence</name>
  <files>BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift, BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift</files>
  <read_first>AGENTS.md; PLANS.md; DESIGN.md; SECURITY.md; RELIABILITY.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-CONTEXT.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-RESEARCH.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-PATTERNS.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-03-SUMMARY.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-04-SUMMARY.md; BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift; BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift</read_first>
  <behavior>
    - Left-only/right-only preserve all per-side fields but remove pair-only spacing; nil apex removes peak only for that side; degenerate chord removes only length/tilt/peak work requiring it; valid siblings remain.
    - Fresh is full, reused is exact 0.5 before conflict, stale/no-face is zero, valid-invalid-valid has no carryover, and concurrent request-local fixtures remain isolated.
    - Exact all-provider fixture retains 44 unique names and yields 13.45, `1/13.45`, 44, final total 1, stable signs, final provider idempotence, and no field re-entry.
    - A deliberately locally degenerate eyebrow emission that disappears after scaling is removed before final activity/metrics/dispatch and does not change safe sibling values.
  </behavior>
  <action>Per D-05 through D-10/D-12/D-13, extend the established table-driven degradation fixtures with every field prerequisite and representative lifecycle row from the seventeen assumptions; assert exact effective key paths and exact named emission arrays. Preserve the distinction between representative Phase 50 coverage and Phase 52 exhaustive calibration/transition closure. Extend the combined fixture to all 44 provider-eligible names and exact subgroup/total/count/scale arithmetic. Recompute final face, chin, eye, eyebrow, nose, and mouth emissions at final strengths; sanitize the retained mask again and assert it is unchanged; prove removed fields never re-enter, provider-empty contributes zero, unrelated domains retain their own final values, and final provider concatenation equals the expected control-point oracle consumed in Task 02.</action>
  <acceptance_criteria>Every local prerequisite and representative lifecycle row passes; exact 44 arithmetic and idempotent final mask pass; final warnings/metrics use no support details; missing/provider-empty eyebrows never weaken unrelated eligible domains.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --disable-sandbox --jobs 1 --filter BeautyEffectsTests.CombinedEffectSafetyTests &amp;&amp; swift test --package-path BeautySDK --disable-sandbox --jobs 1 --filter BeautyEffectsTests.MissingLandmarkDegradationTests</automated></verify>
  <done>One final mask explains every strength, removal, scale, sibling continuation, warning, metric, and named provider array.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 50-05-02: Insert eyebrow work exactly once into unified dispatch</name>
  <files>BeautySDK/Sources/BeautyEffects/Render/BeautyGeometryEffectPipeline.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyGeometryEffectPipelineTests.swift</files>
  <read_first>AGENTS.md; PLANS.md; ARCHITECTURE.md; DESIGN.md; RELIABILITY.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-CONTEXT.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-PATTERNS.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-03-SUMMARY.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-04-SUMMARY.md; BeautySDK/Sources/BeautyEffects/Render/BeautyGeometryEffectPipeline.swift; BeautySDK/Tests/BeautyEffectsTests/BeautyGeometryEffectPipelineTests.swift; BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift</read_first>
  <behavior>
    - Eyebrow-only active plans dispatch nonempty control points through the existing pipeline.
    - All-provider dispatch equals exact stable concatenation Face→Chin→Eye→Eyebrow→Nose→Mouth with eyebrow points appearing once.
    - Empty/finally removed eyebrow work dispatches no eyebrow points, and existing local warp/render implementation remains unchanged.
  </behavior>
  <action>Per D-10 through D-13, add `.eyebrows` to the existing `activeDomains` guard and insert one `EyebrowWarpProvider().makeControlPoints(face:strengths:)` call between eye and nose in the existing concatenation. Do not add a renderer, pass, pre-warp, second scale, conditional public route, or alternate control-point API; leave the warp implementation below the provider list unchanged. Add eyebrow-only, removed-eyebrow, and exact all-provider order/equality tests, comparing arrays directly with the final provider oracle from Task 01 and confirming the resolver geometryPointCount equals the dispatched array count.</action>
  <acceptance_criteria>Eyebrow-only plans no longer fall through the guard; the provider appears exactly once in source and arrays; final metric and dispatch counts agree; all existing pipeline tests remain green.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --disable-sandbox --jobs 1 --filter BeautyEffectsTests.BeautyGeometryEffectPipelineTests &amp;&amp; swift test --package-path BeautySDK --disable-sandbox --jobs 1 --filter BeautyEffectsTests.CombinedEffectSafetyTests</automated></verify>
  <done>All final eyebrow work enters the one existing geometry warp exactly once and in the resolver's accounting order.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 50-05-03: Prove seven redacted public-facade routes and request isolation</name>
  <files>BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift, BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift</files>
  <read_first>AGENTS.md; PLANS.md; ARCHITECTURE.md; DESIGN.md; SECURITY.md; RELIABILITY.md; PRODUCT_SENSE.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-CONTEXT.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-RESEARCH.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-01-SUMMARY.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-03-SUMMARY.md; BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift; BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift</read_first>
  <behavior>
    - Seven isolated public requests invoke the deterministic detector once, preserve output extent, activate `.eyebrows`, retain their same-named effective strength, and report positive aggregate geometryPointCount.
    - Missing/malformed/partial fixtures fail by field prerequisite while a safe sibling domain continues; repeated valid/missing/valid and parallel requests carry no stale support.
    - Results, warnings, metrics, detection summary, description, debugDescription, mirror, and dump disclose no raw/canonical points, sides, endpoints, centers, apex, axes, normals, vectors, or provider internals.
  </behavior>
  <action>Per D-03/D-05 through D-09/D-11/D-15/D-18, finish the Plan 01 deterministic fixtures if needed, then add one table-driven facade row per public eyebrow identifier through the existing `BeautyEngine.processResult(image:metadata:parameters:)` route. Assert one geometry-triggered detection, usable selected face, unchanged extent, exact same-named effective field, aggregate active domain/count, and redaction. Add left-only/right-only pair-vs-per-side, missing/malformed, provider-empty, valid-invalid-valid, and task-group request-isolation rows while keeping safe filter/color or non-eyebrow geometry active. Do not save images, add renderer/gallery cases, inspect decoded ROI pixels, expose raw geometry, or claim visual direction/locality/distinction.</action>
  <acceptance_criteria>All seven facade rows and representative degradation/isolation rows pass through one detector/facade/pipeline path; result evidence is aggregate-only; no Phase 51 artifacts or public/SPI geometry surface appears.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --disable-sandbox --jobs 1 --filter BeautyCoreTests.BeautyEngineGeometryFacadeTests &amp;&amp; python3 .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/check_eyebrow_geometry_boundaries.py &amp;&amp; git diff --check</automated></verify>
  <done>SDK integrators can route each eyebrow intent safely through the one public facade without raw support disclosure or output-evidence overclaim.</done>
</task>

</tasks>

<threat_model>
<blocking_rule>ASVS L1 with block-on-high: any unresolved HIGH finding, field-mask/dispatch mismatch, duplicate warp, request-state leak, raw facade disclosure, or required-test failure blocks completion; HIGH threats may not be accepted or transferred.</blocking_rule>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Resolver final mask → unified pipeline | Provider order, eligibility, metric count, and dispatched arrays must be identical. |
| Request-local detector support → public facade | Parallel/repeated requests must not share biometric-adjacent state; only aggregate evidence may escape. |
| Compiled route evidence → downstream product claims | Routing cannot authorize decoded visibility, final naturalness/safety, promotion, or release readiness. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
| --- | --- | ---: | --- | --- | --- |
| T-50-12 | Tampering / Repudiation | high | combined final-mask evidence | mitigate | Exact 44 ledger, idempotent sanitization, direct final-provider/metric/dispatch equality, and provider-empty-after-scale row. |
| T-50-13 | Tampering / DoS | high | unified dispatch | mitigate | One provider call in stable order, one active-domain guard, no second pass/scale, and exact source/array tests. |
| T-50-14 | Information Disclosure / Spoofing | high | public facade and testing fixtures | mitigate | Actual request-local eyebrow provenance, repeated/concurrent isolation, aggregate-only assertions, and live checker. |
| T-50-SC | Tampering | medium | package supply chain | mitigate | No install, manifest, target, dependency, model, or resource change is planned. |
</threat_model>

<verification>
- Combined, degradation, pipeline, and facade focused suites pass.
- Exact final provider arrays equal unified dispatch and geometryPointCount.
- Live checker passes provider/order/privacy/scope rules.
- No renderer/gallery/output image, Demo/UI, model/resource/dependency/network, product status, or final-cap change appears.
</verification>

<success_criteria>
- All seven semantics reach one public facade and one unified warp with field-local degradation.
- Exact 44-field strengths, totals, count, scale, warnings, metrics, final emissions, and dispatch agree.
- Request isolation and Phase 49 privacy/no-substitution remain intact, with Phase 51/52 evidence explicitly unclaimed.
</success_criteria>

<output>
Create `.planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-05-SUMMARY.md` when done.
</output>
