---
phase: 51-public-facade-eyebrow-output-evidence
plan: "02"
type: execute
wave: 2
depends_on: ["51-01"]
files_modified:
  - .planning/phases/51-public-facade-eyebrow-output-evidence/check_eyebrow_renderer_outputs.py
autonomous: true
requirements: [OUT-02, OUT-03]
must_haves:
  truths:
    - "A self-tested standard-library helper discovers exactly 72 renderer cases, one active portrait e6.jpg, and one separate no-face negative; it names 72 portrait outputs and 144 total generated outputs without conflating the denominators (D-01, D-03, D-04)."
    - "Strict acceptance is structurally separated into brow visibility/locality, six signed-direction predicates, seven-family distinctions, and thirteen no-face no-ops (D-05, D-06, D-07)."
    - "Malformed, stale, duplicate, symlinked, oversized, raced, parked-portrait, unexpected-fixture, dynamic-threshold, raw-disclosure, or inventory-drift evidence fails closed (D-04, D-08, D-11, D-13)."
  artifacts:
    - path: .planning/phases/51-public-facade-eyebrow-output-evidence/check_eyebrow_renderer_outputs.py
      provides: "Bounded exact-matrix decoder and eyebrow semantic acceptance contract"
      min_lines: 700
  key_links:
    - from: .planning/phases/51-public-facade-eyebrow-output-evidence/check_eyebrow_renderer_outputs.py
      to: BeautySDK/Sources/BeautyExampleRenderer/main.swift
      via: "Live duplicate-free RenderCase discovery and exact required-ID validation"
      pattern: "discover_case_ids|EXPECTED_CASE_COUNT"
    - from: .planning/phases/51-public-facade-eyebrow-output-evidence/check_eyebrow_renderer_outputs.py
      to: example-images/input/portraits/e6.jpg
      via: "Descriptor-safe fixture discovery requiring the sole active portrait stem e6"
      pattern: "EXPECTED_PORTRAIT|e6"
    - from: .planning/phases/51-public-facade-eyebrow-output-evidence/check_eyebrow_renderer_outputs.py
      to: example-images/output
      via: "Exact portrait and negative output-name bijection plus bounded PNG decoding"
      pattern: "validate_matrix|decoded_rgb"
---

<objective>
Create the fail-closed strict output helper that will judge the single-portrait eyebrow matrix independently of Swift source assertions.

Purpose: Make untrusted generated images, fixture discovery, pixel locality, signed direction, semantic distinction, and no-face safety testable before any thresholds are accepted.
Output: A self-tested Python helper with exact 72-portrait/144-total inventory semantics and non-circular eyebrow pixel contracts.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/51-public-facade-eyebrow-output-evidence/51-CONTEXT.md
@.planning/phases/51-public-facade-eyebrow-output-evidence/51-01-SUMMARY.md
@.planning/milestones/v1.12-phases/47-public-facade-face-output-evidence/check_face_geometry_renderer_outputs.py
@.planning/milestones/v1.11-phases/43-public-facade-eye-geometry-output-evidence/check_eye_geometry_renderer_outputs.py
@BeautySDK/Sources/BeautyExampleRenderer/main.swift
@example-images/input/portraits/e6.jpg
@example-images/input/negatives/no-face-gradient.png

<interfaces>
From the archived Phase 47 helper:
- `discover_case_ids(renderer_source)`, `discover_fixtures(input_dir)`, `validate_matrix(...)`, `decoded_rgb(path)`, and descriptor-relative bounded reads form the reusable acquisition/decoder boundary.
- PNG decoding validates signature, IHDR/IDAT/IEND order, CRC, zlib stream, filters, dimensions, compressed size, and decoded size.
- `--measure` reports metrics without accepting them; strict default mode consumes committed positive constants; `--self-test` owns adversarial negative paths.

Phase 51 exact IDs are the thirteen renderer IDs created by Plan 51-01. Positive representatives are the six `_plus0p25` cases plus `eyebrowPeakDefinition_0p25`; signed pairs match by field prefix.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 51-02-01: Build the bounded single-portrait matrix and decoder contract</name>
  <files>.planning/phases/51-public-facade-eyebrow-output-evidence/check_eyebrow_renderer_outputs.py</files>
  <behavior>
    - "Live discovery requires 72 unique cases, portrait set exactly `(e6.jpg,)`, negative set exactly `(no-face-gradient.png,)`, 72 portrait outputs, and 144 total output files."
    - "The 72 portrait denominator never includes no-face files; separate no-face checks cover the thirteen new cases against geometryBaseline_noop (D-03)."
    - "Any e1-e5 file under input/portraits, duplicate stem/ID, missing/extra/stale output, symlink/non-regular file, decode error, oversized input/output, or descriptor race fails."
    - "Only aggregate counts/minima and fixture labels `e6`/`no-face-gradient` may be printed; no raw coordinate, support, path, or pixel payload escapes."
  </behavior>
  <action>Copy the archived Phase 47 helper as the decoder/security baseline and specialize it without weakening descriptor/no-follow acquisition, identity and stable-size snapshots, compressed/dimension/decoded ceilings, strict PNG CRC/chunk/zlib/filter validation, bounded JPEG SOF parsing, live renderer discovery, exact output naming, duplicate/stale/missing/extra rejection, or aggregate-only reporting. Per D-01/D-03/D-04/D-11 define separate constants and result counters for `EXPECTED_CASE_COUNT = 72`, `EXPECTED_PORTRAIT_STEMS = ("e6",)`, `EXPECTED_NEGATIVE_STEMS = ("no-face-gradient",)`, `EXPECTED_PORTRAIT_OUTPUT_COUNT = 72`, and `EXPECTED_TOTAL_OUTPUT_COUNT = 144`; require all thirteen exact eyebrow IDs and `geometryBaseline_noop`, reject any other active portrait including e1-e5, and keep no-face results outside the portrait count. Add self-tests for every inventory, descriptor, decoding, budget, race, stale-file, unexpected-portrait, parked-portrait, and disclosure failure before live pixels are examined.</action>
  <verify>
    <automated>PYTHONPYCACHEPREFIX=/tmp/beauty-pycache python3 -m py_compile .planning/phases/51-public-facade-eyebrow-output-evidence/check_eyebrow_renderer_outputs.py &amp;&amp; python3 .planning/phases/51-public-facade-eyebrow-output-evidence/check_eyebrow_renderer_outputs.py --self-test &amp;&amp; git diff --check</automated>
  </verify>
  <done>The helper rejects every malformed or scope-drift matrix and reports the exact 72 portrait outputs separately from the 144-file renderer inventory and no-face safety pool.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 51-02-02: Encode non-circular eyebrow locality, direction, and family gates</name>
  <files>.planning/phases/51-public-facade-eyebrow-output-evidence/check_eyebrow_renderer_outputs.py</files>
  <behavior>
    - "Each of thirteen cases must exceed positive changed-pixel and RGB floors inside one fixed brow ROI relative to geometryBaseline_noop."
    - "Eye, forehead/hair, background, and non-label watermark-safe regions each have explicit maximum budgets; the label band cannot contribute to acceptance (D-05, D-06)."
    - "Six fixed signed-pair predicates establish expected direction and all twenty-one pairwise comparisons among seven positive family representatives exceed fixed family floors (D-07)."
    - "Strict mode refuses acceptance until one immutable calibration object contains positive floors/budgets and fixed predicates produced by Plan 51-03; measurement mode may report metrics but cannot pass requirements (D-08)."
  </behavior>
  <action>Add fixed top-origin normalized region types for the brow ROI, protected eyes, protected forehead/hair, protected background, and renderer-derived label exclusion; every rectangle must be bounded, nonempty, and disjoint from the accepted effect footprint per D-05/D-06. Define thirteen baseline gates, six named signed-direction gates aligned with the Phase 50 contract (positive Y follows image-Y; positive thickness/length/whole-spacing/head-spacing expand their named extent; positive tilt raises canonical outer tails), twenty-one fixed pairwise positive-family distinctions including spacing/head-spacing and thickness/peak, and thirteen exact watermark-safe no-face comparisons per D-07. Make strict mode fail with an explicit non-success result until a complete immutable calibration object supplies positive visibility/direction/family floors and protected-region budgets; measurement mode only reports candidates. Add synthetic/adversarial self-tests proving outside-only signal, label-only signal, reversed sign, collapsed family, dynamic comparator selection, zero/negative thresholds, overlapping regions, no-face changes, and raw disclosure cannot pass per D-08/D-11.</action>
  <verify>
    <automated>python3 .planning/phases/51-public-facade-eyebrow-output-evidence/check_eyebrow_renderer_outputs.py --self-test &amp;&amp; PYTHONPYCACHEPREFIX=/tmp/beauty-pycache python3 -m py_compile .planning/phases/51-public-facade-eyebrow-output-evidence/check_eyebrow_renderer_outputs.py &amp;&amp; git diff --check</automated>
  </verify>
  <done>The helper has fixed, independently self-tested contracts for thirteen visibility/locality gates, six signed directions, twenty-one positive-family distinctions, protected regions, and thirteen separate no-face no-ops; strict live acceptance remains fail-closed until Plan 51-03 freezes measured constants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Renderer/fixture source → helper inventory | Source text and repository layout define expected names but cannot prove pixels. |
| Untrusted PNG/JPEG bytes → decoder | Compressed local evidence can be malformed, oversized, replaced, or raced. |
| Measured pixels → acceptance result | Dynamic thresholds or comparators could make the accepting dataset judge itself. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-51-06 | Spoofing | fixture discovery | mitigate | Exact e6 portrait and no-face negative sets; reject e1-e5 and every unexpected portrait. |
| T-51-07 | Tampering | output matrix | mitigate | Exact 72 portrait/144 total bijection, duplicate/stale/missing/extra/symlink rejection. |
| T-51-08 | Denial of Service | image decoder | mitigate | Descriptor reads, identity snapshots, 16 MiB compressed, 4096-square dimension, and 64 MiB decoded ceilings. |
| T-51-09 | Tampering | threshold selection | mitigate | Measurement cannot pass; strict mode requires committed positive immutable constants and fixed comparators. |
| T-51-10 | Information Disclosure | helper output | mitigate | Aggregate counts/minima only; adversarial raw-token disclosure tests. |
| T-51-11 | Repudiation | semantic evidence | mitigate | Named baseline, signed-pair, family-pair, protected-region, and no-face counters are reported separately. |
| T-51-SC | Tampering | Python/package supply chain | mitigate | Standard library only; no pip, npm, cargo, or SwiftPM install/change. |
</threat_model>

<verification>
- Helper bytecode compilation and adversarial self-tests pass.
- The helper proves exact fixture/output count separation before examining acceptance pixels.
- Strict mode cannot accept until Plan 51-03 freezes a complete positive calibration object from an independent measurement run.
</verification>

<success_criteria>
OUT-02 and OUT-03 have an implementation-ready, bounded, self-tested judge whose strict acceptance is non-circular and whose 72 portrait denominator excludes the separate no-face negative.
</success_criteria>

<output>
Create `.planning/phases/51-public-facade-eyebrow-output-evidence/51-02-SUMMARY.md` when done.
</output>
