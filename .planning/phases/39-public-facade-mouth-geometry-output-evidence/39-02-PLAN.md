---
phase: 39-public-facade-mouth-geometry-output-evidence
plan: "02"
type: execute
wave: 2
depends_on: [39-01]
files_modified:
  - .planning/phases/39-public-facade-mouth-geometry-output-evidence/check_mouth_remaining_renderer_outputs.py
  - .planning/phases/39-public-facade-mouth-geometry-output-evidence/39-MOUTH-OUTPUT-EVIDENCE.md
autonomous: true
requirements: [MOUTH-09, MOUTH-10, MOUTH-11]
must_haves:
  truths:
    - "The helper discovers renderer cases and recursive fixtures, rejects duplicate IDs/stems and missing/extra/corrupt outputs, computes the product, then separately requires 44 × 7 = 308."
    - "All 308 public-facade PNGs decode non-empty at input dimensions; eight no-face outputs preserve 64 × 64 and match baseline outside the watermark label."
    - "One fixed documented mouth ROI and frozen non-circular floors separately prove 48 visibility, 18 signed-direction, 12 peak-independence, and 18 plump-independence comparisons."
  artifacts:
    - path: .planning/phases/39-public-facade-mouth-geometry-output-evidence/check_mouth_remaining_renderer_outputs.py
      provides: self-contained matrix, strict decoder, ROI, family, and no-face gate
    - path: .planning/phases/39-public-facade-mouth-geometry-output-evidence/39-MOUTH-OUTPUT-EVIDENCE.md
      provides: observed inventories, ROI, thresholds, minima, commands, and non-claims
  key_links:
    - from: BeautySDK/Sources/BeautyExampleRenderer/main.swift
      to: check_mouth_remaining_renderer_outputs.py
      via: discovered ordered case IDs and flat output naming
      pattern: "id:"
---

<objective>Create the archive-safe v1.10 helper, calibrate on one guarded render, and accept a separately regenerated strict 308-output matrix with direct visibility, direction, independence, and no-face evidence.</objective>

## Artifacts this phase produces

- New script `check_mouth_remaining_renderer_outputs.py` with CLI, bounded acquisition, case/fixture discovery, PNG/JPEG dimensions, complete PNG RGB decoding, watermark/ROI calculation, sixteen comparison families, fixed thresholds, no-face fallback, self-tests, and deterministic aggregate reports.
- New `39-MOUTH-OUTPUT-EVIDENCE.md`; ignored local 308-output matrices are evidence inputs, never tracked baselines.

<execution_context>@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md</execution_context>

<context>@AGENTS.md
@PLANS.md
@.planning/phases/39-public-facade-mouth-geometry-output-evidence/39-CONTEXT.md
@.planning/phases/39-public-facade-mouth-geometry-output-evidence/39-RESEARCH.md
@.planning/phases/39-public-facade-mouth-geometry-output-evidence/39-PATTERNS.md
@.planning/milestones/v1.9-phases/36-public-facade-output-evidence/check_nose_remaining_renderer_outputs.py
@.planning/milestones/v1.8-phases/33-mouth-renderer-output-evidence/check_mouth_renderer_outputs.py
@BeautySDK/Sources/BeautyExampleRenderer/main.swift</context>

<tasks>
<task type="auto">
  <name>Task 39-02-01: Build the self-contained discovered-inventory decoder and mouth evidence gate</name>
  <files>.planning/phases/39-public-facade-mouth-geometry-output-evidence/check_mouth_remaining_renderer_outputs.py</files>
  <read_first>
    - `39-CONTEXT.md`, `39-RESEARCH.md`, `39-PATTERNS.md`, and `39-VALIDATION.md`
    - `BeautyExampleRenderer/main.swift` — actual source inventory, naming, fixture sort, and watermark constants
    - archived Phase 36 helper — bounded self-contained acquisition/PNG/JPEG decode/matrix/calibration/no-face pattern
    - archived Phase 33 helper — mouth ROI and signed/semantic comparator intent only; do not copy its sibling import
    - `example-images/generate_gallery.py` — duplicate-stem and inventory-bijection pattern
  </read_first>
  <action>Create a Python 3 standard-library-only helper accepting `--input`, `--output`, `--renderer-source`, `--measure`, and `--self-test`. Re-own Phase 36's no-follow bounded acquisition, snapshot checks, PNG CRC/chunk/zlib/filter decode, JPEG dimensions, and 16 MiB/4096×4096/64 MiB budgets. Discover ordered live case IDs and recursive regular fixtures, reject duplicate IDs/stems, derive expected flat names, reject missing/unexpected/symlink/nonregular/zero/corrupt/incomplete outputs and dimension drift, then separately require exact 44 cases, seven fixtures, six portraits, one exact no-face fixture, and 308 outputs. Implement one top-origin normalized mouth ROI wholly above the renderer-matched watermark boundary and sixteen named families: eight new-vs-baseline, three signed pairs, peak-vs-smile/size, and plump-vs-size/color/peak. Return changed pixels, ROI pixels, and absolute RGB delta per pair. Exclude no-face from portrait totals and require all eight new no-face cases equal baseline in the fixed 2,048-pixel right-half fallback. Strict mode uses committed fixed changed-pixel/RGB-delta floors only; measure mode reports but never accepts. Add deterministic self-tests covering duplicates, malformed/missing/extra/bomb/trailing/race output, dimensions, ROI/watermark, and no-face fallback.</action>
  <verify><automated>python3 .planning/phases/39-public-facade-mouth-geometry-output-evidence/check_mouth_remaining_renderer_outputs.py --self-test &amp;&amp; python3 -m py_compile .planning/phases/39-public-facade-mouth-geometry-output-evidence/check_mouth_remaining_renderer_outputs.py &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>
    - Helper is self-contained and archive-rerunnable with only standard-library imports.
    - Live inventory discovery precedes frozen 44/7/308 assertions and self-tests exercise all named fail-closed conditions.
    - Sixteen families, one ROI above watermark, fixed strict floors, and eight no-face exclusions/no-ops are explicit automated gates.
  </acceptance_criteria>
  <done>The missing Wave 0 helper exists and fails closed before live output acceptance.</done>
</task>

<task type="auto">
  <name>Task 39-02-02: Measure, freeze, regenerate, strictly accept, and document 308 outputs</name>
  <files>.planning/phases/39-public-facade-mouth-geometry-output-evidence/check_mouth_remaining_renderer_outputs.py, .planning/phases/39-public-facade-mouth-geometry-output-evidence/39-MOUTH-OUTPUT-EVIDENCE.md</files>
  <read_first>
    - Helper created by Task 39-02-01 and `39-VALIDATION.md`
    - `BeautyExampleRenderer/main.swift` and `39-01-SUMMARY.md`
    - `example-images/README.md` and `.gitignore` — disposable output boundary
    - `39-CONTEXT.md` and `39-RESEARCH.md` — exact families/counts and non-circular protocol
  </read_first>
  <action>Build `BeautyExampleRenderer`. Before measurement and strict renders, resolve `example-images/output` physically, require it equals the exact repository allow-listed ignored root, and delete only files below that root. Render all cases/fixtures and run helper `--measure`; choose one global ROI plus one fixed changed-pixel floor and one fixed RGB-delta floor below the weakest observed family with explicit margin, then freeze the constants. Repeat guarded clean and full public-facade render, run strict mode, and create `39-MOUTH-OUTPUT-EVIDENCE.md` with exact commands, 44/7/308 inventories/dimensions, fixed ROI/watermark formula, observed measurement and strict minima/margins for all sixteen families, group totals 48/18/12/18, eight no-face 64×64 no-ops, ignored/untracked state, and provisional/no-promotion/device/commercial/packaging non-claims. Any unsafe path, zero-test, build, decode, extent, ROI, family, count, or containment failure blocks acceptance.</action>
  <verify><automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift build --package-path BeautySDK --product BeautyExampleRenderer &amp;&amp; OUTPUT_ROOT="$(cd example-images/output &amp;&amp; pwd -P)" &amp;&amp; test "$OUTPUT_ROOT" = "$(pwd -P)/example-images/output" &amp;&amp; git check-ignore -q example-images/output &amp;&amp; find "$OUTPUT_ROOT" -mindepth 1 -delete &amp;&amp; DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift run --package-path BeautySDK BeautyExampleRenderer --input example-images/input --output example-images/output &amp;&amp; python3 .planning/phases/39-public-facade-mouth-geometry-output-evidence/check_mouth_remaining_renderer_outputs.py --input example-images/input --output example-images/output --renderer-source BeautySDK/Sources/BeautyExampleRenderer/main.swift &amp;&amp; test "$(find example-images/output -maxdepth 1 -type f -name '*.png' | wc -l | tr -d ' ')" -eq 308</automated></verify>
  <acceptance_criteria>
    - Strict rerun reports 44 cases, seven fixtures, 308/308 valid same-dimension PNGs, and no unexpected renderer output.
    - All sixteen families pass already-frozen floors with exact group totals 48 visibility, 18 signed, 12 peak, and 18 plump.
    - All eight no-face outputs preserve 64×64 and are baseline-identical in the fixed label-safe region.
    - Evidence records aggregate minima/margins and never claims final caps, naturalness, promotion, or readiness.
  </acceptance_criteria>
  <done>Public-facade output makes all five controls visible, directionally distinct, semantically independent, and safely degraded.</done>
</task>
</tasks>

<threat_model><scope>ASVS L1 generated-evidence integrity and biometric-adjacent privacy.</scope><blocking_rule>Any unresolved HIGH threat blocks acceptance.</blocking_rule><assets>Matrix completeness, decoder correctness, non-circular thresholds, local fixture/output bytes, and untracked artifacts.</assets><trust_boundaries>Untrusted fixture/output bytes enter the helper; only aggregate counts/minima enter committed evidence.</trust_boundaries><threats>Stale files, stem collisions, symlink/race files, decompression bombs, watermark false positives, dynamic thresholds, and raw pixel/geometry leakage.</threats><mitigations>Allow-listed clean root, bounded descriptor acquisition, strict decode, discovery plus exact assertions, frozen ROI/floors, negative self-tests, aggregate-only evidence, and ignore/tracking gates.</mitigations><residual_risk>Commercial naturalness and final exact caps remain Phase 40/outside scope.</residual_risk></threat_model>
<verification>Run helper self-tests/compile, renderer build, guarded measurement, frozen constants, separately clean strict render, exact 308 count, ignore/tracking checks, and diff hygiene.</verification>
<success_criteria>MOUTH-09 through MOUTH-11 output evidence passes every decoded, ROI, direction, independence, no-face, privacy, and inventory gate with no HIGH threat.</success_criteria>
<output>Create `39-02-SUMMARY.md`.</output>
