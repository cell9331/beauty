---
phase: 18-skin-retouch-core-modules
plan: 2
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - BeautySDK/Sources/BeautyEffects/Render/BeautyColorEffectPipeline.swift
  - BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
  - BeautySDK/Tests/BeautyEffectsTests/SkinBasicEffectTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyEngineTests.swift
  - .planning/phases/18-skin-retouch-core-modules/18-02-SUMMARY.md
autonomous: true
requirements:
  - SKIN-02
  - SKIN-03
must_haves:
  truths:
    - "SKIN-02: promoted Basic skin logic is implemented behind SDK boundaries with conservative caps, degradation semantics, and focused tests."
    - "SKIN-03: current MVP skin controls remain separated from local repair, region, teeth, hairline, segmentation, and resource capabilities."
    - "D-01 D-03 D-04 D-05: Basic skin formula output improves inside `BeautyColorEffectPipeline` without becoming repair or a new pass."
    - "D-06 D-07 D-08 D-09: public no-detection facade output remains visible, explicit internal no-face contexts may skip skin, and metadata remains redacted."
    - "D-14 D-17: focused XCTest is the required implementation gate; full `swift test --package-path BeautySDK` remains optional evidence."
  artifacts:
    - path: "BeautySDK/Sources/BeautyEffects/Render/BeautyColorEffectPipeline.swift"
      provides: "conservative Basic skin formula updates for BGRA pixel-buffer and CIImage paths"
    - path: "BeautySDK/Tests/BeautyEffectsTests/SkinBasicEffectTests.swift"
      provides: "focused Basic skin formula regression tests"
      exports:
        - "SkinBasicEffectTests"
        - "testDefaultPlanPreservesCIImagePixelsAndExtent"
        - "testSkinWhiteningLiftsLuminanceConservativelyInCIAndPixelBufferPaths"
        - "testSkinRosyAddsConservativeRedBiasWithoutLargeBlueShift"
        - "testSkinSharpenIncreasesLocalContrastWithoutChangingExtent"
        - "testSkinSmoothingMovesChannelsTowardLuminanceWithoutFlatteningTextureProxy"
        - "testSkinComboProducesVisibleCappedMediumStrengthOutput"
    - path: "BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift"
      provides: "resolver layering, cap, and redaction tests"
      exports:
        - "testPublicResolverKeepsBasicSkinActiveWithoutFaceGeometry"
        - "testInternalNoFaceResolverSkipsBasicSkinWithRedactedWarning"
    - path: "BeautySDK/Tests/BeautyCoreTests/BeautyEngineTests.swift"
      provides: "public facade no-detection Basic skin evidence"
      exports:
        - "testSKIN02FacadeNoDetectionBasicSkinProducesVisibleImageOutput"
        - "testSKIN02FacadeNoDetectionBasicSkinReturnsRedactedMetadata"
  key_links:
    - from: "BeautySDK/Tests/BeautyEffectsTests/SkinBasicEffectTests.swift"
      to: "BeautySDK/Sources/BeautyEffects/Render/BeautyColorEffectPipeline.swift"
      via: "focused channel and extent assertions"
      pattern: "BeautyColorEffectPipeline\\.apply"
    - from: "BeautySDK/Tests/BeautyCoreTests/BeautyEngineTests.swift"
      to: "BeautySDK/Sources/BeautySDK/BeautyEngine.swift"
      via: "public facade import only"
      pattern: "import BeautySDK"
    - from: "BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift"
      to: "BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift"
      via: "public active skin and internal no-face skip semantics"
      pattern: "resolve\\(parameters:"
---

<objective>
Implement the promoted Basic skin module improvements behind existing SDK boundaries.

Purpose: satisfy SKIN-02 by improving the current Basic skin output formulas, preserving public parameter boundaries, and proving facade/resolver behavior with focused XCTest per D-01 through D-09 and D-14 through D-17.

Output: localized formula updates in `BeautyColorEffectPipeline.swift`, a new focused `SkinBasicEffectTests.swift`, resolver/facade assertions, and implementation evidence in `18-02-SUMMARY.md`.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-skin-retouch-core-modules/18-CONTEXT.md
@.planning/phases/18-skin-retouch-core-modules/18-RESEARCH.md
@.planning/phases/18-skin-retouch-core-modules/18-VALIDATION.md
@.planning/phases/18-skin-retouch-core-modules/18-PATTERNS.md
@.planning/phases/18-skin-retouch-core-modules/18-01-SUMMARY.md
@BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift
@BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
@BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift
@BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift
@BeautySDK/Sources/BeautyEffects/Render/BeautyColorEffectPipeline.swift
@BeautySDK/Sources/BeautySDK/BeautyEngine.swift
@BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
@BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift
@BeautySDK/Tests/BeautyEffectsTests/BeautySafetyCapsTests.swift
@BeautySDK/Tests/BeautyCoreTests/BeautyEngineTests.swift
</context>

## Artifacts this phase produces

- Materially changed source: `BeautySDK/Sources/BeautyEffects/Render/BeautyColorEffectPipeline.swift`.
- New focused test file: `BeautySDK/Tests/BeautyEffectsTests/SkinBasicEffectTests.swift`.
- New XCTest class: `SkinBasicEffectTests`.
- New XCTest methods: `testDefaultPlanPreservesCIImagePixelsAndExtent`, `testSkinWhiteningLiftsLuminanceConservativelyInCIAndPixelBufferPaths`, `testSkinRosyAddsConservativeRedBiasWithoutLargeBlueShift`, `testSkinSharpenIncreasesLocalContrastWithoutChangingExtent`, `testSkinSmoothingMovesChannelsTowardLuminanceWithoutFlatteningTextureProxy`, `testSkinComboProducesVisibleCappedMediumStrengthOutput`, `testPublicResolverKeepsBasicSkinActiveWithoutFaceGeometry`, `testInternalNoFaceResolverSkipsBasicSkinWithRedactedWarning`, `testSKIN02FacadeNoDetectionBasicSkinProducesVisibleImageOutput`, `testSKIN02FacadeNoDetectionBasicSkinReturnsRedactedMetadata`.
- Existing warning keys retained: `beauty_strength_capped`, `face_effects_skipped_no_face`.
- Existing metric keys retained: `beauty.effects.activeCount`, `beauty.effects.cappedCount`, `beauty.effects.skippedFaceDomains`.
- New warning or metric keys: none planned.
- Plan output: `.planning/phases/18-skin-retouch-core-modules/18-02-SUMMARY.md`.

## Source Audit

| Source | Items | Coverage |
| --- | --- | --- |
| GOAL | Implement promoted skin-retouch module logic behind SDK boundaries. | This plan implements Basic skin formula improvements in `BeautyEffects` and verifies through public facade tests. |
| REQ | SKIN-02, SKIN-03 | SKIN-02 is covered by formula, resolver, cap, and facade tests; SKIN-03 is covered by no-new-parameter and future-branch scans. |
| RESEARCH | Formula surface, resolver layering, test file gap, public facade evidence. | This plan creates the missing focused test file and modifies the existing formula surface only. |
| CONTEXT | D-01 through D-09, D-14, D-17 | This plan improves Basic skin conservatively, preserves public/internal no-face layering, and uses focused XCTest gates. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement conservative Basic skin formula regressions</name>
  <files>BeautySDK/Sources/BeautyEffects/Render/BeautyColorEffectPipeline.swift, BeautySDK/Tests/BeautyEffectsTests/SkinBasicEffectTests.swift</files>
  <read_first>
    @BeautySDK/Sources/BeautyEffects/Render/BeautyColorEffectPipeline.swift
    @BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift
    @BeautySDK/Tests/BeautyCoreTests/BeautyEngineTests.swift
    @BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift
    @.planning/phases/18-skin-retouch-core-modules/18-CONTEXT.md
    @.planning/phases/18-skin-retouch-core-modules/18-PATTERNS.md
  </read_first>
  <behavior>
    - `BeautyParameters()` through `BeautyEffectResolver.resolve(parameters:)` keeps CI output extent and rendered pixels equal to input.
    - `skinWhitening: 0.5` increases fixture luminance conservatively in CIImage and BGRA pixel-buffer paths without clipping medium skin-tone channels.
    - `skinRosy: 0.4` increases red contribution more than blue contribution while keeping channel deltas bounded.
    - `skinSharpen: 0.4` increases bright/dark fixture contrast directionally and preserves extent.
    - `skinSmoothing: 0.5` reduces a small channel-spread texture proxy toward luminance without collapsing every channel to the same byte value.
    - The medium skin combo produces visible changed bytes and all effective skin strengths remain at or under `BeautySafetyCaps`.
  </behavior>
  <action>Create `SkinBasicEffectTests.swift` using XCTest, CoreImage/CoreVideo fixtures, `BeautyCore`, and `@testable import BeautyEffects`. Add the exact test method names listed in this plan. Use local fixture helpers rather than importing `BeautyEngineTests` helpers across test targets. Then update `BeautyColorEffectPipeline.swift` only inside the existing BGRA `transform(...)` path and the existing CIImage color-control/color-matrix path so the same conservative Basic skin directions are covered in both paths per D-01, D-03, and D-04. Keep smoothing a lightweight full-frame softening proxy per D-05: no local repair, blemish cleanup, pore cleanup, region inpainting, masks, segmentation, or aggressive texture removal. Keep `cropped(to: image.extent)` behavior, keep output dimensions unchanged, keep public `BeautyParameters` untouched per D-02, and do not add a target, render pass, production `SkinPass`, renderer case, or Demo UI.</action>
  <verify>
    <automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path BeautySDK --filter SkinBasicEffectTests</automated>
    <automated>! rg -n "skinRepair|blemish|pore|inpainting|teeth whitening|hairline adjustment|segmentation" BeautySDK/Sources/BeautyEffects/Render/BeautyColorEffectPipeline.swift BeautySDK/Tests/BeautyEffectsTests/SkinBasicEffectTests.swift</automated>
  </verify>
  <acceptance_criteria>`SkinBasicEffectTests` passes; formula changes are localized to the existing color pipeline paths; no public parameter, target, render pass, mask, repair, teeth, hairline, segmentation, or renderer case is added.</acceptance_criteria>
  <done>Conservative Basic skin formula behavior is test-backed for CIImage and pixel-buffer paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify resolver layering and public facade no-detection output</name>
  <files>BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift, BeautySDK/Tests/BeautyCoreTests/BeautyEngineTests.swift</files>
  <read_first>
    @BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
    @BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift
    @BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
    @BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift
    @BeautySDK/Tests/BeautyCoreTests/BeautyEngineTests.swift
    @BeautySDK/Sources/BeautySDK/BeautyEngine.swift
    @SECURITY.md
    @RELIABILITY.md
  </read_first>
  <behavior>
    - Public `BeautyEffectResolver.resolve(parameters:)` with non-zero Basic skin values keeps `.skin` active without face geometry.
    - Internal `BeautyEffectResolver.resolve(parameters:faceGeometry:nil)` skips `.skin`, records `face_effects_skipped_no_face`, and records only redacted aggregate metrics.
    - Public `BeautyEngine.processResult(image:metadata:parameters:)` with Basic skin and no detection produces same-extent changed output through `import BeautySDK` only.
    - Public result warnings and metrics do not include paths, raw image types, bounding boxes, landmarks, Vision objects, or detector internals.
  </behavior>
  <action>Add resolver tests named `testPublicResolverKeepsBasicSkinActiveWithoutFaceGeometry` and `testInternalNoFaceResolverSkipsBasicSkinWithRedactedWarning` to `BeautyEffectResolverTests.swift`. Add public facade tests named `testSKIN02FacadeNoDetectionBasicSkinProducesVisibleImageOutput` and `testSKIN02FacadeNoDetectionBasicSkinReturnsRedactedMetadata` to `BeautyEngineTests.swift`; keep that file importing only `BeautySDK`. Do not change `BeautyEffectResolver.swift` unless a new test exposes a real mismatch. If resolver code is changed, preserve the D-06 and D-07 distinction exactly: public no-detection paths apply lightweight full-frame Basic skin, while explicit internal no-face contexts may skip face-dependent skin. If any metadata is emitted or adjusted, keep it result-scoped and redacted per D-08 and D-09, using only stable warning codes and aggregate metric keys.</action>
  <verify>
    <automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path BeautySDK --filter BeautyEffectResolverTests</automated>
    <automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path BeautySDK --filter BeautyEngineTests</automated>
    <automated>! rg -n "landmark|boundingBox|VNFaceObservation|/private/var|NSError|rawPresetJson|image bytes" BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift BeautySDK/Sources/BeautySDK/BeautyEngine.swift</automated>
  </verify>
  <acceptance_criteria>Resolver tests prove public active Basic skin and internal no-face skip semantics; engine tests prove public facade no-detection Basic skin output and redacted metadata without internal-target imports.</acceptance_criteria>
  <done>Facade-visible Basic skin and explicit internal no-face behavior are both protected by focused XCTest.</done>
</task>

<task type="auto">
  <name>Task 3: Run implementation gates and record Plan 18-02 evidence</name>
  <files>.planning/phases/18-skin-retouch-core-modules/18-02-SUMMARY.md</files>
  <read_first>
    @.planning/phases/18-skin-retouch-core-modules/18-VALIDATION.md
    @.planning/phases/18-skin-retouch-core-modules/18-PATTERNS.md
    @BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift
    @BeautySDK/Sources/BeautyExampleRenderer/main.swift
  </read_first>
  <action>After Tasks 1 and 2 pass, record the exact focused command outcomes in `18-02-SUMMARY.md`. Include the D-17 note that full `swift test --package-path BeautySDK` is optional and was not the fixed Phase 18 gate unless it was actually run. Run scoped negative scans to prove Plan 18-02 did not add future public parameters, future renderer cases, or future-branch implementation terms. Record any failure as a blocker instead of marking SKIN-02 complete.</action>
  <verify>
    <automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path BeautySDK --filter SkinBasicEffectTests</automated>
    <automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path BeautySDK --filter BeautyEffectResolverTests</automated>
    <automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path BeautySDK --filter BeautyEngineTests</automated>
    <automated>! rg -n "blemish|pore|texture|skinRepair|teeth|hairline" BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift</automated>
    <automated>! rg -n "skinRepair|repair|teeth|hairline|blemish|pore" BeautySDK/Sources/BeautyExampleRenderer/main.swift</automated>
    <automated>! rg -n "skinRepair|blemish|pore|inpainting|teeth whitening|hairline adjustment|segmentation" BeautySDK/Sources/BeautyEffects BeautySDK/Sources/BeautyRender BeautySDK/Sources/BeautyResources</automated>
    <automated>git diff --check -- BeautySDK/Sources/BeautyEffects/Render/BeautyColorEffectPipeline.swift BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift BeautySDK/Tests/BeautyEffectsTests/SkinBasicEffectTests.swift BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift BeautySDK/Tests/BeautyCoreTests/BeautyEngineTests.swift .planning/phases/18-skin-retouch-core-modules/18-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>All focused XCTest filters pass, scoped negative scans pass, and `git diff --check` reports no whitespace errors on touched implementation/test files and summary output.</acceptance_criteria>
  <done>Plan 18-02 leaves Basic skin implementation ready for renderer evidence in Plan 18-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| host app to public SDK facade | Host-provided parameters and images enter `BeautyEngine.processResult`. |
| public parameter model to internal effective strengths | Public normalized values are capped internally for natural output. |
| internal resolver metadata to public result | Warnings and metrics cross into `BeautyResult` and must remain redacted. |
| effect pipeline to rendered output | Formula changes can visibly alter user images and must preserve dimensions/no-op defaults. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-18-04 | Tampering | `BeautyColorEffectPipeline.swift` | mitigate | Keep changes localized to existing formula paths and prove no new pass, target, public parameter, mask, segmentation, or repair behavior appears. |
| T-18-05 | Information Disclosure | `BeautyEffectResolver` warnings and metrics | mitigate | Focused tests and static scan forbid raw image, path, bounding box, landmark, Vision, and raw error terms in public metadata. |
| T-18-06 | Denial of Service | CIImage and BGRA transform paths | mitigate | Preserve no-op short-circuit, same extent, same dimensions, and deterministic per-pixel loops; no resource/network work added. |
| T-18-07 | Spoofing | public facade test surface | mitigate | `BeautyEngineTests` imports only `BeautySDK`, proving host-like behavior rather than internal-target test shortcuts. |
| T-18-SC | Tampering | package/dependency surface | mitigate | No package-manager installs are planned. If execution adds one, stop and require the package legitimacy gate before proceeding. |
</threat_model>

<verification>
Required Plan 18-02 gates:
- `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path BeautySDK --filter SkinBasicEffectTests`
- `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path BeautySDK --filter BeautyEffectResolverTests`
- `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path BeautySDK --filter BeautyEngineTests`
- Scoped negative scans for public future parameters, future renderer cases, and future-branch implementation terms.
- `git diff --check` scoped to touched source, test, and summary files.
</verification>

<success_criteria>
- Basic skin formula output is improved conservatively at medium strengths through current public parameters only.
- Both CIImage and BGRA pixel-buffer formula paths are covered by focused tests.
- Public facade no-detection Basic skin remains visibly effective.
- Explicit internal no-face resolver contexts may skip skin with redacted warning/metric evidence.
- No public repair, pore, texture, teeth, hairline, segmentation, resource ownership, new pass, new target, renderer case, or Demo UI is added.
</success_criteria>

<output>
Create `.planning/phases/18-skin-retouch-core-modules/18-02-SUMMARY.md` when done.
</output>
