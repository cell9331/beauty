---
phase: 54-rights-approved-evidence-and-eligibility-decisions
plan: "01"
type: tdd
wave: 0
depends_on: []
files_modified:
  - .planning/phases/54-rights-approved-evidence-and-eligibility-decisions/54-evidence-core.test.js
  - .planning/phases/54-rights-approved-evidence-and-eligibility-decisions/54-review.contract.test.js
  - .planning/phases/54-rights-approved-evidence-and-eligibility-decisions/check_phase54_evidence_boundaries.py
autonomous: true
requirements: [EVID-01, EVID-02, EVID-03, EVID-04, EVID-05, LID-01]
must_haves:
  truths:
    - "D-01/D-02: RED contracts distinguish invalid input from a structurally valid but closed one-feature bundle and fail closed on unsafe paths, duplicate IDs/assets, unsupported enums, mixed features, missing rights, incomplete triples, and missing polarity coverage."
    - "D-03/D-04: mechanics-only, synthetic, AI-generated, disabled, parked, historical, and merely authorized rows can exercise tooling but contribute zero product rows or weight; portrait_001 never becomes a positive by authorization alone."
    - "D-05/D-06: RED contracts freeze the seven-field review schema, exact review-set equality, positive/negative predicates, score bounds, reason consistency, and immutable pre-review polarity/target expectation."
    - "D-07/D-08: RED UI/export contracts require a browser-local File API reviewer, exact positive-allowlist export, fixed filename, deterministic bytes, ephemeral object URLs, and zero network/storage/raw-sensitive output."
    - "D-09/D-10/D-11/D-12/D-13/D-14/D-15/D-16: three isolated reducers reject sibling borrowing; current closed outcomes and the upper-eyelid evidence-AND-design conjunction are executable successful results."
    - "The checker owns a closed UI inventory of exactly 27 rows = 8 UI consideration truths + 19 executable UI acceptance criteria; missing, duplicate, extra, or reclassified rows fail self-test."
  artifacts:
    - path: .planning/phases/54-rights-approved-evidence-and-eligibility-decisions/54-evidence-core.test.js
      provides: RED Node mutation suite for all six Phase 54 requirements and D-01 through D-16
    - path: .planning/phases/54-rights-approved-evidence-and-eligibility-decisions/54-review.contract.test.js
      provides: RED source/DOM/controller contract for all UI considerations and acceptance criteria
    - path: .planning/phases/54-rights-approved-evidence-and-eligibility-decisions/check_phase54_evidence_boundaries.py
      provides: Fail-closed self/live privacy, export, scope, Git, owner, and UI inventory checker
  key_links:
    - from: .planning/phases/54-rights-approved-evidence-and-eligibility-decisions/54-evidence-core.test.js
      to: .planning/phases/54-rights-approved-evidence-and-eligibility-decisions/54-evidence-core.js
      via: CommonJS import of the future phase-owned pure API
      pattern: "require.*54-evidence-core"
    - from: .planning/phases/54-rights-approved-evidence-and-eligibility-decisions/54-review.contract.test.js
      to: .planning/phases/54-rights-approved-evidence-and-eligibility-decisions/54-review.html
      via: dependency-free document/source contract assertions
      pattern: "54-review\\.html|manifest-input|export-review"
    - from: .planning/phases/54-rights-approved-evidence-and-eligibility-decisions/54-review.contract.test.js
      to: .planning/phases/54-rights-approved-evidence-and-eligibility-decisions/54-review-controller.js
      via: external-controller, CSP linkage, state, and File API contract assertions
      pattern: "54-review-controller\\.js|script-src|ReviewCore"
---

<objective>
Author the complete Wave 0 RED contract and fail-closed boundary checker before the evidence core, reviewer, or decision ledger exists.

Purpose: Make every locked decision, mutation, privacy prohibition, and UI contract executable before GREEN implementation.
Output: Two built-in Node test suites plus one standard-library Python checker whose self-test is green while live/core/UI suites are RED only for absent planned artifacts.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/54-rights-approved-evidence-and-eligibility-decisions/54-CONTEXT.md
@.planning/phases/54-rights-approved-evidence-and-eligibility-decisions/54-RESEARCH.md
@.planning/phases/54-rights-approved-evidence-and-eligibility-decisions/54-PATTERNS.md
@.planning/phases/54-rights-approved-evidence-and-eligibility-decisions/54-UI-SPEC.md
@.codex/skills/spike-findings-beauty/references/licensed-fixture-evaluation.md
</context>

<tasks>

<task id="54-01-01" type="auto" tdd="true">
  <name>Task 1: Specify the immutable one-feature evidence, review, reducer, and export core</name>
  <read_first>
    .planning/phases/54-rights-approved-evidence-and-eligibility-decisions/54-CONTEXT.md
    .planning/phases/54-rights-approved-evidence-and-eligibility-decisions/54-RESEARCH.md
    .planning/phases/54-rights-approved-evidence-and-eligibility-decisions/54-PATTERNS.md
    .codex/skills/spike-findings-beauty/sources/006-licensed-fixture-review-gate/test-review-core.js
    .codex/skills/spike-findings-beauty/sources/006-licensed-fixture-review-gate/review-core.js
    example-images/FIXTURE_AUTHORIZATION.md
  </read_first>
  <files>.planning/phases/54-rights-approved-evidence-and-eligibility-decisions/54-evidence-core.test.js</files>
  <behavior>
    - D-01/D-02/EVID-01: one manifest owns exactly one supported feature; current genuine approved positive and negative rows require predeclared polarity/expected target plus complete exact original/mask/after keys before the product bundle is ready, while valid partial bundles return a closed result rather than throwing.
    - D-03/EVID-02: mechanics_only rights and synthetic, ai_generated, disabled, parked, and historical roles remain valid tooling inputs but contribute zero eligible/reviewed/accepted counts and zero naturalness weight.
    - D-04: portrait_001 is never inferred from fixture identity; an explicit current genuine approved teeth-negative row still requires a complete triple and passing frozen review, and it can never satisfy the positive count.
    - D-05/D-06/EVID-03: session creation copies, stable-sorts, and deeply freezes selected rows; every selected row has exactly one review; scores are integers 1...5; positive and negative pass predicates and decision/reason compatibility are authoritative.
    - D-08/EVID-04: exporter constructs the exact top-level/row/decision/aggregate allowlists field by field, never accepts now/time/session/event arguments, and emits stable two-space LF JSON with one final newline.
    - D-09..D-12/EVID-05: teeth, sclera, and upper-eyelid decisions have isolated rows, denominators, reviews, reasons, and status; a sibling cannot open, close, or change another feature.
    - D-13/D-14/D-15/D-16/LID-01: upper eyelid opens only when evidence passes AND non-warp design is qualified; neither the invalidated interior warp nor any eye/brow/aperture/smoothing/dark-circle/eye-bag substitute qualifies, and both current closure reasons survive together.
  </behavior>
  <action>Create a dependency-free `node:test`/`node:assert/strict` suite of opaque in-memory manifests and one-property-at-a-time mutations. Import the future dual browser/Node `54-evidence-core.js` API and make absence of that module the only RED dependency. Before importing, detect that exact absent path and raise the stable marker `RED_MISSING_ARTIFACT:54-evidence-core.js`; no other setup or discovery failure may use that marker. Define exact test names aligned with the Mutation Matrix below and test pure values only—no real media, local path, rights ID, timestamp, reviewer, filesystem write, or product source. Assert deep immutability, input non-mutation, stable ordering, exact review-set equality, field-by-field allowlists, byte-identical repeat export, current reason semantics, and zero cross-feature contamination per D-01 through D-16.</action>
  <verify>
    <automated>node --check .planning/phases/54-rights-approved-evidence-and-eligibility-decisions/54-evidence-core.test.js &amp;&amp; sh -c 'set +e; out=$(node --test .planning/phases/54-rights-approved-evidence-and-eligibility-decisions/54-evidence-core.test.js 2&gt;&amp;1); status=$?; set -e; test "$status" -eq 1 &amp;&amp; test "$(printf "%s" "$out" | grep -c "RED_MISSING_ARTIFACT:54-evidence-core.js")" -ge 1 &amp;&amp; ! printf "%s" "$out" | grep -Eq "SyntaxError|ReferenceError|TypeError"'</automated>
  </verify>
  <done>The complete D-01..D-16/EVID-01..05/LID-01 mutation suite parses and runs, exits nonzero only because `54-evidence-core.js` is absent, and contains no sensitive or side-effecting test data.</done>
</task>

<task id="54-01-02" type="auto" tdd="true">
  <name>Task 2: Specify all UI states and create the fail-closed phase boundary checker</name>
  <read_first>
    .planning/phases/54-rights-approved-evidence-and-eligibility-decisions/54-UI-SPEC.md
    .planning/phases/54-rights-approved-evidence-and-eligibility-decisions/54-RESEARCH.md
    .planning/phases/54-rights-approved-evidence-and-eligibility-decisions/54-PATTERNS.md
    .planning/phases/53-canonical-still-image-contract-and-private-request-foundatio/check_still_image_foundation_boundaries.py
    SECURITY.md
    RELIABILITY.md
    .gitignore
  </read_first>
  <files>.planning/phases/54-rights-approved-evidence-and-eligibility-decisions/54-review.contract.test.js, .planning/phases/54-rights-approved-evidence-and-eligibility-decisions/check_phase54_evidence_boundaries.py</files>
  <behavior>
    - UI-CONSIDERATION-01..08 are a closed inventory: empty, loading, error, populated, partial, overflow, zero-one-many, and long-text states each retain the approved fail-closed/redacted/responsive truth.
    - UI-AC-01..19 are a closed inventory: initial boundary; local-only operation; redacted invalid input; blinded display; original detail; required judgments; no implicit approval; progress/navigation; mechanics exclusion; independent gates; frozen acceptance; upper-eyelid decision; closed-result export; determinism; export privacy; ephemeral media; accessibility; responsive behavior; and production scope boundary.
    - The checker treats missing required files, unreadable UTF-8, JSON parse errors, command errors, unclassified matches, missing/duplicate/extra inventory rows, and scan exit codes other than exact clean/match meanings as failures.
    - Live modes are explicit: `--core`, `--ui`, `--ledger`, `--owners`, `--scope`, and default all-implementation checks; `--self-test` mutates every critical class without printing matched file contents or sensitive local paths.
  </behavior>
  <action>Create `54-review.contract.test.js` with dependency-free source/structure/controller assertions for every stable selector, fixed copy, semantic region/control, seven unselected judgments, state transition, object-URL lifecycle, CSP/privacy token, deterministic export, responsive breakpoint, focus rule, and UI-AC/UI-CONSIDERATION ID. It must check for the exact absent HTML/controller pair before reading either and raise only `RED_MISSING_ARTIFACT:54-review.html,54-review-controller.js`; no syntax, discovery, or assertion failure may use that marker. Create `check_phase54_evidence_boundaries.py` using the Phase 53 immutable Rule/fail-closed subprocess pattern. Self-test must operate in temporary synthetic trees and cover required-file removal, scanner error, malformed JSON/JS/HTML, each forbidden export sentinel/key, each forbidden reviewer network/storage/unsafe-DOM/external-resource token, unsafe path/ID, Git tracking/ignore failure, current-ledger drift, owner/scope drift, and exact `27 = 8 + 19` UI row equality. Add a deterministic `--expect-wave0-red` mode that internally runs the default live rules and exits zero only when the complete, exact allowlist of not-yet-created implementation/ledger/ignore/owner prerequisites is reported and no parse, subprocess, scanner, or unclassified failure is present; it must fail if any expected reason is absent or any extra reason appears. Live scans must prove `.codex` Spike 006 remains unchanged, phase artifacts are the only new executable owner, sensitive local review paths are ignored/untracked, and `BeautySDK/`/`BeautyDemo/` have no candidate field, admission, provider, renderer, preset, realtime, or reviewer import. Do not edit `.gitignore` or any implementation artifact in Wave 0.</action>
  <verify>
    <automated>PYTHONPYCACHEPREFIX=/private/tmp/beauty-phase54-pycache python3 -m py_compile .planning/phases/54-rights-approved-evidence-and-eligibility-decisions/check_phase54_evidence_boundaries.py &amp;&amp; python3 .planning/phases/54-rights-approved-evidence-and-eligibility-decisions/check_phase54_evidence_boundaries.py --self-test &amp;&amp; python3 .planning/phases/54-rights-approved-evidence-and-eligibility-decisions/check_phase54_evidence_boundaries.py --expect-wave0-red &amp;&amp; node --check .planning/phases/54-rights-approved-evidence-and-eligibility-decisions/54-review.contract.test.js &amp;&amp; sh -c 'set +e; out=$(node --test .planning/phases/54-rights-approved-evidence-and-eligibility-decisions/54-review.contract.test.js 2&gt;&amp;1); status=$?; set -e; test "$status" -eq 1 &amp;&amp; test "$(printf "%s" "$out" | grep -c "RED_MISSING_ARTIFACT:54-review.html,54-review-controller.js")" -ge 1 &amp;&amp; ! printf "%s" "$out" | grep -Eq "SyntaxError|ReferenceError|TypeError"' &amp;&amp; git diff --check</automated>
  </verify>
  <done>The checker self-test passes every clean/mutation case including exact 27-row UI equality; live/default and UI contract suites fail only for absent planned core/UI/ledger/ignore/owner artifacts; no SDK, Demo, packaged-spike, or root-contract change occurs.</done>
</task>

</tasks>

<mutation_matrix>
## Closed Mutation Matrix

Every row is owned by `54-evidence-core.test.js`, `54-review.contract.test.js`, or checker `--self-test`; no group may be sampled away.

| Group | Required independent mutations |
|---|---|
| Paths and identity | absolute path; `../`; dot segment; backslash; colon; NUL; duplicate opaque fixture ID; invalid/empty/over-64 opaque ID; duplicate normalized asset key; basename collision; exact-key mismatch; no selected directory |
| Manifest/enums/completeness | unsupported feature, polarity, rights status, evidence role, decision, review reason, or gate reason; mixed feature; missing original; missing mask; missing after; missing rights record; polarity not predeclared; missing positive; missing negative; over 64 rows; oversized manifest |
| Product eligibility | mechanics_only rights; synthetic; ai_generated; disabled; parked; historical; rejected rights; portrait authorization without feature polarity; sibling positive; sibling negative; mechanics denominator/naturalness contamination |
| Frozen review | manifest/polarity/expected-target mutation after session start; duplicate review; missing review; extra review; score 0; score 6; fractional score; unset field; accept with target mismatch, positive coverage below 4, leakage, naturalness below 4, structure change, or rejection reason; reject with `none`; accept with non-none reason |
| Export/privacy | forbidden dataset, generated_at, timestamp, event, metadata, reviewer, notes/freeform, filename/path/directory, rights/documentation/retention, original/mask/after, media/blob, coordinates/landmarks/pupils/descriptors/raw geometry, raw error sentinel at every depth; spread/clone input; changed order/indent/LF/final newline; dynamic filename |
| UI/runtime/scope | fetch/XHR/WebSocket/EventSource/beacon/WebRTC; HTTP(S)/CDN/font/dependency; local/session storage, IndexedDB, Cache API, cookie, service worker, clipboard, worker, form submission; unsafe HTML sink; raw exception/value rendering; missing CSP; missing selector/label/live region; default judgment; unreleased object URL; missing breakpoint/44px/overflow guard; SDK/Demo candidate or reviewer import |
| Upper eyelid | evidence missing only; non-warp design missing only; both missing; invalidated warp offered; eyeHeight/upperEyelidLift/brow/aperture/global smoothing/dark-circle/eye-bag substitute offered; both genuine bundle and credible independent non-warp design passing |
</mutation_matrix>

<threat_model>
## ASVS Level 1 Completion Gate

`security_standard: OWASP ASVS Level 1`; `block_on: HIGH`. Any failed, skipped, not-run, or unverified HIGH mitigation forbids task/plan completion and summary publication.

| Threat ID | STRIDE | Severity | Disposition | Verifiable mitigation |
|---|---|---:|---|---|
| T-54-01 | Tampering | HIGH | mitigate | RED structural/path/enum/identity mutations plus checker fail-closed command classification |
| T-54-02 | Tampering / Repudiation | HIGH | mitigate | frozen snapshot, exact review-set, predicate-consistency, sibling-borrowing, and eyelid-conjunction RED tests |
| T-54-03 | Information Disclosure | HIGH | mitigate | deep export sentinel/key suite and reviewer privacy/source mutations |
| T-54-04 | Elevation of Privilege | HIGH | mitigate | unsafe-DOM/external-source/CSP and active-format rejection contracts |
| T-54-05 | Denial of Service | HIGH | mitigate | manifest/row/file/dimension budget contracts and missing/decode/dimension fail-closed UI rows |
| T-54-06 | Tampering | HIGH | mitigate | production-scope and packaged-spike immutability checker mutations |
| T-54-SC | Tampering | LOW | accept | no package install, registry, CDN, or new dependency is permitted |
</threat_model>

<verification>
Checker `--self-test` passes; both Node suites and checker live mode are expected RED only for absent downstream Phase 54 artifacts; `git diff --check` passes; no production or root-owner file is modified.
</verification>

<success_criteria>
All six requirements, D-01 through D-16, the complete mutation matrix, exactly 8 UI considerations, exactly 19 UI acceptance criteria, all privacy/source/scope prohibitions, and every HIGH threat have executable Wave 0 owners before GREEN implementation.
</success_criteria>

<output>
Create `.planning/phases/54-rights-approved-evidence-and-eligibility-decisions/54-01-SUMMARY.md` when done.
</output>
