---
phase: 67-swiftpm-consumer-and-cli-validation-contract
plan: "03"
type: execute
wave: 3
depends_on: ["67-02"]
files_modified:
  - BeautySDK/Tests/BeautyCoreTests/BeautyExampleRendererProcessTests.swift
autonomous: true
requirements: [CLI-03]
must_haves:
  truths:
    - "SwiftPM tests compile and launch the real BeautyExampleRenderer through Foundation Process; no parser-only helper can satisfy D-12."
    - "Success/list/reproducibility and every deterministic argument/input/output/report failure assert process exit, typed JSON code, output absence/presence, and reconciled counts (D-09 through D-11)."
    - "The compiled executable's internal seam independently reaches render_failed and encode_failed without entering public BeautySDK or the documented CLI."
    - "A nonexistent explicit output directory and each duplicated scalar flag fail before rendering with exact typed behavior."
  artifacts:
    - path: "BeautySDK/Tests/BeautyCoreTests/BeautyExampleRendererProcessTests.swift"
      provides: "Real-binary success, deterministic failure injection, and invalid-matrix coverage"
      contains: "Process()"
  key_links:
    - from: "BeautyExampleRendererProcessTests.swift"
      to: "BeautyExampleRenderer"
      via: "SwiftPM build followed by bounded Foundation Process invocation"
      pattern: "BeautyExampleRenderer"
    - from: "BeautyExampleRendererProcessTests.swift"
      to: "beauty-example-renderer-report.json"
      via: "JSON decode and count/privacy assertions"
      pattern: "beauty-example-renderer-report\\.json"
    - from: "BeautyExampleRendererProcessTests.swift"
      to: "RendererFailureInjection"
      via: "process-local environment seam for render and encode failure branches"
      pattern: "render|encode"
---

<objective>
Prove the compiled renderer's complete success and typed-failure contract at the
process boundary, including deterministic render/encode failures, missing output
directory handling, and duplicate scalar flags.

Purpose: Satisfy CLI-03 through executable behavior rather than parser
duplication or unreachable error declarations.
Output: One generated-temp XCTest process matrix against the real SwiftPM-built
binary.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@SECURITY.md
@RELIABILITY.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/67-swiftpm-consumer-and-cli-validation-contract/67-CONTEXT.md
@.planning/phases/67-swiftpm-consumer-and-cli-validation-contract/67-VALIDATION.md
@.planning/phases/67-swiftpm-consumer-and-cli-validation-contract/67-02-SUMMARY.md
@BeautySDK/Package.swift
@BeautySDK/Sources/BeautyExampleRenderer/main.swift
@BeautySDK/Sources/BeautyExampleRenderer/RendererCLIContract.swift
@BeautySDK/Sources/BeautyExampleRenderer/RendererExecution.swift
@BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift
</context>

<tasks>

<task id="67-03-01" type="auto">
  <name>Task 1: Exercise the actual compiled renderer across success and deterministic failure matrices</name>
  <read_first>BeautySDK/Package.swift; all BeautyExampleRenderer target sources; BeautyRendererOutputRegressionTests.swift; 67-VALIDATION.md; Phase 67 CONTEXT decisions D-05 through D-12</read_first>
  <files>BeautySDK/Tests/BeautyCoreTests/BeautyExampleRendererProcessTests.swift</files>
  <action>Create XCTest process tests that first invoke `swift build --package-path BeautySDK --product BeautyExampleRenderer` through Foundation `Process`, resolve the resulting bin directory through `swift build --show-bin-path`, require the resolved `BeautyExampleRenderer` to be a regular executable, and then invoke only that compiled binary. Cache that executable URL once per test class without importing the executable module or exposing its internal parser. Bound the one build process to 120 seconds and each renderer invocation to 30 seconds, terminate on timeout, and cap captured stdout/stderr separately at 1 MiB. Every test creates a unique temporary tree, generates small named-sRGB PNG/JPEG inputs entirely with Swift CoreGraphics/ImageIO, explicitly creates each success output directory before invocation, and removes the tree in `defer`; no tracked media, portrait, absolute path, or transcript is retained. Per D-12, all assertions observe process environment/exit/files/stdout/stderr and never call or reimplement `RendererArguments` or `RendererExecution`.

Cover: (1) `--list-cases` twice yields byte-identical versioned JSON with the exact ordered 74 IDs; (2) two fresh explicit `--input`, existing `--output`, `--case geometryBaseline_noop`, `--backend cpu`, `--no-watermark` runs both exit zero, produce byte-identical non-empty decodable same-dimension PNGs and byte-identical reports, and reconcile 1/1/0/0 counts; (3) report and diagnostic keys/values contain no temporary absolute prefix or forbidden path/landmark/mask/pixel/coordinate/private-fixture tokens; (4) unknown flag, missing flag value, missing `--output`, nonexistent explicit output directory, output path that is a file, unknown case, literal `gpu`, another unknown backend, missing input directory, empty input directory, normalized duplicate stems, corrupt supported-extension image, output-destination collision, and report-path collision each return nonzero with the exact stable diagnostic code; (5) independently repeat `--input`, `--output`, `--case`, and `--backend` and require `duplicate_argument`, nonzero, no requested output, and no render attempt; (6) set the executable-internal environment seam first to `render` and then `encode`, and for each real compiled invocation assert exact typed stderr JSON `render_failed`/`encode_failed`, nonzero exit, no requested PNG, a written report with requested=1, succeeded=0, failed=1, skipped=0, and count reconciliation; and (7) failures that can write a report maintain requested = succeeded + failed + skipped, while report-write failure never returns success. This is the complete D-09/D-10/D-11 matrix. Assert the seam name/value is absent from `--help`, case-list JSON, render report, and diagnostics. Do not accept a skip, parser-only unit result, source-string-only assertion, or a forced failure that bypasses the compiled runner's normal diagnostic/report path.</action>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyExampleRendererProcessTests</automated></verify>
  <done>The real compiled binary passes deterministic discovery/success and every required invalid class, including independently injected render/encode failures, nonexistent output, and four duplicate scalar flags.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| XCTest → compiled executable | Tests must build and run the product, not a source/parser substitute. |
| Process environment → internal seam | A process-local test value reaches two failure branches but must not enter public SDK/CLI/report contracts. |
| Process → test harness | Exit status and captured output are untrusted, bounded inputs. |
| Temporary filesystem → report assertions | Generated inputs/outputs may be malformed and must not escape or persist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-08 | Spoofing | Executable lookup | mitigate | Resolve only the regular BeautyExampleRenderer file from SwiftPM's reported build-products path. |
| T-67-09 | Elevation of Privilege | Failure-injection seam | mitigate | Keep it executable-internal, environment-only, exact-valued, undocumented, absent from public SDK/help/report, and request local. |
| T-67-10 | Denial of Service | Process tests | mitigate | Enforce bounded build/render timeouts and 1 MiB stdout/stderr ceilings. |
| T-67-11 | Information Disclosure | Test/report evidence | mitigate | Generate fixtures in unique temporary roots, assert redaction, delete on every exit, and record only aggregate outcomes. |
| T-67-SC | Tampering | Build/test dependencies | mitigate | Use existing Apple frameworks and local SwiftPM products only; no remote install is authorized. |
</threat_model>

<verification>
The focused process suite compiles the executable, covers all success and failure
rows in `67-VALIDATION.md`, and produces no tracked or persistent test artifact.
</verification>

<success_criteria>
CLI-03 is achieved only when the real binary returns zero for complete validated
output and typed nonzero JSON diagnostics for every required invalid or forced
failure path, with privacy-safe reconciled reports wherever report writing is
possible.
</success_criteria>

<output>
Create `.planning/phases/67-swiftpm-consumer-and-cli-validation-contract/67-03-SUMMARY.md` when done.
</output>
