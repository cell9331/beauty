---
phase: 55-original-pixel-composition-and-failure-isolation-core
plan: "02"
type: execute
wave: 1
depends_on: [55-01]
files_modified:
  - BeautySDK/Sources/BeautyCore/Models/BeautyCanonicalStillImage.swift
  - BeautySDK/Sources/BeautyEffects/Render/BeautyLocalRetouchComposition.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautyLocalRetouchCompositionTests.swift
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, COMP-04]
security_standard: "OWASP ASVS Level 1"
block_on: HIGH
must_haves:
  truths:
    - "D-05/D-55-05 and D-06/D-55-06/COMP-02: every opaque unit is bound to the exact current canonical storage identity and checked dimensions, not merely to equal dimensions, equal bytes, a hash, or call order."
    - "D-07/D-55-07/COMP-01/03: dimensions, rows, bytes, counts, indices, offsets, unit totals, and claim totals are checked before allocation/ownership; malformed work rejects only its unit."
    - "D-10/D-55-10/COMP-04: duplicate raw indices are detected before hard/zero filtering, and duplicate or foreign owner tokens reject all affected occurrences without removing unrelated valid units."
    - "D-01/D-55-01 through D-04/D-55-04: adding the package core changes no production admission, public/SPI/provider/renderer/preset/Demo/realtime surface, and the checker remains fail-closed."
  artifacts:
    - path: BeautySDK/Sources/BeautyCore/Models/BeautyCanonicalStillImage.swift
      provides: Exact package-level immutable canonical storage binding
      contains: BeautyCanonicalPixelSourceBinding
    - path: BeautySDK/Sources/BeautyEffects/Render/BeautyLocalRetouchComposition.swift
      provides: Package-only opaque proposal/unit/token/owner contracts and unit-local preflight
      contains: BeautyLocalRetouchCompositionOwner
    - path: BeautySDK/Tests/BeautyEffectsTests/BeautyLocalRetouchCompositionTests.swift
      provides: Production-backed source-binding, arithmetic, token, cap, and duplicate tests
  key_links:
    - from: BeautySDK/Sources/BeautyEffects/Render/BeautyLocalRetouchComposition.swift
      to: BeautySDK/Sources/BeautyCore/Models/BeautyCanonicalStillImage.swift
      via: owner captures the canonical value plus exact `pixelSourceBinding`
      pattern: "pixelSourceBinding"
    - from: BeautySDK/Tests/BeautyEffectsTests/BeautyLocalRetouchCompositionTests.swift
      to: BeautySDK/Sources/BeautyEffects/Render/BeautyLocalRetouchComposition.swift
      via: replace Wave 0 missing seam with production-backed exact-carrier/foreign-carrier/preflight assertions
      pattern: "BeautyLocalRetouchCompositionOwner"
  prohibitions:
    - statement: "A byte digest, backing hash, dimensions alone, or a second unverified raster must not authorize a unit for the current request."
      status: resolved
      verification: foreign-byte-equal-carrier-tests
    - statement: "Unchecked arithmetic, dense full-frame owner allocation, anatomy identifiers, public/SPI unit types, or production admission changes are forbidden."
      status: resolved
      verification: source-binding-checker
---

<objective>
Implement the exact canonical-source binding, bounded opaque unit contracts, and fail-closed unit preflight that make later composition safe.

Purpose: Establish the irreversible trust boundary first: a proposal can participate only when it belongs to this request's immutable canonical carrier and passes every checked structural guard.
Output: One exact Core binding value, the feature-neutral BeautyEffects composition contract/preflight, and GREEN production-backed tests for validation rows 55-02-01 and 55-02-02.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/55-original-pixel-composition-and-failure-isolation-core/55-CONTEXT.md
@.planning/phases/55-original-pixel-composition-and-failure-isolation-core/55-RESEARCH.md
@.planning/phases/55-original-pixel-composition-and-failure-isolation-core/55-PATTERNS.md
@.planning/phases/55-original-pixel-composition-and-failure-isolation-core/55-VALIDATION.md
@.planning/phases/55-original-pixel-composition-and-failure-isolation-core/55-01-SUMMARY.md
@.codex/skills/spike-findings-beauty/references/still-image-integration.md

<interfaces>
Create and preserve these package-only, immutable, non-Codable contracts:
- `BeautyCanonicalPixelSourceBinding`: strong opaque storage-owned identity plus `width`, `height`, `rowBytes`, and `byteCount`; created only by `BeautyCanonicalStillImage.pixelSourceBinding` and compared by `===` plus layout equality.
- `BeautyLocalPixelProposal`: `pixelIndex`, `isInsideHardEnvelope`, `softWeightQ16`, `targetRed`, `targetGreen`, `targetBlue`.
- `BeautyLocalRetouchUnit`: exact source binding, opaque owner identity/token, and immutable proposals; no anatomy label or diagnostic representation.
- `BeautyLocalRetouchCompositionOwner`: `maximumUnitCount == 8`, `init(source:)`, `makeUnit(proposals:)`, and later `compose(_:)`.
</interfaces>
</context>

<artifacts_this_phase_produces>
- `BeautyCanonicalPixelSourceBinding` and `BeautyCanonicalStillImage.pixelSourceBinding`.
- `BeautyLocalPixelProposal`, opaque token/unit values, `BeautyLocalRetouchCompositionOwner.maximumUnitCount`, `init(source:)`, and `makeUnit(proposals:)`.
- Checked unit-preflight helpers for dimensions, rows, bytes, pixel offsets, caps, source binding, token frequency, and duplicate raw indices.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 55-02-01: Bind the owner and proposals to exact canonical storage with checked arithmetic</name>
  <read_first>
    BeautySDK/Sources/BeautyCore/Models/BeautyCanonicalStillImage.swift
    BeautySDK/Sources/BeautyEffects/Render/BeautyLocalRetouchComposition.swift
    BeautySDK/Tests/BeautyEffectsTests/BeautyLocalRetouchCompositionTests.swift
    BeautySDK/Package.swift
    .planning/phases/55-original-pixel-composition-and-failure-isolation-core/55-CONTEXT.md
    .planning/phases/55-original-pixel-composition-and-failure-isolation-core/55-RESEARCH.md
    .planning/phases/55-original-pixel-composition-and-failure-isolation-core/55-PATTERNS.md
  </read_first>
  <files>BeautySDK/Sources/BeautyCore/Models/BeautyCanonicalStillImage.swift, BeautySDK/Sources/BeautyEffects/Render/BeautyLocalRetouchComposition.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyLocalRetouchCompositionTests.swift</files>
  <action>Add `BeautyCanonicalPixelSourceBinding` in BeautyCore with a strong opaque storage-owned identity and checked width/height/rowBytes/byteCount fields, exposed only as package `BeautyCanonicalStillImage.pixelSourceBinding`; compare identities with `===`, and retain the existing hash-based `backingIdentity` solely for prior tests, never Phase 55 authorization. Create the package-only, immutable, Sendable, non-Codable `BeautyLocalPixelProposal`, opaque unit token/value, and `BeautyLocalRetouchCompositionOwner` in BeautyEffects. The owner captures the exact canonical value and binding, computes `pixelCount`, row and byte relations with reporting-overflow APIs, and never accepts caller-supplied source bytes. Every proposed pixel offset uses checked `pixelIndex * 4` and checked channel offsets before access. Replace the Wave 0 test-local missing source seam with real production types while retaining literal independent oracles. Make exact-carrier copies eligible and a distinct same-size/same-byte carrier locally rejected; the request-level canonical constructor error remains payload-free `BeautyError.invalidInput` and is not converted into a unit rejection. Do not add a target/dependency, dense pixel-sized ownership array, public/SPI/Codable conformance, digest, description, feature enum, or admission route.</action>
  <verify>
    <automated>CLANG_MODULE_CACHE_PATH=/private/tmp/beauty-phase55-clang-module-cache swift test --package-path BeautySDK --filter BeautyLocalRetouchCompositionTests &amp;&amp; python3 .planning/phases/55-original-pixel-composition-and-failure-isolation-core/check_phase55_composition_boundaries.py --source-binding &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `BeautyCanonicalPixelSourceBinding` strongly retains an opaque storage-owned identity, compares it with `===` plus the four checked layout fields, and is package-only/non-Codable.
    - A unit from the exact carrier is eligible; a unit from a distinct byte-equal/dimension-equal carrier is rejected without affecting an exact-carrier sibling.
    - Overflow-shaped dimensions/row/byte/index/offset mutations fail before allocation or access and produce no raw-value diagnostic.
    - The focused source-binding checker is green and production admission/inventories remain exact-empty/59/5/72.
  </acceptance_criteria>
  <done>Validation row `55-02-01` is green: exact canonical storage binding and all checked layout/index arithmetic are production-backed without widening the package boundary.</done>
</task>

<task type="auto">
  <name>Task 55-02-02: Issue opaque request-local units and reject foreign, duplicate, or over-budget work locally</name>
  <read_first>
    BeautySDK/Sources/BeautyEffects/Render/BeautyLocalRetouchComposition.swift
    BeautySDK/Sources/BeautyCore/Models/BeautyCanonicalStillImage.swift
    BeautySDK/Tests/BeautyEffectsTests/BeautyLocalRetouchCompositionTests.swift
    .planning/phases/55-original-pixel-composition-and-failure-isolation-core/55-VALIDATION.md
    .planning/phases/55-original-pixel-composition-and-failure-isolation-core/check_phase55_composition_boundaries.py
  </read_first>
  <files>BeautySDK/Sources/BeautyEffects/Render/BeautyLocalRetouchComposition.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyLocalRetouchCompositionTests.swift</files>
  <action>Complete owner-local issuance and preflight. Use an opaque owner identity plus monotonically issued request-local token; accept no more than `effectiveUnitLimit = min(BeautyLocalRetouchCompositionOwner.maximumUnitCount, pixelCount)` with `maximumUnitCount == 8`. Bound each unit's raw proposal count to `1...max(1, pixelCount / effectiveUnitLimit)` so total accepted raw claims cannot exceed pixelCount; calculate all limits with checked arithmetic. `makeUnit(proposals:)` binds the exact owner/source and immutable proposals without filtering. Before ownership reduction, validate exact source/owner token, token occurrence frequency across the compose input, proposal count, pixel index/offset, and duplicate raw indices; duplicates remain invalid even when one claim would later be hard-false or zero-weight. A foreign unit, duplicated token supplied twice, over-budget unit, invalid index, or raw duplicate increments only aggregate rejected-unit accounting in the later result and removes that unit/all duplicated occurrences while preserving unrelated unique units. Keep rejection reasons internal and payload-free; do not expose token values, source identity, indices, proposal values, or anatomy through SPI/logs/descriptions. Extend the production-backed tests for effective limit, issuance exhaustion, every cap edge, foreign owner/source, duplicated token frequency, duplicate-before-filter ordering, and unrelated valid sibling survival.</action>
  <verify>
    <automated>CLANG_MODULE_CACHE_PATH=/private/tmp/beauty-phase55-clang-module-cache swift test --package-path BeautySDK --filter BeautyLocalRetouchCompositionTests &amp;&amp; python3 .planning/phases/55-original-pixel-composition-and-failure-isolation-core/check_phase55_composition_boundaries.py --source-binding &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Unit count is bounded by min(8, pixelCount) and per-unit claims by max(1, pixelCount/effectiveUnitLimit), with checked arithmetic and no dense owner allocation.
    - Raw duplicate detection occurs before hard/zero filtering; duplicated token occurrences are all rejected; foreign owner/source units reject locally.
    - Valid siblings remain eligible under each malformed/foreign/duplicate/over-budget mutation.
    - No token, source identity, index, proposal, anatomy, raw reason, or digest crosses public/SPI/Codable/logging boundaries.
  </acceptance_criteria>
  <done>Validation row `55-02-02` is green: opaque request-local issuance and fail-closed unit preflight bound all work and isolate every structural rejection.</done>
</task>

</tasks>

<threat_model>
## ASVS Level 1 Threat Mapping

`security_standard: OWASP ASVS Level 1`; `block_on: HIGH`. Focused tests and `--source-binding` must make the owning T-55-01/02/03 mitigations green; the checker must also keep T-55-04..07 scope guards intact. Any failed, missing, skipped, or unclassified HIGH result blocks the plan.

| Threat ID | STRIDE | Severity | Disposition | Mitigation plan |
|---|---|---:|---|---|
| T-55-01 | Spoofing / Tampering | HIGH | mitigate | strong lifetime-retained canonical/owner identity equality, foreign byte-equal negative tests, and stale-unit churn regression |
| T-55-02 | Tampering / Denial of Service | HIGH | mitigate | checked layout/index/offset arithmetic, min(8,pixels) units, bounded total sparse claims |
| T-55-03 | Tampering | HIGH | mitigate | pre-filter raw duplicate rejection and duplicate-token frequency rejection with sibling retention |
| T-55-04 | Tampering | HIGH | mitigate | preserve Wave 0 collision-to-source contract; no priority field or branch introduced |
| T-55-05 | Information Disclosure | HIGH | mitigate | package-only non-Codable contracts and exact sensitive-surface scan |
| T-55-06 | Tampering / Information Disclosure | HIGH | mitigate | owner/source/token values are request-local immutable values with no engine storage |
| T-55-07 | Elevation of Privilege / Tampering | HIGH | mitigate | checker pins exact-empty admission and unchanged target/dependency/candidate/Demo/realtime inventories |
</threat_model>

<verification>
The complete focused composition suite passes production-backed source-binding/preflight cases; checker source-binding mode and diff hygiene pass. No full SwiftPM or Demo command appears here; Task 55-05-01 owns them.
</verification>

<success_criteria>
The composition core can identify exactly which request/canonical source owns each opaque unit, bound all work before allocation, and reject malformed/foreign/duplicate units without exposing mechanics or changing production admission.
</success_criteria>

<output>
Create `.planning/phases/55-original-pixel-composition-and-failure-isolation-core/55-02-SUMMARY.md` when done.
</output>
