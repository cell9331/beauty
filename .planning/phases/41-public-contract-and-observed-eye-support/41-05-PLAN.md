---
phase: 41-public-contract-and-observed-eye-support
plan: "05"
type: execute
wave: 4
depends_on: ["41-04"]
files_modified:
  - BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift
  - BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift
  - BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift
  - BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautyFaceGeometryAdapterTests.swift
  - BeautySDK/Tests/BeautyDetectionTests/FaceObservationMappingTests.swift
  - .planning/phases/41-public-contract-and-observed-eye-support/41-VALIDATION.md
  - DESIGN.md
  - SECURITY.md
  - PLANS.md
autonomous: true
requirements: [EYE-06]
must_haves:
  truths:
    - "BeautyEyeSemanticSupport stores deterministic image-normalized span (width,height) and a signed, bounded tilt derived from canonical inner/outer geometry; reversing or rotating contour winding cannot change either value."
    - "The adapter rejects side-inverted observed pairs using both mapped contour centers projected onto a CoordinateMapper-derived anatomical horizontal axis, while valid up/right/left/down and input-mirrored frames retain their original left/right identity and remain eligible."
    - "Every locked contour, pupil, and paired-ratio predicate has an executable exact, just-inside, and just-outside fixture; mathematically overlapping guards are tested both as isolated pure predicates and in composed fail-closed precedence, and invalid pairs never discard valid contour support."
    - "41-VALIDATION.md records completed Wave 0 artifacts, green focused/full commands, and an explicit Nyquist sign-off for EYE-06."
  artifacts:
    - "BeautyEyeSemanticSupport exposes only package-internal span/tilt value semantics and no public, Codable, diagnostic, or persistent geometry surface."
    - "BeautyFaceGeometryAdapter performs deterministic span/tilt derivation and fail-closed side-inversion validation before any provider consumes support."
    - "BeautyFaceGeometryAdapterTests and FaceObservationMappingTests contain the complete boundary matrix, winding/orientation/mirror fixtures, and side-inversion rejection assertions."
    - "41-VALIDATION.md is a completed validation ledger with every task row and Wave 0 requirement checked against recorded commands."
  key_links:
    - "VisionFaceDetector computes mapped left/right contour centroids, obtains the mapped face-local left-to-right unit axis through the same CoordinateMapper metadata, and marks side order canonical only when `dot(rightCenter-leftCenter, anatomicalAxis) > epsilon`; zero/negative separation, missing/duplicate sides, or non-finite input is invalid."
    - "Validated contour -> canonical center/span/tilt -> BeautyEyeSemanticSupport remains request-scoped and package-internal; no Phase 42 provider transform, cap, emission, or facade routing is introduced."
    - "Threshold fixtures exercise the same adapter constants used in production, and paired pupil failures retain contour support while clearing only pupil-dependent eligibility."
---

# Plan 41-05: Close EYE-06 semantic and boundary evidence gaps

<objective>
Close the verifier's EYE-06 gaps without expanding Phase 41 into provider behavior: add canonical span/tilt evidence, reject genuinely side-inverted observations without breaking orientation/mirror identity, execute the full strict-boundary matrix, and sign the phase validation ledger.
</objective>

<execution_context>
@/Users/yakangwang/.antigravity_cockpit/instances/codex/cli-037507efedd7/gsd-core/workflows/execute-plan.md
@/Users/yakangwang/.antigravity_cockpit/cli-037507efedd7/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/41-public-contract-and-observed-eye-support/41-CONTEXT.md
@.planning/phases/41-public-contract-and-observed-eye-support/41-RESEARCH.md
@.planning/phases/41-public-contract-and-observed-eye-support/41-PATTERNS.md
@.planning/phases/41-public-contract-and-observed-eye-support/41-03-PLAN.md
@.planning/phases/41-public-contract-and-observed-eye-support/41-VERIFICATION.md
@.planning/phases/41-public-contract-and-observed-eye-support/41-VALIDATION.md
@BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift
@BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift
@BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift
@BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift
@BeautySDK/Tests/BeautyEffectsTests/BeautyFaceGeometryAdapterTests.swift
@BeautySDK/Tests/BeautyDetectionTests/FaceObservationMappingTests.swift
</context>

<scope_fence>
This correction plan owns only EYE-06 semantic support, side-order validation, its XCTest boundary evidence, and the Phase 41 validation ledger. Do not implement eye vectors, provider emissions, caps, resolver convergence, facade output, renderer/gallery evidence, ledger promotion, device/commercial behavior, or any Phase 42 transform.
</scope_fence>

<tasks>

<task type="auto" tdd="true">
  <name>Task 41-05-01: Implement and exhaustively test EYE-06 gap closure</name>
  <files>BeautySDK/Tests/BeautyEffectsTests/BeautyFaceGeometryAdapterTests.swift, BeautySDK/Tests/BeautyDetectionTests/FaceObservationMappingTests.swift, BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift, BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift, BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift, BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift</files>
  <behavior>
    - Reversed and cyclically rotated versions of the same valid contour produce equal canonical span and signed tilt, including positive and negative tilt fixtures and repeated-call determinism.
    - A physically swapped `.left`/`.right` pair is rejected, while the same anatomical pair survives `.up`, `.right`, `.left`, `.down`, and `inputMirrored` mapping fixtures with labels unchanged through the production side-order derivation path.
    - Contour cardinality fixtures cover 5/6/7 and 15/16/17 points; unique-point fixtures cover 3/4/5; closed-unit fixtures cover `-epsilon/0/+epsilon` and `1-epsilon/1/1+epsilon` on both axes.
    - Width and height fixtures independently cover just-below/exact/just-above both lower and upper limits (0.04/0.50 width, 0.01/0.30 height); the isolated pure-area predicate covers below/equal/above 0.0004, and composed fixtures prove the dimension minima subsume the area floor without claiming an impossible all-other-invariants-valid case.
    - Pupil cardinality covers 0/1/2 points; the pure containment predicate covers just-inside/exact/just-outside each of the four 10%-expanded bounds; the pure ellipse-offset predicate covers below/exact/above 0.70; composed tests prove containment-edge points are still rejected when the stricter offset guard fails.
    - Paired width-ratio and height-ratio fixtures independently cover just-below/exact/just-inside/just-above both 0.50 and 2.00; invalid pairs clear both pupils only and retain both contour supports.
  </behavior>
  <action>Follow a single RED-GREEN-REFACTOR loop: first add every fixture listed in behavior, then implement until the focused suites pass. Extract small module-internal pure predicates for contour dimensions/area, pupil containment/ellipse offset, and paired ratios so `@testable` fixtures can exercise each locked boundary independently without weakening the composed validator. This is necessary because width >= 0.04 and height >= 0.01 mathematically subsume area >= 0.0004, and a 10%-expanded containment edge has ellipse offset >= 1.2; isolated predicate tests prove the locked thresholds while composed tests prove stricter guards still fail closed. Add package-internal `span` as image-normalized `(maxX-minX, maxY-minY)` and `tilt` as `atan2(inner.y-outer.y, abs(inner.x-outer.x))/(pi/2)` bounded to `-1...1`, rejecting non-finite derivations. In `VisionFaceDetector`, require exactly one `.left` and one `.right` support, map each contour point once, compute the two mapped centroids, and derive an orientation/mirror-aware anatomical horizontal axis by composing the face-local basis endpoints `(0,0)->(1,0)` through `visionBounds` and mapping those two non-landmark reference points with the same `CoordinateMapper` metadata (normalize the resulting vector; do not hand-roll orientation cases). Produce a package-private order result `.canonical` only when `dot(rightCenter-leftCenter, anatomicalAxis) > 0.000001`; zero/negative separation, missing/duplicate sides, a non-finite axis, or non-finite centers are `.invalid`. Carry the result through `BeautyFaceObservation` with no permissive default for an explicit observed payload. The adapter rejects `.invalid` before per-side validation, then derives semantic support and paired pupils. Production `.up/.right/.left/.down` and `isInputMirrored` fixtures must traverse this derivation rather than injecting a verdict. Keep nil observed support on the legacy proxy path, keep pupil-only failures field-local, and keep all geometry package-internal/non-Codable/non-diagnostic. Do not add provider vectors, emissions, caps, resolver behavior, or facade output.</action>
  <acceptance_criteria>The complete enumerated matrix is GREEN; span/tilt are exact and winding-independent; production-derived swapped order fails closed; all four orientations plus input mirror remain valid; and the full SwiftPM suite passes.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.BeautyFaceGeometryAdapterTests &amp;&amp; swift test --package-path BeautySDK --filter BeautyDetectionTests.FaceObservationMappingTests &amp;&amp; swift test --package-path BeautySDK</automated></verify>
  <done>EYE-06 semantic support, side-order rejection, and every locked exact/inside/outside boundary have executable passing evidence.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 41-05-02: Complete owner and Phase 41 validation evidence</name>
  <files>.planning/phases/41-public-contract-and-observed-eye-support/41-VALIDATION.md, DESIGN.md, SECURITY.md, PLANS.md</files>
  <behavior>
    - The validation ledger marks every 41-01 through 41-05 task, Wave 0 artifact, sampling check, and Nyquist sign-off complete only after the focused suites, full SwiftPM suite, and checked-in boundary helper both pass.
    - The ledger records the exact threshold matrix, canonical span/tilt and side-inversion commands, and the explicit Phase 42 non-claims.
  </behavior>
  <action>Update `41-VALIDATION.md` from draft to completed status (`nyquist_compliant: true`, `wave_0_complete: true`), add rows for 41-05-01/02 with their commands and measured evidence, check all prior task/Wave 0/sign-off boxes, and append a dated approval note. Synchronize `DESIGN.md` with the package-private span/tilt derivation and pre-mapping side-order invariant, `SECURITY.md` with fail-closed invalid/duplicate/missing side-order handling and unchanged raw-geometry privacy boundary, and `PLANS.md` with the verified EYE-06 gap closure plus Phase 42 non-claims. Preserve the fail-closed boundary commands and document any environment limitation honestly; do not claim device, visual, renderer, provider-cap, or commercial evidence.</action>
  <acceptance_criteria>Validation status is internally consistent with the executed summaries and verifier rerun: no pending rows or unchecked sign-off items remain, EYE-06 is evidenced, and Phase 42 remains the owner of transforms/caps.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK &amp;&amp; python3 .planning/phases/41-public-contract-and-observed-eye-support/check_eye_support_boundaries.py --self-test &amp;&amp; python3 .planning/phases/41-public-contract-and-observed-eye-support/check_eye_support_boundaries.py &amp;&amp; git diff --check</automated></verify>
  <done>The owning design/security/plan ledgers agree with implementation and 41-VALIDATION.md is a completed, auditable Nyquist ledger for Phase 41.</done>
</task>

</tasks>

<verification>
Run the focused RED/GREEN adapter and mapping suites, then the full SwiftPM suite, boundary helper self-test/live modes, and `git diff --check`. Re-run the Phase 41 verifier and require EYE-06 satisfied with no new public/persistent/diagnostic geometry or Phase 42 scope.
</verification>

<success_criteria>
EYE-06 is fully verified: private support contains deterministic span and signed tilt, swapped-side structures fail closed without rejecting orientation/mirror-valid observations, every locked threshold has exact/inside/outside executable evidence, and 41-VALIDATION.md is signed complete.
</success_criteria>
