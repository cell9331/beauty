---
phase: 41-public-contract-and-observed-eye-support
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift
  - BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift
  - BeautySDK/Tests/BeautyDetectionTests/VisionFaceDetectorTests.swift
  - BeautySDK/Tests/BeautyDetectionTests/FaceObservationMappingTests.swift
  - BeautySDK/Tests/BeautyDetectionTests/CoordinateMapperTests.swift
autonomous: true
requirements: [EYE-05]
must_haves:
  truths:
    - "One Vision landmark request captures left/right eye contours and optional pupils in request-scoped package data; absent pupils remain absent."
    - "Every Vision point crosses CoordinateMapper exactly once into finite closed-unit image-normalized coordinates with side identity preserved across orientation and input-mirror metadata."
    - "Detector summaries and failures expose only fixed reason codes and aggregate counts; no raw Vision objects or point payloads survive the request."
  artifacts:
    - "VisionDetectionObservation and BeautyFaceObservation carry a package-private Sendable observed-eye support value."
    - "VisionFaceDetector maps contours/pupils through the existing mapper boundary and validates mapped coordinates."
    - "VisionFaceDetectorTests, FaceObservationMappingTests, and CoordinateMapperTests cover conversion, orientation/mirror, malformed points, and redaction."
  key_links:
    - "VNFaceLandmarks2D regions -> VisionDetectionObservation -> CoordinateMapper -> BeautyFaceObservation are a single conversion path."
    - "Optional pupil data is not synthesized and remains independently visible to the adapter eligibility seam."
---

<objective>
Thread private, frame-scoped observed eye contours and optional pupils through the existing Vision detector and coordinate-mapping boundary.

Purpose: Supply trustworthy evidence for later eye geometry without exposing biometric-adjacent points or duplicating orientation math.
Output: Package-only observation payloads, one mapper conversion path, and deterministic detection tests.
</objective>

## Artifacts this phase produces

- Package-private `BeautyObservedEyeSide` and `BeautyObservedEyeSupport` (`Equatable`, `Sendable`, non-Codable) carried by `VisionDetectionObservation` and `BeautyFaceObservation`.
- Vision detector mapping updates plus expanded `VisionFaceDetectorTests`, `FaceObservationMappingTests`, and `CoordinateMapperTests`.

<goal_backward>
Goal: the detector supplies trustworthy private eye evidence. Truths: one-request capture, one mapper conversion, bounded coordinates, side/orientation correctness, and redacted failures. Artifacts: package observation support and detection/mapping tests. Key links: Vision regions to mapped BeautyFaceObservation without retained raw objects.
</goal_backward>

<read_first>
AGENTS.md; PLANS.md; .planning/PROJECT.md; .planning/ROADMAP.md; .planning/REQUIREMENTS.md; .planning/STATE.md; 41-CONTEXT.md; 41-RESEARCH.md; VisionFaceDetector.swift; BeautyFaceObservation.swift; CoordinateMapper.swift; related detection tests
</read_first>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/41-public-contract-and-observed-eye-support/41-CONTEXT.md
@.planning/phases/41-public-contract-and-observed-eye-support/41-RESEARCH.md
@BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift
@BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift
@BeautySDK/Sources/BeautyDetection/CoordinateMapper.swift
</context>

## Multi-source coverage audit

| Source | Coverage in this plan |
| --- | --- |
| ROADMAP goal | Validated private contour/pupil support is acquired without public or diagnostic leakage; public scalars are Plan 01 and adapter validation is Plan 03. |
| Requirements | EYE-05 covers private left/right contours and optional pupils, one conversion boundary, orientation/mirror correctness, and redacted detector behavior. |
| Research constraints | Reuse one VNDetectFaceLandmarksRequest, existing CoordinateMapper, package visibility, Sendable value semantics, and injected fixtures; add no target/dependency/model/network. |
| CONTEXT — Public Scalar Contract | No public field changes here; support remains separate from scalar Codable state. |
| CONTEXT — Observed Eye-Support Model | Capture both anatomical sides plus optional pupils in one request-scoped representation, map once to image-normalized coordinates, and never synthesize absent pupils. |
| CONTEXT — Validation and Degradation | Reject non-finite/out-of-unit mapped values at the boundary; leave contour/pupil eligibility decisions to Plan 03 without globally gating contour support on pupils. |
| CONTEXT — Privacy and Ownership | Keep raw/derived coordinates package-internal, non-Codable, ephemeral, and absent from logs, warnings, errors, descriptions, snapshots, Demo imports, and persistence. |
| CONTEXT — the agent's Discretion | Follow existing observation/provider seams and use explicit side labels plus deterministic fixture payloads; exact contour thresholds remain adapter-owned. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 41-02-01: Define request-scoped observed-eye support and Vision payload capture</name>
  <files>BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift, BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift</files>
  <behavior>
    - Left and right contours are represented independently with explicit anatomical side labels and optional pupils.
    - Vision acquisition uses the existing single landmark request and releases Vision objects after mapping.
    - Pupil absence remains nil; no symmetric or synthetic pupil is created.
  </behavior>
  <action>Introduce package-private Equatable/Sendable `BeautyObservedEyeSide` and `BeautyObservedEyeSupport` beside BeautyFaceObservation, thread optional support through VisionDetectionObservation and mapObservation, and extend the existing landmarks(from:) payload to read eye and pupil regions from the same VNFaceLandmarks2D request. Keep support non-Codable and out of public APIs; map only request-scoped values and retain existing coarse landmark availability semantics.</action>
  <acceptance_criteria>Injected and default Vision paths expose mapped left/right contours with optional pupils only inside package observations, using one landmark request and no retained Vision objects.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyDetectionTests.VisionFaceDetectorTests</automated></verify>
  <done>The detector can carry both observed eye sides and optional pupils without a second request or a retained raw Vision object.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 41-02-02: Enforce one coordinate conversion boundary and redacted mapping failures</name>
  <files>BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift, BeautySDK/Tests/BeautyDetectionTests/FaceObservationMappingTests.swift, BeautySDK/Tests/BeautyDetectionTests/CoordinateMapperTests.swift, BeautySDK/Tests/BeautyDetectionTests/VisionFaceDetectorTests.swift</files>
  <behavior>
    - Representative up/right/left/down and input-mirrored fixtures preserve image-normalized side identity and expected point values.
    - Non-finite or out-of-unit mapped points fail with the existing mapping reason and aggregate counts only.
    - Diagnostics scanners reject raw Vision names, point arrays, bounds, paths, or side-specific payloads.
  </behavior>
  <action>Map every contour and pupil point through the existing CoordinateMapper instance exactly once, validate finite closed-unit results before constructing package support, and preserve the existing summary failure codes. Extend deterministic orientation/mirror matrices and injected detector fixtures for valid, missing-pupil, malformed, and mapping-failure cases; assert only availability/reason/face counts and run the existing no-raw-diagnostics scanner.</action>
  <acceptance_criteria>All four representative orientations and input mirroring produce expected image-normalized points; malformed mapping fails closed with redacted aggregate summaries.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyDetectionTests.CoordinateMapperTests &amp;&amp; swift test --package-path BeautySDK --filter BeautyDetectionTests.FaceObservationMappingTests &amp;&amp; swift test --package-path BeautySDK --filter BeautyDetectionTests.VisionFaceDetectorTests &amp;&amp; git diff --check</automated></verify>
  <done>EYE-05 conversion, side/orientation correctness, optional pupil semantics, and redacted failure behavior are proven at the detector boundary.</done>
</task>

</tasks>

<threat_model>
ASVS L1 applies to untrusted Vision landmark input crossing into package effects. Block high severity for coordinate tampering, oversized/malformed payloads, raw geometry disclosure, or a second provider/dependency path. No raw geometry or biometric-adjacent data may appear on public, diagnostic, or persistent surfaces. Mitigate with one mapper boundary, finite closed-unit validation, request-scoped Sendable values, optional pupil handling, fixed reason codes, and aggregate-only diagnostics; never persist or serialize support.
</threat_model>

<verification>
Run all three focused detection suites and the full SwiftPM suite after Plan 03. Perform source scans for public support symbols, Codable geometry, raw Vision descriptions, network/dependency changes, and retained/generated artifacts.
</verification>

<success_criteria>
Observed contours/pupils are mapped once into bounded package support with deterministic orientation/mirror behavior, absent pupils stay absent, and no raw geometry crosses diagnostics or public surfaces.
</success_criteria>

<output>Create `41-02-SUMMARY.md` with focused mapping/redaction counts and fixture coverage.</output>
