---
phase: 41-public-contract-and-observed-eye-support
plan: "03"
type: execute
wave: 2
depends_on: ["41-01", "41-02"]
files_modified:
  - BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift
  - BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
  - BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautyFaceGeometryAdapterTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift
autonomous: true
requirements: [EYE-06, EYE-07]
must_haves:
  truths:
    - "Validated observed contours produce deterministic side-aware upper/lower/inner/outer/corner/center support independent of input winding."
    - "Empty, duplicate-only, non-finite, out-of-unit, degenerate, implausibly sized, and over-ceiling contours are rejected before effects; missing either contour keeps the complete eye domain unavailable."
    - "Invalid or absent pupils clear only pupil-size and gaze eligibility while valid contour siblings remain available; diagnostics stay fixed-code and aggregate-only."
  artifacts:
    - "BeautyFaceGeometryAdapter owns bounded contour/pupil validation and canonical semantic support derivation."
    - "FaceGeometry carries adapter-produced package-internal observed-eye support for Phase 42 without public/Codable exposure."
    - "BeautyFaceGeometryAdapterTests and MissingLandmarkDegradationTests cover winding, side, malformed support, pupil-local invalidation, complete-eye gating, and redaction."
  key_links:
    - "BeautyFaceObservation observed support -> BeautyFaceGeometryAdapter validation/canonicalization -> FaceGeometry private eligibility is deterministic and request-scoped."
    - "Invalid/missing observed contours force FaceGeometry.leftEye/rightEye eligibility empty (never synthesized proxy fallback), so BeautyEffectResolver's existing complete-eye geometry gate skips the eye domain while safe siblings continue."
---

<objective>
Validate and canonicalize observed eye geometry at the detection-to-effects seam and prove field-local pupil degradation.

Purpose: Make Phase 42's provider inputs honest and privacy-safe while keeping this phase out of transforms, caps, facade output, and ledger promotion.
Output: Adapter-owned bounded semantic support and focused effects tests.
</objective>

## Artifacts this phase produces

- Package-internal `BeautyEyeSemanticSupport` and its adapter validation/canonicalization helpers attached to `FaceGeometry`.
- New `BeautyFaceGeometryAdapterTests` plus expanded `MissingLandmarkDegradationTests` and `EyeWarpProviderTests`.

<goal_backward>
Goal: effects receive deterministic validated support and safe field-local availability. Truths: canonical side-aware subsets, malformed rejection, pupil-local invalidation, and complete-eye gating. Artifacts: adapter support and tests. Key links: observation support to FaceGeometry eligibility with no public/diagnostic leakage.
</goal_backward>

<read_first>
AGENTS.md; PLANS.md; .planning/PROJECT.md; .planning/ROADMAP.md; .planning/REQUIREMENTS.md; .planning/STATE.md; 41-CONTEXT.md; 41-RESEARCH.md; 41-PATTERNS.md; BeautyFaceGeometryAdapter.swift; BeautyEffectResolver.swift; FaceGeometry; MissingLandmarkDegradationTests.swift
</read_first>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/41-public-contract-and-observed-eye-support/41-CONTEXT.md
@.planning/phases/41-public-contract-and-observed-eye-support/41-RESEARCH.md
@.planning/phases/41-public-contract-and-observed-eye-support/41-PATTERNS.md
@BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift
@BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
@BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift
@BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
</context>

## Multi-source coverage audit

| Source | Coverage in this plan |
| --- | --- |
| ROADMAP goal | Canonical, validated private support reaches effects without leakage; Plans 01-02 provide compatible scalars and mapped observations. |
| Requirements | EYE-06 covers deterministic semantic ordering, side identity, malformed rejection, and bounded derived support; EYE-07 covers pupil-local invalidation and complete-eye contour gating. |
| Research constraints | Reuse BeautyFaceGeometryAdapter/FaceGeometry seams and one mapper conversion; lock contour 6...16 / >=4 unique finite / closed-unit / width 0.04...0.50 / height 0.01...0.30 / area >0.0004, pupil one point / 10% containment / offset <=0.70 / paired ratios 0.50...2.00 as support ceilings; use synthetic XCTest fixtures, package visibility, and no new dependency/target/public geometry type. |
| CONTEXT — Public Scalar Contract | Consume the ten independent fields only as eligibility inputs; do not add aliases, transforms, final caps, or promotion here. |
| CONTEXT — Observed Eye-Support Model | Preserve side identity and derive upper/lower/inner/outer/corner/center/pupil supports independent of winding; retain validated evidence only for the request lifecycle. |
| CONTEXT — Validation and Degradation | Enforce finite/closed-unit/cardinality/duplicate/degeneracy/size/ceiling checks; validate pupils for containment, offset, and paired plausibility; invalidate only pupil-dependent work when pupils fail. |
| CONTEXT — Privacy and Ownership | Keep support package-internal, ephemeral, non-Codable, and absent from public/diagnostic/persistent surfaces; use fixed reason codes and counts only. |
| CONTEXT — the agent's Discretion | Select explicit conservative thresholds and point ceiling, document them as support-validation bounds (not visual caps), and retain full contours only when needed for deterministic derivation. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 41-03-01: Validate and canonicalize side-aware eye support</name>
  <files>BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift, BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyFaceGeometryAdapterTests.swift</files>
  <behavior>
    - Reversed and rotated contour order yields identical semantic upper/lower/inner/outer/corner/center supports with explicit left/right identity.
    - Empty, duplicate-only, non-finite, out-of-unit, degenerate, implausibly small/large, and over-ceiling contours produce unavailable support without crashing or fabricating points.
    - A valid contour with an invalid pupil retains contour support while marking pupil-dependent eligibility unavailable; no pupil is synthesized.
  </behavior>
  <action>Extend BeautyFaceGeometryAdapter with package-internal `BeautyEyeSemanticSupport` attached to FaceGeometry, explicit side-aware canonicalization, and named support-validation constants: each contour input is 6...16 points with at least 4 unique finite points, mapped points in closed [0,1], face-relative width 0.04...0.50, height 0.01...0.30, and area greater than 0.0004; each pupil is exactly one unique point per side, inside owning contour bounds expanded 10%, with normalized ellipse offset at most 0.70 and paired width/height ratios 0.50...2.00. Label these as support-validation ceilings, not visual-effect caps. Derive semantic subsets from validated full contours using anatomical side and stable extrema/center relationships rather than input winding. When observed contour support is missing or invalid, set FaceGeometry.leftEye/rightEye eligibility to empty and do not call synthesized leftEye(in:) / rightEye(in:) proxy helpers; preserve the existing BeautyEffectResolver complete-eye domain gate. Keep symmetric proxy arrays only for shipped zero-default compatibility and do not implement Phase 42 vectors, emissions, or caps. Add exact SIMD2 fixtures, repeated-call determinism, side/orientation fixtures, malformed arrays, pupil containment/offset checks, and sibling preservation.</action>
  <acceptance_criteria>Canonical supports are identical across reversed/rotated winding; every threshold boundary and just-inside/just-outside case is fixture-tested; invalid contours produce empty FaceGeometry eye eligibility with no proxy fallback; valid contours remain usable when pupils are invalid; all support stays package-internal.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.BeautyFaceGeometryAdapterTests</automated></verify>
  <done>EYE-06 support validation and canonicalization are deterministic, bounded, side-aware, and private at the adapter boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 41-03-02: Prove pupil-local degradation and complete-eye gating</name>
  <files>BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift, BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift, BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift</files>
  <behavior>
    - Missing or implausible left/right pupils remove only pupilSize and gazeCorrection eligibility while contour-only controls remain eligible.
    - Missing either eye contour keeps the established complete eye-domain skip; paired-only symmetry support never fabricates an absent side.
    - Zero new fields preserve existing four-field EyeWarpProvider output and all warnings/metrics remain redacted and aggregate-only.
  </behavior>
  <action>Extend degradation tables around the adapter-produced support and existing `BeautyEffectResolver` complete-eye gate for absent, malformed, reused, and stale eye data. Assert that invalid/missing observed contours leave `FaceGeometry.leftEye` or `rightEye` empty and cannot reactivate the synthesized proxy path; resolver skips the complete eye domain when either side is unavailable. Assert field-local pupil availability, safe non-eye continuation, fixed warning/reason codes, and no coordinates/side payloads. Keep final strength caps, provider transforms, fourteen emissions, conflict convergence, and facade routing deferred to Phases 42-44.</action>
  <acceptance_criteria>Pupil-only failures remove only pupil-dependent eligibility, missing contours create empty side eligibility and trigger the existing complete-eye skip, safe siblings continue, and diagnostics remain aggregate/redacted.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.MissingLandmarkDegradationTests &amp;&amp; swift test --package-path BeautySDK --filter BeautyEffectsTests.EyeWarpProviderTests</automated></verify>
  <done>EYE-07 is executable at the support/eligibility seam and the shipped eye regression remains neutral.</done>
</task>

</tasks>

<threat_model>
ASVS L1 applies to malformed biometric-adjacent support and cross-module effects inputs. Block high severity for tampering/DoS, raw geometry disclosure, stale retention, public/Codable support, or dependency/network/commercial drift. No raw geometry or biometric-adjacent data may appear on public, diagnostic, or persistent surfaces. Enforce bounded finite validation, deterministic canonicalization, request-scoped package values, field-local pupil invalidation, complete-eye gating, and fixed aggregate diagnostics.
</threat_model>

<verification>
Run focused adapter, degradation, and neutral-provider tests per task. Confirm only SDK-core source/tests changed and leave contract synchronization/boundary scans to Plan 41-04.
</verification>

<success_criteria>
EYE-06 and EYE-07 pass with deterministic validated support, pupil-local degradation, and complete-eye contour gating.
</success_criteria>

<output>Create `41-03-SUMMARY.md` with focused test counts, threshold decisions, and the explicit non-claim that provider transforms/caps/facade evidence/promotion remain downstream.</output>
