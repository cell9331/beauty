---
phase: 45-public-contract-and-observed-face-support
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/45-public-contract-and-observed-face-support/check_face_support_boundaries.py
  - BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift
  - BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautyFaceGeometryAdapterTests.swift
autonomous: true
requirements: [SUPP-02, SUPP-04]
must_haves:
  truths:
    - "D-05/D-10/D-13: observed contour/median data has an immutable package-only Sendable request-lifetime contract with independent region absence, no Codable/persistence/diagnostic surface, and no shared cache or mutable global state."
    - "D-08: FaceGeometry keeps its exact shipped seven-point faceContour compatibility path and gains a separate optional observedFaceSupport seam; the two values cannot be substituted for one another."
    - "D-12/D-18: face-specific open-polyline fixtures and the exact inside/equal/outside topology matrix exist before production validation is added; eye constants and polygon-area fixtures are not reused as face evidence."
    - "D-13/D-19: the boundary checker fails closed on tool errors, missing paths, unclassified matches, raw-coordinate exposure, retention, dependency/model/network drift, preset-byte drift, and semantic-row scope injection."
    - "SUPP-04 edge resolution: cancellation or interruption can discard the local value without cleanup because no support is cached, persisted, logged, or shared between requests."
    - "[FLAGGED ASSUMPTION A1] The planning baseline is contour 7...32, median 3...16, and the research-recorded geometry matrix; execution must lock exact/equal/outside tests and may adjust only when aggregate evidence from the six supported fixtures disproves a bound."
  artifacts:
    - path: .planning/phases/45-public-contract-and-observed-face-support/check_face_support_boundaries.py
      provides: "Fail-closed self-tested Phase 45 privacy, compatibility, dependency, resource, Demo, and scope gate"
    - path: BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift
      provides: "BeautyObservedFaceSupport package contract and optional request-scoped observation carrier"
    - path: BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift
      provides: "BeautyFaceSemanticSupport and separate FaceGeometry.observedFaceSupport seam"
    - path: BeautySDK/Tests/BeautyEffectsTests/BeautyFaceGeometryAdapterTests.swift
      provides: "Face-specific open-contour/median fixtures, threshold matrices, and legacy-proxy contract"
  key_links:
    - from: BeautyFaceObservation.observedFaceSupport
      to: FaceGeometry.observedFaceSupport
      via: "later Plan 45-04 adapter validation, never the shipped faceContour field"
      pattern: "observedFaceSupport"
    - from: check_face_support_boundaries.py
      to: BeautySDK and BeautyDemo active roots
      via: "classified rg/git/hash checks whose error and unclassified states block"
      pattern: "--self-test"
  prohibitions:
    - statement: "Raw observed face landmarks MUST NOT become identity, recognition, or biometric-profiling data."
      status: unresolved
    - statement: "The shipped seven-point synthetic face-box proxy MUST NOT be presented, named, or consumed as observed contour or median-line support."
      status: unresolved
    - statement: "Deferred double-chin, double-chin Pro, and hairline rows MUST NOT be silently enabled by a proxy, model, dependency, resource, manifest, or network path."
      status: unresolved
---

<objective>
Create the execution Wave 0 safeguards and private interfaces that every later Phase 45 plan must obey.

Purpose: Make privacy/scope failures detectable before implementation and hand downstream mapping/validation work explicit, non-public contracts rather than forcing executors to invent them.
Output: A self-tested fail-closed checker, private observed/semantic face-support interfaces, and compiled face-specific topology fixtures.
</objective>

## Artifacts this phase produces

- New file `.planning/phases/45-public-contract-and-observed-face-support/check_face_support_boundaries.py` with live and `--self-test` modes.
- New package symbol `BeautyObservedFaceSupport` and `BeautyFaceObservation.observedFaceSupport`.
- New internal symbol `BeautyFaceSemanticSupport` and `FaceGeometry.observedFaceSupport`.
- Face-specific `FaceSupportFixture`, contour/median builders, exact threshold matrices, and legacy seven-point assertions in `BeautyFaceGeometryAdapterTests` (test-private names may follow the file's naming style).

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<read_first>
AGENTS.md; PLANS.md; .planning/PROJECT.md; .planning/ROADMAP.md; .planning/REQUIREMENTS.md; .planning/STATE.md; .planning/phases/45-public-contract-and-observed-face-support/45-CONTEXT.md; .planning/phases/45-public-contract-and-observed-face-support/45-RESEARCH.md; .planning/phases/45-public-contract-and-observed-face-support/45-PATTERNS.md; .planning/phases/45-public-contract-and-observed-face-support/45-VALIDATION.md; BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift; BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift; BeautySDK/Tests/BeautyEffectsTests/BeautyFaceGeometryAdapterTests.swift; .planning/milestones/v1.11-phases/41-public-contract-and-observed-eye-support/check_eye_support_boundaries.py
</read_first>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/45-public-contract-and-observed-face-support/45-CONTEXT.md
@.planning/phases/45-public-contract-and-observed-face-support/45-RESEARCH.md
@.planning/phases/45-public-contract-and-observed-face-support/45-PATTERNS.md
@.planning/phases/45-public-contract-and-observed-face-support/45-VALIDATION.md
@BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift
@BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift
@BeautySDK/Tests/BeautyEffectsTests/BeautyFaceGeometryAdapterTests.swift

<interfaces>
Use the existing package-only `BeautyObservedEyeSupport: Equatable, Sendable`, optional `BeautyFaceObservation.observedEyeSupport`, internal `BeautyEyeSemanticSupport`, and defaulted `FaceGeometry` initializer pattern. The new face envelope has independently optional contour and medianLine `[CoordinatePoint]` regions. The internal semantic support owns a validated non-empty contour, optional validated median line, optional apex index, `contourEligible`, and `centerlineEligible`; `FaceGeometry.observedFaceSupport` defaults to nil. Keep `FaceGeometry.faceContour` unchanged.
</interfaces>
</context>

## Specless probe audit

- Deterministic edge probe input was 7 applicable / 0 resolved / 7 unresolved. SUPP-04 is resolved here to immutable request-local values, no cache/shared state, and interruption-without-retention acceptance. FACE-07/08/09/12 are resolved in Plan 02; SUPP-01 in Plan 03; SUPP-02 in Plan 04. No edge row is dismissed.
- Prohibition recall first considered routine correctness, generic security canon, biometric misuse, proxy misrepresentation, and deferred semantic-row backfill. Precision removed routine engineering and canon-referral items; the three bespoke privacy/transparency statements retained above are authored exactly once, descriptor-less, `status: unresolved`, so autonomous verification must flag them rather than silently green them.

## Plan-local source coverage

| Source | Coverage |
| --- | --- |
| GOAL | Supplies the private, honest observed-support seam and fail-closed guard required by the Phase 45 goal. |
| REQ | SUPP-02 gets face-specific fixture/matrix scaffolding; SUPP-04 gets lifecycle and boundary enforcement. |
| RESEARCH | Uses the Phase 41 lifecycle analog but diverges to open paths, independent regions, assumed face thresholds, and baseline/hash checks. |
| CONTEXT | D-05, D-08, D-10, D-12, D-13, D-15, D-16, D-18, D-19, and D-20 are implemented or guarded here. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 45-01-01: Create the fail-closed face-support boundary checker</name>
  <files>.planning/phases/45-public-contract-and-observed-face-support/check_face_support_boundaries.py</files>
  <behavior>
    - `rg` exit 0 means classified matches, exit 1 means clean no-match, and every other exit/exception is a blocking failure.
    - Repository-root and scanned-path resolution reject missing paths and symlink/path escapes.
    - Baseline drift, public/Codable/persistent/diagnostic raw geometry, Demo internal imports, dependency/model/resource/network additions, semantic-row tokens, preset mutation, and unclassified matches fail independently.
    - The helper's adversarial fixtures prove every classifier can reject a known mutation before live mode is trusted.
  </behavior>
  <action>Implement the new Python standard-library checker per D-13/D-15/D-16/D-19/D-20, adapting the archived Phase 41 helper without importing or weakening it. Pin pre-phase commit `9aedd6b40a7c033ac86cea2c75e06bac138cf9ef` only for `BeautySDK/Package.swift` and `BeautyDemo`; pin all five preset SHA-256 values from 45-RESEARCH.md and require the four new keys to remain absent. Scan active SDK/Demo source for public/SPI face-support/raw-coordinate exposure; Codable/Encodable/Decodable, persistence/cache/file/UserDefaults retention; coordinate-bearing logs, warnings, errors, metrics, descriptions, and snapshots; forbidden internal Demo imports; new dependency/target/resource-manifest/model/network/cloud paths; and deferred double-chin/hairline implementation or promotion. Use narrow path-and-literal allowlists, aggregate result reporting, repo containment, and a `--self-test` mutation suite covering clean, known classified, unclassified, tool-error/tool-missing, path escape, baseline drift, preset/key mutation, public/Codable/persistence/diagnostic/Demo/dependency/model/network/semantic-scope mutations. Do not print matched coordinates or raw source payloads.</action>
  <acceptance_criteria>`--self-test` passes only when every clean fixture is accepted and every adversarial mutation/tool/path failure blocks; no `|| true`, unchecked subprocess result, missing path, or unclassified match can be interpreted as clean.</acceptance_criteria>
  <verify><automated>python3 .planning/phases/45-public-contract-and-observed-face-support/check_face_support_boundaries.py --self-test &amp;&amp; git diff --check</automated></verify>
  <done>The Phase 45 boundary tool is created first and is demonstrably fail-closed before any live completion claim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 45-01-02: Define private face-support contracts and topology fixtures</name>
  <files>BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift, BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyFaceGeometryAdapterTests.swift</files>
  <behavior>
    - The observation envelope represents contour-only, median-only, both, and neither without a public/Codable/diagnostic conformance.
    - The semantic support distinguishes contour eligibility from contour-plus-centerline/apex eligibility.
    - FaceGeometry stores observed support separately while its existing seven faceContour points and default initializer behavior remain exact.
    - Test fixtures enumerate contour 6/7/8 and 31/32/33, median 2/3/4 and 15/16/17, and the research exact/inside/outside bounds without borrowing eye fixture constants.
  </behavior>
  <action>Per D-05/D-08/D-10/D-12/D-13/D-18, add package-only immutable `BeautyObservedFaceSupport: Equatable, Sendable` with independently optional `contour` and `medianLine`, then add a default-nil carrier to `BeautyFaceObservation`. Add internal `BeautyFaceSemanticSupport: Equatable, Sendable` with validated contour, optional median line/apex index, and explicit `contourEligible`/`centerlineEligible`, plus default-nil `FaceGeometry.observedFaceSupport`; keep `FaceGeometry.faceContour`, all existing initializer labels, eye/nose/lip support, and `center` unchanged. In the adapter test file, introduce face-named open-polyline/median builders and matrix data for the exact 7...32 / 3...16 and geometry edges, plus a compiling contract test for independent optionals, semantic eligibility, default-nil storage, and the exact shipped seven-point contour. These fixtures are Wave 0 inputs for Plan 04; do not add adapter validation, providers, routing, caps, public types, or a new source file.</action>
  <acceptance_criteria>All new values compile as immutable internal/package-only Sendable contracts, every FaceGeometry call site remains source-compatible through the defaulted property, the legacy seven-point assertion passes, and the named face matrices exist without eye constants or polygon sorting.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.BeautyFaceGeometryAdapterTests &amp;&amp; git diff --check</automated></verify>
  <done>Downstream plans receive explicit private interfaces and compiled face-specific fixtures while shipped geometry behavior remains unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Repository/tool output → completion gate | Subprocess status, paths, matches, baselines, and hashes are untrusted until classified. |
| Vision-derived values → package support | Biometric-adjacent coordinates may exist only as immutable request-local package/internal values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-45-01 | Repudiation | boundary checker | mitigate | Classify 0/1/>1, reject exceptions/missing paths/unclassified matches, and adversarially self-test each branch. |
| T-45-02 | Information Disclosure | observed support types | mitigate | Package/internal access, no Codable/description/persistence, raw-point-free checker output, and later live scans. |
| T-45-03 | Spoofing | synthetic faceContour vs observedFaceSupport | mitigate | Separate properties/types and exact seven-point compatibility tests; no alias or fallback name. |
| T-45-04 | Tampering | deferred semantic rows | mitigate | Baseline/dependency/model/resource/network/semantic-token checks block silent scope injection. |
| T-45-SC | Tampering | npm/pip/cargo installs | mitigate | No install task exists; Package.swift/dependency and semantic-resource drift is blocked by the checker. |
</threat_model>

<verification>
Run the checker self-test and focused adapter tests. Plan 01 intentionally does not run checker live mode because the expected 52-field implementation does not exist until Plan 02; Plan 05 owns the live run after all implementation lands.
</verification>

<success_criteria>
Wave 0 ends with a fail-closed checker, explicit private interfaces, face-specific fixtures/matrices, exact legacy-proxy isolation, and no public/dependency/resource/provider/render/Demo scope expansion.
</success_criteria>

<output>Create `45-01-SUMMARY.md` with self-test counts, interface symbols, fixture/matrix inventory, focused test result, and the three descriptor-less prohibition flags.</output>
