---
phase: 45-public-contract-and-observed-face-support
plan: "04"
type: execute
wave: 3
depends_on: ["45-01", "45-03"]
files_modified:
  - BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautyFaceGeometryAdapterTests.swift
autonomous: true
requirements: [SUPP-01, SUPP-02, SUPP-04]
must_haves:
  truths:
    - "D-08/D-11: adapter attachment is parallel to the exact seven-point legacy faceContour; absent/malformed observed support changes only observedFaceSupport and never clears eligible face, eye, nose, lip, or face-agnostic siblings."
    - "D-09/D-12: contour and median use face-named open-polyline predicates, every-point uniqueness, fixed count ceilings, finite/unit/span/chord/curvature/direction/cross-support checks, and no eye constants/angular sorting/polygon-area proof."
    - "D-10: valid contour plus invalid/missing median remains contourEligible but not centerlineEligible; invalid contour cannot produce observed support even when median is valid."
    - "D-13/D-14: raw and derived points/apex data stay internal, request-local, non-Codable, non-diagnostic, and no validation failure emits geometry-bearing text."
    - "SUPP-02 edge resolution: the adapter is a pure per-call transform with deterministic exact/equal/outside predicates; malformed-region isolation and exactly-once support attachment are asserted."
    - "[FLAGGED ASSUMPTION A1] Begin with contour 7...32, median 3...16, width 0.50...1.00, height 0.20...1.00, endpoint separation >=0.35, curvature >=0.10, median down >=0.25, chord position 0.15...0.85, apex distance <=0.40, and two contour points per apex side; fixture evidence may revise only the disproved bound with recorded aggregate rationale and updated exact tests."
  artifacts:
    - path: BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift
      provides: "Face-specific observed open-contour/median validation and semantic support attachment beside the legacy proxy"
    - path: BeautySDK/Tests/BeautyEffectsTests/BeautyFaceGeometryAdapterTests.swift
      provides: "Exact threshold, malformed topology, contour-only eligibility, isolation, and stateless request evidence"
  key_links:
    - from: BeautyFaceObservation.observedFaceSupport
      to: BeautyFaceGeometryAdapter face validators
      via: "independent contour then median/cross-support validation"
      pattern: "validatedFaceSupport|observedFaceSupport"
    - from: validated contour/median
      to: FaceGeometry.observedFaceSupport
      via: "one optional BeautyFaceSemanticSupport attachment"
      pattern: "BeautyFaceSemanticSupport"
    - from: BeautyFaceGeometryAdapter.faceContour(in:)
      to: FaceGeometry.faceContour
      via: "unchanged shipped seven-point construction"
      pattern: "0.05|0.12|0.28|0.50|0.72|0.88|0.95"
---

<objective>
Validate observed face topology and attach conservative semantic eligibility without altering any shipped geometry path.

Purpose: Turn mapped raw regions into trustworthy contour-only or contour-plus-centerline evidence for Phase 46 while failing malformed inputs locally and honestly.
Output: Face-specific pure validators, semantic support attachment, and exact boundary/isolation XCTest evidence.
</objective>

## Artifacts this phase produces

- Face-specific validation constants and pure predicates in `BeautyFaceGeometryAdapter`.
- One private observed-face validation/attachment path producing `BeautyFaceSemanticSupport`.
- Exact count/geometry/topology/cross-support, legacy proxy, sibling isolation, and consecutive-call tests in `BeautyFaceGeometryAdapterTests`.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<read_first>
AGENTS.md; PLANS.md; .planning/PROJECT.md; .planning/ROADMAP.md; .planning/REQUIREMENTS.md; .planning/STATE.md; .planning/phases/45-public-contract-and-observed-face-support/45-CONTEXT.md; .planning/phases/45-public-contract-and-observed-face-support/45-RESEARCH.md; .planning/phases/45-public-contract-and-observed-face-support/45-PATTERNS.md; 45-01-SUMMARY.md; 45-03-SUMMARY.md; BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift; BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift; BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift; BeautySDK/Tests/BeautyEffectsTests/BeautyFaceGeometryAdapterTests.swift
</read_first>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/45-public-contract-and-observed-face-support/45-CONTEXT.md
@.planning/phases/45-public-contract-and-observed-face-support/45-RESEARCH.md
@.planning/phases/45-public-contract-and-observed-face-support/45-PATTERNS.md
@BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift
@BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift
@BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift
@BeautySDK/Tests/BeautyEffectsTests/BeautyFaceGeometryAdapterTests.swift

<interfaces>
Plan 01 defines `BeautyFaceSemanticSupport` and `FaceGeometry.observedFaceSupport`; Plan 03 fills `BeautyFaceObservation.observedFaceSupport` with canonical image-normalized optional arrays. `makeGeometry(from:)` must continue building bounds, the exact legacy faceContour, eye/nose/root/tip/lip supports, and freshness as today, then independently attach validated observed support. Use package/internal pure predicates so exact tests exercise the production checks.
</interfaces>
</context>

## Plan-local source coverage

| Source | Coverage |
| --- | --- |
| GOAL | Completes honest validated observed support without changing shipped behavior. |
| REQ | SUPP-01 rejects proxy substitution, SUPP-02 gets full topology/canonical consistency, SUPP-04 remains internal/request-local/redacted. |
| RESEARCH | Implements open-polyline validation, assumed face envelope, independent eligibility, legacy isolation, and evidence-led adjustment rule. |
| CONTEXT | D-08 through D-14, D-18, and D-20 are covered without Phase 46 provider work. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 45-04-01: Lock face-specific open-path topology validation</name>
  <files>BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyFaceGeometryAdapterTests.swift</files>
  <behavior>
    - Contour count accepts 7 and 32, rejects 6 and 33; median accepts 3 and 16, rejects 2 and 17.
    - Duplicate/adjacent-duplicate/all-identical/non-finite/out-of-unit/flat/collinear/undersized/direction-degenerate input rejects at the matching boundary.
    - Every numeric threshold has just-inside, exact, and just-outside evidence with documented inclusive/exclusive semantics.
    - Forward/reversed canonical arrays retain adjacency; no angular/X/distance sorting or closed-polygon area is used.
  </behavior>
  <action>Per D-09/D-12/D-18, implement face-named constants and pure predicates for the A1 baseline: contour 7...32, median 3...16, all points exact-bit unique and finite/unit-bounded, relative contour width 0.50...1.00, height 0.20...1.00, endpoint horizontal separation at least 0.35, maximum perpendicular depth from the endpoint chord at least 0.10, and median net-down projection at least 0.25. Treat paths as open and preserve Plan 03 order; do not call the eye validators, `stablePointOrder`, angular sort, or `polygonArea`. Convert canonical CoordinatePoint arrays to SIMD only after validation and ensure every derived division/projection is finite. Use the Wave 0 fixtures to write exact/inside/outside, count, uniqueness, finite/unit, collinear, chord, curvature, and direction tests first. If the aggregate six-fixture test rejects an otherwise supported current Vision result, record only aggregate counts/ranges, revise the single disproved bound conservatively, and update its exact matrix and summary; do not silently loosen unrelated checks.</action>
  <acceptance_criteria>Production predicates and exact matrices agree; all named malformed classes reject; supported fixture aggregates pass or drive a narrowly documented evidence-led bound change; eye topology code/constants remain untouched.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.BeautyFaceGeometryAdapterTests &amp;&amp; git diff --check</automated></verify>
  <done>SUPP-02 has executable face-specific open-path validation with explicit assumption handling and bounded input work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 45-04-02: Attach independent eligibility and prove legacy/sibling isolation</name>
  <files>BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyFaceGeometryAdapterTests.swift</files>
  <behavior>
    - Valid contour without valid median yields contourEligible true and centerlineEligible false.
    - Valid contour+median requires median bottom chord position 0.15...0.85, nearest-apex distance <=0.40, and at least two contour points on each apex side; exact/inside/outside cases are covered.
    - Invalid contour yields nil observed support even with valid median; invalid median never removes valid contour eligibility.
    - Every missing/malformed case preserves the exact seven legacy face points and identical eligible eye/nose/root/tip/outer/upper/lower/inner-lip siblings; consecutive calls cannot retain prior support.
  </behavior>
  <action>Per D-08/D-10/D-11/D-13/D-14/D-18/D-20, validate contour and median independently, then apply cross-support checks only when both survive: project median bottom onto the contour endpoint chord within 0.15...0.85, find the nearest contour apex within 0.40 face-relative distance, and require its index to leave at least two contour points on both sides. Attach one `BeautyFaceSemanticSupport` to `FaceGeometry.observedFaceSupport`; contour-only support keeps nil median/apex and centerlineEligible false, while a bad contour yields no observed semantic support. Build the existing FaceGeometry fields exactly as before and never early-return or clear legacy/sibling arrays because observed face support is malformed. Add the four region-presence combinations, each cross-boundary exact matrix, malformed cases, exact legacy seven-point comparison, complete eye/nose/lip sibling equality, and alternating valid→invalid→valid request tests. Do not connect any new field to eligibility, provider, resolver, conflict, cap, facade, renderer, or metrics.</action>
  <acceptance_criteria>Contour and centerline eligibility are independently correct, cross-support bounds are exact, invalid support is field-local and stateless, shipped proxy/siblings are identical, and no provider/routing/output surface consumes the new support.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.BeautyFaceGeometryAdapterTests &amp;&amp; swift test --package-path BeautySDK --filter BeautyDetectionTests &amp;&amp; git diff --check</automated></verify>
  <done>The adapter can distinguish legacy-only, contour-only, and contour-plus-centerline evidence honestly and request-locally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Mapped optional arrays → effects planning | Coordinates are still untrusted until face-specific topology and cross-support validation pass. |
| Observed support → shipped geometry | A malformed/new path must not replace or disable the established compatibility proxy or siblings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-45-13 | Tampering / DoS | adapter topology | mitigate | Fixed counts, finite/unit/unique/open-path predicates, finite derived math, exact boundary tests. |
| T-45-14 | Spoofing | legacy proxy provenance | mitigate | Separate property/type, exact seven-point comparison, no fallback into observed support. |
| T-45-15 | Tampering | side/apex consistency | mitigate | Chord, direction, apex-distance/interior tests and independent contour/median eligibility. |
| T-45-16 | Information Disclosure | derived apex/support | mitigate | Internal immutable values, no warnings/metrics/descriptions, Plan 05 active-source scans. |
| T-45-17 | Repudiation | stale/cross-request support | mitigate | Pure adapter and alternating valid/invalid/valid tests prove no cache or carryover. |
| T-45-SC | Tampering | semantic model/resource fallback | mitigate | No dependency/resource/provider edit; Plan 05 checker blocks scope injection. |
</threat_model>

<verification>
Run the adapter suite after each task and the detection suite after isolation wiring. At wave completion run full SwiftPM and inspect the diff to confirm only the adapter/test file changed and no provider, resolver, facade, renderer, manifest, resource, or Demo path entered.
</verification>

<success_criteria>
Observed support is face-specifically validated, independently eligible, stateless, private, and attached beside—never instead of—the exact shipped proxy, with all malformed cases isolated from eligible siblings.
</success_criteria>

<output>Create `45-04-SUMMARY.md` with final support constants (including any evidence-led A1 adjustment), exact matrix counts, region combinations, legacy/sibling equality, focused/full test counts, and explicit no-consumer/nonclaim status.</output>
