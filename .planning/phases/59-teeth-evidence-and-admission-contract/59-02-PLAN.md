---
phase: 59-teeth-evidence-and-admission-contract
plan: "02"
type: execute
wave: 2
depends_on: [59-01]
files_modified:
  - BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift
  - BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyEngineLocalRetouchFoundationTests.swift
autonomous: true
requirements: [TEETH-07, TEETH-08]
security_standard: OWASP ASVS Level 1
block_on: HIGH
must_haves:
  truths:
    - "D-04: if the canonical teeth row is still closed, production remains the existing 59-field model with literal `.none`; this plan does not add a placeholder or inert teeth field."
    - "D-05/D-06: only an independently open teeth decision permits one trailing public positive-only Float; it defaults to zero, clamps finite values to 0...1, maps non-finite values to zero, and is appended to stored/CodingKey/initializer/decode/normalized order without renaming existing keys."
    - "D-08: the public model exposes only the scalar; the package-private BeautyLocalRetouchAdmission carrier remains opaque and feature-neutral with no evidence, support, mask, geometry, provider, or status state."
    - "D-09/D-10: the sole resolver authority produces exactly one opaque demand only from normalized `teethWhitening > 0`; global color, lipColor, geometry, Testing, aliases, sclera, 去脂, and multiple eligible signals remain `.none`."
    - "D-11/D-12: missing/legacy/zero inputs preserve unrelated effects and one canonical request behavior, while no provider, mask, transform, renderer output, realtime/pixel-buffer, dependency, or Demo route is introduced."
  artifacts:
    - path: BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift
      provides: Conditional append-only public teeth scalar and neutral legacy Codable behavior
    - path: BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
      provides: Conditional teeth-only production admission authority
    - path: BeautySDK/Sources/BeautyEffects/Planning/BeautyLocalRetouchAdmission.swift
      provides: Existing package-private opaque demand carrier, verified unchanged in public shape
    - path: BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift
      provides: Exact 59/60-field, normalization, Codable, and legacy-source compatibility proof
    - path: BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
      provides: Direct positive/negative teeth admission matrix
    - path: BeautySDK/Tests/BeautyCoreTests/BeautyEngineLocalRetouchFoundationTests.swift
      provides: One request-local opaque handoff and zero provider/output effect proof
  key_links:
    - from: BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift
      to: BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
      via: `normalized().teethWhitening` as the only eligible scalar
      pattern: "teethWhitening"
    - from: BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
      to: BeautySDK/Sources/BeautyEffects/Planning/BeautyLocalRetouchAdmission.swift
      via: one package-private `opaqueDemandCount: 1` and `.none` otherwise
      pattern: "opaqueDemandCount: 1|return \\.none"
    - from: BeautySDK/Sources/BeautyEffects/Planning/BeautyLocalRetouchAdmission.swift
      to: BeautySDK/Sources/BeautySDK/BeautyEngine.swift
      via: existing request-local canonicalize/detect/map/render handoff only
      pattern: "isEmpty|localRetouchAdmission"

<objective>
On the exact open branch, add one append-only scalar and one opaque admission
demand. On the current closed branch, stop before production edits and preserve
exact absence. No field or resolver route may exist merely because a later plan
could use it.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/59-teeth-evidence-and-admission-contract/59-CONTEXT.md
@.planning/phases/59-teeth-evidence-and-admission-contract/59-RESEARCH.md
@.planning/phases/59-teeth-evidence-and-admission-contract/59-PATTERNS.md
@.planning/phases/59-teeth-evidence-and-admission-contract/59-VALIDATION.md
@.planning/phases/59-teeth-evidence-and-admission-contract/check_phase59_teeth_admission_boundaries.py
@BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift
@BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
@BeautySDK/Sources/BeautyEffects/Planning/BeautyLocalRetouchAdmission.swift
@BeautySDK/Sources/BeautySDK/BeautyEngine.swift
</context>

<interfaces>
- The first command in every task must read the checker decision. `status=closed` means no production source may change and TEETH-07/08 remain blocked; only the exact-absence checker path may run.
- `status=open` must be the exact Phase 54 serializer output after the real review gate. A test fixture, mechanics row, or hand-edited JSON cannot substitute for it.
- `BeautyParameters` currently has 59 stored/Codable fields and ends in `filterId`, `filterIntensity`; the open branch adds exactly one field after that tail.
- `BeautyLocalRetouchAdmission` already carries only a package-private opaque nonnegative demand count. Preserve that public/package boundary; do not add feature names or evidence state.
</interfaces>

<tasks>

<task id="59-02-01" type="auto">
  <name>Task 1: Add the append-only teethWhitening model only after an open gate</name>
  <read_first>
    .planning/phases/59-teeth-evidence-and-admission-contract/check_phase59_teeth_admission_boundaries.py
    .planning/phases/59-teeth-evidence-and-admission-contract/59-EVIDENCE-ADMISSION-CONTRACT.md
    .planning/phases/59-teeth-evidence-and-admission-contract/59-CONTEXT.md
    BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift
    BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift
  </read_first>
  <files>BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift, BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift</files>
  <action>First require the checker to report the exact `teeth_whitening` row as `status: open`. If it reports the current closed row, do not edit either file to mention a teeth field; run the closed-branch assertion, record TEETH-07 as blocked in the task summary, and stop this task without an inert route. Only on the open branch, implement D-05/D-06 by appending `public var teethWhitening: Float` after `filterIntensity`, adding the trailing `CodingKeys` case, trailing initializer argument defaulted to zero, decoder `decodeFloatIfPresent` call, and normalized copy argument. Reuse the existing unit clamp so finite values clamp to 0...1 and non-finite values become zero. Extend the existing model owner tests to assert exactly 60 stored/CodingKey/encoded fields, one trailing field, default/negative/overflow/NaN/infinity normalization, an unequal-value Codable round trip, missing-key legacy decode to zero, and an unchanged labeled source construction that omits the new argument. Do not change any existing field, key, range, preset JSON, or source-call meaning; do not add the scalar to effective strengths, provider state, renderer helpers, persistence, diagnostics, or Demo state.</action>
  <verify>
    <automated>python3 .planning/phases/59-teeth-evidence-and-admission-contract/check_phase59_teeth_admission_boundaries.py --decision &amp;&amp; CLANG_MODULE_CACHE_PATH=/private/tmp/beauty-phase59-clang-module-cache swift test --package-path BeautySDK --filter BeautyParametersTests &amp;&amp; git diff --check</automated>
  </verify>
  <done>On an open gate, the model and tests prove one trailing normalized field with legacy compatibility; on the current closed gate, no production field or placeholder is added and the blocker remains explicit.</done>
</task>

<task id="59-02-02" type="auto">
  <name>Task 2: Implement teeth-only opaque admission and one-demand lifecycle proof</name>
  <read_first>
    .planning/phases/59-teeth-evidence-and-admission-contract/check_phase59_teeth_admission_boundaries.py
    BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
    BeautySDK/Sources/BeautyEffects/Planning/BeautyLocalRetouchAdmission.swift
    BeautySDK/Sources/BeautySDK/BeautyEngine.swift
    BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
    BeautySDK/Tests/BeautyCoreTests/BeautyEngineLocalRetouchFoundationTests.swift
  </read_first>
  <files>BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift, BeautySDK/Sources/BeautyEffects/Planning/BeautyLocalRetouchAdmission.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift, BeautySDK/Tests/BeautyCoreTests/BeautyEngineLocalRetouchFoundationTests.swift</files>
  <action>Require the exact open decision before changing production admission. If the row remains closed, preserve `BeautyEffectResolver.localRetouchAdmission` as literal `.none`, leave `BeautyLocalRetouchAdmission.swift` feature-neutral and unchanged, run the closed boundary test, and do not create an inert route. On the open branch, implement D-09/D-10/D-11 by normalizing once, checking only `normalized.teethWhitening > 0`, returning `BeautyLocalRetouchAdmission(opaqueDemandCount: 1)` for any effective nonzero value, and returning `.none` for zero, missing, normalized-away, global whitening/brightness/contrast/saturation/temperature/tint/exposure/shadow/highlight, lipColor, geometry, Testing-only, sclera, 去脂, spelling/format aliases, or multiple signals. Keep the carrier package-private and opaque; do not expose feature identity, evidence status, support, masks, geometry, provider, cached state, or diagnostics. Add direct positive/negative resolver tests and assert the existing engine makes one request-local canonical handoff for one non-empty demand with zero provider/output work in this phase. Do not add a provider, mask, transform, renderer case, saved output, realtime/pixel-buffer route, or dependency.</action>
  <verify>
    <automated>python3 .planning/phases/59-teeth-evidence-and-admission-contract/check_phase59_teeth_admission_boundaries.py --decision &amp;&amp; CLANG_MODULE_CACHE_PATH=/private/tmp/beauty-phase59-clang-module-cache swift test --package-path BeautySDK --filter 'BeautyEffectResolverTests|BeautyEngineLocalRetouchFoundationTests' &amp;&amp; git diff --check</automated>
  </verify>
  <done>Only a direct nonzero normalized scalar can produce one opaque demand on the open branch; the closed branch remains `.none`, and both branches have no provider/output/Demo implementation.</done>
</task>

</tasks>

<threat_model>
<scope>ASVS Level 1 public scalar compatibility and package-private admission.</scope>
<blocking_rule>Any closed-branch production edit, field-order/key regression, non-finite activation, forbidden-input activation, public raw state, demand multiplication, or unclassified test result blocks the plan.</blocking_rule>
<assets>Legacy callers/payloads, finite normalization, opaque carrier, one request-local handoff, and exact sibling isolation.</assets>
<threats>T-59-03 owns premature admission; T-59-04 owns append-only/Codable/legacy compatibility; T-59-05 owns aliases, globals, sibling inputs, and demand multiplication; T-59-07 owns downstream/provider/output scope.</threats>
<mitigations>Exact checker branch precondition, append-only source order, `decodeIfPresent` neutral default, existing clamp, direct positive/negative matrix, package-private opaque count, and one-request lifecycle assertions.</mitigations>
<residual_risk>Admission is only an intent handoff. Teeth selection, masks, transforms, original-pixel output, safety, naturalness, visible facade behavior, and promotion remain out of scope.</residual_risk>
</threat_model>

<verification>Run both task commands and the T-59-03/T-59-04/T-59-05/T-59-07 individual HIGH modes. If the decision is closed, verify exact absence and do not mark TEETH-07/08 complete; if open, run the 60-field and one-demand suites before proceeding.</verification>

<success_criteria>The open branch has exactly one append-only scalar and one opaque demand with full forbidden-input isolation; the current closed branch has no scalar, no admission, no inert route, and a durable blocker.</success_criteria>

<output>Create `.planning/phases/59-teeth-evidence-and-admission-contract/59-02-SUMMARY.md` when executed, including the exact `closed`/`open` decision and actual field/admission test counts.</output>
