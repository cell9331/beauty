---
phase: 59-teeth-evidence-and-admission-contract
plan: "02"
type: execute
wave: 2
depends_on: [59-01]
files_modified:
  - BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift
  - BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
  - BeautySDK/Sources/BeautyEffects/Planning/BeautyLocalRetouchAdmission.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyEngineLocalRetouchFoundationTests.swift
autonomous: true
requirements: [TEETH-07, TEETH-08]
must_haves:
  truths:
    - "BeautyParameters exposes one trailing positive-only teethWhitening Float that defaults to zero, clamps finite values to 0...1, and normalizes non-finite values to zero."
    - "Legacy construction and payload decoding remain source/Codable compatible and missing teethWhitening decodes neutral without renaming or repurposing existing keys."
    - "Only a normalized nonzero teethWhitening value produces exactly one opaque local-retouch demand; all forbidden and sibling inputs remain neutral."
  artifacts:
    - path: BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift
      provides: append-only public scalar and legacy Codable normalization
    - path: BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
      provides: teeth-only admission authority
    - path: BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
      provides: positive/forbidden admission matrix
  key_links:
    - from: BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
      to: BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift
      via: normalized().teethWhitening only
      pattern: "normalized.*teethWhitening"
    - from: BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
      to: BeautySDK/Sources/BeautyEffects/Planning/BeautyLocalRetouchAdmission.swift
      via: one opaque demand count
      pattern: "opaqueDemandCount: 1"

<objective>
After the Phase 59 evidence contract is in place, add exactly one compatibility-safe positive-only teeth intent and one resolver admission signal. Keep all mask/provider/render behavior out of this phase and retain the package-private opaque carrier.
</objective>

<context>
@AGENTS.md
@PLANS.md
@.planning/REQUIREMENTS.md
@.planning/phases/59-teeth-evidence-and-admission-contract/59-CONTEXT.md
@.planning/phases/59-teeth-evidence-and-admission-contract/59-PATTERNS.md
@BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift
@BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
@BeautySDK/Sources/BeautyEffects/Planning/BeautyLocalRetouchAdmission.swift
</context>

<tasks>
<task type="auto">
  <name>Task 59-02-01: Add append-only teethWhitening model compatibility</name>
  <files>BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift, BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift</files>
  <action>Per D-05 and D-06, append public teethWhitening after filterIntensity in stored properties, CodingKeys, trailing defaulted initializer, designated decode, and normalized(). Route all values through existing unit clamping so finite out-of-range values clamp and non-finite values become zero. Add exact 60-field inventory assertions, default/finite/non-finite normalization, unequal-value Codable round trip, legacy missing-key decode, and an unchanged labeled construction call without the new argument. Do not change existing field order, ranges, presets, source labels, or any legacy payload behavior.</action>
  <verify><automated>CLANG_MODULE_CACHE_PATH=/private/tmp/beauty-phase59-clang-module-cache swift test --package-path BeautySDK --filter BeautyParametersTests &amp;&amp; git diff --check</automated></verify>
  <done>Model tests prove the one trailing field and all required source/Codable/normalization compatibility cases.</done>
</task>
<task type="auto">
  <name>Task 59-02-02: Implement teeth-only opaque admission and lifecycle single-demand proof</name>
  <files>BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift, BeautySDK/Sources/BeautyEffects/Planning/BeautyLocalRetouchAdmission.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift, BeautySDK/Tests/BeautyCoreTests/BeautyEngineLocalRetouchFoundationTests.swift</files>
  <action>Per D-09 through D-12, replace the exact-empty resolver literal with a normalized teeth-only check: finite normalized teethWhitening greater than zero returns one opaque demand, otherwise .none. Keep BeautyLocalRetouchAdmission feature-neutral and package-private; do not export feature names, support, masks, evidence, or provider state. Add the direct positive matrix and forbidden mutations for zero/missing/non-finite, global color, lipColor, geometry, Testing hooks, sclera, 去脂, aliases, and multiple eligible signals. Assert the existing engine lifecycle makes one request-local canonical handoff for one non-empty admission and zero provider/output work in this phase. If the canonical evidence row remains closed, keep the public branch disabled by the checker and record the conditional implementation truth honestly.</action>
  <verify><automated>CLANG_MODULE_CACHE_PATH=/private/tmp/beauty-phase59-clang-module-cache swift test --package-path BeautySDK --filter 'BeautyEffectResolverTests|BeautyEngineLocalRetouchFoundationTests' &amp;&amp; git diff --check</automated></verify>
  <done>Only direct nonzero normalized teethWhitening creates one demand; every forbidden input remains neutral and the request remains one opaque handoff without a provider or render effect.</done>
</task>
</tasks>

<threat_model><scope>Public scalar and package-private admission seam.</scope><blocking_rule>Any compatibility regression, alias/global activation, or public raw state blocks the plan.</blocking_rule><assets>Legacy callers, payloads, neutral defaults, one-demand lifecycle, and exact feature isolation.</assets><threats>Field-order breakage, missing-key failure, non-finite activation, alias multiplication, global-color reinterpretation, and public state leakage.</threats><mitigations>Append-only model edits, explicit decodeIfPresent helper, normalized unit clamp, focused mutation matrix, opaque carrier, and one-request assertion.</mitigations><residual_risk>Provider/mask/color behavior is deferred to Phase 60.</residual_risk></threat_model>
<verification>Run focused model/resolver/lifecycle suites, exact source inventory checks, and diff hygiene. Record actual test counts in 59-02-SUMMARY.md.</verification>
<success_criteria>TEETH-07 and TEETH-08 pass for the scalar/admission contract without activating any downstream output feature.</success_criteria>
<output>Create .planning/phases/59-teeth-evidence-and-admission-contract/59-02-SUMMARY.md.</output>
