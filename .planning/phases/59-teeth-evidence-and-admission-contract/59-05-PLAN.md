---
phase: 59-teeth-evidence-and-admission-contract
plan: "05"
type: execute
wave: 2
depends_on: [59-04]
files_modified:
  - .planning/phases/59-teeth-evidence-and-admission-contract/59-private-evidence-runner.js
  - .planning/phases/59-teeth-evidence-and-admission-contract/59-authorized-teeth-evidence-export.js
  - .planning/phases/59-teeth-evidence-and-admission-contract/59-evidence-admission.contract.test.js
  - .planning/milestones/v1.14-phases/54-rights-approved-evidence-and-eligibility-decisions/54-EVIDENCE-DECISIONS.json
  - example-images/FIXTURE_AUTHORIZATION.md
  - .planning/phases/59-teeth-evidence-and-admission-contract/59-04-SUMMARY.md
  - PLANS.md
autonomous: true
gap_closure: true
requirements: [SEQ-01, EVID-07]
security_standard: OWASP ASVS Level 1
block_on: HIGH
must_haves:
  truths:
    - "D-01/D-03: the authorized local teeth bundle is admitted only as two genuine teeth_whitening rows—one discolored positive and one already-light negative—with complete exact-bound triples; mechanics, candidate, synthetic, historical, authorization-only, and sibling rows retain zero weight."
    - "D-02: the refreshed output is reviewed under the already-frozen Phase 54 predicate using only the fixed minimum structured judgments and opaque ReviewCore-issued acceptance provenance; tracked artifacts contain no reviewer free text."
    - "D-04: the canonical teeth row opens only when ReviewCore itself accepts both exact issued reviews and serializeDurableExport produces the committed bytes; a hand-edited open row is rejected."
    - "D-08/D-16: media, paths, hashes, rights grants, local carrier files, masks, image outputs, and review prose remain ignored/local; tracked authorization material is migrated to policy-only text and the only committed evidence data is the Phase 54 positive-allowlist durable export."
    - "The ignored bundle is resolved fail-fast inside a child-process wrapper: its path exists only in the child's PHASE59_TEETH_BUNDLE environment and is never printed, logged, persisted, or embedded in a command argument."
    - "Asset digests plus ReviewCore-issued structured reviews are authoritative; mechanics self-tests and aggregate measurements are non-authoritative corroboration and no exact mechanics metric participates in admission."
    - "The two sibling feature decisions and aggregates remain byte-for-byte semantically closed while only teeth_whitening becomes open."
  artifacts:
    - path: .planning/phases/59-teeth-evidence-and-admission-contract/59-private-evidence-runner.js
      provides: Fail-fast ignored-bundle resolution, child-only environment injection, output redaction, and repo-wide tracked/staged privacy scanning
    - path: .planning/phases/59-teeth-evidence-and-admission-contract/59-authorized-teeth-evidence-export.js
      provides: Thin dependency-free orchestration over the existing Phase 54 core; it does not reimplement validation, review predicates, reduction, or serialization
    - path: .planning/phases/59-teeth-evidence-and-admission-contract/59-evidence-admission.contract.test.js
      provides: Independent exact-review acceptance, provenance, sibling-isolation, privacy, and serializer-byte checks
    - path: .planning/milestones/v1.14-phases/54-rights-approved-evidence-and-eligibility-decisions/54-EVIDENCE-DECISIONS.json
      provides: Canonical positive-allowlist durable export with exactly one open teeth decision
    - path: example-images/FIXTURE_AUTHORIZATION.md
      provides: Policy-only authorization boundary with no active local path, rights details, review history, or reviewer prose
  key_links:
    - from: .planning/phases/59-teeth-evidence-and-admission-contract/59-authorized-teeth-evidence-export.js
      to: .planning/milestones/v1.14-phases/54-rights-approved-evidence-and-eligibility-decisions/54-evidence-core.js
      via: createTrustedAuthorizationRegistry -> createReviewSnapshot -> createReview -> evaluateFeature -> serializeDurableExport
      pattern: "createTrustedAuthorizationRegistry|createReviewSnapshot|createReview|evaluateFeature|serializeDurableExport"
    - from: .planning/phases/59-teeth-evidence-and-admission-contract/59-evidence-admission.contract.test.js
      to: .planning/milestones/v1.14-phases/54-rights-approved-evidence-and-eligibility-decisions/54-EVIDENCE-DECISIONS.json
      via: fresh-process local-bundle reduction and exact byte comparison
      pattern: "validateReview|rowPasses|open|accepted_count"
    - from: .planning/phases/59-teeth-evidence-and-admission-contract/59-private-evidence-runner.js
      to: ignored local evidence
      via: NUL-safe Git ignored-file discovery, in-memory exact-bundle classification, and child-only PHASE59_TEETH_BUNDLE injection
      pattern: "git ls-files|PHASE59_TEETH_BUNDLE|scan-tracked-staged"
---

<objective>
Convert the existing ignored teeth bundle and its opaque ReviewCore-issued fixed review set into the one canonical Phase 54 open decision without persisting private intake data or editing a passed ledger by hand (D-01, D-02, D-03, D-04, D-08, D-16).

Purpose: Remove the changed external evidence blocker while preserving the Phase 54 authority, frozen predicate, review provenance, sibling isolation, and privacy boundary.
Output: A tested local-only export adapter and the exact serializer-produced positive-allowlist durable ledger.
</objective>

<execution_context>
@/Users/yakangwang/.antigravity_cockpit/instances/codex/cli-037507efedd7/gsd-core/workflows/execute-plan.md
@/Users/yakangwang/.antigravity_cockpit/instances/codex/cli-037507efedd7/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/59-teeth-evidence-and-admission-contract/59-CONTEXT.md
@.planning/phases/59-teeth-evidence-and-admission-contract/59-RESEARCH.md
@.planning/phases/59-teeth-evidence-and-admission-contract/59-PATTERNS.md
@.planning/phases/59-teeth-evidence-and-admission-contract/59-VERIFICATION.md
@.planning/phases/59-teeth-evidence-and-admission-contract/59-04-SUMMARY.md
@.planning/milestones/v1.14-phases/54-rights-approved-evidence-and-eligibility-decisions/54-evidence-core.js
@.planning/milestones/v1.14-phases/54-rights-approved-evidence-and-eligibility-decisions/54-evidence-core.test.js
@.planning/milestones/v1.14-phases/54-rights-approved-evidence-and-eligibility-decisions/54-review-controller.js
@.planning/milestones/v1.14-phases/54-rights-approved-evidence-and-eligibility-decisions/54-rights-authorization-registry.js
@.planning/milestones/v1.14-phases/54-rights-approved-evidence-and-eligibility-decisions/54-EVIDENCE-DECISIONS.json
@.codex/skills/spike-findings-beauty/references/licensed-fixture-evaluation.md
@.codex/skills/spike-findings-beauty/references/teeth-whitening.md
</context>

<interfaces>
- The executor may read the existing ignored bundle only through `59-private-evidence-runner.js`. The runner discovers exactly one qualifying ignored candidate, injects its location only into the child process as `PHASE59_TEETH_BUNDLE`, and emits fixed privacy-safe statuses without the resolved location.
- The local intake manifest is not itself Phase 54 evidence. The adapter must create an in-memory exact Phase 54 manifest and a trusted in-memory authorization registry whose grants bind distinct opaque rights-record projections to fixture, feature, polarity, expected-target policy, permitted use, classification, asset key, and freshly computed SHA-256.
- The digest-bound positive and negative after assets are the only review targets. Superseded outputs and the invalid Spike 006 export have no canonical weight.
- ReviewCore is the sole validator/reducer. The adapter may orchestrate its public API but may not copy `rowPasses`, construct a decision object, or rewrite serialized JSON.
</interfaces>

## Artifacts this phase produces

- A canonical `teeth_whitening` decision serialized open by the existing Phase 54 core.
- Two allowlisted structured review rows and matching teeth aggregate counts, with sibling decisions unchanged.
- A reusable privacy-safe resolver/export adapter and independent exact-review test; no tracked review media, path, hash, rights carrier, or reviewer prose.
- A policy-only fixture authorization document plus a repo-wide tracked/staged privacy gate that emits no matching source text or paths.

<tasks>

<task id="59-05-01" type="auto" tdd="true">
  <name>Task 1: Bind the refreshed local bundle to the frozen Phase 54 review</name>
  <read_first>
    .planning/phases/59-teeth-evidence-and-admission-contract/59-CONTEXT.md
    .planning/phases/59-teeth-evidence-and-admission-contract/59-VERIFICATION.md
    .planning/phases/59-teeth-evidence-and-admission-contract/59-04-SUMMARY.md
    .planning/milestones/v1.14-phases/54-rights-approved-evidence-and-eligibility-decisions/54-evidence-core.js
    .planning/milestones/v1.14-phases/54-rights-approved-evidence-and-eligibility-decisions/54-evidence-core.test.js
    .planning/milestones/v1.14-phases/54-rights-approved-evidence-and-eligibility-decisions/54-review-controller.js
    .planning/milestones/v1.14-phases/54-rights-approved-evidence-and-eligibility-decisions/54-rights-authorization-registry.js
    .gitignore
    .codex/skills/spike-findings-beauty/references/licensed-fixture-evaluation.md
    .codex/skills/spike-findings-beauty/references/teeth-whitening.md
  </read_first>
  <files>.planning/phases/59-teeth-evidence-and-admission-contract/59-private-evidence-runner.js, .planning/phases/59-teeth-evidence-and-admission-contract/59-authorized-teeth-evidence-export.js, .planning/phases/59-teeth-evidence-and-admission-contract/59-evidence-admission.contract.test.js</files>
  <behavior>
    - "A missing bundle, stale after digest, incomplete triple, duplicate/borrowed rights binding, wrong polarity/target, non-genuine row, or malformed carrier exits nonzero without producing an export."
    - "Resolver discovery accepts exactly one qualifying ignored bundle, passes its location only in the child environment, and returns fixed path-free errors for zero, multiple, malformed, non-ignored, or leaking candidates."
    - "The positive review is issued with target_present=true, mask_coverage=4, protected_leakage=false, naturalness=4, structure_changed=false, decision=accept, reason_code=none."
    - "The negative review is issued with target_present=false, mask_coverage=1, protected_leakage=false, naturalness=4, structure_changed=false, decision=accept, reason_code=none."
    - "Each exact review independently passes ReviewCore.validateReview and ReviewCore.rowPasses; the reducer returns open with eligible/reviewed/accepted/rejected/naturalness counts 2/2/2/0/2."
    - "The adapter never emits private bundle metadata and never writes the canonical ledger unless explicitly given the output destination."
    - "Changing, omitting, or fabricating a mechanics report cannot alter ReviewCore validation, reduction, or serializer bytes; mechanics values are not adapter inputs."
  </behavior>
  <action>Create the tests first, then implement two Node built-ins-only artifacts. `59-private-evidence-runner.js` must enumerate ignored files under the repository's local-evidence root with NUL-safe `git ls-files --others --ignored --exclude-standard -z`, parse candidate manifests in memory, accept exactly one D-01-complete `teeth_whitening` positive/negative bundle whose manifest and six assets are ignored, and spawn the command after `--` with the resolved location only in the child `PHASE59_TEETH_BUNDLE` environment. It must reject zero/multiple/malformed/non-ignored candidates with fixed codes, capture child output, reject/redact any resolved-location occurrence, never enable shell tracing, and never write a locator or temp sidecar. The same runner must expose `--scan-tracked-staged`, enumerate tracked working-tree files and staged blobs with NUL-safe Git commands, and return only aggregate fixed-rule status—never matched text or filenames. The export adapter must fail if invoked without the runner-provided environment when `PHASE59_REQUIRE_LOCAL_EVIDENCE=1`; it must never resolve or print a fallback location. Per D-01/D-03, select rows by exact `feature` and `polarity`, require one genuine positive and one genuine negative, synthesize no media, compute the actual six asset digests, and bind each fixture to a distinct opaque in-memory grant passed through `ReviewCore.createTrustedAuthorizationRegistry`. Per D-02, issue only the fixed values listed in `<behavior>` through `createReview`; ReviewCore's object provenance, `validateReview`, `rowPasses`, `evaluateFeature`, and asset-digest binding are authoritative. Do not consume, compare, or gate on changed-pixel, luminance, texture, or other mechanics metrics. Run the existing harness self-test separately as corroboration and add a test proving arbitrary mechanics-report variation cannot change the structured decision or serializer bytes. Add mutations for every review field, superseded-after substitution, authorization/polarity/feature borrowing, missing assets, extra carrier fields/text, absent local input, ambiguous discovery, non-ignored media, and attempted path leakage. Do not edit the tracked empty Phase 54 authorization registry or persist local paths, hashes, rights details, acceptance prose, or reviewer identity.</action>
  <verify>
    <automated>node --check .planning/phases/59-teeth-evidence-and-admission-contract/59-private-evidence-runner.js &amp;&amp; node --check .planning/phases/59-teeth-evidence-and-admission-contract/59-authorized-teeth-evidence-export.js &amp;&amp; node --test .planning/milestones/v1.14-phases/54-rights-approved-evidence-and-eligibility-decisions/54-evidence-core.test.js &amp;&amp; PHASE59_REQUIRE_LOCAL_EVIDENCE=1 node .planning/phases/59-teeth-evidence-and-admission-contract/59-private-evidence-runner.js -- node --test .planning/phases/59-teeth-evidence-and-admission-contract/59-evidence-admission.contract.test.js &amp;&amp; PHASE59_REQUIRE_LOCAL_EVIDENCE=1 node .planning/phases/59-teeth-evidence-and-admission-contract/59-private-evidence-runner.js -- node .planning/phases/59-teeth-evidence-and-admission-contract/59-authorized-teeth-evidence-export.js --verify-only &amp;&amp; swift run --package-path .codex/skills/spike-findings-beauty/sources/shared-retouch-lab retouch-spike-lab --mode self-test &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - The local Phase 54 snapshot is ready with exactly one positive and one negative selected row.
    - Both exact minimum structured reviews are ReviewCore-issued and independently accepted; every one-field mutation fails closed without relying on review prose.
    - Mechanics self-tests are reported only as corroboration; no exact mechanics value is compared, serialized, or used by the admission decision.
    - Missing or ambiguous local evidence fails before the child runs, and command output contains no resolved location.
    - No local carrier, asset, hash, rights grant, path, review prose, or media file is tracked or staged.
  </acceptance_criteria>
  <done>The refreshed, user-accepted bundle has a complete local trusted binding and an exact ReviewCore-accepted review set, but the canonical ledger has not been hand-edited.</done>
</task>

<task id="59-05-02" type="auto" tdd="true">
  <name>Task 2: Serialize the one canonical open teeth decision</name>
  <read_first>
    .planning/phases/59-teeth-evidence-and-admission-contract/59-authorized-teeth-evidence-export.js
    .planning/phases/59-teeth-evidence-and-admission-contract/59-evidence-admission.contract.test.js
    .planning/milestones/v1.14-phases/54-rights-approved-evidence-and-eligibility-decisions/54-evidence-core.js
    .planning/milestones/v1.14-phases/54-rights-approved-evidence-and-eligibility-decisions/54-EVIDENCE-DECISIONS.json
    .planning/phases/59-teeth-evidence-and-admission-contract/59-EVIDENCE-ADMISSION-CONTRACT.md
    example-images/FIXTURE_AUTHORIZATION.md
    .planning/phases/59-teeth-evidence-and-admission-contract/59-04-SUMMARY.md
    PLANS.md
  </read_first>
  <files>.planning/milestones/v1.14-phases/54-rights-approved-evidence-and-eligibility-decisions/54-EVIDENCE-DECISIONS.json, example-images/FIXTURE_AUTHORIZATION.md, .planning/phases/59-teeth-evidence-and-admission-contract/59-04-SUMMARY.md, PLANS.md</files>
  <behavior>
    - "serializeDurableExport yields teeth status=open, reasons=[], and counts 2/2/2/0/2."
    - "The two exact allowlisted reviews are sorted deterministically and contain no fields beyond fixture_id, feature, polarity, target_present, mask_coverage, protected_leakage, naturalness, structure_changed, decision, reason_code."
    - "sclera_redness remains closed with its two missing-polarity reasons and zeros; upper_eyelid_fullness remains closed with both missing-polarity reasons, non_warp_design_unqualified, and zeros."
    - "A fresh independent process reproduces bytes identical to the canonical ledger."
    - "Tracked and staged repository content contains no exact ignored-bundle locator, active authorization record/date or rights narrative, reviewer free text, superseded-review history, or local asset digest."
  </behavior>
  <action>Before serialization, migrate the currently tracked privacy debt per D-08/D-16. Replace `example-images/FIXTURE_AUTHORIZATION.md` fixture-specific tables and narrative with a policy-only authorization boundary: retain only non-sensitive classification/retention rules and the mechanics-only zero-weight caveat; remove active local paths, bundle names, fixture-to-path mappings, authorization IDs/dates, rights assertions/terms, asset dimensions/status history, review chronology, and reviewer prose. Sanitize the corresponding current entries in `PLANS.md` and `59-04-SUMMARY.md` to opaque authorization/acceptance provenance plus fixed aggregate state, without deleting their valid blocker and recalibration history. Then call the adapter only through `59-private-evidence-runner.js`, with the canonical ledger as explicit output. The adapter must construct all three feature inputs using the genuine teeth snapshot plus `createClosedSnapshot` siblings, pass the ReviewCore-issued review set to `serializeDurableExport`, and write exactly those returned bytes; never patch a status, reason, count, review, or aggregate. In a fresh wrapped process, rerun local validation and `--verify-ledger`, comparing complete bytes to the canonical file. Compare pre/post JSON semantically: only the teeth decision, two teeth review rows, and teeth aggregate may change; sibling rows and aggregates stay exact. Reject durable keys outside the Phase 54 positive allowlist. Run `59-private-evidence-runner.js --scan-tracked-staged` across every tracked working-tree file and staged blob; it must reject dated/deep local-bundle locators, active authorization identifiers/dates or rights narratives, reviewer free text/history, and local asset digests while emitting only aggregate fixed-rule results. Confirm ignored sidecars and all six media files remain untracked/unstaged. Do not update production, checker expectations, or root owner contracts here; dependent waves consume this authority.</action>
  <verify>
    <automated>PHASE59_REQUIRE_LOCAL_EVIDENCE=1 node .planning/phases/59-teeth-evidence-and-admission-contract/59-private-evidence-runner.js -- node --test .planning/phases/59-teeth-evidence-and-admission-contract/59-evidence-admission.contract.test.js &amp;&amp; PHASE59_REQUIRE_LOCAL_EVIDENCE=1 node .planning/phases/59-teeth-evidence-and-admission-contract/59-private-evidence-runner.js -- node .planning/phases/59-teeth-evidence-and-admission-contract/59-authorized-teeth-evidence-export.js --output .planning/milestones/v1.14-phases/54-rights-approved-evidence-and-eligibility-decisions/54-EVIDENCE-DECISIONS.json &amp;&amp; PHASE59_REQUIRE_LOCAL_EVIDENCE=1 node .planning/phases/59-teeth-evidence-and-admission-contract/59-private-evidence-runner.js -- node .planning/phases/59-teeth-evidence-and-admission-contract/59-authorized-teeth-evidence-export.js --verify-ledger .planning/milestones/v1.14-phases/54-rights-approved-evidence-and-eligibility-decisions/54-EVIDENCE-DECISIONS.json &amp;&amp; node .planning/phases/59-teeth-evidence-and-admission-contract/59-private-evidence-runner.js --scan-tracked-staged &amp;&amp; python3 -m json.tool .planning/milestones/v1.14-phases/54-rights-approved-evidence-and-eligibility-decisions/54-EVIDENCE-DECISIONS.json &gt;/dev/null &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - The canonical teeth decision and aggregate are exactly open with `2/2/2/0/2`; no stronger review score than 4 is durable.
    - Both exact reviews pass ReviewCore in a fresh process and the rerun serializer byte-matches the canonical file.
    - Sclera and upper-eyelid decision/aggregate rows are unchanged and closed.
    - `FIXTURE_AUTHORIZATION.md`, `PLANS.md`, and `59-04-SUMMARY.md` retain no active local locator, rights detail, reviewer prose, or local digest; only opaque provenance and fixed aggregate state remain.
    - The repo-wide tracked/staged privacy scan passes without emitting source text or filenames.
    - Among evidence data, only the positive-allowlist durable export is tracked; no local intake or detailed authorization artifact enters Git.
  </acceptance_criteria>
  <done>The canonical Phase 54 authority is genuinely serialized open for teeth, with exact review provenance, sibling isolation, and privacy; production remains unchanged until Plan 59-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| ignored local bundle -> Phase 54 core | Untrusted manifest fields, files, and local authorization bindings enter canonical validation. |
| opaque acceptance provenance -> structured carrier | Only the frozen fixed judgments are issued through ReviewCore; no reviewer prose crosses into tracked state. |
| ReviewCore -> tracked ledger | Only serializer-produced positive-allowlist bytes may cross into Git. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-59-01 | Tampering | canonical decision and sibling rows | mitigate (HIGH) | Exact identity selection, fresh digest binding, serializer-only write, and sibling byte/semantic equality. |
| T-59-02 | Tampering/Repudiation | frozen review carrier | mitigate (HIGH) | Digest-bind the after assets; use the frozen minimum fixed values; require ReviewCore object provenance; mutate every field independently. |
| T-59-06 | Information Disclosure | local bundle and durable export | mitigate (HIGH) | Fail-fast private resolver, child-only env path, policy-only tracked authorization, exact durable key allowlist, and repo-wide tracked/staged scans. |
| T-59-SC | Tampering | package installs | mitigate (HIGH) | No npm, SwiftPM dependency, pip, cargo, model, or network install is permitted; use repository code and platform built-ins only. |

<blocking_rule>Block on every HIGH finding, missing local input, failed mechanics/review predicate, stale digest, serializer mismatch, sibling drift, privacy leak, or unclassified subprocess result. No auto-approval or waiver is allowed.</blocking_rule>
</threat_model>

<verification>Run both task commands from a clean worktree, verify private resolution, the exact open/closed three-row decision matrix, independent serializer byte equality, non-authoritative mechanics isolation, and the repo-wide tracked/staged privacy scan; then inspect `git status --short --untracked-files=all` without printing ignored evidence paths.</verification>

<success_criteria>The external evidence blocker is replaced by one genuine serializer-produced open teeth decision, not by a hand edit, reviewer prose, or mechanics inference; local resolution is path-safe, tracked authorization is policy-only, the canonical export is privacy-allowlisted and sibling-isolated, and no production seam has yet changed.</success_criteria>

<output>Create `.planning/phases/59-teeth-evidence-and-admission-contract/59-05-SUMMARY.md` with the exact ReviewCore decision/counts, private-resolution result, privacy-scan result, and commands, excluding bundle paths, hashes, rights records/details, media names, reviewer identity, and reviewer prose.</output>
