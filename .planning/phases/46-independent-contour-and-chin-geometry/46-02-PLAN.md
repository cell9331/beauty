---
phase: 46-independent-contour-and-chin-geometry
plan: "02"
type: execute
wave: 2
depends_on: ["46-01"]
files_modified:
  - BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift
autonomous: true
requirements: [GEOM-01, GEOM-02, GEOM-03, GEOM-04]
must_haves:
  truths:
    - "D-09 through D-14: RED tests require all four effective values to cap, reuse-scale, sanitize before conflict totals, sanitize again after scaling, and disappear from every final evidence channel when their named provider emission is empty."
    - "The complete geometry ledger is exactly thirty-seven fields: nine face/chin, fourteen eye, six nose, and eight mouth; a full capped baseline is total 11.70, count 37, and one shared scale 1/11.70."
    - "D-12/D-13: the retained baseline can only lose fields across exactly `0..<37` passes; removed fields never re-enter and provider-empty work contributes zero to total, count, scale, weakened count, domains, warnings, metrics, points, and dispatch."
    - "Wave 0 leaves these suites RED until the four effective fields, caps, named emissions, resolver lifecycle, and 37-field conflict implementation exist."
  artifacts:
    - path: BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
      provides: "Four-field cap/trigger/zero-neutral/provider-empty resolver RED contract"
    - path: BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift
      provides: "Exact 37-field total/count/scale and once-only field ledger RED contract"
    - path: BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift
      provides: "Exact 37-removal ceiling, no-reentry, and provider-empty final-evidence RED contract"
  key_links:
    - from: BeautyParameters four contour values
      to: BeautyEffectiveStrengths
      via: "requiresFaceGeometry, provisional capUnit, reuse/freshness, and field-local provider preflight"
      pattern: "faceContourSmooth|templeFullness|cheekboneSlim|chinTaper"
    - from: provider-eligible retained baseline
      to: GeometryConflictResolver
      via: "pre-conflict sanitization and post-scale named-emission recomputation"
      pattern: "0..<37"
    - from: final provider emissions
      to: totals/counts/scales/domains/metrics/points
      via: "exactly-once final accounting"
      pattern: "geometryPointCount|weakenedCount|geometryStrengthScale"
---

<objective>
Write the RED-first resolver and conflict-accounting contracts for the complete 37-field geometry inventory.

Purpose: Prevent the four new values from entering totals, activity, diagnostics, or dispatch unless their own provider arrays emit both before and after weakening.
Output: Deliberately failing cap/routing, exact arithmetic, bounded convergence, and no-reentry tests.
</objective>

## Artifacts this phase produces

- New test method `testGEOMFourFieldsRequireGeometryCapIndependentlyAndPreserveExplicitZeroPlan`.
- New test method `testGEOMProviderEmptyFaceWorkIsRemovedBeforeDomainAndConflictAccounting`.
- New test method `testGEOMAllThirtySevenFieldsShareExactElevenPointSevenBaseline`.
- New test method `testGEOMFourFieldsContributeExactlyOnceAndPreservePositiveDirection`.
- New test method `testGEOMConvergenceLoopHasExactThirtySevenRemovalCeiling`.
- New test method `testGEOMProviderEmptyFieldsNeverReenterAfterCombinedWeakening`.
- New test-private row descriptors `Phase46ResolverFieldRow` and `Phase46GeometryFieldRow`.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-independent-contour-and-chin-geometry/46-CONTEXT.md
@.planning/phases/46-independent-contour-and-chin-geometry/46-RESEARCH.md
@.planning/phases/46-independent-contour-and-chin-geometry/46-PATTERNS.md
@.planning/phases/46-independent-contour-and-chin-geometry/46-VALIDATION.md
@BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
@BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift
@BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
@BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift
@BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift

<interfaces>
`BeautyEffectResolver.requiresFaceGeometry` manually enumerates geometry-triggering normalized values. `BeautyEffectiveStrengths`, reusable-work checks, cap assignments, zero/scale helpers, requested/final domain checks, provider preflight, `resolveGeometryConflict`, and final point accounting are separate manual seams. `GeometryConflictResolver` independently enumerates scaling, `geometryTotal`, and nonzero count. Current exact geometry inventory is 33 with a 10.70 cap total and `0..<28` provider-removal ceiling; Phase 46 adds four positive `0.25` values to make 37, 11.70, and `0..<37`.
</interfaces>
</context>

## Deferred beyond Phase 46

- No renderer case, decoded image, ROI/visibility threshold, output helper, gallery, or generated PNG is introduced; Phase 47 owns them.
- The exact `0.25` values are explicitly provisional. Phase 48 owns final caps/dead zones, exhaustive nine-field face transitions, the exhaustive final 37-field matrix, promotion, and branch closeout.

<tasks>

<task type="auto" tdd="true">
  <name>Task 46-02-01: Write RED resolver trigger, cap, explicit-zero, and provider-empty contracts</name>
  <files>BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift</files>
  <read_first>AGENTS.md; PLANS.md; .planning/phases/46-independent-contour-and-chin-geometry/46-CONTEXT.md; .planning/phases/46-independent-contour-and-chin-geometry/46-RESEARCH.md; 46-01-SUMMARY.md; BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift; BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift; BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift; BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift; BeautySDK/Sources/BeautyEffects/Warp/FaceShapeWarpProvider.swift; BeautySDK/Sources/BeautyEffects/Warp/ChinWarpProvider.swift; BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift; BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift</read_first>
  <behavior>
    - Each isolated nonzero field makes `requiresFaceGeometry` true; zero/negative normalized inputs remain neutral and explicit four-zero parameters preserve the complete shipped plan.
    - Input 1 caps each new effective strength to provisional 0.25, increments cappedCount once, activates faceShape only when its own named emission exists, and exposes only existing aggregate warning/metric keys.
    - Contour-only support retains smooth/temple/cheek and removes taper; proxy-only removes all four while an emitting shipped sibling keeps faceShape active.
    - Provider-empty new work is absent from final strength, active/skipped evidence attributable to that field, combined total/count/scale/weakenedCount, geometryPointCount, and dispatch.
  </behavior>
  <action>Per D-09/D-10/D-11/D-13/D-14, replace the Phase 45 `testFACE07FACE08FACE09FACE12ExplicitZeroPreservesShippedPlanAndNonzeroDoesNotRouteYet` contract with the two exact GEOM methods listed above and a table-driven `Phase46ResolverFieldRow` for the four public/effective key paths. Preserve the omitted-versus-explicit-zero full-plan equality assertion. For each field assert detection triggering, 0.25 capped strength on `FaceGeometry.phase46AsymmetricComplete`, one cap warning/count for overflow, exact 0.125 on eligible `.reused`, zero on no-face/stale/ineligible support, and direct named-emission agreement. Add a valid shipped sibling plus invalid new-field case that keeps `.faceShape` active while the invalid value is zero and absent from conflict/point evidence. Add a valid observed locally-straight smoothing request whose emission is empty even though contour eligibility exists; prove it is removed before accounting. Do not edit production source and record the focused suite as expected RED.</action>
  <acceptance_criteria>The obsolete Phase 45 non-routing assertion no longer exists; all four fields have table-driven trigger/cap/reuse/ineligibility evidence; explicit zero still equals omission; provider-empty behavior is checked across every available aggregate evidence channel; production source is unchanged.</acceptance_criteria>
  <verify>
    <automated>/bin/zsh -lc 'rg -q "testGEOMFourFieldsRequireGeometryCapIndependentlyAndPreserveExplicitZeroPlan" BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift &amp;&amp; ! rg -q "nonzero public intent remains unrouted in Phase 45" BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift &amp;&amp; ! swift test --package-path BeautySDK --disable-sandbox --filter BeautyEffectsTests.BeautyEffectResolverTests'</automated>
  </verify>
  <done>The resolver lifecycle is specified RED-first from public intent through final provider-owned evidence for GEOM-01..04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 46-02-02: Write RED exact 37-field conflict and once-only arithmetic contracts</name>
  <files>BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift, BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift</files>
  <read_first>AGENTS.md; PLANS.md; .planning/phases/46-independent-contour-and-chin-geometry/46-CONTEXT.md; .planning/phases/46-independent-contour-and-chin-geometry/46-RESEARCH.md; .planning/phases/46-independent-contour-and-chin-geometry/46-VALIDATION.md; 46-01-SUMMARY.md; BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift; BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift; BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift; BeautySDK/Sources/BeautyEffects/Warp/EyeWarpProvider.swift; BeautySDK/Sources/BeautyEffects/Warp/NoseWarpProvider.swift; BeautySDK/Sources/BeautyEffects/Warp/MouthWarpProvider.swift; BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift; BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift</read_first>
  <behavior>
    - A full provider-eligible cap ledger contains exactly 37 fields, totals exactly 11.70 before weakening, reports weakenedCount 37, and scales every value once by 1/11.70.
    - Each new value is positive 0.25 before weakening and 0.25/11.70 after; no new value is omitted, duplicated, sign-flipped, or included through an aggregate alias.
    - The resolver source contains exactly one `for _ in 0..<37`, no `0..<28`, and every pass sanitizes the retained unscaled baseline using emissions evaluated at the scaled strengths.
    - A removed new field never re-enters on a later pass; eligible face/eye/nose/mouth siblings retain one recomputed scale and final emission agreement.
  </behavior>
  <action>Per D-09/D-12/D-13, extend the existing exact 33-field/10.70 tables rather than creating a second accounting model. Add `Phase46GeometryFieldRow` entries for the four new key paths, assert row count 37, exact unscaled total 11.70, weakenedCount 37, scale 1/11.70, and each final value against its single multiplication. Add the exact source-level 37-loop test and a representative mixed plan where one positive new field emits before conflict but becomes provider-empty after scaling; assert the retained baseline loses it, the field stays zero on subsequent passes, totals/count/scale are recomputed without it, the combined warning appears once, and valid signed/positive siblings preserve direction and emission. Include direct final face/chin named-array checks so an aggregate pipeline count cannot mask a removed field. Leave final exhaustive combinations to Phase 48 and do not edit production source in this task.</action>
  <acceptance_criteria>The tests derive one exact 37-row ledger and one 11.70 baseline; every new field appears exactly once; the source-bound contract rejects 28 or any non-37 ceiling; representative removal proves no re-entry and exact recomputation; both focused suites are expected RED before production changes.</acceptance_criteria>
  <verify>
    <automated>/bin/zsh -lc 'rg -q "testGEOMAllThirtySevenFieldsShareExactElevenPointSevenBaseline" BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift &amp;&amp; rg -q "testGEOMConvergenceLoopHasExactThirtySevenRemovalCeiling" BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift &amp;&amp; ! swift test --package-path BeautySDK --disable-sandbox --filter BeautyEffectsTests.GeometryConflictResolverTests &amp;&amp; ! swift test --package-path BeautySDK --disable-sandbox --filter BeautyEffectsTests.CombinedEffectSafetyTests'</automated>
  </verify>
  <done>The conflict/convergence contract is RED-first, exact, monotone, bounded, and once-only across all 37 geometry fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Public normalized intent → effective ledger | Requested scalars must be capped and removed when their own trusted provider evidence is absent. |
| Provider emissions → conflict/accounting | Only emitting fields may cross into totals, counts, scales, warnings, metrics, domains, point counts, or dispatch. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-46-03 | Tampering | high | `BeautyEffectResolverTests.swift` | mitigate | Four-field cap/trigger table plus proxy-only, contour-only, shipped-sibling, and provider-empty assertions in Task 46-02-01. |
| T-46-04 | Denial of Service | high | `CombinedEffectSafetyTests.swift` | mitigate | Exact one-loop `0..<37` source gate, retained-mask no-reentry fixture, and focused expected-RED command in Task 46-02-02. |
| T-46-05 | Repudiation / Tampering | high | `GeometryConflictResolverTests.swift` | mitigate | Exact 37-row, 11.70 total, weakenedCount 37, scale 1/11.70, and per-key-path once-only assertions in Task 46-02-02. |
| T-46-SC | Tampering | medium | package manifest/dependencies | mitigate | No install task exists; Plan 01 pins and checks the unchanged package manifest. |
</threat_model>

<verification>
- All six new test names and both row descriptor types exist.
- Resolver, conflict, and combined suites are RED for missing Phase 46 production behavior.
- No production source, renderer, output, gallery, owner document, or ledger row is changed.
</verification>

<success_criteria>
- The exact 37-field ledger and bounded convergence semantics can be implemented without executor interpretation.
- Provider-empty work is contractually excluded both before and after conflict scaling.
- All four requirements remain independently traceable through public/effective key paths.
</success_criteria>

<output>
Create `.planning/phases/46-independent-contour-and-chin-geometry/46-02-SUMMARY.md` when done.
</output>
