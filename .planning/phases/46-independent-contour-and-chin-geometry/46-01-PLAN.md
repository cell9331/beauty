---
phase: 46-independent-contour-and-chin-geometry
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/46-independent-contour-and-chin-geometry/check_face_geometry_boundaries.py
  - BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift
autonomous: true
requirements: [GEOM-01, GEOM-02, GEOM-03, GEOM-04]
must_haves:
  truths:
    - "D-01 through D-08/D-16/D-17: RED provider contracts distinguish local smoothing, upper-lateral temple fullness, mid-lateral cheekbone slimming, and apex-adjacent X-only chin taper from every shipped face/chin emission."
    - "D-07/D-08: the new test fixtures keep actual observed contour/median support separate from the exact seven-point compatibility proxy and lock the five shipped face/chin emissions unchanged."
    - "D-15/D-21/D-22/D-23: a fail-closed checker detects public/raw support exposure, diagnostic leakage, dependency/resource/network/Demo/renderer drift, generated artifacts, deferred-row activation, or downstream evidence overclaim."
    - "Wave 0 ends with the provider suite RED because FaceShapeWarpFieldEmissions and ChinWarpFieldEmissions do not yet exist; production provider source remains untouched."
  artifacts:
    - path: .planning/phases/46-independent-contour-and-chin-geometry/check_face_geometry_boundaries.py
      provides: "Self-tested pre-implementation and live fail-closed privacy/scope/provider boundary"
    - path: BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift
      provides: "Asymmetric complete/contour-only/centerline-missing/proxy-only fixtures and four-field named-emission RED contracts"
  key_links:
    - from: FaceGeometry.phase46AsymmetricComplete
      to: BeautyFaceSemanticSupport
      via: "test-only observed contour, median line, and apex index beside the unchanged legacy faceContour"
      pattern: "phase46AsymmetricComplete"
    - from: provider RED tests
      to: FaceShapeWarpFieldEmissions and ChinWarpFieldEmissions
      via: "direct named-array assertions, not aggregate point counts"
      pattern: "fieldEmissions"
    - from: check_face_geometry_boundaries.py
      to: active SDK, Demo, renderer, manifest, ledger, and generated-artifact roots
      via: "0/1-aware classified rg/git/hash checks; errors and unclassified matches block"
      pattern: "--self-test|--pre-implementation"
  flagged_assumptions:
    - requirement_id: GEOM-01
      probe_category: unclassified
      status: unresolved
      acceptance_predicate: "The emitted interior-contour vector is mean-centered before one uniform finite scale in 0...1; every final displacement stays within the exact 0.012 * face.bounds.width * strength / BeautySafetyCaps.faceContourSmooth ceiling, displacement sum and mean are approximately zero at absolute tolerance 1e-6, aggregate lateral neighbor-chord roughness strictly decreases, and endpoints, horizontal extrema, vertical coordinates, and all five shipped face/chin emissions remain unchanged."
    - requirement_id: GEOM-02
      probe_category: unclassified
      status: unresolved
      acceptance_predicate: "Only the two half-open upper-lateral path bands emit; every target moves outward from the contour-derived axis and the source set is disjoint from cheekboneSlim, faceSlim, faceSmall, and jawSlim evidence."
    - requirement_id: GEOM-03
      probe_category: unclassified
      status: unresolved
      acceptance_predicate: "Only the two half-open mid-lateral path bands emit; every target moves inward toward the contour-derived axis and the source set is disjoint from templeFullness, whole-cheek faceSlim/faceSmall, and lower-jaw jawSlim evidence."
    - requirement_id: GEOM-04
      probe_category: unclassified
      status: unresolved
      acceptance_predicate: "Only apexIndex-1 and apexIndex+1 emit when contour and centerline are eligible; targets move in X toward the interpolated median axis, every Y and the apex remain unchanged, and no chinLength/faceVShape/proxy evidence is borrowed."
  prohibitions:
    - statement: "Raw or derived observed face support MUST NOT become identity, recognition, authentication, or biometric-profiling data."
      status: unverified
    - statement: "The shipped seven-point synthetic faceContour proxy MUST NOT be presented or consumed as observed support for any of the four new fields."
      status: unverified
    - statement: "Deferred double-chin, double-chin Pro, hairline, or branch-completion behavior MUST NOT be silently enabled through a proxy, model, resource, dependency, network path, provider, or status edit."
      status: unverified
---

<objective>
Create the Phase 46 Wave 0 provider contracts and a fail-closed scope/privacy boundary before production geometry is implemented.

Purpose: Make source ownership, locality, eligibility, shipped-vector preservation, and scope violations executable rather than interpretive.
Output: A self-tested boundary checker and deliberately RED asymmetric named-emission tests for GEOM-01 through GEOM-04.
</objective>

## Artifacts this phase produces

- New path `.planning/phases/46-independent-contour-and-chin-geometry/check_face_geometry_boundaries.py`.
- New checker symbols `BoundaryCheckFailure`, `run_checked`, `classify_rg`, `pre_implementation_checks`, `live_checks`, `self_test`, and `main`.
- New test fixtures `FaceGeometry.phase46AsymmetricComplete`, `.phase46ContourOnly`, `.phase46CenterlineMissing`, `.phase46CenterlineIneligible`, `.phase46LegacyProxyOnly`, and `.phase46LocallyStraightContour`.
- New test methods `testGEOM01SmoothContourNamedEmissionRedContract`, `testGEOM02TempleFullnessNamedEmissionRedContract`, `testGEOM03CheekboneSlimNamedEmissionRedContract`, `testGEOM04ChinTaperNamedEmissionRedContract`, and `testGEOMNamedEmissionsPreserveFiveShippedFaceAndChinArrays`.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-independent-contour-and-chin-geometry/46-CONTEXT.md
@.planning/phases/46-independent-contour-and-chin-geometry/46-RESEARCH.md
@.planning/phases/46-independent-contour-and-chin-geometry/46-PATTERNS.md
@.planning/phases/46-independent-contour-and-chin-geometry/46-VALIDATION.md
@.planning/phases/45-public-contract-and-observed-face-support/45-VERIFICATION.md

<interfaces>
`BeautyFaceSemanticSupport` owns `contour`, optional `medianLine`, optional `apexIndex`, `contourEligible`, and `centerlineEligible`; `FaceGeometry.observedFaceSupport` is separate from the exact legacy `faceContour`. `FaceShapeWarpProvider.makeControlPoints` currently owns four shipped arrays only as one aggregate result, while `ChinWarpProvider.makeControlPoints` owns signed chinLength. Eye/nose/mouth providers establish the `fieldEmissions(...).sanitizing(_:)` contract that the RED tests require for face and chin.
</interfaces>
</context>

## Specless probe audit

- The deterministic edge probe supplied exactly four applicable rows and classified all four as `unclassified` / `unresolved`. The four `flagged_assumptions` above preserve that exact input cardinality and replace no row with a silent dismissal; their acceptance predicates come directly from CONTEXT/RESEARCH.
- Prohibition recall considered provider correctness, general ASVS/privacy canon, raw-support misuse, synthetic-proxy misrepresentation, deferred semantic-row backfill, dependency/network drift, and output/promotion overclaim. Precision removed routine engineering and generic security/compliance items as standalone prohibitions; those remain executable threat/checker inputs and later secure-phase breadcrumbs. The three surviving repository-specific transparency/scope statements are descriptor-less and `unverified`, so the checker and Phase 46 closeout must flag rather than auto-green them.

## Deferred beyond Phase 46

- Phase 47 exclusively owns decoded output, ROI/visibility comparisons, renderer case inventory, saved images, and gallery publication.
- Phase 48 exclusively owns final caps/dead zones, the exhaustive nine-face-field transition matrix, exhaustive exact thirty-seven-field convergence evidence, active-source promotion gates, four-row promotion, and branch-status closeout.
- This plan adds no production provider, public API, render pass, Demo behavior, model/resource, dependency, network path, or readiness claim.

<tasks>

<task type="auto" tdd="true">
  <name>Task 46-01-01: Create the fail-closed Phase 46 geometry boundary checker</name>
  <files>.planning/phases/46-independent-contour-and-chin-geometry/check_face_geometry_boundaries.py</files>
  <read_first>AGENTS.md; PLANS.md; SECURITY.md; .planning/phases/46-independent-contour-and-chin-geometry/46-CONTEXT.md; .planning/phases/46-independent-contour-and-chin-geometry/46-RESEARCH.md; .planning/phases/46-independent-contour-and-chin-geometry/46-VALIDATION.md; .planning/phases/45-public-contract-and-observed-face-support/check_face_support_boundaries.py; BeautySDK/Package.swift; docs/meitu-function-blueprint/SHAPE_FEATURE_LEDGER.md; BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift</read_first>
  <behavior>
    - `--self-test` mutates an isolated temporary corpus for every live rule and proves each violation fails; an unclassified match, missing path, command error, repository escape, or unexpected exit status is a failure.
    - `--pre-implementation` passes only while Phase 45 support/privacy boundaries remain intact and face/chin named production emissions are absent.
    - Default live mode requires all nine named face/chin emissions, exact `0..<37` convergence, provisional-cap wording, private/redacted support, unchanged dependency and Phase 45 checker hashes, no renderer/gallery work, and unchanged future ledger rows.
  </behavior>
  <action>Per D-15/D-21/D-22/D-23, model the existing Phase 45 checker without editing it. Pin `BeautySDK/Package.swift` to git-object hash `6f03b078816ad1f7a426e3f70d4f57503f3152e9` and the predecessor checker to `7f7cb4ad0ec7463e065ad7b88c6858c0fceb10c4`. Implement 0/1-aware `rg` classification and checked subprocess execution; every other exit or missing expected root blocks. Live checks must classify public/package/SPI exposure of support/emission/provider/control-point types, Codable/persistence/cache/static mutable support, diagnostic string literals containing contour/median/apex/index/coordinate/bounds/displacement/provider details, dependency/target/resource/model/network/cloud additions, Demo or example-renderer internal imports, renderer cases using the four field names, tracked/staged/non-ignored output/gallery artifacts, and ledger drift for `面部流畅`, `太阳穴`, `颧骨`, `尖下巴`, `去双下巴`, `去双下巴 Pro`, `发际线`, or branch `脸型`. Require exact nine-field face/chin named ownership and exactly one `for _ in 0..<37` only in live mode. Keep the three frontmatter prohibitions as flagged inputs: checker evidence may detect their concrete source manifestations but must not fabricate `check_*` descriptors or claim ethical verification.</action>
  <acceptance_criteria>The new checker has only the three documented modes; self-test rejects at least one mutation per rule family; pre-implementation passes against the current Phase 45 boundary; live mode cannot pass before production emissions exist; neither the predecessor checker nor package manifest changes.</acceptance_criteria>
  <verify>
    <automated>python3 .planning/phases/46-independent-contour-and-chin-geometry/check_face_geometry_boundaries.py --self-test &amp;&amp; python3 .planning/phases/46-independent-contour-and-chin-geometry/check_face_geometry_boundaries.py --pre-implementation &amp;&amp; git diff --check</automated>
  </verify>
  <done>The Phase 46 implementation and deferral boundary is executable, adversarially self-tested, and fail-closed before production work starts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 46-01-02: Write asymmetric named face/chin emission contracts and leave them RED</name>
  <files>BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift</files>
  <read_first>AGENTS.md; PLANS.md; .planning/phases/46-independent-contour-and-chin-geometry/46-CONTEXT.md; .planning/phases/46-independent-contour-and-chin-geometry/46-RESEARCH.md; .planning/phases/46-independent-contour-and-chin-geometry/46-PATTERNS.md; BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift; BeautySDK/Sources/BeautyEffects/Warp/WarpControlPointProvider.swift; BeautySDK/Sources/BeautyEffects/Warp/FaceShapeWarpProvider.swift; BeautySDK/Sources/BeautyEffects/Warp/ChinWarpProvider.swift; BeautySDK/Sources/BeautyEffects/Warp/EyeWarpProvider.swift; BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift</read_first>
  <behavior>
    - GEOM-01 directly inspects `faceContourSmooth`, exact interior sources, mean-centering before one uniform finite scale in `0...1`, every final absolute X displacement at or below `0.012 * face.bounds.width * strength / BeautySafetyCaps.faceContourSmooth`, displacement sum and mean equal to zero within absolute tolerance `1e-6`, strictly reduced aggregate neighbor-chord roughness, unchanged endpoint/extrema/Y, empty output when no finite nonzero improvement exists, and inequality from `faceSmall`.
    - GEOM-02 directly inspects `templeFullness`, exact upper-band sources, outward X direction, local bounds, and disjointness from cheek/new/shipped source sets.
    - GEOM-03 directly inspects `cheekboneSlim`, exact middle-band sources, inward X direction, local bounds, and disjointness from temple, jaw, and whole-cheek source sets.
    - GEOM-04 directly inspects `chinTaper`, exactly the two apex-adjacent sources, interpolated-axis inward X motion, unchanged target Y, absent apex output, and empty output without complete centerline eligibility.
    - All candidates are finite, closed-unit, locally bounded in radius, use falloff 2, and leave the four shipped face arrays plus signed chinLength byte-for-byte equal to their current results.
  </behavior>
  <action>Per D-01 through D-08/D-16/D-17, extend the existing shared `FaceGeometry` test extension with the six exact Phase 46 fixture names listed above. Use one 11-point asymmetric canonical observed contour, a three-point sloped median, and apex index 5; preserve the existing seven-point `faceContour` and every eye/nose/lip value from `.fixture`. Derive contour-only and centerline-failure variants only by replacing `observedFaceSupport`; legacy-proxy-only has nil observed support; locally-straight support keeps valid contour eligibility but makes smoothing produce no finite nonzero roughness improvement. Add the five named tests listed in Artifacts and assert direct `FaceShapeWarpFieldEmissions`/`ChinWarpFieldEmissions` arrays, exact source subsets from half-open path-progress membership, direction/locality, finite/unit/radius/falloff bounds, zero neutrality, proxy non-fallback, and equality of shipped arrays captured from the current provider behavior. In the GEOM-01 RED contract, derive the exact cap-symbol ceiling from the requested strength, assert every final displacement is within it, assert displacement sum and mean are zero within absolute normalized-coordinate tolerance `1e-6`, assert aggregate lateral neighbor-chord roughness strictly decreases, and assert the locally-straight/no-improvement fixture emits an empty smooth array; these assertions lock mean-center-then-uniform-scale behavior and forbid per-delta post-centering clamps. Do not edit any source file in this task. Run the focused suite and record the missing named-emission surface or unmet new behavior as the expected RED result.</action>
  <acceptance_criteria>All six fixtures are deterministic and asymmetric; every GEOM row has a direct named-array predicate; shipped regression assertions use actual current provider results; the production providers, caps, resolver, and pipeline remain unchanged; the focused suite is demonstrably RED for the absent Phase 46 contract.</acceptance_criteria>
  <verify>
    <automated>/bin/zsh -lc 'rg -q "testGEOM01SmoothContourNamedEmissionRedContract" BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift &amp;&amp; ! rg -q "struct FaceShapeWarpFieldEmissions" BeautySDK/Sources/BeautyEffects/Warp/FaceShapeWarpProvider.swift &amp;&amp; ! swift test --package-path BeautySDK --disable-sandbox --filter BeautyEffectsTests.FaceShapeWarpProviderTests'</automated>
  </verify>
  <done>The provider contract is RED-first, source-specific, non-aggregate, and complete for all four unresolved edge-probe rows before production implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Observed-support fixtures → provider contract | Biometric-adjacent contour/median values must authorize only the matching new field and never the compatibility proxy. |
| Repository/tool output → boundary verdict | Tool errors, unclassified matches, mutable files, and generated artifacts are untrusted and must fail closed. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-46-01 | Spoofing / Tampering | high | `FaceShapeWarpProviderTests.swift` | mitigate | Exact asymmetric source arrays, proxy-only negative fixture, named emissions, shipped byte-equality, and expected-RED focused command in Task 46-01-02. |
| T-46-02 | Information Disclosure / Tampering | high | `check_face_geometry_boundaries.py` | mitigate | Classified 0/1-aware scans, pinned hashes, adversarial mutation corpus, generated-artifact/ledger checks, and both self-test/pre-implementation commands in Task 46-01-01. |
| T-46-SC | Tampering | medium | npm/pip/cargo installs | mitigate | No package install is planned; package hash is pinned and any dependency/target/resource drift blocks. |
</threat_model>

<verification>
- Boundary checker self-test and pre-implementation modes pass.
- Focused provider suite fails for the planned absent Phase 46 named-emission behavior, while production source is unchanged.
- `git diff --check` passes and only the checker plus provider test file are modified.
</verification>

<success_criteria>
- Every GEOM requirement has a concrete RED named-emission acceptance predicate.
- The four unclassified/unresolved edge rows and three surviving bespoke prohibitions are visible with no silent dismissal.
- Phase 47/48 work remains absent and mechanically guarded.
</success_criteria>

<output>
Create `.planning/phases/46-independent-contour-and-chin-geometry/46-01-SUMMARY.md` when done.
</output>
