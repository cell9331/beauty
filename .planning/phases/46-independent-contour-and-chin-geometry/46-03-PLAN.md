---
phase: 46-independent-contour-and-chin-geometry
plan: "03"
type: execute
wave: 3
depends_on: ["46-01", "46-02"]
files_modified:
  - BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautyGeometryEffectPipelineTests.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift
autonomous: true
requirements: [GEOM-01, GEOM-02, GEOM-03, GEOM-04]
must_haves:
  truths:
    - "D-14/D-18: RED degradation contracts cover representative contour-only, missing/malformed centerline, legacy-proxy-only, no-face, stale, reused, provider-empty, valid-sibling, and fresh→reused→stale→fresh behavior without expanding into Phase 48's exhaustive matrix."
    - "D-11/D-13/D-19: final effective face/chin emissions enter the existing one-pass BeautyGeometryEffectPipeline, and the aggregate geometryPointCount equals the sum of each final provider array exactly once."
    - "D-15/D-19/D-21: each isolated public scalar triggers one deterministic detection call, obtains eligible observed support, preserves extent, reports positive aggregate geometry evidence, and reveals no contour/median/apex/index/source/vector details."
    - "Wave 0 leaves degradation, pipeline, and facade suites RED until provider, resolver, conflict, and deterministic observed-support fixture production work is complete."
  artifacts:
    - path: BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
      provides: "Representative four-field support/freshness/provider-empty transition RED contract"
    - path: BeautySDK/Tests/BeautyEffectsTests/BeautyGeometryEffectPipelineTests.swift
      provides: "Existing unified-warp dispatch and exactly-once provider-count RED contract"
    - path: BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift
      provides: "Four isolated deterministic observed-support public-facade route and redaction RED contract"
  key_links:
    - from: FaceGeometry observed-support variants
      to: BeautyEffectResolver
      via: "field-local eligibility after no-face/stale/reused freshness policy"
      pattern: "phase46AsymmetricComplete|phase46ContourOnly|phase46CenterlineMissing"
    - from: BeautyEffectPlan.effectiveStrengths
      to: BeautyGeometryEffectPipeline.controlPoints
      via: "the existing face+chin+eye+nose+mouth concatenation and one local warp"
      pattern: "controlPoints"
    - from: SDKTestingFaceDetectionFixture.usableFace
      to: BeautyEngine.processResult
      via: "one deterministic VisionDetectionObservation carrying asymmetric raw contour and median support"
      pattern: "geometryRequired|geometryPointCount"
---

<objective>
Write the RED-first representative degradation, unified-dispatch, and deterministic public-facade integration contracts.

Purpose: Prove the four field-local providers survive real resolver/facade routing without stale carryover, duplicate accounting, support leakage, or a second render pass.
Output: Deliberately failing degradation, pipeline, and facade tests that later production plans must turn green.
</objective>

## Artifacts this phase produces

- New test method `testGEOMRepresentativeSupportAndFreshnessDegradationMatrix`.
- New test method `testGEOMFreshReusedStaleFreshTransitionsCarryNoPriorFaceWork`.
- New test method `testGEOMProviderEmptyNewFieldPreservesValidShippedSibling`.
- New test-private row descriptor `Phase46DegradationFieldRow`.
- New test method `testGEOMFinalNamedEmissionsDispatchOnceThroughExistingPipeline`.
- New test method `testGEOMFourIndependentFieldsRouteThroughObservedSupportAndRedactedFacade`.
- New test method `testGEOMUsableFacadeFixturePreservesShippedFaceAndChinEvidenceAtNewFieldZero`.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-independent-contour-and-chin-geometry/46-CONTEXT.md
@.planning/phases/46-independent-contour-and-chin-geometry/46-RESEARCH.md
@.planning/phases/46-independent-contour-and-chin-geometry/46-VALIDATION.md
@BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
@BeautySDK/Sources/BeautyEffects/Render/BeautyGeometryEffectPipeline.swift
@BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift
@BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
@BeautySDK/Tests/BeautyEffectsTests/BeautyGeometryEffectPipelineTests.swift
@BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift

<interfaces>
`FaceGeometry` test fixtures are target-shared through the extension in `FaceShapeWarpProviderTests.swift`. The resolver's non-eye reused policy is exact 0.5 and stale geometry is skipped. `BeautyGeometryEffectPipeline.controlPoints(for:face:)` already concatenates face, chin, eye, nose, and mouth providers in one pass. `SDKTestingFaceDetectionProvider(.usableFace)` currently supplies bounds and coarse landmark availability but no `BeautyObservedFaceSupport`; `BeautyEngineGeometryFacadeTests` already asserts one detection call, same extent, `.usable`, aggregate metrics, and redacted metadata.
</interfaces>
</context>

## Deferred beyond Phase 46

- Facade verification stops at deterministic route, extent, final strengths, aggregate point evidence, and redaction. Phase 47 owns decoded image deltas, ROI/locality thresholds, renderer cases, output matrices, and galleries.
- The degradation set is representative. Phase 48 owns every nine-face-field transition, exact final 37-field convergence combinations, final constants, promotion, and closeout.

<tasks>

<task type="auto" tdd="true">
  <name>Task 46-03-01: Write RED representative degradation and unified-dispatch contracts</name>
  <files>BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyGeometryEffectPipelineTests.swift</files>
  <read_first>AGENTS.md; PLANS.md; .planning/phases/46-independent-contour-and-chin-geometry/46-CONTEXT.md; .planning/phases/46-independent-contour-and-chin-geometry/46-RESEARCH.md; .planning/phases/46-independent-contour-and-chin-geometry/46-VALIDATION.md; 46-01-SUMMARY.md; 46-02-SUMMARY.md; BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift; BeautySDK/Sources/BeautyEffects/Render/BeautyGeometryEffectPipeline.swift; BeautySDK/Sources/BeautyEffects/Warp/FaceShapeWarpProvider.swift; BeautySDK/Sources/BeautyEffects/Warp/ChinWarpProvider.swift; BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift; BeautySDK/Tests/BeautyEffectsTests/BeautyGeometryEffectPipelineTests.swift; BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift</read_first>
  <behavior>
    - Complete support emits all four; contour-only/missing or malformed centerline preserves three and zeros taper; proxy-only zeros all four while shipped face/chin requests retain their current output.
    - No face and stale zero the four new effective strengths; eligible reused support yields exact cap×0.5 = 0.125 before any independent combined scale; final fresh restores the same emissions without carryover.
    - Provider-empty work is absent from strengths, domain evidence, conflict metrics, point count, and pipeline points while an emitting shipped face sibling keeps faceShape active.
    - Pipeline points equal face named points + chin named points + eye + nose + mouth exactly once and use the existing provider order and render path.
  </behavior>
  <action>Per D-07/D-09/D-13/D-14/D-18/D-19, add the four listed degradation/pipeline symbols. Build `Phase46DegradationFieldRow` from each public parameter setter, effective key path, named emission key path, and required support class. Cover only the locked representative matrix: complete, contour-only, centerline missing/ineligible, legacy proxy only, no face, stale, reused, locally straight provider-empty smoothing, shipped-sibling continuation, and fresh→reused→stale→fresh. Assert exact 0.125 reused values only before any combined scale and assert the final fresh plan/emissions equal the initial fresh values. In the pipeline test compare `BeautyGeometryEffectPipeline.controlPoints` to the stable concatenation of each final provider's named `points`, assert equality with the resolver's aggregate geometryPointCount for the same combined plan, and assert no duplicate control point array is counted by face domain accounting. Do not edit production source; record both focused suites as expected RED.</action>
  <acceptance_criteria>The matrix includes every representative condition named in CONTEXT without becoming the Phase 48 exhaustive matrix; exact strength/emission/domain/metric/point predicates are present; pipeline equality uses provider arrays rather than only a nonzero count; source files remain unchanged.</acceptance_criteria>
  <verify>
    <automated>/bin/zsh -lc 'rg -q "testGEOMRepresentativeSupportAndFreshnessDegradationMatrix" BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift &amp;&amp; rg -q "testGEOMFinalNamedEmissionsDispatchOnceThroughExistingPipeline" BeautySDK/Tests/BeautyEffectsTests/BeautyGeometryEffectPipelineTests.swift &amp;&amp; ! swift test --package-path BeautySDK --disable-sandbox --filter BeautyEffectsTests.MissingLandmarkDegradationTests &amp;&amp; ! swift test --package-path BeautySDK --disable-sandbox --filter BeautyEffectsTests.BeautyGeometryEffectPipelineTests'</automated>
  </verify>
  <done>Representative field-local degradation and exactly-once existing-pipeline dispatch are RED before production implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 46-03-02: Write RED deterministic observed-support facade route and redaction contracts</name>
  <files>BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift</files>
  <read_first>AGENTS.md; PLANS.md; SECURITY.md; .planning/phases/46-independent-contour-and-chin-geometry/46-CONTEXT.md; .planning/phases/46-independent-contour-and-chin-geometry/46-RESEARCH.md; .planning/phases/45-public-contract-and-observed-face-support/45-VERIFICATION.md; BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift; BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift; BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift; BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift</read_first>
  <behavior>
    - Each isolated faceContourSmooth, templeFullness, cheekboneSlim, and chinTaper request invokes detection exactly once and resolves a usable face from deterministic real observed support.
    - Every result preserves input extent, reports geometryRequired=1 and positive aggregate geometryPointCount, and exposes no field-specific support measurement or raw geometry token.
    - Explicit zero for all four preserves the established shipped faceSlim and signed chinLength facade evidence and does not add point-count or warning drift.
  </behavior>
  <action>Per D-11/D-15/D-19/D-21/D-22, add exactly the two facade test methods listed in Artifacts. Use a four-row isolated public `BeautyParameters` table and a fresh `SDKTestingFaceDetectionProvider([.usableFace])` per row. Assert invocationCount 1, extent equality, `.usable`, faceCount/usedFaceCount 1, geometryRequired 1, positive geometryPointCount, and only existing aggregate warning/metric/reason payloads. Extend the forbidden payload scan with `facecontour`, `median`, `apex`, `index`, `pathprogress`, `source`, `target`, `radius`, `falloff`, `displacement`, `support`, `coordinate`, `bounds`, `simd`, `provider`, framework objects, and filesystem paths. The zero regression must exercise existing faceSlim and positive/negative chinLength separately and assert their known provider point counts remain unchanged after `.usableFace` gains observed support. Do not compare decoded pixels or add renderer/gallery cases. Do not edit testing support or other production source; the focused facade suite must remain RED until Plan 05 supplies the fixture and runtime routing.</action>
  <acceptance_criteria>All four public values have isolated one-call route assertions; redaction checks reject raw/derived support tokens; shipped zero-new-field behavior is frozen; no saved-output/ROI/gallery assertion exists; production source is unchanged and the suite is expected RED.</acceptance_criteria>
  <verify>
    <automated>/bin/zsh -lc 'rg -q "testGEOMFourIndependentFieldsRouteThroughObservedSupportAndRedactedFacade" BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift &amp;&amp; ! swift test --package-path BeautySDK --disable-sandbox --filter BeautyCoreTests.BeautyEngineGeometryFacadeTests'</automated>
  </verify>
  <done>The public-facade integration contract is RED-first, deterministic, aggregate-only, and limited to Phase 46 routing evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Face freshness/support → resolver | Current request support may enable only eligible fields; no prior request or compatibility proxy may survive transitions. |
| Internal final emissions → public facade evidence | Only aggregate route/point evidence may cross the public result; raw or derived support stays internal. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-46-06 | Tampering / Spoofing | high | `MissingLandmarkDegradationTests.swift` | mitigate | Representative support/freshness/provider-empty matrix and fresh-reused-stale-fresh no-carryover assertions in Task 46-03-01. |
| T-46-07 | Information Disclosure | high | `BeautyEngineGeometryFacadeTests.swift` | mitigate | Isolated route plus expanded raw/derived token redaction scan and focused expected-RED command in Task 46-03-02. |
| T-46-08 | Tampering / Repudiation | high | `BeautyGeometryEffectPipelineTests.swift` | mitigate | Exact concatenation and metric-to-final-provider point equality in Task 46-03-01. |
| T-46-SC | Tampering | medium | package/dependency boundary | mitigate | No install or manifest change; Plan 01 live checker blocks drift. |
</threat_model>

<verification>
- All seven new test symbols exist and target existing XCTest files.
- Three focused suites are RED for absent Phase 46 production routing/fixture behavior.
- No production source, renderer, output helper, gallery, generated artifact, or owner ledger is changed.
</verification>

<success_criteria>
- Representative degradation and stateless freshness are executable for all four fields.
- Unified dispatch and geometry-point accounting have an exact provider-array oracle.
- Facade evidence is deterministic, route-only, and redacted.
</success_criteria>

<output>
Create `.planning/phases/46-independent-contour-and-chin-geometry/46-03-SUMMARY.md` when done.
</output>
