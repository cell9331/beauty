---
phase: 46-independent-contour-and-chin-geometry
plan: "04"
type: execute
wave: 4
depends_on: ["46-01", "46-02", "46-03"]
files_modified:
  - BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift
  - BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift
  - BeautySDK/Sources/BeautyEffects/Warp/FaceShapeWarpProvider.swift
  - BeautySDK/Sources/BeautyEffects/Warp/ChinWarpProvider.swift
autonomous: true
requirements: [GEOM-01, GEOM-02, GEOM-03, GEOM-04]
must_haves:
  truths:
    - "D-06/D-08: FaceShapeWarpFieldEmissions owns exactly seven arrays and ChinWarpFieldEmissions owns exactly two; stable concatenation and sanitizing(_:) preserve each shipped array byte-for-byte and remove only its own empty requested field."
    - "D-01/GEOM-01: faceContourSmooth uses observed interior neighbor-chord lateral deltas with endpoint/extrema/Y/mean-center preservation and never calls the shipped small-face helper."
    - "D-02/D-03/GEOM-02/GEOM-03: temple and cheekbone use disjoint half-open canonical path-progress bands and opposite local axis directions, never a full-contour/whole-cheek/lower-jaw source."
    - "D-04/GEOM-04: chinTaper requires contour+centerline+apex, emits only apex neighbors toward interpolated median X, keeps every Y and the apex fixed, and never calls chinLength/faceVShape or the compatibility proxy."
    - "D-05/D-07/D-10: all new strengths have explicit provisional 0.25 caps; every intermediate is finite before clamping, every emitted point is unit-bounded/local, and incomplete observed support fails field-locally."
  artifacts:
    - path: BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift
      provides: "Four zero-default positive effective strength slots"
    - path: BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift
      provides: "Four explicitly provisional 0.25 cap constants"
    - path: BeautySDK/Sources/BeautyEffects/Warp/FaceShapeWarpProvider.swift
      provides: "Seven named shipped/new face emissions and three observed-contour local transforms"
    - path: BeautySDK/Sources/BeautyEffects/Warp/ChinWarpProvider.swift
      provides: "Two named chin emissions and centerline-gated apex-adjacent taper"
  key_links:
    - from: FaceGeometry.observedFaceSupport
      to: new face/chin helpers
      via: "contourEligible for three fields; centerlineEligible plus median/apex for taper"
      pattern: "observedFaceSupport"
    - from: FaceShapeWarpFieldEmissions/ChinWarpFieldEmissions
      to: BeautyEffectiveStrengths
      via: "field-local sanitizing(_:) with no aggregate guard"
      pattern: "sanitizing"
    - from: provider makeControlPoints
      to: existing unified pipeline
      via: "stable named-emission points aggregation; no new render pass"
      pattern: "fieldEmissions"
---

<objective>
Implement the four provisional effective values and nine named face/chin provider emissions that satisfy the Wave 0 geometry contracts.

Purpose: Give each shipped and new face/chin field independently sanitizable ownership while preserving existing vectors and one unified warp path.
Output: Four provisional effective/cap symbols, seven face emissions, two chin emissions, and four finite local transforms.
</objective>

## Artifacts this phase produces

- New `BeautyEffectiveStrengths.faceContourSmooth`, `.templeFullness`, `.cheekboneSlim`, and `.chinTaper`.
- New provisional `BeautySafetyCaps.faceContourSmooth`, `.templeFullness`, `.cheekboneSlim`, and `.chinTaper`.
- New internal type `FaceShapeWarpFieldEmissions` with `faceSlim`, `faceSmall`, `faceVShape`, `jawSlim`, `faceContourSmooth`, `templeFullness`, `cheekboneSlim`, `points`, and `sanitizing(_:)`.
- New internal type `ChinWarpFieldEmissions` with `chinLength`, `chinTaper`, `points`, and `sanitizing(_:)`.
- New provider methods `FaceShapeWarpProvider.fieldEmissions(face:strengths:)` and `ChinWarpProvider.fieldEmissions(face:strengths:)`.
- New private helpers `smoothContourPoints`, `templeFullnessPoints`, `cheekboneSlimPoints`, `chinTaperPoints`, `pathProgress`, `medianX`, `validatedPoint`, and `isFiniteUnitPoint`.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-independent-contour-and-chin-geometry/46-CONTEXT.md
@.planning/phases/46-independent-contour-and-chin-geometry/46-RESEARCH.md
@.planning/phases/46-independent-contour-and-chin-geometry/46-PATTERNS.md
@46-01-SUMMARY.md
@46-02-SUMMARY.md
@46-03-SUMMARY.md
@BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift
@BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift
@BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift
@BeautySDK/Sources/BeautyEffects/Warp/FaceShapeWarpProvider.swift
@BeautySDK/Sources/BeautyEffects/Warp/ChinWarpProvider.swift
@BeautySDK/Sources/BeautyEffects/Warp/EyeWarpProvider.swift
@BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift

<interfaces>
`BeautyEffectiveStrengths` is a zero-default plain value ledger. Eye/nose/mouth field-emission structs expose one array per field, stable `points`, and `sanitizing(_:)`. `BeautyFaceSemanticSupport` supplies canonical image-normalized `contour`, optional `medianLine`, optional `apexIndex`, and eligibility booleans. Existing face helpers produce exact shipped faceSlim/faceSmall/faceVShape/jawSlim points; existing chin code produces one signed vertical chinLength point. `WarpControlPointResult` retains `missing_face_contour` for shipped contour-only requests.
</interfaces>
</context>

## Deferred beyond Phase 46

- The exact geometric constants below are provisional provider choices, not decoded visibility or naturalness evidence. Phase 47 owns output proof.
- Phase 48 may recalibrate caps, dead zones, bands, radii, or displacement after evidence and owns final promotion; this plan must label every new cap/constant provisional.

<tasks>

<task type="auto" tdd="true">
  <name>Task 46-04-01: Add four effective values and explicit provisional caps</name>
  <files>BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift, BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift</files>
  <read_first>AGENTS.md; PLANS.md; DESIGN.md; .planning/phases/46-independent-contour-and-chin-geometry/46-CONTEXT.md; .planning/phases/46-independent-contour-and-chin-geometry/46-RESEARCH.md; 46-02-SUMMARY.md; BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift; BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift; BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift; BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift</read_first>
  <behavior>
    - Four independent effective properties initialize to exact zero and do not alter any existing field default or order.
    - Four named constants are exactly 0.25 and explicitly documented as provisional Phase 46 values whose final authority is Phase 48.
    - No public range, BeautyParameters storage, preset, public API, or final-cap claim changes.
  </behavior>
  <action>Per D-10/D-11, add the four exact effective properties adjacent to the five shipped face/chin values in `BeautyEffectiveStrengths`. Add four exact `Float = 0.25` constants adjacent to shipped face caps in `BeautySafetyCaps`, preceded by one unambiguous `Phase 46 provisional; Phase 48 owns final calibration/dead zones` comment. Keep the ledger initializer empty/zero-default, do not add computed aliases, and do not edit any existing cap. Compile through the provider test target; it may remain behaviorally RED until Task 46-04-02 completes.</action>
  <acceptance_criteria>All four effective properties compile and default to zero; all four caps equal 0.25 and are textually provisional; every prior property/cap remains unchanged; no public or resource file changes.</acceptance_criteria>
  <verify>
    <automated>swift build --package-path BeautySDK --disable-sandbox &amp;&amp; rg -n "provisional|faceContourSmooth|templeFullness|cheekboneSlim|chinTaper" BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift &amp;&amp; git diff --check</automated>
  </verify>
  <done>The internal ledger and replaceable provisional cap contract exist without routing or public-surface drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 46-04-02: Implement seven face and two chin named emissions with four local transforms</name>
  <files>BeautySDK/Sources/BeautyEffects/Warp/FaceShapeWarpProvider.swift, BeautySDK/Sources/BeautyEffects/Warp/ChinWarpProvider.swift</files>
  <read_first>AGENTS.md; PLANS.md; DESIGN.md; SECURITY.md; .planning/phases/46-independent-contour-and-chin-geometry/46-CONTEXT.md; .planning/phases/46-independent-contour-and-chin-geometry/46-RESEARCH.md; .planning/phases/46-independent-contour-and-chin-geometry/46-PATTERNS.md; 46-01-SUMMARY.md; BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift; BeautySDK/Sources/BeautyEffects/Warp/WarpControlPointProvider.swift; BeautySDK/Sources/BeautyEffects/Warp/FaceShapeWarpProvider.swift; BeautySDK/Sources/BeautyEffects/Warp/ChinWarpProvider.swift; BeautySDK/Sources/BeautyEffects/Warp/EyeWarpProvider.swift; BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift</read_first>
  <behavior>
    - Named shipped arrays are exactly equal in source, target, radius, strength, falloff, and order to current makeControlPoints results.
    - Smooth uses eligible interior observed-contour points, immediate-neighbor chord X deltas, 1.2%-of-face-width maximum displacement at cap, mean-centering, radius 8% of width, and falloff 2.
    - Temple uses progress `0.10..<0.30` and `0.70..<0.90`, moves X outward by at most 1.8% of face width at cap, radius 14%, and falloff 2.
    - Cheekbone uses progress `0.30..<0.46` and `0.54..<0.70`, moves X inward by at most 1.8% of face width at cap, radius 14%, and falloff 2.
    - Taper uses only apex neighbors, interpolates median X at source Y, moves X toward it by at most 1.6% of face width at cap, preserves Y, uses radius 12%, and falloff 2.
  </behavior>
  <action>Per D-01 through D-09/D-16/D-17, create the two exact emission structs and fieldEmissions methods listed in Artifacts. Move the existing shipped calculations behind their matching named arrays without changing any arithmetic or ordering; remove aggregate guards only as needed so shipped fields still require legacy `faceContour` while new fields can consume observed support independently. For smooth, require contourEligible, exclude endpoints and global horizontal extrema, compute finite lateral neighbor-chord deltas, clamp each requested displacement to `0.012 * bounds.width * strength / 0.25`, subtract the mean emitted delta, preserve Y, and return empty if fewer than two renderable finite deltas remain. For temple/cheek compute one finite `axisX = (observedContour.map(\.x).min()! + observedContour.map(\.x).max()!) / 2`, use the exact half-open bands on each side of that axis, require nonempty left/right sets, validate progress/axis/displacement/target/radius/falloff before construction, and never repair NaN/∞ through clamp. For taper require centerlineEligible, nonnil median/apex, valid apex±1, finite piecewise-linear median-X interpolation at each source Y, X-only movement toward the axis capped at `0.016 * bounds.width * strength / 0.25`, and no apex point. Radius values pass the existing 0.04...0.35 clamp only after finite positive validation. `sanitizing(_:)` zeros only a nonzero field whose own array is empty. Keep `makeControlPoints` as stable named-array aggregation and preserve `missing_face_contour` for legacy-only missing-contour requests without blocking eligible observed-only work.</action>
  <acceptance_criteria>The two structs expose exactly 7+2 arrays in shipped-first order; every invalid/missing field is independently empty; every emitted source/target/radius/strength/falloff is finite and bounded; Wave 0 direct-source/direction/locality/non-alias tests pass; all shipped face/chin arrays are byte-for-byte equal.</acceptance_criteria>
  <verify>
    <automated>swift test --package-path BeautySDK --disable-sandbox --filter BeautyEffectsTests.FaceShapeWarpProviderTests &amp;&amp; git diff --check</automated>
  </verify>
  <done>GEOM-01..04 have independently owned provider emissions and the provider Wave 0 suite is GREEN without new render/public surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Validated observed support → provider math | Support is eligible but every derived scalar/target is still untrusted until finite/local checks pass. |
| Named provider arrays → effective-field eligibility | A requested field may remain active only when its own array emits; sibling arrays cannot authorize it. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-46-09 | Tampering / Denial of Service | high | new provider helpers | mitigate | Pre-clamp finite/unit/cardinality/renderable checks, O(n) bounded contour work, conservative constants, and direct bound tests in Task 46-04-02. |
| T-46-10 | Spoofing | high | observed vs legacy support | mitigate | New helpers read only `observedFaceSupport`; proxy-only negative fixtures and centerline gates pass in Task 46-04-02. |
| T-46-11 | Tampering | high | named emission ownership | mitigate | Exact 7+2 named arrays, field-local sanitization, source disjointness, and byte-equal shipped regressions in Task 46-04-02. |
| T-46-SC | Tampering | medium | package/dependencies | mitigate | No dependency or target change; Plan 01 live checker and package hash remain authoritative. |
</threat_model>

<verification>
- Swift package builds after the ledger/cap change.
- `FaceShapeWarpProviderTests` passes all old and new provider cases.
- Only four planned source files change; `git diff --check` passes.
</verification>

<success_criteria>
- Each GEOM requirement owns a distinct non-aliased named emission.
- Missing/malformed/proxy-only evidence fails field-locally.
- Shipped face/chin vectors and the one-pass architecture remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/46-independent-contour-and-chin-geometry/46-04-SUMMARY.md` when done.
</output>
