---
phase: 40-mouth-geometry-safety-and-ledger-closeout
plan: "02"
type: execute
wave: 2
depends_on: [40-01]
files_modified:
  - BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/MouthWarpProviderTests.swift
  - BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
autonomous: true
requirements: [MOUTH-13, MOUTH-14]
must_haves:
  truths:
    - "Face, eye, six-nose, and eight-mouth work uses one provider-eligible retained baseline and one exact scale."
    - "At most fourteen monotonic nose/mouth removals converge totals, counts, warnings, strengths, and final emissions."
    - "All signed directions survive scaling and provider-empty work contributes zero times."
---

<objective>Prove exact once-only multi-domain weakening and provider/effective convergence for all eight mouth fields.</objective>

<tasks>
<task type="auto">
  <name>Task 40-02-01: Replace relational mouth checks with exact direction-complete arithmetic</name>
  <files>CombinedEffectSafetyTests.swift, GeometryConflictResolverTests.swift</files>
  <action>For all thirteen direction rows, combine representative face, eye, all-six nose, and the selected mouth field. Mirror the retained unscaled total locally and assert exact scale, exact weakened count, exactly one combined warning, exact effective values, signs, lipColor independence, and redacted metadata. Add an all-eight lower-level case.</action>
  <verify><automated>swift test --package-path BeautySDK --filter CombinedEffectSafetyTests &amp;&amp; swift test --package-path BeautySDK --filter GeometryConflictResolverTests &amp;&amp; git diff --check</automated></verify>
  <done>Normal combined work is exact and once-only.</done>
</task>
<task type="auto">
  <name>Task 40-02-02: Prove fourteen-removal provider-empty convergence</name>
  <files>MissingLandmarkDegradationTests.swift, MouthWarpProviderTests.swift, BeautyEffectResolver.swift</files>
  <action>Add integrated mixed-domain cases with all six nose and all eight mouth requests, unsupported/provider-empty mouth work, valid siblings, and both signed mouth directions. Assert removed work is absent from the unscaled total, count, scale, warning evidence, final strengths, and dispatch; final provider sanitization must equal final strengths. Modify production only if a locked test exposes a monotonic convergence defect.</action>
  <verify><automated>swift test --package-path BeautySDK --filter MissingLandmarkDegradationTests &amp;&amp; swift test --package-path BeautySDK --filter MouthWarpProviderTests &amp;&amp; swift test --package-path BeautySDK --filter CombinedEffectSafetyTests &amp;&amp; swift test --package-path BeautySDK --filter GeometryConflictResolverTests &amp;&amp; git diff --check</automated></verify>
  <done>MOUTH-14 and final MOUTH-13 convergence are command-backed.</done>
</task>
</tasks>

<threat_model><scope>Algorithmic abuse, double weakening, sign preservation, and provider eligibility.</scope><blocking_rule>Any effective/emission divergence or non-monotonic mask blocks promotion.</blocking_rule><threats>Double scale, counting empty work, sign loss, non-convergence.</threats><mitigations>Exact arithmetic, fourteen-pass ceiling, final emission equality.</mitigations></threat_model>
<output>Create `40-02-SUMMARY.md` with exact totals/counts/scales and shared-suite counts.</output>

