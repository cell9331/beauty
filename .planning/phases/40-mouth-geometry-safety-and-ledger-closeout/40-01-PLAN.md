---
phase: 40-mouth-geometry-safety-and-ledger-closeout
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - BeautySDK/Tests/BeautyEffectsTests/BeautySafetyCapsTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/MouthWarpProviderTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift
autonomous: true
requirements: [MOUTH-12, MOUTH-13]
must_haves:
  truths:
    - "All five new fields retain their public ranges and finalize exact 0.25 caps with exact capped-count and aggregate-warning semantics."
    - "All eight mouth geometry fields fail closed at their narrowest support/freshness/provider boundary while eligible siblings and safe domains continue."
    - "Reused geometry yields exact 0.5-scaled values; stale/no-face/transitions retain no prior mouth work; diagnostics remain redacted."
---

<objective>Freeze final mouth caps and exhaustive eight-field degradation behavior without changing the public contract.</objective>

<tasks>
<task type="auto">
  <name>Task 40-01-01: Finalize exact cap, normalization, count, and direction evidence</name>
  <files>BeautySafetyCapsTests.swift, BeautyEffectResolverTests.swift</files>
  <action>Rename provisional assertions as final and add a descriptor for zero, exact cap, public overflow, both signed directions, negative positive-only, and non-finite semantics. Assert exact effective values, cappedCount, one aggregate warning only when capped, mouth activation only for emitting work, and redaction.</action>
  <verify><automated>swift test --package-path BeautySDK --filter BeautySafetyCapsTests &amp;&amp; swift test --package-path BeautySDK --filter BeautyEffectResolverTests &amp;&amp; git diff --check</automated></verify>
  <done>MOUTH-12 has exact executable evidence for all five controls.</done>
</task>
<task type="auto">
  <name>Task 40-01-02: Complete eight-field support, freshness, provider-empty, and transition evidence</name>
  <files>MouthWarpProviderTests.swift, MissingLandmarkDegradationTests.swift</files>
  <action>Add typed eight-field tables covering zero, missing/malformed outer support, local upper/lower/inner dependencies, provider-empty work, reused exact 0.5 scale, stale, no-face, and consecutive fresh/reused/stale/missing transitions. Prove unsupported fields contribute no effective strength/emission/evidence while eligible siblings continue and lipColor remains separate.</action>
  <verify><automated>swift test --package-path BeautySDK --filter MouthWarpProviderTests &amp;&amp; swift test --package-path BeautySDK --filter MissingLandmarkDegradationTests &amp;&amp; git diff --check</automated></verify>
  <done>MOUTH-13 is field-complete at provider and resolver seams.</done>
</task>
<task type="auto">
  <name>Task 40-01-03: Close the all-eight public no-face seam</name>
  <files>BeautyEngineGeometryFacadeTests.swift</files>
  <action>Request all eight mouth fields through the public facade alongside safe color/filter work. Assert unchanged extent, no face, zero mouth geometry, safe-domain continuation, aggregate diagnostics, and absence of raw geometry/provider/path/image payloads.</action>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyEngineGeometryFacadeTests &amp;&amp; git diff --check</automated></verify>
  <done>The public facade agrees with internal fail-closed behavior.</done>
</task>
</tasks>

<threat_model><scope>Input normalization, private geometry, freshness, provider eligibility, and diagnostic boundaries.</scope><blocking_rule>Any unsupported field surviving or raw detail leaking blocks later plans.</blocking_rule><threats>Cap bypass, sign loss, sibling masking, stale carryover, raw geometry leakage.</threats><mitigations>Exact tables, per-field emissions, sequential calls, facade redaction.</mitigations></threat_model>
<output>Create `40-01-SUMMARY.md` with observed test counts and no-promotion statement.</output>

