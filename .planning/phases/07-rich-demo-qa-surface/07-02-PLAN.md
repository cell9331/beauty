---
phase: "07-rich-demo-qa-surface"
plan: "07-02"
status: "pending"
type: "execute"
wave: 2
depends_on: ["07-01"]
files_modified:
  - "BeautyDemo/BeautyDemo/Editor/EditorShellView.swift"
  - "BeautyDemo/BeautyDemo/Editor/DetectionStatusPresentation.swift"
  - "BeautyDemo/BeautyDemo/Editor/PreviewDebugOverlayState.swift"
  - "BeautyDemo/BeautyDemo/Editor/PreviewDebugOverlayView.swift"
  - "BeautyDemo/BeautyDemo/Camera/CameraBeautyPipeline.swift"
  - "BeautyDemo/BeautyDemo/Editor/ImageInputModels.swift"
  - "BeautyDemo/BeautyDemo/Editor/ImageEditorPipeline.swift"
  - "BeautyDemo/BeautyDemo/Panel/BeautyCategoryModels.swift"
  - "BeautyDemo/BeautyDemoTests/CompareStateTests.swift"
  - "BeautyDemo/BeautyDemoTests/BeautyDemoViewStateTests.swift"
  - "BeautyDemo/BeautyDemoTests/InputPipelinePrivacyTests.swift"
autonomous: true
requirements: ["DEMO-07"]
requirements_addressed: ["DEMO-07"]
must_haves:
  truths:
    - "D-11: a single preview-surface debug button near compare toggles a read-only overlay shared by camera and photo paths."
    - "D-12: overlay fields are limited to detection availability, reason codes, face/used counts, detection/mapping timings, warning count, and frame status."
    - "D-13: debug overlay draws no face boxes, landmarks, control points, coordinates, or geometry layer."
    - "D-14: recoverable errors show only redacted code/status, never raw framework strings, paths, stack traces, `NSError`, Vision objects, landmarks, bounding boxes, or image bytes."
    - "D-15: debug UI mutates only overlay visibility and never mutates SDK output, parameter state, detection state, compare state, or processing snapshots."
    - "D-16 and D-17: current category/subcategory order remains visible, implemented paths stay active, and future categories remain disabled without extra info pages."
    - "D-22 and D-23: exact debug model/test names may vary, but the overlay remains privacy-safe, deterministic, and testable through value state/static scans."
  artifacts:
    - "`PreviewDebugOverlayState` or equivalent value model with allowed redacted rows."
    - "`PreviewDebugOverlayView` or equivalent compact SwiftUI overlay."
    - "Preview toolbar with unchanged compare labels plus `Show Debug Details` / `Hide Debug Details`."
    - "Camera/photo snapshot or processing state fields for warning count and redacted recoverable error code/status where needed."
    - "Tests proving compare/debug coexistence, read-only toggling, redacted rows, and disabled future-category copy/order."
  key_links:
    - "`BeautyDetectionSummary` -> `DetectionDebugSummary` -> `PreviewDebugOverlayState` -> read-only overlay rows."
    - "Camera/photo processing state -> frame status and redacted last error -> debug overlay."
    - "Compare toggle and debug toggle -> display-only state -> no parameter or SDK output mutation."
---

# Plan 07-02: Add Final Compare, Debug Overlay, and Unavailable-State Polish

## Phase Goal

**As a** Demo QA user, **I want to** compare output and inspect safe debug details without leaking geometry or changing output, **so that** `DEMO-07` is covered through a useful validation surface.

<objective>
Implement the final preview debug overlay and unavailable-state polish.

Purpose: Phase 7 must make detection, degradation, and recoverable processing state inspectable without exposing sensitive face data or mutating the preview.
Output: Preview debug overlay value model/view, toolbar wiring beside compare and JSON, redacted camera/photo debug inputs, compare/debug preservation tests, and unavailable-state tests.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@ARCHITECTURE.md
@DESIGN.md
@FRONTEND.md
@SECURITY.md
@RELIABILITY.md
@PRODUCT_SENSE.md
@QUALITY_SCORE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-rich-demo-qa-surface/07-CONTEXT.md
@.planning/phases/07-rich-demo-qa-surface/07-RESEARCH.md
@.planning/phases/07-rich-demo-qa-surface/07-VALIDATION.md
@.planning/phases/07-rich-demo-qa-surface/07-UI-SPEC.md
@.planning/phases/07-rich-demo-qa-surface/07-PATTERNS.md
@.planning/phases/07-rich-demo-qa-surface/07-01-PLAN.md
@BeautyDemo/BeautyDemo/Editor/EditorShellView.swift
@BeautyDemo/BeautyDemo/Editor/CompareState.swift
@BeautyDemo/BeautyDemo/Editor/DetectionStatusPresentation.swift
@BeautyDemo/BeautyDemo/Camera/CameraBeautyPipeline.swift
@BeautyDemo/BeautyDemo/Editor/ImageInputModels.swift
@BeautyDemo/BeautyDemo/Editor/ImageEditorPipeline.swift
@BeautyDemo/BeautyDemo/Panel/BeautyCategoryModels.swift
@BeautyDemo/BeautyDemo/Panel/BeautyPanelView.swift
@BeautyDemo/BeautyDemoTests/CompareStateTests.swift
@BeautyDemo/BeautyDemoTests/BeautyDemoViewStateTests.swift
@BeautyDemo/BeautyDemoTests/InputPipelinePrivacyTests.swift
@BeautySDK/Sources/BeautyCore/Models/BeautyDetectionSummary.swift
@BeautySDK/Sources/BeautyCore/Models/BeautyResult.swift
</context>

## Artifacts this phase produces

- `PreviewDebugOverlayState`: pure value model for frame status, `DetectionDebugSummary?`, warning count, redacted last error code, and friendly status.
- `PreviewDebugOverlayView`: compact read-only SwiftUI overlay with stable row labels and no geometry drawing.
- `PreviewDebugOverlayState.camera(...)` and `.photo(...)` factories or equivalent mappers from processing states.
- Toolbar copy `Show Debug Details` and `Hide Debug Details`.
- Tests for debug empty state `Debug details are unavailable for this preview.`
- Tests and static scans rejecting geometry/raw framework/path/network/debug leaks.

## Source Coverage Audit

| Source | ID | Feature / Requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | - | Compare/debug states | 07-02 | COVERED | Toolbar and overlay. |
| REQ | DEMO-07 | Before/after compare and debug overlay states | 07-02 | COVERED | Primary plan scope. |
| CONTEXT | D-11 | Single preview-surface debug toggle | 07-02 | COVERED | Toolbar wiring. |
| CONTEXT | D-12 | Safe diagnostic summary fields | 07-02 | COVERED | Overlay model. |
| CONTEXT | D-13 | No geometry overlay | 07-02 | COVERED | View and scans. |
| CONTEXT | D-14 | Redacted recoverable errors | 07-02 | COVERED | Error code/status rows. |
| CONTEXT | D-15 | Read-only debug state | 07-02 | COVERED | Compare/debug tests. |
| CONTEXT | D-16 D-17 | Implemented/future category polish | 07-02 | COVERED | Category copy/order tests. |
| CONTEXT | D-22 D-23 | Names/test organization discretionary | 07-02 | COVERED | Concrete examples, not mandatory names. |
| UI-SPEC | - | Toolbar, debug copy, visual hierarchy, accessibility | 07-02 | COVERED | Uses exact copy and compact styling. |
| RESEARCH | - | Reuse detection summary and privacy scans | 07-02 | COVERED | Public summary only. |

<tasks>

<task id="07-02-01" type="auto" tdd="true">
  <name>Task 1: Add redacted preview debug overlay state</name>
  <files>
    BeautyDemo/BeautyDemo/Editor/DetectionStatusPresentation.swift
    BeautyDemo/BeautyDemo/Editor/PreviewDebugOverlayState.swift
    BeautyDemo/BeautyDemo/Camera/CameraBeautyPipeline.swift
    BeautyDemo/BeautyDemo/Editor/ImageInputModels.swift
    BeautyDemo/BeautyDemo/Editor/ImageEditorPipeline.swift
    BeautyDemo/BeautyDemoTests/CompareStateTests.swift
    BeautyDemo/BeautyDemoTests/InputPipelinePrivacyTests.swift
  </files>
  <read_first>
    .planning/phases/07-rich-demo-qa-surface/07-CONTEXT.md
    .planning/phases/07-rich-demo-qa-surface/07-RESEARCH.md
    .planning/phases/07-rich-demo-qa-surface/07-VALIDATION.md
    .planning/phases/07-rich-demo-qa-surface/07-UI-SPEC.md
    .planning/phases/07-rich-demo-qa-surface/07-PATTERNS.md
    BeautyDemo/BeautyDemo/Editor/DetectionStatusPresentation.swift
    BeautyDemo/BeautyDemo/Camera/CameraBeautyPipeline.swift
    BeautyDemo/BeautyDemo/Editor/ImageInputModels.swift
    BeautyDemo/BeautyDemo/Editor/ImageEditorPipeline.swift
    BeautyDemo/BeautyDemoTests/CompareStateTests.swift
    BeautyDemo/BeautyDemoTests/InputPipelinePrivacyTests.swift
    BeautySDK/Sources/BeautyCore/Models/BeautyDetectionSummary.swift
    BeautySDK/Sources/BeautyCore/Models/BeautyResult.swift
  </read_first>
  <behavior>
    - Debug state rows are derived from public `BeautyDetectionSummary`, friendly processing state, warning count, and redacted error code only.
    - Allowed row labels include `Frame`, `Detection`, `Reasons`, `Faces`, `Timing`, `Warnings`, `Last Error`, and `Status`.
    - Camera states can represent `camera running`, `processing paused`, and dropped-frame count if already available.
    - Photo states can represent `photo empty`, `photo loading`, `photo loaded`, and `photo failed`.
    - Redacted recoverable error codes are stable strings such as `processing_paused` and `photo_decode_failed`.
    - Debug state string rendering contains no `VNFaceObservation`, `boundingBox`, `landmark`, `CGPoint`, `CGRect`, `NSError`, `/private/var`, URL, stack trace, image bytes, or raw JSON payload.
  </behavior>
  <action>Create `PreviewDebugOverlayState` as a pure `Equatable` and `Sendable` value. Reuse `DetectionDebugSummary` for detection details. Add only the minimum snapshot/state fields needed to surface `warningCount` and redacted recoverable error code/status: preserve `BeautyResult.warnings.count` in camera/photo snapshots if available, map camera failure to `processing_paused`, and map photo decode failure to `photo_decode_failed`. Do not preserve raw `Error`, `NSError`, framework strings, image bytes, or face geometry. Add tests that construct no-face, partial, low-confidence, stale, camera paused, photo loading, photo loaded, and photo failed states and assert the rendered/debug strings contain allowed fields and reject forbidden tokens.</action>
  <acceptance_criteria>
    - Tests assert `.noFace`, `.partial`, `.lowConfidence`, and `.stale` detection summaries render availability, reason codes, counts, and optional timings only.
    - Tests assert camera paused state exposes `processing_paused` and friendly copy `Processing paused. Showing the last usable preview.`
    - Tests assert photo failed state exposes `photo_decode_failed` and friendly copy `Could not read that photo. Choose another image.`
    - Tests assert warning count is numeric and no warning message contains geometry/path/framework data.
    - Static scan over `BeautyDemo/BeautyDemo/Camera` and `BeautyDemo/BeautyDemo/Editor` returns no active matches for geometry/raw tokens except policy/test guard strings where scoped in tests.
  </acceptance_criteria>
  <verify>
    <automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project BeautyDemo/BeautyDemo.xcodeproj -scheme BeautyDemo -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' test -only-testing:BeautyDemoTests/CompareStateTests -only-testing:BeautyDemoTests/InputPipelinePrivacyTests</automated>
    <automated>rg -n "VNFaceObservation|boundingBox|landmark|CGPoint|CGRect|NSError|/private/var|rawPresetJson|image bytes|http://|https://" BeautyDemo/BeautyDemo/Camera BeautyDemo/BeautyDemo/Editor</automated>
  </verify>
  <done>Preview debug state is privacy-safe, redacted, and testable without UI automation.</done>
</task>

<task id="07-02-02" type="auto" tdd="true">
  <name>Task 2: Wire debug toggle and compact overlay into the preview toolbar</name>
  <files>
    BeautyDemo/BeautyDemo/Editor/EditorShellView.swift
    BeautyDemo/BeautyDemo/Editor/PreviewDebugOverlayView.swift
    BeautyDemo/BeautyDemo/Editor/PreviewDebugOverlayState.swift
    BeautyDemo/BeautyDemoTests/CompareStateTests.swift
    BeautyDemo/BeautyDemoTests/BeautyDemoViewStateTests.swift
    BeautyDemo/BeautyDemoTests/InputPipelinePrivacyTests.swift
  </files>
  <read_first>
    .planning/phases/07-rich-demo-qa-surface/07-CONTEXT.md
    .planning/phases/07-rich-demo-qa-surface/07-RESEARCH.md
    .planning/phases/07-rich-demo-qa-surface/07-VALIDATION.md
    .planning/phases/07-rich-demo-qa-surface/07-UI-SPEC.md
    .planning/phases/07-rich-demo-qa-surface/07-PATTERNS.md
    .planning/phases/07-rich-demo-qa-surface/07-01-PLAN.md
    BeautyDemo/BeautyDemo/Editor/EditorShellView.swift
    BeautyDemo/BeautyDemo/Editor/CompareState.swift
    BeautyDemo/BeautyDemoTests/CompareStateTests.swift
    BeautyDemo/BeautyDemoTests/BeautyDemoViewStateTests.swift
  </read_first>
  <behavior>
    - Existing compare labels remain exactly `Show Before` and `Show After`.
    - One debug button uses `Show Debug Details` when hidden and `Hide Debug Details` when visible.
    - Debug toggle accessibility value is `Debug details hidden` or `Debug details visible`.
    - Toggling debug changes only overlay visibility and does not call `parameterStore`, `cameraBeautyPipeline.enqueue`, `imageEditorPipeline.process`, `CompareState.toggle`, or SDK processing APIs.
    - Overlay displays compact rows over the preview surface and shows `Debug details are unavailable for this preview.` when no safe state exists.
    - JSON sheet state from 07-01 remains enum-driven and compatible with the same toolbar.
  </behavior>
  <action>Replace the single bottom compare button placement with a compact preview toolbar that contains compare when a snapshot exists, `Parameter JSON` from 07-01, and the debug toggle. Add `PreviewDebugOverlayView` using local SwiftUI only: 13 px row labels, 8 px row gaps, 16 px padding, white overlay surface, `#2F6BFF` accent only for active/focused state, and no geometry drawing. Keep the overlay read-only. Derive camera overlay state from `cameraBeautyPipeline.state` and photo overlay state from `imageEditorPipeline.state`. If no safe debug state exists, render the exact empty copy from the UI contract. Add value-state tests for toolbar copy and compare/debug preservation.</action>
  <acceptance_criteria>
    - `CompareStateTests` or editor state tests assert toggling debug visibility preserves selected mode, category, subcategory, `BeautyParameters`, and compare display.
    - Tests assert compare action titles remain exactly `Show Before` and `Show After`.
    - Tests assert debug hidden/visible titles are exactly `Show Debug Details` and `Hide Debug Details`.
    - Tests assert debug accessibility values are exactly `Debug details hidden` and `Debug details visible`.
    - Tests assert empty debug overlay copy is exactly `Debug details are unavailable for this preview.`
    - Privacy scan asserts `PreviewDebugOverlayView` contains no geometry drawing types or raw error/path tokens.
  </acceptance_criteria>
  <verify>
    <automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project BeautyDemo/BeautyDemo.xcodeproj -scheme BeautyDemo -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' test -only-testing:BeautyDemoTests/CompareStateTests -only-testing:BeautyDemoTests/BeautyDemoViewStateTests -only-testing:BeautyDemoTests/InputPipelinePrivacyTests</automated>
    <automated>rg -n "Show Debug Details|Hide Debug Details|Debug details unavailable|Debug details are unavailable for this preview|Show Before|Show After" BeautyDemo/BeautyDemo BeautyDemo/BeautyDemoTests</automated>
  </verify>
  <done>Compare, JSON, and debug preview controls coexist without state mutation or privacy leakage.</done>
</task>

<task id="07-02-03" type="auto" tdd="true">
  <name>Task 3: Lock final unavailable-state order and copy</name>
  <files>
    BeautyDemo/BeautyDemo/Panel/BeautyCategoryModels.swift
    BeautyDemo/BeautyDemo/Panel/BeautyPanelView.swift
    BeautyDemo/BeautyDemoTests/BeautyDemoViewStateTests.swift
    BeautyDemo/BeautyDemoTests/InputPipelinePrivacyTests.swift
  </files>
  <read_first>
    .planning/phases/07-rich-demo-qa-surface/07-CONTEXT.md
    .planning/phases/07-rich-demo-qa-surface/07-RESEARCH.md
    .planning/phases/07-rich-demo-qa-surface/07-UI-SPEC.md
    .planning/phases/07-rich-demo-qa-surface/07-PATTERNS.md
    BeautyDemo/BeautyDemo/Panel/BeautyCategoryModels.swift
    BeautyDemo/BeautyDemo/Panel/BeautyCategoryRailView.swift
    BeautyDemo/BeautyDemo/Panel/BeautyPanelView.swift
    BeautyDemo/BeautyDemoTests/BeautyDemoViewStateTests.swift
  </read_first>
  <behavior>
    - Top-level order remains Beauty, Face Shape, Facial Features, Makeup, Filters, Stickers, Background, Style.
    - Facial Features order remains Eyes, Nose, Mouth, Eyebrows, Teeth, Hairline.
    - Implemented categories Beauty, Face Shape, Facial Features, and Filters remain active.
    - Future categories Makeup, Stickers, Background, and Style remain visible and disabled.
    - Future subcategories Eyebrows, Teeth, and Hairline remain visible and disabled.
    - Disabled copy is short, explicit, and does not imply v1 support or create tappable info pages.
  </behavior>
  <action>Review `BeautyCategoryModels` and `BeautyPanelView` against the UI contract. If existing strings already satisfy the contract, preserve them and add exact tests. If a disabled reason is too Phase-2-specific or implies support, replace it with concise v1 copy while keeping the same category order and no new navigation. Add tests for exact enabled/disabled category IDs, exact subcategory IDs, disabled badge/reason presence, and absence of extra info-page navigation or future-domain active controls.</action>
  <acceptance_criteria>
    - Tests assert top-level category IDs exactly `[.beauty, .faceShape, .facialFeatures, .makeup, .filters, .stickers, .background, .style]`.
    - Tests assert Facial Features subcategory IDs exactly `[.eyes, .nose, .mouth, .eyebrows, .teeth, .hairline]`.
    - Tests assert Makeup, Stickers, Background, and Style are disabled and visible with non-empty badge and reason.
    - Tests assert Eyebrows, Teeth, and Hairline are disabled and visible with non-empty badge and reason.
    - Tests assert disabled/future copy contains no `Vision`, `landmark`, `bounding`, raw path, `NSError`, or release-grade claim.
    - Tests assert no new tappable info-page route or future-domain active controls were added in Phase 7.
  </acceptance_criteria>
  <verify>
    <automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project BeautyDemo/BeautyDemo.xcodeproj -scheme BeautyDemo -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' test -only-testing:BeautyDemoTests/BeautyDemoViewStateTests -only-testing:BeautyDemoTests/InputPipelinePrivacyTests</automated>
  </verify>
  <done>Implemented and future Demo categories are visibly and accurately represented for v1 readiness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| SDK detection summary -> Demo debug overlay | Only public, geometry-free summary fields can reach UI. |
| Processing failure -> user/debug copy | Failures must be redacted into stable codes and friendly copy. |
| Debug toggle -> preview state | Toggle must only affect overlay visibility and must not alter processing, compare, parameter, or detection state. |
| Future category copy -> product claims | Disabled copy must not imply v1 support for deferred domains. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-07-02-01 | Information Disclosure | Debug overlay | mitigate | Overlay state uses `BeautyDetectionSummary` and redacted codes only; tests and scans reject geometry/raw tokens. |
| T-07-02-02 | Tampering | Debug toggle | mitigate | Tests assert debug visibility changes preserve compare, parameters, category selection, and snapshots. |
| T-07-02-03 | Information Disclosure | Recoverable error copy | mitigate | Map failures to `processing_paused` and `photo_decode_failed`; never store raw `Error` or paths. |
| T-07-02-04 | Spoofing | Future category availability | mitigate | View-state tests assert deferred categories stay disabled and visible with explicit reasons. |
| T-07-02-SC | Supply Chain | UI dependencies | accept | Native SwiftUI only; no remote UI registry or third-party component is added. |
</threat_model>

<verification>
- `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project BeautyDemo/BeautyDemo.xcodeproj -scheme BeautyDemo -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' test -only-testing:BeautyDemoTests/CompareStateTests -only-testing:BeautyDemoTests/BeautyDemoViewStateTests -only-testing:BeautyDemoTests/InputPipelinePrivacyTests`
- `rg -n "import Beauty(Core|Detection|Effects|Render|Resources)" BeautyDemo/BeautyDemo BeautyDemo/BeautyDemoTests`
- `rg -n "VNFaceObservation|boundingBox|landmark|CGPoint|CGRect|NSError|/private/var|rawPresetJson|image bytes|http://|https://|upload" BeautyDemo/BeautyDemo/Camera BeautyDemo/BeautyDemo/Editor BeautyDemo/BeautyDemo/Panel BeautyDemo/BeautyDemoTests`
</verification>

<success_criteria>
- `DEMO-07` debug overlay states are implemented for camera and photo preview paths.
- Compare remains display-only and debug remains read-only.
- Debug output contains allowed redacted summary fields only.
- Future categories and subcategories remain visible, disabled, ordered, and honest.
</success_criteria>

<output>
After completion, create `.planning/phases/07-rich-demo-qa-surface/07-02-SUMMARY.md`.
</output>

