---
phase: 42-independent-eye-geometry-and-pipeline-integration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift
  - BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift
  - BeautySDK/Sources/BeautyEffects/Warp/EyeWarpProvider.swift
  - BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift
autonomous: true
requirements: [EYE-08, EYE-09, EYE-10, EYE-11, EYE-12, EYE-13, EYE-14]
must_haves:
  truths:
    - "All ten new fields have internal normalized strengths and provisional finite caps without changing the public 48-field contract."
    - "EyeWarpProvider owns fourteen named emissions and emits distinct, bounded vectors for complete semantic supports."
    - "Pupil, gaze, and symmetry work fail closed or no-op locally when their observed evidence is absent, neutral, or implausible."
  artifacts:
    - "EyeWarpFieldEmissions with eyeSize, eyeDistance, eyeYPosition, eyeTailLift, eyeHeight, eyeLength, upperEyelidLift, pupilSize, gazeCorrection, lowerEyelidDrop, eyeTilt, innerCornerOpen, outerCornerOpen, eyeSymmetry arrays."
    - "EyeWarpProvider transforms consume package-private semantic support and preserve nil-support legacy shipped behavior."
  key_links:
    - "FaceGeometry.left/rightEyeSupport -> EyeWarpProvider field helpers -> WarpControlPoint arrays -> existing unified warp."
---

<objective>
Implement the independent eye provider behavior and named emission surface that
the resolver can consume. Keep all support private and provisional constants
explicitly separate from Phase 44 final-cap evidence.
</objective>

<read_first>
AGENTS.md; PLANS.md; .planning/PROJECT.md; .planning/ROADMAP.md; .planning/REQUIREMENTS.md; .planning/STATE.md; 42-CONTEXT.md; 42-RESEARCH.md; 42-PATTERNS.md; BeautySDK/Sources/BeautyEffects/Warp/EyeWarpProvider.swift; BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift; BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift; BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift; BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift
</read_first>

<tasks>

<task type="auto" tdd="true">
  <name>Task 42-01-01: Add strengths/caps and contour-local named eye emissions</name>
  <files>BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift, BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift, BeautySDK/Sources/BeautyEffects/Warp/EyeWarpProvider.swift, BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift</files>
  <behavior>
    - Height uses upper/lower lid subsets with a stable center; length uses inner/outer subsets with a stable center; each remains distinct from radial size and distance.
    - Upper-lid lift and lower-lid drop touch only their respective lid subsets; inner/outer corner fields touch only their side-aware corner subset.
    - Signed tilt rotates complete contours about each center with opposite target motion for positive/negative values and unchanged center radius.
  </behavior>
  <action>Extend BeautyEffectiveStrengths and BeautySafetyCaps with the ten new fields using conservative provisional constants (documented in code as Phase 42 provisional). Refactor EyeWarpProvider into EyeWarpFieldEmissions and preserve the four shipped helpers unchanged in neutral behavior. Add semantic-support helpers with a nil-support compatibility path. Implement bounded finite source/target/radius/falloff construction for eyeHeight, eyeLength, upperEyelidLift, lowerEyelidDrop, eyeTilt, innerCornerOpen, and outerCornerOpen. Use image-normalized downward-y semantics explicitly, preserve signed strength for tilt direction, and keep each array independently addressable.</action>
  <acceptance_criteria>Provider tests prove source subsets, stable centers, local falloffs, distinct vectors versus nearest neighbors, both tilt signs, finite unit-clamped control points, and deterministic repeatability. Existing four-field tests remain green with all new fields zero.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.EyeWarpProviderTests</automated></verify>
  <done>EYE-08 through EYE-11 provider geometry is independently emitted and bounded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 42-01-02: Implement pupil, automatic gaze, and measured symmetry emissions</name>
  <files>BeautySDK/Sources/BeautyEffects/Warp/EyeWarpProvider.swift, BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift</files>
  <behavior>
    - Pupil size emits pupil-local radial vectors only for a validated pupil and owning contour.
    - Gaze correction moves an observed pupil monotonically toward its contour center, with a neutral dead zone and no manual direction input.
    - Symmetry reduces measured pair center/span/tilt differences toward a midpoint and no-ops for neutral, absent, or implausible pairs without mirroring identity.
  </behavior>
  <action>Add pupilSize, gazeCorrection, and eyeSymmetry arrays to EyeWarpFieldEmissions and implement support-specific validation. Keep pupil and gaze arrays empty when either pupil is missing/invalid; apply a finite provisional dead zone before emitting gaze. For symmetry require both semantic supports, calculate measured differences from center/span/tilt, and emit bounded movements toward conservative pair midpoints without swapping contour identity. Add direct and repeated-call tests for monotonicity, opposite/neutral cases, local radii, malformed support, and no-fabrication behavior.</action>
  <acceptance_criteria>EYE-12, EYE-13, and EYE-14 provider tests pass with no public/raw support exposure, no manual gaze field, and no work for neutral or implausible evidence.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.EyeWarpProviderTests</automated></verify>
  <done>All fourteen named provider emissions exist and pupil/gaze/symmetry semantics are evidence-driven.</done>
</task>

</tasks>

<threat_model>ASVS L1: malformed or absent private support must not crash, fabricate geometry, or disclose coordinates. Enforce finite/unit-clamped vectors, local support checks, bounded arrays, package-internal types, and aggregate-only diagnostics. No new target, dependency, renderer, Demo, network, or commercial path.</threat_model>

<verification>Run focused provider tests after each task and confirm no public/Codable/diagnostic support surface was added.</verification>

<success_criteria>Provider owns fourteen independently testable emissions with distinct bounded transforms and local evidence gating.</success_criteria>

<output>Create `42-01-SUMMARY.md` with provisional constants, field-emission counts, focused tests, and explicit Phase 43/44 non-claims.</output>
