---
phase: 42-independent-eye-geometry-and-pipeline-integration
plan: "02"
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
  - BeautySDK/Sources/BeautyEffects/Warp/EyeWarpProvider.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift
autonomous: true
requirements: [EYE-08, EYE-09, EYE-10, EYE-11, EYE-12, EYE-13, EYE-14, EYE-15]
must_haves:
  truths:
    - "Resolver normalizes and caps all fourteen eye strengths, then sanitizes each empty named emission before accounting."
    - "Invalid pupil support zeros only pupilSize/gazeCorrection; missing either contour preserves complete eye-domain skip and valid siblings continue."
    - "Every isolated new scalar reaches the existing public facade route without new API or diagnostic geometry payload."
  artifacts:
    - "BeautyEffectResolver calls EyeWarpFieldEmissions preflight/final sanitization and includes ten fields in requiresFaceGeometry, requested-eye, domain, warning, metric, and zeroing paths."
    - "Tests cover active/skipped domains, neutral defaults, cap counts, stale/reused/no-face behavior, and redaction."
  key_links:
    - "BeautyParameters normalized values -> BeautyEffectiveStrengths -> EyeWarpProvider fieldEmissions -> sanitizing -> BeautyEffectPlan accounting."
---

<objective>
Integrate fourteen eye emissions into resolver and facade planning with strict
field-local eligibility and unchanged degradation/privacy behavior.
</objective>

<read_first>
AGENTS.md; PLANS.md; .planning/PROJECT.md; .planning/ROADMAP.md; .planning/REQUIREMENTS.md; .planning/STATE.md; 42-CONTEXT.md; 42-RESEARCH.md; 42-PATTERNS.md; 42-01-SUMMARY.md; BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift; BeautySDK/Sources/BeautyEffects/Warp/EyeWarpProvider.swift; BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift; BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
</read_first>

<tasks>

<task type="auto" tdd="true">
  <name>Task 42-02-01: Thread ten fields through resolver strengths and field-local sanitization</name>
  <files>BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift, BeautySDK/Sources/BeautyEffects/Warp/EyeWarpProvider.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift</files>
  <behavior>
    - Each new public field is normalized, provisionally capped, counted independently, and represented in effective strengths.
    - Provider-empty fields are set to zero before active domains, point counts, capped/weakened totals, warnings, metrics, and dispatch.
    - A valid eye field remains active when a neighboring field has no eligible emission.
  </behavior>
  <action>Extend resolver normalization/capping and requested-face scans for all ten fields. Add an EyeWarpFieldEmissions preflight pass and `sanitizing(_:)` call that clears only the corresponding effective field when its named array is empty; preserve signed eyeTilt and positive-only fields. Ensure the eye domain checks final provider output, reports only existing aggregate warning/metric codes, and computes geometry point counts from final eligible arrays. Add table-driven cap, field-local empty, neutral/default, and redaction assertions for every new field.</action>
  <acceptance_criteria>All ten fields resolve through BeautyEffectPlan with provisional caps, empty field work is absent from every aggregate, and valid contour siblings remain active.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.BeautyEffectResolverTests</automated></verify>
  <done>EYE-15 preflight/final field-local accounting is wired for isolated eye plans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 42-02-02: Preserve complete-eye degradation and facade routing</name>
  <files>BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift, BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift, BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift</files>
  <behavior>
    - Missing either contour skips all eye-domain work with the stable aggregate warning while safe non-eye domains continue.
    - Missing/implausible pupils remove only pupilSize and gazeCorrection; contour fields remain eligible.
    - Reused/stale/no-face transitions retain existing complete-eye policy and no raw support appears in plan diagnostics.
  </behavior>
  <action>Extend resolver zeroing and eye requested-work helpers for the ten fields without changing the established reused/stale complete-eye skip. Assert explicit observed malformed/missing sides never reactivate legacy proxy fallback; nil observed support retains only the compatibility fixture path. Exercise public `BeautySDK`/engine resolution for each isolated field and verify all fourteen names route through the existing detection → adapter → resolver → unified pipeline with no new pass or API.</action>
  <acceptance_criteria>Degradation suites prove contour-complete, pupil-local, stale/reused, no-face, safe-sibling, warning, metric, and redaction behavior for all new fields.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.MissingLandmarkDegradationTests &amp;&amp; swift test --package-path BeautySDK --filter BeautyEffectsTests.EyeWarpProviderTests</automated></verify>
  <done>Resolver/facade field-local eligibility and complete-eye gating satisfy EYE-15.</done>
</task>

</tasks>

<threat_model>ASVS L1: block malformed support, stale/reused misuse, aggregate-accounting drift, and raw geometry disclosure. Keep fixed warning codes, aggregate metrics, package-private support, and no public/persistent geometry.</threat_model>

<verification>Run resolver, degradation, and provider-focused tests; inspect `git diff --check` and ensure no Package.swift/Demo/dependency changes.</verification>

<success_criteria>All fourteen fields reach the existing resolver/facade route with field-local eligibility and safe degradation.</success_criteria>

<output>Create `42-02-SUMMARY.md` with focused counts and explicit deferred output/final-cap/promotion claims.</output>
