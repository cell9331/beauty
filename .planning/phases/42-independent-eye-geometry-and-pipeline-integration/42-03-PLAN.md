---
phase: 42-independent-eye-geometry-and-pipeline-integration
plan: "03"
type: execute
wave: 3
depends_on: ["42-02"]
files_modified:
  - BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift
  - BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
  - BeautySDK/Sources/BeautyEffects/Render/BeautyGeometryEffectPipeline.swift
  - BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
autonomous: true
requirements: [EYE-08, EYE-09, EYE-10, EYE-11, EYE-12, EYE-13, EYE-14, EYE-15]
must_haves:
  truths:
    - "Combined face, eye, nose, and mouth planning uses one retained baseline and includes each eligible eye field exactly once in total/scaling."
    - "Conflict recomputation removes final-scale-empty fields monotonically and remains bounded for fourteen eye plus six nose plus eight mouth named emissions."
    - "Unified pipeline control-point counts agree with resolver metrics and no field is double-weakened or silently reintroduced."
  artifacts:
    - "GeometryConflictResolver accounts for ten new eye strengths alongside shipped eye/nose/mouth fields."
    - "BeautyEffectResolver conflict loop re-evaluates EyeWarpFieldEmissions, NoseWarpFieldEmissions, and MouthWarpFieldEmissions from one baseline with explicit bound."
    - "Combined safety tests cover all fourteen eye names, signed tilt, field removal, siblings, totals, scales, and redacted warnings."
  key_links:
    - "Resolver preflight strengths -> GeometryConflictResolver -> provider emission masks -> retained baseline -> final BeautyEffectPlan and unified pipeline points."
---

<objective>
Integrate independent eye work into combined geometry conflict resolution without
double weakening or aggregate-accounting drift.
</objective>

<read_first>
AGENTS.md; PLANS.md; .planning/PROJECT.md; .planning/ROADMAP.md; .planning/REQUIREMENTS.md; .planning/STATE.md; 42-CONTEXT.md; 42-RESEARCH.md; 42-PATTERNS.md; 42-02-SUMMARY.md; BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift; BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift; BeautySDK/Sources/BeautyEffects/Render/BeautyGeometryEffectPipeline.swift; BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift
</read_first>

<tasks>

<task type="auto" tdd="true">
  <name>Task 42-03-01: Add all fourteen eye fields to conflict totals and scaling</name>
  <files>BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift, BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift, BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift</files>
  <behavior>
    - Geometry total and weakened count include eyeHeight, eyeLength, both lids, pupil/gaze, signed tilt, both corners, and symmetry exactly once.
    - Positive-only strengths remain positive and signed eyeTilt preserves sign after combined scaling.
    - Existing face/nose/mouth scaling and active domains remain unchanged for neutral new fields.
  </behavior>
  <action>Extend `GeometryConflictResolver` total, scale, and nonzero-count logic for all ten new fields and preserve sign using the existing absolute-total pattern. Update resolver conflict preflight to pass eye emissions through the same sanitizing route as nose and mouth. Add combined tests with unequal all-fourteen eye strengths, signed tilt in both directions, neutral fields, expected scale/weakening warning, and redacted metadata.</action>
  <acceptance_criteria>Combined totals contain each field once, neutral behavior remains byte-for-byte equivalent for shipped fields, and signed tilt never loses direction.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.CombinedEffectSafetyTests</automated></verify>
  <done>Conflict totals/scales include the full eye field set without aliasing or sign loss.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 42-03-02: Bound monotone eye/nose/mouth emission convergence and pipeline agreement</name>
  <files>BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift, BeautySDK/Sources/BeautyEffects/Render/BeautyGeometryEffectPipeline.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift, BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift</files>
  <behavior>
    - Resolver re-evaluates eye, nose, and mouth field emissions after each conflict scale from one retained baseline.
    - Empty final emissions are removed once, valid siblings survive, and the loop terminates at the explicit combined-field bound.
    - Resolver geometry point count equals final unified pipeline control-point count for the same face/strengths.
  </behavior>
  <action>Extend `resolveGeometryConflict` to call EyeWarpProvider field emissions and sanitize the retained baseline in the same pass as six nose/eight mouth emissions. Replace the old eye+nose/mouth bound with an explicit 28-removal maximum (or a named bound derived from the fourteen+six+eight field count) while retaining monotone equality termination. Ensure final domain accounting invokes the same providers as `BeautyGeometryEffectPipeline`. Add adversarial tests where eye fields cross provider thresholds after scaling, verify no double weakening, all aggregate metrics/warnings remain redacted, and safe non-eye domains continue.</action>
  <acceptance_criteria>Combined tests pass with all fourteen eye fields, convergence terminates within the bound, final-empty fields are absent from totals/counts/metrics/dispatch, and pipeline/resolver counts agree.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.BeautyEffectResolverTests &amp;&amp; swift test --package-path BeautySDK --filter BeautyEffectsTests.CombinedEffectSafetyTests</automated></verify>
  <done>EYE-15 combined conflict convergence is monotone, bounded, and consistent with unified pipeline output.</done>
</task>

</tasks>

<threat_model>ASVS L1: prevent double weakening, stale retained geometry, metric/count leakage, and unbounded loops. Enforce a single baseline, monotone field masks, explicit 28-field bound, signed direction preservation, and aggregate diagnostics.</threat_model>

<verification>Run combined safety/resolver tests, then full SwiftPM and `git diff --check` before the phase gate.</verification>

<success_criteria>Combined geometry converges through one bounded baseline while all eligible eye siblings and non-eye domains remain correct.</success_criteria>

<output>Create `42-03-SUMMARY.md` with convergence bound, counts, sign-preservation evidence, and deferred Phase 44 exact ledger claim.</output>
