---
phase: 28-face-shape-slice-completion-and-documentation-closeout
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - BeautySDK/Sources/BeautyExampleRenderer/main.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift
  - .planning/phases/28-face-shape-slice-completion-and-documentation-closeout/check_face_shape_renderer_outputs.py
autonomous: true
requirements:
  - FACE-01
  - FACE-02
  - FACE-03
  - FACE-04
  - FACE-05
  - FACE-06
user_setup: []
must_haves:
  truths:
    - "Renderer evidence exists for `faceSlim`, `faceSmall`, `faceVShape`, `jawSlim`, positive `chinLength`, and negative `chinLength`, with `下颌线` sharing `jawSlim` per D-01, D-02, D-06, and D-08."
    - "Every Phase 28 per-tool renderer case preserves input dimensions and proves top-region geometry-vs-`geometryBaseline_noop` delta above the watermark band per D-07."
    - "Renderer changes add no new public `BeautyParameters`, Demo behavior, entitlement behavior, or distinct `下颌线` algorithm per D-05."
  artifacts:
    - path: "BeautySDK/Sources/BeautyExampleRenderer/main.swift"
      provides: "Public-facade renderer cases for Phase 28 scoped face-shape tools"
      contains: "faceSlim_0p45, faceSmall_0p35, faceVShape_0p35, jawSlim_0p35, chinLength_plus0p30, chinLength_minus0p30"
    - path: "BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift"
      provides: "Renderer inventory, public import, alias, and no raw metadata regression coverage"
    - path: ".planning/phases/28-face-shape-slice-completion-and-documentation-closeout/check_face_shape_renderer_outputs.py"
      provides: "Phase 28 generated PNG helper for per-tool dimensions and top-region baseline deltas"
  key_links:
    - from: "BeautySDK/Sources/BeautyExampleRenderer/main.swift"
      to: "BeautySDK/Sources/BeautySDK/BeautyEngine.swift"
      via: "public `BeautyEngine.processResult(image:metadata:parameters:)` call"
      pattern: "processResult\\("
    - from: ".planning/phases/28-face-shape-slice-completion-and-documentation-closeout/check_face_shape_renderer_outputs.py"
      to: "example-images/out"
      via: "expected Phase 28 output names and top-region baseline comparisons"
      pattern: "PHASE28_GEOMETRY_CASE_IDS"
---

<objective>
Add per-tool public-facade renderer cases and a Phase 28 helper that mechanically proves same-dimension, non-watermark-only saved-output evidence for the scoped `脸型` parameters.

Purpose: satisfy the facade-visible output bar for FACE-01 through FACE-06 without adding UI, public API, or a separate `下颌线` behavior.
Output: six Phase 28 renderer case IDs, updated renderer regression tests, and `check_face_shape_renderer_outputs.py`.
</objective>

## Artifacts This Phase Produces

- Renderer case ID `faceSlim_0p45` in `BeautySDK/Sources/BeautyExampleRenderer/main.swift`, using `BeautyParameters(faceSlim: 0.45)` for FACE-01.
- Renderer case ID `faceSmall_0p35` in `BeautySDK/Sources/BeautyExampleRenderer/main.swift`, using `BeautyParameters(faceSmall: 0.35)` for FACE-02.
- Renderer case ID `chinLength_plus0p30` in `BeautySDK/Sources/BeautyExampleRenderer/main.swift`, using `BeautyParameters(chinLength: 0.30)` for the positive half of FACE-03 per D-08.
- Renderer case ID `chinLength_minus0p30` in `BeautySDK/Sources/BeautyExampleRenderer/main.swift`, using `BeautyParameters(chinLength: -0.30)` for the negative half of FACE-03 per D-08.
- Renderer case ID `faceVShape_0p35` in `BeautySDK/Sources/BeautyExampleRenderer/main.swift`, using `BeautyParameters(faceVShape: 0.35)` for FACE-04.
- Renderer case ID `jawSlim_0p35` in `BeautySDK/Sources/BeautyExampleRenderer/main.swift`, using `BeautyParameters(jawSlim: 0.35)` for FACE-05 and alias-backed FACE-06 per D-01 through D-04.
- Updated `BeautyRendererOutputRegressionTests.expectedRendererCaseIDs`, appending the six Phase 28 IDs after `faceShapeCombo_0p35`.
- Test method `BeautyRendererOutputRegressionTests.testPhase28FaceShapeCasesUseOnlyExistingPublicParameters`.
- Test method `BeautyRendererOutputRegressionTests.testJawlineAliasSharesJawSlimRendererEvidence`.
- Helper `.planning/phases/28-face-shape-slice-completion-and-documentation-closeout/check_face_shape_renderer_outputs.py`.
- Helper constants `BASELINE_CASE_ID`, `PHASE28_GEOMETRY_CASE_IDS`, `PORTRAIT_FIXTURE_NAMES`, `NO_FACE_FIXTURE_NAME`, and expected output count `102`.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/28-face-shape-slice-completion-and-documentation-closeout/28-CONTEXT.md
@.planning/phases/28-face-shape-slice-completion-and-documentation-closeout/28-RESEARCH.md
@.planning/phases/28-face-shape-slice-completion-and-documentation-closeout/28-VALIDATION.md
@.planning/phases/28-face-shape-slice-completion-and-documentation-closeout/28-PATTERNS.md
@.planning/phases/27-geometry-render-output-and-verification-harness/27-03-SUMMARY.md
@.planning/phases/27-geometry-render-output-and-verification-harness/27-04-SUMMARY.md
@.planning/phases/27-geometry-render-output-and-verification-harness/check_geometry_renderer_outputs.py
@BeautySDK/Sources/BeautyExampleRenderer/main.swift
@BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift
@docs/meitu-function-blueprint/EXAMPLE_IMAGE_VALIDATION.md
@example-images/input
@example-images/out
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 28-01-01: Add Phase 28 per-tool renderer cases and inventory tests</name>
  <files>BeautySDK/Sources/BeautyExampleRenderer/main.swift, BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift</files>
  <read_first>
    - `BeautySDK/Sources/BeautyExampleRenderer/main.swift`
    - `BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift`
    - `.planning/phases/28-face-shape-slice-completion-and-documentation-closeout/28-CONTEXT.md`
    - `.planning/phases/28-face-shape-slice-completion-and-documentation-closeout/28-PATTERNS.md`
    - `DESIGN.md`
    - `SECURITY.md`
  </read_first>
  <behavior>
    - Test 1: `expectedRendererCaseIDs` equals the existing 11 case IDs plus `faceSlim_0p45`, `faceSmall_0p35`, `chinLength_plus0p30`, `chinLength_minus0p30`, `faceVShape_0p35`, and `jawSlim_0p35` in declaration order.
    - Test 2: Each Phase 28 renderer case uses exactly one existing public `BeautyParameters` face-shape field, except no separate `下颌线` or `jawLine` field appears.
    - Test 3: `jawSlim_0p35` is the shared renderer evidence for `下颌角` and alias-backed `下颌线` per D-01 through D-04.
    - Test 4: `BeautyExampleRenderer/main.swift` imports `BeautySDK` and no internal SDK target.
  </behavior>
  <action>Add the six Phase 28 `RenderCase` entries immediately after `faceShapeCombo_0p35`: `faceSlim_0p45` with `faceSlim: 0.45`; `faceSmall_0p35` with `faceSmall: 0.35`; `chinLength_plus0p30` with `chinLength: 0.30`; `chinLength_minus0p30` with `chinLength: -0.30`; `faceVShape_0p35` with `faceVShape: 0.35`; and `jawSlim_0p35` with `jawSlim: 0.35`. Update `BeautyRendererOutputRegressionTests.expectedRendererCaseIDs` to include those exact IDs. Add tests named `testPhase28FaceShapeCasesUseOnlyExistingPublicParameters` and `testJawlineAliasSharesJawSlimRendererEvidence`. The tests must scan the renderer source and fail if `jawLine`, `faceLine`, `下颌线`, new Demo strings, entitlement/pro tokens, or internal SDK target imports appear in renderer source. Do not add a new public `BeautyParameters` field, distinct `下颌线` case, Demo behavior, entitlement/pro handling, network/cloud behavior, or algorithm split per D-01, D-05, D-06, and D-10.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyRendererOutputRegressionTests</automated>
    <automated>swift build --package-path BeautySDK --product BeautyExampleRenderer</automated>
    <automated>rg -n "import Beauty(Core|Detection|Effects|Render|Resources)" BeautySDK/Sources/BeautyExampleRenderer/main.swift BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift; test $? -eq 1</automated>
    <automated>rg -n "jawLine|faceLine|下颌线|entitlement|pro|VIP|network|cloud" BeautySDK/Sources/BeautyExampleRenderer/main.swift BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift; test $? -eq 1</automated>
    <automated>git diff --check -- BeautySDK/Sources/BeautyExampleRenderer/main.swift BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - Renderer inventory has 17 total case IDs after this task: the existing 11 plus the six exact Phase 28 case IDs.
    - FACE-01 through FACE-05 each have a distinct renderer case; FACE-06 is represented only by shared `jawSlim_0p35` evidence.
    - `chinLength` has both positive and negative cases with signed values.
    - Renderer remains public-facade-only and contains no new public parameter, Demo behavior, entitlement/pro path, or cloud/network behavior.
  </acceptance_criteria>
  <done>Phase 28 per-tool renderer cases are present and guarded by focused renderer inventory, alias, and public-boundary tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 28-01-02: Create per-tool top-region renderer helper and run output checks</name>
  <files>.planning/phases/28-face-shape-slice-completion-and-documentation-closeout/check_face_shape_renderer_outputs.py</files>
  <read_first>
    - `.planning/phases/27-geometry-render-output-and-verification-harness/check_geometry_renderer_outputs.py`
    - `.planning/phases/28-face-shape-slice-completion-and-documentation-closeout/28-VALIDATION.md`
    - `BeautySDK/Sources/BeautyExampleRenderer/main.swift`
    - `BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift`
    - `docs/meitu-function-blueprint/EXAMPLE_IMAGE_VALIDATION.md`
    - `SECURITY.md`
  </read_first>
  <behavior>
    - Test 1: Helper fails if any expected output from 6 fixtures and 17 renderer cases is missing, zero-byte, unreadable, not PNG, or dimension-mismatched.
    - Test 2: Helper compares each of the six Phase 28 per-tool case IDs against the same fixture's `geometryBaseline_noop` for portrait fixtures `e1.png` through `e5.png`.
    - Test 3: Helper reports exactly 30 portrait per-tool top-region comparisons and fails when a comparison differs only in the watermark band.
    - Test 4: Helper output contains only relative names, counts, dimensions, fixture names, and case IDs.
  </behavior>
  <action>Create `check_face_shape_renderer_outputs.py` by mirroring the Phase 27 standard-library PNG decoder and preserving the `comparable_top_region_rows` and `top_region_differs` approach that excludes the watermark band. Set `BASELINE_CASE_ID` to `geometryBaseline_noop`; set `PHASE28_GEOMETRY_CASE_IDS` to `faceSlim_0p45`, `faceSmall_0p35`, `chinLength_plus0p30`, `chinLength_minus0p30`, `faceVShape_0p35`, and `jawSlim_0p35`; keep `PORTRAIT_FIXTURE_NAMES` as `e1.png` through `e5.png`; keep `NO_FACE_FIXTURE_NAME` as `no-face-gradient.png`; and verify expected output count `102` after all renderer cases run. The pass output must report checked/expected outputs, dimension buckets, `30/30` portrait top-region comparisons, representative no-face generated output presence, fixture names, and case IDs. Do not print hashes, raw pixels, absolute local paths, coordinates, landmarks, bounding boxes, control points, raw framework errors, raw JSON, image bytes, or generated PNG baselines per D-07, D-09, D-10, and D-15.</action>
  <verify>
    <automated>swift run --package-path BeautySDK BeautyExampleRenderer --input example-images/input --output example-images/out</automated>
    <automated>python3 .planning/phases/28-face-shape-slice-completion-and-documentation-closeout/check_face_shape_renderer_outputs.py --input example-images/input --output example-images/out | tee /tmp/phase28-face-shape-helper.out</automated>
    <automated>rg -n "102/102|30/30|faceSlim_0p45|faceSmall_0p35|chinLength_plus0p30|chinLength_minus0p30|faceVShape_0p35|jawSlim_0p35" /tmp/phase28-face-shape-helper.out</automated>
    <automated>git check-ignore example-images/out/e1__faceSlim_0p45.png example-images/out/e1__chinLength_minus0p30.png example-images/out/e1__jawSlim_0p35.png example-images/out/no-face-gradient__jawSlim_0p35.png</automated>
    <automated>rg -n "/Users/|/private/var|NSError|AVError|rawPresetJson:|raw JSON:|image bytes:|landmarks=\\[|landmarkCoordinates:|rawLandmark:|VNFaceObservation\\(|boundingBox:|controlPoint\\(|SIMD[0-9]?\\(" /tmp/phase28-face-shape-helper.out; test $? -eq 1</automated>
    <automated>git diff --check -- .planning/phases/28-face-shape-slice-completion-and-documentation-closeout/check_face_shape_renderer_outputs.py</automated>
  </verify>
  <acceptance_criteria>
    - Renderer command writes expected ignored PNGs under `example-images/out/` for 6 fixtures and 17 case IDs.
    - Helper validates non-empty PNG files, input/output dimension preservation, and 30 top-region geometry-vs-baseline deltas for Phase 28 per-tool cases.
    - Helper evidence cannot pass from watermark-label-only differences.
    - Generated outputs remain ignored and no generated PNG baseline, hash, or raw geometry payload enters git or helper output.
  </acceptance_criteria>
  <done>Phase 28 has deterministic per-tool saved-output helper evidence ready for final documentation capture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer CLI -> local filesystem | Public-facade renderer writes generated PNGs under ignored `example-images/out/`. |
| Helper -> evidence docs | Helper output will be pasted into Markdown evidence and must stay redacted and non-claiming. |
| Watermarked output -> geometry evidence | Bottom watermark labels must not be counted as geometric image differences. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01-01 | Information Disclosure | Renderer/helper output | mitigate | Helper and scans allow only relative names, counts, dimensions, and case IDs; raw geometry, local paths, raw JSON, image bytes, hashes, and pixel payloads are forbidden in outputs. |
| T-28-01-02 | Spoofing / Repudiation | Per-tool geometry evidence | mitigate | Helper must compare top-region pixels above the watermark band against `geometryBaseline_noop`; full watermarked PNG differences are insufficient. |
| T-28-01-03 | Information Disclosure / Repudiation | Generated PNG artifacts | mitigate | Generated PNGs stay under `example-images/out/`; `git check-ignore` must pass for representative Phase 28 outputs. |
| T-28-01-04 | Tampering | Public API and renderer scope | mitigate | Renderer tests and scans forbid new `BeautyParameters`, `jawLine`, Demo/pro/entitlement/cloud tokens, and internal SDK target imports. |
| T-28-01-05 | Spoofing / Repudiation | Renderer comments and helper output | mitigate | No wording in this plan may claim UI completion, commercial quality, device parity, broad Meitu parity, or release readiness; final docs are handled by Plan 28-04 scans. |
| T-28-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install is allowed; helper uses Python 3 standard library only. |
</threat_model>

<verification>
Overall Plan 28-01 checks:

- `swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyRendererOutputRegressionTests`
- `swift build --package-path BeautySDK --product BeautyExampleRenderer`
- `swift run --package-path BeautySDK BeautyExampleRenderer --input example-images/input --output example-images/out`
- `python3 .planning/phases/28-face-shape-slice-completion-and-documentation-closeout/check_face_shape_renderer_outputs.py --input example-images/input --output example-images/out`
- `git check-ignore` for representative Phase 28 generated outputs.
- Public import, hidden API expansion, helper-output raw leak, and scoped `git diff --check` scans.
</verification>

<success_criteria>
Plan 28-01 succeeds when six scoped Phase 28 renderer cases exist, `下颌线` has no separate behavior and shares `jawSlim_0p35`, and the helper proves same-dimension top-region deltas for every per-tool case across portrait fixtures without committing generated PNGs.
</success_criteria>

<output>
Create `.planning/phases/28-face-shape-slice-completion-and-documentation-closeout/28-01-SUMMARY.md` when done.
</output>
