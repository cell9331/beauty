---
phase: 27-geometry-render-output-and-verification-harness
plan: "02"
type: execute
wave: 2
depends_on:
  - 27-01
files_modified:
  - BeautySDK/Sources/BeautySDK/BeautyEngine.swift
  - BeautySDK/Sources/BeautySDK/BeautyEngineGeometryDetection.swift
  - BeautySDK/Sources/BeautyEffects/Render/BeautyColorEffectPipeline.swift
  - BeautySDK/Sources/BeautyEffects/Render/BeautyGeometryEffectPipeline.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift
autonomous: true
requirements:
  - GEO-03
  - GEO-04
requirements_addressed:
  - GEO-03
  - GEO-04
user_setup: []
must_haves:
  truths:
    - "Selected-face geometry affects the still-image output through SDK internals before the renderer writes PNG evidence, per D-01, D-09, and D-10."
    - "Geometry output preserves input dimensions and compares against a no-geometry baseline rather than only the source image, per D-09 and D-10."
    - "No-face and unusable detection still degrade through redacted summaries, warnings, and metrics without raw geometry leakage, per D-13, D-14, D-16, and D-17."
  artifacts:
    - path: "BeautySDK/Sources/BeautySDK/BeautyEngineGeometryDetection.swift"
      provides: "Internal route carries selected face for rendering"
      contains: "selectedFaceObservation"
    - path: "BeautySDK/Sources/BeautyEffects/Render/BeautyColorEffectPipeline.swift"
      provides: "Geometry-aware still-image render entry"
      contains: "selectedFaceObservation"
    - path: "BeautySDK/Sources/BeautyEffects/Render/BeautyGeometryEffectPipeline.swift"
      provides: "Deterministic CIImage geometry proxy used by still-image output"
      contains: "applyMVPProxy"
    - path: "BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift"
      provides: "Pre-watermark geometry-vs-baseline output delta tests"
  key_links:
    - from: "BeautySDK/Sources/BeautySDK/BeautyEngine.swift"
      to: "BeautySDK/Sources/BeautyEffects/Render/BeautyColorEffectPipeline.swift"
      via: "geometry-aware image apply call"
      pattern: "selectedFaceObservation"
    - from: "BeautySDK/Sources/BeautyEffects/Render/BeautyColorEffectPipeline.swift"
      to: "BeautySDK/Sources/BeautyEffects/Render/BeautyGeometryEffectPipeline.swift"
      via: "CIImage geometry proxy after color/lip work"
      pattern: "BeautyGeometryEffectPipeline"
---

<objective>
Wire selected-face geometry into still-image rendering so a geometry-triggering public facade request can produce deterministic same-dimension output that differs from a no-geometry baseline.

Purpose: close the Phase 26 saved-output gap identified in research: geometry planning exists, but still-image rendering currently calls the no-face image pipeline.
Output: internal geometry-aware image render path and focused facade tests proving same-dimension non-identity against a baseline with redacted metadata.
</objective>

## Artifacts This Phase Produces

- `BeautyEngineGeometryRoute.selectedFaceObservation`, internal/package only, carrying the selected package observation from detection to rendering.
- `BeautyColorEffectPipeline.apply(to:plan:selectedFaceObservation:)`, package/internal, adapting selected observations to internal `FaceGeometry` inside `BeautyEffects`.
- `BeautyGeometryEffectPipeline.applyMVPProxy(to: CIImage, plan: BeautyEffectPlan, face: FaceGeometry)`, internal, deterministic, cropped to input extent, and driven by geometry control-point count.
- Updated `BeautyEngine.processResult(image:metadata:parameters:)` call site that uses the geometry-aware image render entry.
- Test method `BeautyEngineGeometryFacadeTests.testSelectedFaceGeometryChangesImageBeforeWatermarkComparedToNoGeometryBaseline`.
- Test method `BeautyEngineGeometryFacadeTests.testNoFaceGeometryRequestPreservesDimensionsAndRedactedDegradation`.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/27-geometry-render-output-and-verification-harness/27-CONTEXT.md
@.planning/phases/27-geometry-render-output-and-verification-harness/27-RESEARCH.md
@.planning/phases/27-geometry-render-output-and-verification-harness/27-VALIDATION.md
@.planning/phases/27-geometry-render-output-and-verification-harness/27-PATTERNS.md
@.planning/phases/27-geometry-render-output-and-verification-harness/27-01-SUMMARY.md
@ARCHITECTURE.md
@DESIGN.md
@SECURITY.md
@RELIABILITY.md
@BeautySDK/Sources/BeautySDK/BeautyEngine.swift
@BeautySDK/Sources/BeautySDK/BeautyEngineGeometryDetection.swift
@BeautySDK/Sources/BeautyEffects/Render/BeautyColorEffectPipeline.swift
@BeautySDK/Sources/BeautyEffects/Render/BeautyGeometryEffectPipeline.swift
@BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 27-02-01: Carry selected-face geometry from facade routing into image rendering</name>
  <files>BeautySDK/Sources/BeautySDK/BeautyEngine.swift, BeautySDK/Sources/BeautySDK/BeautyEngineGeometryDetection.swift, BeautySDK/Sources/BeautyEffects/Render/BeautyColorEffectPipeline.swift, BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift</files>
  <read_first>
    - `BeautySDK/Sources/BeautySDK/BeautyEngine.swift`
    - `BeautySDK/Sources/BeautySDK/BeautyEngineGeometryDetection.swift`
    - `BeautySDK/Sources/BeautyEffects/Render/BeautyColorEffectPipeline.swift`
    - `BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift`
    - `BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift`
    - `ARCHITECTURE.md` sections 4, 6.4, and dependency table
    - `DESIGN.md` sections 8 and 9
  </read_first>
  <behavior>
    - Test 1: With `SDKTestingFaceDetectionProvider([.usableFace])`, the D-06 geometry parameters return output with the same extent as the input and rendered RGBA bytes different from `BeautyParameters()` baseline before watermarking.
    - Test 2: With `SDKTestingFaceDetectionProvider([.noFace])`, the same geometry request preserves input extent, uses `.noFace` summary, includes `face_effects_skipped_no_face`, and remains redacted.
  </behavior>
  <action>Extend `BeautyEngineGeometryRoute` with internal `selectedFaceObservation: BeautyFaceObservation?` and set it only from `detection.observations.first` when geometry detection runs. In `BeautyEngine.processResult(image:metadata:parameters:)`, replace the color-only image apply call with the package/internal geometry-aware image apply entry. In `BeautyColorEffectPipeline`, add `package static func apply(to image: CIImage, plan: BeautyEffectPlan, selectedFaceObservation: BeautyFaceObservation?) -> CIImage` and adapt the observation through `BeautyFaceGeometryAdapter.makeGeometry(from:)` inside `BeautyEffects`. Keep `FaceGeometry`, bounds, landmarks, and control points internal; do not add public or SPI raw geometry exports per D-17.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyEngineGeometryFacadeTests/testSelectedFaceGeometryChangesImageBeforeWatermarkComparedToNoGeometryBaseline</automated>
    <automated>swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyEngineGeometryFacadeTests/testNoFaceGeometryRequestPreservesDimensionsAndRedactedDegradation</automated>
    <automated>rg -n "public .*BeautyFaceObservation|public .*FaceGeometry|public .*WarpControlPoint|@_spi\\(Testing\\).*BeautyFaceObservation|@_spi\\(Testing\\).*FaceGeometry" BeautySDK/Sources/BeautySDK BeautySDK/Sources/BeautyEffects; test $? -eq 1</automated>
    <automated>git diff --check -- BeautySDK/Sources/BeautySDK/BeautyEngine.swift BeautySDK/Sources/BeautySDK/BeautyEngineGeometryDetection.swift BeautySDK/Sources/BeautyEffects/Render/BeautyColorEffectPipeline.swift BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - `BeautyEngine.processResult(image:metadata:parameters:)` passes selected-face routing data into the image render tier without changing public signatures.
    - The output-delta test compares the D-06 geometry output to a no-geometry `BeautyParameters()` baseline before watermarking, satisfying D-10.
    - No-face output remains same-dimension and emits only redacted summary/warning/metric evidence, satisfying D-13, D-14, D-16, and D-17.
  </acceptance_criteria>
  <done>The public facade can produce deterministic same-dimension geometry output through SDK internals, and tests prove it is not only a watermark or color/filter artifact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 27-02-02: Add deterministic CIImage geometry proxy output inside the render tier</name>
  <files>BeautySDK/Sources/BeautyEffects/Render/BeautyColorEffectPipeline.swift, BeautySDK/Sources/BeautyEffects/Render/BeautyGeometryEffectPipeline.swift, BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift</files>
  <read_first>
    - `BeautySDK/Sources/BeautyEffects/Render/BeautyColorEffectPipeline.swift`
    - `BeautySDK/Sources/BeautyEffects/Render/BeautyGeometryEffectPipeline.swift`
    - `BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift`
    - `BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift`
    - `RELIABILITY.md` sections 6 and 7
  </read_first>
  <behavior>
    - Test 1: Geometry proxy output is deterministic for the same input, plan, and selected face.
    - Test 2: Geometry proxy output differs from no-geometry baseline when `.faceShape` is active and `beauty.effects.geometryPointCount` is positive.
    - Test 3: Every CIImage geometry proxy result is cropped to the original `image.extent`.
  </behavior>
  <action>Add a CIImage overload in `BeautyGeometryEffectPipeline` that uses `controlPoints(for:plan, face:)` and applies a bounded deterministic image transform only when geometry control points exist. Invoke this overload from `BeautyColorEffectPipeline.apply(to:image,plan,face:)` after existing color and lip-color work, and always return `cropped(to: image.extent)` per D-09. The proxy may remain mechanically simple, but it must be driven by internal geometry control-point evidence and must not emit coordinates, landmarks, point arrays, or quality/parity claims per D-12 and D-17.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyEngineGeometryFacadeTests/testSelectedFaceGeometryChangesImageBeforeWatermarkComparedToNoGeometryBaseline</automated>
    <automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.GeometryConflictResolverTests</automated>
    <automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.CombinedEffectSafetyTests/testCombinedHighStrengthAllDomainsCapAndWeakenGeometry</automated>
    <automated>git diff --check -- BeautySDK/Sources/BeautyEffects/Render/BeautyColorEffectPipeline.swift BeautySDK/Sources/BeautyEffects/Render/BeautyGeometryEffectPipeline.swift BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - The CIImage geometry proxy runs only when internal geometry control points exist for the plan and selected face.
    - Output extent equals input extent for selected-face, no-face, and no-geometry baseline paths.
    - Output-delta tests compare geometry against a no-geometry baseline, not only against input image bytes.
    - Tests and source expose no raw geometry payload across public or SPI boundaries.
  </acceptance_criteria>
  <done>Geometry-aware still-image rendering produces deterministic output changes from selected-face geometry while preserving dimensions and privacy boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BeautySDK facade -> BeautyEffects render tier | Package selected-face data crosses into internal rendering; public API must not expose the data. |
| Internal geometry plan -> CIImage output | Control-point-derived behavior changes pixels; no raw control points may be logged or surfaced. |
| XCTest output -> evidence | Pre-watermark comparisons prove behavior but must not print pixels or local paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02-01 | Information Disclosure | `BeautyEngineGeometryRoute.selectedFaceObservation` | mitigate | Keep route internal/package only and scan for public/SPI exports of observations, bounds, landmarks, `FaceGeometry`, and control points. |
| T-27-02-02 | Tampering | `BeautyColorEffectPipeline` geometry-aware apply | mitigate | Require output-delta tests to compare against `BeautyParameters()` baseline and same extent; this prevents a geometry metric-only pass. |
| T-27-02-03 | Repudiation | Geometry output evidence | mitigate | Tests must name the D-06 exact parameters and record no quality, naturalness, release, device parity, or Meitu parity claims. |
| T-27-02-04 | Information Disclosure | XCTest metadata assertions | mitigate | Reuse `assertRedacted` forbidden-token list for warnings, metrics, and summary reasons; do not print raw pixels or local paths. |
| T-27-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install is allowed in this plan; use existing SwiftPM targets and Apple frameworks only. |
</threat_model>

<verification>
Overall Plan 27-02 checks:

- `swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyEngineGeometryFacadeTests`
- `swift test --package-path BeautySDK --filter BeautyEffectsTests.GeometryConflictResolverTests`
- `swift test --package-path BeautySDK --filter BeautyEffectsTests.CombinedEffectSafetyTests/testCombinedHighStrengthAllDomainsCapAndWeakenGeometry`
- Public/SPI raw geometry export scan over `BeautySDK/Sources/BeautySDK`, `BeautySDK/Sources/BeautyDetection`, and `BeautySDK/Sources/BeautyEffects`.
- Scoped `git diff --check` for all files in `files_modified`.
</verification>

<success_criteria>
Plan 27-02 succeeds when a selected usable face causes a same-dimension still-image output delta against a no-geometry baseline, no-face degrades safely, and no public/SPI raw geometry boundary expands.
</success_criteria>

<output>
Create `.planning/phases/27-geometry-render-output-and-verification-harness/27-02-SUMMARY.md` when done.
</output>
