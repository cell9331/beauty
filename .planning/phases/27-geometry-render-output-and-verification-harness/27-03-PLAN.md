---
phase: 27-geometry-render-output-and-verification-harness
plan: "03"
type: execute
wave: 3
depends_on:
  - 27-02
files_modified:
  - BeautySDK/Sources/BeautyExampleRenderer/main.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift
  - .planning/phases/27-geometry-render-output-and-verification-harness/check_geometry_renderer_outputs.py
  - example-images/input/no-face-gradient.png
autonomous: true
requirements:
  - GEO-03
  - GEO-04
requirements_addressed:
  - GEO-03
  - GEO-04
user_setup: []
must_haves:
  truths:
    - "BeautyExampleRenderer contains exactly one combined face-shape geometry case and remains public-facade-only, per D-01, D-02, D-05, D-06, D-07, and D-08."
    - "Generated PNGs stay under ignored `example-images/out/` and evidence uses helper output, counts, dimensions, and factual notes only, per D-09, D-10, D-11, and D-12."
    - "Happy path and no-face saved-output evidence exist as renderer PNG outputs; missing, stale/reused, and combined-strength paths remain focused XCTest/helper evidence, per D-13, D-14, D-15, D-16, and D-17."
  artifacts:
    - path: "BeautySDK/Sources/BeautyExampleRenderer/main.swift"
      provides: "Renderer matrix with baseline and one combined face-shape geometry case"
      contains: "faceShapeCombo_0p35"
    - path: "BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift"
      provides: "Renderer inventory, public-facade import, no-face fixture, and geometry-case guard tests"
    - path: ".planning/phases/27-geometry-render-output-and-verification-harness/check_geometry_renderer_outputs.py"
      provides: "Phase 27 generated PNG helper with baseline comparison and no-face output checks"
    - path: "example-images/input/no-face-gradient.png"
      provides: "Dedicated deterministic no-face input fixture for D-15"
  key_links:
    - from: "BeautySDK/Sources/BeautyExampleRenderer/main.swift"
      to: "BeautySDK/Sources/BeautySDK/BeautyEngine.swift"
      via: "public `BeautyEngine.processResult(image:metadata:parameters:)` call"
      pattern: "processResult\\("
    - from: ".planning/phases/27-geometry-render-output-and-verification-harness/check_geometry_renderer_outputs.py"
      to: "example-images/out"
      via: "generated PNG inventory and baseline comparison"
      pattern: "faceShapeCombo_0p35"
---

<objective>
Extend the public-facade renderer matrix and generated-output helper so Phase 27 can produce deterministic ignored PNG evidence for geometry output and no-face degradation.

Purpose: satisfy GEO-03 saved-output evidence through `BeautyExampleRenderer` while satisfying GEO-04 no-face saved-output evidence without adding Demo UI or public raw geometry API.
Output: one baseline renderer case, one combined face-shape renderer case, a dedicated no-face fixture, focused renderer tests, and a Phase 27 helper for generated PNG checks.
</objective>

## Artifacts This Phase Produces

- Renderer case ID `geometryBaseline_noop` in `BeautySDK/Sources/BeautyExampleRenderer/main.swift`, using `BeautyParameters()` as the no-geometry baseline for D-10.
- Renderer case ID `faceShapeCombo_0p35` in `BeautySDK/Sources/BeautyExampleRenderer/main.swift`, using `faceSlim: 0.35`, `faceSmall: 0.30`, `faceVShape: 0.35`, `jawSlim: 0.30`, and `chinLength: 0.20` per D-06.
- Dedicated input fixture `example-images/input/no-face-gradient.png` for no-face saved-output evidence per D-15.
- Updated `BeautyRendererOutputRegressionTests.expectedRendererCaseIDs` with `geometryBaseline_noop` and `faceShapeCombo_0p35` appended after the existing 9 cases.
- Updated `BeautyRendererOutputRegressionTests.fixtureNames` with `no-face-gradient.png`.
- Test method `BeautyRendererOutputRegressionTests.testFaceShapeComboCaseUsesOnlyPhase27FaceShapeParameters`.
- Test method `BeautyRendererOutputRegressionTests.testNoFaceFixtureProducesNoFaceSummaryForFaceShapeCombo`.
- Helper `.planning/phases/27-geometry-render-output-and-verification-harness/check_geometry_renderer_outputs.py`.
- Helper constants `FIXTURE_NAMES`, `PORTRAIT_FIXTURE_NAMES`, `NO_FACE_FIXTURE_NAME`, `RENDERER_CASE_IDS`, `BASELINE_CASE_ID`, and `GEOMETRY_CASE_ID`.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/27-geometry-render-output-and-verification-harness/27-CONTEXT.md
@.planning/phases/27-geometry-render-output-and-verification-harness/27-RESEARCH.md
@.planning/phases/27-geometry-render-output-and-verification-harness/27-VALIDATION.md
@.planning/phases/27-geometry-render-output-and-verification-harness/27-PATTERNS.md
@.planning/phases/27-geometry-render-output-and-verification-harness/27-02-SUMMARY.md
@docs/meitu-function-blueprint/EXAMPLE_IMAGE_VALIDATION.md
@BeautySDK/Sources/BeautyExampleRenderer/main.swift
@BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift
@.planning/phases/24-renderer-output-regression-hardening/check_renderer_outputs.py
@example-images/input
@example-images/out
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 27-03-01: Append baseline and combined face-shape cases to the public-facade renderer</name>
  <files>BeautySDK/Sources/BeautyExampleRenderer/main.swift, BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift, example-images/input/no-face-gradient.png</files>
  <read_first>
    - `BeautySDK/Sources/BeautyExampleRenderer/main.swift`
    - `BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift`
    - `docs/meitu-function-blueprint/EXAMPLE_IMAGE_VALIDATION.md`
    - `.planning/phases/24-renderer-output-regression-hardening/24-RENDERER-EVIDENCE.md`
    - `.planning/phases/27-geometry-render-output-and-verification-harness/27-CONTEXT.md`
  </read_first>
  <behavior>
    - Test 1: Renderer case inventory equals existing 9 case IDs plus `geometryBaseline_noop` and `faceShapeCombo_0p35` in declaration order.
    - Test 2: `BeautyExampleRenderer/main.swift` imports `BeautySDK` and no internal targets.
    - Test 3: `faceShapeCombo_0p35` source contains only `faceSlim`, `faceSmall`, `faceVShape`, `jawSlim`, and `chinLength` geometry parameters and no eye, nose, mouth, lip, brow, proportion, 3D, or new public parameter scope.
    - Test 4: `no-face-gradient.png` produces `.noFace` and redacted no-face warnings for `faceShapeCombo_0p35` through default public-facade processing.
  </behavior>
  <action>Add `geometryBaseline_noop` using `BeautyParameters()` and add exactly one geometry case `faceShapeCombo_0p35` using the D-06 strengths. Do not add eye, nose, mouth, lip, brow, proportion, 3D sculpt, or per-tool face-shape cases per D-05, D-07, and D-08. Add a deterministic no-face input fixture named `example-images/input/no-face-gradient.png` for D-15; it must be an input fixture, not a generated output baseline. Update `BeautyRendererOutputRegressionTests` to include both new case IDs, preserve the public-facade import scan, validate the geometry case parameters, include the no-face fixture in no-op extent/pixel checks, and assert the no-face fixture returns `.noFace` with redacted metadata for the geometry case.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyRendererOutputRegressionTests</automated>
    <automated>swift build --package-path BeautySDK --product BeautyExampleRenderer</automated>
    <automated>rg -n "import Beauty(Core|Detection|Effects|Render|Resources)" BeautySDK/Sources/BeautyExampleRenderer/main.swift BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift; test $? -eq 1</automated>
    <automated>rg -n "id: \\\"(eye|nose|mouth|lip|brow|proportion|3d)|BeautyParameters\\([^)]*(eyeSize|eyeDistance|eyeYPosition|eyeTailLift|noseSlim|noseWingSlim|noseTipSize|noseBridge|mouthSize|mouthWidth|smile|lipColor)" BeautySDK/Sources/BeautyExampleRenderer/main.swift; test $? -eq 1</automated>
    <automated>git diff --check -- BeautySDK/Sources/BeautyExampleRenderer/main.swift BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - Renderer source has exactly one new geometry case, `faceShapeCombo_0p35`, and exactly one no-geometry baseline case, `geometryBaseline_noop`.
    - `faceShapeCombo_0p35` uses only the five existing face-shape parameters and moderate strengths locked by D-06.
    - Renderer executable remains public-facade-only with no internal SDK target imports.
    - The no-face fixture is committed under `example-images/input/`, while generated outputs remain under ignored `example-images/out/`.
  </acceptance_criteria>
  <done>The renderer matrix is ready to generate happy-path and no-face Phase 27 saved-output PNGs without broadening geometry scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 27-03-02: Create the Phase 27 geometry output helper and run generated-output checks</name>
  <files>.planning/phases/27-geometry-render-output-and-verification-harness/check_geometry_renderer_outputs.py</files>
  <read_first>
    - `.planning/phases/24-renderer-output-regression-hardening/check_renderer_outputs.py`
    - `BeautySDK/Sources/BeautyExampleRenderer/main.swift`
    - `BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift`
    - `.planning/phases/27-geometry-render-output-and-verification-harness/27-VALIDATION.md`
    - `SECURITY.md` sections 9 and 14
  </read_first>
  <behavior>
    - Test 1: Helper fails when any expected renderer PNG is missing, zero-byte, unreadable, not PNG, or dimension-mismatched against its input fixture.
    - Test 2: Helper compares `faceShapeCombo_0p35` against `geometryBaseline_noop` for portrait fixtures `e1.png` through `e5.png` and fails when saved PNG bytes are identical.
    - Test 3: Helper verifies `no-face-gradient__faceShapeCombo_0p35.png` exists, is non-empty, and matches no-face fixture dimensions; no-face semantic state is proven by XCTest from Task 27-03-01.
    - Test 4: Helper output prints relative names, counts, dimensions, case IDs, and fixture names only.
  </behavior>
  <action>Create `.planning/phases/27-geometry-render-output-and-verification-harness/check_geometry_renderer_outputs.py` by mirroring the Phase 24 standard-library PNG helper and adding Phase 27 constants. Expected output count must be `len(FIXTURE_NAMES) * len(RENDERER_CASE_IDS)` after adding `no-face-gradient.png`, `geometryBaseline_noop`, and `faceShapeCombo_0p35`. The helper must not compute or print hashes, raw pixel payloads, absolute local paths, coordinates, landmarks, bounding boxes, control points, raw framework errors, raw JSON, or image bytes per D-11 and D-17. Its pass line must report checked/expected output count, dimension buckets, portrait baseline comparison count, no-face output presence, fixture names, and case IDs.</action>
  <verify>
    <automated>swift run --package-path BeautySDK BeautyExampleRenderer --input example-images/input --output example-images/out</automated>
    <automated>python3 .planning/phases/27-geometry-render-output-and-verification-harness/check_geometry_renderer_outputs.py --input example-images/input --output example-images/out | tee /tmp/phase27-geometry-helper.out</automated>
    <automated>git check-ignore example-images/out/e1__faceShapeCombo_0p35.png example-images/out/e1__geometryBaseline_noop.png example-images/out/no-face-gradient__faceShapeCombo_0p35.png</automated>
    <automated>rg -n "/Users/|/private/var|NSError|AVError|rawPresetJson:|raw JSON:|image bytes:|landmarks=\\[|landmarkCoordinates:|rawLandmark:|VNFaceObservation\\(|boundingBox:|controlPoint\\(|SIMD[0-9]?\\(" /tmp/phase27-geometry-helper.out; test $? -eq 1</automated>
    <automated>git diff --check -- .planning/phases/27-geometry-render-output-and-verification-harness/check_geometry_renderer_outputs.py</automated>
  </verify>
  <acceptance_criteria>
    - Renderer command writes expected ignored PNGs under `example-images/out/` for all fixtures and cases.
    - Helper verifies non-empty PNGs, same dimensions, portrait geometry-vs-baseline saved-output non-identity, and no-face geometry output existence.
    - Helper output is safe to paste into evidence Markdown because it contains only relative fixture/output labels, case IDs, counts, dimensions, and pass/fail facts.
    - `git check-ignore` confirms generated geometry outputs remain ignored.
  </acceptance_criteria>
  <done>Phase 27 has a deterministic generated-output helper and passing renderer output checks for the combined geometry case and no-face saved-output path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer CLI -> local filesystem | Public-facade renderer writes generated PNGs under ignored `example-images/out/`; evidence must not commit generated outputs. |
| Helper -> evidence Markdown | Helper output is intended for Markdown evidence and must be relative, factual, and redacted. |
| Input fixture -> no-face evidence | Dedicated no-face input is committed as a test fixture; generated no-face outputs remain ignored. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03-01 | Information Disclosure | `BeautyExampleRenderer` stdout and helper output | mitigate | Evidence records relative output names, counts, dimensions, and case IDs only; helper scans avoid raw geometry, local paths, raw JSON, and image bytes. |
| T-27-03-02 | Tampering | `example-images/out/` generated PNGs | mitigate | Run `git check-ignore` for geometry and baseline outputs; do not add generated PNG baselines or hashes. |
| T-27-03-03 | Spoofing | Geometry-vs-baseline saved-output claim | mitigate | Add `geometryBaseline_noop`, compare geometry output against baseline for portrait fixtures, and rely on Plan 27-02 pre-watermark output-delta tests to rule out watermark-only evidence. |
| T-27-03-04 | Tampering | Renderer scope expansion | mitigate | Scan renderer source for non-face-shape geometry case IDs and non-D-06 parameters; keep `SHAPE_FEATURE_LEDGER.md` unchanged in this plan. |
| T-27-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install is allowed; helper uses Python 3 standard library only. |
</threat_model>

<verification>
Overall Plan 27-03 checks:

- `swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyRendererOutputRegressionTests`
- `swift build --package-path BeautySDK --product BeautyExampleRenderer`
- `swift run --package-path BeautySDK BeautyExampleRenderer --input example-images/input --output example-images/out`
- `python3 .planning/phases/27-geometry-render-output-and-verification-harness/check_geometry_renderer_outputs.py --input example-images/input --output example-images/out | tee /tmp/phase27-geometry-helper.out`
- `git check-ignore` for representative baseline, geometry, and no-face generated outputs.
- Public-facade renderer import scan, geometry-scope scan, helper-output raw-leak scan, and scoped `git diff --check`.
</verification>

<success_criteria>
Plan 27-03 succeeds when `BeautyExampleRenderer` can generate ignored same-dimension saved PNGs for the baseline, combined face-shape geometry, and no-face fixture paths, and the helper proves the geometry case differs from its no-geometry baseline for portrait fixtures without overclaiming visual quality.
</success_criteria>

<output>
Create `.planning/phases/27-geometry-render-output-and-verification-harness/27-03-SUMMARY.md` when done.
</output>
