---
phase: 36-public-facade-output-evidence
plan: "02"
type: execute
wave: 2
depends_on: [36-01]
files_modified:
  - .planning/phases/36-public-facade-output-evidence/check_nose_remaining_renderer_outputs.py
  - .planning/phases/36-public-facade-output-evidence/36-NOSE-OUTPUT-EVIDENCE.md
autonomous: true
requirements: [NOSE-07, NOSE-08, NOSE-09]
must_haves:
  truths:
    - "The helper discovers renderer cases and recursive fixtures, rejects duplicate IDs/stems and missing/extra/corrupt outputs, computes the product, then separately requires 36×7=252."
    - "All 252 public-facade PNGs decode non-empty at input dimensions; no-face outputs preserve 64×64 and match baseline above the watermark."
    - "A fixed documented nose ROI wholly above the watermark proves 12 baseline, 6 root-vs-bridge, and 12 lift-vs-signed-tip comparisons as separate families with fixed changed-pixel and RGB-delta floors."
  artifacts:
    - path: .planning/phases/36-public-facade-output-evidence/check_nose_remaining_renderer_outputs.py
      provides: self-contained matrix and ROI gate
    - path: .planning/phases/36-public-facade-output-evidence/36-NOSE-OUTPUT-EVIDENCE.md
      provides: observed inventories, thresholds, minima, commands, and non-claims
  key_links:
    - from: BeautySDK/Sources/BeautyExampleRenderer/main.swift
      to: check_nose_remaining_renderer_outputs.py
      via: discovered ordered case IDs
      pattern: "id:"
---

<objective>Create the archive-safe v1.9 helper, regenerate a clean public-facade matrix, calibrate fixed non-circular thresholds, and record direct output visibility/independence evidence.</objective>

## Artifacts this phase produces

- New script `check_nose_remaining_renderer_outputs.py` with CLI, case/fixture discovery, PNG/JPEG dimensions, full PNG RGB decoder, watermark/ROI calculation, matrix validation, comparison metrics, fixed thresholds, and deterministic report.
- New `36-NOSE-OUTPUT-EVIDENCE.md`; ignored local 252-output matrix is evidence input, never a tracked baseline.

<execution_context>@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md
@PLANS.md
@.planning/phases/36-public-facade-output-evidence/36-CONTEXT.md
@.planning/phases/36-public-facade-output-evidence/36-RESEARCH.md
@.planning/phases/36-public-facade-output-evidence/36-PATTERNS.md
@BeautySDK/Sources/BeautyExampleRenderer/main.swift
@.planning/phases/29-eye-renderer-output-evidence/check_eye_renderer_outputs.py
@.planning/milestones/v1.8-phases/33-mouth-renderer-output-evidence/check_mouth_renderer_outputs.py</context>

<tasks>
<task type="auto">
  <name>Task 36-02-01: Build the self-contained discovered-inventory decoder and ROI gate</name>
  <files>.planning/phases/36-public-facade-output-evidence/check_nose_remaining_renderer_outputs.py</files>
  <read_first>
    - `36-CONTEXT.md`, `36-RESEARCH.md`, `36-PATTERNS.md`, and `36-VALIDATION.md`
    - `BeautySDK/Sources/BeautyExampleRenderer/main.swift` — actual source inventory/naming/watermark constants
    - `.planning/phases/29-eye-renderer-output-evidence/check_eye_renderer_outputs.py` — live decoder/dimension precedent
    - archived Phase 31/33 helpers — algorithms only; do not copy their broken sibling-relative import
    - `example-images/generate_gallery.py` — fixture discovery and duplicate-stem pattern
  </read_first>
  <action>Create a Python 3 standard-library-only helper requiring `--input`, `--output`, and `--renderer-source`. Parse ordered renderer `id:` strings, reject duplicates, discover recursive PNG/JPG/JPEG fixtures with deterministic ordering, reject duplicate stems, compute expected names from observed inventories, and separately require exact current inventories of 36 cases and 7 fixtures yielding 252. Reject missing, unexpected renderer-shaped, zero-byte, unsupported/corrupt/incompletely decoded outputs and dimension mismatch. Own archive-safe PNG RGB decoding and PNG/JPEG input dimensions without sibling-phase imports. Implement one normalized nose ROI with its bottom asserted wholly above the renderer-matched watermark exclusion for every fixture. Return per-pair ROI pixels, changed pixels, and absolute RGB delta. Define five separate families: root/baseline 6, lift/baseline 6, root/bridge 6, lift/positive-tip 6, lift/negative-tip 6. Exclude no-face from portrait totals and require its two new outputs equal baseline above the watermark. Add deterministic negative-path self-test mode or unit-check entry for duplicate IDs/stems, missing/extra/corrupt outputs, and ROI/watermark rejection. Initially expose conservative calibration constants, but do not dynamically choose acceptance thresholds from an accepting run.</action>
  <verify><automated>python3 .planning/phases/36-public-facade-output-evidence/check_nose_remaining_renderer_outputs.py --self-test &amp;&amp; python3 -m py_compile .planning/phases/36-public-facade-output-evidence/check_nose_remaining_renderer_outputs.py &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>
    - Helper is self-contained and remains runnable if the Phase 36 directory is archived.
    - Inventory discovery precedes frozen 36/7/252 assertions and detects all named malformed conditions.
    - Five families, fixed ROI above watermark, two metric floors, and no-face exclusion/no-op are explicit automated gates.
  </acceptance_criteria>
  <done>The missing Wave 0 helper exists and fails closed before live output acceptance.</done>
</task>

<task type="auto">
  <name>Task 36-02-02: Generate, calibrate, freeze, rerun, and document the 252-output evidence</name>
  <files>.planning/phases/36-public-facade-output-evidence/check_nose_remaining_renderer_outputs.py, .planning/phases/36-public-facade-output-evidence/36-NOSE-OUTPUT-EVIDENCE.md</files>
  <read_first>
    - The helper created by Task 36-02-01
    - `BeautySDK/Sources/BeautyExampleRenderer/main.swift` and Plan 36-01 summary
    - `example-images/README.md` and `.gitignore` — disposable output boundary
    - `36-CONTEXT.md` — exact expected comparison counts and non-claims
  </read_first>
  <action>Build `BeautyExampleRenderer`. Before both the measurement render and the strict accepting render, resolve `example-images/output` to its physical path, require it equals the repository's exact allow-listed output root and passes `git check-ignore`, then delete only files below that root; abort before deletion on any mismatch. Render all observed fixtures/cases through the public facade and run the helper in measurement mode. Select one global nose ROI and fixed changed-pixel plus absolute-RGB-delta floors below the observed minimum with documented margin; commit those constants, then repeat the guarded clean, render, and helper in strict acceptance mode so the accepted run cannot set its own threshold or reuse stale PNGs. Create `36-NOSE-OUTPUT-EVIDENCE.md` recording exact commands, observed 36/7/252 inventories and dimensions, fixed ROI/watermark formula, thresholds, per-family counts/minima, 12 baseline + 6 root/bridge + 12 signed lift comparisons, no-face 64×64 above-watermark no-op, ignored/untracked status, and explicit provisional-cap/no-promotion/device/commercial/packaging non-claims. Any unsafe path, zero-test, build, decode, extent, ROI, count, or containment failure remains a blocker.</action>
  <verify><automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift build --package-path BeautySDK --product BeautyExampleRenderer &amp;&amp; OUTPUT_ROOT="$(cd example-images/output &amp;&amp; pwd -P)" &amp;&amp; EXPECTED_ROOT="$(pwd -P)/example-images/output" &amp;&amp; test "$OUTPUT_ROOT" = "$EXPECTED_ROOT" &amp;&amp; git check-ignore -q example-images/output &amp;&amp; find "$OUTPUT_ROOT" -mindepth 1 -type f -delete &amp;&amp; DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift run --package-path BeautySDK BeautyExampleRenderer --input example-images/input --output example-images/output &amp;&amp; python3 .planning/phases/36-public-facade-output-evidence/check_nose_remaining_renderer_outputs.py --input example-images/input --output example-images/output --renderer-source BeautySDK/Sources/BeautyExampleRenderer/main.swift &amp;&amp; test "$(find example-images/output -type f -name '*.png' | wc -l | tr -d ' ')" -eq 252</automated></verify>
  <acceptance_criteria>
    - Strict rerun reports 36 cases, 7 fixtures, 252/252 valid same-dimension PNGs, and no unexpected renderer outputs.
    - All five comparison families pass fixed thresholds with counts 6 each; reported aggregates equal 12 baseline, 6 root/bridge, and 12 signed lift comparisons.
    - Both no-face outputs preserve 64×64 and are baseline-identical above watermark.
    - Evidence records observed minima and does not claim final caps, naturalness, promotion, or release readiness.
  </acceptance_criteria>
  <done>Public-facade saved output proves both fields visible and non-aliased with reproducible ignored evidence.</done>
</task>
</tasks>

<threat_model><scope>ASVS L1 generated-evidence integrity and biometric-adjacent privacy.</scope><blocking_rule>Any unresolved HIGH threat blocks acceptance.</blocking_rule><assets>Matrix completeness, decoder correctness, non-circular thresholds, raw-image locality, and untracked artifacts.</assets><trust_boundaries>Untrusted fixture/output bytes enter the helper; only aggregate metrics enter committed evidence.</trust_boundaries><threats>Stale files, stem collisions, truncated PNGs, watermark false positives, dynamic thresholds, and raw pixel/geometry leakage.</threats><mitigations>Clean allow-listed output, discovery plus exact assertions, complete decode/dimensions, fixed ROI/floors, self-tests, aggregate-only evidence, and ignore/tracking gates.</mitigations><residual_risk>Commercial naturalness and final cap calibration remain outside this plan.</residual_risk></threat_model>
<verification>Run helper self-tests, renderer build, clean full render, strict helper, exact 252 count, ignore/tracking checks, and diff hygiene.</verification>
<success_criteria>NOSE-07 through NOSE-09 output evidence passes every decoded, ROI, no-face, privacy, and inventory gate with no HIGH threat.</success_criteria>
<output>Create `36-02-SUMMARY.md`.</output>
