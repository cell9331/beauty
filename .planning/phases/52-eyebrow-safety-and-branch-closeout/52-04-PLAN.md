---
phase: 52-eyebrow-safety-and-branch-closeout
plan: "04"
type: execute
wave: 4
depends_on: ["52-03"]
files_modified:
  - docs/meitu-function-blueprint/SHAPE_FEATURE_LEDGER.md
  - docs/meitu-function-blueprint/FEATURE_MATRIX.md
  - docs/meitu-function-blueprint/features/beauty-shaping/eyebrows/README.md
  - docs/meitu-function-blueprint/features/beauty-shaping/README.md
autonomous: true
requirements: [DOC-01]
must_haves:
  truths:
    - "Immediately before mutation, fresh runtime, output, privacy, artifact, review, Nyquist, and ASVS evidence still passes and default checker mode confirms all seven target rows and branch `眉毛` remain unpromoted (D-13, D-14, D-15)."
    - "One atomic four-owner transaction promotes exactly `上下`, `粗细`, `长短`, `间距`, `眉头间距`, `倾斜`, and `眉峰`, then marks branch `眉毛` implemented at SDK-core scope; no other row or branch status changes (D-15)."
    - "Each promoted row cites the Phase 49 support, Phase 50 provider/pipeline, Phase 51 output, and Phase 52 safety chain; all v1.14-v1.16, SwiftUI/Demo, device, commercial-naturalness, optimized-performance, packaging, shipping, launch, audit, archive, tag, and cleanup nonclaims remain intact (D-15, D-16)."
  artifacts:
    - path: docs/meitu-function-blueprint/SHAPE_FEATURE_LEDGER.md
      provides: "Exact seven-row product-status authority"
      contains: "眉峰"
    - path: docs/meitu-function-blueprint/FEATURE_MATRIX.md
      provides: "Branch-level `眉毛` SDK-core status authority"
      contains: "眉毛"
    - path: docs/meitu-function-blueprint/features/beauty-shaping/eyebrows/README.md
      provides: "Eyebrow feature status and evidence chain"
      contains: "Phase 52"
    - path: docs/meitu-function-blueprint/features/beauty-shaping/README.md
      provides: "Parent shaping branch scope and preserved nonclaims"
      contains: "眉毛"
  key_links:
    - from: .planning/phases/52-eyebrow-safety-and-branch-closeout/52-EYEBROW-SAFETY-EVIDENCE.md
      to: docs/meitu-function-blueprint/SHAPE_FEATURE_LEDGER.md
      via: "Fresh pre-promotion evidence authorizes only the exact seven-row mutation"
      pattern: "SAFE-01|SAFE-02|SAFE-03"
    - from: docs/meitu-function-blueprint/SHAPE_FEATURE_LEDGER.md
      to: docs/meitu-function-blueprint/FEATURE_MATRIX.md
      via: "Branch becomes implemented only after all seven child rows are implemented"
      pattern: "上下|粗细|长短|间距|眉头间距|倾斜|眉峰"
  prohibitions:
    - "Never promote beyond the exact seven eyebrow rows or beyond the SDK-core branch `眉毛`; no alias, retouch, hairline, double-chin, UI, Demo, device, commercial, or release status may change."
    - "Never edit one product owner without completing the same atomic four-file transaction and immediately passing exact post-promotion classification."
---

<objective>
Perform the single evidence-gated product transaction that promotes exactly seven eyebrow rows and branch `眉毛` at SDK-core scope.

Purpose: DOC-01 requires exact owner/status agreement after, never before, every executable and governance gate passes.
Output: Four synchronized blueprint owners with exact row/branch promotion, complete evidence citations, and preserved nonclaims.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@PRODUCT_SENSE.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-eyebrow-safety-and-branch-closeout/52-CONTEXT.md
@.planning/phases/52-eyebrow-safety-and-branch-closeout/52-VALIDATION.md
@.planning/phases/52-eyebrow-safety-and-branch-closeout/52-EYEBROW-SAFETY-EVIDENCE.md
@.planning/phases/52-eyebrow-safety-and-branch-closeout/52-REVIEW.md
@.planning/phases/52-eyebrow-safety-and-branch-closeout/52-SECURITY.md
@.planning/phases/52-eyebrow-safety-and-branch-closeout/52-03-SUMMARY.md
@docs/meitu-function-blueprint/SHAPE_FEATURE_LEDGER.md
@docs/meitu-function-blueprint/FEATURE_MATRIX.md
@docs/meitu-function-blueprint/features/beauty-shaping/eyebrows/README.md
@docs/meitu-function-blueprint/features/beauty-shaping/README.md

<interfaces>
Promotion contract:
- Default checker mode is the final pre-promotion authorization and requires the seven rows/branch to remain unchanged.
- `--check-promotion` accepts only the exact four-file transaction and rejects missing, duplicate, extra, reordered, or broader status changes.
- The child rows are exactly `上下`, `粗细`, `长短`, `间距`, `眉头间距`, `倾斜`, and `眉峰`; the parent status is implemented only at SDK-core scope.

Decision index used by this plan:
- D-13 fail-closed pre/post-promotion status classification.
- D-14 fresh runtime/output/review/security evidence before mutation.
- D-15 exact atomic seven-row plus branch promotion.
- D-16 preserve all later milestone, UI/device/commercial/release, and lifecycle nonclaims.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 52-04-01: Re-authorize the exact promotion from fresh immutable evidence</name>
  <files>.planning/phases/52-eyebrow-safety-and-branch-closeout/check_eyebrow_safety_boundaries.py, .planning/phases/52-eyebrow-safety-and-branch-closeout/52-EYEBROW-SAFETY-EVIDENCE.md, .planning/phases/52-eyebrow-safety-and-branch-closeout/52-REVIEW.md, .planning/phases/52-eyebrow-safety-and-branch-closeout/52-SECURITY.md, .planning/phases/52-eyebrow-safety-and-branch-closeout/52-VALIDATION.md</files>
  <action>Immediately before editing any owner, run checker self-test and default live mode; require the fresh evidence ledger, clean review, `threats_open: 0`, fourteen-task Nyquist state, strict helper/gallery evidence, generated-artifact containment, and diff hygiene from D-13/D-14. Inspect the exact row and branch windows in all four target owners and confirm every target remains unpromoted and every excluded row/nonclaim is unchanged. If any command, evidence hash/count, owner window, HIGH finding, unclassified match, or status differs, stop without modifying product owners and record the blocker in the execution summary; passing automation never overrides a failed or stale governance precondition.</action>
  <verify>
    <automated>python3 .planning/phases/52-eyebrow-safety-and-branch-closeout/check_eyebrow_safety_boundaries.py --self-test &amp;&amp; python3 .planning/phases/52-eyebrow-safety-and-branch-closeout/check_eyebrow_safety_boundaries.py &amp;&amp; rg -q '^status: clean$' .planning/phases/52-eyebrow-safety-and-branch-closeout/52-REVIEW.md &amp;&amp; rg -q '^threats_open: 0$' .planning/phases/52-eyebrow-safety-and-branch-closeout/52-SECURITY.md &amp;&amp; rg -q '^nyquist_compliant: true$' .planning/phases/52-eyebrow-safety-and-branch-closeout/52-VALIDATION.md &amp;&amp; git diff --check</automated>
  </verify>
  <done>All preconditions pass freshly and the default checker proves the exact seven rows and branch are still unpromoted; no owner file has changed if any precondition failed.</done>
</task>

<task type="auto">
  <name>Task 52-04-02: Promote exactly seven rows and branch `眉毛` atomically</name>
  <files>docs/meitu-function-blueprint/SHAPE_FEATURE_LEDGER.md, docs/meitu-function-blueprint/FEATURE_MATRIX.md, docs/meitu-function-blueprint/features/beauty-shaping/eyebrows/README.md, docs/meitu-function-blueprint/features/beauty-shaping/README.md</files>
  <action>Apply one atomic D-15/D-16 patch to the four product owners. Mark only `上下`, `粗细`, `长短`, `间距`, `眉头间距`, `倾斜`, and `眉峰` implemented, with row-local citations spanning Phase 49 observed support, Phase 50 independent provider/pipeline, Phase 51 accepted public output, and Phase 52 final safety. Then mark branch `眉毛` implemented explicitly at SDK-core scope. Preserve every other ledger/matrix/README row and preserve v1.14-v1.16 retouch/hairline/double-chin scope plus SwiftUI/Demo, device, commercial-naturalness, optimized-performance, packaging, shipping, launch, audit, archive, tag, and cleanup nonclaims. Run exact post-promotion mode immediately; any extra/missing/status/nonclaim mutation fails and must be corrected before commit.</action>
  <verify>
    <automated>python3 .planning/phases/52-eyebrow-safety-and-branch-closeout/check_eyebrow_safety_boundaries.py --check-promotion &amp;&amp; git diff --check</automated>
  </verify>
  <done>Exactly the seven named rows and SDK-core branch `眉毛` are implemented across all four owners, the complete evidence chain is cited, all excluded statuses/nonclaims are preserved, and post-promotion mode passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Fresh evidence → promotion authority | Stale, partial, or contradictory evidence must not authorize status mutation. |
| Child row status → branch status | The branch may become implemented only when exactly all seven authorized child rows agree. |
| Four product owners → downstream owners | A partial or excessive transaction would create contradictory public status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-52-24 | Elevation of Privilege | promotion authorization | mitigate | Immediate default checker plus clean review, zero-open security, Nyquist, strict-output, and containment preconditions. |
| T-52-25 | Tampering | row/branch mutation | mitigate | Exact seven-name cardinality, four-file allowlist, SDK-core branch qualifier, and post-promotion mode. |
| T-52-26 | Repudiation | evidence citations | mitigate | Every row cites the four-phase support→provider→output→safety chain; missing or stale links fail classification. |
| T-52-27 | Elevation of Privilege | broader product/lifecycle claims | mitigate | Explicit nonclaim preservation and negative scans for v1.14-v1.16, UI/device/commercial/release/audit/archive/tag/cleanup drift. |
</threat_model>

<verification>
- Default checker passes immediately before mutation and proves target statuses remain unchanged.
- The four-file patch changes exactly seven child rows and the SDK-core parent branch.
- Post-promotion mode passes immediately after mutation and rejects every extra/missing/nonclaim change.
- No example, root, planning, milestone-audit, archive, tag, cleanup, code, test, renderer, fixture, or generated-artifact file changes in this wave.
</verification>

<success_criteria>
DOC-01 product status is exact, atomic, evidence-gated, and limited to seven eyebrow rows plus the SDK-core branch, ready for downstream owner synchronization.
</success_criteria>

<output>
Create `.planning/phases/52-eyebrow-safety-and-branch-closeout/52-04-SUMMARY.md` when done.
</output>
