---
phase: 26-geometry-facade-and-landmark-routing-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift
  - BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift
  - BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
autonomous: true
requirements:
  - GEO-02
user_setup: []
must_haves:
  truths:
    - "One selected usable detection observation can become internal resolver geometry without exposing raw geometry publicly, per D-05, D-06, and D-07."
    - "Missing eye, nose, mouth, lip, stale, reused, and no-face states keep group-specific degradation through BeautyEffectResolver, per D-08."
    - "Resolver warnings and metrics remain redacted aggregate evidence only, per D-13, D-14, D-15, and D-16."
  artifacts:
    - path: "BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift"
      provides: "Package/internal selected-face observation to internal FaceGeometry adapter"
      contains: "BeautyFaceGeometryAdapter"
    - path: "BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift"
      provides: "Package resolver entry points for facade geometry routing"
      contains: "requiresFaceGeometry"
    - path: "BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift"
      provides: "Selected observation resolver activation tests"
    - path: "BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift"
      provides: "Group-specific degradation and redaction tests"
  key_links:
    - from: "BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift"
      to: "BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift"
      via: "package selected-face resolver overload"
      pattern: "selectedFaceObservation"
    - from: "BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift"
      to: "BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift"
      via: "internal FaceGeometry construction"
      pattern: "FaceGeometry"
---

<objective>
Create the package-only selected-face geometry bridge that Phase 26 needs before the public facade can route detection output into effect planning.

Purpose: satisfy GEO-02 by letting SDK-internal targets convert one selected detection observation into internal geometry intent while preserving the public no-raw-geometry boundary.
Output: a package/internal adapter, resolver entry points, and focused effects tests proving activation, degradation, and redaction.
</objective>

## Artifacts This Phase Produces

- `BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift` with `BeautyFaceGeometryAdapter` that converts one package-visible `BeautyFaceObservation` into internal `FaceGeometry`.
- `BeautyEffectResolver.requiresFaceGeometry(parameters:)` as a package-visible trigger classifier used by the facade, including face shape, eye, nose, mouth, and `lipColor` work per D-01.
- `BeautyEffectResolver.resolve(parameters:selectedFaceObservation:)` as a package-visible resolver entry that builds internal `FaceGeometry` and then calls the existing internal `resolve(parameters:faceGeometry:)`.
- Package-only access for the narrow detection observation/landmark types needed by `BeautyEffects` and `BeautySDK`; no `public` raw geometry, landmark, bounding-box, or control-point API.
- Focused `BeautyEffectResolverTests` and `MissingLandmarkDegradationTests` coverage for selected-face activation, group-specific degradation, stale/reused behavior, and redacted aggregate metrics.
- Phase 26 warning/metric evidence remains limited to existing stable warning codes and numeric aggregate keys such as active/skipped/capped/reused/geometry intent counts; no coordinates or raw framework errors.
- Phase 26 evidence docs are produced by Plan 26-03 after Plans 26-01 and 26-02 pass.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-geometry-facade-and-landmark-routing-foundation/26-CONTEXT.md
@.planning/phases/26-geometry-facade-and-landmark-routing-foundation/26-RESEARCH.md
@.planning/phases/26-geometry-facade-and-landmark-routing-foundation/26-VALIDATION.md
@.planning/phases/26-geometry-facade-and-landmark-routing-foundation/26-PATTERNS.md
@ARCHITECTURE.md
@DESIGN.md
@SECURITY.md
@RELIABILITY.md
@PRODUCT_SENSE.md
@QUALITY_SCORE.md
@PLANS.md
@BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift
@BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
@BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift
@BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
@BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 26-01-01: Add the package-only selected-face geometry adapter</name>
  <files>BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift, BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift, BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift</files>
  <read_first>
    - BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift
    - BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift
    - BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
    - BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
    - .planning/phases/26-geometry-facade-and-landmark-routing-foundation/26-CONTEXT.md
    - .planning/phases/26-geometry-facade-and-landmark-routing-foundation/26-PATTERNS.md
    - ARCHITECTURE.md
    - DESIGN.md
    - SECURITY.md
  </read_first>
  <behavior>
    - Test complete selected observation input routes into active face-shape/eye/nose/mouth/lip planning without public raw geometry exposure.
    - Test default/color/filter/basic-skin parameters make `BeautyEffectResolver.requiresFaceGeometry(parameters:)` return false.
    - Test face-shape, eye, nose, mouth, and `lipColor` parameters make `requiresFaceGeometry(parameters:)` return true.
  </behavior>
  <action>Promote only the `BeautyFaceObservation`, `BeautyFaceLandmarks`, `BeautyLandmarkGroup`, and required members from internal to `package` access so same-package targets can route selected detection results; do not make these declarations `public` and do not add them to `BeautySDK.swift` exports, per D-06. Create `BeautyFaceGeometryAdapter` in `BeautyEffects/Planning` that accepts one package-visible `BeautyFaceObservation`, derives deterministic internal `FaceBounds` from `imageBounds` or a centered normalized-area fallback, fills only the landmark groups reported available, and returns internal `FaceGeometry` for existing providers, per D-05 and D-07. In `BeautyEffectResolver`, add package `requiresFaceGeometry(parameters:)` and package `resolve(parameters:selectedFaceObservation:)`; the latter must call the adapter and then the existing internal `resolve(parameters:faceGeometry:)` path so group-specific degradation stays resolver-owned, per D-08. Keep `FaceGeometry`, `WarpControlPoint`, provider types, coordinates, and control points out of the public facade surface.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.BeautyEffectResolverTests</automated>
    <automated>rg -n "public .*BeautyFaceObservation|public .*BeautyFaceLandmarks|public .*BeautyLandmarkGroup|public .*FaceGeometry|public .*WarpControlPoint" BeautySDK/Sources/BeautyDetection BeautySDK/Sources/BeautyEffects BeautySDK/Sources/BeautySDK; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `BeautyEffectResolver.requiresFaceGeometry(parameters:)` exists with package access and includes face shape, eyes, nose, mouth, and `lipColor` triggers while excluding no-op, color, filter, and basic-skin-only paths, covering D-01 for downstream facade use.
    - `BeautyEffectResolver.resolve(parameters:selectedFaceObservation:)` exists with package access and routes through internal `FaceGeometry`; no new public raw geometry symbols are exported, covering D-06 and GEO-02.
    - Focused effects tests prove selected usable observation activates geometry planning and returns only aggregate metrics/warnings, covering D-09, D-13, and D-14.
  </acceptance_criteria>
  <done>Package-internal selected-face routing compiles, focused resolver tests pass, and public raw geometry type scans return no matches.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 26-01-02: Preserve degradation and redacted aggregate evidence through the selected-face route</name>
  <files>BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift, BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift, BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift</files>
  <read_first>
    - BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
    - BeautySDK/Sources/BeautyEffects/Render/BeautyGeometryEffectPipeline.swift
    - BeautySDK/Sources/BeautyEffects/Warp/EyeWarpProvider.swift
    - BeautySDK/Sources/BeautyEffects/Warp/NoseWarpProvider.swift
    - BeautySDK/Sources/BeautyEffects/Warp/MouthWarpProvider.swift
    - BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
    - .planning/phases/26-geometry-facade-and-landmark-routing-foundation/26-VALIDATION.md
    - RELIABILITY.md
    - SECURITY.md
  </read_first>
  <behavior>
    - Missing eye groups skip only `.eyes`; missing nose skips only `.nose`; missing mouth skips `.mouth` and `.lipColor` while safe color/filter domains stay active.
    - Stale selected-face geometry skips strong geometry; reused selected-face geometry weakens effective strengths.
    - Emitted warnings, metric keys, and detection-independent resolver evidence do not contain coordinates, raw framework identifiers, local paths, image bytes, or detailed point structures.
  </behavior>
  <action>Extend the selected-face route tests to cover complete, missing-eye, missing-nose, missing-mouth, no selected face, stale, and reused geometries through existing `BeautyEffectResolver` behavior, per D-08. Keep warnings as stable redacted codes such as `face_effects_skipped_no_face`, `eye_inputs_missing`, `nose_inputs_missing`, `mouth_inputs_missing`, `lip_inputs_missing`, `geometry_stale_skipped`, and `geometry_stale_reduced`. Keep metrics numeric and aggregate only; retain `beauty.effects.geometryPointCount` only as a count if it remains aggregate and non-coordinate, or rename it everywhere in this task to `beauty.effects.geometryIntentCount` if the focused redaction tests classify the current name as too revealing, per D-16. Do not add renderer cases, image-output assertions, Demo UI work, or `SHAPE_FEATURE_LEDGER.md` status edits, per D-10 and D-12.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.MissingLandmarkDegradationTests</automated>
    <automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.BeautyEffectResolverTests</automated>
    <automated>rg -n "VNFaceObservation|boundingBox|/private/var|NSError|AVError|rawPresetJson|raw JSON|image bytes" BeautySDK/Sources/BeautyEffects/Planning BeautySDK/Tests/BeautyEffectsTests; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - Existing group-specific degradation behavior remains unchanged through the selected-face route, covering D-08 and GEO-02.
    - Redaction tests inspect emitted warning and metric strings, not raw internal source identifiers alone, matching the Phase 19/25 scan-scope pattern and D-15.
    - No `BeautyExampleRenderer` geometry case or saved PNG evidence is introduced in this plan, covering D-10.
  </acceptance_criteria>
  <done>Selected-face routing preserves resolver degradation semantics and emits only redacted aggregate evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Detection -> Effects | Package-visible selected detection observations cross into effect planning. |
| Effects -> public result | Internal geometry planning emits only warnings and numeric metrics through public `BeautyResult`. |
| Package access -> public API | Same-package access must not become public `BeautySDK` API. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01-01 | Information Disclosure | `BeautyFaceObservation` access promotion | mitigate | Use `package` access only, never `public`; add scans for public raw observation, landmark, bounding, `FaceGeometry`, and `WarpControlPoint` exports. |
| T-26-01-02 | Information Disclosure | `BeautyFaceGeometryAdapter` and resolver metrics | mitigate | Emit only existing redacted warning codes and numeric aggregate metrics; add tests scanning emitted warnings/metric keys for raw landmark, bounding, path, raw error, image-byte, and coordinate tokens. |
| T-26-01-03 | Elevation of Privilege | `BeautySDK.swift` public facade exports | mitigate | Do not add raw detection/effects aliases to `BeautySDK.swift`; public facade remains `BeautyResult`, `BeautyDetectionSummary`, warnings, and metrics only. |
| T-26-01-04 | Integrity | Renderer and ledger status | mitigate | Plan files and tests forbid `BeautyExampleRenderer` geometry cases, saved-output PNG claims, and `SHAPE_FEATURE_LEDGER.md` `implemented` status changes in Phase 26. |
</threat_model>

<verification>
Plan-level checks:
- `swift test --package-path BeautySDK --filter BeautyEffectsTests.BeautyEffectResolverTests`
- `swift test --package-path BeautySDK --filter BeautyEffectsTests.MissingLandmarkDegradationTests`
- Public raw geometry export scan over `BeautySDK/Sources/BeautyDetection`, `BeautySDK/Sources/BeautyEffects`, and `BeautySDK/Sources/BeautySDK`
- `git diff --check -- BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift`
</verification>

<success_criteria>
GEO-02 is covered at the package/effects layer: selected-face detection data can feed geometry render planning through internal `FaceGeometry`, missing groups degrade by domain, public raw geometry remains unexported, and aggregate diagnostics stay redacted.
</success_criteria>

<output>
Create `.planning/phases/26-geometry-facade-and-landmark-routing-foundation/26-01-SUMMARY.md` when done.
</output>
