---
phase: 23-performance-and-reliability-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BeautySDK/Tests/BeautyCoreTests/BeautyPerformanceEvidenceTests.swift
  - .planning/phases/23-performance-and-reliability-gates/23-PERFORMANCE-EVIDENCE.md
autonomous: true
requirements:
  - PERF-01
  - PERF-04
  - PERF-05
must_haves:
  truths:
    - "D-01 PERF-01: SDK 1280x720 synthetic BGRA CVPixelBuffer timing runs through BeautyEngine.processResult(pixelBuffer:metadata:parameters:)."
    - "D-02 PERF-01: Timing matrix includes default no-op, current skin/color/filter, and high-but-capped parameter cases."
    - "D-03 PERF-01: First timing results are record-and-compare evidence against RELIABILITY.md budgets, with over-budget classification instead of optimization."
    - "D-05 D-06 D-07 PERF-04: Automated SDK fixture loop records memory trend baseline and explicit non-claim/rerun protocol for any shorter-than-10-minute run."
    - "D-13 D-14 D-16 PERF-05: Performance test output and evidence artifact use only allowlisted redacted fields and do not enable per-frame logging."
  artifacts:
    - "BeautySDK/Tests/BeautyCoreTests/BeautyPerformanceEvidenceTests.swift provides SDK timing and fixture-loop memory evidence tests."
    - ".planning/phases/23-performance-and-reliability-gates/23-PERFORMANCE-EVIDENCE.md provides the initial timing, memory, budget, redaction, and non-claim ledger."
  key_links:
    - "BeautyPerformanceEvidenceTests -> BeautyEngine.processResult(pixelBuffer:metadata:parameters:) via direct SDK facade call."
    - "23-PERFORMANCE-EVIDENCE.md -> RELIABILITY.md budgets via explicit 5 to 12 ms render-total comparison and 10-minute long-run reference."
---

<objective>
Create the primary SDK-side performance evidence path for Phase 23.

Purpose: PERF-01 and PERF-04 need runnable current-environment evidence that is independent of the known Demo simulator Metal Toolchain blocker.
Output: A focused SwiftPM XCTest file plus the initial redacted `23-PERFORMANCE-EVIDENCE.md` timing and memory baseline.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@RELIABILITY.md
@SECURITY.md
@QUALITY_SCORE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-performance-and-reliability-gates/23-CONTEXT.md
@.planning/phases/23-performance-and-reliability-gates/23-RESEARCH.md
@.planning/phases/23-performance-and-reliability-gates/23-VALIDATION.md
@.planning/phases/23-performance-and-reliability-gates/23-PATTERNS.md
@BeautySDK/Sources/BeautySDK/BeautyEngine.swift
@BeautySDK/Sources/BeautyCore/Models/BeautyConfiguration.swift
@BeautySDK/Sources/BeautyCore/Models/BeautyRenderQuality.swift
@BeautySDK/Tests/BeautyCoreTests/BeautyEngineTests.swift
</context>

## Artifacts this phase produces

- `BeautySDK/Tests/BeautyCoreTests/BeautyPerformanceEvidenceTests.swift`
- `BeautyPerformanceEvidenceTests`
- `PerformanceEvidenceCase`
- `PerformanceTimingSummary`
- `PerformanceMemoryTrend`
- `PerformanceEvidenceReport`
- `testPERF01Records720pTimingMatrixAndBudgetComparison`
- `testPERF04RecordsFixtureLoopMemoryTrendWithNonClaimForShortRuns`
- `testPERF05PerformanceEvidenceReportUsesOnlyAllowlistedFields`
- `CameraBeautyPipelineTests.testPERF02BackpressureStressKeepsLatestFrameWinsAndCountsDroppedFrames`
- `CameraBeautyPipelineTests.testPERF03ResetClearsPendingWorkDropCountersWarningsAndSnapshots`
- `ImageEditorPipelineTests.testPERF03SelectionFailureCanRecoverWithLatestValidFixture`
- `BeautyConfigurationTests.testPERF03RenderQualityModesAreStableConfigurationContract`
- `BeautyEngineTests.testPERF03ResetPreservesConfigurationQualityAndDetectionSummaryContract`
- `CombinedEffectSafetyTests.testPERF03HighCappedTimingParametersPreserveSafetyCapsAndRedactedMetrics`
- `MissingLandmarkDegradationTests.testPERF03NoFaceMissingStaleAndReusedGeometryRemainRedactedAndDegraded`
- `.planning/phases/23-performance-and-reliability-gates/23-PERFORMANCE-EVIDENCE.md`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SDK timing and fixture-loop memory evidence tests</name>
  <files>BeautySDK/Tests/BeautyCoreTests/BeautyPerformanceEvidenceTests.swift</files>
  <read_first>
    AGENTS.md
    RELIABILITY.md
    SECURITY.md
    .planning/phases/23-performance-and-reliability-gates/23-CONTEXT.md
    .planning/phases/23-performance-and-reliability-gates/23-RESEARCH.md
    .planning/phases/23-performance-and-reliability-gates/23-PATTERNS.md
    BeautySDK/Sources/BeautySDK/BeautyEngine.swift
    BeautySDK/Tests/BeautyCoreTests/BeautyEngineTests.swift
  </read_first>
  <behavior>
    - RED: a focused SwiftPM filter for BeautyCoreTests.BeautyPerformanceEvidenceTests fails because the new test file or test symbols do not exist.
    - GREEN: the tests create a synthetic 1280x720 BGRA CVPixelBuffer and call BeautyEngine.processResult(pixelBuffer:metadata:parameters:) for default_noop, skin_color_filter, and high_capped cases per D-01 and D-02.
    - GREEN: timing summaries expose sample count, warmup count, mean milliseconds, maximum milliseconds, resolution bucket, quality mode, warning codes, and metric keys only.
    - GREEN: the fixture-loop memory summary records start, end, peak, iteration count, case mix, and growth trend when a local memory sampler is available; otherwise it records memoryMetricStatus=unavailable and makes no memory-growth pass claim.
    - GREEN: the default long-run test duration is short enough for local SwiftPM verification and emits an explicit D-07 non-claim plus a 600-second rerun protocol.
  </behavior>
  <action>
    Create `BeautySDK/Tests/BeautyCoreTests/BeautyPerformanceEvidenceTests.swift` with `final class BeautyPerformanceEvidenceTests: XCTestCase`. Add local helpers named `PerformanceEvidenceCase`, `PerformanceTimingSummary`, `PerformanceMemoryTrend`, and `PerformanceEvidenceReport` unless an equivalent clearer name is needed inside the same file. Build the case matrix from `BeautyParameters()` for `default_noop`, a current skin/color/filter case using skin whitening or smoothing plus brightness/filter, and a `high_capped` case using high skin and geometry values that must preserve existing safety-cap warnings per D-02 and D-11. Use `BeautyInputMetadata(orientation: .up, source: .testFixture)` and `BeautyEngine(configuration:)`; do not add public API, public parameters, Demo UI, renderer strategy rewrites, image files, or per-frame logging per D-09, D-12, and D-14. The report string must be an allowlisted table-like summary, not raw JSON, and must reject forbidden payload terms from D-13.
  </action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyPerformanceEvidenceTests</automated>
    <automated>rg -n "BeautyPerformanceEvidenceTests|testPERF01Records720pTimingMatrixAndBudgetComparison|testPERF04RecordsFixtureLoopMemoryTrendWithNonClaimForShortRuns|testPERF05PerformanceEvidenceReportUsesOnlyAllowlistedFields|1280|720|default_noop|skin_color_filter|high_capped" BeautySDK/Tests/BeautyCoreTests/BeautyPerformanceEvidenceTests.swift</automated>
    <automated>git diff --check -- BeautySDK/Tests/BeautyCoreTests/BeautyPerformanceEvidenceTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `BeautyPerformanceEvidenceTests.swift` contains the three named PERF tests and a direct call to `processResult(pixelBuffer:metadata:parameters:)`.
    - Behavior assertion: focused SwiftPM filter passes or fails only for a concrete implementation issue that is fixed before the task completes.
    - Source assertion: the file contains no writes of image bytes, local/private paths, face geometry coordinates, raw JSON, raw framework errors, user identifiers, tokens, or raw diagnostics.
    - Source assertion: comments and assertion messages do not claim release-grade 30 fps, production naturalness, physical-device stability, all-device parity, screenshot visual pass, or market-ready performance.
  </acceptance_criteria>
  <done>SDK timing and short fixture-loop memory evidence are runnable through SwiftPM without Demo simulator dependency.</done>
</task>

<task type="auto">
  <name>Task 2: Record initial timing and memory evidence artifact</name>
  <files>.planning/phases/23-performance-and-reliability-gates/23-PERFORMANCE-EVIDENCE.md</files>
  <read_first>
    AGENTS.md
    PLANS.md
    RELIABILITY.md
    SECURITY.md
    .planning/phases/23-performance-and-reliability-gates/23-CONTEXT.md
    .planning/phases/23-performance-and-reliability-gates/23-VALIDATION.md
    BeautySDK/Tests/BeautyCoreTests/BeautyPerformanceEvidenceTests.swift
    .planning/phases/21-baseline-audit-and-quality-ledger-refresh/21-BASELINE-AUDIT.md
    .planning/evidence/v1.4/VISUAL-EVIDENCE.md
  </read_first>
  <action>
    Create `.planning/phases/23-performance-and-reliability-gates/23-PERFORMANCE-EVIDENCE.md` using the Phase 21 evidence-ledger shape. Include frontmatter with phase, status, updated date, and requirements PERF-01, PERF-04, PERF-05. Add sections for scope, explicit non-claims, environment, exact commands, 720p timing matrix, RELIABILITY.md budget comparison, fixture-loop memory trend, shorter-run non-claim and 600-second rerun protocol, redaction policy, and initial blockers. For any timing case above the 5 to 12 ms first-version render-total budget, classify environment, result, impact, risk, and next action per D-03; do not optimize in this task. Use only allowed fields from D-13 and keep `BeautyConfiguration.enablePerformanceLog == false` and release `logLevel == .error` as documented facts per D-14.
  </action>
  <verify>
    <automated>swift --version</automated>
    <automated>xcodebuild -version</automated>
    <automated>swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyPerformanceEvidenceTests</automated>
    <automated>rg -n "PERF-01|PERF-04|PERF-05|1280x720|default_noop|skin_color_filter|high_capped|mean|maximum|budget|memory|non-claim|rerun" .planning/phases/23-performance-and-reliability-gates/23-PERFORMANCE-EVIDENCE.md</automated>
    <automated>rg -n "/private/var|NSError|VNFaceObservation|boundingBox|rawPresetJson|image bytes|userToken|userIdentifier|raw JSON|raw diagnostics|CGPoint|CGRect|SIMD|\\[[0-9]+\\." .planning/phases/23-performance-and-reliability-gates/23-PERFORMANCE-EVIDENCE.md; test $? -eq 1</automated>
    <automated>git diff --check -- .planning/phases/23-performance-and-reliability-gates/23-PERFORMANCE-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: evidence artifact contains command, environment, case table, duration summary, budget comparison, memory trend, non-claims, and rerun protocol.
    - Behavior assertion: over-budget cases are classified with environment/result/impact/risk/next-action instead of broad optimization.
    - CLI output: scoped forbidden-token scan exits with no matches against the evidence artifact.
    - Source assertion: the artifact does not claim release-grade 30 fps, production naturalness, physical-device stability, all-device parity, screenshot visual pass, or market-ready performance.
  </acceptance_criteria>
  <done>Initial performance evidence artifact exists and is redacted, command-backed, budget-aware, and overclaim-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Synthetic frame -> SDK facade | Test-generated CVPixelBuffer crosses public SDK input validation. |
| SDK result -> evidence report | Timing, warning, and metric summaries cross from sensitive processing internals into committed Markdown. |
| Local tooling -> blocker/evidence text | Local Swift/Xcode failures are summarized for repository history. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Information Disclosure | `23-PERFORMANCE-EVIDENCE.md` | mitigate | Use allowlisted fields only: case name, duration summary, resolution bucket, quality mode, warning/metric codes, memory trend, blocker class, impact, next step. Run the scoped forbidden-token scan in every task touching the artifact. |
| T-23-02 | Information Disclosure | `BeautyPerformanceEvidenceTests.swift` report output | mitigate | Tests must never print image bytes, local/private paths, face geometry, raw JSON, raw framework errors, identifiers, tokens, or raw diagnostics per D-13. |
| T-23-03 | Tampering | High-capped timing case | mitigate | The high-capped case must preserve resolver safety caps/warning metrics and must not alter caps to improve timings per D-11. |
| T-23-04 | Repudiation | First timing run | mitigate | Record exact command, environment, results, and over-budget classification; do not silently convert the first baseline into a hard optimization gate per D-03. |
| T-23-05 | Denial of Service | Long-run fixture loop | mitigate | Default automated loop must remain bounded for SwiftPM; longer 600-second run is explicit rerun protocol with non-claim if not run per D-07. |
| T-23-SC | Tampering | Package installs | accept | No npm, pip, cargo, SwiftPM dependency, or external package install is planned; package legitimacy gate is not applicable. |
</threat_model>

<verification>
Run `swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyPerformanceEvidenceTests`, then run the artifact redaction scan and `git diff --check` commands listed in task verification.
</verification>

<success_criteria>
- PERF-01 has a repeatable SDK 720p timing command and current result table.
- PERF-04 has an automated SDK fixture-loop memory trend baseline or explicit metric-unavailable non-claim plus 600-second rerun protocol.
- PERF-05 is enforced for this plan by allowlisted report fields and a negative redaction scan.
</success_criteria>

<output>
Create `.planning/phases/23-performance-and-reliability-gates/23-01-SUMMARY.md` when done.
</output>
