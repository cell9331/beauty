---
phase: 68-cpu-algorithm-reference-oracles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BeautySDK/Tests/BeautyEffectsTests/CPUReferenceFixtureFactory.swift
  - BeautySDK/Tests/BeautyEffectsTests/CPUReferenceMetrics.swift
  - BeautySDK/Tests/BeautyEffectsTests/CPUReferenceFixtureTests.swift
  - BeautySDK/Tests/BeautyCoreTests/CPUReferenceFacadeFixtureFactory.swift
  - BeautySDK/Tests/BeautyCoreTests/CPUReferenceFacadeFixtureTests.swift
autonomous: true
requirements: [CPU-01]
must_haves:
  truths:
    - "Mandatory CPU-oracle tests can construct every required small RGBA fixture in Swift memory without opening tracked portrait media."
    - "Fixture contracts identify opaque/alpha-boundary, geometry, protected/outside, and deterministic support cases with finite dimensions and named sRGB semantics."
    - "The fixture helpers are test-target-local and do not widen BeautySDK public or SPI API."
  artifacts:
    - path: "BeautySDK/Tests/BeautyEffectsTests/CPUReferenceFixtureFactory.swift"
      provides: "Generated BeautyEffects RGBA8, geometry, protected-region, and support fixtures"
      contains: "enum CPUReferenceFixtureFactory"
    - path: "BeautySDK/Tests/BeautyEffectsTests/CPUReferenceMetrics.swift"
      provides: "Transient pixel/geometry metric primitives for later oracle plans"
      contains: "struct CPUReference"
    - path: "BeautySDK/Tests/BeautyCoreTests/CPUReferenceFacadeFixtureFactory.swift"
      provides: "Generated public-facade CIImage and alpha-boundary inputs"
      contains: "CPUReferenceFacadeFixture"
    - path: "BeautySDK/Tests/BeautyEffectsTests/CPUReferenceFixtureTests.swift"
      provides: "Fixture completeness and no-media contract tests"
      min_lines: 80
    - path: "BeautySDK/Tests/BeautyCoreTests/CPUReferenceFacadeFixtureTests.swift"
      provides: "Public-facade fixture construction and metadata contract tests"
      min_lines: 60
  key_links:
    - from: "BeautySDK/Tests/BeautyEffectsTests/CPUReferenceFixtureTests.swift"
      to: "BeautySDK/Tests/BeautyEffectsTests/CPUReferenceFixtureFactory.swift"
      via: "in-memory fixture factory calls"
      pattern: "CPUReferenceFixtureFactory"
    - from: "BeautySDK/Tests/BeautyCoreTests/CPUReferenceFacadeFixtureTests.swift"
      to: "BeautySDK/Sources/BeautySDK/BeautyEngine.swift"
      via: "generated CIImage input contract"
      pattern: "CIImage"
---

<objective>
Create the generated-fixture and transient-metric foundation that every Phase 68
CPU oracle can use without tracked portrait media or a production API change.

Purpose: CPU reference evidence must be reproducible in a clean clone and must
make the required image/support boundaries explicit before feature-family tests
are added.

Output: Target-local Swift fixture builders, metric primitives, and contract
tests covering RGBA8/sRGB, alpha boundaries, geometry patterns, protected/outside
regions, and deterministic support stubs (D-01, D-02, D-04).
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/68-cpu-algorithm-reference-oracles/68-CONTEXT.md
@.planning/phases/68-cpu-algorithm-reference-oracles/68-VALIDATION.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@BeautySDK/Sources/BeautyCore/Models/BeautyCanonicalStillImage.swift
@BeautySDK/Sources/BeautySDK/BeautyEngine.swift
@BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift
@BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build target-local generated CPU fixture and metric support</name>
  <files>BeautySDK/Tests/BeautyEffectsTests/CPUReferenceFixtureFactory.swift, BeautySDK/Tests/BeautyEffectsTests/CPUReferenceMetrics.swift, BeautySDK/Tests/BeautyEffectsTests/CPUReferenceFixtureTests.swift</files>
  <action>Per D-01, D-02, and D-04, add XCTest-target-only builders under `BeautyEffectsTests` for small finite opaque named-sRGB RGBA8 byte arrays, alpha-boundary variants, luminance/chroma/gradient/checker geometry patterns, indexed protected/outside/safe regions, and deterministic complete/malformed/no-face support fixtures. Reuse the existing canonical `FaceGeometry` and observed-support shapes where the target already owns them; do not copy portrait files or add production/SPI fixture types. Add transient metric helpers that compute changed indices, alpha/extent invariants, signed displacement and direction, locality/region intersections, luminance/chroma, and red/yellow-excess values without printing raw bytes or support. Add contract tests that prove all builders are finite, dimension-consistent, explicit sRGB/opaque where promised, deterministic across two constructions, and free of file/media loading. Keep all identifiers and values in memory and make no test output contain paths, coordinates, masks, or raw fixture data.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.CPUReferenceFixtureTests</automated>
    <automated>if rg -n 'CIImage\(contentsOf:|CGImageSourceCreateWithURL|\.png|\.jpg|\.heic|example-images|local-retouch-review' BeautySDK/Tests/BeautyEffectsTests/CPUReferenceFixtureFactory.swift BeautySDK/Tests/BeautyEffectsTests/CPUReferenceMetrics.swift BeautySDK/Tests/BeautyEffectsTests/CPUReferenceFixtureTests.swift; then exit 1; else exit 0; fi</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>BeautyEffectsTests has deterministic in-memory fixture and metric contracts covering every CPU-oracle fixture class, with no tracked-media or durable sensitive-data path.</done>
</task>

<task type="auto">
  <name>Task 2: Add public-facade generated input and boundary contracts</name>
  <files>BeautySDK/Tests/BeautyCoreTests/CPUReferenceFacadeFixtureFactory.swift, BeautySDK/Tests/BeautyCoreTests/CPUReferenceFacadeFixtureTests.swift</files>
  <action>Per D-01 and D-04, add a separate `BeautyCoreTests` fixture factory for generated CIImage inputs and rendered RGBA8 observations so the facade suite does not import `BeautyEffectsTests` internals. Cover a small named-sRGB opaque neutral pattern, alpha values at the transparent/near-opaque/opaque boundaries, a geometry-revealing gradient, and deterministic conversion to public `BeautyInputMetadata(source: .testFixture)`. Contract tests must assert finite positive dimensions, exact byte counts, explicit color-space identity, repeatable bytes, and a transparent image that reaches the public validation seam without relying on a tracked image. Keep raw bytes transient, avoid `@testable` imports outside existing target conventions, and do not expose the factory through BeautySDK or its SPI.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter BeautyCoreTests.CPUReferenceFacadeFixtureTests</automated>
    <automated>if rg -n 'BeautyEffects|BeautyDetection|example-images|local-retouch-review' BeautySDK/Tests/BeautyCoreTests/CPUReferenceFacadeFixtureFactory.swift BeautySDK/Tests/BeautyCoreTests/CPUReferenceFacadeFixtureTests.swift; then exit 1; else exit 0; fi</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>BeautyCoreTests can generate and validate all public-facade boundary inputs in memory, including the transparent rejection candidate, without adding a production testing surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Generated test fixture → CPU pipeline | In-memory bytes/support cross into package-internal test seams. |
| Test assertion output → durable transcript | XCTest failures could accidentally expose pixels, support, or paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-68-01-01 | Tampering | Fixture builders | mitigate | Deterministic pure builders, finite/dimension assertions, and no file/media loading keep fixtures source-owned and reproducible. |
| T-68-01-02 | Information Disclosure | Metric/assertion output | mitigate | Metrics are aggregate-only; tests never print raw bytes, masks, landmarks, coordinates, or local paths. |
| T-68-01-SC | Tampering | SwiftPM/test dependencies | accept | No package install or dependency change is authorized; the existing package graph is exercised unchanged. |
</threat_model>

<verification>
Run both focused fixture suites, inspect generated-fixture sources for media/path access, and run `git diff --check`. The phase gate later reruns these tests through the full archive → boundary → consumer → no-skip conjunction.
</verification>

<success_criteria>
- Both test targets build and pass their generated-fixture contract suites.
- Opaque/alpha, geometry, protected/outside, and support-stub fixtures are deterministic and in memory.
- No PNG/JPEG/HEIC, portrait path, raw support, or new production/SPI fixture surface is introduced.
</success_criteria>

<output>
Create `.planning/phases/68-cpu-algorithm-reference-oracles/68-01-SUMMARY.md` when done.
</output>
