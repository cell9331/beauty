---
phase: 68-cpu-algorithm-reference-oracles
plan: 03
type: execute
wave: 2
depends_on: [68-01]
files_modified:
  - BeautySDK/Tests/BeautyEffectsTests/CPUReferenceLocalRetouchOracleTests.swift
  - BeautySDK/Tests/BeautyCoreTests/CPUReferenceDeterminismTests.swift
autonomous: true
requirements: [CPU-02, CPU-04]
must_haves:
  truths:
    - "Generated teeth and sclera units reduce their required local color excess while preserving alpha, hard protected/outside regions, and bounded luminance/texture behavior."
    - "Composition uses immutable original pixels, preserves source on unexpected collisions, and keeps unique neighboring units eligible."
    - "Repeated, fresh, invalid, recovery, and malformed-peer requests produce finite deterministic results without stale request-local state or sibling suppression."
  artifacts:
    - path: "BeautySDK/Tests/BeautyEffectsTests/CPUReferenceLocalRetouchOracleTests.swift"
      provides: "Generated teeth/sclera/composition safety and collision oracle"
      contains: "CPUReferenceLocalRetouchOracleTests"
      min_lines: 260
    - path: "BeautySDK/Tests/BeautyCoreTests/CPUReferenceDeterminismTests.swift"
      provides: "Public-facade repeatability, recovery, and per-unit isolation oracle"
      contains: "CPUReferenceDeterminismTests"
      min_lines: 180
  key_links:
    - from: "BeautySDK/Tests/BeautyEffectsTests/CPUReferenceLocalRetouchOracleTests.swift"
      to: "BeautySDK/Sources/BeautyEffects/LocalRetouch/BeautyTeethWhiteningProvider.swift"
      via: "generated canonical source and lip-support execution"
      pattern: "BeautyTeethWhiteningProvider"
    - from: "BeautySDK/Tests/BeautyEffectsTests/CPUReferenceLocalRetouchOracleTests.swift"
      to: "BeautySDK/Sources/BeautyEffects/Render/BeautyLocalRetouchComposition.swift"
      via: "owner composition and collision summary"
      pattern: "collisionPixelCount"
    - from: "BeautySDK/Tests/BeautyCoreTests/CPUReferenceDeterminismTests.swift"
      to: "BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift"
      via: "deterministic support/failure harness"
      pattern: "SDKTestingLocalRetouchFoundationHarness"
    - from: "BeautySDK/Tests/BeautyCoreTests/CPUReferenceDeterminismTests.swift"
      to: "BeautySDK/Sources/BeautySDK/BeautyEngine.swift"
      via: "public facade processResult calls"
      pattern: "processResult"
---

<objective>
Freeze local-retouch safety/composition semantics and CPU request determinism on
generated fixtures.

Purpose: The highest-risk current CPU behavior is not visual novelty; it is
containment, immutable-source ownership, collision handling, local failure, and
state isolation across requests. These oracles must catch regressions without
depending on a portrait file or native Vision runtime.

Output: Generated teeth/sclera/composition metrics plus public-facade repeated,
failure, recovery, and sibling-isolation tests (D-04, D-05, D-06).
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/68-cpu-algorithm-reference-oracles/68-CONTEXT.md
@.planning/phases/68-cpu-algorithm-reference-oracles/68-VALIDATION.md
@.planning/phases/68-cpu-algorithm-reference-oracles/68-01-PLAN.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@DESIGN.md
@SECURITY.md
@RELIABILITY.md
@BeautySDK/Sources/BeautyEffects/LocalRetouch/BeautyTeethWhiteningProvider.swift
@BeautySDK/Sources/BeautyEffects/LocalRetouch/BeautyScleraRednessProvider.swift
@BeautySDK/Sources/BeautyEffects/Render/BeautyLocalRetouchComposition.swift
@BeautySDK/Sources/BeautySDK/BeautyEngine.swift
@BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift
@BeautySDK/Tests/BeautyEffectsTests/BeautyTeethWhiteningProviderTests.swift
@BeautySDK/Tests/BeautyEffectsTests/BeautyFullScleraRednessProviderTests.swift
@BeautySDK/Tests/BeautyEffectsTests/BeautyLocalRetouchCompositionTests.swift
@BeautySDK/Tests/BeautyCoreTests/BeautyEngineCombinedLocalRetouchCloseoutTests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add generated local-retouch safety and composition oracles</name>
  <files>BeautySDK/Tests/BeautyEffectsTests/CPUReferenceLocalRetouchOracleTests.swift</files>
  <action>Per D-04 and D-05, add generated opaque canonical mouth/eye fixtures with unique source colors, fixed inner-lip/qualified-sclera envelopes, and indexed protected sets for gum/tongue/lookalike, iris/pupil/highlight/lash/caruncle/exterior, and alpha sentinels. Exercise the real teeth and stable sclera providers/transforms and the `BeautyLocalRetouchCompositionOwner`. Assert yellow/red-excess direction only inside eligible units, bounded luminance/chroma/texture/channel deltas, exact alpha and outside/protected bytes, no-op negative/too-light/materially-unqualified inputs, and no unit for malformed or missing support. Add independent composition cases that prove Q16 original-pixel blending, unique-neighbor retention, foreign/stale owner rejection, duplicate-claim rejection, and collision-to-source output/aggregate semantics. Keep every proposal/pixel/mask value transient and use aggregate failure messages only.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.CPUReferenceLocalRetouchOracleTests</automated>
    <automated>if rg -n 'CIImage\(contentsOf:|CGImageSourceCreateWithURL|example-images|local-retouch-review|print\(|debugDescription|CGPoint|SIMD' BeautySDK/Tests/BeautyEffectsTests/CPUReferenceLocalRetouchOracleTests.swift; then exit 1; else exit 0; fi</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>Generated local-retouch tests prove feature-specific color direction, hard containment, source ownership, collision-to-source, alpha preservation, and per-unit abstention without tracked media.</done>
</task>

<task type="auto">
  <name>Task 2: Add public-facade deterministic recovery and sibling-isolation oracles</name>
  <files>BeautySDK/Tests/BeautyCoreTests/CPUReferenceDeterminismTests.swift</files>
  <action>Per D-05 and D-06, use the existing `SDKTestingLocalRetouchFoundationHarness` with generated CIImage input and deterministic support sequences to compare repeated same-parameter results, a fresh engine, and a valid→invalid→valid request sequence. Assert exact rendered RGBA bytes, dimensions, warning codes, aggregate metric keys/values, detection summary, and finite/bounded channel results where the current contract is exact. Cover transparent rejection before provider/composition/render, malformed one-eye support preserving a valid peer, malformed sclera not suppressing eligible teeth, provider abstention preserving unrelated brightness/color work, and no-face/missing-support local degradation. Include a bounded independent-request matrix that proves no earlier request contributes pixels, support, proposals, carriers, or summaries, while avoiding any new public concurrency claim. Keep comparisons aggregate-only and do not persist harness internals.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter BeautyCoreTests.CPUReferenceDeterminismTests</automated>
    <automated>if rg -n 'XCTSkip|PHASE[0-9]+_|CIImage\(contentsOf:|example-images|local-retouch-review|absoluteString|\.path' BeautySDK/Tests/BeautyCoreTests/CPUReferenceDeterminismTests.swift; then exit 1; else exit 0; fi</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>Public-facade generated requests are deterministic and state-independent across repeats and recovery, and face-dependent failure leaves eligible siblings/face-agnostic work intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Generated local support → provider/composition | Synthetic anatomy/support exercises request-local biometric-adjacent logic. |
| Engine result → test evidence | Result bytes/metrics must not become durable raw image or support evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-68-03-01 | Tampering | Local-retouch ownership oracle | mitigate | Assert immutable original-pixel reads, checked ownership/source bindings, collision-to-source, and hard containment. |
| T-68-03-02 | Information Disclosure | Local-retouch test diagnostics | mitigate | Keep raw proposals, masks, support, pixels, and paths in memory; record only bounded aggregate outcomes. |
| T-68-03-03 | Denial of Service | Generated provider/composition workspace | mitigate | Use small fixed images, provider capacity preflight, bounded unit/proposal counts, and no persisted output. |
| T-68-03-04 | Tampering | Request state/recovery | mitigate | Compare repeated/fresh/valid-invalid-valid outputs and assert zero retained sensitive observations after success/throw. |
| T-68-03-SC | Tampering | SwiftPM/test dependencies | accept | No package install or external model/resource is introduced. |
</threat_model>

<verification>
Run the focused local-retouch and determinism suites, then the existing provider/composition regression suites. Confirm that generated tests have no media/path/skip access and that their failures expose only aggregate categories. Run `git diff --check`.
</verification>

<success_criteria>
- Generated teeth/sclera/composition oracles cover direction, bounds, protection, alpha, source ownership, collision, and local failure.
- Public-facade repeat/recovery tests prove deterministic finite output and sibling continuation without state leakage.
- No public API, production algorithm, GPU/Metal, or private-fixture dependency is changed.
</success_criteria>

<output>
Create `.planning/phases/68-cpu-algorithm-reference-oracles/68-03-SUMMARY.md` when done.
</output>
