---
phase: 68-cpu-algorithm-reference-oracles
plan: 02
type: execute
wave: 2
depends_on: [68-01]
files_modified:
  - BeautySDK/Tests/BeautyEffectsTests/CPUReferenceGeometryOracleTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/CPUReferenceColorOracleTests.swift
autonomous: true
requirements: [CPU-03]
must_haves:
  truths:
    - "Every currently implemented geometry family is checked through its CPU provider and unified pipeline with signed direction, finite displacement, and locality metrics."
    - "Every current skin/global/lip color family is checked through explicit luminance/chroma/channel or red/yellow-excess metrics rather than a generic changed-byte assertion."
    - "Caps, alpha, dimensions, protected/outside sentinels, and no-global-color-bias behavior remain bounded under generated fixtures."
  artifacts:
    - path: "BeautySDK/Tests/BeautyEffectsTests/CPUReferenceGeometryOracleTests.swift"
      provides: "Table-driven CPU geometry field-family oracle"
      contains: "CPUReferenceGeometryOracleTests"
      min_lines: 220
    - path: "BeautySDK/Tests/BeautyEffectsTests/CPUReferenceColorOracleTests.swift"
      provides: "Table-driven CPU color field-family oracle"
      contains: "CPUReferenceColorOracleTests"
      min_lines: 180
  key_links:
    - from: "BeautySDK/Tests/BeautyEffectsTests/CPUReferenceGeometryOracleTests.swift"
      to: "BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift"
      via: "normalized parameters and effective strengths"
      pattern: "BeautyEffectResolver.resolve"
    - from: "BeautySDK/Tests/BeautyEffectsTests/CPUReferenceGeometryOracleTests.swift"
      to: "BeautySDK/Sources/BeautyEffects/Render/BeautyGeometryEffectPipeline.swift"
      via: "unified control point and pixel execution"
      pattern: "BeautyGeometryEffectPipeline"
    - from: "BeautySDK/Tests/BeautyEffectsTests/CPUReferenceColorOracleTests.swift"
      to: "BeautySDK/Sources/BeautyEffects/Render/BeautyColorEffectPipeline.swift"
      via: "CPU pixel-buffer/CIImage color execution"
      pattern: "BeautyColorEffectPipeline"
---

<objective>
Add explicit feature-family CPU oracles for geometry and color behavior using the
generated fixtures from Plan 68-01.

Purpose: A CPU reference is useful only when it catches semantic drift—wrong
direction, global color contamination, excessive locality, cap bypass, or
protected-pixel changes—not when it only notices that some byte changed.

Output: Table-driven geometry and color test suites that cover the current public
taxonomy without adding a field, provider, renderer case, backend, or algorithm
(D-02, D-03, D-04).
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/68-cpu-algorithm-reference-oracles/68-CONTEXT.md
@.planning/phases/68-cpu-algorithm-reference-oracles/68-VALIDATION.md
@.planning/phases/68-cpu-algorithm-reference-oracles/68-01-PLAN.md
@.planning/codebase/TESTING.md
@DESIGN.md
@docs/SDK_EFFECT_TAXONOMY.md
@BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
@BeautySDK/Sources/BeautyEffects/Render/BeautyGeometryEffectPipeline.swift
@BeautySDK/Sources/BeautyEffects/Render/BeautyColorEffectPipeline.swift
@BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift
@BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift
@BeautySDK/Tests/BeautyEffectsTests/EyebrowWarpProviderTests.swift
@BeautySDK/Tests/BeautyEffectsTests/NoseWarpProviderTests.swift
@BeautySDK/Tests/BeautyEffectsTests/MouthWarpProviderTests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze geometry direction, displacement, locality, and cap semantics</name>
  <files>BeautySDK/Tests/BeautyEffectsTests/CPUReferenceGeometryOracleTests.swift</files>
  <action>Per D-02, D-03, and D-04, add a table-driven `CPUReferenceGeometryOracleTests` suite using the current `BeautyParameters` fields and the existing complete/malformed support fixtures. Cover the nine face/chin fields, fourteen eye fields, seven eyebrow fields, six nose fields, and eight mouth fields through their real resolver/provider emissions and `BeautyGeometryEffectPipeline` dispatch. For every positive/signed row assert finite normalized source/target points, the documented signed displacement or axis direction, the exact resolver/provider cap relation, and monotonic bounded displacement at low versus high strength. Render a generated gradient/checker pattern through the CPU warp path and measure changed pixels only within the union of control-point support neighborhoods, with protected sentinels and alpha unchanged; assert the neutral plan is byte-identical and geometry does not pass by adding a global RGB bias. Include missing/stale/malformed support rows that remove only dependent geometry and preserve eligible siblings, and assert the test inventory contains no `去脂` or future semantic-mask row.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.CPUReferenceGeometryOracleTests</automated>
    <automated>if rg -n 'Warp\.metal|MTLDevice|Metal|GPU|eye_fat|去脂|double.chin|hairline' BeautySDK/Tests/BeautyEffectsTests/CPUReferenceGeometryOracleTests.swift; then exit 1; else exit 0; fi</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>The generated geometry oracle covers all current geometry families with direction/displacement/locality/cap metrics, exact neutral/protected behavior, and smallest-unit degradation.</done>
</task>

<task type="auto">
  <name>Task 2: Freeze color direction, chroma, red-excess, and bounded output semantics</name>
  <files>BeautySDK/Tests/BeautyEffectsTests/CPUReferenceColorOracleTests.swift</files>
  <action>Per D-02, D-03, and D-04, add a table-driven `CPUReferenceColorOracleTests` suite against `BeautyColorEffectPipeline` and `BeautyEffectResolver` using generated named-sRGB RGBA/BGRA patterns. Cover the current skin controls, global tone controls, filter IDs/intensity, and package-internal lip-color route with metric rows: luminance direction/bounds for brightness, exposure, whitening, highlight, and shadow; channel/chroma direction for saturation, temperature, tint, rosy, and filter contributions; channel-spread reduction/contrast bounds for smoothing/sharpening; and red/yellow-excess direction where the taxonomy requires local color behavior. Assert exact neutral bytes, dimensions, color metadata, alpha preservation, finite values, existing safety caps, lip-envelope containment and outside sentinels, and explicit no-face support abstention. Use metric thresholds derived from current constants and existing tests, keep raw pixel arrays transient, and reject any test that treats a generic nonzero changed count as the semantic oracle.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.CPUReferenceColorOracleTests</automated>
    <automated>if rg -n 'XCTAssertNotEqual\([^\n]*output|output.*changed|bytes.*changed' BeautySDK/Tests/BeautyEffectsTests/CPUReferenceColorOracleTests.swift; then exit 1; else exit 0; fi</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>The generated color oracle covers every current color family with explicit directional/bounded metrics, exact no-op/alpha/extent behavior, and local lip containment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Generated pixels/support → internal effect pipeline | Test-owned fixture data exercises package-only resolver/provider/render seams. |
| Test metrics → evidence transcript | Feature metrics could expose raw image/support detail if assertions are not aggregate-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-68-02-01 | Tampering | Geometry/color oracle assumptions | mitigate | Table rows cite current public fields/taxonomy and assert caps/directions through real providers and pipelines. |
| T-68-02-02 | Information Disclosure | Generated metric failures | mitigate | Keep assertions to counts, bounded deltas, directions, and region categories; never print coordinates or bytes. |
| T-68-02-03 | Denial of Service | Generated raster loops | mitigate | Use fixed small dimensions, checked byte counts, and bounded control-point/metric iteration. |
| T-68-02-SC | Tampering | SwiftPM/test dependencies | accept | No package install or dependency change is authorized for CPU oracle tests. |
</threat_model>

<verification>
Run the focused geometry and color suites after Plan 68-01. Inspect both test files for future taxonomy rows, GPU/Metal tokens, media access, and generic changed-byte assertions. Then run `git diff --check`.
</verification>

<success_criteria>
- Geometry rows cover 44 current geometry fields across face/chin, eyes, brows, nose, and mouth.
- Color rows cover current skin/global/filter/lip semantics with direction- and bound-specific metrics.
- Neutral/protected/alpha/extent behavior is exact and generated-only; no Metal/GPU or new algorithm surface is added.
</success_criteria>

<output>
Create `.planning/phases/68-cpu-algorithm-reference-oracles/68-02-SUMMARY.md` when done.
</output>
