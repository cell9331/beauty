---
phase: 74-cpu-gpu-parity-and-sdk-only-closeout
plan: 02
type: execute
wave: 2
depends_on:
  - 74-01
files_modified:
  - BeautySDK/Tests/BeautyEffectsTests/BeautyBackendSafetyParityTests.swift
autonomous: true
requirements:
  - PARITY-02
must_haves:
  truths:
    - "CPU and GPU preserve alpha, protected bytes, and outside-region bytes exactly while active changes remain inside the CPU-owned eligible envelope."
    - "Geometry collision-to-source, local-retouch immutable-original composition, extent, and containment are equivalent within the declared CPU-reference tolerance and exact where the contract requires source bytes."
    - "No-face, malformed/degraded support, duplicate/foreign claims, and one rejected local unit do not suppress an eligible face-agnostic sibling on either backend."
  artifacts:
    - path: "BeautySDK/Tests/BeautyEffectsTests/BeautyBackendSafetyParityTests.swift"
      provides: "Generated containment, protected-region, degradation, collision, and failure-isolation parity tests"
      contains: "BeautyBackendSafetyParityTests"
  key_links:
    - from: "BeautySDK/Tests/BeautyEffectsTests/BeautyBackendSafetyParityTests.swift"
      to: "BeautySDK/Sources/BeautyEffects/Render/BeautyGeometryEffectPipeline.swift"
      via: "CPU-owned control-point envelope is the eligibility oracle for GPU changed-index checks"
      pattern: "BeautyGeometryEffectPipeline"
    - from: "BeautySDK/Tests/BeautyEffectsTests/BeautyBackendSafetyParityTests.swift"
      to: "BeautySDK/Sources/BeautyEffects/Render/BeautyLocalRetouchComposition.swift"
      via: "composition-owned source/collision/failure summary is compared before backend rendering"
      pattern: "BeautyLocalRetouchCompositionOwner"
---

<objective>
Extend the generated parity matrix with the safety and failure semantics that are
most likely to drift between CPU and Metal.

Purpose: Prove parity is not merely a global numeric average; protected regions,
containment, degradation, collision ownership, and smallest-unit recovery must
remain CPU-owned contracts.
Output: A focused safety-parity XCTest suite with aggregate-only evidence.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.codex/skills/spike-findings-beauty/SKILL.md
@BeautySDK/Tests/BeautyEffectsTests/BeautyBackendParityFixtureFactory.swift
@BeautySDK/Tests/BeautyEffectsTests/BeautyMetalGeometryPassTests.swift
@BeautySDK/Tests/BeautyEffectsTests/BeautyMetalLocalRetouchPassTests.swift
@BeautySDK/Tests/BeautyEffectsTests/BeautyLocalRetouchCompositionTests.swift
@BeautySDK/Sources/BeautyEffects/Render/BeautyGeometryEffectPipeline.swift
@BeautySDK/Sources/BeautyEffects/Render/BeautyLocalRetouchComposition.swift
@BeautySDK/Sources/BeautyEffects/Planning/BeautyLocalRetouchAdmission.swift

The local-retouch skill rules apply: support is request-local, missing or
malformed regions fail closed, edits derive from immutable original pixels under
one owner, and raw masks/landmarks/pixels never enter durable evidence. Do not
add a new provider or use UI/device fixtures.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove color/geometry containment and protected-region parity</name>
  <files>BeautySDK/Tests/BeautyEffectsTests/BeautyBackendSafetyParityTests.swift</files>
  <action>Using the Plan-01 generated fixtures and comparison helpers, add table-driven tests for active global color, face-local color, and all existing geometry families. Derive the eligible envelope from the existing CPU-owned normalized plan/control points, then require every GPU-changed pixel to be inside that envelope; require exact equality for alpha and generated outside/protected bytes; require finite outputs, unchanged dimensions, and bounded RGB deltas only inside eligible regions. Add a translated still-image case to ensure extent preservation is independent of raster origin. Assert collision-to-source by comparing colliding destination bytes with the original source bytes and with CPU output, not by logging pixel payloads. Use aggregate changed/protected/collision counts in XCTest failure messages only.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter 'BeautyEffectsTests.BeautyBackendSafetyParityTests'</automated>
    <automated>swift test --package-path BeautySDK --filter 'BeautyEffectsTests.BeautyBackendSafetyParityTests|BeautyEffectsTests.BeautyMetalGeometryPassTests|BeautyEffectsTests.BeautyMetalLocalRetouchPassTests'</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>Generated safety tests prove active changes remain within CPU-owned containment, protected/outside/alpha bytes remain exact, collision-to-source is retained, and extent/finite/tolerance checks pass for CPU and GPU.</done>
</task>

<task type="auto">
  <name>Task 2: Prove no-face, degraded, and smallest-unit isolation parity</name>
  <files>BeautySDK/Tests/BeautyEffectsTests/BeautyBackendSafetyParityTests.swift</files>
  <action>Extend the same suite with complete, no-face, malformed, stale/degraded, duplicate, and foreign-owner generated support cases. Compare neutral/no-op bytes and aggregate failure counts for unsupported local units, then add a mixed request in which one local-retouch unit is rejected while an eligible face-agnostic color effect remains active; require CPU and GPU to preserve the sibling output, alpha, extent, and source-boundary bytes. Exercise valid-invalid-valid ordering and a terminal Metal unit/resource failure through existing dependency hooks, asserting that no partial carrier is published, request resources return to zero, and CPU or face-agnostic siblings are not erased by the rejected unit. Keep all support and pixel values transient and assert only counts, equality, containment, and bounded deltas.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter 'BeautyEffectsTests.BeautyBackendSafetyParityTests'</automated>
    <automated>swift test --package-path BeautySDK --filter 'BeautyEffectsTests.BeautyMetalLocalRetouchPassTests|BeautyEffectsTests.BeautyBackendSafetyParityTests'</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>No-face/degraded requests are safe no-ops or bounded failures, local-retouch collisions and rejected units remain isolated, and an eligible CPU/face-agnostic sibling survives without partial or out-of-envelope publication on either backend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| CPU-owned support/composition → GPU carrier | Existing CPU detection, containment, and composition summaries are the only support source published to Metal. |
| Rejected unit → sibling units | A malformed, duplicate, foreign, or failed unit must not corrupt eligible sibling output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-74-04 | Tampering | containment/collision parity assertions | mitigate | Compute eligibility from existing CPU control/composition owners and require exact protected/source bytes plus bounded eligible deltas. |
| T-74-05 | Denial of Service | malformed/degraded/failure cases | mitigate | Use bounded synthetic support, terminal typed errors, resource counters, and no retry/partial-output acceptance. |
| T-74-06 | Information Disclosure | safety test evidence | mitigate | Assert aggregate counts/equality only; prohibit raw mask, landmark, pixel, path, and durable fixture output. |
| T-74-SC | Tampering | package installs | accept | No package installation or external dependency is introduced. |
</threat_model>

<verification>
Run the focused safety suite alongside the existing geometry/local-retouch suites,
then run the SDK-only privacy/static scan and `git diff --check`. Confirm no test
uses media, file paths, skip-based availability, or durable raw payloads.
</verification>

<success_criteria>
Containment, collision-to-source, alpha/extent, no-face/degradation, protected
bytes, and smallest-unit failure isolation are checked against the retained CPU
semantics for available Metal, with aggregate-only evidence.
</success_criteria>

<output>
Create `.planning/phases/74-cpu-gpu-parity-and-sdk-only-closeout/74-02-SUMMARY.md` when done.
</output>
