---
phase: 74-cpu-gpu-parity-and-sdk-only-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BeautySDK/Tests/BeautyEffectsTests/BeautyBackendParityFixtureFactory.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautyBackendParityTests.swift
autonomous: true
requirements:
  - PARITY-01
  - PARITY-02
must_haves:
  truths:
    - "The same generated in-memory RGBA8 request matrix reaches the retained CPU executor and the available Metal executor without changing input kind, dimensions, alpha, extent, or named color metadata."
    - "Neutral plans produce exact identical output bytes and dimensions, while active plans compare CPU and GPU bytes through explicit bounded channel and mean-RGB tolerances."
    - "Parity evidence retains only aggregate counts, deltas, and status; no fixture bytes, masks, landmarks, pixels, paths, or host details are written to durable output."
  artifacts:
    - path: "BeautySDK/Tests/BeautyEffectsTests/BeautyBackendParityFixtureFactory.swift"
      provides: "Generated bounded RGBA8 fixtures and request-local CPU/Metal render/observation helpers"
      contains: "CPUReferenceFixtureFactory"
    - path: "BeautySDK/Tests/BeautyEffectsTests/BeautyBackendParityTests.swift"
      provides: "Cross-backend structural and numeric parity matrix"
      contains: "BeautyBackendParityTests"
  key_links:
    - from: "BeautySDK/Tests/BeautyEffectsTests/BeautyBackendParityTests.swift"
      to: "BeautySDK/Sources/BeautyEffects/Backend/BeautyCPUBackend.swift"
      via: "identical generated BeautyBackendRequest rendered by the retained CPU executor"
      pattern: "BeautyCPUBackend"
    - from: "BeautySDK/Tests/BeautyEffectsTests/BeautyBackendParityTests.swift"
      to: "BeautySDK/Sources/BeautyEffects/Backend/BeautyMetalBackend.swift"
      via: "identical generated BeautyBackendRequest rendered by the available Metal executor"
      pattern: "BeautyMetalBackend"
---

<objective>
Create the generated, request-local parity harness and prove the shared structural
contract between the permanent CPU reference and available Metal backend.

Purpose: Establish one reusable fixture/observation seam before adding safety and
concurrency cases, while keeping all raw bytes transient to one XCTest invocation.
Output: Generated fixture helpers plus structural/numeric CPU-vs-GPU parity tests.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@BeautySDK/Tests/BeautyEffectsTests/CPUReferenceFixtureFactory.swift
@BeautySDK/Tests/BeautyEffectsTests/CPUReferenceMetrics.swift
@BeautySDK/Tests/BeautyEffectsTests/BeautyMetalColorPassTests.swift
@BeautySDK/Tests/BeautyEffectsTests/BeautyMetalGeometryPassTests.swift
@BeautySDK/Tests/BeautyEffectsTests/BeautyMetalLocalRetouchPassTests.swift
@BeautySDK/Sources/BeautyEffects/Backend/BeautyBackendContract.swift
@BeautySDK/Sources/BeautyEffects/Backend/BeautyCPUBackend.swift
@BeautySDK/Sources/BeautyEffects/Backend/BeautyMetalBackend.swift

The CPU implementation and its generated oracle remain authoritative. Reuse the
existing generated fixture conventions and backend contract; do not add a new
algorithm, public selector, shader, fixture file, media path, or durable raster
artifact. Metal-unavailable hosts must be represented as a separate gate outcome,
not as a parity pass or an XCTest skip.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define generated parity fixtures and aggregate observations</name>
  <files>BeautySDK/Tests/BeautyEffectsTests/BeautyBackendParityFixtureFactory.swift</files>
  <action>Define target-local generated fixtures for opaque color, alpha-boundary, geometry, protected/outside-region, no-face, malformed/degraded, and composed local-retouch requests by composing the existing `CPUReferenceFixtureFactory` values rather than persisting new data. Add request-local helpers that create matching CPU and Metal `BeautyBackendRequest` values, convert BGRA/RGBA only in memory, rasterize still-image output for comparison, and return aggregate observations (kind, width/height, alpha-preservation, extent, color-space name, changed-count, max channel delta, mean RGB delta). Keep byte arrays scoped to the test invocation; reject non-finite or over-sized generated inputs; never expose masks, landmarks, raw pixels, fixture locators, or framework diagnostics in a report. Preserve exact neutral-byte comparison and declare the active-case tolerances in one helper so later safety tests cannot silently widen them.</action>
  <verify>
    <automated>swift build --package-path BeautySDK</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>A reusable generated-only parity helper exists in the BeautyEffects test target, uses the existing CPU reference fixture vocabulary, and exposes only bounded in-memory comparison values to its tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add structural and numeric backend parity matrix</name>
  <files>BeautySDK/Tests/BeautyEffectsTests/BeautyBackendParityTests.swift</files>
  <action>Implement generated XCTest coverage that renders the same neutral, global-color, geometry, and composed-carrier requests through `BeautyCPUBackend` and `BeautyMetalBackend` on an available host. Assert exact output kind, dimensions, alpha bytes, extent, and named sRGB metadata; assert exact bytes for neutral/no-face plans; and for active plans assert finite bounded bytes plus the declared max-channel and mean-RGB deltas against CPU. Include both pixel-buffer and still-image requests, including a translated extent, and preserve the existing 61-parameter/five-preset/74-renderer inventory by using existing normalized plans only. If Metal construction is unavailable, return only through the parity suite's explicit availability classification hook used by the later gate; do not call `XCTSkip` or count the host as parity success.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter 'BeautyEffectsTests.BeautyBackendParityTests'</automated>
    <automated>swift test --package-path BeautySDK --filter 'BeautyEffectsTests.BeautyBackendParityTests|BeautyEffectsTests.BeautyMetalColorPassTests|BeautyEffectsTests.BeautyMetalGeometryPassTests|BeautyEffectsTests.BeautyMetalLocalRetouchPassTests'</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>The generated parity matrix proves exact structural compatibility and bounded active-output CPU-vs-GPU deltas on an available host, with no skip-based success or durable raw payload.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Generated test input → CPU/Metal executor | Synthetic bounded bytes and normalized plans enter request-local backend calls. |
| Backend result → parity observation | Only dimensions, metadata flags, alpha checks, and bounded deltas are retained. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-74-01 | Tampering | parity fixture/backend comparison | mitigate | Render identical generated requests through both executors and require exact structural assertions plus pinned finite tolerances. |
| T-74-02 | Information Disclosure | parity fixture/evidence output | mitigate | Keep bytes request-local and publish aggregate deltas only; reject file/media/path/mask/landmark/pixel persistence. |
| T-74-03 | Denial of Service | generated fixture construction | mitigate | Bound dimensions, byte products, and matrix size before allocation; use no unbounded loops or external resources. |
| T-74-SC | Tampering | package installs | accept | No package installation or external dependency is introduced. |
</threat_model>

<verification>
Run the focused parity suite and existing Metal feature suites, then build and run
`git diff --check`. Verify that no generated test file writes media, paths, raw
support, or durable output and that neutral requests remain byte-identical.
</verification>

<success_criteria>
Generated CPU/GPU tests compare the same request matrix through the retained CPU
reference and available Metal path, prove exact structural invariants and explicit
active-output tolerances, and preserve the SDK-only/privacy boundary.
</success_criteria>

<output>
Create `.planning/phases/74-cpu-gpu-parity-and-sdk-only-closeout/74-01-SUMMARY.md` when done.
</output>
