---
phase: 74-cpu-gpu-parity-and-sdk-only-closeout
plan: 04
type: execute
wave: 3
depends_on:
  - 74-01
  - 74-02
  - 74-03
files_modified:
  - scripts/check-backend-parity.sh
  - scripts/run-no-skip-swiftpm.sh
autonomous: true
requirements:
  - PARITY-01
  - PARITY-02
  - PARITY-03
  - CLOSE-01
must_haves:
  truths:
    - "A mutation-tested SDK-owned parity gate rejects missing CPU-vs-GPU assertions, widened/removed tolerances, raw durable output, skip-based availability, and unavailable-as-success accounting."
    - "The archive-first no-skip wrapper runs backend parity exactly once after configuration and before consumer/CPU-oracle/opt-in/full-child stages."
    - "The mandatory gate executes CPU reference, configuration, available/unavailable Metal, parity, and static scope checks with zero failures, zero unexpected skips, and a nonzero test denominator."
  artifacts:
    - path: "scripts/check-backend-parity.sh"
      provides: "Static, mutation, focused-test, determinism, safety, and availability parity preflight"
      contains: "BeautyBackendParityTests"
    - path: "scripts/run-no-skip-swiftpm.sh"
      provides: "Archive-first parity gate integration"
      contains: "check-backend-parity.sh"
  key_links:
    - from: "scripts/check-backend-parity.sh"
      to: "BeautySDK/Tests/BeautyEffectsTests/BeautyBackendParityTests.swift"
      via: "focused structural/numeric parity filter and required marker"
      pattern: "BeautyBackendParityTests"
    - from: "scripts/check-backend-parity.sh"
      to: "BeautySDK/Tests/BeautyCoreTests/BeautyBackendSelectionConcurrencyTests.swift"
      via: "request-local/unavailable classification coverage"
      pattern: "BeautyBackendSelectionConcurrencyTests"
    - from: "scripts/run-no-skip-swiftpm.sh"
      to: "scripts/check-backend-parity.sh"
      via: "exactly-once archive-first invocation before child stages"
      pattern: "check-backend-parity.sh"
---

<objective>
Bind all generated parity, safety, determinism, and availability evidence into
the archive-first SDK-owned mandatory gate.

Purpose: Make parity and closeout reproducible in a clean clone and prevent a
passing full child from hiding omitted, skipped, or scope-drifting parity tests.
Output: Mutation-tested parity preflight and full no-skip integration.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@scripts/check-backend-neutral-contract.sh
@scripts/check-metal-runtime.sh
@scripts/check-metal-feature-passes.sh
@scripts/check-backend-configuration.sh
@scripts/check-cpu-reference-oracles.sh
@scripts/run-no-skip-swiftpm.sh
@BeautySDK/Tests/BeautyEffectsTests/BeautyBackendParityTests.swift
@BeautySDK/Tests/BeautyEffectsTests/BeautyBackendSafetyParityTests.swift
@BeautySDK/Tests/BeautyEffectsTests/BeautyBackendDeterminismParityTests.swift
@BeautySDK/Tests/BeautyCoreTests/BeautyBackendSelectionConcurrencyTests.swift

Follow the existing bounded-output, regular-file, mutation, exact-focused-count,
zero-failure/zero-skip conventions. The full wrapper must remain archive-first;
unavailable-host evidence is explicit and cannot be converted into a skip or
GPU parity success. No package dependency, UI/Demo target, device test, or
performance/release claim may enter this gate.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the mutation-tested parity preflight</name>
  <files>scripts/check-backend-parity.sh</files>
  <action>Create an executable gate modeled on the existing backend/Metal/CPU-oracle gates. Require all four generated parity suites to be regular files under SwiftPM test targets; require the named structural fields (kind/dimensions/alpha/extent/color metadata), exact neutral-byte assertions, bounded max-channel/mean-RGB tolerance assertions, containment/protected/collision/no-face/degraded/failure-isolation markers, bounded task-group/actor determinism markers, and explicit `metal_available`/`metal_unavailable` classification. Reject UI/Demo/device/network/file/media imports, `XCTSkip`, raw durable output, private fixture paths, public API/schema drift, CPU fallback/retry in Metal parity, and unbounded loops or sleeps. Add self-test mutations removing the CPU oracle comparison, weakening/removing a tolerance, merging unavailable into success, removing a required safety suite, and adding raw file output; each must fail static validation. Live mode must run the combined focused suites, pin the actual nonzero test count, require zero failures/skips, and emit only aggregate counts and separate availability markers.</action>
  <verify>
    <automated>bash scripts/check-backend-parity.sh --self-test</automated>
    <automated>bash scripts/check-backend-parity.sh</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>The parity gate's static and mutation checks fail closed on omitted comparisons, tolerance/availability/scope drift, raw output, and missing suites; live focused parity coverage is nonzero with zero failures/skips and separate host classifications.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate parity into the mandatory archive-first closeout</name>
  <files>scripts/run-no-skip-swiftpm.sh</files>
  <action>Invoke `check-backend-parity.sh` exactly once after the Phase-73 configuration gate and before the external consumer, generated CPU reference, private opt-ins, and single full SwiftPM child. Preserve all existing archive verification, post-archive SDK-only boundary, runtime/feature gates, transcript bounds, eight opt-in exact-once checks, zero-failure/zero-skip accounting, and nonzero test denominator. Run the complete wrapper in this task and record actual focused/full counts and separate Metal availability classifications for the documentation plan; do not treat the normal SwiftPM environment-gated skips as closeout evidence.</action>
  <verify>
    <automated>bash scripts/run-no-skip-swiftpm.sh --self-test</automated>
    <automated>bash scripts/run-no-skip-swiftpm.sh</automated>
    <automated>count=$(rg -o 'check-backend-parity\.sh' scripts/run-no-skip-swiftpm.sh | wc -l | tr -d ' '); test "$count" -eq 1</automated>
    <automated>bash scripts/check-sdk-only-boundary.sh --post-archive</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>The archive-first wrapper runs parity once in the declared order and completes CPU/configuration/Metal/parity/static/full-child checks with zero failures, zero unexpected skips, eight opt-ins exactly once, and separate availability accounting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Repository source → parity static gate | Mutable tests/scripts are checked for required parity, privacy, and scope invariants. |
| Preflight → no-skip child | Only bounded aggregate status crosses into the closeout transcript. |
| Metal host availability → parity result | Available and unavailable paths remain distinct classifications. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-74-10 | Tampering | parity static/mutation validator | mitigate | Mutate oracle assertions, tolerances, availability, safety suites, and raw-output boundaries; require each mutation to fail. |
| T-74-11 | Denial of Service | focused/full no-skip runner | mitigate | Bound logs/transcripts, require nonzero counts and zero failures/skips, and run gates exactly once in archive-first order. |
| T-74-12 | Information Disclosure | parity/closeout transcript | mitigate | Emit aggregate test counts, deltas, typed errors, and availability markers only; reject raw data/path/framework output. |
| T-74-SC | Tampering | package installs | accept | No package installation or external dependency is introduced. |
</threat_model>

<verification>
Run parity self-test/live mode, the full archive-first no-skip wrapper, post-archive
boundary, transcript self-test, and `git diff --check`. Confirm parity appears
exactly once and before child stages, with no unexpected skip or unavailable-host
credit.
</verification>

<success_criteria>
PARITY-01/02/03 and CLOSE-01 have an executable archive-first gate with mutation
coverage, separate Metal availability accounting, full no-skip evidence, and no
scope/privacy drift.
</success_criteria>

<output>
Create `.planning/phases/74-cpu-gpu-parity-and-sdk-only-closeout/74-04-SUMMARY.md` when done.
</output>
