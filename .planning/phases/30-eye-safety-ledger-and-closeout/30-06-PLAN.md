---
phase: 30-eye-safety-ledger-and-closeout
plan: "06"
type: execute
wave: 6
depends_on:
  - 30-05
files_modified:
  - QUALITY_SCORE.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - EYE-04
  - EYE-05
  - EYE-06
  - EYE-07
  - EYE-08
  - DOC-01
must_haves:
  truths:
    - "QUALITY_SCORE.md independently records observed focused/full-suite facts, fixed 161/161 and 36/36 renderer regression, boundary/review/security results, four-row promotion, branch partial, and remaining eye limitations per D-15 through D-21."
    - "PROJECT.md independently records the verified existing-parameter eye slice while preserving SDK-only, no-public-expansion, local-first, no-UI, no-commercial, no-device/parity/readiness boundaries per D-17 through D-21."
    - "Neither file invents the full-suite count, duplicates detailed evidence tables, or claims whole-branch/product readiness."
  artifacts:
    - path: "QUALITY_SCORE.md"
      provides: "Observed Phase 30 quality evidence and remaining limitations"
      contains: "161/161"
    - path: ".planning/PROJECT.md"
      provides: "Milestone-level verified eye-slice outcome and preserved boundaries"
      contains: "v1.6"
  key_links:
    - from: "QUALITY_SCORE.md"
      to: ".planning/phases/30-eye-safety-ledger-and-closeout/30-EYE-SAFETY-EVIDENCE.md"
      via: "quality facts cite detailed command evidence"
      pattern: "30-EYE-SAFETY-EVIDENCE"
    - from: ".planning/PROJECT.md"
      to: "docs/meitu-function-blueprint/FEATURE_MATRIX.md"
      via: "project outcome preserves branch-level partial status"
      pattern: "眼睛.*partial"
---

<objective>
Synchronize the global quality snapshot and milestone project contract after blueprint/root-contract closeout.

Purpose: isolate quality/project hot spots from both the atomic promotion and the final requirement/roadmap/state/work-ledger transaction.
Output: two independently verified global artifacts based only on observed evidence.
</objective>

## Artifacts this phase produces

- Updated `QUALITY_SCORE.md` with observed Phase 30 tests, renderer/helper counts, boundary/review/security results, promotion, and limitations.
- Updated `.planning/PROJECT.md` with verified v1.6 existing-parameter eye slice and unchanged scope boundaries.
- No requirement checkbox, roadmap status, workflow state, PLANS entry, final verification/validation status, root contract, blueprint file, code, test, renderer, helper, or generated artifact change.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/PROJECT.md
@.planning/phases/30-eye-safety-ledger-and-closeout/30-CONTEXT.md
@.planning/phases/30-eye-safety-ledger-and-closeout/30-EYE-SAFETY-EVIDENCE.md
@.planning/phases/30-eye-safety-ledger-and-closeout/30-REVIEW.md
@.planning/phases/30-eye-safety-ledger-and-closeout/30-SECURITY.md
@.planning/phases/30-eye-safety-ledger-and-closeout/30-VERIFICATION.md
@.planning/phases/30-eye-safety-ledger-and-closeout/30-04-SUMMARY.md
@.planning/phases/30-eye-safety-ledger-and-closeout/30-05-SUMMARY.md
@docs/meitu-function-blueprint/SHAPE_FEATURE_LEDGER.md
@docs/meitu-function-blueprint/FEATURE_MATRIX.md
@QUALITY_SCORE.md
</context>

<tasks>

<task type="auto">
  <name>Task 30-06-01: Record the observed quality snapshot and remaining eye limitations</name>
  <files>QUALITY_SCORE.md</files>
  <read_first>
    - `QUALITY_SCORE.md`
    - `.planning/phases/30-eye-safety-ledger-and-closeout/30-EYE-SAFETY-EVIDENCE.md`
    - `.planning/phases/30-eye-safety-ledger-and-closeout/30-REVIEW.md`
    - `.planning/phases/30-eye-safety-ledger-and-closeout/30-SECURITY.md`
    - `docs/meitu-function-blueprint/SHAPE_FEATURE_LEDGER.md`
    - `docs/meitu-function-blueprint/FEATURE_MATRIX.md`
  </read_first>
  <action>Add or replace one exact `### 3.15 Phase 30 Eye Safety, Ledger, and Closeout Evidence` section using only observed evidence: exact focused test names/results; the observed full-suite count copied from `30-EYE-SAFETY-EVIDENCE.md` rather than predicted and persisted exactly once as `full_suite_tests: {observed integer}`; fixed Phase 29 regression 161/161 and 36/36; public/SPI/import/network/commercial/VIP/public-inventory/generated-artifact results; clean review; zero-open security; exact four-row promotion; branch partial; no Demo build because Demo source was unchanged; and future eye/device/commercial/parity/readiness limitations. Correct stale quality wording that says reused eyes are reduced: eyes now skip on reused/stale while face shape/nose/mouth retain the 0.5 reuse policy. Replace the exact current stale row `| Eyes | 4 | 4 | Eye provider output, caps, reused/stale reduction, and missing-eye landmark skips are tested; Phase 29 adds public-facade renderer evidence with 161/161 outputs and 36/36 comparisons. Phase 30 still owns row and branch closeout. |`; do not append a contradictory update. Explicitly link `30-EYE-SAFETY-EVIDENCE.md` rather than copying its tables per D-15 through D-21. Verify the exact Phase 30 heading through the next heading of equal or higher level so Phase 29 history cannot satisfy the checks.</action>
  <verify>
    <automated>awk '$0 == "### 3.15 Phase 30 Eye Safety, Ledger, and Closeout Evidence" { if (seen) exit 2; seen=1; copy=1 } copy && $0 != "### 3.15 Phase 30 Eye Safety, Ledger, and Closeout Evidence" && /^#{1,3} / { copy=0 } copy { print } END { if (seen != 1) exit 3 }' QUALITY_SCORE.md > /tmp/phase30-quality-section.out || exit $?; for token in 'EYE-04' 'EYE-05' 'EYE-06' 'EYE-07' 'EYE-08' 'DOC-01' 'observed.*test' '161/161' '36/36' 'review.*clean' 'threats_open: 0' '大小' '上下' '眼距' '眼尾上扬' '眼睛.*partial' 'Demo.*not.*required' 'future' 'eyes.*reused.*skip|reused.*eyes.*skip' 'face shape.*0\.5' 'nose.*0\.5' 'mouth.*0\.5'; do rg -ni "$token" /tmp/phase30-quality-section.out || exit 1; done</automated>
    <automated>awk '$0 == "### 3.15 Phase 30 Eye Safety, Ledger, and Closeout Evidence" { if (seen) exit 2; seen=1; copy=1 } copy && $0 != "### 3.15 Phase 30 Eye Safety, Ledger, and Closeout Evidence" && /^#{1,3} / { copy=0 } copy { print } END { if (seen != 1) exit 3 }' QUALITY_SCORE.md > /tmp/phase30-quality-evidence.out || exit $?; rg -n '30-EYE-SAFETY-EVIDENCE\.md' /tmp/phase30-quality-evidence.out</automated>
    <automated>extract_full_suite_count() { awk -F': *' '$1 == "full_suite_tests" { if ($2 ~ /^[0-9]+$/) { count++; value=$2 } } END { if (count != 1) exit 2; print value }' "$1"; }; awk '$0 == "### 3.15 Phase 30 Eye Safety, Ledger, and Closeout Evidence" { if (seen) exit 2; seen=1; copy=1 } copy && $0 != "### 3.15 Phase 30 Eye Safety, Ledger, and Closeout Evidence" && /^#{1,3} / { copy=0 } copy { print } END { if (seen != 1) exit 3 }' QUALITY_SCORE.md > /tmp/phase30-quality-count.out || exit $?; evidence_count=$(extract_full_suite_count .planning/phases/30-eye-safety-ledger-and-closeout/30-EYE-SAFETY-EVIDENCE.md) || exit $?; quality_count=$(extract_full_suite_count /tmp/phase30-quality-count.out) || exit $?; verification_count=$(extract_full_suite_count .planning/phases/30-eye-safety-ledger-and-closeout/30-VERIFICATION.md) || exit $?; validation_count=$(extract_full_suite_count .planning/phases/30-eye-safety-ledger-and-closeout/30-VALIDATION.md) || exit $?; test "$quality_count" = "$evidence_count"; test "$verification_count" = "$evidence_count"; test "$validation_count" = "$evidence_count"</automated>
    <automated>if rg -n -F '| Eyes | 4 | 4 | Eye provider output, caps, reused/stale reduction, and missing-eye landmark skips are tested; Phase 29 adds public-facade renderer evidence with 161/161 outputs and 36/36 comparisons. Phase 30 still owns row and branch closeout. |' QUALITY_SCORE.md > /tmp/phase30-stale-quality.out; then sed -n '1,40p' /tmp/phase30-stale-quality.out; exit 1; else scan_status=$?; case "$scan_status" in 1) : ;; *) exit "$scan_status" ;; esac; fi</automated>
    <automated>git diff --name-only -- QUALITY_SCORE.md | rg -x 'QUALITY_SCORE.md'</automated>
    <automated>awk '$0 == "### 3.15 Phase 30 Eye Safety, Ledger, and Closeout Evidence" { if (seen) exit 2; seen=1; copy=1 } copy && $0 != "### 3.15 Phase 30 Eye Safety, Ledger, and Closeout Evidence" && /^#{1,3} / { copy=0 } copy { print } END { if (seen != 1) exit 3 }' QUALITY_SCORE.md > /tmp/phase30-quality-no-overclaim.out || exit $?; if rg -n '173 tests|commercial quality achieved|production quality|release[- ]ready|shipping ready|device parity passed|matches Meitu|full Meitu parity achieved|whole[- ]branch.*眼睛.*implemented|眼睛.*branch.*implemented' /tmp/phase30-quality-no-overclaim.out > /tmp/phase30-quality-overclaims.out; then sed -n '1,80p' /tmp/phase30-quality-overclaims.out; exit 1; else scan_status=$?; case "$scan_status" in 1) : ;; *) exit "$scan_status" ;; esac; fi</automated>
    <automated>git diff --check -- QUALITY_SCORE.md</automated>
  </verify>
  <acceptance_criteria>
    - `QUALITY_SCORE.md` independently records all six requirements' observed evidence, exact renderer/helper counts, review/security/boundary results, four rows, branch partial, and limitations.
    - The full-suite count is copied from execution evidence and no stale 173-test prediction is added.
    - The file is changed and passes independent no-overclaim/diff-hygiene checks.
  </acceptance_criteria>
  <done>The quality ledger reflects only observed Phase 30 evidence and explicit remaining limitations.</done>
</task>

<task type="auto">
  <name>Task 30-06-02: Record the verified eye slice in the project contract</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - `.planning/PROJECT.md`
    - `QUALITY_SCORE.md`
    - `.planning/phases/30-eye-safety-ledger-and-closeout/30-EYE-SAFETY-EVIDENCE.md`
    - `.planning/phases/30-eye-safety-ledger-and-closeout/30-SECURITY.md`
    - `docs/meitu-function-blueprint/SHAPE_FEATURE_LEDGER.md`
    - `docs/meitu-function-blueprint/FEATURE_MATRIX.md`
  </read_first>
  <action>Under the v1.6 completed area in `.planning/PROJECT.md`, add or replace one exact `#### Phase 30 Eye Safety, Ledger, and Closeout` section that records the verified existing-parameter eye slice: exact public fields and four implemented rows, an explicit `30-EYE-SAFETY-EVIDENCE.md` link, branch partial, and remaining future tools. Preserve SDK-only, no new public fields, no Demo UI, local-first/no network/cloud, no commercial entitlement, no committed generated baseline, and no device/commercial/parity/readiness/whole-branch claims. Do not mark requirements, roadmap, or workflow state here; Plan 30-07 owns that final consistency transaction per D-17 through D-21. Verify this exact h4 section only through the next heading of equal or higher level.</action>
  <verify>
    <automated>awk '$0 == "#### Phase 30 Eye Safety, Ledger, and Closeout" { if (seen) exit 2; seen=1; copy=1 } copy && $0 != "#### Phase 30 Eye Safety, Ledger, and Closeout" && /^#{1,4} / { copy=0 } copy { print } END { if (seen != 1) exit 3 }' .planning/PROJECT.md > /tmp/phase30-project-section.out || exit $?; for token in 'v1\.6' 'eyeSize' 'eyeDistance' 'eyeYPosition' 'eyeTailLift' '大小' '上下' '眼距' '眼尾上扬' '眼睛.*partial' 'future' 'SDK-only' 'public' 'Demo' 'local' 'network' 'commercial' 'device' 'parity' 'readiness'; do rg -ni "$token" /tmp/phase30-project-section.out || exit 1; done</automated>
    <automated>awk '$0 == "#### Phase 30 Eye Safety, Ledger, and Closeout" { if (seen) exit 2; seen=1; copy=1 } copy && $0 != "#### Phase 30 Eye Safety, Ledger, and Closeout" && /^#{1,4} / { copy=0 } copy { print } END { if (seen != 1) exit 3 }' .planning/PROJECT.md > /tmp/phase30-project-evidence.out || exit $?; rg -n '30-EYE-SAFETY-EVIDENCE\.md' /tmp/phase30-project-evidence.out; rg -n '30-SECURITY\.md' /tmp/phase30-project-evidence.out</automated>
    <automated>git diff --name-only -- .planning/PROJECT.md | rg -x '.planning/PROJECT.md'</automated>
    <automated>awk '$0 == "#### Phase 30 Eye Safety, Ledger, and Closeout" { if (seen) exit 2; seen=1; copy=1 } copy && $0 != "#### Phase 30 Eye Safety, Ledger, and Closeout" && /^#{1,4} / { copy=0 } copy { print } END { if (seen != 1) exit 3 }' .planning/PROJECT.md > /tmp/phase30-project-no-overclaim.out || exit $?; if rg -n 'commercial quality achieved|production quality|release[- ]ready|shipping ready|device parity passed|matches Meitu|full Meitu parity achieved|whole[- ]branch.*眼睛.*implemented|眼睛.*branch.*implemented' /tmp/phase30-project-no-overclaim.out > /tmp/phase30-project-overclaims.out; then sed -n '1,80p' /tmp/phase30-project-overclaims.out; exit 1; else scan_status=$?; case "$scan_status" in 1) : ;; *) exit "$scan_status" ;; esac; fi</automated>
    <automated>git diff --check -- .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - Project contract independently names the four fields/rows, evidence links, branch partial, and future gaps.
    - SDK-only, no-public-expansion, no-UI, local-first, no-commercial, generated-artifact, device/parity/readiness boundaries remain explicit.
    - The file is changed and passes independent no-overclaim/diff-hygiene checks.
  </acceptance_criteria>
  <done>The project contract records the verified slice without broadening milestone claims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Detailed command evidence -> global quality snapshot | Only observed facts, exact counts, links, and limitations may be copied. |
| Four-row implementation -> milestone project state | The scoped slice must not imply public expansion, UI, commercial, device, parity, or readiness completion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Task-level mitigation and verification gate |
|-----------|----------|-----------|-------------|---------------------------------------------|
| T-30-01 | Tampering / Denial of Service | Input evidence in quality ledger | mitigate | Task 30-06-01 requires EYE-04 and exact observed evidence link. |
| T-30-02 | Tampering | Degradation policy wording | mitigate | Task 30-06-01 corrects stale reused-eye wording while preserving non-eye 0.5 reuse. |
| T-30-03 | Information Disclosure | Quality/project docs | mitigate | Both tasks use aggregate facts and links only; no raw evidence tables are copied. |
| T-30-04 | Information Disclosure / Elevation of Privilege | Boundary result claims | mitigate | Task 30-06-01 requires EYE-07/security results from verified artifacts. |
| T-30-05 | Elevation of Privilege | Commercial/network scope | mitigate | Task 30-06-02 retains explicit local/no-commercial boundaries. |
| T-30-06 | Information Disclosure / Repudiation | Generated/test evidence | mitigate | Task 30-06-01 records only fixed helper counts and untracked policy; observed full count is not guessed. |
| T-30-07 | Spoofing / Repudiation | False project completion | mitigate | Task 30-06-02 names exactly four rows and keeps branch partial/future. |
| T-30-08 | Spoofing / Repudiation | Quality/readiness overclaim | mitigate | Each file has an independent added-line no-overclaim scan. |
| T-30-SC | Tampering | Package-manager installs | mitigate | Documentation-only tasks use no packages or external services. |
</threat_model>

<verification>
Overall Plan 30-06 checks:

- `QUALITY_SCORE.md` independently carries observed test/count/boundary/security/promotion/limitation facts.
- `.planning/PROJECT.md` independently carries exact slice and preserved scope boundaries.
- Both required files are present in the task diff and pass independent no-overclaim plus diff-hygiene gates.
</verification>

<success_criteria>
Plan 30-06 succeeds when quality and project ledgers independently reflect the observed four-row slice, renderer/security evidence, branch partial status, and all remaining limitations without premature workflow closeout.
</success_criteria>

<output>
Create `.planning/phases/30-eye-safety-ledger-and-closeout/30-06-SUMMARY.md` when done.
</output>
