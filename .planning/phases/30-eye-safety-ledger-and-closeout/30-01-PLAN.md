---
phase: 30-eye-safety-ledger-and-closeout
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift
  - BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
autonomous: true
requirements:
  - EYE-04
user_setup: []
must_haves:
  truths:
    - "`eyeSize` and `eyeTailLift` are positive-only: negative finite input and every non-finite value normalize to zero without requesting geometry or emitting `eye_inputs_missing` per D-01, D-02, and D-06."
    - "`eyeDistance` and `eyeYPosition` preserve both signed directions through public normalization and resolver caps per D-03 and D-04."
    - "Every independent eye cap case resolves to the exact cap and sign, emits `beauty_strength_capped`, and records `beauty.effects.cappedCount == 1`; zero/no-op cases record neither per D-04, D-05, and D-15."
  artifacts:
    - path: "BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift"
      provides: "Positive-only size/tail normalization and signed distance/Y normalization without changing the 31-field public model"
      contains: "self.eyeSize = Self.clampUnit(eyeSize)"
    - path: "BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift"
      provides: "Eye cap families aligned with public semantics"
      contains: "strengths.eyeTailLift = capUnit"
    - path: "BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift"
      provides: "Four-field finite-overflow and NaN/infinity normalization matrix"
      contains: "testEYE04EyeInputsNormalizePositiveOnlySignedOverflowAndNonFiniteValues"
    - path: "BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift"
      provides: "Exact cap/direction/warning/count and positive-only no-op evidence"
      contains: "testEYE04EyeCapsResolveExactValuesDirectionsWarningsAndCounts"
  key_links:
    - from: "BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift"
      to: "BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift"
      via: "`normalized()` feeds `capUnit` for size/tail and `capSigned` for distance/Y"
      pattern: "eye(Size|TailLift).*capUnit|eye(Distance|YPosition).*capSigned"
    - from: "BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift"
      to: "BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift"
      via: "exact expected values 0.45, signed 0.30, signed 0.25, and 0.30"
      pattern: "BeautySafetyCaps\.eye"
---

<objective>
Align the existing four public eye parameters with their locked positive-only or signed semantics and prove exact resolver caps before changing geometry freshness policy.

Purpose: close the input-validation and effective-strength portion of EYE-04 without adding a public field, new visible direction, renderer case, Demo behavior, or package dependency.
Output: focused production changes in `BeautyParameters` and `BeautyEffectResolver`, plus exact normalization and resolver evidence.
</objective>

## Artifacts this phase produces

- Updated `BeautyParameters.init(...)` assignments: `eyeSize` and `eyeTailLift` use existing `clampUnit`; `eyeDistance` and `eyeYPosition` retain existing `clampSigned`.
- Updated `BeautyEffectResolver.resolve(...)` cap assignments: size/tail use existing `capUnit`; distance/Y retain existing `capSigned`.
- XCTest `BeautyParametersTests.testEYE04EyeInputsNormalizePositiveOnlySignedOverflowAndNonFiniteValues`.
- XCTest `BeautyEffectResolverTests.testEYE04EyeCapsResolveExactValuesDirectionsWarningsAndCounts`.
- XCTest `BeautyEffectResolverTests.testEYE04NegativePositiveOnlyEyeInputsAreNoOps`.
- No new public symbol, stored field, warning code, metric key, renderer case, helper script, generated artifact, package, target, or file path is created by this plan.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/30-eye-safety-ledger-and-closeout/30-CONTEXT.md
@.planning/phases/30-eye-safety-ledger-and-closeout/30-RESEARCH.md
@.planning/phases/30-eye-safety-ledger-and-closeout/30-VALIDATION.md
@.planning/phases/30-eye-safety-ledger-and-closeout/30-PATTERNS.md
@BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift
@BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift
@BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
@BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift
@BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
@DESIGN.md
@SECURITY.md
</context>

<tasks>

<task type="auto">
  <name>Task 30-01-01: Lock positive-only, signed, overflow, and non-finite public eye semantics</name>
  <files>BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift, BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift</files>
  <read_first>
    - `BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift`
    - `BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift`
    - `.planning/phases/30-eye-safety-ledger-and-closeout/30-CONTEXT.md`
    - `.planning/phases/30-eye-safety-ledger-and-closeout/30-RESEARCH.md`
    - `DESIGN.md`
  </read_first>
  <action>In `BeautyParameters.init(...)`, change only `eyeSize` and `eyeTailLift` from `Self.clampSigned` to the existing `Self.clampUnit`; keep `eyeDistance` and `eyeYPosition` on `Self.clampSigned`, keep `normalized()` routing through the initializer, and keep the public stored-field inventory at exactly 31. Add `testEYE04EyeInputsNormalizePositiveOnlySignedOverflowAndNonFiniteValues` to `BeautyParametersTests`: assert size/tail `-1 -> 0` and `+2 -> 1`; distance/Y `+2 -> +1` and `-2 -> -1`; and each of the four fields independently maps `Float.nan`, `Float.infinity`, and `-Float.infinity` to zero. Use table-driven local test data if helpful, but name every failing field/case. Do not add an opposite eye-size or tail-lift direction, a new clamp helper, a property wrapper, a public type, a stored field, or a second validation layer per D-01, D-02, D-03, and D-06.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyParametersTests</automated>
    <automated>rg -n "self\.eyeSize = Self\.clampUnit\(eyeSize\)|self\.eyeDistance = Self\.clampSigned\(eyeDistance\)|self\.eyeYPosition = Self\.clampSigned\(eyeYPosition\)|self\.eyeTailLift = Self\.clampUnit\(eyeTailLift\)|testEYE04EyeInputsNormalizePositiveOnlySignedOverflowAndNonFiniteValues" BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift</automated>
    <automated>git diff --check -- BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - `BeautyParameters.swift` contains the exact four clamp assignments required above and no new stored eye field.
    - `testSDK03DefaultsAreZeroEffectAndExpose31StoredFields` still passes with an exact count of 31.
    - The new EYE-04 test mechanically asserts 2 finite-overflow directions for each signed field, positive-only negative/overflow behavior for size/tail, and 12 non-finite field/value combinations.
    - The focused `BeautyParametersTests` command exits 0 and scoped `git diff --check` exits 0.
  </acceptance_criteria>
  <done>Public eye values have unambiguous positive-only or signed semantics and complete abnormal-input coverage.</done>
</task>

<task type="auto">
  <name>Task 30-01-02: Prove exact eye caps, directions, warning, metric, and no-op detection behavior</name>
  <files>BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift</files>
  <read_first>
    - `BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift`
    - `BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift`
    - `BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift`
    - `BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift`
    - `BeautySDK/Sources/BeautyEffects/Warp/EyeWarpProvider.swift`
    - `BeautySDK/Tests/BeautyEffectsTests/BeautySafetyCapsTests.swift`
    - `BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift`
    - `.planning/phases/30-eye-safety-ledger-and-closeout/30-CONTEXT.md`
    - `DESIGN.md`
  </read_first>
  <action>In `BeautyEffectResolver.resolve(...)`, change only the `eyeSize` and `eyeTailLift` cap calls to existing `capUnit`; retain `capSigned` for `eyeDistance` and `eyeYPosition` and retain the constants `0.45`, `0.30`, `0.25`, and `0.30` in `BeautySafetyCaps`. Add `testEYE04EyeCapsResolveExactValuesDirectionsWarningsAndCounts` against fresh complete `FaceGeometry.fixture` with six independent inputs: size `+1 -> +0.45`, distance `+1 -> +0.30`, distance `-1 -> -0.30`, Y `+1 -> +0.25`, Y `-1 -> -0.25`, and tail `+1 -> +0.30`; every case must assert `.eyes` active, exact effective value with `0.0001` accuracy, `beauty_strength_capped`, `beauty.effects.cappedCount == 1`, and absence of `combined_geometry_weakened`. Add `testEYE04NegativePositiveOnlyEyeInputsAreNoOps` for negative size and negative tail: normalized/effective value zero, `requiresFaceGeometry == false`, `.eyes` neither active nor skipped, no `eye_inputs_missing`, no cap warning, and capped count zero. Do not touch freshness/degradation behavior yet; that shared resolver seam belongs to Plan 30-02 per D-04, D-05, and D-15.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.BeautyEffectResolverTests</automated>
    <automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.BeautySafetyCapsTests</automated>
    <automated>swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyParametersTests</automated>
    <automated>rg -n "strengths\.eyeSize = capUnit|strengths\.eyeDistance = capSigned|strengths\.eyeYPosition = capSigned|strengths\.eyeTailLift = capUnit|testEYE04EyeCapsResolveExactValuesDirectionsWarningsAndCounts|testEYE04NegativePositiveOnlyEyeInputsAreNoOps" BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift</automated>
    <automated>git diff --exit-code -- docs/meitu-function-blueprint/SHAPE_FEATURE_LEDGER.md docs/meitu-function-blueprint/FEATURE_MATRIX.md</automated>
    <automated>git diff --check -- BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - Resolver source uses `capUnit` exactly for size/tail and `capSigned` exactly for distance/Y; cap constants are unchanged.
    - Six independent cap/direction cases assert exact effective value, exact cap count 1, stable cap warning, active eye domain, and no combined warning.
    - Two negative positive-only cases assert zero effect, no geometry intent, no skipped eye domain, no missing-eye warning, and cap count 0.
    - Focused normalization, cap-constant, and resolver suites exit 0; the eye ledger and branch matrix remain byte-for-byte unchanged.
  </acceptance_criteria>
  <done>EYE-04 has exact input/cap evidence ready for the later degradation and promotion gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public `BeautyParameters` input -> normalized snapshot | Untrusted finite overflow, wrong-sign values, and NaN/infinity must become bounded product semantics before geometry planning. |
| Normalized snapshot -> resolver effective strengths | Cap family, sign, warning, and aggregate metric must match the public contract exactly. |
| Phase evidence -> status ledgers | Input evidence alone cannot promote eye rows or imply whole-branch/product readiness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Task-level mitigation and verification gate |
|-----------|----------|-----------|-------------|---------------------------------------------|
| T-30-01 | Tampering / Denial of Service | Public eye input and resolver caps | mitigate | Tasks 30-01-01/02 add the 12-case non-finite matrix, wrong-sign/no-op checks, exact six-case cap matrix, and focused XCTest gates. |
| T-30-02 | Tampering | Missing/reused/stale eye geometry | mitigate | Owned by Task 30-02-01; Plan 30-01 does not alter freshness handling and blocks promotion with unchanged-ledger guards. |
| T-30-03 | Information Disclosure | Warnings, metrics, and durable evidence | mitigate | Tasks 30-02-01/02 and 30-03-02 own redaction assertions/scans; Task 30-01-02 asserts negative no-ops do not fabricate `eye_inputs_missing`. |
| T-30-04 | Information Disclosure / Elevation of Privilege | Public/SPI raw geometry and forbidden internal imports | mitigate | Task 30-03-02 owns explicit public/SPI and Demo/renderer import hard gates before promotion. |
| T-30-05 | Elevation of Privilege | Network/cloud or commercial entitlement scope | mitigate | Task 30-03-02 owns API-shaped active-source network and StoreKit/entitlement hard gates; no package or service is added here. |
| T-30-06 | Information Disclosure / Repudiation | Generated renderer/gallery artifacts | mitigate | Task 30-03-01 owns `git ls-files` zero-result and Phase 29 helper gates; Plan 30-01 creates no generated artifacts. |
| T-30-07 | Spoofing / Repudiation | False atomic row promotion | mitigate | Task 30-04-01 owns the evidence-first four-row atomic guard; Task 30-01-02 proves both ledger files remain unchanged. |
| T-30-08 | Spoofing / Repudiation | Whole-branch or product-readiness overclaim | mitigate | Task 30-04-02 owns branch-partial/no-overclaim gates; this plan produces behavior/tests only and no completion docs. |
| T-30-SC | Tampering | Package-manager installs | mitigate | Every task uses existing SwiftPM/XCTest/git/rg only; npm, pip, cargo, external packages, network services, and entitlement SDKs are prohibited. |
</threat_model>

<verification>
Overall Plan 30-01 checks:

- Focused `BeautyParametersTests`, `BeautyEffectResolverTests`, and `BeautySafetyCapsTests` pass.
- Source assertions prove the exact clamp/cap families and named EYE-04 tests exist.
- The public inventory remains exactly 31 stored fields.
- `SHAPE_FEATURE_LEDGER.md` and `FEATURE_MATRIX.md` remain unchanged.
- Scoped `git diff --check` passes.
</verification>

<success_criteria>
Plan 30-01 succeeds when all four eye fields have complete finite/non-finite normalization evidence, the six visible directions resolve to exact caps and signs with exact warning/count evidence, negative size/tail are silent no-ops, and no API/status/scope boundary changes.
</success_criteria>

<output>
Create `.planning/phases/30-eye-safety-ledger-and-closeout/30-01-SUMMARY.md` when done.
</output>
