---
phase: 69-public-concurrency-repair-and-sdk-only-closeout
plan: "02"
type: execute
wave: 2
depends_on: [69-01]
files_modified:
  - scripts/check-sdk-only-boundary.sh
  - scripts/run-no-skip-swiftpm.sh
autonomous: true
requirements: [CLOSE-02]
must_haves:
  truths:
    - "The SDK boundary self-test rejects an unconditional generic BeautyResult Sendable declaration as well as restored UI/Xcode/media/Metal drift."
    - "The mandatory wrapper runs boundary self-tests and live post-archive checks before consumer, generated CPU, optional, and full SwiftPM validation."
  artifacts:
    - path: "scripts/check-sdk-only-boundary.sh"
      provides: "Mutation-tested SDK-only and generic-sendability static boundary"
      contains: "BeautyResult"
    - path: "scripts/run-no-skip-swiftpm.sh"
      provides: "Hardened archive-to-no-skip closeout ordering"
      contains: "no_skip_sdk_boundary_self_tested"
  key_links:
    - from: "scripts/run-no-skip-swiftpm.sh"
      to: "scripts/check-sdk-only-boundary.sh"
      via: "self-test and post-archive preflight calls"
      pattern: "check-sdk-only-boundary.sh"
    - from: "scripts/check-sdk-only-boundary.sh"
      to: "BeautySDK/Sources/BeautyCore/Models/BeautyResult.swift"
      via: "active-tree static conformance scan and temporary mutation"
      pattern: "BeautyResult.*unchecked|BeautyResult"
---

<objective>
Harden the SDK-only static boundary and mandatory wrapper so unconditional
generic sendability or repository scope drift fails before downstream evidence.

Purpose: Closeout must protect both the public concurrency contract and the
archive/UI/Metal/Generated-output boundary through one deterministic, bounded
SwiftPM-owned gate.
Output: Mutation-tested boundary rejection and mandatory wrapper ordering (D-03,
D-04, D-06).
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/69-public-concurrency-repair-and-sdk-only-closeout/69-CONTEXT.md
@.planning/phases/69-public-concurrency-repair-and-sdk-only-closeout/69-VALIDATION.md
@.planning/phases/69-public-concurrency-repair-and-sdk-only-closeout/69-01-SUMMARY.md
@scripts/check-sdk-only-boundary.sh
@scripts/run-no-skip-swiftpm.sh
@scripts/check-cpu-reference-oracles.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unconditional generic sendability rejection to the boundary self-test</name>
  <read_first>scripts/check-sdk-only-boundary.sh; BeautySDK/Sources/BeautyCore/Models/BeautyResult.swift; Phase 66 boundary self-test patterns</read_first>
  <files>scripts/check-sdk-only-boundary.sh</files>
  <action>Per D-03 and D-04, extend the post-archive static scan over active Swift sources to reject any generic `BeautyResult` declaration or extension carrying unconditional `@unchecked Sendable`, while accepting the conditional `Output: Sendable` declaration from Plan 69-01. Extend the temporary self-test fixture with a valid conditional result declaration, run the post-archive validator, replace it with the old unconditional form, and assert `expect_failure`, then restore/remove the fixture before exit. Preserve the existing taxonomy, retired-root, Xcode/SwiftUI/UI-test, symlink, tracked-media, retained-shader digest, GPU/backend, and bounded temporary-tree checks. Keep failure output short and path-free where the caller suppresses diagnostics.</action>
  <verify>
    <automated>bash scripts/check-sdk-only-boundary.sh --self-test</automated>
    <automated>bash scripts/check-sdk-only-boundary.sh --post-archive</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>Boundary self-test passes for the conditional source and fails for an unconditional generic `BeautyResult` `@unchecked Sendable` mutation; live post-archive scanning remains green and all prior scope fences remain intact.</done>
</task>

<task type="auto">
  <name>Task 2: Bind boundary self-tests into the archive-first no-skip wrapper</name>
  <read_first>scripts/run-no-skip-swiftpm.sh; scripts/check-sdk-only-boundary.sh; scripts/check-cpu-reference-oracles.sh; RELIABILITY.md</read_first>
  <files>scripts/run-no-skip-swiftpm.sh</files>
  <action>Per D-04 and D-06, invoke `check-sdk-only-boundary.sh --self-test` as a bounded fail-closed preflight before archive verification and live post-archive scanning, emit a fixed aggregate marker, then preserve the existing archive → post-archive boundary → public consumer → generated CPU oracle → private opt-ins → one-child SwiftPM ordering. Keep the eight expected opt-in identities, bounded transcript capture, zero-failure/zero-skip/nonzero execution accounting, cleanup trap, and path-free aggregate messages unchanged. Any static mutation failure must stop before consumer or SwiftPM execution.</action>
  <verify>
    <automated>bash scripts/run-no-skip-swiftpm.sh --self-test</automated>
    <automated>bash scripts/run-no-skip-swiftpm.sh</automated>
    <automated>rg -n 'no_skip_sdk_boundary_self_tested|check-sdk-only-boundary.sh --self-test|check-cpu-reference-oracles.sh' scripts/run-no-skip-swiftpm.sh</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>The mandatory wrapper reports the boundary self-test and then executes every existing SDK-only preflight and full child with zero failures/skips and a positive denominator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Active repository → static scanner | Source, docs, archives, and generated artifacts are classified before tests run. |
| Static preflight → child SwiftPM process | A clean static contract authorizes downstream test execution. |
| Child transcript → aggregate marker | Untrusted output is bounded and reduced to counts/status only. |

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-69-02-01 | Tampering | Boundary mutation/static scan | mitigate | Scan active Swift declarations and prove the old unconditional generic conformance is rejected by a temporary mutation. |
| T-69-02-02 | Denial of Service | No-skip child process | mitigate | Preserve bounded transcript bytes/lines, exact parser accounting, cleanup trap, and fail-closed preflight ordering. |
| T-69-02-03 | Information Disclosure | Gate output | mitigate | Emit only fixed markers/counts; suppress paths and raw child transcript. |
| T-69-02-SC | Tampering | SwiftPM dependencies | accept | No dependency install or network integration occurs. |
</threat_model>

<verification>
Run the boundary self-test, live post-archive scanner, consumer, generated CPU
preflight, and complete no-skip wrapper. Confirm the self-test marker precedes
the archive/boundary/consumer/CPU/child markers and run `git diff --check`.
</verification>

<success_criteria>
- Unconditional generic result sendability and all existing SDK-only drift classes fail closed.
- Boundary self-test is mandatory before all downstream no-skip work.
- Complete gate remains bounded, cleanup-safe, path-free, zero-failure, zero-skip, and positive-test.
</success_criteria>

<output>
Create `.planning/phases/69-public-concurrency-repair-and-sdk-only-closeout/69-02-SUMMARY.md` when done.
</output>
