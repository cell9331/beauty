---
phase: 38-public-contract-and-lip-support-geometry
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift
  - BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift
  - BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift
  - BeautySDK/Tests/BeautyResourcesTests/BeautyResourceCatalogTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautySafetyCapsTests.swift
autonomous: true
requirements:
  - MOUTH-01
  - MOUTH-02
  - MOUTH-03
must_haves:
  truths:
    - "BeautyParameters has exactly 38 stored fields: 37 numeric fields plus filterId; the five new mouth values are independent stored Float values, not aliases."
    - "mouthYPosition, mouthTilt, and mouthXPosition are signed -1...1; lipPeakDefinition and lipPlump are positive-only 0...1; all default to 0 and map every non-finite value to 0."
    - "A complete legacy 33-field payload and every bundled preset decode all five new fields as 0, while unequal nonzero 38-field values round-trip and existing labeled initializer calls remain source-compatible through defaults."
    - "BeautyEffectiveStrengths owns five distinct values and BeautySafetyCaps exposes provisional 0.25 symbols without claiming final Phase 40 calibration."
  artifacts:
    - path: "BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift"
      provides: "Five public stored values threaded through all six manual model seams"
      contains: "mouthYPosition"
    - path: "BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift"
      provides: "Five distinct effective-strength fields"
      contains: "lipPlump"
    - path: "BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift"
      provides: "Five provisional 0.25 cap constants"
      contains: "lipPeakDefinition"
    - path: "BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift"
      provides: "Exact inventory, normalization, compatibility, round-trip, independence, and Sendable evidence"
    - path: "BeautySDK/Tests/BeautyResourcesTests/BeautyResourceCatalogTests.swift"
      provides: "Bundled-preset missing-key neutrality evidence"
  key_links:
    - from: "BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift"
      to: "BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift"
      via: "all six public-model enumeration seams are locked by exact field/Codable tests"
      pattern: "mouthYPosition|mouthTilt|mouthXPosition|lipPeakDefinition|lipPlump"
    - from: "BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift"
      to: "BeautySDK/Tests/BeautyResourcesTests/BeautyResourceCatalogTests.swift"
      via: "decodeFloatIfPresent maps keys absent from unchanged bundled presets to zero"
      pattern: "builtInPresets"
    - from: "BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift"
      to: "BeautySDK/Tests/BeautyEffectsTests/BeautySafetyCapsTests.swift"
      via: "five provisional constants are asserted as exactly 0.25"
      pattern: "0.25"
---

<objective>
Freeze the compatibility-safe public and effective-value foundation for all five remaining mouth geometry controls before support/provider/resolver code consumes them.

Purpose: establish independent storage, exact normalization, source-rebuild/JSON/preset compatibility, and provisional internal cap symbols.
Output: an exact 38-stored-field public model, five effective-strength fields, five provisional caps, and focused compatibility evidence.
</objective>

## Artifacts this phase produces

- Public stored properties `mouthYPosition`, `mouthTilt`, `mouthXPosition`, `lipPeakDefinition`, and `lipPlump` in all six `BeautyParameters` seams.
- Five corresponding `BeautyEffectiveStrengths` properties.
- Five internal `BeautySafetyCaps` constants, each provisionally `0.25`.
- Requirement-named Phase-38 XCTest coverage for exact inventory, normalization, independence, compatibility, and bundled-preset neutrality.
- No preset JSON, provider, resolver, detector, renderer, Demo, ledger, or generated-artifact change.

## Atomic Commit Intent

- Task `38-01-01`: one atomic commit containing only the public-model implementation and its direct contract tests.
- Task `38-01-02`: one atomic commit containing only effective storage/caps and resource/cap tests.
- Never stage unrelated dirty-worktree files; each commit is made only after its focused automated checks pass.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-public-contract-and-lip-support-geometry/38-CONTEXT.md
@.planning/phases/38-public-contract-and-lip-support-geometry/38-RESEARCH.md
@.planning/phases/38-public-contract-and-lip-support-geometry/38-PATTERNS.md
@.planning/phases/38-public-contract-and-lip-support-geometry/38-VALIDATION.md
@BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift
@BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift
@BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift
@BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift
@BeautySDK/Tests/BeautyResourcesTests/BeautyResourceCatalogTests.swift
@BeautySDK/Tests/BeautyEffectsTests/BeautySafetyCapsTests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 38-01-01: Add five independent public fields through every BeautyParameters seam</name>
  <files>BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift, BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift</files>
  <read_first>
    - `AGENTS.md` and `PLANS.md` — repository workflow and active scope
    - `.planning/REQUIREMENTS.md` — exact MOUTH-01, MOUTH-02, and MOUTH-03 contracts
    - `38-CONTEXT.md` — locked names, signed/positive-only ranges, inventory, and unchanged-preset decision
    - `38-RESEARCH.md` — six model seams and compatibility hazards
    - `38-PATTERNS.md` — exact adjacent-field implementation/test patterns
    - `BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift` — authoritative model implementation
    - `BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift` — authoritative public contract suite
  </read_first>
  <action>Add `public var mouthYPosition`, `mouthTilt`, `mouthXPosition`, `lipPeakDefinition`, and `lipPlump` immediately after `smile` and before `lipColor`. Add the same names/order to `CodingKeys`; add defaulted labeled initializer arguments; assign the three whole-mouth transforms through `Self.clampSigned` and the two local lip controls through `Self.clampUnit`; decode all five through `decodeFloatIfPresent`; and forward all five from `normalized()`. Do not add computed aliases, custom encoding, a second clamp implementation, or reuse any shipped mouth/lip storage.

Extend `BeautyParametersTests` with Phase-38-prefixed names. Rename/extend the exact default inventory test to assert `38` children and all five zero defaults. Table-drive each signed field through `-2 -> -1`, `2 -> 1`, distinct in-range values, `.nan -> 0`, `.infinity -> 0`, and `-.infinity -> 0`; table-drive each positive-only field through negative -> `0`, overflow -> `1`, distinct in-range values, and all three non-finite values -> `0`. Mutate all five vars after construction, call `normalized()`, and prove the returned copy re-applies the rules without mutating the source. Prove independent storage/equality with unequal simultaneous values and retain the compile-time `Sendable` assertion.

Add a literal complete v1.9 JSON object containing exactly 33 current stored keys, including `noseRootNarrowing` and `noseTipLift` but no new mouth keys; assert its key count, decode it, and assert all five new values are zero. Encode a model with unequal nonzero new values, assert the object has exactly 38 keys, decode it, and assert exact equality/non-aliasing. Keep at least one existing source-style labeled initializer call that omits the five new arguments and proceeds to `lipColor`, `filterId`, or `filterIntensity`. After focused checks, create the task's atomic commit with only these two files.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter BeautyParametersTests</automated>
    <automated>test "$(rg -n '^    public var ' BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift | wc -l | tr -d ' ')" -eq 38 &amp;&amp; for field in mouthYPosition mouthTilt mouthXPosition lipPeakDefinition lipPlump; do rg -n "public var $field: Float|case $field|$field: Float = 0|decodeFloatIfPresent\(\.$field\)|$field: $field" BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift || exit 1; done</automated>
    <automated>git diff --check -- BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - The public model is asserted as exactly 38 stored fields = 37 numeric plus `filterId`.
    - Each new field appears once in every manual model seam and remains independent in storage, equality, and Codable behavior.
    - Signed versus positive-only finite ranges, non-finite zeroing, mutable normalization, and `Sendable` evidence pass.
    - A literal exact 33-key legacy payload decodes all five fields to zero; an unequal 38-key model round-trips exactly.
    - Existing labeled source-style initialization compiles with all five values neutral.
  </acceptance_criteria>
  <done>MOUTH-01 through MOUTH-03 have a complete public-model implementation and direct compatibility evidence.</done>
</task>

<task type="auto">
  <name>Task 38-01-02: Add effective storage, provisional caps, and bundled-preset neutrality</name>
  <files>BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift, BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift, BeautySDK/Tests/BeautyEffectsTests/BeautySafetyCapsTests.swift, BeautySDK/Tests/BeautyResourcesTests/BeautyResourceCatalogTests.swift</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — MOUTH-03 compatibility and later MOUTH-08 propagation boundary
    - `38-CONTEXT.md` — provisional `0.25` decision and Phase 40 final-cap boundary
    - `38-RESEARCH.md` and `38-PATTERNS.md` — effective-storage/cap/preset patterns
    - `BeautyEffectPlan.swift` and `BeautySafetyCaps.swift` — modified owners
    - `BeautySafetyCapsTests.swift` and `BeautyResourceCatalogTests.swift` — modified test owners
    - `BeautySDK/Sources/BeautyResources/Resources/Presets/` — read-only compatibility payload source; do not edit JSON
  </read_first>
  <action>Add all five fields to `BeautyEffectiveStrengths` beside the existing mouth geometry fields, defaulting to zero. Add five `BeautySafetyCaps` constants, each exactly `0.25`, while describing them in tests as provisional Phase 38 caps rather than final Phase 40 calibration. Extend `BeautySafetyCapsTests` with an exact five-value assertion.

Extend `BeautyResourceCatalogTests` to load the established five built-in presets, preserve their exact IDs/count, and assert every preset decodes all five new fields as zero, including preset ID in each failure. Do not edit preset JSON or resources. Run focused tests and a scoped diff showing preset payloads unchanged, then create one atomic commit containing only the four listed files.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter BeautySafetyCapsTests &amp;&amp; swift test --package-path BeautySDK --filter BeautyResourceCatalogTests</automated>
    <automated>for field in mouthYPosition mouthTilt mouthXPosition lipPeakDefinition lipPlump; do rg -n "public var $field: Float = 0" BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift || exit 1; rg -n "static let $field: Float = 0.25" BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift || exit 1; done</automated>
    <automated>test -z "$(git diff --name-only -- BeautySDK/Sources/BeautyResources/Resources/Presets)" &amp;&amp; git diff --check -- BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift BeautySDK/Tests/BeautyEffectsTests/BeautySafetyCapsTests.swift BeautySDK/Tests/BeautyResourcesTests/BeautyResourceCatalogTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - Five distinct effective fields default to zero and five provisional caps equal exactly `0.25`.
    - Tests do not describe the provisional constants as final artistic calibration.
    - All five bundled presets decode all five new values as zero through missing-key behavior.
    - No preset/resource payload is edited.
  </acceptance_criteria>
  <done>The effective symbols and compatibility-safe preset/cap foundation required by later plans exist without premature final-cap claims.</done>
</task>

</tasks>

<threat_model>
  <scope>ASVS Level 1 applies to public scalar validation, JSON/preset decoding, source compatibility, effective-value storage, and exact inventory evidence.</scope>
  <blocking_rule>Any unresolved high-severity compatibility, non-finite handling, aliasing, or payload-trust issue blocks plan completion and its summary.</blocking_rule>
  <assets>Public API compatibility, persisted parameter integrity, independent control semantics, safe finite values, preset neutrality, and evidence accuracy.</assets>
  <trust_boundaries>Host/JSON/preset values cross into public mutable storage and then into internal effective-strength/cap symbols.</trust_boundaries>
  <threats>
    - A field omitted from one manual model seam can silently decode, normalize, or round-trip incorrectly.
    - Non-finite or out-of-range input can escape normalization after mutable assignment.
    - New fields can alias shipped storage or break labeled initializer source compatibility.
    - Editing preset JSON can conceal missing-key compatibility failure or introduce nonzero behavior.
    - Exact inventory or final-cap claims can be overstated without observed evidence.
  </threats>
  <mitigations>Exact six-seam scans; literal 33-key and encoded 38-key assertions; mutation normalization; independent equality values; unchanged-preset diff gate; exact cap tests with provisional wording; focused nonzero test counts.</mitigations>
  <residual_risk>Provider geometry, resolver propagation, visible output, final caps, exhaustive safety, and promotion remain Plans 38-02 through 38-04 or Phases 39/40.</residual_risk>
</threat_model>

<verification>
Run both focused public/resource/cap suites, exact field-count and six-seam scans, unchanged-preset checks, and scoped `git diff --check`. Plan completion requires two atomic task commits with no unrelated files.
</verification>

<success_criteria>
Plan 38-01 succeeds when the public model is exactly 38 fields, all five values have correct independent ranges/defaults/non-finite behavior, legacy JSON/source/presets stay neutral, new values round-trip, effective fields/caps exist provisionally, focused tests pass with nonzero counts, and no preset/provider/resolver/output/promotion scope drifts.
</success_criteria>

<output>
Create `.planning/phases/38-public-contract-and-lip-support-geometry/38-01-SUMMARY.md` when done, recording observed focused counts and both atomic commit hashes.
</output>
