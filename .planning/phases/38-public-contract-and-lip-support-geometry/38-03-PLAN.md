---
phase: 38-public-contract-and-lip-support-geometry
plan: "03"
type: execute
wave: 2
depends_on:
  - "38-01"
  - "38-02"
files_modified:
  - BeautySDK/Sources/BeautyEffects/Warp/MouthWarpProvider.swift
  - BeautySDK/Tests/BeautyEffectsTests/MouthWarpProviderTests.swift
autonomous: true
requirements:
  - MOUTH-05
  - MOUTH-06
  - MOUTH-07
  - MOUTH-08
must_haves:
  truths:
    - "MouthWarpFieldEmissions owns eight distinct arrays and sanitizes each requested field to zero when its own final emission is empty."
    - "mouthYPosition and mouthXPosition translate validated whole-mouth sources uniformly on one axis, and mouthTilt rotates noncentral sources around one stable center; both signs remain distinct."
    - "lipPeakDefinition consumes validated upper+inner support for local cupid-bow shaping and cannot borrow smile, size, or whole-mouth vectors."
    - "lipPlump consumes validated upper+lower+inner support and moves both surfaces away from the inner opening; it cannot borrow lipColor, mouthSize, or peak behavior."
    - "Missing, insufficient, duplicate-only, non-finite, out-of-bounds, degenerate, displacement-empty, or otherwise malformed support returns no emission only for dependent fields while valid siblings remain eligible."
    - "Every emitted point is finite, normalized, bounded, deterministic, nonzero/renderable, capped, and created only after validation."
  artifacts:
    - path: "BeautySDK/Sources/BeautyEffects/Warp/MouthWarpProvider.swift"
      provides: "Eight-field provider-owned eligibility, strict support validators, and five independent geometry helpers"
      contains: "struct MouthWarpFieldEmissions"
    - path: "BeautySDK/Tests/BeautyEffectsTests/MouthWarpProviderTests.swift"
      provides: "Direct source/vector/non-alias/fail-closed evidence for all eight geometry fields"
      contains: "Phase38"
  key_links:
    - from: "BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift"
      to: "BeautySDK/Sources/BeautyEffects/Warp/MouthWarpProvider.swift"
      via: "default-empty outer/upper/lower/inner arrays are consumed only through field-specific validators"
      pattern: "outerLips|upperLips|lowerLips|innerLips"
    - from: "BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift"
      to: "BeautySDK/Sources/BeautyEffects/Warp/MouthWarpProvider.swift"
      via: "five effective strengths select five independent emissions"
      pattern: "mouthYPosition|mouthTilt|mouthXPosition|lipPeakDefinition|lipPlump"
    - from: "BeautySDK/Sources/BeautyEffects/Warp/MouthWarpProvider.swift"
      to: "BeautySDK/Tests/BeautyEffectsTests/MouthWarpProviderTests.swift"
      via: "complete emissions/source/target/displacement arrays prove direct geometry semantics"
      pattern: "fieldEmissions"
---

<objective>
Implement and directly prove eight independently eligible mouth geometry emissions, including three signed whole-mouth transforms and two local lip-shape controls.

Purpose: make provider output the authoritative per-field eligibility contract before resolver/conflict/facade integration.
Output: strict support validation, five new bounded helper paths, expanded emissions/sanitization, preserved shipped valid output, and complete provider tests.
</objective>

## Artifacts this phase produces

- Eight-array `MouthWarpFieldEmissions`, aggregate point ordering, and per-field `sanitizing(_:)`.
- Requested-work-driven provider skip behavior without a global outer-center guard masking independent supports.
- Strict whole/upper/lower/inner support validators using finite, bounds, distinctness, cardinality, degeneracy, and displacement checks before clamp.
- Signed vertical translation, center rotation, and horizontal translation helpers.
- Positive-only upper-local peak-definition and upper/lower plump helpers.
- Direct provider tests for source subsets, axes/directions, signed opposites, rotation, local semantics, non-aliasing, deterministic ordering, bounds, nonzero renderability, malformed supports, and sibling continuation.
- No resolver, conflict, facade, renderer, Demo, documentation, cap-finalization, or promotion edit.

## Atomic Commit Intent

- Task `38-03-01` is one atomic commit containing the provider implementation and its direct test suite only.
- The commit is created after focused provider tests and scoped structural/diff checks pass; unrelated worktree files are never staged.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-public-contract-and-lip-support-geometry/38-CONTEXT.md
@.planning/phases/38-public-contract-and-lip-support-geometry/38-RESEARCH.md
@.planning/phases/38-public-contract-and-lip-support-geometry/38-PATTERNS.md
@.planning/phases/38-public-contract-and-lip-support-geometry/38-VALIDATION.md
@.planning/phases/38-public-contract-and-lip-support-geometry/38-01-SUMMARY.md
@.planning/phases/38-public-contract-and-lip-support-geometry/38-02-SUMMARY.md
@BeautySDK/Sources/BeautyEffects/Warp/MouthWarpProvider.swift
@BeautySDK/Sources/BeautyEffects/Warp/NoseWarpProvider.swift
@BeautySDK/Sources/BeautyEffects/Warp/LandmarkGeometryHelper.swift
@BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift
@BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift
@BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift
@BeautySDK/Tests/BeautyEffectsTests/MouthWarpProviderTests.swift
@BeautySDK/Tests/BeautyEffectsTests/NoseWarpProviderTests.swift
@BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 38-03-01: Implement strict eight-field mouth emissions and independent geometry</name>
  <files>BeautySDK/Sources/BeautyEffects/Warp/MouthWarpProvider.swift, BeautySDK/Tests/BeautyEffectsTests/MouthWarpProviderTests.swift</files>
  <read_first>
    - `AGENTS.md`, `PLANS.md`, and `.planning/REQUIREMENTS.md` — workflow and MOUTH-05 through MOUTH-08 contracts
    - `38-CONTEXT.md` — locked source/support/vector/cap/fail-closed decisions
    - `38-RESEARCH.md` — support validators, geometry invariants, and provider hazards
    - `38-PATTERNS.md` — exact current provider/emission/nose-validation analogs
    - `38-01-SUMMARY.md` and `38-02-SUMMARY.md` — prerequisite public/effective/support symbols and observed facts
    - `MouthWarpProvider.swift` — modified provider and shipped-output owner
    - `NoseWarpProvider.swift` — read-only strict support/emission analog
    - `LandmarkGeometryHelper.swift` and `WarpControlPoint.swift` — read-only math/support contracts
    - `MouthWarpProviderTests.swift`, `NoseWarpProviderTests.swift`, and shared fixtures — test/vector patterns
  </read_first>
  <action>Expand `MouthWarpFieldEmissions` from three to eight arrays in canonical order: shipped `mouthSize`, `mouthWidth`, `smile`, then `mouthYPosition`, `mouthTilt`, `mouthXPosition`, `lipPeakDefinition`, `lipPlump`. Enumerate all eight in `points` and `sanitizing(_:)`; a requested field with an empty emission becomes exact zero without affecting any sibling. Preserve deterministic aggregate ordering.

Refactor `makeControlPoints` so it obtains `fieldEmissions` first and derives `mouth_inputs_missing` only when any of the eight geometry fields was requested and aggregate points are empty. Remove/refine the global outer-center early return so each helper can evaluate its actual prerequisites. Preserve valid shipped `mouthSize`, `mouthWidth`, and `smile` output arrays exactly for `.fixture`; harden their support validation only as needed for malformed/non-finite/duplicate inputs.

Add private validators following `NoseWarpProvider`: support points and face bounds must be finite; points must be normalized and within face bounds; required arrays must have sufficient distinct/nondegenerate structure and deterministic order. Validate centers/references, coefficient math, displacement, targets, radius, strength, and falloff before `LandmarkGeometryHelper.clamp`. Reject zero/displacement-ineligible output rather than relying on later `RenderableWarpPoint` filtering.

Implement five independent helpers with conservative coefficients/radii:

- `mouthYPosition`: validated whole `outerLips`; every target X equals source X, every nonzero Y delta has one uniform signed direction/magnitude, and opposite input reverses it.
- `mouthXPosition`: validated whole `outerLips`; every target Y equals source Y, every nonzero X delta has one uniform signed direction/magnitude, and opposite input reverses it. It must differ from `mouthWidth`, whose corners move in opposite directions.
- `mouthTilt`: validated whole support and stable center; rotate noncentral sources with bounded trigonometric motion. Lock a documented sign convention for the repository's top-to-bottom image-normalized coordinates, preserve radius within tolerance, omit zero-displacement center sources, and prove opposite signs produce opposite tangential vectors.
- `lipPeakDefinition`: valid explicit `upperLips` plus `innerLips`; shape a local cupid-bow subset with symmetric flanking-peak/center relationship, excluding corners and avoiding `smile`, radial size, and whole-transform vectors.
- `lipPlump`: valid explicit `upperLips`, `lowerLips`, and `innerLips`; derive a finite nondegenerate inner-opening reference and move both upper and lower surfaces away from the opening. Require output from both surfaces and avoid `lipColor`, radial size, and peak substitution.

Use exact provisional cap symbols only for scaling; do not call them final. All output must be finite, normalized, face-bounded where required, deterministic, nonzero, strength-capped, and radius-bounded. Missing/insufficient/duplicate-only/non-finite/out-of-bounds/degenerate support or nonrenderable displacement returns only that field's empty array.

Extend `MouthWarpProviderTests` with Phase-38-prefixed direct evidence. Lock valid shipped output arrays before/after refactor. For each new signed field, test both directions, source order, exact axis/tangential invariants, sign reversal, determinism, bounds, cap, and nonzero displacement. For peak/plump, test required subsets, upper-local versus upper+lower behavior, symmetry/direction, and complete point/source/target/displacement non-alias comparisons against nearest shipped/new controls. Add a table/matrix for missing, one-point/insufficient, duplicate-only, non-finite, out-of-bounds, degenerate-center/reference, and tiny/final-scale-like strengths. Test isolated failures and mixed valid-sibling requests through `fieldEmissions` and `sanitizing(_:)` so aggregate nonempty output cannot mask a failed field. Assert aggregate skip reason only when all requested emissions are empty and no raw support data appears in it.

Run the focused provider suite twice if needed to confirm determinism, structural scans, and scoped diff checks. Create one atomic commit containing only the provider and its test file.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter MouthWarpProviderTests</automated>
    <automated>for field in mouthSize mouthWidth smile mouthYPosition mouthTilt mouthXPosition lipPeakDefinition lipPlump; do test "$(rg -o "let $field: \[WarpControlPoint\]" BeautySDK/Sources/BeautyEffects/Warp/MouthWarpProvider.swift | wc -l | tr -d ' ')" -eq 1 || exit 1; rg -n "strengths\.$field.*isEmpty|sanitized\.$field = 0" BeautySDK/Sources/BeautyEffects/Warp/MouthWarpProvider.swift || exit 1; done</automated>
    <automated>for field in mouthYPosition mouthTilt mouthXPosition lipPeakDefinition lipPlump; do rg -n "$field" BeautySDK/Sources/BeautyEffects/Warp/MouthWarpProvider.swift BeautySDK/Tests/BeautyEffectsTests/MouthWarpProviderTests.swift || exit 1; done</automated>
    <automated>if git diff --unified=0 -- BeautySDK/Sources/BeautyEffects/Warp/MouthWarpProvider.swift | rg '^\+.*(print\(|NSLog|support.*\[|coordinate|SIMD.*description)' &gt; /tmp/phase38-provider-leaks.out; then cat /tmp/phase38-provider-leaks.out; exit 1; else status=$?; test "$status" -eq 1; fi</automated>
    <automated>git diff --check -- BeautySDK/Sources/BeautyEffects/Warp/MouthWarpProvider.swift BeautySDK/Tests/BeautyEffectsTests/MouthWarpProviderTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - Eight emission arrays and eight sanitization branches exist exactly once and aggregate in deterministic order.
    - The provider does not globally require inner support and reports `mouth_inputs_missing` only for requested aggregate-empty work.
    - Y/X translations and tilt preserve both signed directions with exact axis/rotation invariants and complete-array non-alias evidence.
    - Peak consumes upper+inner local support; plump consumes upper+lower+inner and emits both surfaces; neither borrows shipped behavior.
    - Every malformed/support-empty/displacement-empty case removes only dependent work while valid siblings remain present and sanitization zeros the failed effective field.
    - Valid shipped mouth emissions remain unchanged and all emitted points satisfy finite/bounds/determinism/cap/nonzero checks.
  </acceptance_criteria>
  <done>MOUTH-05 through MOUTH-08 have direct provider-owned implementation and vector/fail-closed evidence ready for resolver integration.</done>
</task>

</tasks>

<threat_model>
  <scope>ASVS Level 1 applies to untrusted/malformed package support input, floating-point geometry math, per-field eligibility, diagnostic skip reasons, and provider output consumed by rendering.</scope>
  <blocking_rule>Any unresolved high-severity raw-support leak, non-finite output, alias/fallback, sibling-coupling, or shipped-output regression blocks completion.</blocking_rule>
  <assets>Geometry correctness, sign semantics, support integrity, provider/effective agreement, render safety, shipped mouth behavior, and biometric-adjacent privacy.</assets>
  <trust_boundaries>Public effective scalar values and package-internal support arrays cross into vector math; field emissions cross into resolver conflict accounting and unified warp dispatch.</trust_boundaries>
  <threats>
    - A global guard or aggregate result can disable valid siblings or hide a failed requested field.
    - Malformed/non-finite/duplicate/degenerate support can create invalid math that clamp conceals.
    - X/Y/tilt signs can collapse or alias width/smile behavior.
    - Peak/plump can borrow shipped semantics and create false product evidence.
    - Tiny/final-scale-empty work can remain nonzero in effective strengths despite no renderable points.
    - Refactoring support validation can change valid shipped output or leak raw coordinates in diagnostics.
  </threats>
  <mitigations>Eight explicit emission arrays; strict pre-clamp validators; isolated and mixed-sibling matrices; complete vector/source/target comparisons; signed opposite tests; direct sanitization assertions; exact shipped-output regression; fixed category skip reason; scoped leak/diff scans.</mitigations>
  <residual_risk>Facade-visible output naturalness and ROI distinction remain Phase 39; exhaustive all-eight freshness/transitions and final caps remain Phase 40.</residual_risk>
</threat_model>

<verification>
Run the nonzero focused provider suite, eight-field structural/sanitization scans, raw diagnostic leak scan, exact valid shipped-output assertions, determinism checks, and scoped `git diff --check`. The single task commit must contain only provider implementation/tests.
</verification>

<success_criteria>
Plan 38-03 succeeds when all eight geometry fields have provider-owned independent emissions, every new vector satisfies its signed/local contract, malformed or nonrenderable support fails only dependent fields, final sanitization can zero empty work, valid shipped outputs remain stable, focused tests pass, ASVS L1 has no high finding, and Phase 39/40 scope remains untouched.
</success_criteria>

<output>
Create `.planning/phases/38-public-contract-and-lip-support-geometry/38-03-SUMMARY.md` when done, recording the chosen private sign/ordering conventions, observed provider count, and atomic commit hash.
</output>
