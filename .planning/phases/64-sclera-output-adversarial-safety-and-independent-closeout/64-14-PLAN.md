---
phase: 64-sclera-output-adversarial-safety-and-independent-closeout
plan: "14"
type: execute
wave: 14
depends_on: [64-13]
gap_closure: true
security_standard: OWASP ASVS Level 1
block_on: HIGH
files_modified:
  - .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-no-skip-swiftpm-runner.js
  - .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/check_phase64_sclera_closeout.py
  - .planning/phases/59-teeth-evidence-and-admission-contract/59-private-evidence-runner.js
autonomous: true
requirements: [SCLERA-14, SCLERA-15, SCLERA-16, SCLERA-17, SCLERA-18, OUT-05]
must_haves:
  truths:
    - "One bounded runner executes the complete unfiltered BeautySDK SwiftPM suite exactly once with all six Apple Vision tests and both rights-gated local-evidence tests opted in."
    - "The runner discovers the ignored teeth and sclera bundles through the audited Phase 59/62 discovery functions, passes locators only in one child environment, and exposes no locator in arguments, stdout, stderr, or durable artifacts."
    - "The runner accepts only one unambiguous `All tests` XCTest summary with a nonzero executed count, zero failures, zero skips, and all eight exact opt-in test methods observed once as passed."
    - "The checker self-test accepts a source-matching current-review fixture, rejects changed-tree/index/worktree/manifest mutations, and no longer contains the stale Plan 64-08 deliberate RED branch."
    - "The checker inventory is exactly nineteen plans and thirty-four ordered task IDs; the immutable Plan 64-12 candidate remains historical and is not required to validate the post-repair inventory."
    - "The checker owns a pre-write candidate guard and live candidate validator that reject malformed manifests, stale authority, owner drift, canonical drift, extra repository writes, skipped opt-ins, or any HIGH threat before Plan 19 can consume a candidate."
    - "Plan-structure validation is bounded to new Plans 64-14 through 64-19; immutable executed Plans 64-01 through 64-13 are grandfathered only for the newer document schema while remaining subject to exact ordered wave, dependency, task-ID, summary, requirement, evidence, and authority checks."
    - "SCLERA-14 adjacency/empty/ordering, SCLERA-15 boundary/precision, SCLERA-16 boundary/precision, SCLERA-17 unclassified, SCLERA-18 adjacency/empty/ordering, and OUT-05 adjacency/empty/ordering/concurrency are executable checker/runner predicates."
  prohibitions:
    - statement: "Private bundle locators, media, digests, rights details, reviewer identity, raw child output, support, masks, geometry, coordinates, pixels, or vein-like detail must not leave the request-local runner process."
      status: unverified
      verification: flagged
    - statement: "A failed, skipped, zero-count, malformed, ambiguous, locator-leaking, stale-review, or HIGH result must not become a green aggregate or promotion authority."
      status: unverified
      verification: flagged
    - statement: "The immutable Plan 64-12 candidate must not be edited, deleted, overwritten, or reinterpreted as post-repair authority."
      status: unverified
      verification: flagged
    - statement: "The historical-plan schema grandfather must not waive or omit any requirement, task ID, summary, evidence gate, serial dependency, or authority predicate."
      status: unverified
      verification: flagged
    - statement: "The repair must not alter production implementation, public API, Demo source, realtime/pixel-buffer behavior, models, network behavior, dependencies, DeviceRGB/named-sRGB, or `去脂`."
      status: unverified
      verification: flagged
  artifacts:
    - path: .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-no-skip-swiftpm-runner.js
      provides: "Request-local no-skip full SwiftPM execution and allowlisted aggregate parser"
      contains: "phase64-no-skip-swiftpm-v1"
    - path: .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/check_phase64_sclera_closeout.py
      provides: "Positive/negative review self-tests and exact 19-plan/34-task post-repair authority modes"
      contains: "EXPECTED_PLAN_COUNT = 19"
  key_links:
    - from: .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-no-skip-swiftpm-runner.js
      to: .planning/phases/62-sclera-evidence-and-admission-contract/62-private-evidence-runner.js
      via: "Import audited sclera discovery/assertion functions; import Phase 59 discovery separately for teeth"
      pattern: "discoverBundle|assertIgnoredBundle"
    - from: .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/check_phase64_sclera_closeout.py
      to: .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-no-skip-swiftpm-runner.js
      via: "T-64-08 parses the exact allowlisted live aggregate instead of accepting a bare full-suite exit code"
      pattern: "executed_tests|opt_in_tests_executed|skipped_tests"
    - from: .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/check_phase64_sclera_closeout.py
      to: .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-POST-REPAIR-REVIEW.md
      via: "Post-repair live modes consume only the fresh source-bound review; the old review remains historical"
      pattern: "POST_REPAIR_REVIEW"
    - from: .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/check_phase64_sclera_closeout.py
      to: .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-POST-REPAIR-CANDIDATE-VERIFICATION.md
      via: "A memory-only pre-write guard and the live validator enforce exact candidate schema, manifests, authority, owner immutability, and candidate-only repository delta"
      pattern: "candidate-guard|validate-candidate"
---

<objective>
Repair the two independently disqualifying Plan 64-12 command gates without changing production behavior or historical evidence.

Purpose: Make a complete zero-skip SwiftPM run possible and make checker self-test semantics reflect the current source-bound review contract.
Output: A mutation-tested no-skip runner and a post-repair checker state machine pinned to 19 plans / 34 task IDs.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@SECURITY.md
@RELIABILITY.md
@PRODUCT_SENSE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-CONTEXT.md
@.planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-RESEARCH.md
@.planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-VERIFICATION.md
@.planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-POST-PROMOTION-CANDIDATE-VERIFICATION.md
@.planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-13-SUMMARY.md
@.planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/check_phase64_sclera_closeout.py
@.planning/phases/62-sclera-evidence-and-admission-contract/62-private-evidence-runner.js
@.planning/phases/59-teeth-evidence-and-admission-contract/59-private-evidence-runner.js
@.codex/skills/spike-findings-beauty/references/sclera-redness.md
@.codex/skills/spike-findings-beauty/references/still-image-integration.md
@.codex/skills/spike-findings-beauty/references/licensed-fixture-evaluation.md
</context>

<tasks>

<task id="64-14-01" type="auto" tdd="true">
  <name>Task 1: Add the request-local zero-skip full SwiftPM runner</name>
  <read_first>
    - .planning/phases/62-sclera-evidence-and-admission-contract/62-private-evidence-runner.js
    - .planning/phases/59-teeth-evidence-and-admission-contract/59-private-evidence-runner.js
    - BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift
    - BeautySDK/Tests/BeautyEffectsTests/BeautyFaceGeometryAdapterTests.swift
    - BeautySDK/Tests/BeautyDetectionTests/VisionFaceDetectorTests.swift
    - BeautySDK/Tests/BeautyCoreTests/BeautyTeethWhiteningRealFixtureTests.swift
    - BeautySDK/Tests/BeautyCoreTests/BeautyScleraRednessRealFixtureTests.swift
    - .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-POST-PROMOTION-CANDIDATE-VERIFICATION.md
  </read_first>
  <files>.planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-no-skip-swiftpm-runner.js, .planning/phases/59-teeth-evidence-and-admission-contract/59-private-evidence-runner.js</files>
  <behavior>
    - `--self-test` returns schema `phase64-no-skip-swiftpm-v1`, status pass, and exactly fourteen mutation rejections without requiring either private bundle.
    - `--run` imports and calls the existing Phase 59/62 ignored-bundle discovery and validation functions, then creates one child-only environment without mutating `process.env`.
    - The one child is exactly `swift test --package-path BeautySDK`; it receives `BEAUTYSDK_RUN_VISION_INTEGRATION_TESTS=1`, `PHASE60_REQUIRE_LOCAL_EVIDENCE=1`, `PHASE63_REQUIRE_LOCAL_EVIDENCE=1`, `PHASE59_TEETH_BUNDLE`, and `PHASE62_SCLERA_BUNDLE`.
    - The parser observes the six exact Vision integration method names and the two exact real-fixture method names once as passed, one unambiguous top-level XCTest summary, nonzero executed tests, zero failed, and zero skipped.
    - Pass output contains only `schema`, `status`, `executed_tests`, `failed_tests`, `skipped_tests`, and `opt_in_tests_executed`; failure emits the same keys with fixed zero counts.
  </behavior>
  <action>
Add the existing Phase 59 `assertIgnoredBundle` function to that runner's `module.exports` without changing its body, discovery, child execution, privacy scan, CLI, or fixed output. Create `64-no-skip-swiftpm-runner.js` with exported constants/functions `OPT_IN_TESTS`, `parseXCTestSummary`, `classifyChildResult`, `discoverPrivateBundles`, `runSelfTests`, and `runFullSuite`. Reuse Phase 62 `discoverBundle`/`assertIgnoredBundle` for sclera and Phase 59 `discoverBundle`/`assertIgnoredBundle` for teeth; do not copy, weaken, or otherwise modify their discovery rules. Accept only `--self-test` and `--run`; locator arguments are forbidden.

Set `OPT_IN_TESTS` to exactly these suite/method pairs: `VisionFaceDetectorTests.testIntegrationDefaultStillImageProviderReturnsRedactedNoFaceForNoFaceFixture`, `VisionFaceDetectorTests.testIntegrationDefaultStillImageProviderReportsAggregateObservedFaceAvailabilityWithoutRawPayload`, `VisionFaceDetectorTests.testIntegrationDefaultStillImageProviderReportsObservedEyebrowAvailabilityWithoutRawPayload`, `BeautyEngineGeometryFacadeTests.testIntegrationLocalAuthorizedPortraitRoutesAllEyebrowFieldsThroughPublicFacade`, `BeautyFaceGeometryAdapterTests.testIntegrationLocalAuthorizedPortraitAggregateFitsLockedFaceValidationEnvelope`, `BeautyFaceGeometryAdapterTests.testIntegrationLocalAuthorizedPortraitFitsLockedEyebrowValidationEnvelope`, `BeautyTeethWhiteningRealFixtureTests.testAuthorizedPositiveAndNegativeStayWithinFrozenAggregateBounds`, and `BeautyScleraRednessRealFixtureTests.testAuthorizedPositiveAndNegativeStayWithinFrozenAggregateBounds`. Normalize XCTest's module/suite separators only; never reduce identity matching to a method basename because the two rights-gated methods intentionally share one basename.

For `--run`, obtain the teeth bundle only from Phase 59 `discoverBundle`, obtain the sclera bundle only from Phase 62 `discoverBundle`, and call each module's `assertIgnoredBundle` on its own result before spawning. Construct a new child environment object with the three opt-in flags and two locator variables, and invoke exactly one unfiltered `swift test --package-path BeautySDK` child with a bounded timeout and output-size ceiling. Do not mutate global environment state, write a raw log, or forward child output. Reject child spawn/timeout/nonzero, malformed UTF-8, missing/duplicated top-level `All tests` summary, inconsistent pass/execute totals, any nonzero failure or skip, missing/duplicated/failed/skipped exact opt-in method, unexpected second test-run summary, unknown aggregate key, and either bundle locator or its path components appearing in child stdout/stderr.

Implement fourteen deterministic parser/classifier mutations: nonzero exit, spawn error, timeout, oversized output, missing summary, duplicated summary, malformed counts, zero executed, nonzero failure, nonzero skip, missing opt-in method, duplicated opt-in method, method not passed, and locator leakage. Include a positive synthetic transcript containing all eight exact method identifiers. Add a two-invocation isolation case within the fourteen-mutation table that uses distinct sentinel locators and proves fixed outputs do not cross-contaminate, satisfying the OUT-05 concurrency probe. This implements D-04, D-15, D-18, and D-21 while preserving D-01 through D-14 and D-19/D-20 unchanged.
  </action>
  <verify>
    <automated>set -e; node -e 'const runner=require("./.planning/phases/59-teeth-evidence-and-admission-contract/59-private-evidence-runner.js"); if (typeof runner.assertIgnoredBundle !== "function") process.exit(1)'; node .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-no-skip-swiftpm-runner.js --self-test; ! node .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-no-skip-swiftpm-runner.js --unknown; rg -n 'BEAUTYSDK_RUN_VISION_INTEGRATION_TESTS|PHASE60_REQUIRE_LOCAL_EVIDENCE|PHASE63_REQUIRE_LOCAL_EVIDENCE|PHASE59_TEETH_BUNDLE|PHASE62_SCLERA_BUNDLE|opt_in_tests_executed' .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-no-skip-swiftpm-runner.js; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - The self-test passes exactly fourteen fail-closed mutation cases and one positive transcript.
    - Live mode has no locator CLI argument and uses one full SwiftPM child with all five environment entries.
    - All eight opt-in tests are identified by exact test method and must pass once.
    - The only durable output is the six-key fixed aggregate; no raw child output or private value is written.
  </acceptance_criteria>
  <done>A bounded one-child full-suite runner can prove nonzero execution, zero failures, zero skips, and all eight formerly skipped tests genuinely passed.</done>
</task>

<task id="64-14-02" type="auto" tdd="true">
  <name>Task 2: Repair review self-tests and pin post-repair authority inventory</name>
  <read_first>
    - .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/check_phase64_sclera_closeout.py
    - .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-REVIEW.md
    - .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-POST-PROMOTION-CANDIDATE-VERIFICATION.md
    - .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-VALIDATION.md
    - .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-no-skip-swiftpm-runner.js
    - .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-14-PLAN.md
    - .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-15-PLAN.md
    - .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-16-PLAN.md
    - .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-17-PLAN.md
    - .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-18-PLAN.md
    - .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-19-PLAN.md
    - .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-01-PLAN.md through `64-13-PLAN.md` and their matching `64-01-SUMMARY.md` through `64-13-SUMMARY.md` as immutable executed inventory inputs.
  </read_first>
  <files>.planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/check_phase64_sclera_closeout.py</files>
  <behavior>
    - Self-test has a positive source-matching review case and explicit negative cases for tree, frozen blob, index, worktree, missing/reordered/extra manifest rows, and malformed review categories.
    - The literal branch that raises `RED: stale review accepted without immutable source manifest` after current valid review success is absent.
    - `EXPECTED_PLAN_COUNT` is 19 and `EXPECTED_TASKS` is the exact 34-ID sequence from 64-01-01 through 64-19-01.
    - Fresh live authority reads only `64-POST-REPAIR-*` evidence/review/code-review/review-fix/security/pre-promotion/candidate artifacts; old artifacts remain historical and are never overwritten.
    - `--candidate-guard` snapshots repository/owner/source/authority state before the candidate exists, emits a fixed ready nonce, waits boundedly for one candidate file, then accepts only that one repository delta and a fully valid candidate.
    - `--validate-candidate` revalidates exact candidate schema, graph, manifests, no-skip identities, fresh authority, zero-HIGH, and all eight threats against live promotion-pending state.
    - New Plans 64-14 through 64-19 receive current structure validation; Plans 64-01 through 64-13 receive exact historical graph/task/summary validation without current-schema retrofitting.
    - CLI modes are `--self-test`, `--pre-promotion`, `--promotion-pending-verification`, `--candidate-guard`, `--validate-candidate`, `--final`, and `--quarantine`, each applicable live authority mode retaining isolated `--threat T-64-01` through `T-64-08`.
  </behavior>
  <action>
Replace the stale Plan 64-08 RED block at the start of `run_self_test` with an explicit positive current-review fixture whose tree, sorted exact manifest, index, and working bytes agree, then run the existing and expanded negative/mutation set. The positive case must call the same `validate_review` used by live mode. Retain negative coverage for changed tree/blob/index/worktree, future/stale tree, missing/extra/reordered/duplicate/malformed rows, locator fields, incomplete fixed categories, and tool/read failures. The self-test must not treat the historical `64-REVIEW.md` as current after this checker edit; D-16 deliberately invalidates that old freeze.

Add `EXPECTED_PLAN_COUNT = 19` and append exactly `64-14-01`, `64-14-02`, `64-15-01`, `64-15-02`, `64-16-01`, `64-16-02`, `64-17-01`, `64-17-02`, `64-18-01`, `64-19-01` after the existing 24 IDs. Require strict serial waves/dependencies 1 through 19. Do not require the immutable Plan 64-12 candidate to contain the new inventory; validate it only as historical `gaps_found` evidence and require new post-repair modes/artifacts to carry 19/34.

Encode the gap-closure structural grandfather as two disjoint constants: `HISTORICAL_EXECUTED_PLAN_IDS = (1, ..., 13)` and `CURRENT_STRUCTURE_PLAN_IDS = (14, ..., 19)`. For Plans 14–19, fail closed on current frontmatter fields, explicit XML task IDs, required task elements, threat model, artifacts section, and exact serial dependencies. For Plans 01–13, do not invoke or emulate the newer `must_haves` schema gate; instead require every immutable file, its exact historical wave/dependency, task cardinality/order, and matching summary. Derive each historical task ID deterministically from phase + plan + one-based task ordinal and require it to equal the corresponding `EXPECTED_TASKS` entry; when an old task already has an `id` attribute, require exact equality, but never edit an old file merely to add one. Across both groups still require all 19 plan IDs, all 34 task IDs, all six requirements, all summaries due at the active mode, and the complete serial graph. A historical structure exception is never a requirement/evidence/inventory waiver. Any GSD `verify.plan-structure` command used during execution must iterate only over `64-14-PLAN.md` through `64-19-PLAN.md`; whole-phase correctness comes from the checker's exact graph/inventory validator.

Add constants for `64-POST-REPAIR-SCLERA-OUTPUT-EVIDENCE.md`, `64-POST-REPAIR-REVIEW.md`, `64-POST-REPAIR-CODE-REVIEW.md`, `64-POST-REPAIR-REVIEW-FIX.md`, `64-POST-REPAIR-SECURITY.md`, `64-POST-REPAIR-PRE-PROMOTION-VERIFICATION.md`, and `64-POST-REPAIR-CANDIDATE-VERIFICATION.md`. Add the Phase 64 no-skip runner and its Phase 59/62 discovery dependencies to `RELEVANT_SOURCE_PATHS`, making the fresh manifest closure exactly 19 sorted paths. Parse the new runner's six-key live JSON fail-closed and require nonzero executed / 0 failed / 0 skipped / 8 opt-in.

Add `CANDIDATE_SCHEMA = "phase64-post-repair-candidate-v1"`, `CANDIDATE_GUARD_TIMEOUT_SECONDS = 1800`, `CANDIDATE_GUARD_POLL_MILLISECONDS = 250`, `CANDIDATE_INPUT_OWNER_PATHS`, `CANDIDATE_IMMUTABLE_OWNER_PATHS`, `POST_REPAIR_AUTHORITY_PATHS`, `capture_candidate_baseline`, `parse_candidate_manifest`, and `validate_post_repair_candidate`. `CANDIDATE_INPUT_OWNER_PATHS` must be exactly the canonical verification, validation, four Meitu product owners, five roots (`DESIGN.md`, `SECURITY.md`, `RELIABILITY.md`, `PRODUCT_SENSE.md`, `QUALITY_SCORE.md`), and four lifecycle owners (`PLANS.md`, REQUIREMENTS, ROADMAP, STATE): fifteen unique sorted paths. `CANDIDATE_IMMUTABLE_OWNER_PATHS` must be exactly the four product plus five root paths: nine unique sorted paths. `POST_REPAIR_AUTHORITY_PATHS` must be exactly the post-repair evidence, review, code-review, review-fix, security, and pre-promotion files: six unique sorted paths.

Implement `--candidate-guard` as a bounded pre-write/live seam. It must require the distinct candidate path absent and promotion-pending mode green, snapshot in memory the NUL-safe Git tracked/staged/untracked-nonignored delta with regular-file hashes plus the exact fifteen owner, nine immutable-owner, nineteen relevant-source, and six authority hashes, then emit only schema/status and a random 128-bit `guard_nonce`. Poll every 250 milliseconds for at most 1,800 seconds for one atomically written regular candidate within the existing `MAX_SCAN_FILE_BYTES` ceiling, and require two consecutive identical bounded reads before parsing. Validate `schema: phase64-post-repair-candidate-v1`, `verification_stage: post_repair_candidate`, `independent: true`, the exact `candidate_passed|gaps_found` branch, and the matching nonce. Require exactly 19 ordered plan rows, the exact 34 ordered task IDs, exactly 15 pre/post input-owner rows, exactly nine immutable-owner rows, exactly 19 relevant-source rows, and exactly six authority rows with no missing, duplicate, reordered, extra, malformed, or forged path/hash. Candidate pre hashes must equal the in-memory baseline; candidate post hashes and live bytes must equal the baseline; the repository delta after generation must equal the pre-snapshot plus only `64-POST-REPAIR-CANDIDATE-VERIFICATION.md`. Require canonical, validation, and every owner unchanged.

For `candidate_passed`, require one and only one complete no-skip authority command row, the exact executed count frozen by Plan 15 and greater than zero, `failed_tests: 0`, `skipped_tests: 0`, eight unique suite-qualified `OPT_IN_TESTS` identities in constant order, every identity passed exactly once, fresh review/code-review/review-fix/security hashes, `unresolved_high: 0`, and exact passing rows for T-64-01 through T-64-08. For `gaps_found`, require nonempty fixed blocker categories, `promotion_authorized: false`, and `required_next_action: execute_64_19_full_requarantine`; never allow a failed predicate to coexist with a passing branch. Both branches require owner/canonical/validation immutability and candidate-only delta. `--validate-candidate` applies the same live schema/manifest/authority/no-skip/threat checks after guard completion; it cannot waive the guard's pre-write delta result.

Extend `run_self_test` with a positive guarded candidate fixture and independent negative mutations for missing, reordered, duplicate, extra, and forged task/owner/source/authority rows; wrong schema/status/nonce; stale source/review/code-review/review-fix/security hashes; mismatched pre/post owner hashes; live owner drift; canonical drift; validation drift; nonzero HIGH or a missing/failed threat; absent/zero/failed/skipped/duplicate/method-basename-only opt-in results; a second full-suite aggregate; preexisting candidate; symlink/nonregular/oversized candidate; timeout; and any extra tracked, staged, or untracked-nonignored file write. Inject the guard clock/read/poll functions in self-tests so the 1,800-second timeout mutation completes deterministically without sleeping. Fixed guard/validator output may contain only schema, status, branch, and allowlisted counts; it must never emit repository bytes, hashes, private bundle locators, or paths.

Keep the serial live modes exact: `--pre-promotion` requires fresh post-repair evidence/review/audits and quarantined product/canonical state without requiring lifecycle synchronization; `--promotion-pending-verification` requires fresh eligibility, exact promoted product/root owners, 19/34 lifecycle/validation with 64-18-01 and 64-19-01 pending; `--candidate-guard`/`--validate-candidate` enforce the candidate contract above; `--final` requires a guard-validated fresh `candidate_passed`, canonical success, 34/34 accounted with only 64-13-01 explicitly historical/superseded, and final lifecycle; `--quarantine` requires canonical gaps/incomplete validation/full fifteen-owner future-unproven state after candidate/final failure without treating a stale review as authority. Update T-64-08 and self-test mutations for every probe predicate named in `must_haves.truths`. This implements D-05, D-09, D-10, D-12, D-14, D-16, D-17, D-18, and D-21; D-01 through D-04, D-06 through D-08, D-11, D-13, D-15, D-19, and D-20 remain exact checked invariants.
  </action>
  <verify>
    <automated>set -e; python3 .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/check_phase64_sclera_closeout.py --self-test; for plan_path in .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-{14,15,16,17,18,19}-PLAN.md; do node /Users/yakangwang/.codex/get-shit-done/bin/gsd-tools.cjs query verify.plan-structure "$plan_path" | jq -e '.valid == true' &gt;/dev/null || exit 1; done; ! rg -n 'RED: stale review accepted without immutable source manifest' .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/check_phase64_sclera_closeout.py; rg -n 'EXPECTED_PLAN_COUNT = 19|64-14-01|64-19-01|CURRENT_STRUCTURE_PLAN_IDS|CANDIDATE_SCHEMA|--candidate-guard|--validate-candidate|--quarantine|64-no-skip-swiftpm-runner.js' .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/check_phase64_sclera_closeout.py; ! python3 .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/check_phase64_sclera_closeout.py --pre-promotion; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Current-review positive coverage uses the live validator; all source/manifest/category mutations fail.
    - The old deliberate RED branch is removed while immutable source binding remains fail closed.
    - The checker knows exactly 19 plans / 34 tasks and all seven modes, including the candidate guard/validator.
    - Candidate self-tests reject every manifest/hash/owner/drift/extra-write mutation and accept one exact guarded fixture.
    - Current plan-structure validation passes for Plans 14–19 only; immutable Plans 01–13 pass exact graph/task/summary checks without retrofit or waiver.
    - Post-repair modes consume only distinct fresh artifacts and never overwrite or upgrade old evidence.
    - Pre-promotion remains closed until Plan 64-15 produces fresh authority.
  </acceptance_criteria>
  <done>The checker self-test is semantically green, source-bound, post-repair-aware, and unable to accept skipped or stale authority.</done>
</task>

</tasks>

<threat_model>
## OWASP ASVS Level 1 Trust Boundaries

| Boundary | Description |
|---|---|
| Ignored private bundles → runner child | Locators are untrusted sensitive values confined to one child environment. |
| SwiftPM/XCTest output → fixed aggregate | Raw output is untrusted and must satisfy exact grammar, cardinality, test identity, and no-leak checks. |
| Review manifest → checker authority | A review is current only when tree, index, worktree, and exact relevant blobs match. |
| Historical 13/24 authority → post-repair 19/34 authority | Old candidate evidence is immutable history and cannot validate the new chain. |
| Candidate verifier → repository | A memory-only guard snapshots the repository before candidate creation and permits only one exact candidate-file delta. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-64-01 | Spoofing | Opt-in test identity | mitigate | Require all eight exact methods once as passed in one complete suite. |
| T-64-02 | Tampering | XCTest aggregate | mitigate | Exact top-level parser, zero fail/skip, mutation self-tests. |
| T-64-03 | Repudiation | Candidate manifests and delta | mitigate | Guard nonce, exact pre/post hashes, exact manifest cardinalities, and extra-write mutation tests. |
| T-64-04 | Information disclosure | Bundle locators/raw output | mitigate | Child-env-only injection, leak scan, fixed six-key output. |
| T-64-05 | Repudiation | Review freshness | mitigate | Positive current fixture plus tree/index/worktree/manifest mutations. |
| T-64-08 | Elevation of privilege | Inventory and modes | mitigate | Exact 19/34 graph and post-repair-only authority artifacts. |
| T-64-SC | Tampering | Dependency surface | mitigate | Standard-library Node/Python only; no install or manifest change. |

Every HIGH result blocks. No passed aggregate offsets a failed identity, privacy, review, or inventory check.
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

- `64-no-skip-swiftpm-runner.js`.
- Existing Phase 59 symbol `assertIgnoredBundle` added to `module.exports` without logic or CLI changes.
- CLI modes `--self-test` and `--run` with schema `phase64-no-skip-swiftpm-v1`.
- Symbols `OPT_IN_TESTS`, `parseXCTestSummary`, `classifyChildResult`, `discoverPrivateBundles`, `runSelfTests`, and `runFullSuite`.
- Updated checker constants for the seven `64-POST-REPAIR-*` artifacts, exact 19-path source closure, `EXPECTED_PLAN_COUNT`, and 34 ordered tasks.
- Candidate constants/symbols `CANDIDATE_SCHEMA`, `CANDIDATE_GUARD_TIMEOUT_SECONDS`, `CANDIDATE_GUARD_POLL_MILLISECONDS`, `CANDIDATE_INPUT_OWNER_PATHS`, `CANDIDATE_IMMUTABLE_OWNER_PATHS`, `POST_REPAIR_AUTHORITY_PATHS`, `capture_candidate_baseline`, `parse_candidate_manifest`, and `validate_post_repair_candidate`.
- Checker modes `--candidate-guard`, `--validate-candidate`, and `--quarantine` plus repaired existing self/pre/pending/final modes.
- Structural constants `HISTORICAL_EXECUTED_PLAN_IDS` and `CURRENT_STRUCTURE_PLAN_IDS` implementing the bounded grandfather rule.
</artifacts_this_phase_produces>

<source_coverage_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Independent public/per-eye/adversarial closeout | Repair makes the complete conjunction executable; no product claim occurs here. |
| REQ | SCLERA-14..18, OUT-05 | All IDs are in frontmatter; exact probe predicates enter checker/runner self-tests. |
| CONTEXT | D-01..D-21 | D-04/D-05/D-09/D-10/D-12/D-14..D-18/D-21 implemented; all other decisions retained as checked invariants. |
| RESEARCH | No runtime tuning; aggregate privacy; source-bound review; serial promotion | Standard-library tooling, fixed output, fresh-review constants, 19/34 graph. |
| CANONICAL GAP | Stale self-test RED | Removed and replaced by positive current-review plus negative mutation coverage. |
| CANONICAL GAP | 636/0/8 full suite | Replaced by one opt-in complete full-suite runner requiring nonzero/0/0 and exact 8/8. |
| IMMUTABLE CANDIDATE | Plan 64-12 `gaps_found` | Read-only historical input; excluded from new inventory authority. |
| PLAN 64-13 | Complete failure quarantine | Preserved as predecessor and historical failed/requarantined row. |
| HISTORICAL PLAN FORMAT | Plans 01–13 predate the current must_haves schema | Immutable executed inputs receive exact graph/task/summary validation; only Plans 14–19 receive current structure validation, with no requirement or evidence waiver. |
| SPIKE | Request-local masks, protected anatomy, licensed bundle privacy | No source algorithm change; locators stay child-local and aggregate-only. |
| SPEC probe | All 15 unresolved items | Converted to the six executable truth groups and runner/checker mutations above. |

Prohibition recall kept only biometric/privacy non-disclosure, fail-closed authority, immutable-candidate integrity, and product/scope value boundaries. Generic repository hygiene from `AGENTS.md`, generic plan naming/inventory mechanics, and ordinary diff/commit rules were dropped from `must_haves.prohibitions` and remain execution breadcrumbs in `<execution_context>`.
</source_coverage_audit>

<verification>
Run runner/review/candidate mutation self-tests, prove the stale RED text is absent, validate exact 19/34 constants and seven modes, validate structure only for Plans 14–19, and require live pre-promotion to remain closed until fresh authority exists.
</verification>

<success_criteria>
- Checker `--self-test` exits zero with positive and negative review-source coverage.
- No-skip runner `--self-test` exits zero with exactly fourteen mutation rejections.
- No private locator can be supplied as an argument or emitted by the runner.
- The post-repair checker rejects old authority and pins 19 plans / 34 tasks.
- Candidate guard/validator rejects malformed or stale candidate authority, owner/canonical drift, and every extra repository write.
- Historical schema grandfathering is bounded to Plans 01–13 and preserves every whole-phase inventory/evidence requirement.
</success_criteria>

<output>
Create `.planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-14-SUMMARY.md` when done.
</output>
