---
phase: 64-sclera-output-adversarial-safety-and-independent-closeout
plan: 18
type: execute
wave: 18
depends_on: [64-17]
gap_closure: true
security_standard: OWASP ASVS Level 1
block_on: HIGH
files_modified:
  - .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-POST-REPAIR-CANDIDATE-VERIFICATION.md
autonomous: true
requirements: [SCLERA-14, SCLERA-15, SCLERA-16, SCLERA-17, SCLERA-18, OUT-05]
must_haves:
  truths:
    - "A distinct verifier reruns the complete fresh conjunction and writes a new post-repair candidate without mutating any of the fifteen input owners."
    - "Candidate success requires the exact nonzero SwiftPM executed count, zero failures, zero skips, and each of the eight opt-in tests observed exactly once as passed."
    - "The candidate freezes the exact 19-plan/34-task graph, all fifteen owner hashes, all nine promoted product/root owner hashes, and all fresh review/evidence authority hashes."
    - "A checker-owned pre-write guard proves the candidate is the only repository delta and a checker-owned validator mutation-tests exact schema, manifests, hashes, no-skip identities, zero-HIGH threats, and live owner/canonical/validation immutability."
    - "A candidate result is only candidate_passed or gaps_found; candidate_passed authorizes Plan 19 but never changes canonical verification or Phase 65."
    - "Any candidate failure is explicit authority for Plan 19 to reapply the complete fifteen-owner quarantine; mixed state is never accepted."
  artifacts:
    - path: ".planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-POST-REPAIR-CANDIDATE-VERIFICATION.md"
      provides: "Distinct immutable candidate decision and exact input manifests"
      contains: "candidate_passed"
  key_links:
    - from: ".planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-no-skip-swiftpm-runner.js"
      to: ".planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-POST-REPAIR-CANDIDATE-VERIFICATION.md"
      via: "allowlisted aggregate and exact opt-in identity result"
      pattern: "executed_tests.*failed_tests.*skipped_tests.*opt_in_tests_executed"
    - from: ".planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-POST-REPAIR-CANDIDATE-VERIFICATION.md"
      to: ".planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/check_phase64_sclera_closeout.py"
      via: "candidate status plus frozen fifteen-owner manifest consumed by checker mode `--final`"
      pattern: "candidate_(passed|gaps_found)"
    - from: ".planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/check_phase64_sclera_closeout.py"
      to: ".planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-POST-REPAIR-CANDIDATE-VERIFICATION.md"
      via: "Checker mode `--candidate-guard` starts before verifier output, binds a nonce and in-memory baseline, then allows only the candidate-file delta"
      pattern: "phase64-post-repair-candidate-v1|guard_nonce"
  prohibitions:
    - statement: "Must not write or normalize any of the fifteen product, root, lifecycle, canonical, or validation owners during candidate verification."
      status: unverified
      verification: flagged
    - statement: "Must not treat skipped opt-in tests, partial test commands, or malformed XCTest output as candidate evidence."
      status: unverified
      verification: flagged
    - statement: "Must not expose private bundle locators in command arguments, stdout, stderr, artifact text, or hashes derived from locator strings."
      status: unverified
      verification: flagged
    - statement: "Must not mark canonical Phase 64 passed or unblock Phase 65 from candidate_passed alone."
      status: unverified
      verification: flagged
---

<objective>
Produce a distinct immutable post-repair candidate from a complete no-skip, source-bound, adversarially checked snapshot without owner mutation.

Purpose: Separate candidate judgment from eligibility, owner synchronization, and the later success-or-requarantine transaction.
Output: `64-POST-REPAIR-CANDIDATE-VERIFICATION.md` with a fail-closed decision and exact manifests.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-CONTEXT.md
@.planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-RESEARCH.md
@.planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-VALIDATION.md
@.planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-VERIFICATION.md
@.planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-POST-REPAIR-PRE-PROMOTION-VERIFICATION.md
@.planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-no-skip-swiftpm-runner.js
@.planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/check_phase64_sclera_closeout.py
@.planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-17-SUMMARY.md
</context>

<tasks>

<task id="64-18-01" type="auto">
  <name>Task 64-18-01: Independently verify and freeze the post-repair candidate</name>
  <files>.planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-POST-REPAIR-CANDIDATE-VERIFICATION.md</files>
  <read_first>
    - `.planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-POST-REPAIR-PRE-PROMOTION-VERIFICATION.md` and its five fresh evidence/review/audit inputs for eligibility predicates and frozen source hashes.
    - `.planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-VALIDATION.md`, `PLANS.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md` for the exact promotion-pending inventory.
    - The nine product/root owners synchronized by Plan 16 for the exact bounded product snapshot.
    - `.planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-no-skip-swiftpm-runner.js` for locator-free `--self-test`/`--run` output schema and exact eight-test identity list.
    - `.planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/check_phase64_sclera_closeout.py` for candidate/final invariants and all threat probes.
    - `.planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-POST-PROMOTION-CANDIDATE-VERIFICATION.md` only as immutable historical gaps evidence; never edit or copy its verdict as current authority.
  </read_first>
  <action>
Require the distinct candidate file to be absent, then launch the Plan 14 checker `--candidate-guard` in a persistent process before dispatching the verifier. Wait for its fixed `phase64-candidate-guard-v1` ready record and pass only its 128-bit `guard_nonce` to the verifier; the guard retains all baseline paths/hashes in memory and emits none. Dispatch a fresh `gsd-verifier` that is distinct from the Plan 14/15 executors, private reviewer, code reviewer, security auditor, owner synchronizer, and future final executor. The verifier reruns the exact conjunction: focused Swift tests, output-helper self-tests, private output evidence, checker `--self-test`, no-skip runner `--self-test`, no-skip runner `--run` as the sole complete unfiltered `swift test --package-path BeautySDK` authority invocation, Demo build/test, fresh 19-path source freeze comparison, fresh review/code-review/review-fix/security checks, all 15 spec-probe predicates, and each T-64-01 through T-64-08. Do not add a bare full SwiftPM invocation whose opt-ins would skip. Require the no-skip result's executed count to equal the Plan 15 evidence count and be nonzero, with failed=0, skipped=0, exactly eight suite-qualified unique opt-in identities in `OPT_IN_TESTS` order, and every identity passed once. Reject duplicate, ambiguous, absent, malformed, basename-only, or locator-bearing output.

Write only `64-POST-REPAIR-CANDIDATE-VERIFICATION.md` atomically as a bounded regular file. Its exact header is `schema: phase64-post-repair-candidate-v1`, `verification_stage: post_repair_candidate`, `independent: true`, `guard_nonce: &lt;32 lowercase hex&gt;`, and exactly `status: candidate_passed|gaps_found`. Include exact scalar cardinalities `expected_plan_count: 19`, `expected_task_count: 34`, `input_owner_count: 15`, `immutable_owner_count: 9`, `relevant_source_count: 19`, and `authority_count: 6`. Record one ordered row per 19 plan IDs and 34 task IDs; exact pre/post rows for all fifteen input owners; exact rows for the nine immutable product/root owners; the exact 19-path fresh source manifest; exact hashes for post-repair evidence, review, code-review, review-fix, security, and pre-promotion eligibility; one no-skip aggregate and all eight suite-qualified identities; `unresolved_high: 0`; and T-64-01 through T-64-08 results. Apply the Plan 14 grandfather rule: current structure validation is only Plans 14–19, while immutable Plans 01–13 must still have exact waves/dependencies/tasks/summaries and cannot lose requirements or evidence.

Emit `candidate_passed` only when every predicate succeeds. Otherwise emit `gaps_found`, fixed non-sensitive blocker categories, `promotion_authorized: false`, and `required_next_action: execute_64_19_full_requarantine`. State that either branch leaves canonical `64-VERIFICATION.md`, validation, and all product/root/lifecycle owners unchanged; candidate_passed authorizes only Plan 19. Wait for `--candidate-guard` to exit zero, proving live pre/post owner identity, canonical/validation immutability, exact baseline hashes, and candidate-only Git delta. Then run `--validate-candidate` and require its fixed allowlisted result; if the guard or validator fails, the task is not complete and Plan 19 must take the full-requarantine branch. No private path, repository byte, raw output, or hash derived from private fixture content enters durable output. This implements D-01 through D-21 and all six requirements without owner mutation.
  </action>
  <verify>
    <automated>phase64_candidate=.planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-POST-REPAIR-CANDIDATE-VERIFICATION.md &amp;&amp; python3 .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/check_phase64_sclera_closeout.py --validate-candidate &amp;&amp; rg -q '^schema: phase64-post-repair-candidate-v1$' "$phase64_candidate" &amp;&amp; rg -q '^verification_stage: post_repair_candidate$' "$phase64_candidate" &amp;&amp; rg -q '^independent: true$' "$phase64_candidate" &amp;&amp; rg -q '^guard_nonce: [0-9a-f]{32}$' "$phase64_candidate" &amp;&amp; rg -q '^status: (candidate_passed|gaps_found)$' "$phase64_candidate" &amp;&amp; rg -q '^expected_plan_count: 19$' "$phase64_candidate" &amp;&amp; rg -q '^expected_task_count: 34$' "$phase64_candidate" &amp;&amp; rg -q '^input_owner_count: 15$' "$phase64_candidate" &amp;&amp; rg -q '^immutable_owner_count: 9$' "$phase64_candidate" &amp;&amp; rg -q '^relevant_source_count: 19$' "$phase64_candidate" &amp;&amp; rg -q '^authority_count: 6$' "$phase64_candidate" &amp;&amp; phase64_candidate_status=$(sed -n 's/^status: //p' "$phase64_candidate") &amp;&amp; if [ "$phase64_candidate_status" = candidate_passed ]; then rg -q '^executed_tests: [1-9][0-9]*$' "$phase64_candidate" &amp;&amp; rg -q '^opt_in_tests_executed: 8$' "$phase64_candidate" &amp;&amp; rg -q '^failed_tests: 0$' "$phase64_candidate" &amp;&amp; rg -q '^skipped_tests: 0$' "$phase64_candidate" &amp;&amp; rg -q '^unresolved_high: 0$' "$phase64_candidate" &amp;&amp; python3 .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/check_phase64_sclera_closeout.py --promotion-pending-verification &amp;&amp; for phase64_threat in T-64-01 T-64-02 T-64-03 T-64-04 T-64-05 T-64-06 T-64-07 T-64-08; do python3 .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/check_phase64_sclera_closeout.py --validate-candidate --threat "$phase64_threat" || exit 1; done; elif [ "$phase64_candidate_status" = gaps_found ]; then rg -q '^promotion_authorized: false$' "$phase64_candidate" &amp;&amp; rg -q '^required_next_action: execute_64_19_full_requarantine$' "$phase64_candidate"; else exit 1; fi</automated>
  </verify>
  <acceptance_criteria>
    - A distinct verifier reruns the full conjunction; no command is substituted with old evidence.
    - The new candidate contains exact no-skip counts and all eight unique opt-in identities.
    - The candidate contains exact source, authority, fifteen-owner, and nine-owner hashes plus pre/post identity.
    - The pre-write guard exits zero after proving the candidate is the only repository delta; the live validator and all eight isolated threats exit zero.
    - Plan 14 candidate mutation tests reject missing/reordered/duplicate/forged rows, stale hashes, owner/canonical/validation drift, and extra writes.
    - Candidate status is exactly candidate_passed or gaps_found and authorizes only the bounded Plan 19 branch.
    - No owner, immutable historical candidate, or source file is modified by this task.
  </acceptance_criteria>
  <done>A distinct immutable candidate exists with a complete, privacy-safe, fail-closed decision over an unchanged promotion-pending snapshot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Private bundle discovery → SwiftPM child | Locator values exist only in the child environment and must never cross into observable output. |
| Promotion-pending owners → independent verifier | The verifier may read/hash owners but cannot repair or normalize them. |
| Candidate artifact → final transaction | A candidate is untrusted until exact status, hashes, identities, and complete conjunction are checked. |
| Pre-write repository snapshot → candidate artifact | The guard retains baseline bytes/hashes in memory and permits only one candidate-file delta. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-64-01 | Spoofing | Candidate identity | mitigate | Distinct artifact name, verifier identity, fresh timestamps, and source hashes. |
| T-64-02 | Tampering | Owner snapshot | mitigate | Fifteen-owner pre/post SHA-256 identity manifest. |
| T-64-03 | Repudiation | Candidate commands and candidate-only delta | mitigate | Record exact commands/allowlisted aggregates and require the pre-write guard nonce, in-memory Git snapshot, bounded wait, and extra-write rejection. |
| T-64-04 | Information disclosure | Bundle locators | mitigate | Child-environment-only injection and full output leak scan. |
| T-64-05 | Denial of service | XCTest parser | mitigate | Reject absent, duplicate, ambiguous, malformed, or nonzero-skip summaries. |
| T-64-06 | Tampering | Protected regions | mitigate | Recheck all adversarial effect/leakage predicates and per-eye evidence. |
| T-64-07 | Elevation of privilege | Candidate status | mitigate | candidate_passed authorizes only Plan 19, never canonical success. |
| T-64-08 | Elevation of privilege | Inventory | mitigate | Exact 19-plan/34-task and 15-owner manifests are mandatory. |
| T-64-SC | Tampering | Package installs | accept | No install task or dependency change occurs. |
</threat_model>

## Artifacts this phase produces

- `64-POST-REPAIR-CANDIDATE-VERIFICATION.md` with `candidate_passed` or `gaps_found`.
- Exact fifteen-owner and nine-owner SHA-256 manifests.
- Exact fresh-authority and 19-path source manifests.
- Allowlisted no-skip aggregate plus eight exact opt-in test identities.
- Checker `--candidate-guard` and `--validate-candidate` pass records, retained in execution results without a new repository artifact.
- No owner, source, API, runtime, model, network, dependency, or locator artifact.

## Multi-Source Coverage Audit

| Source | Items covered | Plan binding |
|---|---|---|
| GOAL | Independent public-output/per-eye/adversarial closeout | Complete fresh conjunction judged by a distinct verifier. |
| REQ | SCLERA-14 through SCLERA-18, OUT-05 | Each is rechecked and recorded in the candidate. |
| CONTEXT | D-01 through D-21 | Full decision set is a candidate predicate; no owner mutation. |
| RESEARCH | Independent review, privacy-safe aggregates, fail-closed evidence | Fresh review/source hashes, exact no-skip schema, locator scan. |
| CANONICAL GAPS | Stale self-test and eight skips | Both repairs rerun; no waiver or reinterpretation. |
| IMMUTABLE CANDIDATE | Historical failed attempt | Read only as history and never overwritten. |
| PLAN 13 FAILURE | Requarantine on failed candidate | gaps_found explicitly authorizes Plan 19 failure branch. |
| SPIKE | Protected masks, original composition, real licensed fixtures | Rechecked through adversarial and rights-gated tests. |
| SPEC PROBES | Fifteen exact probe gaps | All are named mandatory predicates, never auto-dismissed. |
| PROHIBITION RECALL | Safety/value must-NOTs only | Four flagged prohibitions retained; generic repo hygiene is enforced by manifests. |

<verification>
Require the complete conjunction, exact eight test identities, exact source/authority/owner manifests, and owner byte identity. Candidate success must leave canonical gaps and Phase 65 blocked.
</verification>

<success_criteria>
- A distinct post-repair candidate exists and is immutable after creation.
- `candidate_passed` occurs only with complete fresh no-skip and source-bound authority.
- Any failure is explicit `gaps_found` requiring the Plan 19 full-requarantine branch.
- Candidate generation performs zero owner/source mutations and grants no canonical authority.
</success_criteria>

<output>
Create `.planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-18-SUMMARY.md` when done.
</output>
