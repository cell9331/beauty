---
phase: 60-teeth-provider-and-production-integration
plan: "02"
type: execute
wave: 2
depends_on: [60-01]
files_modified:
  - BeautySDK/Sources/BeautyEffects/LocalRetouch/BeautyTeethWhiteningProvider.swift
  - BeautySDK/Sources/BeautyEffects/LocalRetouch/BeautyTeethWhiteningTransform.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautyTeethWhiteningProviderTests.swift
autonomous: true
requirements: [TEETH-09, TEETH-10, TEETH-11, TEETH-12, TEETH-13]
security_standard: OWASP ASVS Level 1
block_on: HIGH
must_haves:
  truths:
    - "The stateless provider accepts only complete actual mapped lip support and returns one unit or local abstention."
    - "Adaptive coverage can add only connected color-qualified pixels inside the hard envelope and cannot drop the fixed strong baseline."
    - "Every target derives from the immutable canonical source and already-light/protected pixels remain unchanged."
---

<objective>
Implement the package-only stateless teeth selector and bounded source-derived
transform against the frozen Wave 0 contracts.

Purpose: Convert direct teeth intent and current request support into one safe
composition unit without broadening the facade yet.
Output: Green deterministic provider/transform/containment tests.
</objective>

<context>
@.planning/phases/60-teeth-provider-and-production-integration/60-CONTEXT.md
@.planning/phases/60-teeth-provider-and-production-integration/60-RESEARCH.md
@.planning/phases/60-teeth-provider-and-production-integration/60-01-SUMMARY.md
@BeautySDK/Sources/BeautyCore/Models/BeautyCanonicalStillImage.swift
@BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift
@BeautySDK/Sources/BeautyEffects/Render/BeautyLocalRetouchComposition.swift
</context>

<tasks>

<task id="60-02-01" type="auto">
  <name>Task 1: Implement validated fixed-baseline and connected adaptive selection</name>
  <files>BeautySDK/Sources/BeautyEffects/LocalRetouch/BeautyTeethWhiteningProvider.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyTeethWhiteningProviderTests.swift</files>
  <action>Implement D-02 through D-07 as a stateless package provider with internal scratch values only. D-05: use checked pixel arithmetic; finite closed-unit simple/nested polygon validation; inner/outer rasterization at pixel centers; minimum mouth span and aperture ratio; fixed luminance/neutrality/blue-floor/red-imbalance score; and the exact 1.5%...94% strong-area gate. D-06: require fixed >0.15 seeds, clip the outer-lip region with the upper safety inset and lower extension, derive local Otsu/seed-percentile candidate limits, and retain only an eight-connected flood from accepted seeds. Apply radius-one blur, post-filter hard re-clip, and final max with the clipped fixed baseline. Return aggregate counts only and abstain on every failed invariant. Do not synthesize landmarks, infer from face bounds, relax thresholds for coverage, or expose masks beyond module tests.</action>
  <verify><automated>CLANG_MODULE_CACHE_PATH=/private/tmp/beauty-phase60-clang-module-cache swift test --package-path BeautySDK --filter BeautyTeethWhiteningProviderTests</automated></verify>
  <done>Support and mask tests pass with zero fixed-baseline drops, connected-only growth, hard-envelope containment, and fail-closed malformed/closed inputs.</done>
</task>

<task id="60-02-02" type="auto">
  <name>Task 2: Implement bounded immutable-source targets and owner-issued unit</name>
  <files>BeautySDK/Sources/BeautyEffects/LocalRetouch/BeautyTeethWhiteningTransform.swift, BeautySDK/Sources/BeautyEffects/LocalRetouch/BeautyTeethWhiteningProvider.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyTeethWhiteningProviderTests.swift</files>
  <action>Implement D-08 through D-10. D-09: scale normalized public strength by the locked conservative maximum, require material yellow excess, apply the 1.45 neutralization factor and small capped luminance target, deterministically quantize source-derived target RGB, and skip neutral, light, lightly warm, protected, or otherwise unchanged targets. Compute the target without mask multiplication, then convert the final soft mask to Q16 so the existing composition owner applies it exactly once. Request one unit only inside the hard union. Preserve source binding, alpha, unowned bytes, and collision semantics. Prove yellow excess decreases, channel/luminance movement stays bounded, order is deterministic, and no output pixel is used as transform input.</action>
  <verify><automated>CLANG_MODULE_CACHE_PATH=/private/tmp/beauty-phase60-clang-module-cache swift test --package-path BeautySDK --filter 'BeautyTeethWhiteningProviderTests|BeautyLocalRetouchCompositionTests' &amp;&amp; python3 .planning/phases/60-teeth-provider-and-production-integration/check_phase60_teeth_provider_boundaries.py --provider --repo-root . &amp;&amp; git diff --check</automated></verify>
  <done>The provider emits zero or one valid owner-bound unit whose proposals are hard-contained and immutable-source-derived; focused tests and provider HIGH gates pass.</done>
</task>

</tasks>

<threat_model>
This plan primarily mitigates T-60-02, T-60-03, T-60-04, and T-60-05. Any
malformed support accepted, dropped baseline, post-blur leak, protected sentinel
change, output-derived target, or unbounded arithmetic blocks completion.
</threat_model>

<verification>Run focused provider/composition tests and the isolated provider checker after both tasks.</verification>

<success_criteria>The package-level provider safely produces one optional teeth unit without facade activation or private-state exposure.</success_criteria>

<output>Create `.planning/phases/60-teeth-provider-and-production-integration/60-02-SUMMARY.md` with actual focused counts and bounded nonclaims.</output>
