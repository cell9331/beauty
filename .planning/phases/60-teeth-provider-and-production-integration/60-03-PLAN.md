---
phase: 60-teeth-provider-and-production-integration
plan: "03"
type: execute
wave: 3
depends_on: [60-02]
files_modified:
  - BeautySDK/Sources/BeautySDK/BeautyEngine.swift
  - BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyEngineTeethWhiteningIntegrationTests.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyEngineLocalRetouchFoundationTests.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyEngineLocalRetouchCompositionTests.swift
autonomous: true
requirements: [TEETH-09, TEETH-12, TEETH-14]
security_standard: OWASP ASVS Level 1
block_on: HIGH
must_haves:
  truths:
    - "Direct positive teeth intent invokes one provider inside the existing one-request still-image lifecycle and composes once."
    - "Local abstention preserves unrelated eligible output and cannot reuse prior support, mask, proposals, result, or summary."
    - "Pixel-buffer, reset, Demo, sibling, model, and network paths gain no teeth provider work."
---

<objective>
Connect the package provider to the admitted production still-image branch and
prove lifecycle/failure isolation without activating deferred surfaces.

Purpose: Make the Phase 59 intent perform bounded production work using the one
current request context.
Output: Green deterministic facade integration and recovery tests.
</objective>

<context>
@.planning/phases/60-teeth-provider-and-production-integration/60-CONTEXT.md
@.planning/phases/60-teeth-provider-and-production-integration/60-02-SUMMARY.md
@BeautySDK/Sources/BeautySDK/BeautyEngine.swift
@BeautySDK/Sources/BeautySDK/BeautyEngineGeometryDetection.swift
@BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift
@BeautySDK/Sources/BeautySDK/BeautyStillImageRequestContext.swift
</context>

<tasks>

<task id="60-03-01" type="auto">
  <name>Task 1: Wire one production teeth unit into the admitted still-image composition</name>
  <files>BeautySDK/Sources/BeautySDK/BeautyEngine.swift, BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift, BeautySDK/Tests/BeautyCoreTests/BeautyEngineTeethWhiteningIntegrationTests.swift, BeautySDK/Tests/BeautyCoreTests/BeautyEngineLocalRetouchCompositionTests.swift</files>
  <action>Implement D-01, D-02, D-10, and D-11. In the already admitted still-image branch, create one composition owner from `requestContext.canonicalImage`, invoke the production provider once only for direct normalized positive `teethWhitening` using the selected observation's current mapped lip support, append existing opaque Testing-only units to the same owner without letting those hooks activate the teeth provider, compose once, then render the existing plan. Preserve legacy zero-demand route exactly. Add aggregate Testing observations sufficient to prove provider invocation/unit/abstention counts without exposing geometry or pixels. Verify both public still-image entries, source identity, exact event order, one detector request, and unrelated color continuation.</action>
  <verify><automated>CLANG_MODULE_CACHE_PATH=/private/tmp/beauty-phase60-clang-module-cache swift test --package-path BeautySDK --filter 'BeautyEngineTeethWhiteningIntegrationTests|BeautyEngineLocalRetouchCompositionTests|BeautyEngineLocalRetouchFoundationTests'</automated></verify>
  <done>Positive direct intent reaches one production provider and one composition using the current canonical/request support; zero intent and Testing-only demand cannot activate teeth work.</done>
</task>

<task id="60-03-02" type="auto">
  <name>Task 2: Prove local abstention, recovery, parallel isolation, and deferred-path absence</name>
  <files>BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift, BeautySDK/Tests/BeautyCoreTests/BeautyEngineTeethWhiteningIntegrationTests.swift, BeautySDK/Tests/BeautyCoreTests/BeautyEngineLocalRetouchFoundationTests.swift</files>
  <action>Implement D-12, D-13, and D-15 contracts. Cover no face, missing either lip array, malformed/closed/occluded support, no yellow candidates, already-light input, valid-invalid-valid in both directions, repeated calls, independent engines, parallel completion-order calls, interrupted callers, provider/unit abstention, and continued unrelated color work. Assert request-local aggregate state returns to zero and no previous support, mask, unit, output, or summary is reused. Pixel-buffer processing and reset must perform zero provider work, and no Demo/sclera/去脂/realtime/model/network route changes. Keep typed canonical failures unchanged.</action>
  <verify><automated>CLANG_MODULE_CACHE_PATH=/private/tmp/beauty-phase60-clang-module-cache swift test --package-path BeautySDK --filter 'BeautyEngineTeethWhiteningIntegrationTests|BeautyEngineLocalRetouchFoundationTests|BeautyEngineMetadataCompatibilityTests|BeautyEngineGeometryFacadeTests' &amp;&amp; python3 .planning/phases/60-teeth-provider-and-production-integration/check_phase60_teeth_provider_boundaries.py --integration --repo-root . &amp;&amp; git diff --check</automated></verify>
  <done>All deterministic production lifecycle and recovery cases pass with local abstention and zero deferred-path activation.</done>
</task>

</tasks>

<threat_model>
This plan primarily mitigates T-60-01, T-60-05, T-60-06, and the production
absence portion of T-60-08. Any second Vision/canonical request, Testing-only
teeth activation, stale request data, suppressed unrelated effect, or
pixel-buffer/Demo/sibling route blocks completion.
</threat_model>

<verification>Run focused facade/foundation suites and isolated integration checker; record actual counts in the summary.</verification>

<success_criteria>The direct public intent is connected to one safe request-local provider invocation on still images only.</success_criteria>

<output>Create `.planning/phases/60-teeth-provider-and-production-integration/60-03-SUMMARY.md` with actual lifecycle counts and unchanged deferred inventories.</output>
