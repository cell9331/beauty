# Phase 48: Face Safety and Scoped Closeout - Research

**Researched:** 2026-07-24
**Domain:** Swift package geometry safety, deterministic retained-set evidence, fail-closed source boundaries, and scoped documentation promotion
**Confidence:** HIGH for repository seams and closeout mechanics; MEDIUM for subjective naturalness (explicitly out of scope)

## Finding

Phase 48 requires no new geometry design. Phase 46 already installed exact provisional `0.25` caps, nine independent face/chin emissions, field-local contour/centerline eligibility, exact reused `0.5`, provider-empty sanitization, and a monotonic 37-field resolver loop. Phase 47 proved the four additions through 413 decoded public outputs. Remaining work is to freeze those contracts with exhaustive requirement-named tests, add a Phase 48 fail-closed promotion checker, rerun all immutable evidence, then promote exactly four rows and synchronize current owners.

## Existing Evidence

- `BeautySafetyCaps.swift` already contains the intended four exact `0.25` constants.
- `BeautyEffectResolverTests` covers representative caps, overflow, no-face/stale, and exact reused `0.125` behavior.
- `FaceShapeWarpProviderTests` proves independent contour continuity, upper-lateral outward temple motion, mid-lateral inward cheekbone motion, and centerline-gated X-only taper.
- `MissingLandmarkDegradationTests` already has a four-field table and representative missing/malformed/provider-empty behavior; Phase 48 must expand the acceptance matrix to the complete nine face/chin fields and explicit transitions.
- `GeometryConflictResolverTests` already mirrors the exact 37-field inventory, total `11.70`, retained count 37, scale `1/11.70`, and signed polarity.
- `CombinedEffectSafetyTests` already enforces one `0..<37` loop and representative post-scale provider-empty removal; Phase 48 must close exact emission/strength equality and field-local masks.
- Phase 47 records 371 full SwiftPM tests, 413/413 outputs, 18/18 visibility/locality, 49/49 neighbor distinctions, 6/6 ineligible no-ops, 4/4 no-face no-ops, and exact ignored gallery containment.
- Phase 40 and Phase 44 are exact evidence-first safety/promotion precedents.

## Gap Analysis

1. Freeze the four constants as final and distinguish zero/exact-cap/overflow/negative/non-finite behavior with exact capped-count and diagnostic assertions.
2. Make the nine-field inventory executable across fresh/reused/stale/no-face, missing/malformed contour, missing/malformed centerline, and provider-empty transitions.
3. Retain the existing 37-field arithmetic proof while connecting final strengths, totals, counts, scale, warnings, metrics, and provider emissions to the same final mask.
4. Add a self-tested Phase 48 checker covering source/API/import/dependency/network/commercial/compatibility/artifact/owner/status/lifecycle boundaries.
5. Rerun unchanged Phase 47 strict output and gallery gates before any product-row mutation.
6. Promote exactly four rows and synchronize only their authoritative owners; preserve three future semantic rows and branch `脸型` partial.

## Standard Stack

| Tool | Use |
|---|---|
| Swift Package Manager + XCTest | Focused and full runtime safety evidence |
| Python 3 standard library | Self-tested boundary/promotion checker |
| `rg`, `git`, `git check-ignore` | Active-source, scope, and generated-artifact gates |
| Existing Phase 47 renderer/helper/gallery | Immutable decoded-output regression |

No package install, network lookup, target, render pass, or resource addition is justified.

## Architecture and Execution Pattern

1. Cap and normalize public scalars.
2. Apply freshness policy exactly once (`0.5` for reused non-eye geometry; zero for stale/no-face).
3. Sanitize all named provider fields against their own support.
4. Resolve one combined scale from one retained baseline.
5. Re-sanitize scaled values; remove only fields that stop emitting.
6. Repeat monotonically for at most 37 removals; never re-add.
7. Derive strengths, domains, warnings, metrics, point count, and dispatch from the final named-emission mask.
8. Keep status owners read-only until runtime, output, boundary, review, security, and validation gates pass.

## Exact Arithmetic

The fully eligible final inventory is:

| Domain | Fields | Exact cap magnitude total |
|---|---:|---:|
| Face/chin | 9 | 3.35 |
| Eye | 14 | 4.10 |
| Nose | 6 | 1.80 |
| Mouth | 8 | 2.45 |
| **Total** | **37** | **11.70** |

The exact scale is `1 / 11.70`; signed `chinLength`, eye, nose, and mouth directions retain polarity. Removal fixtures must subtract only the corresponding final-emitting field and state their own totals/counts/scales.

## Risks and Controls

- Premature promotion: default checker rejects promoted rows; product owners stay read-only through Plans 01-03.
- Proxy borrowing: explicit invalid/missing observed support may not activate any new field.
- Over-removal: shipped compatibility fields and contour-only siblings continue when their own evidence remains valid.
- Double weakening/re-entry: exact loop source gate plus final strength/emission equality.
- False clean scan: subprocess 0/1/>1 states are distinguished and every match must be classified.
- Overclaim: semantic-region rows, complete `脸型`, Demo/device/commercial/performance/packaging/shipping/readiness, and lifecycle claims remain excluded.

## Recommended Plan

Six serial evidence waves match Phase 44:

1. Exact caps, nine-field dependency and transition matrix.
2. Exact 37-field convergence and final emission agreement.
3. Boundary checker, fresh runtime/output evidence, review/security preparation.
4. Atomic four-file promotion.
5. Example and root-owner synchronization.
6. Planning ledger, final phase verification, and milestone-audit handoff.

## Sources Consulted

- Phase 45-47 contexts, verification, security, validation, and output evidence
- Phase 40 and Phase 44 archived safety closeouts and checker implementations
- Current resolver, cap, provider, conflict, pipeline, facade, and test owners
- Current blueprint, example-image, root, and `.planning` owner documents

