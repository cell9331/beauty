---
phase: 48-face-safety-and-scoped-closeout
plan: "02"
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
autonomous: true
requirements: [SAFE-01, SAFE-02]
must_haves:
  truths:
    - "D-12: exact 37-field total 11.70, count 37, and scale 1/11.70 are locked."
    - "D-13..D-15: convergence is monotonic, bounded, once-only, signed, and identical to final named emissions."
  artifacts:
    - path: BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift
      provides: exact retained arithmetic
    - path: BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift
      provides: final mask/emission equality
  key_links:
    - from: BeautyEffectResolver.resolveGeometryConflict
      to: GeometryConflictResolver
      via: one retained unscaled baseline
    - from: final effective strengths
      to: unified warp dispatch
      via: named provider emissions
---

<objective>Freeze exact thirty-seven-field one-baseline arithmetic and prove every final effective field agrees with provider-eligible emitted work without re-entry or double scaling.</objective>

<threat_model><scope>Algorithmic integrity under mixed-domain conflict and provider-empty masks.</scope><blocking_rule>Any count/total/scale mismatch, sign loss, re-entry, second weakening, or strength/emission divergence blocks promotion.</blocking_rule></threat_model>

<tasks>
<task type="test">
  <name>Task 48-02-01: Lock complete exact retained-set arithmetic</name>
  <decision_refs>D-12,D-15</decision_refs>
  <files>GeometryConflictResolverTests.swift</files>
  <action>Make the existing 37-row table the final Phase 48 oracle. Assert nine face/chin + fourteen eye + six nose + eight mouth unique rows, exact domain subtotals 3.35/4.10/1.80/2.45, total 11.70, count/weakenedCount 37, reciprocal scale, total 1 after weakening, warning shape, and signed polarity. Preserve below-threshold no-warning behavior.</action>
  <done>The final exact inventory, subtotals, total, count, scale, warning, and sign assertions pass.</done>
  <acceptance_criteria>Every field occurs exactly once; exact arithmetic and signs pass at tight tolerance; no new public metric exists.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.GeometryConflictResolverTests &amp;&amp; git diff --check</automated></verify>
</task>
<task type="test">
  <name>Task 48-02-02: Prove 37-removal convergence and final provider equality</name>
  <decision_refs>D-13,D-14,D-15</decision_refs>
  <files>CombinedEffectSafetyTests.swift, MissingLandmarkDegradationTests.swift</files>
  <action>Assert the source contains exactly one `0..&lt;37` monotone loop and no alternate bound. Extend complete and adversarial support fixtures so each final nonzero effective field has a non-empty named provider emission and each empty field is zero in totals/counts/warnings/metrics/domains/point count/dispatch. Include late provider-empty face/chin, missing contour/centerline, reused, and signed mixed-domain cases with exact retained arithmetic.</action>
  <done>The loop, final masks, exact arithmetic, provider equality, and mixed-domain assertions pass.</done>
  <acceptance_criteria>Loop is bounded and monotone; removed fields never return; exact final masks, strengths, emissions, domains, warnings, metrics, point counts, and dispatch agree.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.CombinedEffectSafetyTests &amp;&amp; swift test --package-path BeautySDK --filter BeautyEffectsTests.MissingLandmarkDegradationTests &amp;&amp; git diff --check</automated></verify>
</task>
</tasks>

<verification>Run conflict, combined, degradation, provider, resolver, facade, and full SwiftPM suites. No production conflict path may be added.</verification>
<success_criteria>SAFE-02 is executable with exact final arithmetic and field-level dispatch agreement.</success_criteria>
