---
phase: 48-face-safety-and-scoped-closeout
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautySafetyCapsTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift
autonomous: true
requirements: [SAFE-01]
must_haves:
  truths:
    - "D-01..D-05: four exact final 0.25 caps and neutral/count semantics are executable."
    - "D-06..D-11: all nine face/chin fields have exhaustive field-local support, freshness, provider-empty, transition, and redaction evidence."
  artifacts:
    - path: BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
      provides: exact cap/count and freshness evidence
    - path: BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
      provides: nine-field support and transition matrix
  key_links:
    - from: BeautySafetyCaps
      to: BeautyEffectResolver
      via: exact capUnit/capSigned ownership
    - from: named face/chin emissions
      to: final BeautyEffectPlan
      via: field-local sanitization
---

<objective>Freeze the four evidence-backed face caps and prove exact nine-field normalization, support dependency, freshness, provider-empty, transition, safe-sibling, and public-redaction behavior.</objective>

<threat_model><scope>Untrusted public scalars and biometric-adjacent request-local support.</scope><blocking_rule>Any non-finite bypass, proxy borrowing, stale carryover, raw geometry disclosure, or strength/emission mismatch blocks downstream plans.</blocking_rule><mitigations>Exact tables, named emissions, stateless transitions, focused/full tests.</mitigations></threat_model>

<tasks>
<task type="test">
  <name>Task 48-01-01: Lock exact final caps and capped-count semantics</name>
  <decision_refs>D-01,D-02,D-03,D-04,D-05</decision_refs>
  <files>BeautySafetyCaps.swift, BeautySafetyCapsTests.swift, BeautyEffectResolverTests.swift</files>
  <action>Add one exact four-row cap table and resolver cases for zero, exact cap, overflow, negative, NaN, positive infinity, and negative infinity. Assert exact effective values, capped count/warning/metric, active work, and aggregate-only diagnostics. Once green, replace only the Phase 46 provisional cap comment with Phase 48 final ownership.</action>
  <done>Exact constants, input classes, counts, diagnostics, focused tests, and diff hygiene pass.</done>
  <acceptance_criteria>All four constants are exactly 0.25; exact-cap does not count as capped; overflow counts once; invalid/negative values are zero; shipped cap/vector assertions remain unchanged.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.BeautySafetyCapsTests &amp;&amp; swift test --package-path BeautySDK --filter BeautyEffectsTests.BeautyEffectResolverTests &amp;&amp; git diff --check</automated></verify>
</task>
<task type="test">
  <name>Task 48-01-02: Prove the complete nine-field support and provider-empty matrix</name>
  <decision_refs>D-06,D-07,D-08,D-09</decision_refs>
  <files>FaceShapeWarpProviderTests.swift, MissingLandmarkDegradationTests.swift</files>
  <action>Create one descriptor table for five shipped and four new fields. Assert fresh named emission, exact reused half-strength when eligible, stale/no-face zero, contour missing/malformed removal of only the four new fields, centerline missing/malformed removal of only chinTaper, proxy compatibility only for shipped fields, and provider-empty removal before every aggregate/dispatch surface.</action>
  <done>The nine-row dependency/provider-empty matrix passes with field-local continuation.</done>
  <acceptance_criteria>Nine unique names are covered; support failures remove only dependents; valid siblings continue; provider-empty final strengths and emissions agree.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.FaceShapeWarpProviderTests &amp;&amp; swift test --package-path BeautySDK --filter BeautyEffectsTests.MissingLandmarkDegradationTests &amp;&amp; git diff --check</automated></verify>
</task>
<task type="test">
  <name>Task 48-01-03: Cover stateless transitions, safe siblings, and facade redaction</name>
  <decision_refs>D-10,D-11</decision_refs>
  <files>MissingLandmarkDegradationTests.swift, BeautyEngineGeometryFacadeTests.swift</files>
  <action>Exercise every locked transition in both directions using independent resolver/facade calls. Assert no prior work carries forward, safe color/filter and eligible sibling domains continue, public output extent remains unchanged, and warning/metric/result descriptions contain no geometry details or per-field failure payloads.</action>
  <done>All transitions, sibling continuation, extent, and redaction assertions pass.</done>
  <acceptance_criteria>All named transitions pass deterministically; no stale vectors or support state survive; facade remains scalar-only and redacted.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.MissingLandmarkDegradationTests &amp;&amp; swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyEngineGeometryFacadeTests &amp;&amp; git diff --check</automated></verify>
</task>
</tasks>

<verification>Run all focused suites and then full SwiftPM. Confirm no public API, Demo, dependency, renderer, resource, or generated-artifact drift.</verification>
<success_criteria>SAFE-01 runtime evidence is complete and green without promoting documentation.</success_criteria>
