---
phase: 49-public-contract-and-observed-eyebrow-support
plan: "04"
type: execute
wave: 3
depends_on: ["49-01", "49-03"]
files_modified:
  - BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautyFaceGeometryAdapterTests.swift
autonomous: true
requirements: [SUPP-02, SUPP-03]
must_haves:
  truths:
    - "D-10: each canonical eyebrow is independently accepted only with 4...16 exact-bit-unique finite closed-unit points, endpoint chord 0.08...0.50 face width, vertical span no greater than 0.25 face height, nondegenerate derived math, and no non-adjacent open-polyline intersection."
    - "D-09/D-11: the adapter consumes Plan 49-03 anatomical slots and inner-to-outer adjacency without sorting, screen-axis recanonicalization, endpoint closure, winding, area, hull, or reuse of closed-eye topology constants."
    - "D-12: left and right traces validate independently; valid-left/invalid-right and inverse retain the valid semantic trace, while pairedEligible is true only for two valid distinct sides."
    - "D-08/D-16: observed eyebrow semantic support attaches beside existing face/eye/nose/lip and seven-point face compatibility paths, enters no provider/resolver/facade/renderer surface, and cannot be populated from an eye/synthetic proxy."
    - "D-13/D-14/D-15: raw/derived traces, endpoints, centers, and apex candidates remain immutable target-internal request values; description, reflection, Mirror, dump, warnings, and metrics expose fixed availability/counts only, and valid-invalid-valid calls retain no prior support."
  artifacts:
    - path: BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift
      provides: "Brow-specific pure open-path validators, independent semantic trace construction, and one optional FaceGeometry attachment"
    - path: BeautySDK/Tests/BeautyEffectsTests/BeautyFaceGeometryAdapterTests.swift
      provides: "Exact count/span/chord/topology/side/eligibility/privacy/sibling/lifecycle matrices"
  key_links:
    - from: BeautyFaceObservation.observedEyebrowSupport
      to: BeautyFaceGeometryAdapter eyebrow validators
      via: "independent left/right validation against exact positive face bounds"
      pattern: "observedEyebrowSupport"
    - from: validated canonical side traces
      to: FaceGeometry.observedEyebrowSupport
      via: "one BeautyEyebrowSemanticSupport attachment with per-side optionals and derived pairedEligible"
      pattern: "BeautyEyebrowSemanticSupport"
    - from: BeautyFaceGeometryAdapter.makeGeometry(from:)
      to: shipped face/eye/nose/lip arrays
      via: "unchanged existing construction regardless of invalid optional eyebrow support"
      pattern: "makeGeometry"
  prohibitions:
    - statement: "Eyebrow validation must not reuse closed-eye contour counts, polygon area/winding, endpoint closure, screen-axis sorting, cross-side symmetry ratios, or the shipped eye/face proxy arrays."
      status: required
      verification: "Boundary tables, exact sibling comparisons, source-token checker, and no-fallback fixtures."
    - statement: "Raw or semantic coordinates, endpoints, apex indices, arrays, CGPoint/SIMD values, hashes, stable signatures, or biometric/profile-like values must not escape diagnostics or reflection."
      status: required
      verification: "Leaf/parent description, reflection, Mirror, dump, warning/metric negative tests and live checker."
    - statement: "No public/SPI support type, Codable/persistence/cache/network path, provider/effective-strength/cap/resolver/facade/renderer/Demo code, or product-row promotion may be added."
      status: required
      verification: "Files_modified ownership plus Phase 49 live boundary checker."
---

<objective>
Validate mapped eyebrow traces as independent open paths and attach private semantic support without activating any eyebrow effect.

Purpose: Give Phase 50 trustworthy per-side and paired evidence while preserving every shipped geometry path, privacy boundary, and downstream nonclaim.
Output: Exact pure topology validators, internal semantic trace attachment, aggregate-only diagnostics, and malformed/sibling/lifecycle evidence.
</objective>

## Artifacts this phase produces

- Brow-specific acceptance constants and open-polyline helpers inside `BeautyFaceGeometryAdapter`.
- Independent conversion from mapped `BeautyObservedEyebrowSupport.left/right` arrays into `BeautyEyebrowSemanticTrace` values and one optional `BeautyEyebrowSemanticSupport` attachment on `FaceGeometry`.
- Derived per-trace inner/outer endpoints, arithmetic center, and optional deterministic interior apex index without provider eligibility or visible output.
- SUPP-02/SUPP-03 tests covering exact boundaries, malformed topology, per-side versus paired eligibility, sibling equality, redaction, and stateless calls.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<read_first>
AGENTS.md; PLANS.md; .planning/PROJECT.md; .planning/ROADMAP.md; .planning/REQUIREMENTS.md; .planning/STATE.md; .planning/phases/49-public-contract-and-observed-eyebrow-support/49-CONTEXT.md; .planning/phases/49-public-contract-and-observed-eyebrow-support/49-RESEARCH.md; .planning/phases/49-public-contract-and-observed-eyebrow-support/49-PATTERNS.md; .planning/phases/49-public-contract-and-observed-eyebrow-support/49-VALIDATION.md; 49-01-SUMMARY.md; 49-03-SUMMARY.md; BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift; BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift; BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift; BeautySDK/Tests/BeautyEffectsTests/BeautyFaceGeometryAdapterTests.swift
</read_first>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/49-public-contract-and-observed-eyebrow-support/49-CONTEXT.md
@.planning/phases/49-public-contract-and-observed-eyebrow-support/49-RESEARCH.md
@.planning/phases/49-public-contract-and-observed-eyebrow-support/49-PATTERNS.md
@BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift
@BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift
@BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift
@BeautySDK/Tests/BeautyEffectsTests/BeautyFaceGeometryAdapterTests.swift

<interfaces>
Plan 49-01 supplies default-nil `FaceGeometry.observedEyebrowSupport`, `BeautyEyebrowSemanticTrace(side:points:innerEndpoint:outerEndpoint:center:apexIndex:)`, and `BeautyEyebrowSemanticSupport(left:right:)` with computed `pairedEligible`. Plan 49-03 supplies package-only image-normalized `BeautyFaceObservation.observedEyebrowSupport` whose left/right arrays already passed actual-source preflight and mapper-axis canonicalization. `BeautyFaceGeometryAdapter.makeGeometry(from:)` must keep building bounds, the exact shipped face contour, observed face support, observed/shipped eyes, nose/root/tip, lips, and freshness exactly as before, then independently attach eyebrow semantics. Follow the current face-specific exact-bit/open-path helpers, but use brow-named constants and never reuse closed-eye or face-contour thresholds.
</interfaces>
</context>

## Plan-local source coverage

| Source | Coverage |
| --- | --- |
| GOAL | Completes trustworthy private observed eyebrow support while preserving shipped behavior. |
| REQ | SUPP-02 receives exact open-path validation and independent/paired eligibility; SUPP-03 receives private attachment, redaction, and stateless lifecycle evidence. |
| RESEARCH | Applies 4...16, chord/span/epsilon defaults, exact-bit uniqueness, non-adjacent open-path checks, optional apex representation, no symmetry gate, and no closed-eye reuse. |
| CONTEXT | D-08 through D-16 and D-17 are implemented or guarded; every provider/output/promotion claim remains deferred. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 49-04-01: Lock independent eyebrow open-path validation</name>
  <files>BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyFaceGeometryAdapterTests.swift</files>
  <read_first>AGENTS.md; PLANS.md; .planning/phases/49-public-contract-and-observed-eyebrow-support/49-CONTEXT.md; .planning/phases/49-public-contract-and-observed-eyebrow-support/49-RESEARCH.md; 49-01-SUMMARY.md; 49-03-SUMMARY.md; BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift; BeautySDK/Tests/BeautyEffectsTests/BeautyFaceGeometryAdapterTests.swift</read_first>
  <behavior>
    - Counts 4, 5, 15, and 16 pass count policy; 3 and 17 fail. Exact finite unit edges may pass, while out-of-unit, NaN, infinities, and exact-bit duplicates fail.
    - Endpoint chord exactly 0.08 and 0.50 face width passes; shorter/longer/zero chord fails. Vertical span exactly 0.25 face height passes; greater span fails. Derived non-finite or epsilon-degenerate values fail.
    - An open non-self-intersecting adjacent trace passes; any non-adjacent crossing fails without adding a last-to-first segment, polygon area/winding, hull, sort, or eye-topology rule.
    - Each accepted trace preserves the exact canonical point order, inner/outer endpoints, arithmetic center, and an optional deterministic interior apex index; validators never mutate or clamp input points.
  </behavior>
  <action>Per D-09/D-10/D-11/D-12/D-13/D-17, first add one RED boundary table for every exact/equal/outside case compiled in Plan 49-01. Introduce brow-named constants in `BeautyFaceGeometryAdapter`: accepted count `4...16`, endpoint chord `0.08...0.50` in face-width units, vertical span at most `0.25` in face-height units, and finite projection/derived magnitude greater than `1e-6`. Build pure helpers that require exact positive face bounds, finite closed-unit `CoordinatePoint`s, exact-bit uniqueness, finite face-relative math, nondegenerate endpoints, and no intersection between non-adjacent segments of the open polyline. Do not close last-to-first, sort, calculate polygon area/winding/hull, reuse eye/face constants, clamp points, or impose cross-side width/symmetry. Preserve the already canonical array exactly. Derive `innerEndpoint = points.first`, `outerEndpoint = points.last`, arithmetic `center`, and `apexIndex` only when a unique interior point has the greatest finite face-up perpendicular displacement from the endpoint chord above the epsilon; otherwise keep apex evidence nil. Treat apex selection as private evidence only, never as Phase 50 provider eligibility.</action>
  <acceptance_criteria>Every boundary row has an exact pass/fail assertion; accepted arrays retain order and values; malformed count/coordinate/duplicate/chord/span/intersection/derived cases fail deterministically; no closed-contour, sorting, symmetry, proxy, or provider behavior appears.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.BeautyFaceGeometryAdapterTests &amp;&amp; python3 .planning/phases/49-public-contract-and-observed-eyebrow-support/check_eyebrow_support_boundaries.py --self-test &amp;&amp; git diff --check</automated></verify>
  <commit_expectation>Commit RED as `test(49-04): specify eyebrow open-path boundaries`, then GREEN as `feat(49-04): validate canonical eyebrow traces`; do not squash.</commit_expectation>
  <done>SUPP-02 has a bounded, exact, open-path validator whose accepted representation is deterministic and independent per side.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 49-04-02: Attach semantic support with local failure, redaction, and stateless lifetime</name>
  <files>BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyFaceGeometryAdapterTests.swift</files>
  <read_first>AGENTS.md; PLANS.md; .planning/phases/49-public-contract-and-observed-eyebrow-support/49-CONTEXT.md; 49-01-SUMMARY.md; 49-03-SUMMARY.md; BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift; BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift; BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift; BeautySDK/Tests/BeautyEffectsTests/BeautyFaceGeometryAdapterTests.swift</read_first>
  <behavior>
    - Neither side yields nil observed semantic support; one valid side yields that side only and pairedEligible false; two valid distinct sides yield both and pairedEligible true.
    - A malformed side cannot erase the valid side or change faceContour, observedFaceSupport, left/right eye arrays/support, nose/root/tip, lips, bounds, center fallback, or freshness.
    - Alternating valid→invalid→valid, repeated, stale/no-face, and independent parallel geometry constructions use only their call's support and never retain a prior trace.
    - String(describing:), String(reflecting:), Mirror, dump, warnings, metrics, and captured failures expose only fixed labels, side availability, paired eligibility, and aggregate counts—never points/endpoints/center/apex/raw numbers/signatures.
  </behavior>
  <action>Per D-08/D-10/D-12/D-13/D-14/D-15/D-16/D-17, validate `BeautyFaceObservation.observedEyebrowSupport.left` and `.right` independently inside `makeGeometry(from:)`, assign the matching `BeautyObservedEyebrowSide`, retain a semantic envelope when either side survives, and let the Plan 49-01 `pairedEligible` computation become true only for two non-nil distinct canonical sides. Keep absent or malformed eyebrow support optional and never early-return, erase, replace, or reconstruct any existing geometry sibling. Add exact four-presence combinations, valid-left/invalid-right and inverse, wrong-side/wrong-order direct fixtures, complete shipped sibling equality, valid-invalid-valid, repeated/stale/no-face, and independent parallel construction tests. Exercise leaf/envelope/`BeautyFaceObservation`/`FaceGeometry` descriptions, reflection, Mirror, and dump plus warning/metric surfaces; require only fixed booleans/counts and reject coordinate fragments, arrays, CGPoint/SIMD names, endpoint/apex values, hashes, or stable signatures. Add no warning/metric reason containing geometry, no Codable/persistence/cache/network state, and no consumer in parameters, provider, resolver, conflict, facade, renderer, or Demo.</action>
  <acceptance_criteria>Per-side and paired eligibility match the four presence combinations; malformed input is local and stateless; every shipped sibling remains exactly equal; all diagnostic surfaces are aggregate-only; checker live markers can prove private lifecycle and no downstream consumer.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.BeautyFaceGeometryAdapterTests &amp;&amp; swift test --package-path BeautySDK --filter BeautyDetectionTests &amp;&amp; python3 .planning/phases/49-public-contract-and-observed-eyebrow-support/check_eyebrow_support_boundaries.py --self-test &amp;&amp; git diff --check</automated></verify>
  <commit_expectation>Commit RED as `test(49-04): specify eyebrow eligibility and privacy`, then GREEN as `feat(49-04): attach private eyebrow semantics`; do not squash.</commit_expectation>
  <done>SUPP-02/SUPP-03 have independent private semantic support with paired eligibility, aggregate diagnostics, sibling preservation, and no shared lifetime.</done>
</task>

</tasks>

<threat_model>
<blocking_rule>ASVS L1: any unresolved HIGH finding, unclassified checker result, malformed-input acceptance outside the exact envelope, proxy substitution, sibling regression, coordinate disclosure, stale carryover, or required-command failure blocks this plan and every dependent wave; HIGH threats may not be accepted or transferred.</blocking_rule>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Mapped optional eyebrow arrays → effects adapter | Coordinates remain untrusted until brow-specific count, finite/unit, uniqueness, chord/span, and open-topology checks pass. |
| Optional eyebrow semantics → shipped FaceGeometry siblings | Malformed new support must not replace, erase, or alter established face/eye/nose/lip behavior. |
| Private semantic values → diagnostics and later requests | Derived endpoints/center/apex and arrays must remain aggregate-only and request-scoped. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-49-16 | Tampering / Denial of Service | open-path validator | mitigate | Fixed 4...16 counts, finite/unit/exact-bit checks, bounded derived math, exact chord/span limits, and non-adjacent intersection matrix. |
| T-49-17 | Spoofing | side/proxy semantic attachment | mitigate | Distinct eyebrow types/slots, no eye/face fallback, wrong-side/order fixtures, and exact shipped sibling equality. |
| T-49-18 | Tampering | independent/paired eligibility | mitigate | Four presence combinations and valid/invalid side matrices; paired eligibility derives only from two distinct valid sides. |
| T-49-19 | Information Disclosure | semantic carriers/parents | mitigate | Target-internal immutable values, transitive redacted diagnostics/reflection/dump tests, and no warning/metric geometry. |
| T-49-20 | Repudiation | stateless lifecycle | mitigate | Pure per-call adapter, valid-invalid-valid/stale/no-face/parallel tests, and no cache/persistence/network path. |
| T-49-SC | Tampering | dependency/model/resource fallback | mitigate | No install/manifest/resource edit; checker rejects dependency, model, resource, provider, and network scope. |
</threat_model>

<verification>
Run the focused adapter suite after each task, then all detection tests to preserve the mapped-support contract. At wave completion run checker self-tests and inspect the diff: only the adapter and its owning test file may change, every exact boundary and sibling comparison must pass, and no public/Codable/provider/resolver/facade/renderer/Demo/persistence/network surface may consume the support.
</verification>

<success_criteria>
Mapped brows become independently validated private open-path semantics with truthful per-side and paired eligibility, exact bounded topology, aggregate-only diagnostics, stateless lifecycle, and zero change to shipped geometry or downstream behavior.
</success_criteria>

<output>Create `49-04-SUMMARY.md` with final constants, exact boundary matrix results, four eligibility combinations, sibling equality/privacy/lifecycle results, focused test counts, atomic commit hashes, and explicit no-provider/no-output/no-promotion status.</output>
