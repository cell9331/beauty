---
phase: 49-public-contract-and-observed-eyebrow-support
plan: "02"
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift
  - BeautySDK/Tests/BeautyResourcesTests/BeautyResourceCatalogTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
autonomous: true
requirements: [BROW-01, BROW-02]
must_haves:
  truths:
    - "D-01/D-02: exactly eyebrowYPosition, eyebrowThickness, eyebrowLength, eyebrowSpacing, eyebrowHeadSpacing, eyebrowTilt, and eyebrowPeakDefinition are independent public Float fields; the first six normalize to -1...1 and peak normalizes to 0...1, with every non-finite value becoming zero."
    - "D-03: BeautyParameters is exactly 59 stored fields, 58 numeric plus filterId; source omission and missing JSON keys produce seven zeros."
    - "Legacy 52-key JSON decodes seven zeros, a complete unequal 59-key payload round-trips exactly, historical 31/33/38/48/52 fixture counts retain their meanings, and all five bundled preset bytes remain unchanged with no eyebrow key."
    - "Equality, mutable-copy normalization, .init() reset, synthesized snapshot diff behavior, and non-mutating normalized() behavior include all seven fields without aliases."
  artifacts:
    - path: BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift
      provides: "Exact seven-field public model and 59/58 compatibility contract"
    - path: BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift
      provides: "Ranges, defaults, independence, inventory, reset/diff, legacy, and round-trip evidence"
    - path: BeautySDK/Tests/BeautyResourcesTests/BeautyResourceCatalogTests.swift
      provides: "Five-preset missing-key neutrality without resource edits"
    - path: BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
      provides: "Nonzero seven-field Phase 49 inertness and shipped-plan equality"
  key_links:
    - from: BeautyParameters stored properties
      to: CodingKeys, init, decode, and normalized
      via: "the same seven identifiers forwarded exactly once through every manual lifecycle seam"
      pattern: "eyebrowYPosition|eyebrowThickness|eyebrowLength|eyebrowSpacing|eyebrowHeadSpacing|eyebrowTilt|eyebrowPeakDefinition"
    - from: bundled preset JSON
      to: BeautyResourceCatalog decode
      via: "absent keys use decodeFloatIfPresent and remain zero"
      pattern: "decodeFloatIfPresent"
  prohibitions:
    - statement: "The seven fields must not enter requiresFaceGeometry, effective strengths, caps, providers, resolver cases, facade routing, renderer/gallery output, or product-row promotion in Phase 49."
      status: required
      verification: "Nonzero inertness test plus live boundary checker production-source allowlists."
    - statement: "Bundled preset files and their five pinned byte hashes must not change or gain eyebrow keys."
      status: required
      verification: "Resource tests and checker hash/key-absence gate."
    - statement: "No alias, computed proxy, dependency, model, resource, network, persistence, or Demo/UI behavior may be added for the public fields."
      status: required
      verification: "Exact reflected/encoded inventory, resolver equality, and checker scope scan."
---

<objective>
Deliver the exact compatibility-safe 52-to-59 public eyebrow contract while keeping every value runtime-inert.

Purpose: Let SDK hosts store, normalize, compare, reset, diff, and serialize seven independent eyebrow intents without changing shipped behavior or legacy/preset payloads.
Output: Seven normalized public fields plus focused model, resource, and resolver evidence.
</objective>

## Artifacts this phase produces

- Public properties, coding keys, defaulted initializer labels, clamped assignments, missing-key decode calls, and normalized-copy forwarding for `eyebrowYPosition`, `eyebrowThickness`, `eyebrowLength`, `eyebrowSpacing`, `eyebrowHeadSpacing`, `eyebrowTilt`, and `eyebrowPeakDefinition`.
- BROW-01/BROW-02 tests in the three existing test owners; no new source file, public support type, provider, resolver production case, preset key, or output case.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<read_first>
AGENTS.md; PLANS.md; .planning/PROJECT.md; .planning/ROADMAP.md; .planning/REQUIREMENTS.md; .planning/STATE.md; .planning/phases/49-public-contract-and-observed-eyebrow-support/49-CONTEXT.md; .planning/phases/49-public-contract-and-observed-eyebrow-support/49-RESEARCH.md; .planning/phases/49-public-contract-and-observed-eyebrow-support/49-PATTERNS.md; 49-01-SUMMARY.md; BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift; BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift; BeautySDK/Tests/BeautyResourcesTests/BeautyResourceCatalogTests.swift; BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
</read_first>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/49-public-contract-and-observed-eyebrow-support/49-CONTEXT.md
@.planning/phases/49-public-contract-and-observed-eyebrow-support/49-RESEARCH.md
@.planning/phases/49-public-contract-and-observed-eyebrow-support/49-PATTERNS.md
@BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift
@BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift
@BeautySDK/Tests/BeautyResourcesTests/BeautyResourceCatalogTests.swift
@BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift

<interfaces>
`BeautyParameters` manually owns stored properties, `CodingKeys`, defaulted public initializer labels, clamp assignments, `init(from:)`, and `normalized()`. Use existing `clampSigned` for the first six exact names, `clampUnit` only for `eyebrowPeakDefinition`, and `decodeFloatIfPresent` for all seven. Reset is `BeautyParameters()`, equality/diff evidence is synthesized `Equatable` snapshot comparison, and the resolver must remain production-unchanged.
</interfaces>
</context>

## Plan-local source coverage

| Source | Coverage |
| --- | --- |
| GOAL | Establishes exact compatibility-safe public eyebrow semantics. |
| REQ | BROW-01 gets seven independent domains/defaults; BROW-02 gets exact inventory/source/legacy/preset/equality/reset/diff/round-trip compatibility. |
| RESEARCH | Applies the manual model lifecycle, exact current-versus-historical counts, five-preset neutrality, and inertness pattern. |
| CONTEXT | D-01 through D-04 and D-16 are directly implemented or guarded. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 49-02-01: Add seven independent normalized public fields</name>
  <files>BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift, BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift</files>
  <read_first>AGENTS.md; PLANS.md; .planning/phases/49-public-contract-and-observed-eyebrow-support/49-CONTEXT.md; .planning/phases/49-public-contract-and-observed-eyebrow-support/49-RESEARCH.md; BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift; BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift</read_first>
  <behavior>
    - For each signed field: -2→-1, -1→-1, 0→0, 1→1, 2→1, and NaN/+infinity/-infinity→0.
    - For peak definition: -2/-1→0, 0→0, 1→1, 2→1, and NaN/+infinity/-infinity→0.
    - Seven pairwise unequal finite values survive direct initialization, mutable assignment followed by normalized(), equality comparison, and JSON round trip without aliasing.
    - `BeautyParameters()` resets all seven to zero and `normalized()` never mutates its source.
  </behavior>
  <action>Per D-01/D-02/D-03, first commit BROW-labeled failing table tests, then wire exactly the seven declared identifiers through stored properties, CodingKeys, defaulted initializer labels, assignments, decoder arguments, and normalized-copy arguments. Place them as one eyebrow group without reordering or changing existing fields. Use `clampSigned` for `eyebrowYPosition`, `eyebrowThickness`, `eyebrowLength`, `eyebrowSpacing`, `eyebrowHeadSpacing`, and `eyebrowTilt`; use `clampUnit` only for `eyebrowPeakDefinition`. Update only current full-model reflected/encoded counts from 52 to 59 and numeric count from 51 to 58; preserve every historical literal count. Add no reset/diff API, alias, wrapper, cap, provider, routing, facade, renderer, or public geometry type.</action>
  <acceptance_criteria>All seven identifiers compile through every manual seam, exact 59/58 inventory passes, signed/unit/non-finite tables pass independently, source omission/reset yields seven zeros, normalized copies are non-mutating, and pairwise unequal equality detects every field.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyParametersTests &amp;&amp; git diff --check</automated></verify>
  <commit_expectation>Commit RED as `test(49-02): specify seven-field eyebrow parameter contract`, then GREEN as `feat(49-02): add neutral eyebrow parameter storage`; do not squash.</commit_expectation>
  <done>BROW-01 has exact public names, ranges, defaults, independence, and finite behavior without runtime activation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 49-02-02: Prove legacy, preset, reset-diff, round-trip, and runtime neutrality</name>
  <files>BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift, BeautySDK/Tests/BeautyResourcesTests/BeautyResourceCatalogTests.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift</files>
  <read_first>AGENTS.md; PLANS.md; .planning/phases/49-public-contract-and-observed-eyebrow-support/49-CONTEXT.md; BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift; BeautySDK/Tests/BeautyResourcesTests/BeautyResourceCatalogTests.swift; BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift; 49-01-SUMMARY.md</read_first>
  <behavior>
    - A complete 59-key payload with seven unequal values round-trips exactly; removing all seven keys produces exactly 52 keys and decodes seven zeros.
    - Historical 31/33/38/48/52 fixtures retain their intended literal counts; only the current full model is 59.
    - Exactly five bundled presets omit all seven keys, retain pinned bytes/hashes, and decode seven zeros.
    - One input with all seven nonzero values leaves requiresFaceGeometry and the complete shipped resolver plan/emissions exactly equal to the same input with all seven zero.
  </behavior>
  <action>Per D-03/D-04/D-16, write compatibility and inertness tests before any implementation adjustment. In core tests add exact Mirror/encoded 59 counts, unequal round trip, a real complete 52-key payload with no brow keys, seven-zero decode, `.init()` reset, equality/snapshot-diff evidence, and explicit guards that distinguish historical 31/33/38/48/52 fixtures from the current model. In resource tests assert exactly the existing five preset IDs and seven decoded zeros; do not edit preset JSON. In resolver tests pass all seven fields nonzero together and assert `requiresFaceGeometry`, effective strengths, domains, warnings, metrics, named emissions, and control points are identical to the all-seven-zero request. Keep resolver and provider production sources unchanged.</action>
  <acceptance_criteria>59-key independence, legacy-52 neutrality, unchanged historical fixtures, exactly five byte-stable neutral presets, reset/diff/equality behavior, and nonzero Phase 49 inertness all pass.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyParametersTests &amp;&amp; swift test --package-path BeautySDK --filter BeautyResourcesTests.BeautyResourceCatalogTests &amp;&amp; swift test --package-path BeautySDK --filter BeautyEffectsTests.BeautyEffectResolverTests &amp;&amp; python3 .planning/phases/49-public-contract-and-observed-eyebrow-support/check_eyebrow_support_boundaries.py --self-test &amp;&amp; git diff --check</automated></verify>
  <commit_expectation>Commit tests and any directly required model correction atomically as `test(49-02): prove eyebrow compatibility and inertness`; do not modify resolver production or preset files.</commit_expectation>
  <done>BROW-02 is executable without implying provider, facade, output, renderer, or promotion readiness.</done>
</task>

</tasks>

<threat_model>
<blocking_rule>ASVS L1: any unresolved HIGH finding, unclassified checker result, compatibility regression, premature runtime activation, or required-command failure blocks this plan and every dependent wave; HIGH threats may not be accepted or transferred.</blocking_rule>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Host Float/JSON → BeautyParameters | Untrusted numeric and serialized values enter a public Codable model. |
| Public model → existing resolver | New nonzero intent must remain inert until Phase 50 explicitly activates it. |
| Bundled bytes → resource decode | Preset mutation could silently opt existing hosts into eyebrow behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-49-06 | Tampering | initializer/decode/normalized | mitigate | Exact boundary/non-finite tables and non-mutating normalized-copy tests. |
| T-49-07 | Spoofing | independent fields | mitigate | Exact identifiers, pairwise unequal 59-key round trip, and equality/diff tests reject aliases. |
| T-49-08 | Denial of Service | legacy/preset decode | mitigate | Reuse bounded Codable/resource paths and missing-key zero behavior; add no parser/resource. |
| T-49-09 | Elevation of Privilege | premature resolver activation | mitigate | Nonzero seven-field full-plan equality plus live forbidden-source scan. |
| T-49-10 | Tampering | bundled presets | mitigate | Five pinned hashes and eyebrow-key absence; no preset file ownership in this plan. |
| T-49-SC | Tampering | package supply chain | mitigate | No install/manifest edit; Plan 49-01 checker blocks dependency/target/resource drift. |
</threat_model>

<verification>
Run the three focused suites, checker self-tests, and `git diff --check`. At wave completion inspect `git diff --name-only` and fail if any preset JSON, Package.swift, provider/resolver production, facade, renderer, gallery, Demo, model, resource, network, or persistence file changed.
</verification>

<success_criteria>
Hosts receive exactly seven independent neutral eyebrow values in a 59-field model; legacy source/JSON/presets, equality/reset/diff/round-trip behavior, and shipped resolver output remain compatible and inert.
</success_criteria>

<output>Create `49-02-SUMMARY.md` with exact 59/58 and legacy counts, seven range tables, five preset results/hashes, resolver inertness evidence, focused/full test status, atomic commit hashes, and explicit no-activation scope.</output>
