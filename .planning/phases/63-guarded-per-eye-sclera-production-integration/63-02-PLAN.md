---
phase: 63-guarded-per-eye-sclera-production-integration
plan: "02"
type: execute
wave: 2
depends_on: [63-01]
files_modified:
  - BeautySDK/Sources/BeautyEffects/LocalRetouch/BeautyScleraRednessProvider.swift
  - BeautySDK/Sources/BeautyEffects/LocalRetouch/BeautyScleraRednessTransform.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautyScleraRednessProviderTests.swift
autonomous: true
requirements: [SCLERA-09, SCLERA-10, SCLERA-11, SCLERA-12, SCLERA-13]
security_standard: OWASP ASVS Level 1
block_on: HIGH
must_haves:
  truths:
    - "The stateless provider consumes only canonical-order actual mapped support and independently emits stable left-then-right zero/one/two units."
    - "Every accepted pixel lies in a pre-score anatomy guard and remains in that exact guard after feathering."
    - "Every target derives from immutable source bytes, reduces only measured positive red excess, restores bounded luminance and applies soft weight exactly once."
    - "Empty, threshold-adjacent, malformed, unchanged or unsafe eyes abstain locally without weakening a peer guard."
  prohibitions:
    - statement: "The provider must not guess, mirror, cache or borrow contour/pupil geometry, nor modify iris, pupil, highlight, lash, skin or aperture-exterior pixels."
      status: unverified
      verification: flagged
    - statement: "Production types must not expose public/SPI/Codable masks, geometry, source colors, pixels or vein-like diagnostics."
      status: unverified
      verification: flagged
    - statement: "Thresholds or protection margins must not be relaxed to recover visible coverage after a failed fixture or test."
      status: unverified
      verification: flagged
  artifacts:
    - path: BeautySDK/Sources/BeautyEffects/LocalRetouch/BeautyScleraRednessProvider.swift
      provides: Stateless independently guarded per-eye selector and zero-to-two owner units
    - path: BeautySDK/Sources/BeautyEffects/LocalRetouch/BeautyScleraRednessTransform.swift
      provides: Bounded immutable-source red-excess target
    - path: BeautySDK/Tests/BeautyEffectsTests/BeautyScleraRednessProviderTests.swift
      provides: Green geometry, containment, transform, rounding and peer-isolation tests
---

<objective>
Implement the package-only per-eye sclera provider and bounded source transform
against the frozen Wave 0 contracts.

Purpose: Convert admitted intent and current eye support into safe composition
units without facade routing or output promotion.
Output: Green deterministic provider/transform/containment tests.
</objective>

<context>
@.planning/phases/63-guarded-per-eye-sclera-production-integration/63-CONTEXT.md
@.planning/phases/63-guarded-per-eye-sclera-production-integration/63-RESEARCH.md
@.planning/phases/63-guarded-per-eye-sclera-production-integration/63-01-SUMMARY.md
@BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift
@BeautySDK/Sources/BeautyCore/Models/BeautyCanonicalStillImage.swift
@BeautySDK/Sources/BeautyEffects/Render/BeautyLocalRetouchComposition.swift
</context>

## Artifacts this phase produces

- `BeautyScleraRednessProvider`, its fixed aggregate result/outcome types and
  checked request-local raster helpers.
- `BeautyScleraRednessTransform` and deterministic tests. No engine, renderer,
  Demo, public API or private-fixture artifact changes in this wave.

<tasks>

<task id="63-02-01" type="auto">
  <name>Task 1: Implement per-eye validation and guard-before-score selection</name>
  <files>BeautySDK/Sources/BeautyEffects/LocalRetouch/BeautyScleraRednessProvider.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyScleraRednessProviderTests.swift</files>
  <read_first>63-01-SUMMARY.md; provider RED tests; `BeautyObservedEyeSupport`; teeth provider checked polygon/ROI helpers; D-02 through D-09 and D-12.</read_first>
  <action>Implement D-02 through D-09 and D-12 in a stateless package provider. Require `.canonical` ownership; normalize arbitrary support-array order by side; reject duplicate sides globally; validate each present side independently with finite unit-bounded simple contour, checked bounds/area/aspect/minimum pixel span, and exactly one actual finite polygon-contained plausible pupil. Build a checked per-eye ROI, rasterized aperture, conservative contour erosion, actual-pupil-centered uncertainty-inflated iris exclusion, expanded near-white highlights and dark lash/margin exclusions before scoring. Freeze selected constants from the predeclared mechanics sweep rather than copying spike seeds. Score only source pixels inside the remaining guard using luminance/low-saturation likelihood times positive red excess; enforce material score/area bounds; radius-one blur and exact same-guard reclip. Emit aggregate-only outcomes in stable left-then-right order and abstain only the affected eye except ambiguous side ownership.</action>
  <acceptance_criteria>Provider tests pass all malformed, threshold-side, empty, one-eye, stable-order and protected-region cases with no cross-eye suppression or inferred geometry.</acceptance_criteria>
  <verify><automated>CLANG_MODULE_CACHE_PATH=/private/tmp/beauty-phase63-clang-module-cache swift test --package-path BeautySDK --filter BeautyScleraRednessProviderTests</automated></verify>
  <done>Each accepted eye owns a nonempty request-local hard envelope and softened score wholly contained by it; unsafe eyes abstain independently.</done>
</task>

<task id="63-02-02" type="auto">
  <name>Task 2: Implement bounded source-only targets and owner-issued per-eye units</name>
  <files>BeautySDK/Sources/BeautyEffects/LocalRetouch/BeautyScleraRednessTransform.swift, BeautySDK/Sources/BeautyEffects/LocalRetouch/BeautyScleraRednessProvider.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyScleraRednessProviderTests.swift</files>
  <read_first>provider RED transform tests; `BeautyTeethWhiteningTransform.swift`; `BeautyLocalRetouchComposition.swift`; D-08 through D-12.</read_first>
  <action>Implement D-08 through D-12. Compute a full-strength target from only the immutable canonical source triplet: require positive measured red excess, apply bounded normalized strength, limited compensating green/blue movement and explicit luminance restoration cap, clamp and round half-away to RGBA8 while preserving alpha. Return no target for neutral/low-redness/unchanged pixels. Convert the post-reclip soft mask once to Q16 and let the existing owner blend once; do not multiply mask weight in the target. Every proposal asserts guard membership. Request one separate owner unit per accepted eye, preserve stable side order, checked proposal bounds/source binding, collision-to-source and unowned-byte identity. Prove monotonic reduction, channel/luminance/detail bounds, exact no-ops, deterministic rounding and no output feedback.</action>
  <acceptance_criteria>Focused provider/composition tests show zero protected/outside changes, source-only deterministic targets, Q16 exactly once, alpha identity and zero-to-two valid owner units.</acceptance_criteria>
  <verify><automated>CLANG_MODULE_CACHE_PATH=/private/tmp/beauty-phase63-clang-module-cache swift test --package-path BeautySDK --filter 'BeautyScleraRednessProviderTests|BeautyLocalRetouchCompositionTests' &amp;&amp; python3 .planning/phases/63-guarded-per-eye-sclera-production-integration/check_phase63_sclera_provider_boundaries.py --provider --repo-root . &amp;&amp; git diff --check</automated></verify>
  <done>The package provider safely produces independently guarded source-bound units without facade activation or private-state exposure.</done>
</task>

</tasks>

<threat_model>
This plan mitigates T-63-02 through T-63-05. Accepted malformed support,
inferred pupils, pre-score protected membership, post-blur leakage,
output-derived targets, double weighting or unbounded arithmetic blocks completion.
</threat_model>

<verification>Run focused provider/composition suites and isolated provider checker after both tasks.</verification>

<success_criteria>Safe zero-to-two per-eye units exist at package level with no engine or deferred-surface broadening.</success_criteria>

<output>Create `63-02-SUMMARY.md` with actual test counts, frozen constants and bounded nonclaims.</output>

