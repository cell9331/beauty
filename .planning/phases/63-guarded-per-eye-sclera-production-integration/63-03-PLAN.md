---
phase: 63-guarded-per-eye-sclera-production-integration
plan: "03"
type: execute
wave: 3
depends_on: [63-02]
files_modified:
  - BeautySDK/Sources/BeautySDK/BeautyEngine.swift
  - BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift
  - BeautySDK/Sources/BeautySDK/BeautyStillImageRequestContext.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyEngineScleraRednessIntegrationTests.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyEngineLocalRetouchFoundationTests.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyEngineLocalRetouchCompositionTests.swift
autonomous: true
requirements: [SCLERA-09, SCLERA-13]
security_standard: OWASP ASVS Level 1
block_on: HIGH
must_haves:
  truths:
    - "Direct positive sclera intent invokes one provider after the existing one detect/map and shares one composition owner with independently activated teeth units."
    - "Canonical side-invalid support disables sclera only; under canonical order one unsafe eye cannot suppress or reuse a safe peer."
    - "No-face, empty, repeated, parallel, interrupted, reset and valid-invalid-valid calls retain no previous support, units, output or summary."
    - "Teeth-only, opaque Testing demand, pixel-buffer and reset perform zero sclera-provider work, while unrelated effects continue after local abstention."
  prohibitions:
    - statement: "Testing hooks, teeth intent, aliases, color/skin/geometry fields, Demo labels or `去脂` proxies must not activate production sclera behavior."
      status: unverified
      verification: flagged
    - statement: "Aggregate observations must not expose eye coordinates, pupils, masks, source colors, pixels, stable identity or free-form diagnostics."
      status: unverified
      verification: flagged
    - statement: "Integration must not issue another canonicalization/Vision request, create a second composition pass or change pixel-buffer/realtime/Demo behavior."
      status: unverified
      verification: flagged
  artifacts:
    - path: BeautySDK/Sources/BeautySDK/BeautyEngine.swift
      provides: One admitted sclera-provider invocation and shared one-pass composition
    - path: BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift
      provides: Fixed aggregate per-eye observations and safe deterministic support fixtures
    - path: BeautySDK/Tests/BeautyCoreTests/BeautyEngineScleraRednessIntegrationTests.swift
      provides: Production routing, peer isolation, recovery and deferred-path tests
---

<objective>
Connect the per-eye provider to the admitted production still-image lifecycle
and prove independent activation, local abstention and recovery.

Purpose: Make direct sclera intent perform bounded work through the one current
request context without changing deferred surfaces.
Output: Green engine integration, composition and lifecycle tests.
</objective>

<context>
@.planning/phases/63-guarded-per-eye-sclera-production-integration/63-CONTEXT.md
@.planning/phases/63-guarded-per-eye-sclera-production-integration/63-02-SUMMARY.md
@BeautySDK/Sources/BeautySDK/BeautyEngine.swift
@BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift
@BeautySDK/Sources/BeautySDK/BeautyStillImageRequestContext.swift
@BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift
</context>

## Artifacts this phase produces

- One admitted engine call to `BeautyScleraRednessProvider` and shared teeth/
  sclera/opaque composition ownership.
- Fixed aggregate Testing observations and deterministic eye fixtures.
- Lifecycle tests; no renderer case, Demo mapping, public anatomy or new Vision request.

<tasks>

<task id="63-03-01" type="auto">
  <name>Task 1: Wire one sclera provider invocation into shared still-image composition</name>
  <files>BeautySDK/Sources/BeautySDK/BeautyEngine.swift, BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift, BeautySDK/Sources/BeautySDK/BeautyStillImageRequestContext.swift, BeautySDK/Tests/BeautyCoreTests/BeautyEngineScleraRednessIntegrationTests.swift, BeautySDK/Tests/BeautyCoreTests/BeautyEngineLocalRetouchCompositionTests.swift</files>
  <read_first>63-02-SUMMARY.md; `BeautyEngine.swift` admitted branch; existing teeth integration tests; D-01, D-02, D-12 and D-13.</read_first>
  <action>Implement D-01, D-02, D-12 and D-13. Detect direct positive sclera intent from validated parameters. Create one composition owner when teeth, sclera or opaque composition work exists; invoke teeth only for direct teeth intent and invoke the sclera provider exactly once only for direct sclera intent using the selected observation's current `observedEyeSupport` and `observedEyeOrder`. Append stable per-eye units, then opaque Testing units, compose once and render through the existing route. Keep canonicalize/detect/map/context order and detector invocation count exact. Add only fixed aggregate provider invocation, left/right unit and outcome observations; clear them at call boundaries. Verify teeth-only, sclera-only, both, zero, opaque-only and unrelated-effect cases for both still-image entries.</action>
  <acceptance_criteria>One direct sclera intent causes exactly one provider invocation, no additional detect/canonical request, stable unit order and one shared composition; no proxy activates it.</acceptance_criteria>
  <verify><automated>CLANG_MODULE_CACHE_PATH=/private/tmp/beauty-phase63-clang-module-cache swift test --package-path BeautySDK --filter 'BeautyEngineScleraRednessIntegrationTests|BeautyEngineLocalRetouchCompositionTests|BeautyEngineLocalRetouchFoundationTests'</automated></verify>
  <done>Sclera-only and combined intent use the current support and one composition while teeth and opaque activation remain independent.</done>
</task>

<task id="63-03-02" type="auto">
  <name>Task 2: Prove affected-eye abstention, recovery and deferred-route absence</name>
  <files>BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift, BeautySDK/Tests/BeautyCoreTests/BeautyEngineScleraRednessIntegrationTests.swift, BeautySDK/Tests/BeautyCoreTests/BeautyEngineLocalRetouchFoundationTests.swift</files>
  <read_first>D-14 and D-16; provider result/outcome API; existing engine teeth recovery tests; pixel-buffer and reset branches.</read_first>
  <action>Implement D-14 and the runtime portion of D-16. Add deterministic current-request fixtures for canonical paired, left-only, right-only, one malformed peer, invalid order, blink/closure, severe gaze, glare, occlusion and no-face support without creating a production fallback. Verify affected-eye-only abstention, valid peer continuation, eligible teeth/color/geometry continuation, exact aggregate resets, valid-invalid-valid in both directions, repeated calls, independent engines, parallel completion order and interrupted callers. Pixel-buffer entry and reset clear observations and perform zero provider work. Preserve typed canonical failures, renderer inventory 73, disabled Demo, no realtime/model/network/upper-eyelid/`去脂` work.</action>
  <acceptance_criteria>Lifecycle tests show no stale support/unit/output/summary and no rejected eye suppresses a safe peer or unrelated eligible effect; integration HIGH mode passes.</acceptance_criteria>
  <verify><automated>CLANG_MODULE_CACHE_PATH=/private/tmp/beauty-phase63-clang-module-cache swift test --package-path BeautySDK --filter 'BeautyEngineScleraRednessIntegrationTests|BeautyEngineLocalRetouchFoundationTests|BeautyEngineMetadataCompatibilityTests|BeautyEngineGeometryFacadeTests' &amp;&amp; python3 .planning/phases/63-guarded-per-eye-sclera-production-integration/check_phase63_sclera_provider_boundaries.py --integration --repo-root . &amp;&amp; git diff --check</automated></verify>
  <done>All deterministic still-image routing/recovery cases pass with local per-eye abstention and zero deferred-path activation.</done>
</task>

</tasks>

<threat_model>
This plan primarily mitigates T-63-01, T-63-02, T-63-06 and T-63-08. A
second request, proxy activation, cross-eye suppression, stale aggregate state,
suppressed unrelated effect or pixel-buffer/Demo scope change blocks completion.
</threat_model>

<verification>Run focused engine/foundation/composition suites and isolated integration checker; record actual counts only.</verification>

<success_criteria>Direct sclera intent is connected to one safe request-local provider invocation on still images only.</success_criteria>

<output>Create `63-03-SUMMARY.md` with actual lifecycle counts, peer-isolation evidence and unchanged inventories.</output>

