---
phase: 35-public-contract-and-independent-geometry
plan: "02"
type: execute
wave: 2
depends_on:
  - 35-01
files_modified:
  - BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift
  - BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift
  - BeautySDK/Sources/BeautyEffects/Warp/NoseWarpProvider.swift
  - BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/NoseWarpProviderTests.swift
autonomous: true
requirements:
  - NOSE-04
  - NOSE-05
  - NOSE-06
must_haves:
  truths:
    - "The legacy FaceGeometry.nose array and adapter nose(in:) coordinates remain unchanged; new package-internal noseRoot and noseTip supports are explicit, default-empty, auditable, and populated only when the nose group exists."
    - "noseRootNarrowing emits a deterministic left-to-right symmetric pair with equal inward horizontal displacement, unchanged Y, no centerline crossing, finite bounded points, and exact effective strength at or below 0.25."
    - "noseTipLift emits deterministic lower-tip points with unchanged X and strictly smaller target Y, finite bounded points, and exact effective strength at or below 0.25."
    - "Empty, one-point, non-finite, same-side, asymmetric, degenerate, or insufficient supports fail closed and never borrow FaceGeometry.nose, bridgePoints, tipPoints, or lowerNosePoints."
    - "Complete point arrays, sources, targets, and displacement vectors prove root output differs from noseBridge and lift output differs from both signs of noseTipSize."
  artifacts:
    - path: "BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift"
      provides: "Default-empty package-internal FaceGeometry.noseRoot and FaceGeometry.noseTip supports"
      contains: "let noseRoot"
    - path: "BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift"
      provides: "Deterministic explicit root/tip support builders while preserving legacy nose proxy points"
      contains: "noseRoot(in:"
    - path: "BeautySDK/Sources/BeautyEffects/Warp/NoseWarpProvider.swift"
      provides: "Independent support validation, support availability, root narrowing, and tip lift helpers"
      contains: "rootNarrowingPoints"
    - path: "BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift"
      provides: "Valid and malformed private support fixtures without changing legacy fixture nose arrays"
    - path: "BeautySDK/Tests/BeautyEffectsTests/NoseWarpProviderTests.swift"
      provides: "Axis, direction, symmetry, bounds, determinism, non-alias, and fail-closed provider evidence"
  key_links:
    - from: "BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift"
      to: "BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift"
      via: "adapter populates only explicit package-internal noseRoot/noseTip fields"
      pattern: "noseRoot:|noseTip:"
    - from: "BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift"
      to: "BeautySDK/Sources/BeautyEffects/Warp/NoseWarpProvider.swift"
      via: "new helpers consume dedicated supports and never synthesize/fallback from legacy nose"
      pattern: "face.noseRoot|face.noseTip"
    - from: "BeautySDK/Sources/BeautyEffects/Warp/NoseWarpProvider.swift"
      to: "BeautySDK/Tests/BeautyEffectsTests/NoseWarpProviderTests.swift"
      via: "complete control-point vectors and malformed fixtures lock distinct fail-closed semantics"
      pattern: "noseRootNarrowing|noseTipLift"
---

<objective>
Establish auditable private root/tip geometry and implement two bounded nose-provider paths without modifying or borrowing the legacy upper-root proxy.

Purpose: resolve the source-audited upper-root proxy hazard honestly, preserve all four shipped nose inputs, and give Plan 35-03 a provider-level support-availability seam for field-specific fail-closed routing.
Output: explicit private supports, deterministic adapter fixtures, independent root/tip helpers, and strict vector/invalid-input tests.
</objective>

## Artifacts this phase produces

- Package-internal default-empty `FaceGeometry.noseRoot: [SIMD2<Float>]` and `FaceGeometry.noseTip: [SIMD2<Float>]` stored supports and initializer arguments.
- Adapter helpers `BeautyFaceGeometryAdapter.noseRoot(in:)` and `noseTip(in:)`, populated only for `.nose`; the existing `nose(in:)` helper and its four coordinates remain unchanged.
- Internal `NoseWarpSupportAvailability` with Boolean `rootNarrowing` and `tipLift`, plus `NoseWarpProvider.supportAvailability(for:)` for Plan 35-03 field-specific sanitization.
- Provider helpers `validatedRootPair(in:)`, `validatedTipSupport(in:)`, `rootNarrowingPoints(face:strength:)`, and `tipLiftPoints(face:strength:)`.
- New `FaceGeometry` fixture variants for valid explicit supports and empty/one-point/non-finite/same-side/asymmetric/degenerate root plus empty/one-point/non-finite/degenerate tip cases; existing `.fixture`, `.missingNose`, `.reused`, and `.stale` legacy `nose` arrays remain byte-for-byte/source-point equal.
- Focused provider tests for subset, axis, direction, symmetry, deterministic order, nonzero movement, bounds/radius/strength/finiteness, non-aliasing, global-guard independence, and fail-closed malformed supports.
- No public geometry type, raw support diagnostic, resolver change, renderer/gallery case, Demo change, dependency, ledger promotion, or branch-status change.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-public-contract-and-independent-geometry/35-CONTEXT.md
@.planning/phases/35-public-contract-and-independent-geometry/35-RESEARCH.md
@.planning/phases/35-public-contract-and-independent-geometry/35-PATTERNS.md
@.planning/phases/35-public-contract-and-independent-geometry/35-VALIDATION.md
@.planning/phases/35-public-contract-and-independent-geometry/35-01-SUMMARY.md
@BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift
@BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift
@BeautySDK/Sources/BeautyEffects/Warp/NoseWarpProvider.swift
@BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift
@BeautySDK/Tests/BeautyEffectsTests/NoseWarpProviderTests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 35-02-01: Add explicit default-empty root/tip supports while preserving the legacy nose proxy</name>
  <files>BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift, BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift, BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift</files>
  <read_first>
    - `AGENTS.md`
    - `.planning/REQUIREMENTS.md` — NOSE-04 through NOSE-06 source-subset and fail-closed contract
    - `.planning/phases/35-public-contract-and-independent-geometry/35-CONTEXT.md` — non-alias and insufficient-support decisions
    - `.planning/phases/35-public-contract-and-independent-geometry/35-RESEARCH.md` — source-audited upper-root hazard and recommended support representation
    - `.planning/phases/35-public-contract-and-independent-geometry/35-PATTERNS.md` — explicit support/fixture pattern
    - `BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift` — modified internal geometry owner
    - `BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift` — modified facade-observation adapter and exact legacy nose coordinates
    - `BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift` — modified shared `FaceGeometry` fixture owner
    - `BeautySDK/Sources/BeautyEffects/Warp/NoseWarpProvider.swift` — read-only consumer contract for the next task
  </read_first>
  <action>Add package-internal immutable arrays `let noseRoot: [SIMD2&lt;Float&gt;]` and `let noseTip: [SIMD2&lt;Float&gt;]` to `FaceGeometry`, with default-empty initializer arguments immediately after `nose`. Assign them directly. Do not make them `public`, `package`, SPI, computed aliases, or derived from `nose` in this model.

In `BeautyFaceGeometryAdapter.makeGeometry(from:)`, retain the exact existing `nose(in:)` implementation and coordinates `(0.46,0.43)`, `(0.50,0.55)`, `(0.40,0.64)`, `(0.60,0.64)`. When and only when `availableGroups` contains `.nose`, also populate `noseRoot` from exact bounds-relative points `(0.44,0.48)` and `(0.56,0.48)`, and populate `noseTip` from exact bounds-relative points `(0.44,0.62)`, `(0.50,0.66)`, `(0.56,0.62)`, all through the existing `point(_:x:y:)` clamp builder. Missing `.nose` must set all three arrays—legacy `nose`, `noseRoot`, and `noseTip`—to empty.

Extend the shared fixture owner so `.fixture`, `.reused`, and `.stale` carry a symmetric same-Y root pair and a deterministic three-point lower tip support while their existing `nose` arrays remain exactly unchanged; `.missingNose` carries all three nose arrays empty. Add named malformed fixtures (exact names: `.onePointNoseRoot`, `.nonFiniteNoseRoot`, `.sameSideNoseRoot`, `.asymmetricNoseRoot`, `.degenerateNoseRoot`, `.onePointNoseTip`, `.nonFiniteNoseTip`, `.degenerateNoseTip`) by copying `.fixture`'s unaffected fields and altering only the named support. Ensure reused/stale fixtures forward the same valid supports plus freshness. Add an adapter-focused assertion in an existing effects test seam if needed to prove complete observations create two root and three tip supports, missing `.nose` creates none, and the legacy `nose` sequence remains the established four values. Do not reshape the legacy fixture to satisfy root symmetry.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter FaceShapeWarpProviderTests &amp;&amp; swift test --package-path BeautySDK --filter NoseWarpProviderTests</automated>
    <automated>rg -n 'let noseRoot: \[SIMD2.*Float.*\]|let noseTip: \[SIMD2.*Float.*\]|noseRoot: \[SIMD2.*Float.*\] = \[\]|noseTip: \[SIMD2.*Float.*\] = \[\]' BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift</automated>
    <automated>rg -n 'x: 0\.46, y: 0\.43|x: 0\.50, y: 0\.55|x: 0\.40, y: 0\.64|x: 0\.60, y: 0\.64|x: 0\.44, y: 0\.48|x: 0\.56, y: 0\.48|x: 0\.50, y: 0\.66' BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift</automated>
    <automated>git diff --check -- BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - `FaceGeometry` has explicit package-internal default-empty `noseRoot` and `noseTip` arrays and no public/SPI exposure.
    - The legacy adapter and fixture `nose` arrays remain exactly unchanged; valid root support is exactly two same-Y points symmetric around bounds mid-X, and valid tip support is exactly three lower points.
    - Missing `.nose` produces empty legacy/root/tip arrays, while malformed named fixtures alter only the support under test.
    - `.fixture`, `.reused`, and `.stale` contain the same valid supports and preserve their former fields/freshness behavior.
    - The task fails rather than deriving root supports from `noseBridge`/`face.nose` if the explicit pair cannot be represented as specified.
  </acceptance_criteria>
  <done>The private geometry model honestly represents independent root/tip inputs without modifying shipped nose proxy data.</done>
</task>

<task type="auto">
  <name>Task 35-02-02: Implement independent provider vectors and strict field-specific support validation</name>
  <files>BeautySDK/Sources/BeautyEffects/Warp/NoseWarpProvider.swift, BeautySDK/Tests/BeautyEffectsTests/NoseWarpProviderTests.swift</files>
  <read_first>
    - `AGENTS.md`
    - `.planning/REQUIREMENTS.md` — exact NOSE-04, NOSE-05, and NOSE-06 assertions
    - `.planning/phases/35-public-contract-and-independent-geometry/35-CONTEXT.md` — locked axes/directions/non-alias/fail-closed behavior
    - `.planning/phases/35-public-contract-and-independent-geometry/35-RESEARCH.md` — motion/bounds semantics and global proxy hazard
    - `.planning/phases/35-public-contract-and-independent-geometry/35-PATTERNS.md` — provider branch, full-vector comparison, and invalid-support rules
    - `BeautySDK/Sources/BeautyEffects/Warp/NoseWarpProvider.swift` — modified provider and current global `face.nose` center guard
    - `BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift` — explicit supports created by Task 35-02-01
    - `BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift` — exact `0.25` constants from Plan 35-01
    - `BeautySDK/Tests/BeautyEffectsTests/NoseWarpProviderTests.swift` — modified provider suite
    - `BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift` — authoritative valid/malformed fixtures created by Task 35-02-01
  </read_first>
  <action>Refactor `NoseWarpProvider.makeControlPoints` so the current `LandmarkGeometryHelper.center(of: face.nose)` guard applies only when at least one legacy strength (`noseSlim`, `noseWingSlim`, signed `noseTipSize`, `noseBridge`) is requested. A request containing only a new field must work from its explicit support even when `face.nose` is empty; conversely, a valid legacy `face.nose` must never rescue an invalid new support. Preserve the four legacy helper bodies and their output order.

Add internal `NoseWarpSupportAvailability: Equatable, Sendable` with `let rootNarrowing: Bool` and `let tipLift: Bool`, and `NoseWarpProvider.supportAvailability(for:)`. Back it and the new point helpers with the same validators: `validatedRootPair(in:)` accepts exactly two finite, distinct, normalized points sorted left-to-right, same Y within `0.0001`, one on each side of `face.bounds.midX`, equal left/right distance from that centerline within `0.0001`, and nonzero room before crossing; `validatedTipSupport(in:)` accepts at least two finite, distinct, normalized points at or below `face.bounds.midY` in the repository's top-to-bottom coordinates and returns deterministic `(x,y)` order. Empty, one-point, non-finite, out-of-bounds, same-side, asymmetric, crossed, duplicate, or degenerate support returns unavailable. Do not validate by clamping first.

Add independent branches after the legacy branches. `rootNarrowingPoints(face:strength:)` consumes only the validated root pair, computes displacement `face.bounds.width * 0.025 * strength / BeautySafetyCaps.noseRootNarrowing`, bounds it below each anchor's distance to the centerline, moves left X rightward and right X leftward by the same nonzero amount, preserves Y exactly, and emits left-to-right points with radius `face.bounds.width * 0.07`, `strength` unchanged, falloff `2`. `tipLiftPoints(face:strength:)` consumes only the validated tip support, computes displacement `face.bounds.height * 0.020 * strength / BeautySafetyCaps.noseTipLift`, subtracts it from every source Y, preserves X exactly, rejects zero/non-finite/pre-clamp out-of-range displacement, and emits deterministic points with radius `face.bounds.width * 0.08`, strength unchanged, falloff `2`. Reuse `makePoint` only after these pre-clamp validations. If a positive requested new field cannot emit points, it contributes no points and must not call `bridgePoints`, `tipPoints`, or `lowerNosePoints`. Return `skipReason: "nose_inputs_missing"` when requested nose work produces no points.

Extend the test `strengths(...)` helper with both new fields capped against their exact `0.25` constants. Add separate tests that assert root source order, same-Y targets, equal absolute inward X deltas, no centerline crossing, nonzero movement, exact capped strength, radius bounds, normalized/finiteness, and deterministic equality; tip tests assert lower-support sources, exact X preservation, strictly upward `target.y &lt; source.y`, nonzero movement, exact capped strength, radius bounds, normalized/finiteness, and deterministic equality. Compare complete results, source arrays, target arrays, and displacement vectors: root versus `noseBridge`; lift versus both positive and negative `noseTipSize`. Assert all named malformed root/tip fixtures return no points for the isolated affected field and `nose_inputs_missing`, even though their legacy `nose` remains valid. Also construct a face with empty legacy `nose` but valid explicit supports and prove each new field remains nonempty, closing the upper-root global-guard hazard.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter NoseWarpProviderTests &amp;&amp; swift test --package-path BeautySDK --filter FaceShapeWarpProviderTests</automated>
    <automated>rg -n 'NoseWarpSupportAvailability|supportAvailability\(for:|validatedRootPair|validatedTipSupport|rootNarrowingPoints|tipLiftPoints|0\.025|0\.020' BeautySDK/Sources/BeautyEffects/Warp/NoseWarpProvider.swift</automated>
    <automated>git diff --check -- BeautySDK/Sources/BeautyEffects/Warp/NoseWarpProvider.swift BeautySDK/Tests/BeautyEffectsTests/NoseWarpProviderTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - Root-only and tip-only requests produce deterministic nonempty output from valid explicit supports even if legacy `face.nose` is empty.
    - Root output is exactly horizontal, symmetric, inward, non-crossing, left-to-right, finite, bounded, and nonzero; tip output is exactly vertical-up, deterministic, finite, bounded, and nonzero.
    - Complete arrays/sources/targets/displacements differ from `noseBridge` and both signed `noseTipSize` paths; point-count inequality alone is not used as proof.
    - Every empty/insufficient/non-finite/same-side/asymmetric/degenerate affected support fails closed with no legacy point substitution and the category-only skip reason.
    - Existing four-field provider tests remain green and the bodies/ordering of shipped legacy helpers are unchanged.
  </acceptance_criteria>
  <done>Two honest provider paths exist with shared auditable validation and no legacy fallback or global-guard alias.</done>
</task>

</tasks>

<threat_model>
  <scope>ASVS Level 1 applies to this plan's biometric-adjacent private geometry, malformed-input validation, provider output, and package/public boundaries.</scope>
  <blocking_rule>Any unresolved high-severity threat blocks task completion and prevents `35-02-SUMMARY.md` from claiming the plan complete.</blocking_rule>
  <assets>Biometric-adjacent raw geometry, legacy four-field nose behavior, deterministic bounded control points, field identity/non-aliasing, and the package-only geometry boundary.</assets>
  <trust_boundaries>Package-only observations cross the adapter into `FaceGeometry`; untrusted/malformed supports cross validators into warp calculations; provider control points cross into the unified local warp; only aggregate results may later cross the public facade.</trust_boundaries>
  <threats>
    - Raw supports could leak publicly or into diagnostics.
    - Non-finite, out-of-range, asymmetric, same-side, insufficient, or degenerate supports could be hidden by clamping and emit unsafe vectors.
    - The old top-level `face.nose` guard could block valid new supports, or valid legacy points could silently substitute for invalid root/tip supports.
    - Geometry could alias `noseBridge`/signed `noseTipSize`, change legacy output, or drift into renderer/ledger/dependency scope.
  </threats>
  <mitigations>Keep supports internal/default-empty; preserve the exact legacy proxy; validate before clamping; require explicit paired/lower supports; use per-feature availability and isolated branches; compare full vectors; cover malformed fixtures and empty-legacy/valid-new cases; keep skip diagnostics fixed/category-only; run focused legacy and new provider tests plus public/SPI and scope scans in Plan 35-04.</mitigations>
  <residual_risk>Proxy supports are deterministic fixtures, not a claim of production landmark precision or commercial visual quality. Phase 36 must validate saved output/ROI visibility, while Phase 37 owns final caps and exhaustive six-field fallback/once-only safety.</residual_risk>
</threat_model>

<verification>
Overall Plan 35-02 verification:

- Run `FaceShapeWarpProviderTests` after support/fixture edits and `NoseWarpProviderTests` after provider edits.
- Assert the exact legacy adapter nose coordinates remain present and the new root/tip coordinates are explicit.
- Confirm complete-vector non-alias, malformed-support fail-closed, deterministic bounds, and empty-legacy/valid-new behavior in XCTest.
- Run scoped `git diff --check`; defer public-facade routing, full-suite, public/SPI, renderer/Demo/dependency/ledger boundary scans to Plans 35-03 and 35-04.
</verification>

<success_criteria>
Plan 35-02 succeeds when the repository preserves the legacy `FaceGeometry.nose` proxy, adds explicit default-empty internal root/tip supports, produces deterministic bounded horizontal root contraction and vertical upward tip lift, proves full-vector non-aliasing, fails closed for every malformed/insufficient support without legacy fallback or raw-geometry disclosure, and ASVS Level 1 has no unresolved high-severity threat.
</success_criteria>

<output>
Create `.planning/phases/35-public-contract-and-independent-geometry/35-02-SUMMARY.md` when done.
</output>
