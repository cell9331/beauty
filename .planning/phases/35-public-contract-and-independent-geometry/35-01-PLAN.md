---
phase: 35-public-contract-and-independent-geometry
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift
  - BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift
  - BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift
  - BeautySDK/Tests/BeautyResourcesTests/BeautyResourceCatalogTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautySafetyCapsTests.swift
autonomous: true
requirements:
  - NOSE-01
  - NOSE-02
  - NOSE-03
must_haves:
  truths:
    - "BeautyParameters has exactly 33 stored fields: 32 numeric fields plus filterId; noseRootNarrowing and noseTipLift are independent positive-only Float storage, not aliases."
    - "Both new public values default to 0, clamp finite input to 0...1, map NaN and both infinities to 0, and are re-normalized after mutable assignment."
    - "A complete legacy 31-field JSON payload and every bundled preset decode both new fields as 0, while new nonzero 33-field values round-trip exactly and existing labeled initializer calls remain source-compatible through defaults."
    - "BeautyEffectiveStrengths owns distinct values and BeautySafetyCaps provisionally caps each at exactly 0.25 without changing the public 0...1 contract."
  artifacts:
    - path: "BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift"
      provides: "Two public stored fields threaded through all six manual model seams"
      contains: "noseRootNarrowing"
    - path: "BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift"
      provides: "Distinct effective-strength storage for both new fields"
      contains: "noseTipLift"
    - path: "BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift"
      provides: "Provisional 0.25 internal caps"
      contains: "noseRootNarrowing"
    - path: "BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift"
      provides: "Exact inventory, normalization, compatibility, round-trip, independence, and Sendable evidence"
    - path: "BeautySDK/Tests/BeautyResourcesTests/BeautyResourceCatalogTests.swift"
      provides: "Bundled preset missing-key neutrality evidence"
    - path: "BeautySDK/Tests/BeautyEffectsTests/BeautySafetyCapsTests.swift"
      provides: "Exact provisional cap assertions"
  key_links:
    - from: "BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift"
      to: "BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift"
      via: "all six public-model enumeration seams are locked by exact field and Codable tests"
      pattern: "noseRootNarrowing|noseTipLift"
    - from: "BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift"
      to: "BeautySDK/Tests/BeautyEffectsTests/BeautySafetyCapsTests.swift"
      via: "both internal constants are asserted as exactly 0.25"
      pattern: "0.25"
    - from: "BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift"
      to: "BeautySDK/Tests/BeautyResourcesTests/BeautyResourceCatalogTests.swift"
      via: "missing keys in unedited bundled presets decode through decodeFloatIfPresent to zero"
      pattern: "builtInPresets"
---

<objective>
Freeze the compatibility-safe public and effective-value contract for `noseRootNarrowing` and `noseTipLift` before any geometry provider or resolver routing uses them.

Purpose: establish independent storage, exact normalization, source-rebuild/JSON/preset compatibility, and provisional internal cap symbols as the compile-time foundation for Plans 35-02 and 35-03.
Output: a 33-stored-field public model, two effective-strength fields, two exact `0.25` caps, and focused compatibility tests.
</objective>

## Artifacts this phase produces

- Public stored properties `BeautyParameters.noseRootNarrowing` and `BeautyParameters.noseTipLift`.
- `BeautyParameters.CodingKeys.noseRootNarrowing` and `.noseTipLift`, defaulted initializer arguments, `clampUnit` assignments, missing-key decoding, and `normalized()` forwarding for both fields.
- Effective stored properties `BeautyEffectiveStrengths.noseRootNarrowing` and `.noseTipLift`.
- Internal constants `BeautySafetyCaps.noseRootNarrowing` and `.noseTipLift`, each exactly `0.25` for provisional Phase 35 behavior.
- Requirement-named XCTest coverage in `BeautyParametersTests`, `BeautyResourceCatalogTests`, and `BeautySafetyCapsTests`; no new test file or dependency.
- No preset JSON edit, provider behavior, resolver routing, renderer/gallery case, Demo change, ledger promotion, or branch-status change.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-public-contract-and-independent-geometry/35-CONTEXT.md
@.planning/phases/35-public-contract-and-independent-geometry/35-RESEARCH.md
@.planning/phases/35-public-contract-and-independent-geometry/35-PATTERNS.md
@.planning/phases/35-public-contract-and-independent-geometry/35-VALIDATION.md
@BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift
@BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift
@BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift
@BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift
@BeautySDK/Tests/BeautyResourcesTests/BeautyResourceCatalogTests.swift
@BeautySDK/Tests/BeautyEffectsTests/BeautySafetyCapsTests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 35-01-01: Add the two independent public fields through every BeautyParameters seam</name>
  <files>BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift, BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift</files>
  <read_first>
    - `AGENTS.md`
    - `PLANS.md`
    - `.planning/REQUIREMENTS.md` — exact NOSE-01 and NOSE-02 contracts
    - `.planning/phases/35-public-contract-and-independent-geometry/35-CONTEXT.md` — locked positive-only and compatibility decisions
    - `.planning/phases/35-public-contract-and-independent-geometry/35-RESEARCH.md` — six public-model seams and exact 33-field accounting
    - `.planning/phases/35-public-contract-and-independent-geometry/35-PATTERNS.md` — required model and test patterns
    - `BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift` — every file modified by this task and the authoritative model implementation
    - `BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift` — every test file modified by this task and the authoritative model contract suite
  </read_first>
  <action>In `BeautyParameters`, add `public var noseRootNarrowing: Float` and `public var noseTipLift: Float` immediately after `noseBridge` and before the mouth fields. Add the same two names, in the same order, to `CodingKeys`; add defaulted labeled initializer arguments `noseRootNarrowing: Float = 0` and `noseTipLift: Float = 0`; assign each through the existing `Self.clampUnit`; decode each with `decodeFloatIfPresent`; and forward each from `normalized()`. Do not add custom Codable code, a second clamp implementation, computed aliases, or reuse any legacy nose storage.

Extend `BeautyParametersTests` so the renamed default inventory test asserts exactly `33` children and explicitly checks both new defaults. Add table-driven positive-only evidence for each field covering finite `-1 -> 0`, `2 -> 1`, normal in-range distinct values, `.nan -> 0`, `.infinity -> 0`, and `-.infinity -> 0`. Mutate each public var to an out-of-range or non-finite value after construction, call `normalized()`, and assert the returned copy re-applies the same rules while the two fields remain separately addressable and participate independently in `Equatable`. Preserve the compile-time `Sendable` assertion.

Add a literal representative legacy JSON object containing exactly the existing 31 stored keys (30 numeric keys plus `filterId`) and no new keys; use `JSONSerialization` to assert the object has 31 keys, decode it, and assert both new fields are zero. Add a new 33-field round-trip with unequal nonzero values (use `noseRootNarrowing: 0.21` and `noseTipLift: 0.37`) and assert exact equality after encode/decode. Keep at least one existing source-style labeled initializer call that omits both new arguments compiling and yielding zero. Do not edit archived v1.7 artifacts.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter BeautyParametersTests</automated>
    <automated>test "$(rg -n '^    public var ' BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift | wc -l | tr -d ' ')" -eq 33 &amp;&amp; rg -n 'noseRootNarrowing: Float = 0|noseTipLift: Float = 0|decodeFloatIfPresent\(\.noseRootNarrowing\)|decodeFloatIfPresent\(\.noseTipLift\)' BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift</automated>
    <automated>git diff --check -- BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - `Mirror(reflecting: BeautyParameters()).children.count` is asserted as exactly 33, representing 32 numeric fields plus `filterId`.
    - Both new properties exist as independent stored `Float` values and appear exactly once in each of stored properties, coding keys, initializer signature, initializer assignments, decoder forwarding, and normalized-copy forwarding.
    - Defaults, negative/overflow finite inputs, all three non-finite inputs, post-init mutation normalization, independent equality/storage, and `Sendable` compile coverage pass.
    - A JSON object asserted to contain exactly 31 legacy keys decodes both new fields to zero; unequal values `0.21` and `0.37` round-trip without aliasing.
    - Existing labeled initializer usage that omits both new fields compiles and stays neutral.
  </acceptance_criteria>
  <done>The public model has a source-compatible, JSON-compatible, independently stored 33-field contract.</done>
</task>

<task type="auto">
  <name>Task 35-01-02: Add effective storage, provisional caps, and bundled-preset neutrality evidence</name>
  <files>BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift, BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift, BeautySDK/Tests/BeautyEffectsTests/BeautySafetyCapsTests.swift, BeautySDK/Tests/BeautyResourcesTests/BeautyResourceCatalogTests.swift</files>
  <read_first>
    - `AGENTS.md`
    - `.planning/REQUIREMENTS.md` — NOSE-02 compatibility and NOSE-03 propagation boundary
    - `.planning/phases/35-public-contract-and-independent-geometry/35-CONTEXT.md` — provisional `0.25` decision and Phase 37 final-cap boundary
    - `.planning/phases/35-public-contract-and-independent-geometry/35-RESEARCH.md` — preset absence and effective-strength seams
    - `.planning/phases/35-public-contract-and-independent-geometry/35-PATTERNS.md` — exact adjacent-field patterns
    - `BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift` — modified effective-strength owner
    - `BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift` — modified cap owner
    - `BeautySDK/Tests/BeautyEffectsTests/BeautySafetyCapsTests.swift` — modified exact-constant suite
    - `BeautySDK/Tests/BeautyResourcesTests/BeautyResourceCatalogTests.swift` — modified preset catalog suite
    - `BeautySDK/Sources/BeautyResources/Resources/Presets/` — read-only compatibility payload source of truth; do not edit any JSON
  </read_first>
  <action>Add `public var noseRootNarrowing: Float = 0` and `public var noseTipLift: Float = 0` to `BeautyEffectiveStrengths` immediately after `noseBridge`. Add `static let noseRootNarrowing: Float = 0.25` and `static let noseTipLift: Float = 0.25` to `BeautySafetyCaps` in the same nose grouping. Extend `BeautySafetyCapsTests.testPhase6SafetyCapsMatchCanonicalConstants` (or split a clearly named Phase 35 test) to assert both values exactly `0.25` with the suite's `0.0001` accuracy. Treat these as provisional Phase 35 constants; do not rename existing caps or claim Phase 37 final calibration.

Add `BeautyResourceCatalogTests` coverage that loads `try BeautyResourceCatalog.bundled().builtInPresets()`, asserts the established five-preset inventory remains present, and for every preset asserts `parameters.noseRootNarrowing == 0` and `parameters.noseTipLift == 0`, including the preset ID in failures. Confirm the five preset JSON files remain absent of both keys; do not add explicit zero keys because missing-key decoding is the compatibility behavior under test.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter BeautySafetyCapsTests &amp;&amp; swift test --package-path BeautySDK --filter BeautyResourceCatalogTests</automated>
    <automated>test "$(rg -l 'noseRootNarrowing|noseTipLift' BeautySDK/Sources/BeautyResources/Resources/Presets | wc -l | tr -d ' ')" -eq 0</automated>
    <automated>rg -n 'noseRootNarrowing: Float = 0|noseTipLift: Float = 0' BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift &amp;&amp; rg -n 'noseRootNarrowing: Float = 0\.25|noseTipLift: Float = 0\.25' BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift</automated>
    <automated>git diff --check -- BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift BeautySDK/Tests/BeautyEffectsTests/BeautySafetyCapsTests.swift BeautySDK/Tests/BeautyResourcesTests/BeautyResourceCatalogTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - `BeautyEffectiveStrengths` exposes two distinct zero-default effective values for later provider/resolver compilation.
    - `BeautySafetyCaps.noseRootNarrowing` and `.noseTipLift` both equal exactly `0.25` and are directly asserted.
    - Every bundled preset decodes both fields to exact zero through missing-key compatibility.
    - No preset JSON, package manifest, dependency, renderer, Demo, feature ledger, or branch README changes.
  </acceptance_criteria>
  <done>The effective model and provisional caps exist, and all bundled resources remain behaviorally neutral without payload churn.</done>
</task>

</tasks>

<threat_model>
  <scope>ASVS Level 1 applies to this plan's public-input, serialization, resource-compatibility, and dependency boundaries.</scope>
  <blocking_rule>Any unresolved high-severity threat blocks task completion and prevents `35-01-SUMMARY.md` from claiming the plan complete.</blocking_rule>
  <assets>Public API/source compatibility, 31-field JSON and bundled-preset neutrality, independent parameter identity, finite normalized values, stable package dependency surface, and the absence of raw geometry in public data.</assets>
  <trust_boundaries>Untrusted host/JSON float input crosses into `BeautyParameters`; bundled JSON crosses the resource decoder; public `0...1` values cross later into internal capped effective strengths; phase scope crosses into later renderer/ledger work only through documented dependencies.</trust_boundaries>
  <threats>
    - Malformed or non-finite input could propagate into geometry; missing keys could accidentally become nonzero.
    - A new name could alias legacy `noseBridge`/`noseTipSize` storage or be omitted from a manual model seam.
    - Initializer growth could break source-style callers; preset edits could hide compatibility regressions.
    - Diagnostics or public model changes could expose raw geometry, and scope drift could add dependencies, renderer work, or premature ledger promotion.
  </threats>
  <mitigations>Use the existing `clampUnit` and `decodeFloatIfPresent`; lock six enumeration seams, exact 33 stored fields, independent equality, literal 31-key decode, nonzero round-trip, omitted-argument source calls, and unedited-preset neutrality. Limit production edits to the public/effective models and cap constants, keep all geometry/support types absent from public API, run focused tests and diff hygiene, and forbid package/renderer/Demo/ledger edits.</mitigations>
  <residual_risk>The `0.25` caps are provisional until Phase 36 output calibration and Phase 37 final lock; ABI compatibility for precompiled binaries is explicitly out of scope. Plan 35-03 must still prove that every resolver/conflict enumeration consumes these fields.</residual_risk>
</threat_model>

<verification>
Overall Plan 35-01 verification:

- Run `BeautyParametersTests`, `BeautySafetyCapsTests`, and `BeautyResourceCatalogTests` after their owning tasks.
- Prove exactly 33 public stored properties and exact `0.25` internal constants with structural scans.
- Prove all preset payloads remain absent of the new keys while catalog decoding yields zero.
- Run scoped `git diff --check`; confirm no preset JSON, package dependency, provider, resolver, renderer, Demo, ledger, or branch-status edit occurred.
</verification>

<success_criteria>
Plan 35-01 succeeds when the model has exactly 33 stored fields (32 numeric plus `filterId`), both new fields are independent positive-only values with default/non-finite zero behavior, literal legacy JSON and every bundled preset stay neutral, unequal new values round-trip, source-style initializers remain compatible, the two effective values plus provisional exact `0.25` caps compile under focused green tests, and ASVS Level 1 has no unresolved high-severity threat.
</success_criteria>

<output>
Create `.planning/phases/35-public-contract-and-independent-geometry/35-01-SUMMARY.md` when done.
</output>
