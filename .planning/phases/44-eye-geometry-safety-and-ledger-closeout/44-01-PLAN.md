---
phase: 44-eye-geometry-safety-and-ledger-closeout
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautySafetyCapsTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift
autonomous: true
requirements: [EYE-19, EYE-20]
must_haves:
  truths:
    - "D-01/D-02/D-04: ten exact caps, normalization classes, counts, warnings, and signed/positive-only directions are locked."
    - "D-03/D-05: exact gaze/symmetry dead zones and post-6e4704e aggregate gaze evidence remain authoritative."
    - "D-06..D-10: fourteen eye fields degrade at the narrowest dependency, transitions are stateless, and diagnostics stay aggregate."
    - "D-07: pupil/gaze and neutral symmetry fail locally without borrowing proxy, sibling, mirror, or cached evidence."
    - "D-08: provider-empty work is removed after preflight and final scale before every aggregate and dispatch surface."
    - "D-09: reused/stale eyes use complete-domain skip; all named fresh/reused/stale/no-face/support transitions are stateless."
  artifacts:
    - path: BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
      provides: exact cap/count/normalization evidence
    - path: BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift
      provides: dead-zone and field-local emission evidence
    - path: BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
      provides: degradation and transition evidence
  key_links:
    - from: BeautySafetyCaps
      to: BeautyEffectResolver
      via: capUnit/capSigned before accounting
    - from: EyeWarpFieldEmissions
      to: BeautyEffectPlan
      via: sanitizing before metrics and dispatch
  prohibitions:
    - statement: "No public geometry, invalid-support proxy fallback, promotion, or subjective naturalness claim."
      status: resolved
      verification: source-and-test
---

<objective>Freeze the ten evidence-backed eye caps and exhaustively prove field-local normalization, dead zones, observed-support dependencies, provider-empty removal, complete-eye degradation, redaction, and fresh/reused/stale/no-face transitions without changing public ranges or adding geometry surfaces.</objective>

<threat_model><scope>ASVS L1 input normalization, biometric-adjacent observed support, stale-state and diagnostic boundaries.</scope><blocking_rule>Any unresolved HIGH threat, raw geometry assertion failure, or effective/emission divergence blocks Plans 02-04 and promotion.</blocking_rule><assets>48-field public scalar compatibility, package-only contour/pupil support, aggregate redacted diagnostics, deterministic current-call plans.</assets><trust_boundaries>Untrusted public floats and Vision-derived support enter normalization, resolver, and provider eligibility before local warp dispatch.</trust_boundaries><threats>Non-finite/negative bypass, unsupported field surviving via sibling, stale vector carryover, public/raw geometry disclosure.</threats><mitigations>Exact cap/dead-zone tables, one-field provider assertions, transition fixtures, no-face facade redaction, focused/full SwiftPM tests.</mitigations><verification>Nonzero XCTest suites plus `git diff --check`; later boundary checker owns source scans.</verification><residual_risk>Combined convergence and active-source/promotion gates remain later plans.</residual_risk></threat_model>

<tasks>
<task type="test">
  <name>Task 44-01-01: Lock exact ten cap constants and cap-count semantics</name>
  <decision_refs>D-01, D-02, D-04</decision_refs>
  <done>Exact values, normalization, counts, warnings, directions, focused tests, and diff hygiene satisfy this task.</done>
  <files>BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift, BeautySDK/Tests/BeautyEffectsTests/BeautySafetyCapsTests.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift</files>
  <read_first>BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift; BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift; BeautySDK/Tests/BeautyEffectsTests/BeautySafetyCapsTests.swift; BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift; .planning/phases/44-eye-geometry-safety-and-ledger-closeout/44-CONTEXT.md</read_first>
  <action>Assert exact final values `eyeHeight=.35`, `eyeLength=.35`, `upperEyelidLift=.30`, `pupilSize=.25`, `gazeCorrection=.25`, `lowerEyelidDrop=.30`, `eyeTilt=.25`, `innerCornerOpen=.25`, `outerCornerOpen=.25`, and `eyeSymmetry=.25`; remove only the `Phase 42 provisional` wording after the assertions are green. Add table-driven resolver cases for zero, exact cap (capped count unchanged), public overflow (resolved exactly to cap and counted once), negative positive-only zero, NaN/+infinity/-infinity zero, and both signed eyeTilt directions. Preserve the existing aggregate capped warning/metric keys and forbid field/raw-geometry names.</action>
  <acceptance_criteria>BeautySafetyCaps contains all ten exact constants; exact-cap inputs produce no capped increment; each overflow increments capped count once and resolves exactly; positive-only negative/non-finite cases resolve zero; eyeTilt positive and negative signs remain; focused cap/resolver suites pass and diagnostics contain no raw geometry.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.BeautySafetyCapsTests && swift test --package-path BeautySDK --filter BeautyEffectsTests.BeautyEffectResolverTests && git diff --check</automated></verify>
</task>
<task type="test">
  <name>Task 44-01-02: Prove exact gaze and symmetry dead zones</name>
  <decision_refs>D-03, D-05</decision_refs>
  <done>Exact dead-zone boundaries and authoritative aggregate gaze evidence satisfy this task.</done>
  <files>BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift</files>
  <read_first>BeautySDK/Sources/BeautyEffects/Warp/EyeWarpProvider.swift; BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift; BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift; .planning/phases/43-public-facade-eye-geometry-output-evidence/43-VERIFICATION.md</read_first>
  <action>Add deterministic boundary fixtures around gaze pupil-to-own-center distance `0.002` and symmetry measured span/tilt delta `0.0001`. Assert distances at or below each threshold emit no work, values just above emit bounded work, cap gaze uses at most `0.35` correction fraction, symmetry uses at most `0.30` midpoint blend, and positive/negative tilt vectors remain opposite. Reuse `gazeCorrectionEvidence` as the only aggregate automatic-correction proof; do not reintroduce RGB mirror scoring or record coordinates.</action>
  <acceptance_criteria>Gaze exact dead-zone and monotonic aggregate tests pass at both sides of `.002`; symmetry exact neutral tests pass at both sides of `.0001`; all emitted points are finite/unit-bounded and signed tilt directions differ; no raw support appears in test-facing plan/diagnostics.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.EyeWarpProviderTests && git diff --check</automated></verify>
</task>
<task type="test">
  <name>Task 44-01-03: Lock fourteen-field dependency and provider-empty matrix</name>
  <decision_refs>D-06, D-07, D-08</decision_refs>
  <done>Fourteen-field dependency/provider-empty tests satisfy this task.</done>
  <files>BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift, BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift</files>
  <read_first>BeautySDK/Sources/BeautyEffects/Warp/EyeWarpProvider.swift; BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift; BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift; BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift</read_first>
  <action>Build a table for all fourteen eye names. Assert complete observed contours emit each eligible field independently; missing/implausible pupil zeros only `pupilSize` and `gazeCorrection`; neutral gaze/symmetry remove only their own work; malformed/side-inverted/duplicate/coincident or missing either contour removes all fourteen with no proxy fallback; each provider-empty field is removed from strengths, active domains, point counts, totals, warnings, metrics, and dispatch while valid siblings remain. Keep nil observed support on the Phase 41 compatibility path only.</action>
  <acceptance_criteria>Fourteen-field named emission matrix passes; missing pupil preserves contour siblings; malformed/missing contour produces complete-eye skip; every final-empty field is absent from accounting and dispatch; explicit invalid support never synthesizes a proxy.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.EyeWarpProviderTests && swift test --package-path BeautySDK --filter BeautyEffectsTests.MissingLandmarkDegradationTests && git diff --check</automated></verify>
</task>
<task type="test">
  <name>Task 44-01-04: Cover no-face, freshness, transitions, and public redaction</name>
  <decision_refs>D-09, D-10</decision_refs>
  <done>Every named transition, safe sibling, facade extent, and redaction check satisfies this task.</done>
  <files>BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift, BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift</files>
  <read_first>BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift; BeautySDK/Sources/BeautySDK/BeautyEngine.swift; BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift; BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift; .planning/milestones/v1.10-phases/40-mouth-geometry-safety-and-ledger-closeout/40-VERIFICATION.md</read_first>
  <action>Extend transition coverage across fresh->reused, reused->fresh, fresh->stale, stale->fresh, face->no-face, missing contour, missing/implausible pupil, neutral support, and provider-empty states. Assert reused/stale complete-eye skip and zero fourteen eye strengths/emissions, safe non-eye/color/filter domains continue, no prior vectors survive, facade extent/no-face behavior remains unchanged, and warning/metric output remains category-only and aggregate.</action>
  <acceptance_criteria>All named transitions pass with no retained stale work; no-face facade preserves extent and redacted result; safe non-eye siblings continue; all fourteen eye fields are zero/skipped when complete-eye support is invalid; focused degradation/facade suites pass.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.MissingLandmarkDegradationTests && swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyEngineGeometryFacadeTests && git diff --check</automated></verify>
</task>
</tasks>

<verification>Run all focused suites above, then `swift test --package-path BeautySDK`; confirm no source API, Demo, Package.swift, dependency, or generated-artifact drift. Record requirement-named evidence in `44-EYE-SAFETY-EVIDENCE.md` only in Plan 03.</verification>
<success_criteria>EYE-19 exact caps/dead zones/counts and EYE-20 fourteen-field dependency/degradation/transition behavior are executable, redacted, and green; no promotion or boundary claim is made.</success_criteria>

## Artifacts this phase produces

- Extended exact cap/dead-zone/degradation XCTest coverage in the listed existing test files.
- No new public types, fields, packages, renderer cases, or generated images.
