---
phase: 24-renderer-output-regression-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift
autonomous: true
requirements:
  - RENDER-01
  - RENDER-02
user_setup: []
must_haves:
  truths:
    - "D-01/D-02/D-04: BeautyExampleRenderer remains the code-owned source of truth for exactly the current 9 visible skin/color/filter case IDs."
    - "D-05/D-06/D-08: Default BeautyParameters preserve fixture pixels before renderer watermarking, with exact equality unless a specific platform color-management reason is documented."
    - "D-07: No-op fixture coverage includes example-images/input/e1.png through example-images/input/e5.png."
    - "RENDER-01: The public-facade renderer matrix is guarded by a focused SwiftPM regression test."
    - "RENDER-02: The no-op path is guarded at BeautyEngine.processResult(image:metadata:parameters:) before watermarking."
  artifacts:
    - path: "BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift"
      provides: "Focused renderer matrix and no-op fixture regression tests"
      exports: ["BeautyRendererOutputRegressionTests", "testRendererCaseInventoryMatchesCurrentPublicFacadeMatrix", "testDefaultParametersPreserveCurrentFixturePixelsBeforeWatermark"]
    - path: ".planning/phases/24-renderer-output-regression-hardening/check_renderer_outputs.py"
      provides: "Generated PNG invariant helper for 5 fixtures x 9 cases"
    - path: ".planning/phases/24-renderer-output-regression-hardening/24-RENDERER-EVIDENCE.md"
      provides: "Renderer command, invariant, watermark, non-claim, and requirement evidence"
    - path: "docs/meitu-function-blueprint/EXAMPLE_IMAGE_VALIDATION.md"
      provides: "Durable public-facade renderer matrix and rerun rules"
    - path: ".planning/phases/24-renderer-output-regression-hardening/24-VERIFICATION.md"
      provides: "Final geometry/no-overclaim and requirement verification ledger"
  key_links:
    - from: "BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift"
      to: "BeautySDK/Sources/BeautyExampleRenderer/main.swift"
      via: "source-file case inventory scan"
      pattern: "id: \\\"skinSmoothing_0p50\\\""
    - from: "BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift"
      to: "BeautySDK/Sources/BeautySDK/BeautyEngine.swift"
      via: "BeautyEngine.processResult(image:metadata:parameters:)"
      pattern: "processResult\\("
---

<objective>
Create the focused SwiftPM regression gate for the current public-facade renderer matrix and pre-watermark default/no-op fixture behavior.

Purpose: Phase 24 must promote `BeautyExampleRenderer` from closeout evidence into a stable regression gate without adding renderer cases, public parameters, Demo UI, or geometry saved-output scope.
Output: `BeautyRendererOutputRegressionTests.swift` with exact 9-case inventory coverage and all-five-fixture no-op pixel preservation coverage.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@ARCHITECTURE.md
@DESIGN.md
@SECURITY.md
@RELIABILITY.md
@PRODUCT_SENSE.md
@QUALITY_SCORE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/24-renderer-output-regression-hardening/24-CONTEXT.md
@.planning/phases/24-renderer-output-regression-hardening/24-RESEARCH.md
@.planning/phases/24-renderer-output-regression-hardening/24-VALIDATION.md
@.planning/phases/24-renderer-output-regression-hardening/24-PATTERNS.md
@BeautySDK/Package.swift
@BeautySDK/Sources/BeautyExampleRenderer/main.swift
@BeautySDK/Sources/BeautySDK/BeautyEngine.swift
@BeautySDK/Tests/BeautyCoreTests/BeautyEngineTests.swift
@docs/meitu-function-blueprint/EXAMPLE_IMAGE_VALIDATION.md
@docs/meitu-function-blueprint/FEATURE_MATRIX.md
</context>

## Artifacts this phase produces

- Test file: `BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift`.
- Test class: `BeautyRendererOutputRegressionTests`.
- Test methods: `testRendererCaseInventoryMatchesCurrentPublicFacadeMatrix`, `testDefaultParametersPreserveCurrentFixturePixelsBeforeWatermark`.
- Test helpers: `rendererSourceURL()`, `exampleFixtureURLs()`, `renderedRGBABytes(from:)`, `rendererCaseIDs(in:)`, and `relativeFixtureName(for:)`.
- Evidence helper: `.planning/phases/24-renderer-output-regression-hardening/check_renderer_outputs.py`.
- Evidence artifact: `.planning/phases/24-renderer-output-regression-hardening/24-RENDERER-EVIDENCE.md`.
- Durable doc artifact: `docs/meitu-function-blueprint/EXAMPLE_IMAGE_VALIDATION.md`.
- Verification artifact: `.planning/phases/24-renderer-output-regression-hardening/24-VERIFICATION.md`.
- Ledger artifacts: `.planning/phases/24-renderer-output-regression-hardening/24-VALIDATION.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, `QUALITY_SCORE.md`, and `PLANS.md`.
- Commands: `swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyRendererOutputRegressionTests`, `swift test --package-path BeautySDK`, `swift build --package-path BeautySDK --product BeautyExampleRenderer`, `swift run --package-path BeautySDK BeautyExampleRenderer --input example-images/input --output example-images/out`, `python3 .planning/phases/24-renderer-output-regression-hardening/check_renderer_outputs.py --input example-images/input --output example-images/out`, and scoped `rg` no-overclaim/import/status scans.

<tasks>

<task type="auto">
  <name>Task 24-01-01: Add the code-owned renderer case inventory regression</name>
  <files>BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift</files>
  <read_first>
    @AGENTS.md
    @PLANS.md
    @ARCHITECTURE.md
    @.planning/phases/24-renderer-output-regression-hardening/24-CONTEXT.md
    @.planning/phases/24-renderer-output-regression-hardening/24-RESEARCH.md
    @.planning/phases/24-renderer-output-regression-hardening/24-PATTERNS.md
    @BeautySDK/Package.swift
    @BeautySDK/Sources/BeautyExampleRenderer/main.swift
    @BeautySDK/Tests/BeautyCoreTests/BeautyEngineTests.swift
    @docs/meitu-function-blueprint/EXAMPLE_IMAGE_VALIDATION.md
  </read_first>
  <action>Create `BeautyRendererOutputRegressionTests.swift` in `BeautyCoreTests` with imports limited to `CoreImage`, `ImageIO`, `XCTest`, and `BeautySDK`. Add `testRendererCaseInventoryMatchesCurrentPublicFacadeMatrix` for RENDER-01 per D-01, D-02, and D-04. The test reads `BeautySDK/Sources/BeautyExampleRenderer/main.swift`, extracts `RenderCase` IDs in declaration order, and asserts exactly these 9 IDs: `skinSmoothing_0p50`, `skinWhitening_0p50`, `skinRosy_0p40`, `skinSharpen_0p40`, `brightness_plus0p25`, `contrast_plus0p25`, `filter_softClean_0p50`, `filter_warmLight_0p50`, and `skinCombo_0p50`. Also assert the executable source imports `BeautySDK` and does not import `BeautyCore`, `BeautyDetection`, `BeautyEffects`, `BeautyRender`, or `BeautyResources`; keep the test failure messages to relative file names and case IDs only.</action>
  <acceptance_criteria>
    - Source assertion: `BeautyRendererOutputRegressionTests.swift` contains `final class BeautyRendererOutputRegressionTests: XCTestCase`.
    - Behavior assertion: `testRendererCaseInventoryMatchesCurrentPublicFacadeMatrix` fails if any current renderer case ID is added, removed, renamed, or reordered without an intentional test update.
    - Boundary assertion: the new test imports only the public `BeautySDK` facade from SDK targets and verifies `BeautyExampleRenderer/main.swift` remains public-facade scoped.
    - Scope assertion: no geometry or new product-feature case IDs are added to `BeautyExampleRenderer/main.swift` in this task.
    - CLI assertion: the focused test method passes with the automated command below.
  </acceptance_criteria>
  <verify>
    <automated>swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyRendererOutputRegressionTests/testRendererCaseInventoryMatchesCurrentPublicFacadeMatrix</automated>
    <automated>! rg -n 'import Beauty(Core|Detection|Effects|Render|Resources)' BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift BeautySDK/Sources/BeautyExampleRenderer/main.swift</automated>
  </verify>
  <done>The exact current 9-case code-owned renderer matrix is protected by a focused SwiftPM regression test, and the public-facade boundary is machine-checked for RENDER-01.</done>
</task>

<task type="auto">
  <name>Task 24-01-02: Add pre-watermark no-op fixture pixel regression</name>
  <files>BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift</files>
  <read_first>
    @SECURITY.md
    @RELIABILITY.md
    @PRODUCT_SENSE.md
    @.planning/phases/24-renderer-output-regression-hardening/24-CONTEXT.md
    @.planning/phases/24-renderer-output-regression-hardening/24-RESEARCH.md
    @.planning/phases/24-renderer-output-regression-hardening/24-VALIDATION.md
    @.planning/phases/24-renderer-output-regression-hardening/24-PATTERNS.md
    @BeautySDK/Sources/BeautyExampleRenderer/main.swift
    @BeautySDK/Sources/BeautySDK/BeautyEngine.swift
    @BeautySDK/Tests/BeautyCoreTests/BeautyEngineTests.swift
    @example-images/input/e1.png
    @example-images/input/e2.png
    @example-images/input/e3.png
    @example-images/input/e4.png
    @example-images/input/e5.png
  </read_first>
  <action>Extend `BeautyRendererOutputRegressionTests.swift` with `testDefaultParametersPreserveCurrentFixturePixelsBeforeWatermark` for RENDER-02 per D-05, D-06, D-07, and D-08. Resolve the repository root from the test file location, load exactly `example-images/input/e1.png` through `e5.png` with orientation applied, call `BeautyEngine.processResult(image:metadata:parameters:)` using default `BeautyParameters` and `BeautyInputMetadata(orientation: .up, source: .testFixture)`, and compare input versus result output before any renderer watermark is drawn. Add `renderedRGBABytes(from:)` that uses the same fixed DeviceRGB `CIContext`, RGBA8 format, row bytes, and full image bounds for input and output. Start with exact byte equality; do not introduce tolerance unless the execution artifact records a specific platform color-management reason and the smallest explicit tolerance, otherwise deterministic drift is a hard failure.</action>
  <acceptance_criteria>
    - Source assertion: the test explicitly names and loads `e1.png`, `e2.png`, `e3.png`, `e4.png`, and `e5.png`.
    - Behavior assertion: each fixture keeps the same extent after `BeautyEngine.processResult(image:metadata:parameters:)` with default `BeautyParameters`.
    - Behavior assertion: each fixture has exact rendered RGBA byte equality before watermarking unless a documented platform color-management tolerance is intentionally added with a specific reason.
    - Security assertion: test messages and helper output avoid image bytes, absolute local paths, face geometry payloads, raw framework errors, and serialized diagnostics.
    - CLI assertion: the full focused renderer regression test class passes with the automated command below.
  </acceptance_criteria>
  <verify>
    <automated>swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyRendererOutputRegressionTests</automated>
    <automated>swift test --package-path BeautySDK</automated>
  </verify>
  <done>All five current example fixtures have a pre-watermark public-facade default/no-op regression check for RENDER-02, preserving D-05 through D-08 without committed PNG baselines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository files -> XCTest | Tests read renderer source and fixture files as local inputs. |
| Fixture pixels -> test assertions | Sensitive image bytes are rendered in memory and must not be printed or committed. |
| Public facade -> regression gate | Main renderer regression must use `BeautySDK`, not internal SDK targets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01-01 | Tampering | `BeautyExampleRenderer/main.swift` case matrix | mitigate | Exact 9-case source inventory test for D-01/D-02/D-04 fails on unplanned case drift. |
| T-24-01-02 | Information Disclosure | Fixture no-op regression output | mitigate | Keep comparisons in memory; failure messages use relative fixture names only and never print bytes, absolute paths, face geometry, raw framework errors, or serialized diagnostics. |
| T-24-01-03 | Elevation of Privilege | Public facade boundary | mitigate | New test imports `BeautySDK` only from SDK targets and scans renderer/test sources for internal SDK imports. |
| T-24-01-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install is planned; Phase 24 uses SwiftPM, XCTest, Python standard library, and `rg` only. |
</threat_model>

<verification>
Plan-level automated checks:

- `swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyRendererOutputRegressionTests`
- `swift test --package-path BeautySDK`
- `! rg -n 'import Beauty(Core|Detection|Effects|Render|Resources)' BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift BeautySDK/Sources/BeautyExampleRenderer/main.swift`
- `git diff --check -- BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift`
</verification>

<success_criteria>
- RENDER-01 is protected by an exact code-owned renderer case inventory test tied to `BeautyExampleRenderer/main.swift`.
- RENDER-02 is protected by a pre-watermark no-op fixture regression across `e1.png` through `e5.png`.
- The test gate uses only the public `BeautySDK` facade and does not add renderer cases, geometry implementation, public parameters, Demo UI, product routes, PNG baselines, network behavior, or quality/parity claims.
</success_criteria>

<output>
Create `.planning/phases/24-renderer-output-regression-hardening/24-01-SUMMARY.md` when done.
</output>
