---
phase: 19-beauty-shaping-core-modules
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - BeautySDK/Sources/BeautyEffects/Warp/FaceShapeWarpProvider.swift
  - BeautySDK/Sources/BeautyEffects/Warp/ChinWarpProvider.swift
  - BeautySDK/Sources/BeautyEffects/Warp/EyeWarpProvider.swift
  - BeautySDK/Sources/BeautyEffects/Warp/NoseWarpProvider.swift
  - BeautySDK/Sources/BeautyEffects/Warp/MouthWarpProvider.swift
  - BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift
  - BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
  - BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/NoseWarpProviderTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/MouthWarpProviderTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/LipColorEffectTests.swift
autonomous: true
requirements:
  - BSHAPE-02
  - BSHAPE-03
must_haves:
  truths:
    - "D-01/D-02/D-03: All implementation work remains inside `BeautySDK` sources/tests and uses SwiftPM verification, with no Demo or SwiftUI work."
    - "D-04/D-05/D-06: Provider/control-point/MVP proxy evidence remains internal evidence and does not wire public facade saved-image geometry output."
    - "D-07/D-08/D-09: Production code uses only existing public shaping/lip fields and adds no `BeautyParameters` fields, coding keys, initializer arguments, or normalized copies."
    - "D-10/D-11/D-12/D-13/D-14: Face/proportion, eye, nose, mouth/lip behavior is strengthened only within current partial branch coverage."
    - "D-15/D-16: No pose-aware 3D sculpt, eyebrow geometry, texture synthesis, resource overlays, or public eyebrow parameters are introduced."
    - "D-17/D-18/D-19/D-20: Focused shaping tests and redaction scans prove safety/degradation without requiring renderer geometry cases."
  artifacts:
    - path: "BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift"
      provides: "Face-shape, chin, and proportion-provider evidence for existing public fields."
    - path: "BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift"
      provides: "Eye-provider evidence for `eyeSize`, `eyeDistance`, `eyeYPosition`, and `eyeTailLift`."
    - path: "BeautySDK/Tests/BeautyEffectsTests/NoseWarpProviderTests.swift"
      provides: "Nose-provider evidence for `noseSlim`, `noseWingSlim`, `noseTipSize`, and `noseBridge`."
    - path: "BeautySDK/Tests/BeautyEffectsTests/MouthWarpProviderTests.swift"
      provides: "Mouth-provider evidence for `mouthSize`, `mouthWidth`, and `smile`."
    - path: "BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift"
      provides: "Missing, stale, reused, and redacted degradation evidence for shaping branches."
  key_links:
    - from: ".planning/phases/19-beauty-shaping-core-modules/19-SHAPING-AUDIT.md"
      to: "BeautySDK/Sources/BeautyEffects/Warp"
      via: "Plan 19-01 gap list drives bounded provider/test hardening"
      pattern: "Gap|Provider|Test"
    - from: "BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift"
      to: "BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift"
      via: "domain-specific skippedDomains/warnings/metrics assertions"
      pattern: "skippedDomains|geometry_stale|inputs_missing"
---

<objective>
Strengthen promoted shaping module logic and integration-prep evidence behind SDK boundaries using only existing public parameters and existing safety/degradation patterns.

Purpose: This plan closes BSHAPE-02 implementation/test gaps from `19-SHAPING-AUDIT.md` without public API drift, Demo/UI scope creep, renderer geometry cases, or false saved-image claims.
Output: Focused source/test hardening in `BeautyEffects` and `BeautyEffectsTests`.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-beauty-shaping-core-modules/19-CONTEXT.md
@.planning/phases/19-beauty-shaping-core-modules/19-RESEARCH.md
@.planning/phases/19-beauty-shaping-core-modules/19-VALIDATION.md
@.planning/phases/19-beauty-shaping-core-modules/19-PATTERNS.md
@.planning/phases/19-beauty-shaping-core-modules/19-SHAPING-AUDIT.md
</context>

## Source Coverage Audit

| Source | Items | Coverage |
| --- | --- | --- |
| GOAL | Implement or prepare promoted face/facial-feature shaping logic behind SDK boundaries, with honest geometry-output status. | This plan implements/hardens SDK provider, resolver, conflict, and degradation evidence; Plan 19-03 handles final status docs and scans. |
| REQ | BSHAPE-02, BSHAPE-03 | BSHAPE-02 is the primary requirement; BSHAPE-03 is preserved by keeping geometry evidence internal and branch statuses below `implemented`. |
| RESEARCH | Use existing `BeautyEffects` providers, `BeautyEffectResolver`, `BeautySafetyCaps`, `GeometryConflictResolver`, and XCTest. | Tasks target those exact files and add no external packages, no new public API, no UI, and no renderer geometry cases. |
| CONTEXT | D-01 through D-20 | D-01 through D-20 are cited in frontmatter and actions; deferred public facade geometry output, advanced shaping parameters, 3D sculpt, and eyebrows stay out of scope. |

## Artifacts this phase produces

- Updated or newly added XCTest methods in `FaceShapeWarpProviderTests`, `EyeWarpProviderTests`, `NoseWarpProviderTests`, `MouthWarpProviderTests`, `GeometryConflictResolverTests`, `MissingLandmarkDegradationTests`, `BeautyEffectResolverTests`, `CombinedEffectSafetyTests`, and `LipColorEffectTests`.
- Source-level hardening in `BeautyEffects` provider/resolver/conflict files only when a new assertion exposes a real gap.
- No new public `BeautyParameters` fields, no SwiftUI/Demo files, no `BeautyExampleRenderer` geometry cases, no public facade geometry saved-image output, and no new public SDK symbols are produced by this plan.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden face-shape, chin, and proportion provider evidence</name>
  <files>BeautySDK/Sources/BeautyEffects/Warp/FaceShapeWarpProvider.swift, BeautySDK/Sources/BeautyEffects/Warp/ChinWarpProvider.swift, BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift, BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift, BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift</files>
  <read_first>
    - `.planning/phases/19-beauty-shaping-core-modules/19-SHAPING-AUDIT.md`
    - `.planning/phases/19-beauty-shaping-core-modules/19-CONTEXT.md`
    - `.planning/phases/19-beauty-shaping-core-modules/19-PATTERNS.md`
    - `BeautySDK/Sources/BeautyEffects/Warp/FaceShapeWarpProvider.swift`
    - `BeautySDK/Sources/BeautyEffects/Warp/ChinWarpProvider.swift`
    - `BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift`
    - `BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift`
    - `BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift`
    - `BeautySDK/Sources/BeautyEffects/Render/BeautyGeometryEffectPipeline.swift`
    - `BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift`
    - `BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift`
  </read_first>
  <behavior>
    - `faceSlim`, `faceSmall`, `faceVShape`, `jawSlim`, and `chinLength` produce deterministic, clamped control points from `.fixture` and report `missing_face_contour` when contour input is absent.
    - `faceSmall` may be documented in tests as proportion-adjacent partial coverage, but no forehead, mid-face, philtrum, lower-face, short-face, or head-face parameter is added.
    - Combined geometry weakening applies to the existing shaping strengths and reports only `combined_geometry_weakened`, `beauty.effects.weakenedCount`, and `beauty.effects.geometryStrengthScale`.
  </behavior>
  <action>Add failing assertions first for any face-shape/proportion gaps recorded in `19-SHAPING-AUDIT.md`, then update `FaceShapeWarpProvider`, `ChinWarpProvider`, or `GeometryConflictResolver` only if those assertions expose an actual source bug. Keep the implementation bounded to D-07 fields `faceSlim`, `faceSmall`, `faceVShape`, `jawSlim`, and `chinLength`; keep `比例` partial per D-14; keep `脸型` partial per D-10; do not add D-08/D-09 advanced controls; do not touch `BeautyParameters`, `BeautyExampleRenderer`, `BeautyEngine.processResult(image:)`, or Demo/UI files per D-01 through D-06.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter FaceShapeWarpProviderTests</automated>
    <automated>swift test --package-path BeautySDK --filter GeometryConflictResolverTests</automated>
    <automated>git diff --check -- BeautySDK/Sources/BeautyEffects/Warp/FaceShapeWarpProvider.swift BeautySDK/Sources/BeautyEffects/Warp/ChinWarpProvider.swift BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove face/chin/proportion-adjacent control points are clamped, deterministic, and capped.
    - `GeometryConflictResolver` still weakens combined current shaping fields without leaking raw geometry values.
    - No public parameter, renderer, facade, or UI file is modified by this task.
  </acceptance_criteria>
  <done>Face-shape/chin/proportion provider evidence is stronger while branch status remains `partial`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden eye and nose provider/degradation evidence</name>
  <files>BeautySDK/Sources/BeautyEffects/Warp/EyeWarpProvider.swift, BeautySDK/Sources/BeautyEffects/Warp/NoseWarpProvider.swift, BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift, BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift, BeautySDK/Tests/BeautyEffectsTests/NoseWarpProviderTests.swift, BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift</files>
  <read_first>
    - `.planning/phases/19-beauty-shaping-core-modules/19-SHAPING-AUDIT.md`
    - `.planning/phases/19-beauty-shaping-core-modules/19-CONTEXT.md`
    - `.planning/phases/19-beauty-shaping-core-modules/19-PATTERNS.md`
    - `BeautySDK/Sources/BeautyEffects/Warp/EyeWarpProvider.swift`
    - `BeautySDK/Sources/BeautyEffects/Warp/NoseWarpProvider.swift`
    - `BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift`
    - `BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift`
    - `BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift`
    - `BeautySDK/Tests/BeautyEffectsTests/NoseWarpProviderTests.swift`
    - `BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift`
  </read_first>
  <behavior>
    - `eyeSize`, `eyeDistance`, `eyeYPosition`, and `eyeTailLift` activate only the eye domain when valid eye landmarks exist and skip with `eye_inputs_missing` when eye inputs are incomplete.
    - `noseSlim`, `noseWingSlim`, `noseTipSize`, and `noseBridge` activate only the nose domain when valid nose landmarks exist and skip with `nose_inputs_missing` when nose inputs are incomplete.
    - Missing, stale, and reused geometry keep unaffected safe domains active and emit redacted warnings/metrics only.
  </behavior>
  <action>Add missing assertions from `19-SHAPING-AUDIT.md` to `EyeWarpProviderTests`, `NoseWarpProviderTests`, and `MissingLandmarkDegradationTests`; then change `EyeWarpProvider`, `NoseWarpProvider`, or `BeautyEffectResolver` only if needed for those assertions to pass. Preserve D-11 and D-12 partial status, D-07 field boundaries, D-18 focused suite names, and D-19 redaction requirements. Do not add eye height/length, pupil/gaze, nose lift/root split, or any D-08 advanced public parameter.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter EyeWarpProviderTests</automated>
    <automated>swift test --package-path BeautySDK --filter NoseWarpProviderTests</automated>
    <automated>swift test --package-path BeautySDK --filter MissingLandmarkDegradationTests</automated>
    <automated>git diff --check -- BeautySDK/Sources/BeautyEffects/Warp/EyeWarpProvider.swift BeautySDK/Sources/BeautyEffects/Warp/NoseWarpProvider.swift BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift BeautySDK/Tests/BeautyEffectsTests/NoseWarpProviderTests.swift BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - Eye and nose focused tests cover valid landmarks, missing inputs, caps, stale/reused degradation, and redacted metadata.
    - Safe domains such as color/filter remain active when only eye or nose landmarks are missing.
    - No new public eye or nose controls appear in `BeautyParameters`.
  </acceptance_criteria>
  <done>Eye and nose shaping evidence is strengthened behind SDK boundaries while statuses remain `partial`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Harden mouth, lip-color, resolver, and combined redaction evidence</name>
  <files>BeautySDK/Sources/BeautyEffects/Warp/MouthWarpProvider.swift, BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift, BeautySDK/Tests/BeautyEffectsTests/MouthWarpProviderTests.swift, BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift, BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift, BeautySDK/Tests/BeautyEffectsTests/LipColorEffectTests.swift</files>
  <read_first>
    - `.planning/phases/19-beauty-shaping-core-modules/19-SHAPING-AUDIT.md`
    - `.planning/phases/19-beauty-shaping-core-modules/19-CONTEXT.md`
    - `.planning/phases/19-beauty-shaping-core-modules/19-PATTERNS.md`
    - `BeautySDK/Sources/BeautyEffects/Warp/MouthWarpProvider.swift`
    - `BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift`
    - `BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift`
    - `BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift`
    - `BeautySDK/Sources/BeautyEffects/Render/BeautyColorEffectPipeline.swift`
    - `BeautySDK/Tests/BeautyEffectsTests/MouthWarpProviderTests.swift`
    - `BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift`
    - `BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift`
    - `BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift`
    - `BeautySDK/Tests/BeautyEffectsTests/LipColorEffectTests.swift`
  </read_first>
  <behavior>
    - `mouthSize`, `mouthWidth`, and `smile` produce deterministic, clamped mouth control points when mouth landmarks exist and skip with `mouth_inputs_missing` when lip inputs are absent.
    - `lipColor` remains visible color evidence for a subtool, but the broader `嘴唇` branch remains partial because mouth geometry still lacks public facade saved-image output.
    - Warning and metric metadata never exposes landmarks, control points, bounding boxes, raw Vision objects, local paths, image bytes, `SIMD`, or point arrays.
  </behavior>
  <action>Add missing assertions from `19-SHAPING-AUDIT.md` to mouth/lip/resolver/combined tests, then update `MouthWarpProvider` or `BeautyEffectResolver` only if those assertions expose a real source defect. Preserve D-13 partial status and the D-19 redaction surface; `lipColor` may remain visible color evidence but must not make the full lips branch `implemented`. Do not add M-lip, teeth, mouth position/tilt/left-right controls, public facade geometry output, renderer geometry cases, or UI state per D-01 through D-09 and D-13/D-20.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter MouthWarpProviderTests</automated>
    <automated>swift test --package-path BeautySDK --filter MissingLandmarkDegradationTests</automated>
    <automated>swift test --package-path BeautySDK --filter BeautyEffectResolverTests</automated>
    <automated>swift test --package-path BeautySDK --filter CombinedEffectSafetyTests</automated>
    <automated>swift test --package-path BeautySDK --filter LipColorEffectTests</automated>
    <automated>git diff --check -- BeautySDK/Sources/BeautyEffects/Warp/MouthWarpProvider.swift BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift BeautySDK/Tests/BeautyEffectsTests/MouthWarpProviderTests.swift BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift BeautySDK/Tests/BeautyEffectsTests/LipColorEffectTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - Mouth geometry tests cover valid landmarks, missing mouth inputs, signed size/width behavior, caps, and redacted degradation.
    - Lip-color tests still prove visible color evidence without claiming full mouth/lips branch completion.
    - Resolver and combined tests scan warnings/metrics for sensitive diagnostic terms and fail if raw geometry data is exposed.
  </acceptance_criteria>
  <done>Mouth, lip-color, resolver, and combined safety evidence is strengthened while the lips branch remains `partial`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Public host parameters to `BeautyEffects` internals | Only D-07 fields may affect Phase 19 shaping behavior. |
| Internal landmarks/control points to warnings/metrics | Diagnostics must stay redacted even when tests strengthen geometry evidence. |
| `BeautyEffects` provider evidence to public renderer | Internal control-point evidence must not be treated as saved-image output. |
| SDK implementation to Demo/UI | No SwiftUI, Demo route, category rail, tool panel, or app interaction state changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-19-01 | Repudiation | Tests and summaries | mitigate | Test names and summaries must say provider/resolver evidence supports `partial`; Plan 19-03 scans branch docs for beauty-shaping `implemented` overclaims. |
| T-19-02 | Tampering | `BeautyParameters` | mitigate | This plan does not list `BeautyParameters.swift` in `files_modified`; Plan 19-03 verifies no D-08/D-09 fields were added. |
| T-19-03 | Elevation of Privilege / Scope Creep | `BeautyDemo`, SwiftUI | mitigate | This plan only lists `BeautySDK/Sources/BeautyEffects` and `BeautySDK/Tests/BeautyEffectsTests`; no Demo/UI file is modified. |
| T-19-04 | Information Disclosure | `BeautyEffectResolver`, `GeometryConflictResolver`, tests | mitigate | Add/keep redaction assertions in `MissingLandmarkDegradationTests`, `BeautyEffectResolverTests`, and `CombinedEffectSafetyTests`; scan warning/metric text for landmarks, control points, bounding boxes, raw Vision objects, paths, and image bytes. |
| T-19-05 | Spoofing / Repudiation | `BeautyExampleRenderer`, public facade output | mitigate | This plan does not modify `BeautyExampleRenderer` or `BeautyEngine.processResult(image:)`; Plan 19-03 verifies no geometry renderer cases or saved-image geometry claims were added. |
| T-19-SC | Tampering | Package installs | accept | No npm/pip/cargo/SPM dependency install is part of this plan; SwiftPM uses existing local package targets. |
</threat_model>

<verification>
Plan-level verification:
- `swift test --package-path BeautySDK --filter FaceShapeWarpProviderTests`
- `swift test --package-path BeautySDK --filter EyeWarpProviderTests`
- `swift test --package-path BeautySDK --filter NoseWarpProviderTests`
- `swift test --package-path BeautySDK --filter MouthWarpProviderTests`
- `swift test --package-path BeautySDK --filter GeometryConflictResolverTests`
- `swift test --package-path BeautySDK --filter MissingLandmarkDegradationTests`
- `swift test --package-path BeautySDK --filter BeautyEffectResolverTests`
- `swift test --package-path BeautySDK --filter CombinedEffectSafetyTests`
- `swift test --package-path BeautySDK --filter LipColorEffectTests`
- `git diff --check -- BeautySDK/Sources/BeautyEffects BeautySDK/Tests/BeautyEffectsTests`
</verification>

<success_criteria>
- BSHAPE-02 promoted partial branches have stronger provider, cap, conflict, degradation, and redaction evidence.
- BSHAPE-03 is preserved: no beauty-shaping geometry branch is marked or implied as public facade saved-image complete.
- Existing public shaping/lip parameters are the only fields used.
- No SwiftUI/Demo scope, public facade geometry output, renderer geometry cases, 3D sculpt, eyebrow work, or external package install is introduced.
</success_criteria>

<output>
Create `.planning/phases/19-beauty-shaping-core-modules/19-02-SUMMARY.md` when done.
</output>
