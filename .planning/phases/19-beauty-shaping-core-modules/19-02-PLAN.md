---
phase: 19-beauty-shaping-core-modules
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - BeautySDK/Sources/BeautyEffects/Warp/FaceShapeWarpProvider.swift
  - BeautySDK/Sources/BeautyEffects/Warp/ChinWarpProvider.swift
  - BeautySDK/Sources/BeautyEffects/Warp/EyeWarpProvider.swift
  - BeautySDK/Sources/BeautyEffects/Warp/NoseWarpProvider.swift
  - BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift
  - BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/NoseWarpProviderTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift
autonomous: true
requirements:
  - BSHAPE-02
  - BSHAPE-03
must_haves:
  truths:
    - "D-01/D-02/D-03: Face/chin/proportion and eye/nose hardening remains inside `BeautySDK` sources/tests and uses SwiftPM verification, with no Demo or SwiftUI work."
    - "D-04/D-05/D-06: Provider/control-point evidence remains internal evidence and does not wire public facade saved-image geometry output."
    - "D-07/D-08/D-09: Production code uses only existing public shaping fields and adds no `BeautyParameters` fields, coding keys, initializer arguments, or normalized copies."
    - "D-10/D-11/D-12/D-14: Face/proportion, eye, and nose behavior is strengthened only within current partial branch coverage."
    - "D-15/D-16: No pose-aware 3D sculpt, eyebrow geometry, texture synthesis, resource overlays, or public eyebrow parameters are introduced."
    - "D-17/D-18/D-19/D-20: Focused provider tests prove safety without requiring renderer geometry cases."
  artifacts:
    - path: "BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift"
      provides: "Face-shape, chin, and proportion-adjacent provider evidence for existing public fields."
    - path: "BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift"
      provides: "Eye-provider evidence for `eyeSize`, `eyeDistance`, `eyeYPosition`, and `eyeTailLift`."
    - path: "BeautySDK/Tests/BeautyEffectsTests/NoseWarpProviderTests.swift"
      provides: "Nose-provider evidence for `noseSlim`, `noseWingSlim`, `noseTipSize`, and `noseBridge`."
    - path: "BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift"
      provides: "Combined face/eye/nose shaping weakening metadata evidence."
  key_links:
    - from: ".planning/phases/19-beauty-shaping-core-modules/19-SHAPING-AUDIT.md"
      to: "BeautySDK/Sources/BeautyEffects/Warp"
      via: "fixed provider evidence matrix for Plan 19-02"
      pattern: "faceSlim|faceSmall|eyeSize|noseSlim|combined_geometry_weakened"
    - from: "BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift"
      to: "BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift"
      via: "combined geometry weakening assertions"
      pattern: "combined_geometry_weakened|geometryStrengthScale|weakenedCount"
---

<objective>
Strengthen face-shape/chin/proportion-adjacent, eye, and nose provider evidence behind SDK boundaries using only existing public parameters and existing safety patterns.

Purpose: This plan closes a bounded part of BSHAPE-02 without public API drift, Demo/UI scope creep, renderer geometry cases, or false saved-image claims.
Output: Focused source/test hardening in `BeautyEffects` providers and provider tests.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-beauty-shaping-core-modules/19-CONTEXT.md
@.planning/phases/19-beauty-shaping-core-modules/19-RESEARCH.md
@.planning/phases/19-beauty-shaping-core-modules/19-VALIDATION.md
@.planning/phases/19-beauty-shaping-core-modules/19-PATTERNS.md
@.planning/phases/19-beauty-shaping-core-modules/19-SHAPING-AUDIT.md
</context>

## Source Coverage Audit

| Source | Items | Coverage |
| --- | --- | --- |
| GOAL | Implement or prepare promoted face/facial-feature shaping logic behind SDK boundaries, with honest geometry-output status. | This plan hardens the face/chin/proportion, eye, nose, and conflict provider slice; Plan 19-03 covers mouth/lip/resolver degradation; Plan 19-04/19-05 handle final verification and closeout. |
| REQ | BSHAPE-02, BSHAPE-03 | BSHAPE-02 is addressed through provider evidence; BSHAPE-03 is preserved by keeping geometry evidence internal and branch statuses below `implemented`. |
| RESEARCH | Use existing `BeautyEffects` providers, `BeautySafetyCaps`, `GeometryConflictResolver`, and XCTest. | Tasks target those exact files and add no external packages, no new public API, no UI, and no renderer geometry cases. |
| CONTEXT | D-01 through D-20 | D-01 through D-20 are cited in frontmatter and actions; deferred public facade geometry output, advanced shaping parameters, 3D sculpt, and eyebrows stay out of scope. |

## Artifacts this phase produces

- Updated or newly added XCTest methods in `FaceShapeWarpProviderTests`, `EyeWarpProviderTests`, `NoseWarpProviderTests`, and `GeometryConflictResolverTests`.
- Source-level hardening in `FaceShapeWarpProvider`, `ChinWarpProvider`, `EyeWarpProvider`, `NoseWarpProvider`, or `GeometryConflictResolver` only when the concrete provider assertions require it.
- No new public `BeautyParameters` fields, no SwiftUI/Demo files, no `BeautyExampleRenderer` geometry cases, no public facade geometry saved-image output, and no new public SDK symbols are produced by this plan.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden face-shape, chin, proportion, and conflict provider evidence</name>
  <files>BeautySDK/Sources/BeautyEffects/Warp/FaceShapeWarpProvider.swift, BeautySDK/Sources/BeautyEffects/Warp/ChinWarpProvider.swift, BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift, BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift, BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift</files>
  <read_first>
    - `.planning/phases/19-beauty-shaping-core-modules/19-SHAPING-AUDIT.md`
    - `.planning/phases/19-beauty-shaping-core-modules/19-CONTEXT.md`
    - `.planning/phases/19-beauty-shaping-core-modules/19-PATTERNS.md`
    - `BeautySDK/Sources/BeautyEffects/Warp/FaceShapeWarpProvider.swift`
    - `BeautySDK/Sources/BeautyEffects/Warp/ChinWarpProvider.swift`
    - `BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift`
    - `BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift`
    - `BeautySDK/Sources/BeautyEffects/Render/BeautyGeometryEffectPipeline.swift`
    - `BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift`
    - `BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift`
  </read_first>
  <behavior>
    - `faceSlim`, `faceSmall`, `faceVShape`, `jawSlim`, and `chinLength` produce deterministic, clamped control points from `.fixture` and report `missing_face_contour` when contour input is absent.
    - `faceSmall` is treated as proportion-adjacent partial coverage; no forehead, mid-face, philtrum, lower-face, short-face, or head-face parameter is added.
    - Combined geometry weakening applies to the existing shaping strengths and reports only `combined_geometry_weakened`, `beauty.effects.weakenedCount`, and `beauty.effects.geometryStrengthScale`.
  </behavior>
  <action>Write or keep the concrete Phase 19 assertions for face-shape/chin/proportion and combined weakening, then adjust `FaceShapeWarpProvider`, `ChinWarpProvider`, or `GeometryConflictResolver` only when those assertions require a source correction. Keep the implementation bounded to D-07 fields `faceSlim`, `faceSmall`, `faceVShape`, `jawSlim`, and `chinLength`; keep `比例` partial per D-14; keep `脸型` partial per D-10; do not add D-08/D-09 advanced controls; do not touch `BeautyParameters`, `BeautyExampleRenderer`, `BeautyEngine.processResult(image:)`, or Demo/UI files per D-01 through D-06.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter FaceShapeWarpProviderTests</automated>
    <automated>swift test --package-path BeautySDK --filter GeometryConflictResolverTests</automated>
    <automated>git diff --check -- BeautySDK/Sources/BeautyEffects/Warp/FaceShapeWarpProvider.swift BeautySDK/Sources/BeautyEffects/Warp/ChinWarpProvider.swift BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove face/chin/proportion-adjacent control points are clamped, deterministic, and capped.
    - `GeometryConflictResolver` still weakens combined current shaping fields without leaking raw geometry values.
    - No public parameter, renderer, facade, or UI file is modified by this task.
  </acceptance_criteria>
  <done>Face-shape/chin/proportion provider evidence is stronger while branch status remains `partial`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden eye and nose provider evidence</name>
  <files>BeautySDK/Sources/BeautyEffects/Warp/EyeWarpProvider.swift, BeautySDK/Sources/BeautyEffects/Warp/NoseWarpProvider.swift, BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift, BeautySDK/Tests/BeautyEffectsTests/NoseWarpProviderTests.swift</files>
  <read_first>
    - `.planning/phases/19-beauty-shaping-core-modules/19-SHAPING-AUDIT.md`
    - `.planning/phases/19-beauty-shaping-core-modules/19-CONTEXT.md`
    - `.planning/phases/19-beauty-shaping-core-modules/19-PATTERNS.md`
    - `BeautySDK/Sources/BeautyEffects/Warp/EyeWarpProvider.swift`
    - `BeautySDK/Sources/BeautyEffects/Warp/NoseWarpProvider.swift`
    - `BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift`
    - `BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift`
    - `BeautySDK/Tests/BeautyEffectsTests/NoseWarpProviderTests.swift`
  </read_first>
  <behavior>
    - `eyeSize`, `eyeDistance`, `eyeYPosition`, and `eyeTailLift` generate deterministic, clamped eye control points when valid eye landmarks exist and skip with `eye_inputs_missing` when eye inputs are incomplete.
    - `noseSlim`, `noseWingSlim`, `noseTipSize`, and `noseBridge` generate deterministic, clamped nose control points when valid nose landmarks exist and skip with `nose_inputs_missing` when nose inputs are incomplete.
    - Provider tests assert cap-aware strength behavior using existing public parameters only.
  </behavior>
  <action>Write or keep the concrete Phase 19 assertions for eye and nose provider behavior, then adjust `EyeWarpProvider` or `NoseWarpProvider` only when those assertions require a source correction. Preserve D-11 and D-12 partial status, D-07 field boundaries, D-18 focused suite names, and D-19 redaction requirements. Do not add eye height/length, pupil/gaze, nose lift/root split, or any D-08 advanced public parameter.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter EyeWarpProviderTests</automated>
    <automated>swift test --package-path BeautySDK --filter NoseWarpProviderTests</automated>
    <automated>git diff --check -- BeautySDK/Sources/BeautyEffects/Warp/EyeWarpProvider.swift BeautySDK/Sources/BeautyEffects/Warp/NoseWarpProvider.swift BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift BeautySDK/Tests/BeautyEffectsTests/NoseWarpProviderTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - Eye and nose focused tests cover valid landmarks, missing inputs, clamping, and caps.
    - Tests use only existing public eye and nose fields from D-07.
    - No new public eye or nose controls appear in `BeautyParameters`.
  </acceptance_criteria>
  <done>Eye and nose shaping provider evidence is strengthened behind SDK boundaries while statuses remain `partial`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Public host parameters to `BeautyEffects` internals | Only D-07 fields may affect Phase 19 shaping behavior. |
| Internal landmarks/control points to tests and diagnostics | Provider tests may inspect internals, but warnings/metrics must stay redacted. |
| `BeautyEffects` provider evidence to public renderer | Internal control-point evidence must not be treated as saved-image output. |
| SDK implementation to Demo/UI | No SwiftUI, Demo route, category rail, tool panel, or app interaction state changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-19-02-01 | Repudiation | Provider tests and summaries | mitigate | Test names and summaries must say provider evidence supports `partial`; Plan 19-04 scans branch docs for beauty-shaping `implemented` overclaims. |
| T-19-02-02 | Tampering | `BeautyParameters` | mitigate | This plan does not list `BeautyParameters.swift` in `files_modified`; Plan 19-05 verifies no D-08/D-09 fields were added. |
| T-19-02-03 | Elevation of Privilege / Scope Creep | `BeautyDemo`, SwiftUI | mitigate | This plan only lists `BeautySDK/Sources/BeautyEffects/Warp` and `BeautySDK/Tests/BeautyEffectsTests`; no Demo/UI file is modified. |
| T-19-02-04 | Information Disclosure | Provider evidence | mitigate | Keep raw control-point details in internal tests only; public warning/metric redaction is verified in Plan 19-03 and Plan 19-05. |
| T-19-02-05 | Spoofing / Repudiation | `BeautyExampleRenderer`, public facade output | mitigate | This plan does not modify `BeautyExampleRenderer` or `BeautyEngine.processResult(image:)`; Plan 19-05 verifies no geometry renderer cases or saved-image geometry claims were added. |
| T-19-02-SC | Tampering | Package installs | accept | No npm/pip/cargo/SPM dependency install is part of this plan; SwiftPM uses existing local package targets. |
</threat_model>

<verification>
Plan-level verification:
- `swift test --package-path BeautySDK --filter FaceShapeWarpProviderTests`
- `swift test --package-path BeautySDK --filter EyeWarpProviderTests`
- `swift test --package-path BeautySDK --filter NoseWarpProviderTests`
- `swift test --package-path BeautySDK --filter GeometryConflictResolverTests`
- `git diff --check -- BeautySDK/Sources/BeautyEffects/Warp/FaceShapeWarpProvider.swift BeautySDK/Sources/BeautyEffects/Warp/ChinWarpProvider.swift BeautySDK/Sources/BeautyEffects/Warp/EyeWarpProvider.swift BeautySDK/Sources/BeautyEffects/Warp/NoseWarpProvider.swift BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift BeautySDK/Tests/BeautyEffectsTests/NoseWarpProviderTests.swift BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift`
</verification>

<success_criteria>
- BSHAPE-02 face/chin/proportion, eye, nose, and conflict-provider evidence is stronger.
- BSHAPE-03 is preserved: no beauty-shaping geometry branch is marked or implied as public facade saved-image complete.
- Existing public shaping parameters are the only fields used.
- No SwiftUI/Demo scope, public facade geometry output, renderer geometry cases, 3D sculpt, eyebrow work, or external package install is introduced.
</success_criteria>

<output>
Create `.planning/phases/19-beauty-shaping-core-modules/19-02-SUMMARY.md` when done.
</output>
