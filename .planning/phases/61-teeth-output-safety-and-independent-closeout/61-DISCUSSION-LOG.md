# Phase 61: Teeth Output, Safety, and Independent Closeout - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-07
**Phase:** 61-teeth-output-safety-and-independent-closeout
**Areas discussed:** Public output matrix, strict decoded acceptance, adversarial protected-tissue safety, original-detail review, atomic promotion and sequencing

---

## Public output matrix

| Option | Description | Selected |
|--------|-------------|----------|
| One maximum case plus baseline | Add `teethWhitening_1p00` and compare it with the existing no-op case. | ✓ |
| Several strength variants | Add multiple public saved-output strengths. | |
| Broad matrix expansion | Rerender many or all feature combinations for teeth. | |

**User's choice:** `[auto]` One maximum case plus baseline (recommended default).
**Notes:** This is the smallest public-facade proof and preserves all existing cases.

---

## Strict decoded acceptance

| Option | Description | Selected |
|--------|-------------|----------|
| Authorized pair plus no-face | Use the private genuine positive/negative and a separate no-face input with frozen decoded bounds. | ✓ |
| Synthetic only | Avoid genuine saved-output evidence. | |
| Provider tests only | Infer public output from internal tests without saving/decoding facade output. | |

**User's choice:** `[auto]` Authorized pair plus no-face (recommended default).
**Notes:** The user has already supplied licensed positive/negative fixtures and expects autonomous execution.

---

## Adversarial protected-tissue safety

| Option | Description | Selected |
|--------|-------------|----------|
| Geometry plus final-output oracles | Combine color-independent perturbation truth with recolored protected pixels through the real pipeline. | ✓ |
| Mask containment only | Check only generated mask ownership. | |
| Visual review only | Depend on inspection without deterministic exact-pixel gates. | |

**User's choice:** `[auto]` Geometry plus final-output oracles (recommended default).
**Notes:** Synthetic challenges prove safety only and retain zero naturalness weight.

---

## Original-detail review

| Option | Description | Selected |
|--------|-------------|----------|
| Agent performs blinded review | Generate fresh production outputs, open at original detail, reveal roles only after judgments, and store fixed verdicts. | ✓ |
| Ask user again | Require another manual user comparison. | |
| Metrics only | Treat decoded aggregate thresholds as sufficient naturalness evidence. | |

**User's choice:** `[auto]` Agent performs blinded review (recommended default).
**Notes:** This follows the user's explicit request that the implementation call and visual comparison be performed autonomously.

---

## Atomic promotion and sequencing

| Option | Description | Selected |
|--------|-------------|----------|
| Evidence-gated transaction | Keep owners unchanged until every HIGH gate passes; then promote exactly `白牙` and close `嘴唇`, followed by independent verification. | ✓ |
| Early promotion | Change product status before full regression and review. | |
| No promotion | Leave a completed feature marked future. | |

**User's choice:** `[auto]` Evidence-gated transaction (recommended default).
**Notes:** `祛红血丝` and `去脂` remain disabled/future, `眼睛` remains partial, and Phase 62 stays blocked until post-promotion verification.

## the agent's Discretion

- Exact helper decomposition and temporary opaque names.
- Deterministic challenge pixel layouts and blinded montage layout.
- Frozen numeric thresholds within the accepted Phase 60 safety/naturalness bounds.
- Smallest table-driven XCTest organization that preserves all exact gates.

## Deferred Ideas

- Demo activation.
- Sclera evidence/provider/output work.
- Upper-eyelid fullness reduction.
