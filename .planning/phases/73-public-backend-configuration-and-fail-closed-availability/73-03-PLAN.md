---
phase: 73-public-backend-configuration-and-fail-closed-availability
plan: 03
type: execute
wave: 3
depends_on:
  - 73-02
files_modified:
  - scripts/check-backend-configuration.sh
  - scripts/run-no-skip-swiftpm.sh
  - ARCHITECTURE.md
  - DESIGN.md
  - SECURITY.md
  - RELIABILITY.md
  - PRODUCT_SENSE.md
  - QUALITY_SCORE.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/STACK.md
  - .planning/codebase/TESTING.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - PLANS.md
autonomous: true
requirements:
  - CONFIG-01
  - CONFIG-02
must_haves:
  truths:
    - "The archive-first SDK-owned gate validates public backend configuration before the full SwiftPM child and rejects selector/default/fallback drift through mutation checks."
    - "The mandatory no-skip wrapper runs configuration, runtime, CPU-reference, consumer, and SwiftPM checks with zero failures and zero unexpected skips."
    - "All current owner documents agree that CPU is the permanent reference, GPU is explicitly selectable, unavailable GPU is typed fail-closed, and UI/Demo/device/release claims remain excluded."
  artifacts:
    - path: "scripts/check-backend-configuration.sh"
      provides: "Static, mutation, focused-test, and aggregate availability gate for Phase 73"
      contains: "BeautyRenderBackend"
    - path: "scripts/run-no-skip-swiftpm.sh"
      provides: "Archive-first ordering with the Phase 73 configuration gate"
      contains: "check-backend-configuration.sh"
    - path: "ARCHITECTURE.md"
      provides: "Current public backend ownership and target dependency contract"
    - path: "DESIGN.md"
      provides: "Configuration field/default and fail-closed policy contract"
    - path: "SECURITY.md"
      provides: "Configuration/Metal capability trust boundary and no-fallback mitigation"
    - path: "RELIABILITY.md"
      provides: "Typed unavailable behavior and request-local selection/recovery contract"
    - path: "PRODUCT_SENSE.md"
      provides: "SDK integrator journey and explicit CPU/GPU acceptance boundary"
    - path: "QUALITY_SCORE.md"
      provides: "Measured Phase 73 test/gate evidence and current inventory"
  key_links:
    - from: "scripts/run-no-skip-swiftpm.sh"
      to: "scripts/check-backend-configuration.sh"
      via: "exactly-once archive-first invocation before consumer/oracle/opt-ins/child"
      pattern: "check-backend-configuration.sh"
    - from: "scripts/check-backend-configuration.sh"
      to: "BeautySDK/Tests/BeautyCoreTests/BeautyConfigurationTests.swift"
      via: "focused SwiftPM configuration and routing filter with exact nonzero accounting"
      pattern: "BeautyConfigurationTests"
    - from: "ARCHITECTURE.md"
      to: "BeautySDK/Sources/BeautySDK/BeautyBackendFactory.swift"
      via: "documented ownership of public policy to package executor selection"
      pattern: "BeautyBackendFactory"
---

<objective>
Close Phase 73 with an archive-first configuration gate and synchronized SDK owner documents.

Purpose: Make the public selector and fail-closed availability behavior durable, mutation-tested, and discoverable while preserving the SDK-only boundary and leaving parity/closeout evidence to Phase 74.
Output: A standalone configuration preflight, mandatory no-skip integration, updated current owners/ledgers, and exact Phase 73 completion evidence.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@PLANS.md
@ARCHITECTURE.md
@DESIGN.md
@SECURITY.md
@RELIABILITY.md
@PRODUCT_SENSE.md
@QUALITY_SCORE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STACK.md
@.planning/codebase/TESTING.md
@scripts/check-backend-neutral-contract.sh
@scripts/check-metal-runtime.sh
@scripts/check-metal-feature-passes.sh
@scripts/run-no-skip-swiftpm.sh

Phase 73 changes the current public contract, so historical Phase 70–72 statements that selection was absent must remain clearly phase-qualified while current owner sections describe the new behavior. Do not rewrite historical archive evidence or claim cross-backend parity, physical-device behavior, performance, commercial approval, packaging, shipping, launch, or release readiness; those remain Phase 74/future scopes.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add and wire the backend-configuration preflight</name>
  <files>scripts/check-backend-configuration.sh, scripts/run-no-skip-swiftpm.sh</files>
  <action>Create an executable SDK-owned check-backend-configuration.sh following the existing bounded-output/mutation-test gate style. Its static validation must require regular files, exactly the two public backend cases, the defaulted renderBackend field and missing-key decode, the factory's closed CPU/GPU switch, explicit BeautyBackendRequest policy propagation on all three engine routes, package-only injection boundaries, unchanged 61-parameter/five-preset/74-renderer inventories, no UI/Demo/device/network dependency, and no fallback/retry branch. Its self-test must mutate or remove the selector/default/policy/fallback markers and prove each mutation is rejected. Its live mode must run the focused configuration/routing suites with nonzero execution and zero failures/skips, call the existing runtime preflight so metal_available and metal_unavailable remain separate classifications, and print only bounded aggregate counts. Integrate this gate exactly once into run-no-skip-swiftpm.sh after the Metal feature gate and before consumer/CPU-oracle/opt-in/full-child stages, preserving archive verification and SDK-only boundary ordering. Update expected focused counts from actual test output rather than accepting zero or skip-only runs.</action>
  <verify>
    <automated>bash scripts/check-backend-configuration.sh --self-test &amp;&amp; bash scripts/check-backend-configuration.sh &amp;&amp; bash scripts/run-no-skip-swiftpm.sh --self-test</automated>
  </verify>
  <done>The new gate's static/mutation/live modes pass, it records explicit available/unavailable classifications without raw details, and the no-skip wrapper invokes it exactly once in archive-first order.</done>
</task>

<task type="auto">
  <name>Task 2: Synchronize current owners and Phase 73 ledgers</name>
  <files>ARCHITECTURE.md, DESIGN.md, SECURITY.md, RELIABILITY.md, PRODUCT_SENSE.md, QUALITY_SCORE.md, .planning/codebase/ARCHITECTURE.md, .planning/codebase/STACK.md, .planning/codebase/TESTING.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, PLANS.md</files>
  <action>Record the current contract in each owning document: BeautyCore owns the public BeautyRenderBackend/configuration field; BeautySDK owns immutable factory selection and request policy propagation; CPU remains the permanent reference; explicit GPU construction uses the package Metal runtime; missing/legacy configuration defaults to CPU; .metalUnavailable is terminal with no CPU fallback; and package-only injection is test-only. Update configuration inventory counts to 11 fields while preserving the 61 parameter/five preset/74 renderer inventories. Add the Phase 73 test/gate ordering and measured counts only from fresh command output, update current journey/acceptance/error/security/reliability language, and leave historical Phase 70–72 evidence phase-qualified. Update .planning/ROADMAP.md plan count/list and progress, .planning/REQUIREMENTS.md CONFIG-01/CONFIG-02 evidence/status, .planning/STATE.md phase/plan/next-position fields, and PLANS.md with the completed ledger and exact verification. Do not add UI/Demo, CLI backend behavior beyond the existing SDK-owned scope, device, performance, commercial, packaging, shipping, launch, or release claims.</action>
  <verify>
    <automated>git diff --check &amp;&amp; bash scripts/check-sdk-only-boundary.sh --post-archive &amp;&amp; swift test --package-path BeautySDK --filter 'BeautyCoreTests.BeautyConfigurationTests|BeautyCoreTests.BeautyEngineBackendRoutingTests'</automated>
  </verify>
  <done>All current owners and planning ledgers agree with the implemented public selector and fail-closed policy, historical evidence is not rewritten, the SDK-only boundary remains green, and CONFIG-01/CONFIG-02 have traceable measured evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Repository source/docs → SDK-owned gate | Mutable source and owner text are checked for policy/schema drift before SwiftPM execution. |
| Gate → SwiftPM child | Only aggregate pass/fail/skip counts and availability classifications cross into durable validation output. |
| Current contract → historical archive evidence | Phase-qualified documentation prevents current selector claims from rewriting immutable prior-phase scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-73-08 | Tampering | configuration preflight | mitigate | Static and mutation checks reject missing/defaulted selectors, CPU substitution in GPU branches, omitted request policy, public test hooks, and inventory drift before the child run. |
| T-73-09 | Denial of Service | mandatory no-skip wrapper | mitigate | Require nonzero focused execution, zero failures, zero skips, bounded transcripts, and exact once-only gate ordering. |
| T-73-10 | Information Disclosure | gate/docs output | mitigate | Persist only aggregate counts, error codes, and availability classifications; prohibit pixels, support, paths, framework objects, and fixture locators. |
| T-73-11 | Spoofing | current vs historical owner records | mitigate | Keep Phase 70–72 records immutable/phase-qualified and update only current sections plus Phase 73 ledgers. |
| T-73-SC | Tampering | package installs | accept | No package installation or external dependency is introduced by this plan. |
</threat_model>

<verification>
Run check-backend-configuration.sh --self-test, the live configuration gate, the post-archive SDK-only boundary, the focused configuration/routing suite, and the full bash scripts/run-no-skip-swiftpm.sh closeout. Recheck git diff --check and ensure no generated media or UI/Demo source enters the active tree.
</verification>

<success_criteria>
Phase 73 closes CONFIG-01 and CONFIG-02 with a durable archive-first gate, exact zero-failure/zero-skip evidence, synchronized owners, and no expansion into Phase 74 parity or excluded UI/device/release scope.
</success_criteria>

<output>
Create .planning/phases/73-public-backend-configuration-and-fail-closed-availability/73-03-SUMMARY.md when done.
</output>
