---
phase: 66-legacy-ui-demo-archive-and-sdk-only-boundary
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/archive-legacy-ui.py
  - scripts/check-sdk-only-boundary.sh
  - archives/legacy-ui/README.md
  - docs/SDK_EFFECT_TAXONOMY.md
autonomous: true
requirements: [BOUNDARY-02, ARCHIVE-01, ARCHIVE-02]
must_haves:
  truths:
    - "Archive creation is deterministic, explicit about scope, and independently verifiable before source deletion."
    - "Algorithm-relevant Demo/UI taxonomy survives in a current SDK-owned text authority."
  prohibitions:
    - statement: "Transient files, per-user Xcode state, or build/cache artifacts may enter a retained archive."
      status: unverified
      verification: flagged
    - statement: "Archive tooling may delete a source tree before both archives pass verification."
      status: unverified
      verification: flagged
---

<objective>
Build the fail-closed archive and boundary tooling, and preserve the legacy
algorithm taxonomy before any original UI/Demo file is removed.
</objective>

<context>
@AGENTS.md
@PLANS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/66-legacy-ui-demo-archive-and-sdk-only-boundary/66-CONTEXT.md
@BeautyDemo/BeautyDemo/Editor/MeituEditorToolModels.swift
@BeautyDemo/BeautyDemo/Panel/BeautyControlDescriptor.swift
@meituxiuxiu/FUNCTION_MAP.md
@meituxiuxiu/HOME_MAP.md
</context>

<tasks>

<task id="66-01-01" type="auto">
  <name>Task 1: Implement deterministic archive creation and verification</name>
  <read_first>.gitignore; current BeautyDemo and meituxiuxiu inventories; available standard-library archive/hash tooling</read_first>
  <files>scripts/archive-legacy-ui.py, archives/legacy-ui/README.md</files>
  <action>Create a standard-library archive tool with explicit `create`, `verify`, `reproduce`, and guarded `retire` operations for exactly `BeautyDemo/` and `meituxiuxiu/`. Include intentional ignored legacy reference images, exclude only the documented `.DS_Store`, `.build`, cache, `xcuserdata`, and `*.xcuserstate` classes, reject symlinks and unexpected source roots, normalize entry order, timestamps and modes, and write separate ZIP, sorted per-file path/size/SHA-256 manifest, and ZIP SHA-256 record for each source. Creation and preflight must independently enumerate the live filesystem (not `git ls-files`), require exact source-to-manifest-to-ZIP equality, and assert the current legacy UI source includes all 19 ignored PNG references. Verification must check recorded archive digests, safe ZIP paths, exact entry/manifest agreement, extracted bytes and hashes in a temporary directory, and fail non-zero on any mismatch. `retire` must resolve the repository root and require the two exact non-symlink directory targets, rerun the combined fresh source-to-archive/reproduction gate, bind approval to both archive digests, delete only those targets, compare tracked deletions to an exact precomputed allowlist, and prove SDK/docs/planning/private-fixture sentinels survive. Creation and verification never delete source. Document exact archive scope, exclusions, verification, guarded retirement, and restoration commands.</action>
  <verify><automated>python3 scripts/archive-legacy-ui.py self-test &amp;&amp; python3 scripts/archive-legacy-ui.py create --output archives/legacy-ui --dry-run</automated></verify>
  <done>Archive tooling proves complete deterministic source-to-archive creation, safe extraction/hash verification, and an exact-target digest-bound retirement transaction without modifying either source tree during create/verify.</done>
</task>

<task id="66-01-02" type="auto">
  <name>Task 2: Preserve the SDK effect taxonomy and add the boundary scanner</name>
  <read_first>MeituEditorToolModels.swift; BeautyControlDescriptor.swift; FUNCTION_MAP.md; HOME_MAP.md; SHAPE_FEATURE_LEDGER.md; FEATURE_MATRIX.md; existing root SDK contracts</read_first>
  <files>docs/SDK_EFFECT_TAXONOMY.md, scripts/check-sdk-only-boundary.sh</files>
  <action>Write one SDK-owned current taxonomy that preserves algorithm/control grouping, implemented/partial/future meaning, and canonical SDK parameter mappings while explicitly excluding visual layout and application behavior. Add a fail-closed static scanner with pre-archive and post-archive modes: pre-archive validates exact archive inputs/exclusions and taxonomy coverage; post-archive rejects active `BeautyDemo/` or `meituxiuxiu/`, Xcode project/scheme/simulator commands in active current owners, SwiftUI/UI-test build dependencies, restored generated media, and Metal/GPU scope drift. Allow immutable archived milestone history and the retained archive documentation to mention historical paths.</action>
  <verify><automated>bash scripts/check-sdk-only-boundary.sh --self-test &amp;&amp; bash scripts/check-sdk-only-boundary.sh --pre-archive &amp;&amp; git diff --check</automated></verify>
  <done>The taxonomy no longer depends on deletable UI source and the scanner can distinguish valid history from an active SDK-boundary violation.</done>
</task>

</tasks>

<verification>Archive self-tests, dry-run inventory, pre-archive boundary checks, and documentation checks pass while both source trees remain untouched.</verification>
<success_criteria>Creation can proceed without losing taxonomy or accepting an incomplete/unsafe archive.</success_criteria>
