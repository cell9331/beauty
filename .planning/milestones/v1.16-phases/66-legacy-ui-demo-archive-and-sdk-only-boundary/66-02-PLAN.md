---
phase: 66-legacy-ui-demo-archive-and-sdk-only-boundary
plan: "02"
type: execute
wave: 2
depends_on: ["66-01"]
files_modified:
  - archives/legacy-ui/BeautyDemo-v1.16.zip
  - archives/legacy-ui/BeautyDemo-v1.16.manifest.tsv
  - archives/legacy-ui/BeautyDemo-v1.16.zip.sha256
  - archives/legacy-ui/meituxiuxiu-v1.16.zip
  - archives/legacy-ui/meituxiuxiu-v1.16.manifest.tsv
  - archives/legacy-ui/meituxiuxiu-v1.16.zip.sha256
  - BeautyDemo/
  - meituxiuxiu/
autonomous: true
requirements: [ARCHIVE-01, ARCHIVE-02, ARCHIVE-03]
must_haves:
  truths:
    - "Both complete intentional source inventories are retained and independently recoverable from verified ZIP artifacts."
    - "Original Demo/UI trees are removed only after the verification tool passes both archives in the same execution transaction."
  prohibitions:
    - statement: "A Git-tracked-only inventory may omit ignored legacy UI reference images."
      status: unverified
      verification: flagged
    - statement: "An archive or source deletion may be accepted after a digest, path-safety, extraction, listing, or content-hash failure."
      status: unverified
      verification: flagged
---

<objective>
Create and verify both historical archives, then remove the original UI/Demo
trees while retaining exact recovery evidence.
</objective>

<context>
@.planning/phases/66-legacy-ui-demo-archive-and-sdk-only-boundary/66-01-PLAN.md
@scripts/archive-legacy-ui.py
@archives/legacy-ui/README.md
@.gitignore
</context>

<tasks>

<task id="66-02-01" type="auto">
  <name>Task 1: Materialize and independently verify both archives</name>
  <read_first>archive tool; pre-archive scanner; full filesystem inventories including ignored files</read_first>
  <files>archives/legacy-ui/BeautyDemo-v1.16.zip, archives/legacy-ui/BeautyDemo-v1.16.manifest.tsv, archives/legacy-ui/BeautyDemo-v1.16.zip.sha256, archives/legacy-ui/meituxiuxiu-v1.16.zip, archives/legacy-ui/meituxiuxiu-v1.16.manifest.tsv, archives/legacy-ui/meituxiuxiu-v1.16.zip.sha256</files>
  <action>Run the archive creator against the exact two roots. Independently compare the live filesystem inventory to each manifest and archive. Confirm the BeautyDemo inventory contains all intentional tracked source/tests/project data except excluded per-user/transient files, and confirm the meituxiuxiu inventory includes exactly the current 19 ignored PNG reference assets plus its text/html maps. Run verification from the retained artifacts into fresh temporary extraction directories, compare exact normalized listings and all per-file hashes, validate ZIP SHA-256 records, and rerun creation into a temporary output to prove byte-identical deterministic artifacts before allowing deletion.</action>
  <verify><automated>python3 scripts/archive-legacy-ui.py verify --output archives/legacy-ui &amp;&amp; python3 scripts/archive-legacy-ui.py reproduce --output archives/legacy-ui</automated></verify>
  <done>Two deterministic archives pass digest, safe-path, listing, extraction, and content-hash verification and include the ignored reference media intentionally.</done>
</task>

<task id="66-02-02" type="auto">
  <name>Task 2: Retire the exact original UI/Demo trees through the guarded transaction</name>
  <read_first>successful archive verification output; archive manifests; exact filesystem targets</read_first>
  <files>BeautyDemo/, meituxiuxiu/</files>
  <action>Invoke only the archive tool's guarded `retire` operation. It must resolve the repository root, require targets equal exactly `<repo>/BeautyDemo` and `<repo>/meituxiuxiu`, reject symlinks/non-directories/root escape, rerun the combined live-source freshness plus archive verification and reproduction gate immediately before mutation, bind the mutation to both verified archive digests, and delete only those two directories. It must then re-run artifact-only extraction/hash verification, require both original paths absent, compare Git tracked name-status against the exact pre-retirement allowlist for those trees, reject any unrelated tracked deletion, and prove explicit `BeautySDK`, docs, planning, script, private-fixture and unrelated ignored-tree sentinels survive.</action>
  <verify><automated>python3 scripts/archive-legacy-ui.py retire --output archives/legacy-ui --verify-only-postcondition &amp;&amp; test ! -e BeautyDemo &amp;&amp; test ! -e meituxiuxiu &amp;&amp; python3 scripts/archive-legacy-ui.py verify --output archives/legacy-ui &amp;&amp; git diff --name-status --diff-filter=D -- BeautyDemo meituxiuxiu</automated></verify>
  <done>The guarded transaction proves exact targets and deletion allowlist, preserves unrelated sentinels, removes the original application/UI trees, and leaves both historical inventories fully recoverable.</done>
</task>

</tasks>

<verification>Both retained archives still verify after original removal, exact target absence is proven, and Git status contains no unrelated deletion.</verification>
<success_criteria>No active UI/Demo source remains and no intentional historical content is lost.</success_criteria>
