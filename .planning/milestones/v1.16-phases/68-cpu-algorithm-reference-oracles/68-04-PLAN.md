---
phase: 68-cpu-algorithm-reference-oracles
plan: 04
type: execute
wave: 3
depends_on: [68-02, 68-03]
files_modified:
  - scripts/check-cpu-reference-oracles.sh
  - scripts/run-no-skip-swiftpm.sh
  - ARCHITECTURE.md
  - SECURITY.md
  - RELIABILITY.md
  - QUALITY_SCORE.md
  - .planning/codebase/TESTING.md
  - PLANS.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [CPU-05]
must_haves:
  truths:
    - "A clean clone can run the generated CPU oracle suites without private media and without a skip, before the complete no-skip SwiftPM child."
    - "Rights-approved portrait/native-Vision tests remain private, ignored, explicitly environment-gated, and cannot become generated-oracle evidence."
    - "Current owners, maps, project state, roadmap, and ledger report the measured CPU oracle gate and preserve all nonclaims."
  artifacts:
    - path: "scripts/check-cpu-reference-oracles.sh"
      provides: "Fail-closed generated-fixture/privacy/optional-boundary preflight"
      contains: "cpu_reference_oracles_passed"
      min_lines: 80
    - path: "scripts/run-no-skip-swiftpm.sh"
      provides: "Archive/boundary/consumer/generated-oracle/optional/no-skip ordering"
      contains: "check-cpu-reference-oracles.sh"
    - path: ".planning/codebase/TESTING.md"
      provides: "Current generated CPU oracle and optional-fixture testing map"
      contains: "CPU reference"
    - path: "PLANS.md"
      provides: "Completed Phase 68 ledger with aggregate evidence and nonclaims"
      contains: "phase-68"
  key_links:
    - from: "scripts/run-no-skip-swiftpm.sh"
      to: "scripts/check-cpu-reference-oracles.sh"
      via: "preflight invocation before one-child SwiftPM run"
      pattern: "check-cpu-reference-oracles.sh"
    - from: "scripts/check-cpu-reference-oracles.sh"
      to: "BeautySDK/Tests/BeautyEffectsTests/CPUReferenceGeometryOracleTests.swift"
      via: "focused generated SwiftPM test selection"
      pattern: "CPUReferenceGeometryOracleTests"
    - from: "SECURITY.md"
      to: "scripts/check-cpu-reference-oracles.sh"
      via: "generated-only and private-fixture boundary contract"
      pattern: "generated"
---

<objective>
Close Phase 68 with a fail-closed generated-only preflight, explicit optional
fixture separation, and synchronized SDK-only owner/ledger documentation.

Purpose: CPU reference evidence must remain reproducible and privacy-safe after
the focused suites pass; the repository must not silently drift toward tracked
media, borrowed optional success, or a GPU/device claim.

Output: CPU oracle preflight wired into the mandatory gate, current owner/map
updates, and aggregate-only project/phase ledger records (D-02, D-07, D-08).
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/68-cpu-algorithm-reference-oracles/68-CONTEXT.md
@.planning/phases/68-cpu-algorithm-reference-oracles/68-VALIDATION.md
@.planning/phases/68-cpu-algorithm-reference-oracles/68-02-PLAN.md
@.planning/phases/68-cpu-algorithm-reference-oracles/68-03-PLAN.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@ARCHITECTURE.md
@SECURITY.md
@RELIABILITY.md
@QUALITY_SCORE.md
@PLANS.md
@scripts/run-no-skip-swiftpm.sh
@BeautySDK/Tests/BeautyCoreTests/BeautyTeethWhiteningRealFixtureTests.swift
@BeautySDK/Tests/BeautyCoreTests/BeautyScleraRednessRealFixtureTests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add generated-only CPU oracle preflight to the mandatory gate</name>
  <files>scripts/check-cpu-reference-oracles.sh, scripts/run-no-skip-swiftpm.sh</files>
  <action>Per D-02, D-07, and D-08, create an SDK-owned `check-cpu-reference-oracles.sh` that uses bounded `rg`/file checks to require every Phase 68 generated-oracle file to be a regular source file under the SwiftPM test tree, reject media reads, tracked output paths, absolute/private locators, raw diagnostic printing, Metal/GPU/backend tokens, and test-only fixture writes, then run the generated fixture/geometry/color/local-retouch/determinism filters with captured aggregate-only output and a nonzero execution requirement. Assert the existing teeth/sclera native-Vision suites retain explicit environment guards and that they are not included in the generated filter list. Invoke this preflight after the external consumer and before private bundle checks/one-child SwiftPM execution in `run-no-skip-swiftpm.sh`; preserve archive-first ordering, bounded output, cleanup, and the existing all-eight optional opt-ins. Make failures short, typed, and path-free.</action>
  <verify>
    <automated>bash scripts/check-cpu-reference-oracles.sh</automated>
    <automated>bash scripts/run-no-skip-swiftpm.sh</automated>
    <automated>bash scripts/check-cpu-reference-oracles.sh --self-test</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>The generated CPU oracle preflight passes independently and is mandatory before the private opt-ins and sole no-skip child; its static boundary rejects media, raw diagnostics, and GPU scope drift.</done>
</task>

<task type="auto">
  <name>Task 2: Synchronize CPU oracle owner and testing contracts</name>
  <files>ARCHITECTURE.md, SECURITY.md, RELIABILITY.md, QUALITY_SCORE.md, .planning/codebase/TESTING.md</files>
  <action>Per D-02, D-07, and D-08, update current owner/map text to describe the generated in-memory CPU oracle inventory, the exact geometry/color/local-retouch metric families, the generated-only mandatory preflight ordering, and the existing private/native-Vision opt-in boundary. Record measured focused/full SwiftPM counts only after running the gate; keep raw pixels, masks, landmarks, support points, local paths, child transcripts, generated outputs, and private fixture metadata explicitly non-durable. State that the CPU/Core Image implementation remains the only current reference and that no public backend, Metal/GPU, UI/Demo, device, performance, commercial, packaging, shipping, launch, or release-readiness claim follows from these tests. Do not alter the public field inventory or taxonomy status.</action>
  <verify>
    <automated>rg -n 'CPU reference|generated|native.Vision|private|no.skp|Metal|GPU|device|release' ARCHITECTURE.md SECURITY.md RELIABILITY.md QUALITY_SCORE.md .planning/codebase/TESTING.md</automated>
    <automated>if rg -n 'tracked portrait|persisted pixels|raw landmarks|raw masks|device performance|GPU parity' ARCHITECTURE.md SECURITY.md RELIABILITY.md QUALITY_SCORE.md .planning/codebase/TESTING.md | grep -vE 'no |not |without|remain|outside|absent|forbidden|reject'; then exit 1; else exit 0; fi</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>Current architecture/security/reliability/quality/testing owners agree on the generated-only CPU reference boundary, measured gates, privacy rules, and nonclaims.</done>
</task>

<task type="auto">
  <name>Task 3: Record Phase 68 aggregate ledger and state transition evidence</name>
  <files>PLANS.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>Per D-07 and D-08, add the completed Phase 68 ledger row with the exact focused/generated preflight and final no-skip aggregate counts obtained from execution, list the four plans and CPU-01..CPU-05 evidence, and record any remaining optional/native-Vision skips only as environment-gated non-mandatory evidence. Update the project/roadmap/state text and plan counts to mark Phase 68 ready for independent verification and Phase 69, while preserving the queued Metal milestone and all UI/Demo/device/release nonclaims. Do not store child transcripts, raw fixture bytes, coordinates, masks, support, absolute paths, or environment values; leave canonical pass/fail status to the independent verifier/lifecycle command.</action>
  <verify>
    <automated>rg -n -A18 'Phase 68|CPU Algorithm Reference Oracles|CPU-01|CPU-05' PLANS.md .planning/PROJECT.md .planning/ROADMAP.md .planning/STATE.md</automated>
    <automated>if rg -n '(/private/|/Users/|raw pixel|raw mask|landmark array|child transcript|fixture locator|PHASE[0-9]+_.*=)' PLANS.md .planning/PROJECT.md .planning/ROADMAP.md .planning/STATE.md; then exit 1; else exit 0; fi</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>Planning/project/roadmap/state owners contain only aggregate Phase 68 evidence, show all five CPU requirements covered, and route cleanly to independent verification and Phase 69.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Generated oracle source → repository gate | Static/preflight checks decide whether mandatory evidence is clean-clone safe. |
| Optional private bundle → no-skip child | Rights-approved/native-Vision media enters only through existing explicit environment gates. |
| Test/gate result → durable docs | Only aggregate counts and bounded contract statements may persist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-68-04-01 | Tampering | Generated-oracle preflight | mitigate | Fail closed on media/path/raw-output/GPU drift and require bounded focused execution before the one-child gate. |
| T-68-04-02 | Information Disclosure | Phase docs/ledger | mitigate | Persist only counts, categories, requirement IDs, and nonclaims; reject paths, transcripts, pixels, masks, and support. |
| T-68-04-03 | Elevation of Privilege | Optional private fixtures | mitigate | Require existing environment opt-ins, manifest/path validation, ignored bundles, and the documented no-skip identities. |
| T-68-04-SC | Tampering | SwiftPM/test dependencies | accept | No package installation or external service/model is introduced; boundary scanner remains unchanged except for the oracle preflight call. |
</threat_model>

<verification>
Run the generated preflight self-test and normal mode, the full `scripts/run-no-skip-swiftpm.sh`, archive/boundary/consumer checks, and `git diff --check`. Then the independent phase verifier must inspect CPU-01..CPU-05 against the resulting source/tests and write `68-VERIFICATION.md` before lifecycle completion.
</verification>

<success_criteria>
- Generated mandatory tests pass with zero skips in a clean clone and are wired before optional media/no-skip execution.
- Optional rights/native-Vision fixtures remain private, ignored, explicit, and non-authoritative for generated success.
- Current owner/docs/maps/ledger/state contain measured aggregate evidence and preserve all CPU-only/non-release boundaries.
</success_criteria>

<output>
Create `.planning/phases/68-cpu-algorithm-reference-oracles/68-04-SUMMARY.md` when done.
</output>
