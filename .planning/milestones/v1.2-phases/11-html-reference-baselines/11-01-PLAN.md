---
phase: "11-html-reference-baselines"
plan: "11-01"
status: "pending"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "meituxiuxiu/html/README.md"
  - "meituxiuxiu/html/styles.css"
  - ".planning/evidence/v1.2/"
autonomous: true
requirements: ["HTML-04"]
requirements_addressed: ["HTML-04"]
must_haves:
  truths:
    - "D-01 and D-02: HTML references are reconstructed DOM/CSS pages, not full-screen screenshot backgrounds."
    - "D-03: shared CSS tokens make colors, spacing, radii, typography, rails, badges, and phone viewport framing measurable for Phase 12."
    - "D-10: HTML references are deterministic and offline with no remote fonts, remote images, analytics, upload forms, CDN scripts, or hidden services."
    - "D-12: screenshot viewport and capture targets are documented for Phase 12."
  artifacts:
    - "Created `meituxiuxiu/html/README.md` with source list, local-only policy, viewport, capture commands, and accepted limits."
    - "Created `meituxiuxiu/html/styles.css` with shared tokens and state selectors."
    - "Created `.planning/evidence/v1.2/` as evidence destination."
---

# Plan 11-01: Create HTML Reference Package and Local-Only Policy

<objective>
Create the static HTML reference package structure, shared CSS token file, evidence directory, and local-only policy before any surface page is implemented.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-html-reference-baselines/11-CONTEXT.md
@.planning/phases/11-html-reference-baselines/11-RESEARCH.md
@.planning/phases/11-html-reference-baselines/11-VALIDATION.md
@.planning/phases/11-html-reference-baselines/11-PATTERNS.md
@meituxiuxiu/HOME_MAP.md
@meituxiuxiu/FUNCTION_MAP.md
</context>

## Artifacts this phase produces

- `meituxiuxiu/html/README.md`
- `meituxiuxiu/html/styles.css`
- `.planning/evidence/v1.2/`
- CSS custom properties prefixed with `--mt-` for phone frame, Home dark surface, editor panel, accent pink, orange CTA, brown action cards, spacing, radii, badge, rail, and tab dimensions.

## Source Coverage Audit

| Source | ID | Feature / Requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| REQ | HTML-04 | Local deterministic offline references | 11-01 | COVERED | Defines policy and scan target. |
| CONTEXT | D-01, D-02 | Reconstructed DOM/CSS, no screenshot wrapper | 11-01 | COVERED | README bans screenshot backgrounds. |
| CONTEXT | D-03 | Shared tokens | 11-01 | COVERED | `styles.css` owns tokens. |
| CONTEXT | D-10, D-12 | Offline and viewport policy | 11-01 | COVERED | README capture contract. |

<threat_model>
T-11-01 Remote or hidden runtime dependency: Blocked by README policy plus scans for `http://`, `https://`, `@import`, remote `url(...)`, `fetch(`, `XMLHttpRequest`, `sendBeacon`, analytics, upload, forms, and file inputs.
T-11-02 Reference laundering: Blocked by banning full-screen source screenshots as rendered backgrounds in `README.md`.
</threat_model>

<tasks>

<task id="11-01-01" type="auto">
  <name>Task 1: Create package structure and README contract</name>
  <files>
    meituxiuxiu/html/README.md
    .planning/evidence/v1.2/
  </files>
  <read_first>
    AGENTS.md
    PLANS.md
    .planning/phases/11-html-reference-baselines/11-CONTEXT.md
    .planning/phases/11-html-reference-baselines/11-RESEARCH.md
    meituxiuxiu/HOME_MAP.md
    meituxiuxiu/FUNCTION_MAP.md
  </read_first>
  <action>Create `meituxiuxiu/html/README.md` and `.planning/evidence/v1.2/`. The README must list planned files `home.html`, `editor.html`, and `styles.css`; list source references `meituxiuxiu/home/IMG_0871.PNG` through `IMG_0874.PNG` and `meituxiuxiu/IMG_0856.PNG` through `IMG_0870.PNG`; state that rendered pages must use inspectable DOM/CSS rather than full-screen source screenshot backgrounds; document an offline policy banning remote fonts, remote images, analytics, upload forms, CDN scripts, and hidden services; document screenshot outputs `home-html-first-screen.png`, `home-html-sticky-state.png`, and `editor-html-tool-panel.png`; document the intended viewport `390x844` CSS pixels or a recorded local equivalent.</action>
  <acceptance_criteria>
    - `meituxiuxiu/html/README.md` contains `home.html`, `editor.html`, `styles.css`, `.planning/evidence/v1.2/`, and `390x844`.
    - README contains every source range string: `IMG_0871.PNG`, `IMG_0874.PNG`, `IMG_0856.PNG`, and `IMG_0870.PNG`.
    - README contains the phrase `inspectable DOM/CSS` and explicitly says not to use full-screen screenshot backgrounds.
    - `.planning/evidence/v1.2/` exists as a directory.
  </acceptance_criteria>
  <verify>
    <automated>test -f meituxiuxiu/html/README.md && test -d .planning/evidence/v1.2</automated>
    <automated>rg -n "home.html|editor.html|styles.css|390x844|inspectable DOM/CSS|IMG_0871.PNG|IMG_0874.PNG|IMG_0856.PNG|IMG_0870.PNG" meituxiuxiu/html/README.md</automated>
  </verify>
  <done>Reference package and evidence destination are established.</done>
</task>

<task id="11-01-02" type="auto">
  <name>Task 2: Create shared CSS tokens</name>
  <files>
    meituxiuxiu/html/styles.css
  </files>
  <read_first>
    .planning/phases/11-html-reference-baselines/11-CONTEXT.md
    .planning/phases/11-html-reference-baselines/11-RESEARCH.md
    meituxiuxiu/HOME_MAP.md
    meituxiuxiu/FUNCTION_MAP.md
  </read_first>
  <action>Create `styles.css` with `:root` CSS variables prefixed `--mt-` for `--mt-phone-width: 390px`, `--mt-phone-height: 844px`, dark Home background, white editor panel, pink accent, orange CTA, brown action cards, common text colors, spacing steps `4px`, `8px`, `12px`, `16px`, `24px`, radii `8px`, `16px`, `24px`, badge styling, horizontal rails, floating bottom tab, and capture state IDs/classes for Home first screen, Home sticky state, and editor tool panel. Add shared selectors for `.phone-frame`, `.brand-capsule`, `.badge`, `.tool-rail`, `.category-rail`, `.bottom-tabs`, and `.capture-section` without referencing remote URLs.</action>
  <acceptance_criteria>
    - `styles.css` contains `--mt-phone-width: 390px` and `--mt-phone-height: 844px`.
    - `styles.css` contains selectors `.phone-frame`, `.brand-capsule`, `.badge`, `.tool-rail`, `.category-rail`, `.bottom-tabs`, and `.capture-section`.
    - `styles.css` contains no `http://`, `https://`, `@import`, or remote `url(` references.
  </acceptance_criteria>
  <verify>
    <automated>test -f meituxiuxiu/html/styles.css</automated>
    <automated>rg -n -- "--mt-phone-width: 390px|--mt-phone-height: 844px|\\.phone-frame|\\.brand-capsule|\\.badge|\\.tool-rail|\\.category-rail|\\.bottom-tabs|\\.capture-section" meituxiuxiu/html/styles.css</automated>
    <automated>! rg -n "https?://|@import|url\\((['\\\"]?)https?:" meituxiuxiu/html/styles.css</automated>
  </verify>
  <done>Shared local CSS token system exists for both HTML pages.</done>
</task>

</tasks>

## Verification

- `test -f meituxiuxiu/html/README.md && test -f meituxiuxiu/html/styles.css && test -d .planning/evidence/v1.2`
- `! rg -n "https?://|@import|url\\((['\\\"]?)https?:|analytics|upload|fetch\\(|XMLHttpRequest|navigator\\.sendBeacon|<form|type=['\\\"]file" meituxiuxiu/html`

## Success Criteria

- The HTML reference package can receive Home and Editor pages without new setup work.
- Local-only policy and viewport/evidence conventions are documented before page implementation.
- Shared CSS tokens are concrete and scan-clean.
