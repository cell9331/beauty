# Phase 64 Pattern Map

| Phase 64 role | Reuse |
| --- | --- |
| Public renderer and strict decoded output | Phase 61 renderer case, `--no-watermark`, helper and private runner |
| Evidence discovery | Phase 62 fixed-output ignored-bundle authority |
| Genuine metrics | Phase 63 `BeautyScleraRednessRealFixtureTests` frozen bounds |
| Adversarial safety | Phase 63 provider fixtures plus Phase 61 closeout-oracle structure |
| Promotion | Phase 61 pre/post checker and four owner-document transaction |

Data flow:

`authorized ignored originals -> opaque staging -> public renderer -> six ignored PNGs -> strict aggregate checks -> blinded original-detail review -> pre-promotion conjunction -> exact row update -> post-promotion verification`

Landmines: never point the recursive renderer at the evidence bundle; never use
reviewed masks as runtime input; never infer geometric safety from dark iris
color; never change aggregate `眼睛` to implemented while `去脂` is future; never
commit generated media or private locators.
