---
phase: 64-sclera-output-adversarial-safety-and-independent-closeout
plan: 07
type: execute
wave: 7
depends_on: [64-06]
gap_closure: true
security_standard: OWASP ASVS Level 1
block_on: HIGH
files_modified:
  - BeautySDK/Sources/BeautyEffects/LocalRetouch/BeautyScleraRednessProvider.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautyScleraRednessProviderTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautyScleraRednessAdversarialCloseoutTests.swift
autonomous: true
requirements: [SCLERA-14, SCLERA-15]
must_haves:
  truths:
    - "The exact historical asymmetric right-eye counterexample at center +0.004, pupil -0.006, and skew +0.003 is restored unchanged and can never leak into protected truth."
    - "A deterministic surrounding boundary sweep contains both accepted and rejected samples, obeys a monotone calibrated envelope, and keeps the left peer active whenever the right eye fails closed."
    - "Contour validation rejects every inclusive non-adjacent intersection, endpoint touch, collinear overlap, retraced edge, and zero-length edge before rasterization."
    - "Adjacent contour edges may share only their intended endpoint; adjacent overlap/retrace and first-last closure overlap are rejected."
    - "Malformed-eye rejection is local and valid-invalid-valid plus parallel requests recover without stale state."
  artifacts:
    - path: "BeautySDK/Sources/BeautyEffects/LocalRetouch/BeautyScleraRednessProvider.swift"
      provides: "Inclusive contour validity and production containment/fail-closed envelope"
      contains: "SelfIntersections"
    - path: "BeautySDK/Tests/BeautyEffectsTests/BeautyScleraRednessProviderTests.swift"
      provides: "Focused collinear, on-segment, retrace, zero-length, endpoint-touch, peer, and recovery tests"
      contains: "BeautyScleraRednessProviderTests"
    - path: "BeautySDK/Tests/BeautyEffectsTests/BeautyScleraRednessAdversarialCloseoutTests.swift"
      provides: "Exact leaking tuple and surrounding accepted/rejected boundary sweep"
      contains: "0.004"
  key_links:
    - from: "BeautySDK/Tests/BeautyEffectsTests/BeautyScleraRednessAdversarialCloseoutTests.swift"
      to: "BeautySDK/Sources/BeautyEffects/LocalRetouch/BeautyScleraRednessProvider.swift"
      via: "Exact historical tuple and monotone sweep exercise the production proposal/containment path"
      pattern: "0\\.004|0\\.006|0\\.003"
    - from: "BeautySDK/Tests/BeautyEffectsTests/BeautyScleraRednessProviderTests.swift"
      to: "BeautySDK/Sources/BeautyEffects/LocalRetouch/BeautyScleraRednessProvider.swift"
      via: "Inclusive segment predicates fail malformed contours locally before mask creation"
      pattern: "segmentsIntersect|onSegment"
  prohibitions:
    - "Do not shrink, round, mirror away, direction-scale away, delete, skip, or weaken the exact center +0.004 / pupil -0.006 / skew +0.003 right-eye counterexample."
    - "Do not make every surrounding sample reject; the calibrated sweep must contain meaningful accepted and rejected cases."
    - "Do not use strict-crossing-only orientation products for contour validity."
    - "Do not expose raw support, coordinates, proposal indices, masks, protected pixels, or vein-like detail outside request-local tests."
    - "Do not change DeviceRGB/named-sRGB behavior; SAFE-06 belongs exclusively to Phase 65."
  backstops:
    - { statement: "SCLERA-14 adjacency: peer-eye continuation and adjacent protected-family coverage are asserted around every local rejection.", verification: backstop }
    - { statement: "SCLERA-14 empty: every protected family and every accepted-eye proposal must be nonempty; empty truth cannot pass vacuously.", verification: backstop }
    - { statement: "SCLERA-14 ordering: the exact counterexample and sweep identifiers/outcomes are unique and deterministically ordered.", verification: backstop }
    - { statement: "SCLERA-15 boundary: exact protected and outside-proposal RGBA identity is checked at the accepted/rejected envelope boundary.", verification: backstop }
    - { statement: "SCLERA-15 adjacency: intended adjacent contour endpoints are distinguished from non-adjacent touches and adjacent overlap/retrace.", verification: backstop }
    - { statement: "SCLERA-15 empty: zero-length edges, empty proposals, and empty protected regions fail explicitly rather than passing by empty comparison.", verification: backstop }
    - { statement: "SCLERA-15 ordering: contour edge order, first-last closure, and scenario order are checked explicitly.", verification: backstop }
    - { statement: "SCLERA-15 precision: orientation/on-segment tolerance is scaled and bounded so near-collinear cases are deterministic without accepting overlap.", verification: backstop }
---

<objective>
Close the two production-proof regressions before any fresh evidence or promotion work: the exact asymmetric right-eye leak and strict-crossing-only contour validation.

Purpose: Restore the strongest known counterexample, prove a real accepted/rejected containment boundary without parameter shrinkage, and reject every inclusive malformed contour while preserving per-eye recovery per D-09 through D-12 and D-16.
Output: Hardened provider containment/contour validation plus focused and adversarial regression tests.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@DESIGN.md
@SECURITY.md
@RELIABILITY.md
@QUALITY_SCORE.md
@.planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-CONTEXT.md
@.planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-RESEARCH.md
@.planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-PATTERNS.md
@.planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-AI-SPEC.md
@.planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-CODE-REVIEW.md
@.planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-PRE-PROMOTION-VERIFICATION.md
@.planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-06-SUMMARY.md
@.codex/skills/spike-findings-beauty/references/sclera-redness.md
@.codex/skills/spike-findings-beauty/references/still-image-integration.md
</context>

<tasks>

<task id="64-07-01" type="auto" tdd="true">
  <name>Task 1: Restore the exact leak and freeze a calibrated containment envelope</name>
  <read_first>
    - BeautySDK/Sources/BeautyEffects/LocalRetouch/BeautyScleraRednessProvider.swift
    - BeautySDK/Tests/BeautyEffectsTests/BeautyScleraRednessAdversarialCloseoutTests.swift
    - .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-CODE-REVIEW.md
    - .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-06-SUMMARY.md
    - .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-CONTEXT.md
    - .codex/skills/spike-findings-beauty/references/sclera-redness.md
  </read_first>
  <files>BeautySDK/Sources/BeautyEffects/LocalRetouch/BeautyScleraRednessProvider.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyScleraRednessAdversarialCloseoutTests.swift</files>
  <behavior>
    - RED begins with the literal historical right-eye tuple: contour center offset +0.004, pupil offset -0.006, contour skew +0.003; the left peer remains the unchanged valid baseline.
    - The Cartesian neighborhood uses center offsets 0.003/0.004/0.005, pupil offsets -0.005/-0.006/-0.007, and skews 0.002/0.003/0.004, with stable unique identifiers and exact tuple retained.
    - Every accepted right-eye sample has zero actual-proposal intersection with independent protected truth and exact protected/outside RGBA bytes through the complete production path.
    - Every rejected right-eye sample is an explicit local fail-closed result while the left peer remains accepted and nonempty.
    - Accepted and rejected sets are both nonempty and monotone by perturbation magnitude; the exact tuple is either safely accepted or explicitly rejected under the documented envelope.
  </behavior>
  <action>
Write the failing regression before touching production. Reinstate the exact pre-tuning right-eye tuple center `+0.004`, pupil `-0.006`, skew `+0.003` as a named mandatory case; do not derive it by multiplying the smaller current values by a direction. Add the full 3×3×3 surrounding Cartesian sweep listed in the behavior plus explicit inside/outside boundary assertions. Freeze a named calibrated-envelope table in the test: every sample has an expected accepted or local-rejected classification, both classes are nonempty, and increasing all three perturbation magnitudes cannot move a rejected sample back to accepted. Keep D-09's full independent protected truth, D-10's score-attractive recoloring of every protected pixel, D-11's zero synthetic authority, and D-12's peer/recovery assertions.

Then make the narrow production containment change needed for the RED proof. Prefer strengthening pupil/iris exclusion or rejecting the affected eye at a documented production geometry limit; either disposition is valid only if the literal tuple remains unchanged in the test. An accepted case must have empty actual-proposal/protected-truth intersection and byte-identical protected/outside output after guard, score, feather, hard re-clip, bounded transform, Q16 composition, and final render. A rejected case must issue no right-eye unit while the left eye continues. Do not tune the fixture, truth, score-attractive color, thresholds, or perturbation values to the implementation. Preserve D-16: this production change invalidates the old original-detail review and no old review may authorize later work.
  </action>
  <verify>
    <automated>set -e; swift test --package-path BeautySDK --filter BeautyScleraRednessAdversarialCloseoutTests; rg -n '0\.004|0\.006|0\.003' BeautySDK/Tests/BeautyEffectsTests/BeautyScleraRednessAdversarialCloseoutTests.swift; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - The exact historical tuple is literal, named, executed, and unchanged.
    - All 27 surrounding combinations have deterministic ordered outcomes, including nonempty accepted and rejected sets with a monotone boundary.
    - Accepted outcomes prove direct proposal/truth separation and whole-region byte identity; rejected outcomes prove right-only fail-closed plus left-peer continuation.
    - No fixture/test parameter was reduced and no review/promotion artifact was edited.
  </acceptance_criteria>
  <done>The historical leak is permanently exercised and is safe either by production containment or exact local fail-closed behavior inside a frozen calibrated envelope.</done>
</task>

<task id="64-07-02" type="auto" tdd="true">
  <name>Task 2: Replace strict crossings with inclusive contour validity</name>
  <read_first>
    - BeautySDK/Sources/BeautyEffects/LocalRetouch/BeautyScleraRednessProvider.swift
    - BeautySDK/Tests/BeautyEffectsTests/BeautyScleraRednessProviderTests.swift
    - BeautySDK/Tests/BeautyEffectsTests/BeautyScleraRednessAdversarialCloseoutTests.swift
    - .planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-CODE-REVIEW.md
    - .codex/skills/spike-findings-beauty/references/sclera-redness.md
  </read_first>
  <files>BeautySDK/Sources/BeautyEffects/LocalRetouch/BeautyScleraRednessProvider.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyScleraRednessProviderTests.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyScleraRednessAdversarialCloseoutTests.swift</files>
  <behavior>
    - Collinear overlap and any point lying on a non-adjacent segment are intersections and reject the affected contour.
    - Non-adjacent endpoint touches reject; adjacent edges may meet only at exactly their intended shared vertex.
    - Zero-length edges, repeated consecutive vertices, adjacent retraces/overlaps, and overlapping first-last closure edges reject.
    - Near-collinear but separated segments have deterministic scaled-tolerance outcomes and valid ordinary contours remain accepted.
    - For every malformed left/right variant, the peer continues; valid-invalid-valid and concurrent independent requests recover with no stale proposal.
  </behavior>
  <action>
Write focused RED tests for: non-adjacent collinear overlap; a vertex on a non-adjacent segment; non-adjacent endpoint touch; consecutive duplicate vertices/zero-length edge; adjacent edge retrace; first-last closure retrace; reversed overlapping segment; and near-collinear separated controls. Mirror local malformed cases across both eyes, assert the valid peer remains active, and repeat valid → invalid → valid plus parallel independent invocations.

Replace `segmentsIntersect` strict-crossing-only logic with an inclusive orientation/on-segment predicate. Reject zero-length edges before pair comparison. For non-adjacent pairs, any proper crossing, collinear overlap, on-segment point, or endpoint touch is invalid. For adjacent pairs including the first/last pair, allow only their one intended shared endpoint; reject any overlap, retrace, extra touch, or degenerate edge. Use a finite, scale-aware tolerance derived from contour extent for orientation and bounding checks so precision behavior is deterministic, without widening acceptance for actual overlap. Keep all validation before rasterization and local to the affected eye. Do not change public API, composition order, transform constants, reviewed bounds, or evidence surfaces.
  </action>
  <verify>
    <automated>set -e; swift test --package-path BeautySDK --filter BeautyScleraRednessProviderTests; swift test --package-path BeautySDK --filter BeautyScleraRednessAdversarialCloseoutTests; swift test --package-path BeautySDK --filter BeautyEngineScleraRednessIntegrationTests; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Every listed inclusive-intersection/degenerate class has a focused failing-then-passing test.
    - Intended adjacent endpoints remain valid while any extra adjacent/non-adjacent contact rejects.
    - Both eye directions preserve peer continuation and sequential/parallel recovery.
    - No raw geometry becomes durable and no unrelated behavior changes.
  </acceptance_criteria>
  <done>Contour admission is inclusive, precision-bounded, per-eye fail-closed, and recovery-safe.</done>
</task>

</tasks>

<threat_model>
## OWASP ASVS Level 1 Trust Boundaries

| Boundary | Description |
|---|---|
| Untrusted mapped eye geometry → provider | Perturbed/degenerate contours and pupil locations must be rejected or contained before masks are created. |
| Production proposal → independent truth | The exact historical tuple is judged by test-authored truth and final bytes, not provider self-report. |
| One eye → peer eye/request | Local malformed geometry must not suppress or poison peer/sequential/concurrent work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-64-03 | Tampering | Protected-region proof | mitigate | Exact historical tuple, full neighborhood, direct proposal intersection, and full protected/outside byte equality. |
| T-64-04 | Information disclosure | Test observability | mitigate | Request-local internal proposal evidence only; aggregate durable outputs. |
| T-64-07 | Spoofing | Local acceptance | mitigate | Frozen outcome table, nonempty accepted/rejected classes, and peer continuation. |
| T-64-08 | Elevation of privilege | Contour admission | mitigate | Inclusive intersections, explicit degeneracy rejection, and recovery tests. |
| T-64-SC | Tampering | Dependency surface | mitigate | No dependency/install change; any HIGH finding blocks. |

No HIGH finding is accepted. Green counts cannot override an open HIGH result.
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

- A provider that contains or locally rejects the exact known asymmetric leak.
- A deterministic accepted/rejected boundary sweep retaining the original tuple unchanged.
- Inclusive segment-intersection and degenerate-contour regression coverage with peer recovery.
</artifacts_this_phase_produces>

<source_coverage_audit>
## Multi-Source Coverage Audit

| Source | Covered items | Plan evidence |
|---|---|---|
| GOAL | Per-eye adversarial protected-region proof | Tasks 64-07-01 and 64-07-02. |
| REQ | SCLERA-14, SCLERA-15 | Exact leak, full truth/bytes, inclusive contours, peer recovery. |
| RESEARCH | Fail closed, full-resolution truth, original-source composition, aggregate privacy | Preserved in both tasks. |
| CONTEXT | D-09, D-10, D-11, D-12, D-16, D-18 | Cited and enforced in actions/acceptance. |
| EXCLUDED | D-01 through D-08 output review, D-13 through D-15 review act, D-17/D-19 through D-21 promotion; DeviceRGB/去脂/UI/API/realtime/model/network | Routed to later serial plans or explicitly out of Phase 64 scope. |

All eight SCLERA-14/15 spec-less probe items are explicitly resolved by the flat backstops above; no assumption is silently dropped.
</source_coverage_audit>

<verification>
Run the focused provider, adversarial, and integration suites. The exact tuple and every malformed class must execute; no skip, lowered parameter, peer suppression, raw-data persistence, or HIGH override is allowed.
</verification>

<success_criteria>
- The literal known right-eye counterexample cannot change protected pixels.
- The surrounding calibrated envelope proves both safe acceptance and local rejection without shrinking parameters.
- Inclusive contour validity covers collinear/on-segment/retrace/zero-length/endpoint-touch cases.
- Per-eye peer continuation and sequential/concurrent recovery remain green.
</success_criteria>

<output>
Create `.planning/phases/64-sclera-output-adversarial-safety-and-independent-closeout/64-07-SUMMARY.md` when done.
</output>
