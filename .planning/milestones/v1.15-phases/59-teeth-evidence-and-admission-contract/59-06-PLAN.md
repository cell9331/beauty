---
phase: 59-teeth-evidence-and-admission-contract
plan: "06"
type: execute
wave: 3
depends_on: [59-05]
files_modified:
  - BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift
  - BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyEngineLocalRetouchFoundationTests.swift
autonomous: true
gap_closure: true
requirements: [SEQ-01, TEETH-07, TEETH-08]
security_standard: OWASP ASVS Level 1
block_on: HIGH
must_haves:
  truths:
    - "D-05/D-06: BeautyParameters has exactly one new trailing `teethWhitening: Float`, positive-only finite-normalized to 0...1, defaulting and missing-key decoding to zero, with all earlier source/Codable order unchanged."
    - "D-07/D-11: legacy labeled calls, legacy JSON, five existing presets, default/zero/non-finite input, and unrelated shipped effects remain neutral and compatible."
    - "D-09/D-10: only direct normalized `teethWhitening > 0` produces exactly one opaque demand; every global, lip, geometry, Testing, sibling, alias, zero, missing, negative, and non-finite input is neutral for teeth admission."
    - "D-08/D-12: the public surface contains only the scalar and the admission carrier remains opaque; no evidence, support, mask, provider, renderer, output, realtime/pixel-buffer, or Demo state is added."
    - "The canonical Phase 54 teeth row is open before either production file changes, and both production changes fail closed if that authority regresses."
  artifacts:
    - path: BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift
      provides: Exact trailing default-zero positive-only teeth intent
    - path: BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
      provides: Sole teeth-only one-demand admission route
    - path: BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift
      provides: Exact 59-to-60 stored/CodingKey/source/Codable compatibility proof
    - path: BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
      provides: Direct positive and forbidden-input admission matrix
    - path: BeautySDK/Tests/BeautyCoreTests/BeautyEngineLocalRetouchFoundationTests.swift
      provides: Opaque one-demand lifecycle and no-provider/no-output boundary proof
  key_links:
    - from: BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift
      to: BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
      via: parameters.normalized().teethWhitening as the only admission input
      pattern: "normalized\(\).*teethWhitening|teethWhitening > 0"
    - from: BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
      to: BeautySDK/Sources/BeautyEffects/Planning/BeautyLocalRetouchAdmission.swift
      via: one opaqueDemandCount: 1 or .none
      pattern: "opaqueDemandCount: 1|return \.none"
---

<objective>
Implement the previously skipped exact-open TEETH-07/08 scalar and admission seam, and nothing downstream (D-05 through D-12).

Purpose: Make the open evidence decision usable as one compatibility-safe SDK intent while preserving all legacy and sibling neutrality.
Output: One trailing public field, one binary opaque demand, and focused model/resolver/lifecycle tests.
</objective>

<execution_context>
@/Users/yakangwang/.antigravity_cockpit/instances/codex/cli-037507efedd7/gsd-core/workflows/execute-plan.md
@/Users/yakangwang/.antigravity_cockpit/instances/codex/cli-037507efedd7/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/REQUIREMENTS.md
@.planning/phases/59-teeth-evidence-and-admission-contract/59-CONTEXT.md
@.planning/phases/59-teeth-evidence-and-admission-contract/59-PATTERNS.md
@.planning/phases/59-teeth-evidence-and-admission-contract/59-05-SUMMARY.md
@.planning/milestones/v1.14-phases/54-rights-approved-evidence-and-eligibility-decisions/54-EVIDENCE-DECISIONS.json
@BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift
@BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
@BeautySDK/Sources/BeautyEffects/Planning/BeautyLocalRetouchAdmission.swift
@BeautySDK/Sources/BeautySDK/BeautyEngine.swift
</context>

<interfaces>
- Gate the first edit by parsing the canonical decision by `feature == teeth_whitening` and requiring the exact open `2/2/2/0/2` serializer result; do not rely on array position, the plan summary, or local media.
- `BeautyParameters` currently ends with `filterId`, `filterIntensity` and has 59 stored values: 58 Float values plus one optional filter ID. The sole addition follows `filterIntensity` on every ordered surface, yielding 60 stored values: 59 Float values plus filter ID.
- `BeautyEffectResolver.localRetouchAdmission(parameters:)` remains the only production authority. `BeautyLocalRetouchAdmission` stays package-private and feature-neutral with its private count.
- The existing Testing hook may exercise the generic opaque foundation but may not name or synthesize teeth eligibility; production teeth admission comes only from the public scalar.
</interfaces>

## Artifacts this phase produces

- A constructible, encodable, decodable, normalized `teethWhitening` scalar with exact legacy neutrality.
- Exactly one opaque request demand for any effective positive teeth value.
- Focused tests that reject all global/lip/geometry/Testing/sibling/alias/zero/missing activation.

<tasks>

<task id="59-06-01" type="auto" tdd="true">
  <name>Task 1: Append the exact teethWhitening public contract</name>
  <read_first>
    .planning/milestones/v1.14-phases/54-rights-approved-evidence-and-eligibility-decisions/54-EVIDENCE-DECISIONS.json
    .planning/phases/59-teeth-evidence-and-admission-contract/59-CONTEXT.md
    .planning/phases/59-teeth-evidence-and-admission-contract/59-PATTERNS.md
    BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift
    BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift
  </read_first>
  <files>BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift, BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift</files>
  <behavior>
    - "Default, omitted key, negative, NaN, and both infinities produce teethWhitening == 0; positive finite values clamp to 0...1."
    - "The field is last after filterIntensity in stored, CodingKeys, initializer, decoder, and normalized construction order."
    - "A nonnil-filter encoded object has 60 keys; a default encoded object has 59 keys because only nil filterId is omitted; both include teethWhitening."
    - "A legacy 59-field payload and legacy labeled initializer call that omit teethWhitening remain source/semantic compatible and decode/construct zero."
    - "Unequal values round-trip independently without changing any existing value."
  </behavior>
  <action>First assert the canonical teeth decision is the exact serializer-produced open row; abort without source changes otherwise. Write/update tests to fail on the current 59-field model, then implement D-05/D-06: append `public var teethWhitening: Float` after `filterIntensity`, append its CodingKey, append a trailing defaulted initializer parameter, assign with the existing `clampUnit`, append `decodeFloatIfPresent(.teethWhitening)`, and append the argument to `normalized()`. Update the historical exact-inventory tests so they preserve their original decisions while asserting the current 60-field state; do not leave contradictory active tests that require current teeth absence. Prove exact stored/CodingKey order, default and nonnil-filter encoded shapes, legacy missing-key decode, old labeled construction, finite bounds, non-finite reset, and unequal-value round trip. Do not add the field to `requiresFaceGeometry`, presets, effective strengths, public results, diagnostics, persistence, provider state, renderer cases, or Demo state (D-07/D-08/D-11/D-12).</action>
  <verify>
    <automated>node -e 'const d=require("./.planning/milestones/v1.14-phases/54-rights-approved-evidence-and-eligibility-decisions/54-EVIDENCE-DECISIONS.json");const r=d.feature_decisions.find(x=&gt;x.feature==="teeth_whitening");if(JSON.stringify(r)!==JSON.stringify({feature:"teeth_whitening",status:"open",reasons:[],eligible_count:2,reviewed_count:2,accepted_count:2,rejected_count:0,naturalness_weight:2}))process.exit(1)' &amp;&amp; CLANG_MODULE_CACHE_PATH=/private/tmp/beauty-phase59-clang-module-cache swift test --package-path BeautySDK --filter BeautyParametersTests &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Current model inventory is exactly 60 stored/CodingKeys with teethWhitening last and all 59 prior labels in prior order.
    - Normalization, default, legacy decode/source, and unequal Codable tests pass for the exact positive-only semantics.
    - Existing keys, initializers, preset resources, and unrelated parameter values are unchanged.
    - No provider/output/Demo or sibling field appears in production.
  </acceptance_criteria>
  <done>TEETH-07 is implemented as one exact trailing finite-normalized default-zero field with complete append-only compatibility evidence.</done>
</task>

<task id="59-06-02" type="auto" tdd="true">
  <name>Task 2: Admit exactly one direct teeth demand</name>
  <read_first>
    .planning/milestones/v1.14-phases/54-rights-approved-evidence-and-eligibility-decisions/54-EVIDENCE-DECISIONS.json
    BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift
    BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
    BeautySDK/Sources/BeautyEffects/Planning/BeautyLocalRetouchAdmission.swift
    BeautySDK/Sources/BeautySDK/BeautyEngine.swift
    BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
    BeautySDK/Tests/BeautyCoreTests/BeautyEngineLocalRetouchFoundationTests.swift
  </read_first>
  <files>BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift, BeautySDK/Tests/BeautyCoreTests/BeautyEngineLocalRetouchFoundationTests.swift</files>
  <behavior>
    - "Float.ulpOfOne through 1.0 produce one non-empty opaque admission; every positive value still produces one demand, never a multiplicity."
    - "Zero, omitted, negative, NaN, and infinities produce .none after normalization."
    - "Every existing global color/skin whitening field, lipColor, all geometry fields, filter values, generic Testing injection, sibling identity, and spelling/snake/dotted/Chinese alias remains neutral for production teeth admission."
    - "The one demand enters the existing still-image canonical request foundation but adds zero provider, mask, transform, composition contribution, renderer output, or public diagnostic identity."
  </behavior>
  <action>Write the resolver matrix first, then implement D-09/D-10 using one normalized parameter value: return `BeautyLocalRetouchAdmission(opaqueDemandCount: 1)` only when `parameters.normalized().teethWhitening > 0`, else return `.none`. Keep the carrier unchanged, private-counted, and feature-neutral. Test direct exact-bound positives, every normalized-away value, and a table spanning skinWhitening, brightness, contrast, saturation, temperature, tint, exposure, highlight, shadow, lipColor, all geometry families, filter state, unknown JSON aliases, sibling identities, and Testing-only opaque injection; unrelated values may retain their shipped effects but cannot alter the production teeth decision. Update the foundation tests that currently assert literal `.none`: require the exact one-demand resolver source/behavior, preserve the generic zero/one/many one-request oracle, and prove no provider/output contribution is created. Do not add `teethWhitening` to geometry requirements/effective strengths, do not expose an admission name through Testing, and do not implement provider/mask/renderer/output/Demo/realtime work (D-08/D-11/D-12).</action>
  <verify>
    <automated>CLANG_MODULE_CACHE_PATH=/private/tmp/beauty-phase59-clang-module-cache swift test --package-path BeautySDK --filter 'BeautyEffectResolverTests|BeautyEngineLocalRetouchFoundationTests' &amp;&amp; ! rg -n 'teethWhitening.*(Provider|Mask|Transform|Renderer|Output)|scleraRednessReduction|upperEyelidFullnessReduction' BeautySDK/Sources BeautyDemo/BeautyDemo &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Direct normalized positive teeth input is the sole non-empty admission case and source constructs exactly `opaqueDemandCount: 1`.
    - The complete forbidden-input matrix remains `.none` for teeth; unknown aliases decode without activation.
    - The package-private carrier exposes no feature name/evidence/support and generic Testing hooks do not become teeth authority.
    - No provider, mask, renderer, output, Demo, realtime, pixel-buffer, model, or network implementation exists.
  </acceptance_criteria>
  <done>TEETH-08 is implemented as exactly one teeth-only opaque demand with complete legacy/global/lip/geometry/Testing/sibling/alias/zero/missing neutrality.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| JSON/source caller -> BeautyParameters | Untrusted numeric and unknown-key input crosses normalization and decode. |
| BeautyParameters -> resolver | Public intent crosses into package-private admission. |
| admission -> still-image foundation | One binary request demand may trigger expensive canonical/detection work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-59-03 | Elevation of Privilege | public/admission gate | mitigate (HIGH) | Require exact canonical open row before edits and direct positive scalar before non-empty admission. |
| T-59-04 | Tampering | BeautyParameters compatibility | mitigate (HIGH) | Exact ordered 60-field/CodingKey/encoded inventories, legacy payload/source tests, finite normalization. |
| T-59-05 | Spoofing/Elevation of Privilege | resolver inputs and aliases | mitigate (HIGH) | Table-driven global/lip/geometry/Testing/sibling/alias negatives and exact one-demand source gate. |
| T-59-07 | Denial of Service | request admission | mitigate (HIGH) | Binary opaque demand count; zero/malformed/non-finite inputs do not trigger the local request. |
| T-59-SC | Tampering | package installs | mitigate (HIGH) | No dependency or package install is permitted. |

<blocking_rule>Any HIGH failure, closed/malformed canonical decision, field-order drift, legacy incompatibility, forbidden-input activation, demand multiplication, public raw state, or downstream implementation blocks the plan.</blocking_rule>
</threat_model>

<verification>Run both focused task commands, then run `swift test --package-path BeautySDK --filter 'BeautyParametersTests|BeautyEffectResolverTests|BeautyEngineLocalRetouchFoundationTests'` as one combined seam check and inspect the production diff for exact scope.</verification>

<success_criteria>The exact-open branch now has one and only one public scalar and one and only one opaque demand; every forbidden input is neutral and every provider/output/Demo surface remains absent.</success_criteria>

<output>Create `.planning/phases/59-teeth-evidence-and-admission-contract/59-06-SUMMARY.md` with the exact model inventory and admission matrix counts, without claiming visible teeth output.</output>
