---
phase: 59
plan: 07
status: completed-exact-open-boundary
---

# Plan 59-07 Summary

Moved every active Phase 59 checker and contract from the former closed branch
to the exact serializer-produced open state while preserving all downstream and
sibling absence.

## Exact state

- The canonical teeth row must be open at `2/2/2/0/2` with no reasons.
- Exactly two structured teeth reviews are required: one opaque positive and
  one opaque negative with their fixed accepted judgments.
- Sclera redness and upper-eyelid fullness remain exact closed zero-weight
  siblings with their existing reasons.
- `BeautyParameters` remains exactly 60 stored/CodingKey/initializer fields,
  with `teethWhitening` last; only its normalized positive value creates one
  opaque demand.

## Compatibility and scope

The five preset files remain byte-identical to the pre-gap baseline and decode
the missing teeth key as zero. The example renderer remains exactly 72 cases
with one facade output call and no teeth case. The Demo keeps exactly the three
local-retouch taxonomy rows disabled with nil controls. No provider, mask,
transform, saved output, realtime/pixel-buffer route, model, network route,
sclera surface, or `去脂` surface was added.

## Privacy and security

The durable checker now uses exact object schemas, permitting the fixed
`mask_coverage` and `protected_leakage` judgments while rejecting extra local
media, locator, digest, rights, geometry, pixel, identity, prose, and raw-error
fields. Privacy mode invokes the private runner's fixed-output tracked/staged
scan. Each T-59-01 through T-59-08 has an isolated owner mode, and the mutation
self-test exercises 235 fail-closed cases.

## Verification

- Private Phase 59 evidence contract: 9/9; fresh canonical-ledger verification passed.
- Checker: 235 mutation cases; decision, evidence, privacy, live, and 8/8 isolated HIGH modes passed.
- Preset/renderer compatibility: 44/44 focused SwiftPM tests.
- Demo disabled-taxonomy boundary: 30/30 tests on iPhone 17e / iOS 26.5.
- Five preset hashes, 72 renderer IDs, unchanged production-path diff, JSON,
  Python syntax, privacy, and diff-hygiene checks passed.

The result admits intent only. Visible whitening, provider safety, output
behavior, Demo activation, performance, and product promotion remain unclaimed.
