---
phase: 62-sclera-evidence-and-admission-contract
plan: "02"
type: execute
wave: 2
depends_on: [62-01]
files_modified:
  - .planning/phases/62-sclera-evidence-and-admission-contract/62-private-evidence-runner.js
  - .planning/phases/62-sclera-evidence-and-admission-contract/62-authorized-sclera-evidence-export.js
  - .planning/phases/62-sclera-evidence-and-admission-contract/62-evidence-admission.contract.test.js
  - .planning/phases/62-sclera-evidence-and-admission-contract/check_phase62_sclera_admission_boundaries.py
  - .planning/phases/62-sclera-evidence-and-admission-contract/62-VALIDATION.md
autonomous: true
requirements: [EVID-06, EVID-09, EVID-10]
decisions: [D-01, D-02, D-04, D-08, D-17, D-18]
security_standard: OWASP ASVS Level 1
block_on: HIGH
must_haves:
  truths:
    - "D-02: The two rows have independent opaque IDs, predeclared polarity/target, approved rights and exact-bound triples; same-person identity is unnecessary."
    - "D-08: Phase 54 trusted binding, ReviewCore reducer and serializer are the sole authority while teeth open and eyelid closed remain unchanged."
    - "Private discovery is NUL-safe, bounded, nofollow, exact-feature and exact-polarity; zero or multiple matching bundles fail with fixed path-free output."
    - "The adapter accepts only two approved `sclera_redness` rows with distinct opaque IDs, one positive, one negative, complete bound triples and unique logical asset basenames."
    - "Actual asset digests and rights projections exist only in child-process memory and are passed directly into the Phase 54 trusted authorization registry."
    - "Without a real local pair, all adapter/open commands fail closed, the canonical ledger remains byte-identical, and no scalar or demand is added."
    - "Tracked/staged privacy scanning works in the closed state and fails on sensitive keys, local locators, review prose, scanner errors or path-bearing child output."
  prohibitions:
    - statement: "Disposable mechanics fixtures used for negative-path tests must not serialize an open canonical sclera decision or contribute review weight."
      status: unverified
      verification: flagged
    - statement: "The Phase 59 teeth runner, adapter, private bundle, reviews and verified ledger reproduction must not be modified or repurposed for sclera."
      status: unverified
      verification: flagged
    - statement: "Private runner errors must not reveal candidate paths, filenames, digests, rights records, scanner matches or local review content."
      status: unverified
      verification: flagged
  artifacts:
    - path: .planning/phases/62-sclera-evidence-and-admission-contract/62-private-evidence-runner.js
      provides: Fixed-output exact sclera bundle discovery and tracked/staged privacy scan
    - path: .planning/phases/62-sclera-evidence-and-admission-contract/62-authorized-sclera-evidence-export.js
      provides: Phase 54-only trusted binding and serializer adapter
    - path: .planning/phases/62-sclera-evidence-and-admission-contract/62-evidence-admission.contract.test.js
      provides: Missing/malformed/ambiguous/private adapter contracts
  key_links:
    - from: .planning/phases/62-sclera-evidence-and-admission-contract/62-private-evidence-runner.js
      to: .planning/phases/62-sclera-evidence-and-admission-contract/62-authorized-sclera-evidence-export.js
      via: absolute bundle path only in child environment `PHASE62_SCLERA_BUNDLE`
      pattern: "PHASE62_SCLERA_BUNDLE|--scan-tracked-staged"
    - from: .planning/phases/62-sclera-evidence-and-admission-contract/62-authorized-sclera-evidence-export.js
      to: .planning/milestones/v1.14-phases/54-rights-approved-evidence-and-eligibility-decisions/54-evidence-core.js
      via: trusted registry, ReviewCore-issued reviews and serializeDurableExport
      pattern: "createTrustedAuthorizationRegistry|createReviewSnapshot|serializeDurableExport"
---

<objective>
Prepare the private sclera evidence intake and serialization path while keeping
the canonical decision and production runtime exactly closed without real input.

Purpose: Make the eventual licensed review reproducible and privacy-safe before
the user supplies images.
Output: Sclera-only private runner, adapter, negative-path tests and closed-state
privacy proof.
</objective>

<execution_context>
@/Users/yakangwang/.antigravity_cockpit/instances/codex/cli-037507efedd7/gsd-core/workflows/execute-plan.md
@/Users/yakangwang/.antigravity_cockpit/instances/codex/cli-037507efedd7/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/phases/62-sclera-evidence-and-admission-contract/62-CONTEXT.md
@.planning/phases/62-sclera-evidence-and-admission-contract/62-RESEARCH.md
@.planning/phases/62-sclera-evidence-and-admission-contract/62-PATTERNS.md
@.planning/phases/62-sclera-evidence-and-admission-contract/62-SCLERA-EVIDENCE-ADMISSION-CONTRACT.md
@.planning/phases/62-sclera-evidence-and-admission-contract/62-VALIDATION.md
@.planning/phases/59-teeth-evidence-and-admission-contract/59-private-evidence-runner.js
@.planning/phases/59-teeth-evidence-and-admission-contract/59-authorized-teeth-evidence-export.js
</context>

<interfaces>
- The runner discovers ignored manifests by parsed feature/polarity shape, not dates, user filenames or a path embedded in tracked text.
- Child output is a fixed JSON/status vocabulary; resolved bundle paths and manifest content never appear in stdout, stderr, argv or tracked artifacts.
- Logical asset keys use unique basenames such as `sclera_fixture_001-original.png` so Phase 54 duplicate-basename rejection remains active.
- A local fixed-judgment review record is untrusted input; ReviewCore must issue and validate the durable review before serialization.
- Closed-state privacy scanning does not require a bundle; once a bundle exists it additionally rejects all six local digests in tracked and staged content.
</interfaces>

## Artifacts this phase produces

- `discoverBundle`, `assertIgnoredBundle`, `trackedStagedPrivacyScan` and fixed child execution in `62-private-evidence-runner.js`.
- `loadCanonicalInput`, `reviewFor`, `buildExport`, `--verify-only` and `--verify-ledger` in `62-authorized-sclera-evidence-export.js`.
- Negative-path fixtures and assertions that cannot write or open the canonical ledger.

<tasks>

<task id="62-02-01" type="auto" tdd="true">
  <name>Task 1: Implement exact private discovery and ReviewCore adapter boundaries</name>
  <read_first>
    .planning/phases/62-sclera-evidence-and-admission-contract/62-SCLERA-EVIDENCE-ADMISSION-CONTRACT.md
    .planning/phases/62-sclera-evidence-and-admission-contract/62-evidence-admission.contract.test.js
    .planning/phases/59-teeth-evidence-and-admission-contract/59-private-evidence-runner.js
    .planning/phases/59-teeth-evidence-and-admission-contract/59-authorized-teeth-evidence-export.js
    .planning/milestones/v1.14-phases/54-rights-approved-evidence-and-eligibility-decisions/54-evidence-core.js
    .planning/milestones/v1.14-phases/54-rights-approved-evidence-and-eligibility-decisions/54-evidence-manifest.schema.json
  </read_first>
  <files>.planning/phases/62-sclera-evidence-and-admission-contract/62-private-evidence-runner.js, .planning/phases/62-sclera-evidence-and-admission-contract/62-authorized-sclera-evidence-export.js, .planning/phases/62-sclera-evidence-and-admission-contract/62-evidence-admission.contract.test.js</files>
  <action>Implement a new sclera-only runner and adapter without editing the Phase 59 files. Enumerate ignored files with Git NUL output, reject symlinks/non-regular/oversized assets, require exactly one manifest with exactly two `sclera_redness` fixtures and distinct positive/negative polarity, and verify all seven bundle files are ignored. In the adapter, require an absolute environment-supplied bundle when `PHASE62_REQUIRE_LOCAL_EVIDENCE=1`; validate opaque IDs, approved internal-evaluation rights, exact fixture-relative bindings, unique logical basenames and a local fixed-judgment record; compute six SHA-256 values only in memory; construct the trusted registry; and issue reviews through ReviewCore. Define complete-export composition so the unmodified Phase 59 runner/adapter reconstructs teeth from its own authorized pair, Phase 62 reconstructs sclera, and ReviewCore creates the closed eyelid input; serialize only through ReviewCore. Before real sclera input exists, tests use only missing/malformed/mechanics-only disposable cases and must never obtain or write a canonical open row. Emit fixed status/errors without local values.</action>
  <verify>
    <automated>node --test .planning/phases/62-sclera-evidence-and-admission-contract/62-evidence-admission.contract.test.js &amp;&amp; ! PHASE62_REQUIRE_LOCAL_EVIDENCE=1 node .planning/phases/62-sclera-evidence-and-admission-contract/62-authorized-sclera-evidence-export.js --verify-only 2&gt;/dev/null &amp;&amp; git diff --exit-code 5793d1b -- .planning/phases/59-teeth-evidence-and-admission-contract/59-private-evidence-runner.js .planning/phases/59-teeth-evidence-and-admission-contract/59-authorized-teeth-evidence-export.js &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Missing, wrong-feature, mechanics-only, one-polarity, duplicate-ID, ambiguous-basename, symlink, oversized and incomplete bundles fail fixed and nonzero.
    - No negative-path test can serialize or write an open canonical sclera row.
    - Adapter source calls the Phase 54 trusted registry, snapshot, review, reducer and serializer APIs directly.
    - Phase 59 private runner and adapter have zero diff.
  </acceptance_criteria>
  <done>The private route is ready to consume a future genuine pair but cannot fabricate one.</done>
</task>

<task id="62-02-02" type="auto" tdd="true">
  <name>Task 2: Prove closed-state privacy and intake failure behavior</name>
  <read_first>
    .planning/phases/62-sclera-evidence-and-admission-contract/62-private-evidence-runner.js
    .planning/phases/62-sclera-evidence-and-admission-contract/62-authorized-sclera-evidence-export.js
    .planning/phases/62-sclera-evidence-and-admission-contract/check_phase62_sclera_admission_boundaries.py
    .planning/phases/62-sclera-evidence-and-admission-contract/62-VALIDATION.md
    .planning/milestones/v1.14-phases/54-rights-approved-evidence-and-eligibility-decisions/54-EVIDENCE-DECISIONS.json
  </read_first>
  <files>.planning/phases/62-sclera-evidence-and-admission-contract/62-private-evidence-runner.js, .planning/phases/62-sclera-evidence-and-admission-contract/62-evidence-admission.contract.test.js, .planning/phases/62-sclera-evidence-and-admission-contract/check_phase62_sclera_admission_boundaries.py, .planning/phases/62-sclera-evidence-and-admission-contract/62-VALIDATION.md</files>
  <action>Add a closed-state tracked/staged scan that checks exact evidence schemas and active sensitive patterns without requiring local media. Seed temporary tracked and staged mutations for a locator, digest-shaped value, rights detail, identity, raw support/mask/geometry/pixel field, reviewer prose and raw error; each must fail T-62-06 without echoing the seeded value. Simulate Git scan failure, child failure, malformed NUL inventory, zero and multiple candidates; classify all as failures. Re-run the canonical closed checker and record Wave 0 complete only when adapter negative paths, privacy mutations and all eight baseline HIGH owners pass. Leave the ledger bytes and all production sources unchanged.</action>
  <verify>
    <automated>node .planning/phases/62-sclera-evidence-and-admission-contract/62-private-evidence-runner.js --scan-tracked-staged --closed &amp;&amp; node --test .planning/phases/62-sclera-evidence-and-admission-contract/62-evidence-admission.contract.test.js &amp;&amp; python3 .planning/phases/62-sclera-evidence-and-admission-contract/check_phase62_sclera_admission_boundaries.py --privacy --repo-root . &amp;&amp; python3 .planning/phases/62-sclera-evidence-and-admission-contract/check_phase62_sclera_admission_boundaries.py --closed --repo-root . &amp;&amp; git diff --exit-code -- .planning/milestones/v1.14-phases/54-rights-approved-evidence-and-eligibility-decisions/54-EVIDENCE-DECISIONS.json BeautySDK/Sources &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Closed privacy scan succeeds with fixed aggregate output and no bundle requirement.
    - Every sensitive tracked/staged mutation and every scanner/child error fails without reproducing the sensitive value.
    - Canonical ledger is byte-identical and runtime still contains no sclera scalar, demand, provider or output.
    - `62-VALIDATION.md` marks Wave 0 complete but keeps Tasks 62-03-01 onward blocked.
  </acceptance_criteria>
  <done>All safe preparation is complete; real licensed originals are now the only intake blocker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
| --- | --- | --- |
| Ignored filesystem to child | Ambiguous, escaped, linked or substituted bundle | NUL-safe exact discovery, nofollow, bounds and complete ignored inventory |
| Local manifest to ReviewCore | Claimed rights or asset identity is spoofed | Exact fields, actual digests, opaque trusted projection and unique basenames |
| Child to tracked process | Local path/content leaks through output or errors | Environment-only locator and fixed status vocabulary |
| Scanner result to closeout | Tool failure appears clean | Classified exit status and negative tool-error tests |

<blocking_rule>Any discovery ambiguity, rights/binding mismatch, path-bearing output, privacy mutation miss, Phase 59 diff, canonical ledger diff or production sclera surface blocks the real intake checkpoint.</blocking_rule>
</threat_model>

<verification>Run all adapter/runner negative paths, closed tracked/staged privacy, checker self/closed/privacy/live and canonical/production no-diff assertions.</verification>

<success_criteria>A privacy-safe private sclera route exists, but with no real pair the durable decision and runtime remain exactly closed.</success_criteria>

<output>Create `.planning/phases/62-sclera-evidence-and-admission-contract/62-02-SUMMARY.md` with negative-path, privacy and Wave 0 counts.</output>
