---
phase: 50-independent-eyebrow-geometry-and-pipeline-integration
plan: "04"
type: execute
wave: 3
depends_on: ["50-02"]
files_modified:
  - BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift
  - BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift
autonomous: true
requirements: [PIPE-02]
must_haves:
  truths:
    - "D-10/D-12: all seven eyebrow fields appear exactly once in the conflict resolver's scale assignment, absolute-value geometry total, and nonzero retained-count inventories."
    - "With seven provisional 0.25 eyebrow caps, the exact 44-field all-eligible ledger is face/chin 3.35 + eye 4.10 + eyebrow 1.75 + nose 1.80 + mouth 2.45 = 13.45."
    - "At threshold 1.0, one shared scale equals `1 / 13.45`, weakened count equals 44, final total equals 1.0, and all six signed eyebrow values retain polarity."
    - "Provider-ineligible eyebrow names are absent before this resolver and therefore contribute zero times and cannot weaken unrelated domains."
  artifacts:
    - path: BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift
      provides: "Exact 44-name shared geometry scale, total, and count inventories"
    - path: BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift
      provides: "Exact names/subtotals/13.45/44/scale/polarity and missing-name mutation evidence"
  key_links:
    - from: BeautyEffectiveStrengths eyebrow fields
      to: GeometryConflictResolver.resolve
      via: "seven exact scale assignments and absolute-value ledger entries"
      pattern: "eyebrowYPosition|eyebrowPeakDefinition"
    - from: geometryTotal and nonZeroGeometryCount
      to: BeautyConflictResolution
      via: "one scale and weakenedCount derived from the same exact inventory"
      pattern: "geometryTotal|weakenedCount"
---

<objective>
Extend shared geometry conflict arithmetic from 37 to exactly 44 provider-eligible names.

Purpose: Make eyebrow work participate in the same single weakening budget as face, eye, nose, and mouth without aliases, omissions, or double counting.
Output: Exact conflict resolver inventories and tests for 13.45 total, 44 retained fields, one scale, and signed-polarity preservation.
</objective>

## Artifacts this phase produces

- Seven new exact scale assignments in `GeometryConflictResolver.resolve(strengths:)` for the seven eyebrow effective fields.
- Seven new exact absolute-value entries in `geometryTotal(_:)` and seven new exact nonzero entries in `nonZeroGeometryCount(_:)`.
- Exact 44-name test ledger and mutation tests proving an omitted/duplicated eyebrow name, sign loss, wrong subtotal, wrong scale, or wrong weakened count fails.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<read_first>
AGENTS.md; PLANS.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-CONTEXT.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-RESEARCH.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-PATTERNS.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-VALIDATION.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-02-SUMMARY.md; BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift
</read_first>

<context>
@.planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-CONTEXT.md
@.planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-RESEARCH.md
@.planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-PATTERNS.md
@.planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-02-SUMMARY.md
@BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift

<interfaces>
Plan 02 adds the seven `BeautyEffectiveStrengths` fields. `GeometryConflictResolver.resolve(strengths:)` currently has three manually parallel 37-field inventories: scaled assignments, `geometryTotal(_:)`, and `nonZeroGeometryCount(_:)`. Every signed value is totaled/counted by absolute value; the result exposes one final `strengths`, `geometryScale`, and `weakenedCount`.
</interfaces>
</context>

## Decision trace

- D-09/D-10: exactly seven named effective fields expand the provider-eligible inventory to exactly 44.
- D-12/D-13: one shared scale preserves polarity and must agree with exact total/count evidence.
- D-14: 0.25 and 13.45 are provisional Phase 50 arithmetic; Phase 52 owns final calibration.
- D-15/D-16/D-18: no API/pass/output/gallery/promotion expansion is part of conflict arithmetic.

## Plan-local source coverage

| Source | Coverage |
| --- | --- |
| GOAL | Connects eyebrow provider work to the existing conflict seam. |
| REQ | PIPE-02 receives exact names, total, count, scale, and polarity evidence. |
| RESEARCH | Implements A2's exact 13.45 arithmetic and the three-inventory pattern. |
| CONTEXT | D-09/D-10/D-12..16/D-18 are implemented or fenced. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 50-04-01: Extend all three conflict inventories to seven eyebrow names</name>
  <files>BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift, BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift</files>
  <read_first>AGENTS.md; PLANS.md; DESIGN.md; RELIABILITY.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-CONTEXT.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-RESEARCH.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-PATTERNS.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-02-SUMMARY.md; BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift; BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift</read_first>
  <behavior>
    - Each eyebrow field is scaled exactly once when the provider-eligible total exceeds the threshold.
    - Each eyebrow field contributes `abs(value)` exactly once to total and one exactly once to retained count when nonzero.
    - Negative values for all six signed fields retain sign after scaling; peak remains nonnegative.
  </behavior>
  <action>Per D-09/D-10/D-12, write failing table-driven tests over the exact seven eyebrow key paths, then add the seven fields to all three manual inventories in stable eyebrow group order between eye and nose. Do not introduce a brow-specific threshold, subtotal, scale, loop, or resolver. Preserve the existing threshold and every shipped assignment verbatim. Add source-cardinality assertions/mutations so a name missing from any one inventory or duplicated within an inventory fails.</action>
  <acceptance_criteria>Exactly 44 distinct geometry names are represented across the parallel inventories; each eyebrow name scales/totals/counts once; signed polarity is retained; every shipped conflict-only test remains unchanged and green.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --disable-sandbox --jobs 1 --filter BeautyEffectsTests.GeometryConflictResolverTests</automated></verify>
  <done>The conflict resolver treats seven eyebrow fields as first-class members of one shared 44-name budget.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 50-04-02: Lock exact 44-field provisional arithmetic</name>
  <files>BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift</files>
  <read_first>AGENTS.md; PLANS.md; DESIGN.md; RELIABILITY.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-CONTEXT.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-RESEARCH.md; .planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-02-SUMMARY.md; BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift; BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift; BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift</read_first>
  <behavior>
    - Exact provider-eligible cap subtotals are 3.35 face/chin, 4.10 eye, 1.75 eyebrow, 1.80 nose, and 2.45 mouth.
    - Exact unscaled total is 13.45; retained count is 44; expected scale is `1 / 13.45`; weakened count is 44; final absolute total is 1 within existing floating tolerance.
    - Removing any one named eyebrow field changes total by exactly 0.25, count by exactly one, and recomputes the single scale without changing other field signs.
  </behavior>
  <action>Per D-10/D-13/D-14, construct one exact all-provider-eligible `BeautyEffectiveStrengths` fixture at the current caps and assert every named subgroup and final arithmetic value, not relational bounds. Include alternating signs across the six signed eyebrow fields and direct polarity assertions after scaling. Add seven one-field-removal rows and a provider-ineligible eyebrow baseline showing absent work contributes zero and cannot weaken unrelated values. Label 13.45 and 0.25 as provisional Phase 50 expectations, not final cap evidence.</action>
  <acceptance_criteria>The focused suite proves exact 13.45/44/1-over-13.45 arithmetic and catches every omitted, duplicated, sign-lost, or pre-eligibility-counted eyebrow field.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --disable-sandbox --jobs 1 --filter BeautyEffectsTests.GeometryConflictResolverTests &amp;&amp; git diff --check</automated></verify>
  <done>PIPE-02 conflict arithmetic is exact and auditable before combined provider/dispatch evidence.</done>
</task>

</tasks>

<threat_model>
<blocking_rule>ASVS L1 with block-on-high: any unresolved HIGH integrity finding, omitted/duplicated field, sign loss, second scale, wrong exact arithmetic, or focused-test failure blocks completion; HIGH threats may not be accepted or transferred.</blocking_rule>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Provider-eligible retained strengths → shared conflict math | Manual inventories can omit, duplicate, or sign-corrupt a field. |
| Conflict result → downstream resolver/dispatch evidence | Total, count, scale, and weakenedCount must describe the same exact retained mask. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
| --- | --- | ---: | --- | --- | --- |
| T-50-10 | Tampering | high | GeometryConflictResolver inventories | mitigate | Seven-key-path table, three-inventory cardinality checks, omission/duplication mutations, and signed rows. |
| T-50-11 | Repudiation | high | exact combined arithmetic | mitigate | Literal subgroup/13.45/44/scale/final-total assertions with no relational-only acceptance. |
| T-50-SC | Tampering | medium | package supply chain | mitigate | No install or package/resource/target change is planned. |
</threat_model>

<verification>
- Focused conflict tests pass exact seven-name inventory, 13.45 total, 44 count, one scale, weakened count, and polarity rows.
- No resolver, provider, pipeline, facade, renderer, gallery, or documentation file changes in this plan.
- `git diff --check` passes.
</verification>

<success_criteria>
- PIPE-02's shared conflict arithmetic is exact across all 44 provider-eligible fields.
- Missing or provider-empty eyebrow work contributes zero before conflict and unrelated domains keep their prior values.
- All values remain explicitly provisional pending Phase 52.
</success_criteria>

<output>
Create `.planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-04-SUMMARY.md` when done.
</output>
