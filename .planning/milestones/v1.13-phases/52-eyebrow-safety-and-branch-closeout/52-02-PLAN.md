---
phase: 52-eyebrow-safety-and-branch-closeout
plan: "02"
type: execute
wave: 2
depends_on: ["52-01"]
files_modified:
  - BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautyGeometryEffectPipelineTests.swift
autonomous: true
requirements: [SAFE-01, SAFE-02]
must_haves:
  truths:
    - "The complete final geometry oracle contains exactly 44 unique fields in stable 9 face/chin + 14 eye + 7 eyebrow + 6 nose + 8 mouth order; eyebrow named emission order is Y position, thickness, length, spacing, head spacing, tilt, then peak (D-09, D-12)."
    - "At final caps, domain subtotals remain exactly 3.35 + 4.10 + 1.75 + 1.80 + 2.45 = 13.45, count/weakened count is 44, the single scale is `1 / 13.45`, final total is 1, and every signed polarity is preserved (D-10)."
    - "One unscaled retained baseline permits at most 44 monotone provider-eligibility removals; no field re-enters, no alternate bound exists, reused input scales only once, and there is no second conflict pass (D-11)."
    - "Late eyebrow provider-empty work, pair-only loss, per-side continuation, reused input, and mixed signed domains leave final effective strengths, totals, counts, scale, warnings, metrics, named emissions, point count, and Face→Chin→Eye→Eyebrow→Nose→Mouth dispatch exactly equal (D-11, D-12)."
  artifacts:
    - path: BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift
      provides: "Exact 44-field inventory, domain arithmetic, one-scale, count, and polarity oracle"
      contains: "13.45"
    - path: BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift
      provides: "Monotone retained-mask and final named-emission equality evidence"
      contains: "0..<44"
    - path: BeautySDK/Tests/BeautyEffectsTests/BeautyGeometryEffectPipelineTests.swift
      provides: "Stable exactly-once named emission and unified dispatch order evidence"
      contains: "EyebrowWarpProvider"
  key_links:
    - from: BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift
      to: BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift
      via: "The same exact 44-name retained inventory determines one scale and the final nonzero mask"
      pattern: "44|13\\.45"
    - from: BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift
      to: BeautySDK/Tests/BeautyEffectsTests/BeautyGeometryEffectPipelineTests.swift
      via: "Every final nonzero field owns one nonempty named array and the arrays concatenate once in stable provider order"
      pattern: "Face.*Chin.*Eye.*Eyebrow.*Nose.*Mouth"
  prohibitions:
    - "Never add a second conflict pass, scale an already scaled retained baseline, permit provider-empty re-entry, or use a convergence bound other than the exact 44-field inventory."
    - "Never reorder or duplicate eyebrow named emissions or unified provider dispatch, and never treat aggregate counts as a substitute for field-to-emission equality."
---

<objective>
Freeze the exact 44-field one-baseline convergence oracle and prove final strengths, accounting, named emissions, and unified dispatch agree under every eyebrow removal and reuse case.

Purpose: SAFE-02 cannot close from representative arithmetic; the final retained mask must be identical across conflict resolution, providers, metrics, and rendering.
Output: Final exact arithmetic, bounded monotone convergence, and exactly-once stable dispatch evidence.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@DESIGN.md
@RELIABILITY.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-eyebrow-safety-and-branch-closeout/52-CONTEXT.md
@.planning/phases/52-eyebrow-safety-and-branch-closeout/52-RESEARCH.md
@.planning/phases/52-eyebrow-safety-and-branch-closeout/52-PATTERNS.md
@.planning/phases/52-eyebrow-safety-and-branch-closeout/52-VALIDATION.md
@.planning/phases/52-eyebrow-safety-and-branch-closeout/52-01-SUMMARY.md
@BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
@BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift
@BeautySDK/Sources/BeautyEffects/Warp/EyebrowWarpProvider.swift
@BeautySDK/Sources/BeautyEffects/Render/BeautyGeometryEffectPipeline.swift

<interfaces>
Existing production contracts this plan verifies rather than redesigns:
- `GeometryConflictResolver.resolve(strengths:)` computes a magnitude total, applies one shared scale only when total exceeds 1, preserves signs, and emits only `combined_geometry_weakened`, `beauty.effects.weakenedCount`, and `beauty.effects.geometryStrengthScale`.
- `BeautyEffectResolver.resolveGeometryConflict` evolves one unscaled retained baseline through provider-owned field sanitization in Face→Chin→Eye→Eyebrow→Nose→Mouth order and contains exactly one `0..<44` loop.
- `EyebrowWarpFieldEmissions.points` concatenates the seven named arrays in public-field order.
- `BeautyGeometryEffectPipeline.controlPoints(for:face:)` concatenates provider results once in Face→Chin→Eye→Eyebrow→Nose→Mouth order.

Decision index used by this plan:
- D-09 exact 44 unique fields in 9/14/7/6/8 domain order.
- D-10 exact 13.45/44/`1 ÷ 13.45`/1 arithmetic with preserved signs.
- D-11 at most 44 monotone removals, no re-entry, alternate bound, or second conflict pass.
- D-12 late eyebrow loss, pair/per-side, reused, mixed-sign, and final strength/emission/metric/dispatch equality.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 52-02-01: Freeze the exact final 44-field inventory and arithmetic</name>
  <files>BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift, BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift</files>
  <action>Promote the existing Phase 50 44-row inventory from provisional vocabulary to the final D-09/D-10 oracle without changing production arithmetic. Assert cardinality and uniqueness before values; preserve stable 9 face/chin, 14 eye, 7 eyebrow, 6 nose, and 8 mouth ordering; assert exact domain subtotals `3.35`, `4.10`, `1.75`, `1.80`, `2.45`, exact total `13.45`, count and weakened count `44`, one reciprocal scale `1 / 13.45`, final magnitude total `1` within the established tight tolerance, one fixed warning, and every signed polarity. Add below-threshold and exact-threshold adjacency rows to prove no warning/scale at or below 1 and one weakening only just above 1. Keep the exact seven final eyebrow caps from Plan 01 and do not add a brow-specific weakening path, metric, warning, or alternate inventory.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --disable-sandbox --jobs 1 --filter BeautyEffectsTests.GeometryConflictResolverTests &amp;&amp; swift test --package-path BeautySDK --disable-sandbox --jobs 1 --filter BeautyEffectsTests.CombinedEffectSafetyTests &amp;&amp; git diff --check</automated>
  </verify>
  <done>The unique 44-field order, five subtotals, 13.45 total, 44 count, reciprocal scale, final total, threshold adjacency, warning, and signed polarity all pass as the final oracle.</done>
</task>

<task type="auto">
  <name>Task 52-02-02: Prove monotone removal and exact final emission/dispatch equality</name>
  <files>BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyGeometryEffectPipelineTests.swift</files>
  <action>Extend the final-mask and pipeline suites with D-11/D-12 cases for late provider-empty loss of each eyebrow row, pair-only spacing loss with per-side continuation, single-side retention, missing interior apex, reused support, mixed positive/negative signed domains, and repeated resolution. Assert source-level exactly one `0..<44` loop and reject alternate convergence bounds or a second `GeometryConflictResolver` pass. For every case, derive expected work from the 44-name inventory and require removed fields never re-enter; reused eligible eyebrow values are exactly half-strength once; each final nonzero effective field has one nonempty same-named provider array; each zero field has no named work; final domain activity, total, count, weakened count, scale, warning, aggregate geometry point count, stable seven-name eyebrow concatenation, and Face→Chin→Eye→Eyebrow→Nose→Mouth control-point dispatch agree exactly. Repeat with different request order to prove no hidden retained-mask state.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --disable-sandbox --jobs 1 --filter BeautyEffectsTests.CombinedEffectSafetyTests &amp;&amp; swift test --package-path BeautySDK --disable-sandbox --jobs 1 --filter BeautyEffectsTests.BeautyGeometryEffectPipelineTests &amp;&amp; test "$(rg -n 'for _ in 0\\.\\.<44' BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift | wc -l | tr -d ' ')" -eq 1 &amp;&amp; git diff --check</automated>
  </verify>
  <done>All late-loss, pair/per-side, reused, mixed-sign, repeated-order, final-mask, named-emission, point-count, metric, and exactly-once unified-dispatch assertions pass with one bounded monotone loop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Forty-four effective strengths → conflict resolver | Signed magnitudes and domain inventories can be omitted, duplicated, reordered, or weakened inconsistently. |
| Conflict-scaled strengths → provider sanitization | Provider-empty work can re-enter or contaminate totals if the scaled result replaces the unscaled retained baseline. |
| Final retained mask → named emissions/metrics | Aggregate evidence can disagree with field-level provider work. |
| Named emissions → unified warp dispatch | Provider or eyebrow field order can duplicate, drop, or reorder control points. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-52-09 | Tampering | 44-field inventory/arithmetic | mitigate | Cardinality/uniqueness/order, exact domain subtotals, total, count, reciprocal scale, final total, and sign assertions. |
| T-52-10 | Tampering | retained-baseline convergence | mitigate | Exactly one `0..<44` loop, unscaled monotone mask, threshold adjacency, repeated resolution, and no second pass. |
| T-52-11 | Tampering | provider-empty accounting | mitigate | Late-loss/pair/per-side/reuse cases require final strengths, totals, counts, warnings, metrics, and named arrays to share one mask. |
| T-52-12 | Tampering | emission/dispatch ordering | mitigate | Stable seven-name eyebrow order and exactly-once Face→Chin→Eye→Eyebrow→Nose→Mouth concatenation assertions. |
</threat_model>

<verification>
- Run conflict, combined-safety, and pipeline suites after each task and together at wave completion.
- Confirm the source contains exactly one `0..<44` convergence loop and the tests contain one final 44-name oracle.
- Confirm every spec-less ordering edge is explicit: stable named eyebrow emission order and unified provider dispatch order.
- Keep all product-owner statuses unchanged; a passing arithmetic oracle alone never authorizes promotion.
</verification>

<success_criteria>
The exact final 44-field convergence contract is executable and final effective strengths, arithmetic, named work, metrics, point count, and unified dispatch agree under every required removal and reuse case.
</success_criteria>

<output>
Create `.planning/phases/52-eyebrow-safety-and-branch-closeout/52-02-SUMMARY.md` when done.
</output>
