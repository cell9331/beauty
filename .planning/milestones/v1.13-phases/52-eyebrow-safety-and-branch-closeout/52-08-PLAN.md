---
phase: 52-eyebrow-safety-and-branch-closeout
plan: 08
type: execute
wave: 8
depends_on:
  - "52-07"
files_modified:
  - .planning/phases/52-eyebrow-safety-and-branch-closeout/check_eyebrow_safety_boundaries.py
  - .planning/phases/52-eyebrow-safety-and-branch-closeout/52-EYEBROW-SAFETY-EVIDENCE.md
  - .planning/phases/52-eyebrow-safety-and-branch-closeout/52-SECURITY.md
  - .planning/phases/52-eyebrow-safety-and-branch-closeout/52-VALIDATION.md
autonomous: true
gap_closure: true
requirements:
  - SAFE-01
  - SAFE-02
  - SAFE-03
  - DOC-01
must_haves:
  truths:
    - "The corrected focused suites, full SwiftPM suite, and unchanged Demo simulator build/test all pass before the independent post-execution review boundary."
    - "The fail-closed checker treats the existing independent gaps-found report as pending re-verification, rejects executor-authored passed status, and preserves exactly 130/130 self-tests and 35 live checks."
    - "Evidence, validation, and ASVS L1 security records contain only command-derived counts, cover all twenty-three Phase 52 tasks, and explicitly leave both independent code review and final verification pending."
    - "Wave 8 neither invokes the Phase 52 code-review workflow nor writes 52-REVIEW.md; the independent execute:post review exclusively owns that artifact."
  artifacts:
    - path: ".planning/phases/52-eyebrow-safety-and-branch-closeout/check_eyebrow_safety_boundaries.py"
      provides: "Fail-closed pending-independent-verification planning gate"
      contains: "pending-independent"
    - path: ".planning/phases/52-eyebrow-safety-and-branch-closeout/52-EYEBROW-SAFETY-EVIDENCE.md"
      provides: "Command-derived WR closure and regression inputs for independent review"
      contains: "WR-01"
    - path: ".planning/phases/52-eyebrow-safety-and-branch-closeout/52-VALIDATION.md"
      provides: "Twenty-three-task Nyquist matrix with all nine gap-closure rows and exact command expectations"
      contains: "52-10-02"
  key_links:
    - from: ".planning/phases/52-eyebrow-safety-and-branch-closeout/check_eyebrow_safety_boundaries.py"
      to: ".planning/phases/52-eyebrow-safety-and-branch-closeout/52-VERIFICATION.md"
      via: "The planning gate requires an independent gaps-found report to remain pending until an independent verifier reruns"
      pattern: "status:\\s*gaps_found"
    - from: ".planning/phases/52-eyebrow-safety-and-branch-closeout/52-VALIDATION.md"
      to: ".planning/phases/52-eyebrow-safety-and-branch-closeout/52-EYEBROW-SAFETY-EVIDENCE.md"
      via: "All twenty-three Nyquist rows cite exact commands/results or explicit pending evidence from their owning wave"
      pattern: "23/23"
---

<objective>
Produce fresh executable, security, and twenty-three-task Nyquist inputs for an independently owned post-execution review, while adapting the fail-closed gate for the required independent re-verification handoff per D-13 and D-14.

Purpose: Ensure gap closure is based on actual tests, builds, security checks, and complete Nyquist coverage without letting the executor author its own clean review or final verification.
Output: Passing regression evidence, unchanged Demo proof, a 130/130 self-tested checker, refreshed validation/security records, and an explicit stop-before-review handoff.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@SECURITY.md
@RELIABILITY.md
@QUALITY_SCORE.md
@.planning/phases/52-eyebrow-safety-and-branch-closeout/52-CONTEXT.md
@.planning/phases/52-eyebrow-safety-and-branch-closeout/52-RESEARCH.md
@.planning/phases/52-eyebrow-safety-and-branch-closeout/52-VERIFICATION.md
@.planning/phases/52-eyebrow-safety-and-branch-closeout/52-REVIEW.md
@.planning/phases/52-eyebrow-safety-and-branch-closeout/52-EYEBROW-SAFETY-EVIDENCE.md
@.planning/phases/52-eyebrow-safety-and-branch-closeout/52-VALIDATION.md
@.planning/phases/52-eyebrow-safety-and-branch-closeout/52-SECURITY.md
@.planning/phases/52-eyebrow-safety-and-branch-closeout/check_eyebrow_safety_boundaries.py
@.planning/phases/52-eyebrow-safety-and-branch-closeout/52-07-SUMMARY.md
</context>

## Artifacts this phase produces

- Updated planning evidence, validation, security, and checker artifacts only; `52-REVIEW.md` is read-only and unchanged in this wave.
- No new SDK product, public symbol, UI file, asset, screenshot, user-visible copy, or Xcode UI setting.

<execution_gate>
Wave 8 is the last executor wave before independent code review. A normal all-waves invocation MUST stop after `52-08-SUMMARY.md` is committed. The execute:post capability, or an explicit `$gsd-code-review 52`, must then run in a separate `gsd-code-reviewer` context and exclusively own `.planning/phases/52-eyebrow-safety-and-branch-closeout/52-REVIEW.md`. Do not dispatch Wave 9 or Wave 10 in the same uninterrupted execute-phase run, and do not let the Plan 08 executor invoke, edit, prewrite, normalize, or mark that review clean.
</execution_gate>

<tasks>

<task type="auto">
  <name>Task 1: Re-run the full evidence ladder and harden the pending-verification gate</name>
  <files>.planning/phases/52-eyebrow-safety-and-branch-closeout/check_eyebrow_safety_boundaries.py</files>
  <read_first>
    <file>.planning/phases/52-eyebrow-safety-and-branch-closeout/check_eyebrow_safety_boundaries.py</file>
    <file>.planning/phases/52-eyebrow-safety-and-branch-closeout/52-VERIFICATION.md</file>
    <file>.planning/phases/52-eyebrow-safety-and-branch-closeout/52-VALIDATION.md</file>
    <file>.planning/phases/52-eyebrow-safety-and-branch-closeout/52-07-SUMMARY.md</file>
    <file>AGENTS.md</file>
  </read_first>
  <action>
First run the corrected SAFE-01 provider suite (14/14), SAFE-02 degradation suite (51/51), combined suite (17/17), and the remaining Phase 52 focused suites at their unchanged exact counts: caps 7/7, resolver 27/27, conflict resolver 14/14, pipeline 4/4, and facade 20 executed with its existing one conditional skip. Then run the full `BeautySDK` suite and require 450 executed tests with six documented conditional skips and zero failures; because Task 1 of Plan 07 deliberately extended existing methods instead of adding methods, any count drift is a failure to investigate rather than a number to rewrite. Discover an installed iOS Simulator and run the unchanged `BeautyDemo` scheme build and test with an explicit iOS Simulator destination, requiring `** BUILD SUCCEEDED **` and `** TEST SUCCEEDED **`. After those commands pass, revise only the checker's final planning gate and its paired self-test fixtures: a legitimate gap-closure handoff requires the existing independent `52-VERIFICATION.md` to remain exactly `status: gaps_found` with an independent verifier attribution, renders that as `verification=pending-independent`, rejects missing/unclassified/failed status, and specifically rejects an executor-written `status: passed` before independent re-verification. The same final gate must require `52-VALIDATION.md` to enumerate exactly the twenty-three Phase 52 task IDs and carry a `23/23` sign-off; the old fourteen-task cardinality and duplicate/missing task IDs fail closed. Before promotion, Wave 9/10 rows must remain pending; `--allow-promotion` later requires all twenty-three rows green from their owning command evidence. Preserve fail-closed D-13 behavior, exact 35-check live cardinality, exact 130/130 self-test cardinality by replacing—not appending—the old verification/validation fixture cases, high-severity blocking, four requirement IDs, ten planned Phase 52 plans, and no automatic rewrite of the verifier artifact. Run the self-test now; Task 2 runs the non-promotion live checker only after it has replaced the stale fourteen-row validation matrix. Defer `--allow-promotion` until planning owners are synchronized in Plan 10. Do not edit `52-VERIFICATION.md`.
  </action>
  <verify>
    <automated>python3 .planning/phases/52-eyebrow-safety-and-branch-closeout/check_eyebrow_safety_boundaries.py --self-test &amp;&amp; swift test --package-path BeautySDK &amp;&amp; xcrun simctl list devices available | awk '/^-- iOS 26\.5 --$/{runtime=1; next} /^-- /{runtime=0} runtime &amp;&amp; /iPhone 17 Pro \([0-9A-F-]+\) \((Booted|Shutdown)\)$/{found=1} END{exit !found}' &amp;&amp; xcodebuild -quiet -project BeautyDemo/BeautyDemo.xcodeproj -scheme BeautyDemo -destination 'platform=iOS Simulator,name=iPhone 17 Pro,OS=26.5' build &amp;&amp; xcodebuild -quiet -project BeautyDemo/BeautyDemo.xcodeproj -scheme BeautyDemo -destination 'platform=iOS Simulator,name=iPhone 17 Pro,OS=26.5' test &amp;&amp; test -z "$(git diff --name-only -- BeautyDemo)"</automated>
  </verify>
  <acceptance_criteria>
    - Checker self-test reports exactly 130/130 with zero failures.
    - The focused suite counts are exactly 7, 27, 14, 51, 14, 17, 4, and 20 with only the documented facade conditional skip.
    - Full SwiftPM reports 450 executed, six documented conditional skips, and zero failures.
    - `xcrun simctl list devices available` proves that `iPhone 17 Pro` on iOS 26.5 is available before either Xcode command runs; the explicit-destination Demo build and test both exit 0.
    - If that named runtime is unavailable, execution records the reproducible discovery blocker and does not fabricate a Demo pass.
    - The checker rejects executor-authored `status: passed`, preserves 35 live checks, and leaves `52-VERIFICATION.md` byte-for-byte untouched.
    - The checker rejects the stale fourteen-task validation claim and requires exactly twenty-three unique task rows plus `23/23`, while preserving 130/130 self-tests and 35 live checks.
    - `git diff --name-only -- BeautyDemo` is empty.
  </acceptance_criteria>
  <done>The executable ladder is green and the checker can truthfully hand off to an independent verifier without weakening its gate.</done>
</task>

<task type="auto">
  <name>Task 2: Prepare independent-review evidence, security, and complete Nyquist inputs</name>
  <files>.planning/phases/52-eyebrow-safety-and-branch-closeout/52-EYEBROW-SAFETY-EVIDENCE.md, .planning/phases/52-eyebrow-safety-and-branch-closeout/52-SECURITY.md, .planning/phases/52-eyebrow-safety-and-branch-closeout/52-VALIDATION.md</files>
  <read_first>
    <file>.planning/phases/52-eyebrow-safety-and-branch-closeout/52-REVIEW.md</file>
    <file>.planning/phases/52-eyebrow-safety-and-branch-closeout/52-EYEBROW-SAFETY-EVIDENCE.md</file>
    <file>.planning/phases/52-eyebrow-safety-and-branch-closeout/52-SECURITY.md</file>
    <file>.planning/phases/52-eyebrow-safety-and-branch-closeout/52-VALIDATION.md</file>
    <file>.planning/phases/52-eyebrow-safety-and-branch-closeout/52-VERIFICATION.md</file>
    <file>BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift</file>
    <file>BeautySDK/Tests/BeautyEffectsTests/EyebrowSafetyFixtures.swift</file>
    <file>BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift</file>
    <file>BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift</file>
  </read_first>
  <action>
Refresh `52-EYEBROW-SAFETY-EVIDENCE.md` with Task 1's exact command outputs and source-line anchors showing WR-01 production-adapter validation/canonical ordering, WR-02 separate resolver-entry and actual provider-work-entry signals followed by cancellation-aware discard/safe completion, and WR-03 production-loop retained-mask transitions from a strictly nonzero scaled value through monotone removal and no re-entry. Update `52-SECURITY.md` with explicit ASVS L1 input-validation, request-isolation, aggregate-redaction, internal-observation-seam, and fail-closed-gate evidence; any unresolved high-severity item remains blocking.

Refresh the exact 23/23 Phase 52 task matrix already established in `52-VALIDATION.md`. Preserve the fourteen rows for Plans 01–06 and the nine explicit gap rows `52-07-01`, `52-07-02`, `52-07-03`, `52-08-01`, `52-08-02`, `52-09-01`, `52-09-02`, `52-10-01`, and `52-10-02`; do not collapse it back to the stale fourteen-row claim. For all twenty-three IDs, copy the concrete automated command from the owning plan's `<verify>` block into one same-ID `G52-*` command-registry entry and give that entry a measurable expected result. Every task row must map exactly once to its same-ID registry key; shared subcommands may repeat, but prose-only suite labels are forbidden. Preserve the owning plans' exact count/status assertions, including seven cap declarations, seven unique provider-cap references, one `0..<44` loop, fourteen original validation rows at their historical gate, exact output/gallery/visual counts, four requirements, six original roadmap plans, and all later 14/1/1/130/450/23/35 gap-closure counts. Every row must retain its wave, requirement IDs, applicable threat IDs, exact automated command key, exact expected result, and current evidence status. Use these exact gap command/result contracts:

- `52-07-01`: `cd BeautySDK &amp;&amp; swift test --filter EyebrowWarpProviderTests` → exactly 14 executed, zero failures.
- `52-07-02`: `cd BeautySDK &amp;&amp; swift test --filter MissingLandmarkDegradationTests/testSAFE02ParallelCompletionOrderAndInterruptedWorkCannotLeakRequestState` → exactly one targeted test, zero failures, with resolver and provider-entry assertions.
- `52-07-03`: `cd BeautySDK &amp;&amp; swift test --filter CombinedEffectSafetyTests/testSAFE02EveryEyebrowRowIsRemovedMonotonicallyWhenSharedScaleMakesProviderEmpty` → exactly one targeted test covering all seven rows, zero failures, with production transition-trace assertions.
- `52-08-01`: checker self-test, root-relative full SwiftPM, `xcrun simctl list devices available` proof for `iPhone 17 Pro` on iOS 26.5, then the explicit-destination `xcodebuild -quiet` build and test commands, followed by the no-UI diff gate → checker exactly 130/130, SwiftPM exactly 450 executed with six documented conditional skips and zero failures, simulator discovery succeeds, both Xcode commands exit 0, and the `BeautyDemo` diff is empty. If discovery cannot prove the named runtime, record that reproducible blocker and do not claim build/test success.
- `52-08-02`: `python3 -c '...assert WR-01/02/03, threats_open: 0, exact 23-row ID set, 23/23, and gaps_found...' &amp;&amp; git diff --exit-code -- .planning/phases/52-eyebrow-safety-and-branch-closeout/52-REVIEW.md &amp;&amp; python3 .planning/phases/52-eyebrow-safety-and-branch-closeout/check_eyebrow_safety_boundaries.py` (copy the complete automated verification command immediately below into the validation row) → evidence present, ASVS L1 high-severity open count zero, validation exactly 23/23, verifier still `gaps_found`, review unchanged, and non-promotion live checker passes with Wave 9/10 pending.
- `52-09-01`: both `--check-security` and `--check-reliability` → both owner modes pass, gated by a fresh independent clean review.
- `52-09-02`: all seven architecture/design/security/reliability/product/quality/examples modes → all seven pass, gated by the same fresh independent clean review.
- `52-10-01`: `roadmap.analyze` plus ten-plan/pending-verification assertions → roadmap analysis passes, exactly ten Phase 52 plans are listed, and verifier status remains `gaps_found`.
- `52-10-02`: checker `--self-test`, then `--allow-promotion`, roadmap analysis, and no-UI diff → exactly 130/130, exactly 35/35 with `verification=pending-independent`, roadmap passes, and no `BeautyDemo` diff.

Rows for completed Tasks 07 and 08 may be marked green only from their owning command evidence; rows for gated Waves 9–10 must remain pending until their owning waves execute. Update frontmatter/sign-off text to reject the old fourteen-task cardinality and require exactly 23/23 rows before final gate promotion. State plainly that independent code review and independent re-verification are pending, and `52-VERIFICATION.md` remains the independent gaps-found report. This task MUST NOT invoke `$gsd-code-review 52`, run any standard Phase 52 review workflow, edit or write `52-REVIEW.md`, claim 0/0/0/0 review findings, claim independent verification, or run a milestone audit.
  </action>
  <verify>
    <automated>python3 -c 'from pathlib import Path; import re; base=Path(".planning/phases/52-eyebrow-safety-and-branch-closeout"); e=(base/"52-EYEBROW-SAFETY-EVIDENCE.md").read_text(); s=(base/"52-SECURITY.md").read_text(); n=(base/"52-VALIDATION.md").read_text(); v=(base/"52-VERIFICATION.md").read_text(); assert all(x in e for x in ("WR-01", "WR-02", "WR-03", "pending")); assert "threats_open: 0" in s; ids={f"52-{p:02d}-{t:02d}" for p,count in ((1,3),(2,2),(3,3),(4,2),(5,2),(6,2),(7,3),(8,2),(9,2),(10,2)) for t in range(1,count+1)}; row_lines=re.findall(r"^\| (52-\d{2}-\d{2}) \|([^\n]+)$",n,re.M); rows=[task for task,_ in row_lines]; assert len(rows)==23 and set(rows)==ids; assert all(rest.count(f"`G{task}`")==1 for task,rest in row_lines); entries=re.findall(r"^`G(52-\d{2}-\d{2})`\n\n```bash\n(.*?)\n```\n\n\*\*Expected result:\*\* ([^\n]+)$",n,re.M|re.S); assert len(entries)==23 and {task for task,_,_ in entries}==ids; assert all(command.strip() and expected.strip() for _,command,expected in entries); assert "xcrun simctl list devices available" in dict((task,command) for task,command,_ in entries)["52-08-01"]; assert all(token in dict((task,command) for task,command,_ in entries)["52-08-01"] for token in ("platform=iOS Simulator,name=iPhone 17 Pro,OS=26.5", " build", " test", "git diff --name-only -- BeautyDemo")); assert "23/23" in n and "execution are complete for all fourteen" not in n.lower() and "all fourteen task rows are green" not in n.lower(); assert "status: gaps_found" in v' &amp;&amp; git diff --exit-code -- .planning/phases/52-eyebrow-safety-and-branch-closeout/52-REVIEW.md &amp;&amp; python3 .planning/phases/52-eyebrow-safety-and-branch-closeout/check_eyebrow_safety_boundaries.py</automated>
  </verify>
  <acceptance_criteria>
    - Evidence cites the exact passing focused/full/Demo results and concrete corrected source lines for resolver entry, actual provider entry, and retained-mask transitions.
    - Validation contains exactly twenty-three unique task rows: fourteen preserved rows plus all nine Tasks 07-01 through 10-02, each mapping once to a same-ID registry entry with a concrete copy-pasteable command, measurable expected result, wave, requirements, threat IDs, and evidence status.
    - `52-08-01` includes the named-runtime discovery precheck, both required explicit-simulator `xcodebuild -quiet` commands, and the no-UI diff gate; unavailable runtime evidence remains a reproducible blocker, never a fabricated pass.
    - Validation rejects stale fourteen-task sign-off; completed Wave 7/8 rows are command-derived and gated Wave 9/10 rows remain pending.
    - The security artifact has no unresolved high-severity issue and records aggregate-only, fail-closed behavior.
    - DOC-01 truthfully distinguishes executable gap evidence from pending independent code review and final verification.
    - `52-REVIEW.md` is unchanged; no code-review workflow, milestone audit, or verifier-status rewrite occurs.
  </acceptance_criteria>
  <done>Fresh evidence, security, and complete 23/23 Nyquist inputs are ready for the independently owned post-execution review.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 contribution

This plan verifies ASVS L1-style input validation, safe cancellation, diagnostic minimization, build integrity, and fail-closed promotion evidence. Any high-severity security or review finding blocks all owner and promotion work.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test/build output → evidence/validation documents | Command output is the only authority for counts and statuses, including all twenty-three task rows. |
| Executor → independent verifier artifact | The executor may read the verifier report but may not promote its status to passed. |
| Wave 8 executor → independent code-review artifact | The executor must not invoke the review workflow or modify `52-REVIEW.md`; the post-execution reviewer owns it exclusively. |
| Checker fixtures → live repository gate | Self-test behavior must match live fail-closed semantics and exact cardinality. |
| SDK diff → Demo application | The SDK safety closure must not mutate UI paths, assets, settings, or copy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-08-01 | Repudiation | Evidence/Nyquist status | mitigate | Require command-derived counts and exactly 23/23 task rows; keep Wave 9/10 rows pending until their owning execution. |
| T-52-08-02 | Elevation of Privilege | Verification planning gate | mitigate | Accept only the exact independent gaps-found pending state and reject executor-authored passed status. |
| T-52-08-03 | Tampering | Checker self-tests | mitigate | Preserve exact 130/130 and 35-check cardinality by replacing paired fixtures, then run self-test. |
| T-52-08-04 | Information Disclosure | Security/review evidence | mitigate | Cite aggregate diagnostics and field identifiers only; never record raw landmark coordinates from a request. |
| T-52-08-05 | Tampering | Demo/UI scope | mitigate | Run unchanged simulator build/test and require an empty `BeautyDemo` path diff. |
| T-52-08-06 | Elevation of Privilege | Independent review ownership | mitigate | Stop after Wave 8 and prohibit the executor from invoking or editing the standard review; resume only after `gsd-code-reviewer` produces the fresh artifact. |
</threat_model>

<verification>
- Checker self-test: exactly 130/130.
- Phase 52 focused suite counts: 7, 27, 14, 51, 14, 17, 4, and 20 with only the documented conditional skip.
- Full `BeautySDK`: 450 executed, six documented conditional skips, zero failures.
- Demo: explicit simulator destination, exact `** BUILD SUCCEEDED **` and `** TEST SUCCEEDED **`.
- Nyquist: exactly 23/23 task rows, with all nine gap tasks explicit and Waves 9–10 pending.
- Review: existing `52-REVIEW.md` remains unchanged in Wave 8; independent post-execution review is pending.
- `52-VERIFICATION.md`: still `status: gaps_found`, independently owned, pending rerun.
- `git diff --name-only -- BeautyDemo`: empty.
</verification>

<success_criteria>
- WR-01 through WR-03 have fresh source and command inputs ready for independent review.
- Checker semantics remain fail closed while permitting an honest gap-closure-to-verifier handoff.
- Evidence, 23/23 validation, and security agree on exact results and pending independent review/verification.
- No UI or milestone-audit scope is introduced.
</success_criteria>

<output>
Create `.planning/phases/52-eyebrow-safety-and-branch-closeout/52-08-SUMMARY.md` when done.
</output>
