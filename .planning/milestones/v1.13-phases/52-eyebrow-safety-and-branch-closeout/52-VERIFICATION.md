---
phase: 52-eyebrow-safety-and-branch-closeout
verified: 2026-07-27T10:17:04Z
status: passed
score: 16/16 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 12/16
  gaps_closed:
    - "The seven-field cap/dead-zone/direction/radius oracle now exercises production-adapter-validated, canonically ordered observed eyebrow support."
    - "Interrupted eyebrow work now enters the real resolver and provider path before cancellation and remains request-local under the strict cooperative-pool setting."
    - "Late provider-empty eyebrow removal now runs through the production 44-pass retained-mask convergence loop with a strictly nonzero pre-removal value, monotone removal, and no re-entry."
    - "The post-fix review, exact 23-task ledger, 35-check executor gate, routed owners, and planning handoff now agree."
  gaps_remaining: []
  regressions: []
human_verification: []
---

# Phase 52: Eyebrow Safety and Branch Closeout Verification Report

**Phase Goal:** Freeze conservative behavior and promote exactly the seven eyebrow rows only after runtime, output, privacy, and owner evidence agree.
**Verified:** 2026-07-27T10:17:04Z
**Status:** passed
**Re-verification:** Yes — independent re-verification after Plans 52-07 through 52-10 closed the prior four blocker concerns.

## Verdict

Phase 52 achieves its goal. The three previously defective proof paths are now
production-faithful and executable: fixtures enter the real adapter contract,
the interruption case reaches provider-owned work without blocking a Swift
cooperative-executor worker, and all seven late provider-empty rows are traced
through the real retained-mask loop. Fresh focused, full-package, strict-output,
privacy, simulator, review, validation, promotion, and owner checks agree.

This verdict promotes only the exact SDK-core eyebrow scope. It does not run or
claim the v1.13 milestone audit, archive, tag, cleanup, physical-device parity,
commercial naturalness, optimized performance, packaging, shipping, launch, or
release readiness.

## Goal Achievement

### Observable Truths

The prior verification's sixteen truths remain the re-verification contract.
The four formerly failed truths received full existence/substance/wiring/data-
flow checks; the twelve formerly passing truths received regression checks.

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Seven exact final caps, the exact ULP dead zone, correct direction semantics, bounded radii, and provider maxima are proved through production-valid support. | ✓ VERIFIED | `BeautySafetyCaps.swift:37-44` owns seven exact `0.25` values. `EyebrowSafetyFixtures.validatedTrace` calls the production adapter and asserts inner-to-outer center ordering, inclusive chord bounds, and vertical span. Fresh strict provider suite: 14/14. |
| 2 | Fresh, reused, stale, no-face, missing, malformed, provider-empty, sign-transition, and valid-invalid-valid calls are stateless. | ✓ VERIFIED | Descriptor-driven resolver/degradation cases cover seven rows, both signed directions, reused `0.125`, stale/no-face zeroing, and recovery. Fresh degradation suite: 51/51. |
| 3 | Parallel and interrupted work cannot leak request-local eyebrow support or results. | ✓ VERIFIED | The test waits for resolver and provider-owned entry, runs synchronous work on the dedicated `beauty.phase52.interrupted-request` queue, cancels, releases, joins the request, and checks 28 sibling plus seven subsequent identities. Targeted and complete 51-test runs pass with `LIBDISPATCH_COOPERATIVE_POOL_STRICT=1`. |
| 4 | Side/pair/chord/apex failures remove only dependent eyebrow work while safe eyebrow and unrelated domains continue with aggregate-only diagnostics. | ✓ VERIFIED | Provider guards and resolver sanitization preserve per-side work, require a valid pair only for whole spacing, and require an apex only for peak. Resolver/facade suites verify safe face/eye/nose/mouth/color/filter continuation and fixed aggregate diagnostics. |
| 5 | The final geometry inventory is exactly 44 unique fields in stable 9/14/7/6/8 domain order. | ✓ VERIFIED | `BeautyEffectResolver.geometryFieldNames` contains the stable 44-name list; `GeometryConflictResolverTests` verifies cardinality, uniqueness, and domain order. Fresh conflict suite: 14/14. |
| 6 | Final-cap arithmetic is exactly 3.35 + 4.10 + 1.75 + 1.80 + 2.45 = 13.45, with count 44, one `1 / 13.45` scale, final total 1, and preserved signs. | ✓ VERIFIED | Production conflict code scales every geometry field once; focused conflict and combined suites assert exact subtotals, total, count, reciprocal scale, final magnitude, warning, and signs. |
| 7 | Late provider-empty eyebrow work is removed through the production retained-baseline loop with at most 44 monotone removals, no re-entry, and no second scaling pass. | ✓ VERIFIED | `BeautyEffectResolver.resolve(...onRetainedMaskIteration:)` observes the real `0..<44` loop. The seven-row test proves pre-scale emission, a strictly nonzero scaled value at or below the provider dead zone, exactly one removal, no later retention/re-removal, final exclusion, and an identical repeated fixed point. |
| 8 | Final effective strengths, totals, counts, warnings, metrics, named emissions, point count, and unified Face→Chin→Eye→Eyebrow→Nose→Mouth dispatch agree. | ✓ VERIFIED | Final-mask/provider equality tests and `BeautyGeometryEffectPipeline` exact concatenation/order tests pass; the provider appears once between eye and nose. |
| 9 | The Phase 52 checker fails closed on command, path, source, public/SPI, privacy, dependency, artifact, status, concurrency, interruption, and lifecycle ambiguity. | ✓ VERIFIED | Fresh `--self-test` reports exactly 130/130. Pre-verdict `--allow-promotion` reports exactly 35/35 with `verification=pending-independent`. |
| 10 | Fresh runtime, strict output, gallery, containment, and fourteen-image evidence agree with the frozen-cap output. | ✓ VERIFIED | Fresh full SwiftPM passes 450 tests with six documented skips. A fresh strict decode of the hash-matched 144-file output set passes 72/72 portrait, 13/13 visibility, 6/6 direction, 21/21 distinctions, 40/40 direct comparisons, and 13/13 separate no-face comparisons. Output/gallery remain 144/144 ignored and untracked; the recorded fourteen-file original-detail review is complete. |
| 11 | Standard review is clean, exact 23-task Nyquist is complete, ASVS L1 has zero open threats, and no unresolved evidence assumption remains. | ✓ VERIFIED | `52-REVIEW.md` is independently authored, reviews 25 files, is newer than the final repair, and records clean 0/0/0/0. Validation has exactly 23 unique green rows and 23 same-ID commands. `52-SECURITY.md` records `threats_open: 0`. |
| 12 | Exactly seven named eyebrow rows and branch `眉毛` are implemented atomically at SDK-core scope. | ✓ VERIFIED | Ledger rows 113-119 are exactly `上下`, `粗细`, `长短`, `间距`, `眉头间距`, `倾斜`, and `眉峰`; the matrix and both branch owners mark only the SDK-core eyebrow branch implemented. |
| 13 | Each promoted row cites the Phase 49→50→51→52 chain while later milestones and UI/device/commercial/release/lifecycle claims remain excluded. | ✓ VERIFIED | Every ledger row carries the four-phase chain. The branch README and matrix preserve v1.14-v1.16, SwiftUI/Demo, device, commercial, performance, packaging, shipping, launch, audit, archive, tag, and cleanup nonclaims. |
| 14 | Example and routed root owners agree on exact 72/13/144 vocabulary, caps, lifecycle, convergence, privacy, product scope, and limitations. | ✓ VERIFIED | All seven direct routed owner modes and aggregate planning/owner checks pass; source hashes in `52-EYEBROW-SAFETY-EVIDENCE.md` match the current package, provider, resolver, pipeline, renderer, strict helper, and gallery publisher. |
| 15 | Requirements, roadmap, project, state, PLANS, validation, evidence, review, and verification agree one-to-one. | ✓ VERIFIED | All ten plans and summaries exist; all four Phase 52 requirements are implemented; the clean review and 23/23 validation are current. Planning owners correctly preserved `verifying` only until this independently owned report replaced the old verdict. |
| 16 | Phase completion hands off only to the independent v1.13 audit and does not claim audit/archive/tag/cleanup or broader readiness. | ✓ VERIFIED | No v1.13 audit artifact or tag exists. Lifecycle scans and D-16 owner text preserve the separate audit and all broader nonclaims. This verification does not start those workflows. |

**Score:** 16/16 truths verified

## Prior Gap Closure

| Prior Gap | Closure Evidence | Status |
| --- | --- | --- |
| WR-01 — reversed/direct and adapter-invalid fixture oracle | Shared fixtures call `BeautyFaceGeometryAdapter.validatedBrowTrace`; exact 0.08/0.50 and adjacent rejection cases, canonical endpoint ordering, malformed local failure, and 14 provider tests pass. | ✓ CLOSED |
| WR-02 — cancellation stopped before request work | Separate resolver/provider entry signals, a dedicated dispatch queue, bounded request-local barrier, cancellation after entry, complete joins, and sibling/subsequent identity assertions execute under the strict cooperative-pool environment. | ✓ CLOSED |
| WR-03 — manual convergence approximation and vacuous tolerance | The test calls the public internal resolver seam, records every real production iteration, asserts a positive pre-removal value with quarter-value accuracy, proves one removal/no re-entry, and compares the repeated trace/fixed point. | ✓ CLOSED |
| Evidence/owner disagreement | Fresh independent review is clean, validation is 23/23, the executor readiness gate is 35/35 pending-independent, and routed/planning owners point to this independent re-verification as the only remaining gate. | ✓ CLOSED |

## Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `BeautySafetyCaps.swift` | One final authority for seven caps | ✓ VERIFIED | Seven exact `0.25` constants; no provisional eyebrow marker. |
| `EyebrowWarpProvider.swift` | Seven substantive bounded named emissions | ✓ VERIFIED | Seven arrays, exact cap binding, ULP dead zone, finite/unit/radius guards, side/pair/chord/apex prerequisites, and stable concatenation. |
| `EyebrowSafetyFixtures.swift` | Production-faithful seven-row oracle | ✓ VERIFIED | Exactly seven descriptors; reusable traces are adapter-validated and canonical. |
| `EyebrowWarpProviderTests.swift` | Dead-zone, cap, direction, radius, and prerequisite evidence | ✓ VERIFIED | Consumes the shared fixture; fresh 14/14 pass. |
| `MissingLandmarkDegradationTests.swift` | Lifecycle, transition, concurrency, interruption evidence | ✓ VERIFIED | Substantive 51-test suite; real provider entry and strict-pool interruption case pass. |
| `GeometryConflictResolverTests.swift` | Exact inventory/arithmetic oracle | ✓ VERIFIED | Exact 44/13.45/count/scale/sign assertions; fresh 14/14 pass. |
| `CombinedEffectSafetyTests.swift` | Production monotone final-mask proof | ✓ VERIFIED | Calls the real resolver trace seam for all seven rows; fresh 17/17 pass. |
| `BeautyGeometryEffectPipelineTests.swift` | Exactly-once stable dispatch proof | ✓ VERIFIED | Exact seven-array concatenation and provider dispatch order; fresh 4/4 pass. |
| `BeautyEngineGeometryFacadeTests.swift` | Public extent, sibling, and redaction evidence | ✓ VERIFIED | Fresh 20 executed with one opt-in skip and zero failures. |
| `check_eyebrow_safety_boundaries.py` | Fail-closed boundary/governance checker | ✓ VERIFIED | Substantive 1,392-line checker; 130/130 self-tests and 35/35 pre-verdict gate. |
| `52-EYEBROW-SAFETY-EVIDENCE.md` | Runtime/output/visual/containment ledger | ✓ VERIFIED | Eight current hashes match; exact 72/13/144 and fourteen-image record retained. |
| `52-REVIEW.md` | Clean independent review | ✓ VERIFIED | Independent `gsd-code-reviewer`, 25 files, 0/0/0/0, `status: clean`. |
| `52-SECURITY.md` | ASVS L1 closure | ✓ VERIFIED | Complete register, `threats_open: 0`, no unresolved high finding. |
| `52-VALIDATION.md` | Exact complete Nyquist ledger | ✓ VERIFIED | 23 unique task rows, 23 same-ID command/result entries, all green. |
| Product/example/root/planning owner set | Exact bounded promotion and handoff | ✓ VERIFIED | Exact seven-row/branch transaction, aggregate evidence, package-only support, no-UI scope, and D-16 nonclaims agree. |

The generic artifact query reports formatting/case false negatives in older
plans (`validInvalidValid` casing and a historical `status: passed` expectation).
Manual source and executable checks above establish the live artifact status.

## Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `BeautySafetyCaps.swift` | `EyebrowWarpProvider.swift` | Seven named bindings | ✓ WIRED | Provider binds every named cap once and uses it for acceptance, formulas, and point validation. |
| Vision eyebrow regions | Package observation/adapter | One request-local mapping boundary | ✓ WIRED | Production capture reads actual left/right Vision regions; checker provenance and full-suite detector/adapter tests pass. |
| `EyebrowSafetyFixtures.swift` | `BeautyFaceGeometryAdapter` | `validatedBrowTrace` | ✓ WIRED | Direct call at fixture lines 127-136; the plan's generic link query misses the actual `Planning/` path. |
| Public eyebrow parameters | Resolver/provider | Normalization, caps, freshness, sanitization | ✓ WIRED | Same-named fields flow from public values to final effective strengths and provider emissions. |
| Cancellation test | Resolver/provider work | Resolver entry + provider callback + dedicated queue/barrier | ✓ WIRED | Both callbacks occur before cancellation; synchronous work completes request-locally and is joined. |
| Late-removal test | Production convergence loop | `onRetainedMaskIteration` | ✓ WIRED | Real resolver execution exposes only scalar values/stable names and proves all seven removals. |
| Final strengths | Named emissions/accounting | One retained mask | ✓ WIRED | Final providers, domains, warnings, metrics, point count, count, scale, and totals agree. |
| Named emissions | Unified pipeline | Stable provider concatenation | ✓ WIRED | Face→Chin→Eye→Eyebrow→Nose→Mouth appears exactly once. |
| Strict output | Gallery/evidence owners | Hash-matched helper and exact inventory | ✓ WIRED | Fresh strict decode passes; 144 output and 144 gallery files remain contained. |
| Safety/review/validation | Product/planning owners | Fail-closed checker and routed owner modes | ✓ WIRED | Exact seven-row scope, clean review, 23/23 validation, and D-16 handoff pass together. |

## Data-Flow Trace (Level 4)

| Artifact | Data | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| `VisionFaceDetector.swift` / observation support | Left/right eyebrow samples | Actual selected-face Vision eyebrow regions | Yes; package-only, request-local, bounded | ✓ FLOWING |
| `BeautyFaceGeometryAdapter.swift` | Semantic traces | Exactly-once mapped observed support | Yes when independently valid; invalid sides stay absent | ✓ FLOWING |
| `EyebrowWarpProvider.swift` | Seven named control-point arrays | Adapter-produced semantic support + final strengths | Yes for satisfied per-field prerequisites | ✓ FLOWING |
| `BeautyEffectResolver.swift` | Final retained strengths and aggregate accounting | Public parameters + request-local face geometry + provider sanitization + one conflict loop | Yes; removal-only fixed point | ✓ FLOWING |
| `BeautyGeometryEffectPipeline.swift` | Unified control points | Final effective strengths + selected request geometry | Yes; stable exactly-once dispatch | ✓ FLOWING |
| Public renderer outputs | Decoded `e6` images | Public `BeautySDK` facade through the unified warp | Yes; fresh strict 72/72 acceptance | ✓ FLOWING |

Raw eyebrow carriers remain `package` or module-internal and non-Codable.
Public results contain only scalar parameters/effective strengths, fixed reason
codes, and aggregate counters; no raw support, coordinate array, endpoint,
center, apex, provider object, or image byte payload crosses the facade.

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| WR-01 adapter-valid cap/dead-zone/provider proof | `LIBDISPATCH_COOPERATIVE_POOL_STRICT=1 swift test ... --filter EyebrowWarpProviderTests` | 14 executed, 0 failures | ✓ PASS |
| WR-02 in-flight request isolation | strict-pool targeted `testSAFE02ParallelCompletionOrderAndInterruptedWorkCannotLeakRequestState` | 1 executed, 0 failures | ✓ PASS |
| WR-03 production convergence | strict-pool targeted `testSAFE02EveryEyebrowRowIsRemovedMonotonicallyWhenSharedScaleMakesProviderEmpty` | 1 executed, 0 failures | ✓ PASS |
| Eight focused Phase 52 suites | strict-pool sequential filtered SwiftPM commands | 7/27/14/51/14/17/4/20, one facade opt-in skip, 0 failures | ✓ PASS |
| Full SwiftPM regression | `LIBDISPATCH_COOPERATIVE_POOL_STRICT=1 swift test --package-path BeautySDK --disable-sandbox --jobs 1 --skip-build -q` | 450 executed, 6 skipped, 0 failures | ✓ PASS |
| Strict decoded output | Phase 51 strict helper against current 144 outputs | 72/72 portrait; 13/13 visibility; 6/6 direction; 21/21 distinction; 40/40 direct; 13/13 no-face | ✓ PASS |
| Checker adversarial behavior | `python3 .../check_eyebrow_safety_boundaries.py --self-test` | 130/130 | ✓ PASS |
| Final executor readiness before verifier transition | `python3 .../check_eyebrow_safety_boundaries.py --allow-promotion` | 35/35; `verification=pending-independent`; Nyquist 23/23 | ✓ PASS |
| Demo simulator regression | explicit iPhone 17 Pro / iOS 26.5 `xcodebuild` build and test | Both exit 0 | ✓ PASS |
| Demo/UI scope | Phase-base-to-HEAD and working-tree diffs under `BeautyDemo` | No changed file | ✓ PASS |
| Diff hygiene | `git diff --check` before report | Exit 0 | ✓ PASS |

The final checker result is intentionally recorded before this verifier-owned
status transition. Its executor-only planning gate is designed to require the
old independent `gaps_found` report and therefore is not a post-verdict phase
acceptance command.

## Probe Execution

No `probe-*.sh` is declared by the ten plans or summaries, and no conventional
Phase 52 shell probe exists. The declared Python checker and strict output
helper were run independently as behavioral gates above.

## Requirements Coverage

Every requirement ID declared in all ten PLAN frontmatters was cross-referenced
against `.planning/REQUIREMENTS.md`. No Phase 52 requirement is orphaned.

| Requirement | Source Plans | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| SAFE-01 | 52-01, 52-02, 52-03, 52-05, 52-06, 52-07, 52-08, 52-09, 52-10 | Exact caps, dead zone, signed/positive-only direction, bounded radii | ✓ SATISFIED | Seven exact cap declarations/bindings, adapter-valid boundary fixtures, cap/resolver/provider tests, strict output, and fresh full regression pass. |
| SAFE-02 | 52-01, 52-02, 52-03, 52-05, 52-06, 52-07, 52-08, 52-09, 52-10 | Stateless degradation, safe siblings, request isolation, convergence | ✓ SATISFIED | Complete lifecycle matrix, real in-flight interruption case, per-field degradation, real-loop seven-row removal, exact final accounting, and strict-pool full regression pass. |
| SAFE-03 | 52-03, 52-05, 52-06, 52-07, 52-08, 52-09, 52-10 | Fail-closed privacy/public/dependency/artifact/active-source gates | ✓ SATISFIED | 130/130 checker self-tests, 35/35 pre-verdict gate, package-only raw carriers, aggregate diagnostics, unchanged Demo, clean review, and ASVS `threats_open: 0`. |
| DOC-01 | 52-03, 52-04, 52-05, 52-06, 52-08, 52-09, 52-10 | Exact seven-row and branch SDK-core promotion with nonclaims | ✓ SATISFIED | Exact seven ledger rows, branch implemented in all four owners, four-phase lineage, synchronized routed/planning owners, and preserved D-16 nonclaims. |

Later phases in the current milestone do not exist, so no failed or missing
Phase 52 item is deferred. v1.14-v1.16 goals are explicit future scope rather
than Phase 52 gaps.

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | ---: | --- | --- | --- |
| `EyebrowWarpProvider.swift` | multiple guards | `return []` | ℹ️ Info | Deliberate field-local fail-closed behavior; corresponding sanitization and sibling tests are wired. |
| `BeautyGeometryEffectPipeline.swift` | 23, 41 | historical `MVP` proxy comments | ℹ️ Info | Existing renderer implementation terminology; actual public-facade decoded output is substantive and freshly accepted. |

No unreferenced debt marker exists in the Phase 52 production/test/checker
scope. Historical marker words in planning verification prose and unrelated
quality/debt history do not flow into runtime or user-visible output. No stub,
orphaned Phase 52 artifact, hardcoded empty public data, public raw geometry,
network/cloud path, dependency drift, generated tracked artifact, or Demo/UI
implementation was found.

## Disconfirmation Pass

- **Partial requirement search:** SAFE-01's former adapter-bypass weakness is
  closed, not merely renamed: the shared fixture calls the production adapter,
  checks canonical endpoints, and exact boundary tests execute.
- **Misleading-test search:** the former cancellation sleep and manual
  convergence approximation are gone from the required proof paths. Both
  targeted tests now cross explicit production seams and pass in the strict
  cooperative-pool environment.
- **Error-path search:** malformed/absent side, pair-only loss, missing apex,
  provider-empty work, no face, stale/reused input, cancellation, and bounded
  barrier timeout cleanup all have executable coverage. The opt-in Apple Vision
  integration skips remain explicit environment-dependent tests; hash-matched
  public renderer output and non-opt-in facade/detector tests cover the phase
  goal without converting device parity into a claim.

## Human Verification Required

None for Phase 52 acceptance. The required mechanical original-detail review
of the baseline plus thirteen eyebrow outputs is already recorded one-to-one
and bound to current source hashes; fresh strict decoded evidence agrees. This
phase deliberately does not claim commercial naturalness or physical-device
parity, which remain future evidence scopes rather than hidden acceptance
requirements.

## Gaps Summary

No remaining Phase 52 gap. All four previous blockers are closed, all sixteen
truths are verified, all four requirements are satisfied, and no regression or
deferred in-milestone blocker was found. The next permitted lifecycle action is
the separate independent v1.13 milestone audit; it was not started here.

---

_Verified: 2026-07-27T10:17:04Z_
_Verifier: the agent (gsd-verifier)_
