---
phase: 52-eyebrow-safety-and-branch-closeout
plan: 07
type: execute
wave: 7
depends_on:
  - "52-06"
files_modified:
  - BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
  - BeautySDK/Sources/BeautyEffects/Warp/EyebrowWarpProvider.swift
  - BeautySDK/Tests/BeautyEffectsTests/EyebrowSafetyFixtures.swift
  - BeautySDK/Tests/BeautyEffectsTests/EyebrowWarpProviderTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift
autonomous: true
gap_closure: true
requirements:
  - SAFE-01
  - SAFE-02
  - SAFE-03
must_haves:
  truths:
    - "SAFE-01 tests exercise production-validated eyebrow traces whose inner endpoints are closer to the face center than their outer endpoints, including exact adjacency/equality boundaries and fail-closed invalid support."
    - "SAFE-02 cancellation is requested only after both resolver entry and provider-owned entry inside EyebrowWarpProvider.fieldEmissions have been observed; the blocked provider is then released, and completed cancelled work is discarded or completes safely without contaminating another request."
    - "SAFE-03 late provider-empty removal is observed at every retained-mask transition in BeautyEffectResolver's real bounded convergence loop from a strictly nonzero scaled value below the provider dead zone."
    - "No test helper or resolver seam exposes raw landmark support publicly, weakens request isolation, or changes BeautyDemo/UI behavior."
  artifacts:
    - path: "BeautySDK/Tests/BeautyEffectsTests/EyebrowSafetyFixtures.swift"
      provides: "Reusable adapter-validated canonical and precision eyebrow fixtures"
      contains: "validatedBrowTrace"
    - path: "BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift"
      provides: "Request-local internal resolver-entry observation, provider-entry callback threading, and retained-mask iteration observation, all nil by default"
      contains: "onProviderWorkStarted"
    - path: "BeautySDK/Sources/BeautyEffects/Warp/EyebrowWarpProvider.swift"
      provides: "Provider-owned synchronous entry observation/barrier inside fieldEmissions, internal and nil by default"
      contains: "fieldEmissions"
    - path: "BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift"
      provides: "Deterministic in-flight cancellation and request-isolation proof"
      contains: "testSAFE02ParallelCompletionOrderAndInterruptedWorkCannotLeakRequestState"
    - path: "BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift"
      provides: "Production-traced late provider-empty convergence proof"
      contains: "testSAFE02EveryEyebrowRowIsRemovedMonotonicallyWhenSharedScaleMakesProviderEmpty"
  key_links:
    - from: "BeautySDK/Tests/BeautyEffectsTests/EyebrowSafetyFixtures.swift"
      to: "BeautySDK/Sources/BeautyEffects/BeautyFaceGeometryAdapter.swift"
      via: "Every reusable brow trace is constructed through validatedBrowTrace"
      pattern: "BeautyFaceGeometryAdapter\\.validatedBrowTrace"
    - from: "BeautySDK/Tests/BeautyEffectsTests/EyebrowWarpProviderTests.swift"
      to: "BeautySDK/Tests/BeautyEffectsTests/EyebrowSafetyFixtures.swift"
      via: "Provider tests consume the shared canonical fixture instead of a duplicated semantic-trace constructor"
      pattern: "EyebrowSafetyFixtures"
    - from: "BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift"
      to: "BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift"
      via: "Separate request-local signals prove resolver entry and actual provider-work entry before cancellation"
      pattern: "onProvider(Resolution|Work)Started"
    - from: "BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift"
      to: "BeautySDK/Sources/BeautyEffects/Warp/EyebrowWarpProvider.swift"
      via: "The resolver threads the request-local argument-free provider-entry observation/barrier into the first real eyebrow fieldEmissions invocation"
      pattern: "fieldEmissions.*onProviderWorkStarted"
    - from: "BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift"
      to: "BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift"
      via: "The late-removal test calls the real resolver and observes every production retained-mask transition"
      pattern: "onRetainedMaskIteration"
---

<objective>
Close WR-01, WR-02, and WR-03 with executable tests over production mapping and resolver paths, preserving the seven-row safety contract in D-01 through D-12.

Purpose: Replace hollow fixtures and manual approximations with proofs that can fail when the production adapter, cancellation boundary, or convergence loop regresses.
Output: Adapter-valid shared eyebrow fixtures, a private-to-the-module request-entry observation seam, and corrected SAFE-01/SAFE-02 convergence and cancellation tests.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@ARCHITECTURE.md
@DESIGN.md
@SECURITY.md
@RELIABILITY.md
@PRODUCT_SENSE.md
@QUALITY_SCORE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/52-eyebrow-safety-and-branch-closeout/52-CONTEXT.md
@.planning/phases/52-eyebrow-safety-and-branch-closeout/52-RESEARCH.md
@.planning/phases/52-eyebrow-safety-and-branch-closeout/52-PATTERNS.md
@.planning/phases/52-eyebrow-safety-and-branch-closeout/52-VERIFICATION.md
@.planning/phases/52-eyebrow-safety-and-branch-closeout/52-REVIEW.md
@.planning/phases/52-eyebrow-safety-and-branch-closeout/52-01-SUMMARY.md
@.planning/phases/52-eyebrow-safety-and-branch-closeout/52-02-SUMMARY.md
@BeautySDK/Sources/BeautyEffects/BeautyFaceGeometryAdapter.swift
@BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
@BeautySDK/Tests/BeautyEffectsTests/EyebrowSafetyFixtures.swift
@BeautySDK/Tests/BeautyEffectsTests/EyebrowWarpProviderTests.swift
@BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
@BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift
</context>

## Artifacts this phase produces

- Internal, request-local resolver-entry, provider-owned `EyebrowWarpProvider.fieldEmissions` entry, and retained-mask iteration observation seams with nil production defaults; none is public/SPI or returns raw support.
- Reusable test-only canonical/precision eyebrow fixtures inside the existing fixture file.
- No new production files, SDK products, UI files, assets, screenshots, or user-visible copy.

<tasks>

<task type="auto">
  <name>Task 1: Replace reversed and adapter-invalid eyebrow fixtures</name>
  <files>BeautySDK/Tests/BeautyEffectsTests/EyebrowSafetyFixtures.swift, BeautySDK/Tests/BeautyEffectsTests/EyebrowWarpProviderTests.swift</files>
  <read_first>
    <file>BeautySDK/Sources/BeautyEffects/BeautyFaceGeometryAdapter.swift</file>
    <file>BeautySDK/Tests/BeautyEffectsTests/EyebrowSafetyFixtures.swift</file>
    <file>BeautySDK/Tests/BeautyEffectsTests/EyebrowWarpProviderTests.swift</file>
    <file>.planning/phases/52-eyebrow-safety-and-branch-closeout/52-VERIFICATION.md</file>
    <file>.planning/phases/52-eyebrow-safety-and-branch-closeout/52-REVIEW.md</file>
  </read_first>
  <action>
Replace `EyebrowSafetyFixtures.trace` and the duplicated private trace constructor in `EyebrowWarpProviderTests` with one reusable test-only builder that calls `BeautyFaceGeometryAdapter.validatedBrowTrace(_:side:bounds:)` and fails the calling test if validation returns nil. Reorder both default brows inner-to-outer so `abs(inner.x - bounds.center.x) &lt; abs(outer.x - bounds.center.x)` is asserted for each side before the fixture is returned; keep a production-valid face-relative chord in the adapter's inclusive 0.08...0.50 interval and a vertical span no greater than 0.25. Rebuild the precision/adjacency fixtures with a deliberately chosen synthetic bounds-and-coordinate scale that is adapter-valid and still lets Float-adjacent movements remain representable; assert validator success rather than trusting hand-written `BeautyEyebrowSemanticTrace` values. Per D-05 and SAFE-01, fold the spec-less edge fallback into the existing SAFE-01 provider test methods so the suite count remains stable: exact 0.08 and 0.50 chord equality is accepted, the next representable value below/above fails closed, equal-projection/order cases retain deterministic canonical endpoint order, and nil/empty/single/duplicate/non-finite or otherwise adapter-invalid support suppresses only the affected field while a valid sibling still emits. Delete the duplicated provider-test constructor and route all provider/GEOM fixtures through the shared builder. Do not add a public raw-support initializer, do not loosen production adapter bounds, and do not touch `BeautyDemo/`.
  </action>
  <verify>
    <automated>cd BeautySDK &amp;&amp; swift test --filter EyebrowWarpProviderTests</automated>
  </verify>
  <acceptance_criteria>
    - `EyebrowWarpProviderTests` reports 14 executed tests with zero failures.
    - Both canonical sides prove inner-to-outer center distance ordering before use.
    - Adjacency/equality and invalid-support cases execute through `validatedBrowTrace`; no direct semantic-trace test fixture remains.
    - Empty/degenerate/invalid support fails closed without suppressing a valid safe sibling.
  </acceptance_criteria>
  <done>WR-01 is closed by adapter-valid, canonically ordered shared fixtures and exhaustive SAFE-01 boundary assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove cancellation after real resolver entry without request leakage</name>
  <files>BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift, BeautySDK/Sources/BeautyEffects/Warp/EyebrowWarpProvider.swift, BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift</files>
  <read_first>
    <file>BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift</file>
    <file>BeautySDK/Sources/BeautyEffects/Warp/EyebrowWarpProvider.swift</file>
    <file>BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift</file>
    <file>RELIABILITY.md</file>
    <file>SECURITY.md</file>
    <file>.planning/phases/52-eyebrow-safety-and-branch-closeout/52-CONTEXT.md</file>
  </read_first>
  <behavior>
    - One request-local signal observes entry into `BeautyEffectResolver.resolve`; a second is fired synchronously by `EyebrowWarpProvider.fieldEmissions` after provider code has been entered but before its computation completes, and the test cancels only after both signals.
    - The provider-owned callback signals entry and holds that request on its own release barrier; only after cancellation does the test release the barrier and allow real provider/resolver work to complete.
    - After release, the cancelled wrapper observes cancellation before publication and returns a typed discarded outcome, or a documented safe-completion outcome.
    - Parallel and subsequent request identities, effective strengths, warning summaries, counts, and aggregate metrics remain request-local.
    - Logs and assertions use only aggregate diagnostics and stable field identifiers, never raw support coordinates.
  </behavior>
  <action>
Add two internal optional, argument-free observation parameters to the existing resolver entry point: `onProviderResolutionStarted: (() -&gt; Void)? = nil` and `onProviderWorkStarted: (() -&gt; Void)? = nil`. Thread them only through the request-local private implementation. Fire the resolver signal exactly once after normalization/capping enters the real resolver. Extend the internal `EyebrowWarpProvider.fieldEmissions` entry point with a nil-by-default, argument-free `onProviderWorkStarted` parameter, and pass the resolver's request-local callback only into the first real eyebrow-provider invocation. Invoke it synchronously as a provider-owned action after the `fieldEmissions` method body has been entered and before semantic-trace lookup or emission computation completes; the test-supplied callback must signal provider entry and then hold that request on its request-local release barrier. Do not forward the callback into later convergence or final-emission invocations. Existing production callers omit the parameter, and the nil/default path must allocate no barrier or observation state, perform no synchronization or wait, and mutate no state. Both callbacks remain non-public, non-SPI, unable to inspect support, and free of global/static/shared mutable state.

Replace the `Task.sleep` branch in `testSAFE02ParallelCompletionOrderAndInterruptedWorkCannotLeakRequestState` with request-local sendable barriers: start the real resolve, observe resolver entry without blocking it, wait until the callback running inside `EyebrowWarpProvider.fieldEmissions` signals provider-owned entry and proves that invocation is still held, cancel only then, release the provider barrier, prove the real provider/resolver path returned, and check cancellation at the publication boundary before classifying the typed result as discarded or documented safe completion. Preserve D-06 stateless lifecycle behavior and D-08 aggregate-only diagnostics. Exercise the cancelled identity alongside parallel and subsequent unique identities, giving each request independent callback/barrier state or nil defaults, and assert that no observation signal, release, effective eyebrow strength, warning field, emitted count, metric total, or request identity crosses between results. Map the previously unclassified SAFE-02 edge to the full lifecycle/local-failure/concurrency contract in `52-VERIFICATION.md`; do not treat cancellation as permission for shared hooks, mutable singleton coordination, partial-result publication, raw-support diagnostics, or any `BeautyDemo`/UI change.
  </action>
  <verify>
    <automated>cd BeautySDK &amp;&amp; swift test --filter MissingLandmarkDegradationTests/testSAFE02ParallelCompletionOrderAndInterruptedWorkCannotLeakRequestState</automated>
  </verify>
  <acceptance_criteria>
    - The targeted test passes without `Task.sleep`, timeouts, or scheduling-probability assertions.
    - The second signal demonstrably originates from inside `EyebrowWarpProvider.fieldEmissions` after method entry and before computation completes, and that provider invocation remains held until the test releases its request-local barrier.
    - Cancellation is requested only after resolver entry and provider-owned entry, the provider barrier is released only after cancellation, and publication occurs only after cancellation-aware discard or documented safe completion.
    - The actual provider/resolver path returns after release, while the cancelled result is discarded or safely completed under an explicit typed outcome.
    - Interrupted, parallel, and subsequent requests expose no cross-request observation, release, result, identity, or aggregate-diagnostic leakage.
    - Both callbacks are internal, argument-free, nil by default, request-local, and expose no raw support or shared mutable state; the production nil path allocates no barrier/observation state and changes no UI behavior.
  </acceptance_criteria>
  <done>WR-02 is closed by a deterministic real-request cancellation proof that preserves SAFE-02/SAFE-03 isolation and redaction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Drive late provider-empty removal through the production 44-pass loop</name>
  <files>BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift, BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift</files>
  <read_first>
    <file>BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift</file>
    <file>BeautySDK/Sources/BeautyEffects/EyebrowWarpProvider.swift</file>
    <file>BeautySDK/Sources/BeautyEffects/GeometryConflictResolver.swift</file>
    <file>BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift</file>
    <file>BeautySDK/Tests/BeautyEffectsTests/EyebrowSafetyFixtures.swift</file>
    <file>.planning/phases/52-eyebrow-safety-and-branch-closeout/52-CONTEXT.md</file>
  </read_first>
  <behavior>
    - Each of the seven eyebrow rows emits from the corrected precision fixture before shared scaling.
    - A request-local production-path trace records every retained-mask transition, including a strictly positive scaled pre-sanitization value whose test accuracy is strictly smaller than that value.
    - The real bounded resolver removes the row after provider sanitization; every later observed iteration keeps it absent, and final counts/totals/metrics/emissions exclude it.
    - Repeating the same production resolve yields the identical trace and fixed-point result.
  </behavior>
  <action>
Add an internal request-local `onRetainedMaskIteration` observation parameter to `BeautyEffectResolver.resolve`, nil by default and unavailable as public or SPI. Define the smallest internal trace value needed to report, for each real production pass, the iteration index, scaled pre-sanitization strengths entering provider evaluation, field names removed in that pass, and retained field names leaving it; it must contain no geometry, coordinates, raw support, request identity, or other sensitive values. Invoke it synchronously from the production convergence loop after the shared scale is applied and after that pass's provider-empty removals, without changing loop order, termination, output, or accounting. Rewrite `testSAFE02EveryEyebrowRowIsRemovedMonotonicallyWhenSharedScaleMakesProviderEmpty` so the system under test is `BeautyEffectResolver.resolve(parameters:faceGeometry:onRetainedMaskIteration:)`, never a reconstructed provider/loop. For each exact eyebrow row from D-01, start with a representable value strictly above the provider ULP dead zone and corrected adapter-valid precision geometry; first prove the named eyebrow provider emission is nonempty. Add the existing production-valid capped face fields needed to push the shared conflict total over 1. From the production trace, assert the eyebrow row begins with a strictly nonzero scaled pre-sanitization value and compare it with accuracy no larger than one quarter of that value—never accept zero with an equal-or-larger tolerance. Assert the trace shows one monotone removal, the row is absent from every later iteration and never re-enters, and final effective strength, named emission, count, absolute total, warning summary, and aggregate metrics exclude it. Repeat the same production resolve and assert an identical transition trace and final fixed point. Cover signed rows with allowed polarity and keep peak strictly positive per D-02; do not expose `resolveGeometryConflict`, do not recreate the loop in the test, and do not add shared mutable trace storage.
  </action>
  <verify>
    <automated>cd BeautySDK &amp;&amp; swift test --filter CombinedEffectSafetyTests/testSAFE02EveryEyebrowRowIsRemovedMonotonicallyWhenSharedScaleMakesProviderEmpty</automated>
  </verify>
  <acceptance_criteria>
    - The targeted test passes for all seven eyebrow rows through the real resolver.
    - Every row has a proven nonempty pre-scale provider emission and a strictly nonzero expected scaled value.
    - Every numeric assertion uses an accuracy smaller than the value being distinguished from zero.
    - The production trace observes every retained-mask transition; removal is monotone and every later iteration keeps the row absent.
    - Final output, count, total, warning summary, and aggregate metrics exclude the removed row, and a repeated resolve produces the identical trace and fixed point.
  </acceptance_criteria>
  <done>WR-03 is closed with a production-loop fixed-point proof rather than a manual approximation.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 contribution

This plan supplies executable ASVS L1-style input-validation, request-isolation, fail-closed output, and aggregate-only diagnostic evidence. Any unresolved high-severity finding blocks later review, owner synchronization, and promotion.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture → production geometry adapter | Hand-authored coordinates are untrusted until the adapter validates cardinality, finiteness, ordering, chord, span, and topology. |
| Async request wrapper → synchronous resolver/provider path | Resolver entry and actual provider-work entry are distinct boundaries; cancellation may race only after both are observed and may not publish a partial result or share request state. |
| Resolver retained mask → internal iteration trace → final output/diagnostics | A removed field must remain absent from every later observed iteration and final output, counts, totals, metrics, or warnings; the trace discloses no geometry. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-07-01 | Tampering | Eyebrow test support | mitigate | Construct reusable support only through `validatedBrowTrace`, assert canonical endpoint order, and cover exact inclusive and invalid boundaries. |
| T-52-07-02 | Information Disclosure | Resolver/provider observation callbacks | mitigate | Keep both callbacks internal, request-local, nil by default, argument-free, and free of shared state so they cannot disclose raw support. |
| T-52-07-03 | Elevation of Privilege | Cancellation publication boundary | mitigate | Allow computation to finish safely but require cancellation-aware discard before publication; never use a global test hook. |
| T-52-07-04 | Repudiation | Late provider-empty convergence | mitigate | Observe every real retained-mask transition, strictly nonzero scale, monotone removal/no-reentry, final aggregate exclusion, and repeated identical fixed point through the production resolver. |
| T-52-07-05 | Denial of Service | 44-pass convergence | accept | The loop remains production-bounded at exactly 44 passes per D-09/D-11; this plan adds no unbounded retry or wait. |
</threat_model>

<verification>
- `cd BeautySDK &amp;&amp; swift test --filter EyebrowWarpProviderTests` reports 14/14 passing.
- `cd BeautySDK &amp;&amp; swift test --filter MissingLandmarkDegradationTests` reports 51/51 passing.
- `cd BeautySDK &amp;&amp; swift test --filter CombinedEffectSafetyTests` reports 17/17 passing.
- `git diff --name-only -- BeautyDemo` is empty.
- `rg -n "public .*onProvider(Resolution|Work)Started|@_spi.*onProvider(Resolution|Work)Started|public .*onRetainedMaskIteration|@_spi.*onRetainedMaskIteration" BeautySDK/Sources` returns no matches.
</verification>

<success_criteria>
- WR-01, WR-02, and WR-03 each have a direct executable regression proof.
- SAFE-01 covers exact touching/equality, stable ordering, and nil/empty/single/duplicate/non-finite/adapter-invalid support without suppressing safe siblings.
- SAFE-02 cancellation maps to the full stateless lifecycle and local-failure contract.
- SAFE-03 interrupted/parallel requests remain isolated and expose aggregate-only diagnostics.
- The production API and all UI paths remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/52-eyebrow-safety-and-branch-closeout/52-07-SUMMARY.md` when done.
</output>
