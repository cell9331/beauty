---
phase: 52-eyebrow-safety-and-branch-closeout
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift
  - BeautySDK/Sources/BeautyEffects/Warp/EyebrowWarpProvider.swift
  - BeautySDK/Tests/BeautyEffectsTests/EyebrowSafetyFixtures.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautySafetyCapsTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/EyebrowWarpProviderTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift
autonomous: true
requirements: [SAFE-01, SAFE-02]
must_haves:
  truths:
    - "All seven final eyebrow caps are exactly 0.25 under one `BeautySafetyCaps` authority; zero and magnitudes at or below `Float.ulpOfOne` are neutral, the adjacent representable magnitude above the dead zone is eligible, exact-cap input is not capped, and overflow clamps once with one aggregate count/warning/metric (D-01, D-02, D-03)."
    - "The six signed fields preserve both polarities; `eyebrowPeakDefinition` is positive-only and rejects negative/non-finite input; every emitted point is finite/unit-bound and stays within the unchanged field radius ceilings 0.08/0.055/0.07/0.08/0.06/0.075/0.055 of face width (D-02, D-04)."
    - "One typed, exactly seven-row descriptor is the shared oracle for public construction, effective key path, named emission, semantics, fresh 0.25, reused 0.125, radius, and narrowest unavailable fixture (D-05)."
    - "Fresh, reused, stale, no-face, nil support, single-side, missing, malformed, provider-empty, positive-to-negative, negative-to-positive, and valid-invalid-valid calls are stateless; parallel and interrupted work cannot carry request-local support into another result (D-06, D-07, D-08)."
    - "Single-side support retains only independently supportable per-side work, whole-brow spacing requires a valid pair, peak requires an eligible interior apex, and failures preserve valid eyebrow siblings plus unrelated face/eye/nose/mouth/color/filter work with stable extent and aggregate-only diagnostics (D-07, D-08)."
  artifacts:
    - path: BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift
      provides: "Single final authority for seven exact eyebrow caps"
      contains: "eyebrowPeakDefinition"
    - path: BeautySDK/Tests/BeautyEffectsTests/EyebrowSafetyFixtures.swift
      provides: "Shared seven-field executable descriptor and request-local fixtures"
      contains: "EyebrowSafetyRow"
    - path: BeautySDK/Tests/BeautyEffectsTests/EyebrowWarpProviderTests.swift
      provides: "Exact dead-zone, direction, formula, radius, and support-prerequisite evidence"
      contains: "eyebrow"
    - path: BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
      provides: "Exhaustive lifecycle, local-failure, transition, and concurrency matrix"
      contains: "validInvalidValid"
    - path: BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift
      provides: "Public extent, safe-sibling, and redaction evidence"
      contains: "Eyebrow"
  key_links:
    - from: BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift
      to: BeautySDK/Sources/BeautyEffects/Warp/EyebrowWarpProvider.swift
      via: "Every strength normalization, displacement divisor, positive-only maximum, and generic point maximum uses the same named final cap"
      pattern: "BeautySafetyCaps\\.eyebrow"
    - from: BeautySDK/Tests/BeautyEffectsTests/EyebrowSafetyFixtures.swift
      to: BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
      via: "One seven-row descriptor drives lifecycle and unavailable-support cases"
      pattern: "EyebrowSafetyRow"
    - from: BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
      to: BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift
      via: "The same field-local failures are proved at resolver and public-facade boundaries"
      pattern: "eyebrow_inputs_missing"
  prohibitions:
    - "Never fabricate, borrow, mirror, close, retry, cache, or synthesize eyebrow support from eyes, the opposite side, a prior request, or any other geometry."
    - "Never let exact-cap finalization introduce a larger provider-only or renderer-only maximum, alter accepted Phase 51 formulas, or promote any product status in this wave."
---

<objective>
Freeze the exact seven-field cap/dead-zone contract and prove the exhaustive request-local lifecycle and local-failure matrix without changing product status.

Purpose: SAFE-01 and the lifecycle half of SAFE-02 must be executable before combined convergence or promotion can be trusted.
Output: One final cap authority, one shared seven-field test descriptor, exhaustive provider/resolver/facade evidence, and no documentation promotion.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@DESIGN.md
@SECURITY.md
@RELIABILITY.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-eyebrow-safety-and-branch-closeout/52-CONTEXT.md
@.planning/phases/52-eyebrow-safety-and-branch-closeout/52-RESEARCH.md
@.planning/phases/52-eyebrow-safety-and-branch-closeout/52-PATTERNS.md
@.planning/phases/52-eyebrow-safety-and-branch-closeout/52-VALIDATION.md
@.planning/phases/49-public-contract-and-observed-eyebrow-support/49-05-SUMMARY.md
@.planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-06-SUMMARY.md
@.planning/phases/51-public-facade-eyebrow-output-evidence/51-05-SUMMARY.md
@BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift
@BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
@BeautySDK/Sources/BeautyEffects/Warp/EyebrowWarpProvider.swift

<interfaces>
Existing production contracts to preserve:
- `BeautySafetyCaps.eyebrowYPosition` through `.eyebrowPeakDefinition` are seven `Float` constants currently equal to `0.25`.
- `EyebrowWarpFieldEmissions` exposes seven same-named arrays, stable `points` concatenation, and field-local `sanitizing(_:)`.
- `EyebrowWarpProvider.fieldEmissions(face:strengths:)` consumes request-local `FaceGeometry.observedEyebrowSupport`; `makeControlPoints` exposes only fixed skip reason `eyebrow_inputs_missing`.
- `BeautyEffectResolver.resolve(parameters:faceGeometry:)` owns public normalization, capped-count evidence, freshness scaling, provider sanitization, and aggregate warnings/metrics.

Decision index used by this plan:
- D-01 exact seven `0.25` caps and ownership-only change unless an executable row exposes a defect.
- D-02 exact `Float.ulpOfOne` dead zone, six signed fields, positive-only peak.
- D-03 overflow clamps once; exact cap is not capped.
- D-04 preserve brow-relative formulas and finite/unit/radius checks; provider and final cap maxima agree.
- D-05 one typed seven-field descriptor.
- D-06 exhaustive stateless lifecycle and transitions.
- D-07 field-local side/pair/apex prerequisites with no substitute support.
- D-08 safe siblings, unchanged extent, aggregate-only redaction.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 52-01-01: Establish the shared seven-field oracle and freeze exact cap adjacency</name>
  <files>BeautySDK/Tests/BeautyEffectsTests/EyebrowSafetyFixtures.swift, BeautySDK/Tests/BeautyEffectsTests/BeautySafetyCapsTests.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift, BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift</files>
  <behavior>
    - "Exactly seven unique descriptor names map one-to-one to the public constructor, `BeautyEffectiveStrengths` key path, named provider emission, signed/positive-only semantics, fresh `0.25`, reused `0.125`, maximum radius, and narrowest unavailable fixture (D-05)."
    - "For every row, zero and `±Float.ulpOfOne` are neutral; `Float.ulpOfOne.nextUp` with eligible support is not treated as neutral; exact `±0.25` is retained without capped evidence; the next representable magnitude above `0.25` and ordinary overflow clamp to `±0.25` once (D-01, D-02, D-03)."
    - "Peak negative values and every non-finite public input resolve to zero; signed rows preserve polarity; aggregate evidence contains exactly one capped count/warning/metric for one overflowing field and no field name or raw geometry (D-02, D-03)."
  </behavior>
  <action>Create `EyebrowSafetyFixtures.swift` as test-only shared infrastructure in the existing BeautyEffects test target; define `EyebrowSafetyRow` and exactly seven rows, using closures/key paths rather than duplicated switch tables. Write the cap/resolver tests first and observe failure on provisional ownership or any adjacency mismatch. Freeze each cap at exact `0.25`, change only the provisional Phase 50 comment to final Phase 52 ownership, and preserve the existing public finite normalization. Treat a just-above-`Float.ulpOfOne` value as outside the strength dead zone; if the current provider arithmetic later rejects it because the topology epsilon is overloaded as a strength-derived displacement threshold, Task 02 must repair that real defect without weakening topology/vector/radius validation. Keep all row/branch statuses unchanged per D-01 through D-05.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --disable-sandbox --jobs 1 --filter BeautyEffectsTests.BeautySafetyCapsTests &amp;&amp; swift test --package-path BeautySDK --disable-sandbox --jobs 1 --filter BeautyEffectsTests.BeautyEffectResolverTests &amp;&amp; test "$(rg -n 'static let eyebrow(YPosition|Thickness|Length|Spacing|HeadSpacing|Tilt|PeakDefinition): Float = 0\\.25\\b' BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift | wc -l | tr -d ' ')" -eq 7 &amp;&amp; ! rg -n 'Provisional Phase 50 eyebrow caps' BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift &amp;&amp; git diff --check</automated>
  </verify>
  <done>The shared oracle has seven unique rows; all neutral/adjacent/exact-cap/overflow/non-finite cases, signed/positive semantics, exact counts, warnings, metrics, and final cap ownership pass without status promotion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 52-01-02: Make provider acceptance match the final cap and prove every support boundary</name>
  <files>BeautySDK/Sources/BeautyEffects/Warp/EyebrowWarpProvider.swift, BeautySDK/Tests/BeautyEffectsTests/EyebrowSafetyFixtures.swift, BeautySDK/Tests/BeautyEffectsTests/EyebrowWarpProviderTests.swift, BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift</files>
  <behavior>
    - "All seven fields use the matching `BeautySafetyCaps` constant for strength acceptance and formula division; no `provisionalCap`, generic eyebrow maximum, provider-only maximum, or renderer-only maximum remains (D-01, D-04)."
    - "The exact radius ceilings are 0.08, 0.055, 0.07, 0.08, 0.06, 0.075, and 0.055 of face width in stable row order; all sources/targets/radii/strengths are finite and unit-bound and accepted Phase 51 displacement/angle/weight/falloff formulas remain unchanged (D-04, D-05)."
    - "Nil/no-face zeroes all rows; a single valid side retains vertical/thickness/length/head-spacing/tilt and an eligible peak for that side, whole spacing requires the valid pair, and missing chord/apex/provider output removes only dependent rows (D-06, D-07)."
    - "At/below dead-zone input emits nothing, just-above input on an eligible fixture is not rejected by a broader strength dead zone, exact cap emits bounded work, and overflow reaches the provider only as the exact final cap (D-02, D-03, D-04)."
  </behavior>
  <action>Write descriptor-driven provider and degradation assertions before changing production. Replace the single provider-local `provisionalCap` with the corresponding named `BeautySafetyCaps` value in signed acceptance, peak acceptance, all seven displacement/angle denominators, and generic point validation. Separate the exact public/provider strength dead zone from the existing `0.000_001` topology/vector/radius safety epsilon so a representable strength just above `Float.ulpOfOne` is not silently reclassified as neutral; preserve rejection of non-finite, zero-length, out-of-unit, zero-radius, zero-extent, malformed trace, and non-representable point work. Assert the unchanged formula/radius inventory, named emission order, pair-only spacing, per-side continuation, and apex-only peak behavior per D-01 through D-07. Never create an eye-derived, mirrored, cached, cross-side, or synthetic substitute.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --disable-sandbox --jobs 1 --filter BeautyEffectsTests.EyebrowWarpProviderTests &amp;&amp; swift test --package-path BeautySDK --disable-sandbox --jobs 1 --filter BeautyEffectsTests.MissingLandmarkDegradationTests &amp;&amp; ! rg -n 'provisionalCap' BeautySDK/Sources/BeautyEffects/Warp/EyebrowWarpProvider.swift &amp;&amp; test "$(rg -n 'BeautySafetyCaps\\.eyebrow(YPosition|Thickness|Length|Spacing|HeadSpacing|Tilt|PeakDefinition)' BeautySDK/Sources/BeautyEffects/Warp/EyebrowWarpProvider.swift | sed 's/.*BeautySafetyCaps\\.//' | sort -u | wc -l | tr -d ' ')" -eq 7 &amp;&amp; git diff --check</automated>
  </verify>
  <done>Provider acceptance, formulas, maximums, dead-zone adjacency, point bounds, radii, and side/pair/apex failure locality agree with the final seven caps; no support is fabricated or borrowed.</done>
</task>

<task type="auto">
  <name>Task 52-01-03: Complete the stateless transition, concurrency, sibling, and facade matrix</name>
  <files>BeautySDK/Tests/BeautyEffectsTests/EyebrowSafetyFixtures.swift, BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift, BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift</files>
  <action>Drive fresh, reused, stale, no-face, nil support, per-row narrow missing, malformed, provider-empty, positive-to-negative, negative-to-positive, and valid-invalid-valid sequences from the one descriptor per D-05 through D-08. Assert fresh exact `0.25`, reused exact `0.125` once with one reuse metric, stale/no-face exact zero, no suppression of eligible eyebrow siblings or unrelated face/eye/nose/mouth/color/filter domains, stable public output extent, and fixed aggregate-only warnings/metrics. Add deterministic parallel request-identity coverage plus an interruption/cancellation case that proves no task, facade call, provider instance, fixture, warning, metric, or support value leaks into a sibling or subsequent call; repeated runs must produce the same per-request result regardless of completion order. Explicitly reject diagnostic/reflection text containing side names, point counts tied to a field, endpoints, centers, apexes, axes, coordinates, arrays, provider types, or image bytes.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --disable-sandbox --jobs 1 --filter BeautyEffectsTests.MissingLandmarkDegradationTests &amp;&amp; swift test --package-path BeautySDK --disable-sandbox --jobs 1 --filter BeautyCoreTests.BeautyEngineGeometryFacadeTests &amp;&amp; git diff --check</automated>
  </verify>
  <done>The exact lifecycle/local-failure matrix, both transition directions, valid-invalid-valid recovery, parallel/interrupted isolation, safe-sibling continuation, extent, and aggregate redaction pass deterministically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Public scalars → normalization/caps | Untrusted finite, non-finite, adjacent, exact-cap, and overflow input reaches effective strengths. |
| Request-local eyebrow support → provider | Biometric-adjacent sides/traces may be nil, single-side, malformed, stale, reused, or provider-empty. |
| Resolver/provider → public facade | Internal named work must reduce to stable output extent and aggregate-only warnings/metrics. |
| Parallel/interrupted calls → subsequent requests | Cancellation and completion order must not create shared support, cached emissions, or stale diagnostics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-52-01 | Tampering | final cap authority | mitigate | Seven exact constants, named provider references, exact-cap/overflow tests, and no local maximum. |
| T-52-02 | Tampering | dead-zone/direction handling | mitigate | Zero/ulp/nextUp/cap/overflow/non-finite table with signed and positive-only assertions. |
| T-52-03 | Spoofing | eyebrow support provenance | mitigate | Actual request-local support only; explicit no-eye/no-mirror/no-cache/no-synthesis assertions. |
| T-52-04 | Tampering | side/pair/apex prerequisites | mitigate | Descriptor-owned nil/single-side/chord/apex/malformed/provider-empty cases remove only dependents. |
| T-52-05 | Tampering | freshness transitions | mitigate | Fresh/reused/stale/no-face and both-direction valid-invalid-valid calls assert exact stateless values. |
| T-52-06 | Information Disclosure | facade diagnostics | mitigate | Fixed reasons and aggregate metrics; negative scans for support/provider/coordinate/image details. |
| T-52-07 | Denial of Service | sibling domains | mitigate | Every failure row asserts valid eyebrow and unrelated face/eye/nose/mouth/color/filter continuation. |
| T-52-08 | Tampering | parallel/interrupted execution | mitigate | Parallel identity and cancellation fixtures prove request-local results independent of completion order. |
</threat_model>

<verification>
- Run the four focused suites after their owning tasks, then rerun all Plan 01 suites together with `--jobs 1`.
- Confirm seven exact cap constants, seven distinct provider cap references, no provisional provider maximum, and no product-owner status changes.
- Confirm the spec-less edge categories are classified explicitly: dead-zone adjacency, exact-cap/overflow, nil/no-face/missing/single-side, full lifecycle matrix, and parallel/interrupted isolation.
- Treat any unclassified input class, support prerequisite, diagnostic match, or concurrency result as a failing test; do not infer acceptance.
</verification>

<success_criteria>
SAFE-01 cap/direction/radius evidence and SAFE-02 lifecycle/local-failure evidence are complete and executable while all seven product rows and branch `眉毛` remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/52-eyebrow-safety-and-branch-closeout/52-01-SUMMARY.md` when done.
</output>
