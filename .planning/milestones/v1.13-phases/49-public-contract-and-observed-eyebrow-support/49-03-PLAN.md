---
phase: 49-public-contract-and-observed-eyebrow-support
plan: "03"
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift
  - BeautySDK/Tests/BeautyDetectionTests/VisionFaceDetectorTests.swift
  - BeautySDK/Tests/BeautyDetectionTests/FaceObservationMappingTests.swift
autonomous: true
requirements: [SUPP-01, SUPP-02, SUPP-03]
must_haves:
  truths:
    - "D-05/D-06: the existing one VNDetectFaceLandmarksRequest reads actual VNFaceLandmarks2D.leftEyebrow/rightEyebrow regions, independently requires open-path classification and 1...16 points before copying or mapping, and preserves the selected face plus valid siblings when one brow is rejected."
    - "D-07: each accepted eyebrow point is composed with the selected face bounds and mapped exactly once through the call-local CoordinateMapper; rejected points cause zero eyebrow point-map calls and no retry, remap, cache, or second Vision request exists."
    - "D-08: neither leftEye/rightEye, BeautyObservedEyeSupport, shipped eye arrays, the historical eye proxy, nor generated/synthetic points can populate observed eyebrow support."
    - "D-09/D-11: mapper-derived face center/right/down axes classify anatomical side and inner-to-outer direction; canonicalization uses whole-array reversal only and remains identical across 4 orientations, both input-mirror states, both preview-mirror states, and original/reversed Vision order."
    - "D-13/D-14/D-15: copied and mapped values remain package-only and request-local; repeated, alternating, interrupted, stale/no-face, and eight-way parallel requests retain no prior support and expose aggregate counts only."
  artifacts:
    - path: BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift
      provides: "Actual eyebrow-region preflight/copy, independent exactly-once mapping, mapper-axis side/order canonicalization, and redacted carrier propagation"
    - path: BeautySDK/Tests/BeautyDetectionTests/VisionFaceDetectorTests.swift
      provides: "Direct property provenance, classification/count preflight, local failure, one-request, mapping-count, and lifecycle evidence"
    - path: BeautySDK/Tests/BeautyDetectionTests/FaceObservationMappingTests.swift
      provides: "Complete orientation × input mirror × preview mirror × source-order canonicalization matrix"
  key_links:
    - from: VNFaceLandmarks2D.leftEyebrow/rightEyebrow
      to: VisionDetectionObservation.observedEyebrowSupport
      via: "immediate bounded value copy inside landmarks(from:) from the existing request"
      pattern: "leftEyebrow|rightEyebrow"
    - from: VisionDetectionObservation.observedEyebrowSupport
      to: BeautyFaceObservation.observedEyebrowSupport
      via: "one call-local CoordinateMapper pass plus mapped face-axis canonicalization"
      pattern: "observedEyebrowSupport"
    - from: mapped face center/right axis
      to: left/right inner-to-outer arrays
      via: "centroid and endpoint dot products followed only by optional whole-array reversal"
      pattern: "reversed"
  prohibitions:
    - statement: "No eye contour, old eye geometry, generated trace, synthetic point, face-bounds reconstruction, or fallback array may satisfy actual eyebrow provenance."
      status: required
      verification: "Actual-property tests, negative proxy fixtures, and the Phase 49 live checker."
    - statement: "No second Vision request, retry, remap, retained VNFaceLandmarkRegion2D, cache, actor/global support state, persistence, network, or coordinate-bearing diagnostic may be introduced."
      status: required
      verification: "Counting mapper/request tests, lifecycle matrices, source scan, and diagnostic redaction tests."
    - statement: "This plan must not add providers, effective strengths, caps, resolver/conflict cases, facade routing, renderer/gallery cases, Demo/UI code, or product-row promotion."
      status: required
      verification: "Files_modified ownership and fail-closed boundary checker."
---

<objective>
Capture actual left/right Apple Vision eyebrow traces and canonicalize them once at the existing request-local detection boundary.

Purpose: Supply honest anatomical eyebrow evidence to the semantic adapter without substituting shipped eye geometry, retaining framework objects, or allowing orientation/mirroring to change side or endpoint meaning.
Output: Independently bounded actual-region capture, exactly-once coordinate mapping, canonical left/right inner-to-outer arrays, and lifecycle/concurrency evidence.
</objective>

## Artifacts this phase produces

- A default-nil `VisionDetectionObservation.observedEyebrowSupport` seam feeding the Plan 49-01 `BeautyObservedEyebrowSupport` value.
- Actual `VNFaceLandmarks2D.leftEyebrow` and `.rightEyebrow` copy logic inside the existing selected-face request path, with a fixed 16-point preflight ceiling and open-path classification gate.
- Detection-owned mapping/canonicalization helpers using the existing `CoordinateMapper`, mapped face center/right/down axes, and whole-array reversal only.
- SUPP-01/SUPP-02/SUPP-03 tests for property provenance, exact map counts, complete metadata invariance, local sibling failure, aggregate diagnostics, and request isolation.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<read_first>
AGENTS.md; PLANS.md; .planning/PROJECT.md; .planning/ROADMAP.md; .planning/REQUIREMENTS.md; .planning/STATE.md; .planning/phases/49-public-contract-and-observed-eyebrow-support/49-CONTEXT.md; .planning/phases/49-public-contract-and-observed-eyebrow-support/49-RESEARCH.md; .planning/phases/49-public-contract-and-observed-eyebrow-support/49-PATTERNS.md; .planning/phases/49-public-contract-and-observed-eyebrow-support/49-VALIDATION.md; 49-01-SUMMARY.md; BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift; BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift; BeautySDK/Sources/BeautyDetection/CoordinateSpace.swift; BeautySDK/Tests/BeautyDetectionTests/VisionFaceDetectorTests.swift; BeautySDK/Tests/BeautyDetectionTests/FaceObservationMappingTests.swift
</read_first>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/49-public-contract-and-observed-eyebrow-support/49-CONTEXT.md
@.planning/phases/49-public-contract-and-observed-eyebrow-support/49-RESEARCH.md
@.planning/phases/49-public-contract-and-observed-eyebrow-support/49-PATTERNS.md
@BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift
@BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift
@BeautySDK/Tests/BeautyDetectionTests/VisionFaceDetectorTests.swift
@BeautySDK/Tests/BeautyDetectionTests/FaceObservationMappingTests.swift

<interfaces>
Plan 49-01 supplies package-only `BeautyObservedEyebrowSide`, `BeautyObservedEyebrowSupport(left:right:)`, and default-nil `BeautyFaceObservation.observedEyebrowSupport`. Extend `VisionDetectionObservation` with the same default-nil envelope and update its aggregate-only description/debugDescription/customMirror. `defaultObservationProvider` must retain its one `VNDetectFaceLandmarksRequest`; `landmarks(from:)` is the sole framework-object copy seam. `mapObservation(_:mapper:)`, `mapPoints(_:in:with:)`, and `mappedFaceAxes(in:with:)` are the current conversion patterns. Preserve existing eye/face support, selection, summary, and source-compatible initializer behavior.
</interfaces>
</context>

## Plan-local source coverage

| Source | Coverage |
| --- | --- |
| GOAL | Supplies actual, orientation-stable, request-scoped Vision eyebrow evidence. |
| REQ | SUPP-01 receives direct actual-region capture and exactly-once mapping; SUPP-02 receives independent preflight and side/order canonicalization; SUPP-03 receives redaction and lifecycle isolation. |
| RESEARCH | Applies the one-request copy seam, 16-point preflight, open classification, fixed axis probes, whole-array reversal, complete metadata matrix, and local failure pattern. |
| CONTEXT | D-05 through D-09, D-11 through D-16, and D-17 are implemented or guarded without a downstream consumer. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 49-03-01: Copy actual Vision eyebrow regions behind independent preflight</name>
  <files>BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift, BeautySDK/Tests/BeautyDetectionTests/VisionFaceDetectorTests.swift</files>
  <read_first>AGENTS.md; PLANS.md; .planning/phases/49-public-contract-and-observed-eyebrow-support/49-CONTEXT.md; .planning/phases/49-public-contract-and-observed-eyebrow-support/49-RESEARCH.md; 49-01-SUMMARY.md; BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift; BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift; BeautySDK/Tests/BeautyDetectionTests/VisionFaceDetectorTests.swift</read_first>
  <behavior>
    - The existing default provider reads only actual leftEyebrow/rightEyebrow from the same VNFaceLandmarks2D result and retains no framework region or pointer.
    - Each side independently accepts open-path counts 1, 15, and 16 for bounded value copying; count 0, count 17, disconnected, closed-path, or unavailable regions become nil before point mapping.
    - Valid-left/invalid-right and invalid-left/valid-right preserve the valid side, selected face, observed eye/face support, and existing landmark-group inventory.
    - No eyebrow input creates a second request, extra dependency/model/resource, raw diagnostic value, or eye/synthetic fallback.
  </behavior>
  <action>Per D-05/D-06/D-08/D-12/D-13/D-14/D-16, first add failing direct-property and preflight table tests using the compiled Plan 49-01 Vision fixtures. Add a defaulted `observedEyebrowSupport` label to `VisionDetectionObservation` and its fixed aggregate-only diagnostic surfaces. Extend `landmarks(from:)` inside the one existing `VNDetectFaceLandmarksRequest` provider to inspect `landmarks.leftEyebrow` and `landmarks.rightEyebrow` directly. For each side, require `pointsClassification == .openPath` and `1...16` points before reading/copying `normalizedPoints`; copy immediately to `[CoordinatePoint]`, keep side absence independent, and build no envelope when neither survives. Do not add an eyebrow `BeautyLandmarkGroup`, reuse `makeSupport` for eyes, retain `VNFaceLandmarkRegion2D`, reconstruct from bounds, retry, or synthesize points. Preserve selected-face, eye, face-contour, median, nose, lip, and summary behavior when either optional brow fails.</action>
  <acceptance_criteria>Tests prove the actual property names and one-request seam, exact classification/count preflight, zero later mapping for rejected regions, independent side/sibling retention, aggregate-only diagnostics, and absence of eye/synthetic fallback or framework-object retention.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyDetectionTests.VisionFaceDetectorTests &amp;&amp; python3 .planning/phases/49-public-contract-and-observed-eyebrow-support/check_eyebrow_support_boundaries.py --self-test &amp;&amp; git diff --check</automated></verify>
  <commit_expectation>Commit RED as `test(49-03): specify actual Vision eyebrow capture`, then GREEN as `feat(49-03): copy bounded eyebrow regions`; do not squash.</commit_expectation>
  <done>SUPP-01 has actual, bounded, independent request-local source data before coordinate conversion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 49-03-02: Map once and canonicalize anatomical side and endpoint order</name>
  <files>BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift, BeautySDK/Tests/BeautyDetectionTests/VisionFaceDetectorTests.swift, BeautySDK/Tests/BeautyDetectionTests/FaceObservationMappingTests.swift</files>
  <read_first>AGENTS.md; PLANS.md; .planning/phases/49-public-contract-and-observed-eyebrow-support/49-CONTEXT.md; 49-01-SUMMARY.md; BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift; BeautySDK/Sources/BeautyDetection/CoordinateSpace.swift; BeautySDK/Tests/BeautyDetectionTests/VisionFaceDetectorTests.swift; BeautySDK/Tests/BeautyDetectionTests/FaceObservationMappingTests.swift</read_first>
  <behavior>
    - Every accepted eyebrow point is composed with visionBounds and passed through CoordinateMapper exactly once; rejected regions receive zero point-map calls, apart from no hidden retry or remap.
    - Four orientations × two input-mirror states × two preview-mirror states × two sides × original/reversed input produce the same anatomical side and canonical inner/outer endpoints.
    - Declared left requires negative centroid projection on mapped face-right and canonical left inner-to-outer requires negative endpoint projection; declared right requires the corresponding positive projections.
    - Only whole-array reversal is allowed; adjacency and exact point multiset are preserved, while epsilon-degenerate center/endpoint projections fail that side locally.
    - Alternating, repeated, interrupted, valid-to-stale/no-face, opposite-metadata, and eight-way parallel requests never reuse another request's brow arrays or metadata.
  </behavior>
  <action>Per D-07/D-09/D-10/D-11/D-12/D-13/D-14/D-15/D-17, write the complete counting-mapper and 4×2×2×2-per-side RED matrix before production mapping. In `mapObservation(_:mapper:)`, validate shared positive finite face bounds once, then map each preflighted brow independently through the existing face-local-to-Vision-image composition and one `CoordinateMapper.map(point:from:to:)` call per accepted point. Derive call-local mapped face center plus normalized face-right/down axes from four fixed basis probes; exclude those fixed probes from the per-brow point count but assert their exact bounded count. Classify the left slot by negative and right slot by positive centroid projection relative to mapped center, reject absolute projection at or below the `1e-6` policy epsilon, then establish the research-locked inner-to-outer endpoint sign with retention or one whole-array reversal. Never sort, close the path, infer side from screen X, consult preview-space output, remap in the adapter, or share axes/state. Propagate the surviving envelope into `BeautyFaceObservation`; add full local-failure, adjacency, exact-map-count, redaction, repeated/interrupted/stale/no-face, and eight-task concurrency assertions.</action>
  <acceptance_criteria>All 64 canonicalization rows (4 orientations × 2 input mirror × 2 preview mirror × 2 sides × 2 source orders) agree on side/endpoints and adjacency; accepted point calls are exactly once plus four fixed axis probes; malformed sides fail locally; every lifecycle/concurrency identity is isolated and aggregate-only.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyDetectionTests.FaceObservationMappingTests &amp;&amp; swift test --package-path BeautySDK --filter BeautyDetectionTests.VisionFaceDetectorTests &amp;&amp; python3 .planning/phases/49-public-contract-and-observed-eyebrow-support/check_eyebrow_support_boundaries.py --self-test &amp;&amp; git diff --check</automated></verify>
  <commit_expectation>Commit RED as `test(49-03): specify eyebrow mapping invariants`, then GREEN as `feat(49-03): canonicalize mapped eyebrow traces`; do not squash.</commit_expectation>
  <done>SUPP-01/SUPP-02/SUPP-03 have exactly-once, orientation-stable, local-failure, stateless mapped evidence ready for the adapter.</done>
</task>

</tasks>

<threat_model>
<blocking_rule>ASVS L1: any unresolved HIGH finding, unclassified checker result, proxy provenance violation, mapper-count mismatch, cross-request carryover, raw-coordinate disclosure, or required-command failure blocks this plan and every dependent wave; HIGH threats may not be accepted or transferred.</blocking_rule>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Apple Vision region → request-local copied values | Framework classification, count, pointers, and coordinates are untrusted before bounded copy. |
| Copied face-local values → mapped image values | Bounds, orientation, mirroring, and point values can corrupt side/order if mapped more than once or with shared metadata. |
| Mapped support → later requests/diagnostics | Request-local arrays must disappear without coordinate-bearing descriptions, persistence, or concurrency bleed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-49-11 | Spoofing | eyebrow source provenance | mitigate | Direct leftEyebrow/rightEyebrow property tests plus static rejection of eye/synthetic substitutes. |
| T-49-12 | Denial of Service | Vision region copy/map | mitigate | Open-path and 1...16 preflight before copy/mapping, independent rejection, and exact boundary tables. |
| T-49-13 | Tampering | side/order canonicalization | mitigate | Mapper-derived center/right/down axes, fixed epsilon, reversal only, and complete metadata/source-order matrix. |
| T-49-14 | Repudiation | exactly-once mapping/request count | mitigate | Counting mapper, exact fixed axis probes, one request assertion, and no retry/remap source gate. |
| T-49-15 | Information Disclosure | request lifecycle/diagnostics | mitigate | Immutable package values, aggregate-only parents, repeated/interrupted/no-face/parallel isolation tests, and no persistence/network. |
| T-49-SC | Tampering | package supply chain | mitigate | No install/manifest change; the Phase 49 checker blocks dependency/target/model/resource/network drift. |
</threat_model>

<verification>
Run both focused detection suites and checker self-tests after each task. At wave completion run the complete `BeautyDetectionTests` target and inspect the plan diff: only the declared detector/test files may change, every actual/proxy/lifecycle marker must classify, and no provider/resolver/facade/renderer/Demo/dependency/resource path may enter.
</verification>

<success_criteria>
The existing Vision request copies actual bounded left/right eyebrow regions, maps every accepted point exactly once, canonicalizes side and inner-to-outer order across all metadata combinations, rejects malformed evidence locally, and leaks or retains no request data.
</success_criteria>

<output>Create `49-03-SUMMARY.md` with actual-property/classification evidence, exact accepted/rejected count rows, mapping-call arithmetic, 64-row canonicalization result, lifecycle/concurrency results, focused test counts, atomic commit hashes, and explicit no-proxy/no-consumer/nonclaim status.</output>
