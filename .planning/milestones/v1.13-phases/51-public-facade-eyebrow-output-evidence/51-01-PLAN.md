---
phase: 51-public-facade-eyebrow-output-evidence
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - BeautySDK/Sources/BeautyExampleRenderer/main.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift
autonomous: true
requirements: [OUT-01, OUT-03]
must_haves:
  truths:
    - "The public renderer exposes exactly 72 ordered, duplicate-free cases, including thirteen isolated eyebrow cases and no parked portrait inputs (D-01, D-02, D-04, D-09)."
    - "Each eyebrow case sets exactly one same-named public field at provisional magnitude 0.25 and still traverses the one shared BeautyEngine.processResult call (D-09, D-12)."
    - "No-face, missing, malformed, and left/right-only support preserve output extent, remove only dependent eyebrow work, and keep safe siblings plus aggregate-only diagnostics (D-10, D-11)."
  artifacts:
    - path: BeautySDK/Sources/BeautyExampleRenderer/main.swift
      provides: "Exact thirteen-case public eyebrow renderer extension"
      contains: "eyebrowPeakDefinition_0p25"
    - path: BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift
      provides: "Ordered inventory, isolation, active-fixture, facade, and no-face contracts"
      contains: "72"
    - path: BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift
      provides: "Representative missing, malformed, and partial-support public degradation evidence"
      contains: "missingObservedEyebrows"
  key_links:
    - from: BeautySDK/Sources/BeautyExampleRenderer/main.swift
      to: BeautySDK/Sources/BeautySDK/BeautyEngine.swift
      via: "The existing single engine.processResult call receives each isolated BeautyParameters value"
      pattern: "engine\\.processResult\\("
    - from: BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift
      to: BeautySDK/Sources/BeautyExampleRenderer/main.swift
      via: "Ordered source parsing and one-field initializer assertions"
      pattern: "expectedRendererCaseIDs|rendererCaseSnippet"
    - from: BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift
      to: BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift
      via: "Existing paired, left-only, right-only, missing, and malformed request-local fixtures"
      pattern: "pairedObservedEyebrows|leftOnlyObservedEyebrow|missingObservedEyebrows"
---

<objective>
Extend the public-facade renderer and focused regression surface with the exact eyebrow matrix and representative degradation behavior required by OUT-01 and OUT-03.

Purpose: Freeze the executable case and fixture contract before pixel calibration, so later output evidence cannot be satisfied by aliases, parked portraits, internal imports, or test-only rendering.
Output: A 72-case public renderer, exact isolated-case tests, and request-local no-face/missing/malformed/partial-support facade evidence.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/51-public-facade-eyebrow-output-evidence/51-CONTEXT.md
@.planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-06-SUMMARY.md
@BeautySDK/Sources/BeautyExampleRenderer/main.swift
@BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift
@BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift
@BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift

<interfaces>
From `BeautySDK/Sources/BeautyExampleRenderer/main.swift`:
- `RenderCase { id: String, displayName: String, parameters: BeautyParameters }`
- `cases` is the ordered renderer inventory; output naming is `{fixtureStem}__{caseId}.png`.
- One `BeautyEngine.processResult(image:metadata:parameters:)` call is shared by every case.

From `BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift`:
- `SDKTestingFaceDetectionFixture` already exposes `.pairedObservedEyebrows`, `.leftOnlyObservedEyebrow`, `.rightOnlyObservedEyebrow`, `.missingObservedEyebrows`, `.malformedObservedEyebrows`, and `.noFace`.
- `SDKTestingFaceDetectionProvider` feeds those fixtures through the production detector/adapter/facade route and exposes aggregate invocation count only.

From `BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift`:
- `expectedRendererCaseIDs`, `fixtureNames`, `rendererCaseIDs(in:)`, and `rendererCaseSnippet(for:in:)` own ordered source isolation.
- Current active fixtures are exactly `portraits/e6.jpg` and `negatives/no-face-gradient.png`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 51-01-01: Lock and add the exact thirteen public eyebrow renderer cases</name>
  <files>BeautySDK/Sources/BeautyExampleRenderer/main.swift, BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift</files>
  <behavior>
    - "The ordered renderer inventory grows from 59 to exactly 72 unique IDs while all original 59 retain their relative order (D-02)."
    - "The six signed fields each have `_plus0p25` and `_minus0p25` cases; `eyebrowPeakDefinition` has only `_0p25`."
    - "Every new snippet contains one matching BeautyParameters initializer label, no second eyebrow/face/eye field, no internal module import, and no second facade call (D-09)."
    - "Fixture scope is exactly e6.jpg plus the separate no-face negative; e1 through e5 are rejected as active fixture names (D-01, D-03, D-04)."
  </behavior>
  <action>Write the failing ordered-inventory, one-field-isolation, fixture-scope, and no-face facade tests first, then add exactly `eyebrowYPosition_plus0p25`, `eyebrowYPosition_minus0p25`, `eyebrowThickness_plus0p25`, `eyebrowThickness_minus0p25`, `eyebrowLength_plus0p25`, `eyebrowLength_minus0p25`, `eyebrowSpacing_plus0p25`, `eyebrowSpacing_minus0p25`, `eyebrowHeadSpacing_plus0p25`, `eyebrowHeadSpacing_minus0p25`, `eyebrowTilt_plus0p25`, `eyebrowTilt_minus0p25`, and `eyebrowPeakDefinition_0p25` after the existing eye cases and before nose cases. Each case constructs only its same-named public `BeautyParameters` field at signed `±0.25` or positive `0.25`; per D-09/D-12 preserve the recursive fixture reader, flat naming, watermark, public-only imports, original 59 ordering, and the single shared facade call. Lock `fixtureNames` and repository checks to sole portrait `portraits/e6.jpg` plus `negatives/no-face-gradient.png`; assert all parked e1-e5 paths remain outside input and never restore or count them per D-01/D-03/D-04.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --disable-sandbox --jobs 1 --filter BeautyCoreTests.BeautyRendererOutputRegressionTests &amp;&amp; test "$(rg -n 'RenderCase\\(' BeautySDK/Sources/BeautyExampleRenderer/main.swift | wc -l | tr -d ' ')" -eq 72 &amp;&amp; test "$(rg -n 'engine\\.processResult\\(' BeautySDK/Sources/BeautyExampleRenderer/main.swift | wc -l | tr -d ' ')" -eq 1 &amp;&amp; test "$(find example-images/input/portraits -maxdepth 1 -type f | wc -l | tr -d ' ')" -eq 1 &amp;&amp; test -f example-images/input/portraits/e6.jpg &amp;&amp; /bin/zsh -lc 'for stem in e1 e2 e3 e4 e5; do test -f "example-images/parked-portraits/${stem}.png" &amp;&amp; test ! -e "example-images/input/portraits/${stem}.png" || exit 1; done' &amp;&amp; git diff --check</automated>
  </verify>
  <done>The renderer and XCTest agree on exactly 72 unique public cases, the thirteen additions are isolated at provisional 0.25, only e6 is an active portrait, and the no-face negative is explicit but not part of the 72-output portrait denominator.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 51-01-02: Strengthen representative public degradation and redaction evidence</name>
  <files>BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift</files>
  <behavior>
    - "Paired observed brows emit every field and preserve extent through one detector call."
    - "Left-only and right-only support keep per-side work but remove pair-only whole spacing without suppressing a safe sibling."
    - "Missing and malformed support remove requested eyebrow work while a fresh equivalent safe-sibling-only request has the same final output, domains, warnings, metrics, and extent."
    - "Public results contain only fixed reasons and aggregate counts, with no side, support, coordinate, endpoint, axis, apex, provider, or path payload (D-11)."
  </behavior>
  <action>Extend the existing eyebrow facade tables rather than adding another fixture seam. For paired support cover all seven fields; for `.leftOnlyObservedEyebrow` and `.rightOnlyObservedEyebrow` compare per-side `eyebrowYPosition` with pair-gated `eyebrowSpacing`; for `.missingObservedEyebrows` and `.malformedObservedEyebrows` pair each requested eyebrow case with an eligible safe color sibling and process an equivalent fresh sibling-only baseline. Per D-10/D-11 assert one detector call per request, same output extent and rendered bytes as the equivalent local no-op/sibling baseline where eyebrow work is ineligible, continuation of the safe sibling, exact local skipped-eyebrow aggregates, and absence of raw/derived support vocabulary. Preserve request-local valid→invalid→valid equality and do not modify testing support, production providers, caps, or diagnostics.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --disable-sandbox --jobs 1 --filter BeautyCoreTests.BeautyEngineGeometryFacadeTests &amp;&amp; test "$(git hash-object BeautySDK/Package.swift)" = "6f03b078816ad1f7a426e3f70d4f57503f3152e9" &amp;&amp; git diff --check</automated>
  </verify>
  <done>Representative no-face, missing, malformed, partial-pair, and sequential requests degrade locally through the public facade, keep extent and safe siblings, and expose aggregate-only evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Fixture filesystem → renderer | Active and parked biometric-adjacent portrait files must not be confused. |
| Public parameters → SDK facade | Each isolated case must traverse the production public facade without an internal bypass. |
| Testing fixtures → public result | Request-local support may authorize work internally, but only aggregate redacted results may escape. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-51-01 | Spoofing | renderer fixture discovery | mitigate | Lock `e6.jpg` as the sole portrait and reject parked e1-e5 as active inputs. |
| T-51-02 | Tampering | renderer case inventory | mitigate | Exact ordered 72-ID XCTest, duplicate rejection, and one-field snippet assertions. |
| T-51-03 | Elevation of Privilege | renderer facade route | mitigate | One `BeautyEngine.processResult` call and no internal SDK-module imports. |
| T-51-04 | Information Disclosure | degradation diagnostics | mitigate | Reuse redaction assertions and forbid side/support/coordinate/provider/path payloads. |
| T-51-05 | Denial of Service | malformed support | mitigate | Existing bounded adapter rejects malformed work locally while safe siblings continue. |
| T-51-SC | Tampering | SwiftPM package/dependencies | mitigate | No install task; pin unchanged `Package.swift` hash and add no target, package, resource, or model. |
</threat_model>

<verification>
- Focused renderer regression passes with exactly 72 ordered unique cases, thirteen eyebrow IDs, one facade call, e6-only portrait scope, and explicit separate no-face fixture.
- Focused facade regression passes representative paired/partial/missing/malformed/no-face and redaction contracts.
- The package manifest hash and diff hygiene remain unchanged.
</verification>

<success_criteria>
OUT-01 is implemented and OUT-03 has compiled representative degradation evidence without output acceptance, cap finalization, or product promotion.
</success_criteria>

<output>
Create `.planning/phases/51-public-facade-eyebrow-output-evidence/51-01-SUMMARY.md` when done.
</output>
