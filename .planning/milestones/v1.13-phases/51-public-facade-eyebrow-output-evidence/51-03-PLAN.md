---
phase: 51-public-facade-eyebrow-output-evidence
plan: "03"
type: execute
wave: 3
depends_on: ["51-02"]
files_modified:
  - .planning/phases/51-public-facade-eyebrow-output-evidence/check_eyebrow_renderer_outputs.py
  - .planning/phases/51-public-facade-eyebrow-output-evidence/51-EYEBROW-OUTPUT-EVIDENCE.md
  - BeautySDK/Sources/BeautyEffects/Warp/EyebrowWarpProvider.swift
  - BeautySDK/Tests/BeautyEffectsTests/EyebrowWarpProviderTests.swift
autonomous: true
requirements: [OUT-02, OUT-03]
must_haves:
  truths:
    - "A separate measurement render and independent clean strict render produce exactly 72 decoded e6 portrait outputs, while 13 no-face comparisons are reported separately within an exact 144-file total inventory (D-03, D-08, D-13)."
    - "Frozen decoded-pixel gates prove all thirteen cases visible inside the brow ROI, below budgets in protected eye/forehead-hair/background regions, correctly signed for six controls, and pairwise distinct across seven families (D-05, D-06, D-07)."
    - "The actual e6 baseline and all thirteen eyebrow output images are opened at original detail and inspected before any passing visual claim; reversed, spilled, imperceptible, or collapsed results block acceptance and trigger a root-cause correction (D-15, D-16)."
  artifacts:
    - path: .planning/phases/51-public-facade-eyebrow-output-evidence/check_eyebrow_renderer_outputs.py
      provides: "Frozen strict constants and accepted single-portrait semantic gates"
      contains: "EXPECTED_PORTRAIT_OUTPUT_COUNT"
    - path: .planning/phases/51-public-facade-eyebrow-output-evidence/51-EYEBROW-OUTPUT-EVIDENCE.md
      provides: "Commands, exact denominators, frozen metrics, strict results, and image-by-image visual review record"
      contains: "Visual Review"
  key_links:
    - from: .planning/phases/51-public-facade-eyebrow-output-evidence/check_eyebrow_renderer_outputs.py
      to: example-images/output/e6__geometryBaseline_noop.png
      via: "Every new e6 case uses the same decoded baseline and fixed regions"
      pattern: "BASELINE_CASE_ID"
    - from: .planning/phases/51-public-facade-eyebrow-output-evidence/51-EYEBROW-OUTPUT-EVIDENCE.md
      to: example-images/output
      via: "Exact reproduction commands and fourteen opened representative filenames"
      pattern: "e6__eyebrow|e6__geometryBaseline"
    - from: BeautySDK/Sources/BeautyEffects/Warp/EyebrowWarpProvider.swift
      to: .planning/phases/51-public-facade-eyebrow-output-evidence/check_eyebrow_renderer_outputs.py
      via: "Only a demonstrated semantic-direction/root-cause defect may trigger provider correction; helper thresholds never redefine product direction"
      pattern: "eyebrowSpacing|eyebrowTilt|eyebrowPeakDefinition"
---

<objective>
Calibrate and freeze the strict e6 pixel contract, accept an independent clean render, and perform the mandatory actual-image visual review.

Purpose: Convert the compiled eyebrow path into observed, non-circular public-facade evidence without allowing stale files, source assertions, or automated scores to substitute for visible semantics.
Output: Frozen helper constants, exactly accepted portrait/no-face results, and an aggregate evidence record backed by opened representative images.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/51-public-facade-eyebrow-output-evidence/51-CONTEXT.md
@.planning/phases/51-public-facade-eyebrow-output-evidence/51-02-SUMMARY.md
@.planning/phases/51-public-facade-eyebrow-output-evidence/check_eyebrow_renderer_outputs.py
@.planning/phases/50-independent-eyebrow-geometry-and-pipeline-integration/50-RESEARCH.md
@BeautySDK/Sources/BeautyEffects/Warp/EyebrowWarpProvider.swift
@BeautySDK/Tests/BeautyEffectsTests/EyebrowWarpProviderTests.swift

<interfaces>
The Phase 51 helper has three distinct modes:
- `--self-test`: adversarial contract tests only.
- `--measure`: emits fixed candidate metrics and must not close OUT-02.
- default strict mode: consumes only the committed immutable calibration object and returns nonzero on any missing/weak/reversed/spilled/collapsed evidence.

Phase 50 semantic signs to preserve:
- positive Y follows the existing image-Y convention;
- positive thickness expands and negative contracts the brow strip;
- positive length expands outer endpoints;
- positive whole spacing separates complete brows;
- positive head spacing separates canonical inner heads;
- positive tilt raises both canonical outer tails;
- positive peak moves only the stored apex neighborhood away from its endpoint chord.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 51-03-01: Measure once, freeze constants, and accept an independent clean e6 matrix</name>
  <files>.planning/phases/51-public-facade-eyebrow-output-evidence/check_eyebrow_renderer_outputs.py, .planning/phases/51-public-facade-eyebrow-output-evidence/51-EYEBROW-OUTPUT-EVIDENCE.md</files>
  <behavior>
    - "Measurement mode reports all thirteen baseline metrics, four protected-region budgets, six signed metrics, twenty-one family-pair metrics, and thirteen no-face comparisons without returning a requirement pass."
    - "Frozen floors sit below the weakest qualifying measurement with an explicit positive margin; protected-region ceilings sit above observed maxima with a bounded documented margin."
    - "After a second guarded clean render, strict mode accepts exactly 72/72 decoded same-dimension e6 portrait outputs and reports thirteen separate no-face no-ops, never 85 portrait outputs or 144 portrait outputs."
    - "Any stale e1-e5 output or gallery artifact cannot satisfy the matrix and is absent from the accepting output root (D-13)."
  </behavior>
  <action>Build the renderer, resolve `example-images/output` physically, require it equals the repository allow-list and is ignored, then clean only that resolved output root per D-13. Render all 72 cases across the exact two active inputs, run `--measure`, derive one fixed e6 brow ROI plus fixed protected eye/forehead-hair/background regions, freeze positive visibility/direction/family floors and bounded protected-region ceilings below/above the measured extrema with explicit margins, and commit the immutable calibration object per D-05 through D-08. Clean the same allow-listed output root again, rerender independently, and run default strict mode. Require exactly 144 regular nonempty generated files, fully decode and dimension-check exactly 72 e6 portrait outputs, decode the baseline plus thirteen no-face cases for separate no-op evidence, pass thirteen visibility/locality, six signed-direction, twenty-one positive-family distinction, and thirteen negative checks, and reject any e1-e5 filename. Record commands, dimensions, exact denominators, regions, floors/ceilings, measured extrema/margins, and conservative Phase 52 ownership in `51-EYEBROW-OUTPUT-EVIDENCE.md`; do not call measurement output a pass.</action>
  <verify>
    <automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift build --package-path BeautySDK --product BeautyExampleRenderer &amp;&amp; OUTPUT_ROOT="$(cd example-images/output &amp;&amp; pwd -P)" &amp;&amp; test "$OUTPUT_ROOT" = "$(pwd -P)/example-images/output" &amp;&amp; git check-ignore -q example-images/output &amp;&amp; find "$OUTPUT_ROOT" -mindepth 1 -delete &amp;&amp; DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift run --package-path BeautySDK BeautyExampleRenderer --input example-images/input --output example-images/output &amp;&amp; test "$(find "$OUTPUT_ROOT" -maxdepth 1 -type f -name '*.png' | wc -l | tr -d ' ')" -eq 144 &amp;&amp; test -z "$(find "$OUTPUT_ROOT" -maxdepth 1 -type f \( -name 'e1__*' -o -name 'e2__*' -o -name 'e3__*' -o -name 'e4__*' -o -name 'e5__*' \) -print -quit)" &amp;&amp; python3 .planning/phases/51-public-facade-eyebrow-output-evidence/check_eyebrow_renderer_outputs.py --input example-images/input --output example-images/output --renderer-source BeautySDK/Sources/BeautyExampleRenderer/main.swift &amp;&amp; git diff --check</automated>
  </verify>
  <done>An independent clean strict run accepts 72/72 e6 portrait outputs with frozen brow-local, protected-region, signed-direction, and family-distinction evidence; thirteen no-face results remain a separately reported safety set inside the exact 144-file total inventory.</done>
</task>

<task type="auto">
  <name>Task 51-03-02: Open and review every representative e6 eyebrow image before the visual verdict</name>
  <files>.planning/phases/51-public-facade-eyebrow-output-evidence/51-EYEBROW-OUTPUT-EVIDENCE.md, BeautySDK/Sources/BeautyEffects/Warp/EyebrowWarpProvider.swift, BeautySDK/Tests/BeautyEffectsTests/EyebrowWarpProviderTests.swift, .planning/phases/51-public-facade-eyebrow-output-evidence/check_eyebrow_renderer_outputs.py</files>
  <action>After the independent strict run, use the image-viewing capability at original detail to open `e6__geometryBaseline_noop.png` and each of the thirteen `e6__eyebrow*.png` outputs; a contact sheet may assist orientation but never replaces opening the actual files. Per D-15 inspect the positive/negative pair side by side for vertical position, thickness, length, whole spacing, head spacing, and tilt, then inspect peak against baseline and thickness, checking intended direction, visible but brow-local displacement, protected eyes/forehead-hair/background, and family separation. Record a fourteen-row filename/observation table and explicit review verdict in the evidence document. Per D-16, if any direction is reversed, spill is visible, movement is imperceptible, or spacing/head-spacing or thickness/peak collapses, leave the verdict non-passing and use the execution deviation protocol to correct the root cause in provider/tests/helper, rerun focused provider tests plus the complete measure→freeze→clean strict cycle, and reopen the corrected images; never raise the provisional cap, lower a gate to admit the defect, or replace the e6 fixture.</action>
  <verify>
    <automated>test "$(rg -c '^\| `e6__(geometryBaseline_noop|eyebrow[^`]*)\\.png`' .planning/phases/51-public-facade-eyebrow-output-evidence/51-EYEBROW-OUTPUT-EVIDENCE.md)" -eq 14 &amp;&amp; rg -q '^Visual review verdict: PASS$' .planning/phases/51-public-facade-eyebrow-output-evidence/51-EYEBROW-OUTPUT-EVIDENCE.md &amp;&amp; swift test --package-path BeautySDK --disable-sandbox --jobs 1 --filter BeautyEffectsTests.EyebrowWarpProviderTests &amp;&amp; python3 .planning/phases/51-public-facade-eyebrow-output-evidence/check_eyebrow_renderer_outputs.py --input example-images/input --output example-images/output --renderer-source BeautySDK/Sources/BeautyExampleRenderer/main.swift &amp;&amp; git diff --check</automated>
    <human-check>Open the accepted `e6__geometryBaseline_noop.png` and all thirteen `e6__eyebrow*.png` files at original detail. Confirm the six signed pairs move in opposite intended directions, every result is visible and brow-local, eyes/forehead-hair/background remain protected, whole spacing is not head spacing, thickness is not peak, and the evidence table accurately describes what is visible. Do not approve if any actual file was not opened.</human-check>
  </verify>
  <done>All fourteen actual e6 images have been opened and reviewed; automated and visual evidence agree on direction, locality, visibility, and seven-family distinction, or the task remains incomplete with the observed gap recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Ignored output root → accepting helper | Stale or foreign files can masquerade as fresh evidence unless the physical root is guarded and cleaned. |
| Measurement run → frozen strict run | The same generated bytes must not both select and satisfy thresholds. |
| Pixel metrics → visual claim | Numerical differences do not establish that the intended semantics are visible and correctly directed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-51-12 | Tampering | ignored output root | mitigate | Resolve exact physical allow-list, require ignore, clean only that root, and reject e1-e5 names. |
| T-51-13 | Repudiation | measurement/strict chronology | mitigate | Two guarded clean renders; evidence records commands, extrema, margins, and strict results separately. |
| T-51-14 | Tampering | semantic thresholds | mitigate | Immutable positive constants, fixed predicates, protected budgets, and adversarial self-tests. |
| T-51-15 | Spoofing | watermark/label signal | mitigate | Label band is excluded and cannot satisfy any visibility, direction, or distinction gate. |
| T-51-16 | Information Disclosure | evidence document | mitigate | Record aggregate metrics and generated filenames only, never raw coordinates/pixels/support/provider payloads. |
| T-51-17 | Repudiation | visual verdict | mitigate | Open fourteen actual files, record one row per file, require explicit verdict, and retain end-of-phase human check. |
| T-51-SC | Tampering | build/dependency surface | mitigate | Use existing SwiftPM graph and standard-library Python; no install or manifest modification. |
</threat_model>

<verification>
- Measurement and strict acceptance are separate guarded clean renders.
- Strict helper passes exact 72 portrait outputs, fixed semantic/protected-region gates, and thirteen separately reported no-face comparisons.
- Evidence contains fourteen actual filenames and observations only after those files were opened; end-of-phase human review confirms the claim.
- Any visual contradiction blocks the plan rather than being converted into a source-level or threshold-only pass.
</verification>

<success_criteria>
OUT-02 has both bounded decoded evidence and actual-image review for the sole active portrait, with exact count separation and no passing visual claim based only on provider tests or source cardinality.
</success_criteria>

<output>
Create `.planning/phases/51-public-facade-eyebrow-output-evidence/51-03-SUMMARY.md` when done.
</output>
