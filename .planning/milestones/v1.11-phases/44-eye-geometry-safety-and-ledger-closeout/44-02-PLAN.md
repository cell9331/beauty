---
phase: 44-eye-geometry-safety-and-ledger-closeout
plan: "02"
type: execute
wave: 2
depends_on: ["44-01"]
files_modified:
  - BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
autonomous: true
requirements: [EYE-20, EYE-21]
must_haves:
  truths:
    - "D-11/D-14: the fully eligible retained set is exactly total 10.70, count 33, scale 1/10.70 with aggregate-only diagnostics."
    - "D-12/D-13: at most 28 monotonic provider removals occur, no field is re-added/double-scaled, and strengths equal final emissions."
  artifacts:
    - path: BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift
      provides: exact retained arithmetic evidence
    - path: BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift
      provides: 28-removal and provider equality evidence
  key_links:
    - from: BeautyEffectResolver.resolveGeometryConflict
      to: GeometryConflictResolver
      via: one retained unscaled baseline
    - from: final effective strengths
      to: eye/nose/mouth provider emissions
      via: sequential final-scale sanitization
  prohibitions:
    - statement: "No second conflict path, public total metric, re-added field, or sign loss."
      status: resolved
      verification: exact-test
---

<objective>Prove one provider-eligible retained baseline for face, fourteen eye, six nose, and eight mouth geometry, with exact full-set arithmetic, field-local removals, signed preservation, and a monotonic at-most-28 removal convergence bound.</objective>

<threat_model><scope>ASVS L1 algorithmic abuse and integrity: double weakening, empty-work counting, sign loss, non-convergent masks, and sibling-supported leakage.</scope><blocking_rule>Any final effective-strength/emission mismatch, second weakening path, re-added field, or bound violation blocks Plans 03-04.</blocking_rule><assets>Resolver retained baseline, provider field emissions, exact aggregate totals/counts/scales, signed strengths.</assets><trust_boundaries>Public normalized strengths cross provider eligibility and combined conflict scaling before unified local warp dispatch.</trust_boundaries><threats>Double scaling, provider-empty fields included in totals, signed direction lost, late removal reintroduced, safe siblings removed.</threats><mitigations>Exact 10.70/33 fixture, adversarial one-field removal cases, monotonic iteration assertions, provider emission equality.</mitigations><verification>Focused conflict/combined XCTest plus full SwiftPM.</verification><residual_risk>Source-boundary and owner promotion gates remain Plan 03/04.</residual_risk></threat_model>

<tasks>
<task type="test">
  <name>Task 44-02-01: Lock complete retained-set arithmetic</name>
  <decision_refs>D-11, D-14</decision_refs>
  <done>Exact full-set total/count/scale, warnings, and sign tests satisfy this task.</done>
  <files>BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift</files>
  <read_first>BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift; BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift; BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift; BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift</read_first>
  <action>Create a fully eligible capped `BeautyEffectiveStrengths` fixture containing five face, all fourteen eye, all six nose, and all eight mouth fields. Assert private-mirrored magnitude total exactly `10.70`, retained field count exactly `33`, combined scale exactly `1/10.70`, `combined_geometry_weakened` warning presence, exact weakened count, and preservation of signed `eyeTilt`, `chinLength`, noseTipSize, and mouth directions. Assert no conflict warning/scale appears below threshold and no public metric beyond existing aggregate keys is added.</action>
  <acceptance_criteria>Full eligible fixture reports exact 10.70 total, 33 retained fields, exact reciprocal scale and expected aggregate warning/count; all signed values retain sign; below-threshold fixture remains unchanged; test arithmetic matches source field inventory exactly once.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.GeometryConflictResolverTests && git diff --check</automated></verify>
</task>
<task type="test">
  <name>Task 44-02-02: Prove monotonic 28-removal convergence and provider equality</name>
  <decision_refs>D-12, D-13</decision_refs>
  <done>All removal masks are bounded/monotonic and final strengths equal emissions.</done>
  <files>BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift, BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift</files>
  <read_first>BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift; BeautySDK/Sources/BeautyEffects/Warp/EyeWarpProvider.swift; BeautySDK/Sources/BeautyEffects/Warp/NoseWarpProvider.swift; BeautySDK/Sources/BeautyEffects/Warp/MouthWarpProvider.swift; BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift; BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift</read_first>
  <action>Add adversarial field-removal fixtures that make each of fourteen eye, six nose, and eight mouth emissions empty after scaling. Assert the resolver loop never exceeds exactly 28 passes/removals, never re-adds a cleared field, and returns final effective strengths equal to the same fields whose provider emissions are non-empty. For complete and representative missing-pupil/provider-empty sets assert exact final totals/counts/scales, active/skipped domains, warning/metric presence, point counts, and emitted vector equality. Preserve positive and negative directions and safe face/non-eye sibling continuation.</action>
  <acceptance_criteria>All 28 potential removal paths are covered or table-driven; complete and adversarial retained sets have exact arithmetic; final strengths and field emissions are equal; cleared fields contribute zero to totals/counts/warnings/metrics/domains/dispatch; no sign is lost and loop is bounded/monotonic.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.CombinedEffectSafetyTests && swift test --package-path BeautySDK --filter BeautyEffectsTests.MissingLandmarkDegradationTests && git diff --check</automated></verify>
</task>
<task type="test">
  <name>Task 44-02-03: Regress resolver/facade safe-domain continuation under mixed masks</name>
  <decision_refs>D-09, D-10, D-13</decision_refs>
  <done>Mixed masks preserve safe domains, scalar-only facade, extent, and redaction.</done>
  <files>BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift, BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift</files>
  <read_first>BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift; BeautySDK/Sources/BeautySDK/BeautyEngine.swift; BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift; BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift; .planning/phases/43-public-facade-eye-geometry-output-evidence/43-VERIFICATION.md</read_first>
  <action>Exercise mixed face+eye+nose+mouth requests with one or more eye fields removed late, no face, reused/stale eye, and missing pupil. Assert safe face/nose/mouth/color/filter domains continue according to their policies, public facade remains scalar-only and extent-safe, and no stale eye vectors or raw geometry enter result metadata. Keep Phase 43 output/helper evidence unchanged and defer strict output reruns to Plan 03.</action>
  <acceptance_criteria>Mixed masks retain safe siblings, no-face/reused/stale eye behavior remains explicit, no raw geometry appears, and resolver/facade focused tests pass without adding renderer cases or API types.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.CombinedEffectSafetyTests && swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyEngineGeometryFacadeTests && git diff --check</automated></verify>
</task>
</tasks>

<verification>Run all Plan 44-02 focused suites followed by `swift test --package-path BeautySDK`; inspect the exact field list in `GeometryConflictResolver` and ensure no second conflict path or public metric was introduced.</verification>
<success_criteria>EYE-21 exact one-baseline convergence, signed preservation, field-local exclusion, and 28-removal bound are executable and green; EYE-20 mixed safe-domain continuation remains green; no boundary/promotion edits occur.</success_criteria>

## Artifacts this phase produces

- Extended exact retained-set, provider-equality, and mixed-mask XCTest evidence.
- No new production API, package, renderer matrix, or generated artifact.
