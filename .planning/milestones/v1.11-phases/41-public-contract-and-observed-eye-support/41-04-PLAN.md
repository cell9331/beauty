---
phase: 41-public-contract-and-observed-eye-support
plan: "04"
type: execute
wave: 3
depends_on: ["41-03"]
files_modified:
  - .planning/phases/41-public-contract-and-observed-eye-support/check_eye_support_boundaries.py
  - DESIGN.md
  - SECURITY.md
  - RELIABILITY.md
  - PRODUCT_SENSE.md
  - PLANS.md
autonomous: true
requirements: [EYE-01, EYE-02, EYE-03, EYE-04, EYE-05, EYE-06, EYE-07]
must_haves:
  truths:
    - "A checked-in fail-closed helper proves Phase 41 did not change Package.swift or BeautyDemo from baseline f1c28fa and classifies rg exit 0/1/>1 without treating tool errors as clean results."
    - "All active BeautySDK source targets are scanned for public/SPI support, Codable/persistence, raw diagnostic geometry, network/cloud, and commercial paths; every unclassified match blocks completion."
    - "example-images/output and example-images/gallery remain untracked and unstaged, with non-ignored untracked additions rejected and representative paths still ignored."
    - "DESIGN, SECURITY, RELIABILITY, PRODUCT_SENSE, and PLANS agree on the 48-field contract, private support lifecycle, validation ceilings, degradation wiring, and downstream non-claims."
  artifacts:
    - "check_eye_support_boundaries.py provides deterministic live checks and adversarial --self-test coverage."
    - "DESIGN.md, SECURITY.md, RELIABILITY.md, PRODUCT_SENSE.md, and PLANS.md contain synchronized Phase 41 contract evidence."
  key_links:
    - "Phase baseline f1c28fa -> Package.swift/BeautyDemo diff checks prevent dependency, target, or Demo drift."
    - "All BeautySDK/Sources targets -> fail-closed classified scans -> zero public/diagnostic/persistent biometric-adjacent leakage."
    - "Git tracked/staged/untracked queries -> actual example-images/output and example-images/gallery roots -> artifact containment."
---

<objective>
Close Phase 41 with a reusable fail-closed boundary gate and synchronized owning contracts.

Purpose: Prove the compatible scalar/private-support implementation stayed inside its SDK-core, privacy, dependency, Demo, and artifact boundaries before Phase 42 begins.
Output: Checked-in boundary helper, self-test/live evidence, and synchronized root contracts/ledger.
</objective>

## Artifacts this phase produces

- `.planning/phases/41-public-contract-and-observed-eye-support/check_eye_support_boundaries.py` with `--self-test` and live modes.
- Updated `DESIGN.md`, `SECURITY.md`, `RELIABILITY.md`, `PRODUCT_SENSE.md`, and `PLANS.md` Phase 41 entries.

<goal_backward>
Goal: every Phase 41 contract and boundary is independently enforceable. Truths: full tests pass, helper self-tests reject adversarial mutations, live scans pass, baseline-owned files are unchanged, artifacts remain contained, and owner docs agree. Artifacts: one checked-in helper plus five synchronized ledgers. Key links: implementation evidence to contracts and baseline/git/source scans to fail-closed completion.
</goal_backward>

<read_first>
AGENTS.md; PLANS.md; .planning/PROJECT.md; .planning/ROADMAP.md; .planning/REQUIREMENTS.md; .planning/STATE.md; 41-CONTEXT.md; 41-RESEARCH.md; 41-PATTERNS.md; 41-01-SUMMARY.md; 41-02-SUMMARY.md; 41-03-SUMMARY.md; .planning/milestones/v1.9-phases/37-nose-safety-boundary-and-branch-closeout/check_nose_safety_boundaries.py; DESIGN.md; SECURITY.md; RELIABILITY.md; PRODUCT_SENSE.md
</read_first>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/41-public-contract-and-observed-eye-support/41-CONTEXT.md
@.planning/phases/41-public-contract-and-observed-eye-support/41-RESEARCH.md
@.planning/phases/41-public-contract-and-observed-eye-support/41-PATTERNS.md
@.planning/milestones/v1.9-phases/37-nose-safety-boundary-and-branch-closeout/check_nose_safety_boundaries.py
@DESIGN.md
@SECURITY.md
@RELIABILITY.md
@PRODUCT_SENSE.md
@PLANS.md
</context>

## Multi-source coverage audit

| Source | Coverage in this plan |
| --- | --- |
| ROADMAP goal | The phase-level gate proves ten compatible public controls and validated private support without biometric-adjacent exposure or scope drift. |
| Requirements | EYE-01–EYE-04 are checked through exact public inventory/owner facts and full tests; EYE-05–EYE-07 are checked through private-support, diagnostic, persistence, gating, and full-suite evidence. |
| Research constraints | Preserve existing SwiftPM/Vision/CoordinateMapper/observation/adapter seams, no dependency/target/public support type, deterministic synthetic evidence, fixed support ceilings, and no provider/output/final-cap/promotion scope. |
| CONTEXT — Public Scalar Contract | Docs and helper assert exactly ten named fields, signed eyeTilt, nine positive-only fields, zero defaults, and 48 stored fields. |
| CONTEXT — Observed Eye-Support Model | Docs record one mapper conversion, side-aware request-scoped private support, optional pupils, and no synthetic pupil. |
| CONTEXT — Validation and Degradation | Docs record locked contour/pupil support ceilings, pupil-local invalidation, empty FaceGeometry side eligibility, no proxy fallback, and the existing resolver complete-eye gate. |
| CONTEXT — Privacy and Ownership | Helper blocks public/SPI/Codable/persistent/diagnostic geometry, dependency/network/commercial/Demo drift, and tracked/staged/non-ignored-untracked generated artifacts. |
| CONTEXT — the agent's Discretion | Reuse the Phase 37 classified-scan/self-test pattern and keep allowlists narrow, path-specific, and adversarially tested. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 41-04-01: Build and adversarially self-test the eye-support boundary helper</name>
  <files>.planning/phases/41-public-contract-and-observed-eye-support/check_eye_support_boundaries.py</files>
  <behavior>
    - The rg wrapper treats exit 0 as matches requiring classification, exit 1 as clean no-match, and every other status as a blocking tool error.
    - Baseline comparison fails when `BeautySDK/Package.swift` or any `BeautyDemo` file differs from commit f1c28fa.
    - Public/SPI support, Codable/persistence geometry, raw diagnostic geometry, network/cloud/commercial paths, or artifact tracking/staging/non-ignored-untracked mutations each fail independently.
  </behavior>
  <action>Create `check_eye_support_boundaries.py`, modeled on the Phase 37 helper, with repository/path containment, structured Result/SearchResult values, explicit subprocess exit classification, and `--self-test` mutation fixtures. Live mode must verify baseline commit f1c28fa exists; compare `BeautySDK/Package.swift` and `BeautyDemo` against that commit; scan all active `BeautySDK/Sources` targets (including BeautyCore, BeautyDetection, BeautyEffects, BeautyRender, BeautyResources, BeautySDK, and the example renderer) for public/SPI eye-support or raw-geometry exposure, support types conforming to Codable/Encodable/Decodable, geometry-related logging/metrics/warnings/errors/descriptions and persistence APIs, network/cloud APIs, and commercial/entitlement paths. Permit only explicit existing path-and-literal allowlists and fail every unclassified match. Check actual `example-images/output` and `example-images/gallery` (plus existing staging/quarantine siblings) with `git ls-files`, `git diff --cached --name-only`, and `git ls-files --others --exclude-standard`; require zero tracked, staged, or non-ignored untracked generated artifacts and confirm representative output/gallery paths remain ignored. Self-tests must cover clean, classified, unclassified, rg exit 2/tool-missing, baseline mutation, public/Codable/diagnostic/network/commercial mutations, Demo/manifest drift, and tracked/staged/untracked artifact failures.</action>
  <acceptance_criteria>`python3 .../check_eye_support_boundaries.py --self-test` exits 0 only after every positive fixture passes and every adversarial mutation is rejected; no rg/tool error, missing path, or unclassified match can produce a clean result.</acceptance_criteria>
  <verify><automated>python3 .planning/phases/41-public-contract-and-observed-eye-support/check_eye_support_boundaries.py --self-test</automated></verify>
  <done>The checked-in gate has deterministic evidence that each security/scope/artifact failure mode blocks.</done>
</task>

<task type="auto">
  <name>Task 41-04-02: Synchronize owner contracts and run the full live gate</name>
  <files>DESIGN.md, SECURITY.md, RELIABILITY.md, PRODUCT_SENSE.md, PLANS.md</files>
  <action>Record the exact 48-field public inventory, zero/legacy compatibility, package-private request-scoped observed contour/pupil lifecycle, one-mapper conversion, support-validation ceilings (not visual caps), pupil-local degradation, empty-side/no-proxy-fallback wiring into the resolver complete-eye gate, fixed aggregate diagnostics, and Phase 41 evidence/non-claims in the single owning documents. PRODUCT_SENSE acceptance must remain SDK-core and PLANS must record observed test/helper results without claiming provider transforms, output evidence, final caps, promotion, Demo, device, commercial, packaging, shipping, or launch readiness. Run full SwiftPM tests, helper self-tests, and the live helper after documentation synchronization.</action>
  <acceptance_criteria>All five owners state the same Phase 41 contract; the full suite, helper self-test, live baseline/source/artifact scans, and diff hygiene pass; downstream scope remains explicitly deferred.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK &amp;&amp; python3 .planning/phases/41-public-contract-and-observed-eye-support/check_eye_support_boundaries.py --self-test &amp;&amp; python3 .planning/phases/41-public-contract-and-observed-eye-support/check_eye_support_boundaries.py &amp;&amp; rg -n 'eyeHeight|eyeLength|upperEyelidLift|pupilSize|gazeCorrection|lowerEyelidDrop|eyeTilt|innerCornerOpen|outerCornerOpen|eyeSymmetry' DESIGN.md SECURITY.md RELIABILITY.md PRODUCT_SENSE.md PLANS.md &amp;&amp; git diff --check</automated></verify>
  <done>Phase 41 contracts and fail-closed boundaries are synchronized and proven before Phase 42.</done>
</task>

</tasks>

<threat_model>
ASVS L1 applies to boundary validation and evidence integrity. Block high severity for a scan/tool error, unclassified source match, baseline drift, public/SPI/Codable/persistent support, raw diagnostic geometry, network/commercial path, Demo change, or tracked/staged/non-ignored-untracked generated artifact. No raw geometry or biometric-adjacent data may appear on public, diagnostic, or persistent surfaces; helper self-tests and live scans must both pass.
</threat_model>

<verification>
Run the full SwiftPM suite, helper `--self-test`, helper live mode, owner-fact scans, and `git diff --check`. Record observed counts and every live check disposition in `41-04-SUMMARY.md`.
</verification>

<success_criteria>
Phase 41 is contract-synchronized and independently fail-closed with unchanged manifest/Demo baseline, clean active-source/privacy/commercial scans, contained output/gallery roots, and no downstream scope claim.
</success_criteria>

<output>Create `41-04-SUMMARY.md` with full test count, helper self-test/live check totals, baseline commit, artifact dispositions, owner updates, and explicit deferred-scope statement.</output>
