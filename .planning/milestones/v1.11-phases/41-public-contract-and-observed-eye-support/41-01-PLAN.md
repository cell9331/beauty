---
phase: 41-public-contract-and-observed-eye-support
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift
autonomous: true
requirements: [EYE-01, EYE-02, EYE-03, EYE-04]
must_haves:
  truths:
    - "Nine new positive-only eye scalars and signed eyeTilt are independently stored, finite-normalized, and default to zero without aliases."
    - "Legacy source initialization, bundled presets, and 38-key JSON decode all remain neutral while a complete 48-field value round-trips exactly."
    - "With all ten new values at zero, shipped eye fields and existing provider behavior remain byte-for-byte equivalent to the pre-Phase-41 baseline."
  artifacts:
    - "BeautyParameters exposes exactly 48 stored fields (47 numeric plus filterId), including the ten named eye fields."
    - "BeautyParametersTests contains independent range, inventory, legacy decode, and round-trip coverage."
  key_links:
    - "Initializer, CodingKeys, decoder defaults, normalized(), and synthesized Codable all include the same ten fields."
    - "Neutral-parameter regression compares existing eye provider output before and after adding the fields."
---

<objective>
Establish the compatibility-safe public contract for the ten unresolved eye geometry controls and prove that zero defaults preserve shipped behavior.

Purpose: Give later geometry phases independent scalar inputs without changing existing source, JSON, preset, or vector behavior.
Output: Updated BeautyParameters model and executable core contract tests.
</objective>

## Artifacts this phase produces

- `BeautyParameters.eyeHeight`, `.eyeLength`, `.upperEyelidLift`, `.pupilSize`, `.gazeCorrection`, `.lowerEyelidDrop`, `.eyeTilt`, `.innerCornerOpen`, `.outerCornerOpen`, and `.eyeSymmetry` stored fields, coding keys, initializer arguments, decoder paths, and normalized-copy wiring.
- Expanded `BeautyParametersTests` compatibility/inventory fixtures and the neutral `EyeWarpProviderTests` regression.

<goal_backward>
Goal: hosts can configure ten independent eye controls while legacy behavior stays neutral. Truths: 48-field inventory, finite normalization, legacy decode, and zero-default shipped-eye equality. Artifacts: BeautyParameters and its contract tests. Key links: initializer/CodingKeys/decoder/normalized paths and neutral EyeWarpProvider regression.
</goal_backward>

<read_first>
AGENTS.md; PLANS.md; .planning/PROJECT.md; .planning/ROADMAP.md; .planning/REQUIREMENTS.md; .planning/STATE.md; 41-CONTEXT.md; 41-RESEARCH.md; BeautyParameters.swift; BeautyParametersTests.swift; EyeWarpProviderTests.swift
</read_first>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/41-public-contract-and-observed-eye-support/41-CONTEXT.md
@.planning/phases/41-public-contract-and-observed-eye-support/41-RESEARCH.md
@BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift
@BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift
</context>

## Multi-source coverage audit

| Source | Coverage in this plan |
| --- | --- |
| ROADMAP goal | Ten host-configurable eye scalars are source/JSON compatible and neutral at zero; observed-support work is delivered by Plans 02-03. |
| Requirements | EYE-01 and EYE-02 are range/default/independence tests; EYE-03 is 38-to-48 compatibility; EYE-04 is neutral shipped-eye regression. |
| Research constraints | Reuse BeautyParameters clamp helpers and Codable seams; no dependency, target, public geometry type, provider transform, facade output, or Demo work. |
| CONTEXT — Public Scalar Contract | Add exactly the ten named fields; eyeTilt is signed [-1,1], the other nine are [0,1], all default/missing-key to zero, and inventory is 47 numeric plus filterId. |
| CONTEXT — Observed Eye-Support Model | No support model is added here; Plans 02-03 carry private request-scoped support through existing seams. |
| CONTEXT — Validation and Degradation | No provider eligibility is changed here; zero/default compatibility is tested so later field-local degradation can compose safely. |
| CONTEXT — Privacy and Ownership | Scalars remain scalar-only public API; no coordinates, diagnostics, persistence, dependency, network, render pass, or facade result is introduced. |
| CONTEXT — the agent's Discretion | Preserve existing initializer ordering and choose table-driven XCTest fixtures consistent with current tests. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 41-01-01: Add the ten defaulted normalized stored fields</name>
  <files>BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift</files>
  <behavior>
    - Each of eyeHeight, eyeLength, upperEyelidLift, pupilSize, gazeCorrection, lowerEyelidDrop, innerCornerOpen, outerCornerOpen, and eyeSymmetry accepts only finite values in 0...1 and falls back to zero for non-finite input.
    - eyeTilt accepts both signed directions in -1...1 and falls back to zero when non-finite.
    - Existing call sites remain source-compatible because all ten initializer arguments default to zero and missing decoder keys decode to zero.
  </behavior>
  <action>Extend the existing stored-property declaration, CodingKeys, full labeled initializer, missing-key decoder helper calls, and normalized() copy with exactly the ten named fields. Use the established clampUnit helper for nine positive-only fields and clampSigned for eyeTilt; preserve argument order, preset text, and all existing fields. Do not add aliases, public support types, setters, persistence, or geometry logic.</action>
  <acceptance_criteria>All ten names compile as independent stored properties; finite values normalize to the locked ranges, non-finite values become zero, and old source call sites compile unchanged.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyParametersTests</automated></verify>
  <done>The model compiles with ten independent default-zero fields and all normalization/decoding paths agree on their ranges.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 41-01-02: Prove inventory, legacy compatibility, independence, and neutral eye behavior</name>
  <files>BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift, BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift</files>
  <behavior>
    - Mirror(reflecting:) reports exactly 48 stored properties and every new field starts at zero.
    - A legacy complete 38-key payload decodes ten zeros; a complete 48-field payload with unequal values round-trips without aliasing.
    - Non-finite/overflow and both eyeTilt directions are covered, and all ten values survive normalized() independently.
    - Zeroing all new fields leaves the existing four shipped eye controls and provider control points unchanged.
  </behavior>
  <action>Extend the established table-driven contract tests and add a neutral regression assertion in EyeWarpProviderTests. Keep accuracy at the existing tolerance, assert exact JSON key count and legacy missing-key neutrality, and compare only the existing shipped eye output/control points so this phase does not claim provider transforms for the new fields.</action>
  <acceptance_criteria>The reflected inventory is 48, legacy 38-key decode yields ten zeros, unequal 48-field round-trip is exact, and zero new fields preserve the shipped provider output.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyParametersTests &amp;&amp; swift test --package-path BeautySDK --filter BeautyEffectsTests.EyeWarpProviderTests &amp;&amp; git diff --check</automated></verify>
  <done>EYE-01 through EYE-04 have executable compatibility and neutral-behavior evidence with no public geometry leakage.</done>
</task>

</tasks>

<threat_model>
ASVS L1 applies to public model input and serialized compatibility. Block high severity for public biometric-adjacent geometry exposure, persistence, dependency/network drift, or unsafe normalization bypass. No raw geometry or biometric-adjacent data may appear on public, diagnostic, or persistent surfaces; validate finite ranges at every construction/normalization seam and expose only scalar fields.
</threat_model>

<verification>
Run the focused core and eye-provider tests, then run `swift test --package-path BeautySDK` after Plans 02-03 merge. Confirm no preset files, package manifest, public geometry symbols, or generated artifacts changed.
</verification>

<success_criteria>
The ten named fields are independently represented in a 48-field model, legacy payloads remain neutral, all values normalize deterministically, and zero defaults preserve shipped eye behavior.
</success_criteria>

<output>Create `41-01-SUMMARY.md` with test counts, inventory/compatibility evidence, and an explicit statement that new provider transforms remain deferred to Phase 42.</output>
