---
phase: 23-performance-and-reliability-gates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - BeautyDemo/BeautyDemoTests/CameraBeautyPipelineTests.swift
  - BeautyDemo/BeautyDemoTests/ImageEditorPipelineTests.swift
autonomous: true
requirements:
  - PERF-02
  - PERF-03
  - PERF-04
  - PERF-05
must_haves:
  truths:
    - "D-10 PERF-03: CameraBeautyPipeline.reset() and still-image recovery have direct regression evidence."
    - "D-15 PERF-02: Backpressure evidence preserves in-flight limiting, one latest pending frame, dropped-frame accounting, and latest-frame-wins behavior."
    - "D-08 PERF-04: Demo simulator evidence is secondary and blocker-honest when the local Metal Toolchain remains unavailable."
    - "D-13 D-16 PERF-05: Demo test assertions and blocker summaries avoid raw path/error leakage and release-grade overclaims."
  artifacts:
    - "CameraBeautyPipelineTests PERF-02/PERF-03 test methods provide Demo pipeline backpressure/reset evidence."
    - "ImageEditorPipelineTests PERF-03 test method provides still-image selection-failure recovery evidence."
  key_links:
    - "CameraBeautyPipelineTests -> CameraBeautyPipeline.enqueue/reset/waitUntilIdle verifies realtime latest-frame-wins and recovery."
    - "ImageEditorPipelineTests -> ImageEditorPipeline.process/waitUntilIdle verifies stale-work and selection-failure recovery."
---

<objective>
Preserve Demo-side realtime and still-image reliability behavior while Phase 23 adds performance evidence.

Purpose: PERF-02 and the Demo portions of PERF-03/PERF-04 need reproducible tests or documented blocker-honest commands without making live camera, simulator UI automation, or physical iPhone evidence primary.
Output: Focused Demo pipeline regression tests and exact xcodebuild pass/blocker verification.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@FRONTEND.md
@RELIABILITY.md
@SECURITY.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-performance-and-reliability-gates/23-CONTEXT.md
@.planning/phases/23-performance-and-reliability-gates/23-RESEARCH.md
@.planning/phases/23-performance-and-reliability-gates/23-VALIDATION.md
@.planning/phases/23-performance-and-reliability-gates/23-PATTERNS.md
@BeautyDemo/BeautyDemo/Camera/CameraBeautyPipeline.swift
@BeautyDemo/BeautyDemo/Editor/ImageEditorPipeline.swift
@BeautyDemo/BeautyDemoTests/CameraBeautyPipelineTests.swift
@BeautyDemo/BeautyDemoTests/ImageEditorPipelineTests.swift
@.planning/evidence/v1.4/VISUAL-EVIDENCE.md
</context>

## Artifacts this phase produces

- `BeautySDK/Tests/BeautyCoreTests/BeautyPerformanceEvidenceTests.swift`
- `BeautyPerformanceEvidenceTests`
- `PerformanceEvidenceCase`
- `PerformanceTimingSummary`
- `PerformanceMemoryTrend`
- `PerformanceEvidenceReport`
- `testPERF01Records720pTimingMatrixAndBudgetComparison`
- `testPERF04RecordsFixtureLoopMemoryTrendWithNonClaimForShortRuns`
- `testPERF05PerformanceEvidenceReportUsesOnlyAllowlistedFields`
- `CameraBeautyPipelineTests.testPERF02BackpressureStressKeepsLatestFrameWinsAndCountsDroppedFrames`
- `CameraBeautyPipelineTests.testPERF03ResetClearsPendingWorkDropCountersWarningsAndSnapshots`
- `ImageEditorPipelineTests.testPERF03SelectionFailureCanRecoverWithLatestValidFixture`
- `BeautyConfigurationTests.testPERF03RenderQualityModesAreStableConfigurationContract`
- `BeautyEngineTests.testPERF03ResetPreservesConfigurationQualityAndDetectionSummaryContract`
- `CombinedEffectSafetyTests.testPERF03HighCappedTimingParametersPreserveSafetyCapsAndRedactedMetrics`
- `MissingLandmarkDegradationTests.testPERF03NoFaceMissingStaleAndReusedGeometryRemainRedactedAndDegraded`
- `.planning/phases/23-performance-and-reliability-gates/23-PERFORMANCE-EVIDENCE.md`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a narrow backpressure stress regression</name>
  <files>BeautyDemo/BeautyDemoTests/CameraBeautyPipelineTests.swift</files>
  <read_first>
    AGENTS.md
    FRONTEND.md
    RELIABILITY.md
    SECURITY.md
    .planning/phases/23-performance-and-reliability-gates/23-CONTEXT.md
    .planning/phases/23-performance-and-reliability-gates/23-PATTERNS.md
    BeautyDemo/BeautyDemo/Camera/CameraBeautyPipeline.swift
    BeautyDemo/BeautyDemoTests/CameraBeautyPipelineTests.swift
  </read_first>
  <behavior>
    - RED: `CameraBeautyPipelineTests.testPERF02BackpressureStressKeepsLatestFrameWinsAndCountsDroppedFrames` fails before the test is implemented.
    - GREEN: with `maxInFlight: 1`, a blocked first frame plus four queued frames processes timestamps `[1, 5]`, drops three stale pending frames, keeps `lastDropReason == .backpressure`, and uses the latest parameter snapshot.
  </behavior>
  <action>
    Extend `CameraBeautyPipelineTests.swift` with `testPERF02BackpressureStressKeepsLatestFrameWinsAndCountsDroppedFrames`. Use the existing `CameraFrameProcessor`, `DispatchSemaphore`, `LockedValues`, `makeFrame(timestamp:)`, and `waitUntilIdle()` patterns. The test must enqueue a first blocked frame, then enqueue timestamps 2, 3, 4, and 5 with distinct `skinSmoothing` values. Assert exactly one in-flight frame while blocked, exactly three dropped stale pending frames after the burst, final processed timestamps `[1, 5]`, final processed skin smoothing values from the first and fifth frames only, and `pipeline.state.droppedFrameCount == 3`. Do not change `CameraBeautyPipeline` unless the new test exposes a real implementation regression; if source changes are needed, keep them limited to preserving the existing latest-frame-wins policy.
  </action>
  <verify>
    <automated>xcodebuild -project BeautyDemo/BeautyDemo.xcodeproj -scheme BeautyDemo -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:BeautyDemoTests/CameraBeautyPipelineTests test</automated>
    <automated>rg -n "testPERF02BackpressureStressKeepsLatestFrameWinsAndCountsDroppedFrames|droppedFrameCount|lastDropReason|backpressure|\\[1, 5\\]" BeautyDemo/BeautyDemoTests/CameraBeautyPipelineTests.swift</automated>
    <automated>git diff --check -- BeautyDemo/BeautyDemoTests/CameraBeautyPipelineTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the named PERF-02 test exists and asserts latest-frame-wins, drop count, drop reason, and latest parameter snapshot.
    - CLI output: the focused xcodebuild command passes, or exits with the known Metal Toolchain blocker and that blocker is recorded in the plan summary with command, environment, impact, next step, and no pass claim.
    - Source assertion: no raw framework error, local/private path, image bytes, face geometry, or raw diagnostics are added to test output or assertions.
  </acceptance_criteria>
  <done>PERF-02 has a stronger reproducible backpressure stress regression or a blocker-honest focused test record.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Demo reset and still-image recovery regressions</name>
  <files>BeautyDemo/BeautyDemoTests/CameraBeautyPipelineTests.swift, BeautyDemo/BeautyDemoTests/ImageEditorPipelineTests.swift</files>
  <read_first>
    AGENTS.md
    FRONTEND.md
    RELIABILITY.md
    SECURITY.md
    .planning/phases/23-performance-and-reliability-gates/23-CONTEXT.md
    .planning/phases/23-performance-and-reliability-gates/23-PATTERNS.md
    BeautyDemo/BeautyDemo/Camera/CameraBeautyPipeline.swift
    BeautyDemo/BeautyDemo/Editor/ImageEditorPipeline.swift
    BeautyDemo/BeautyDemoTests/CameraBeautyPipelineTests.swift
    BeautyDemo/BeautyDemoTests/ImageEditorPipelineTests.swift
  </read_first>
  <behavior>
    - RED: reset and still-image recovery PERF-03 tests fail before they are added.
    - GREEN: camera reset clears pending work, latest snapshot, warning, in-flight/drop counters, drop reason, and returns to idle after old work is released.
    - GREEN: a still-image selection/decode failure preserves the previous snapshot, then a later valid fixture replaces it and clears failure copy.
  </behavior>
  <action>
    Add `CameraBeautyPipelineTests.testPERF03ResetClearsPendingWorkDropCountersWarningsAndSnapshots` and `ImageEditorPipelineTests.testPERF03SelectionFailureCanRecoverWithLatestValidFixture`. The camera reset test must cover `CameraBeautyPipeline.reset()` per D-10 without claiming GPU cache reset; use the existing generation behavior so old in-flight completion cannot republish stale output. The still-image test must cover selection/decode failure recovery by first creating a valid snapshot, then processing `.photosPickerData(Data())` to preserve that snapshot with friendly copy, then processing a valid fixture and asserting the latest valid input wins and status clears. Do not add Demo UI routes, public SDK surface, or product copy beyond existing test assertions.
  </action>
  <verify>
    <automated>xcodebuild -project BeautyDemo/BeautyDemo.xcodeproj -scheme BeautyDemo -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:BeautyDemoTests/CameraBeautyPipelineTests -only-testing:BeautyDemoTests/ImageEditorPipelineTests test</automated>
    <automated>rg -n "testPERF03ResetClearsPendingWorkDropCountersWarningsAndSnapshots|testPERF03SelectionFailureCanRecoverWithLatestValidFixture|reset\\(\\)|photosPickerData\\(Data\\(\\)\\)|latest valid" BeautyDemo/BeautyDemoTests/CameraBeautyPipelineTests.swift BeautyDemo/BeautyDemoTests/ImageEditorPipelineTests.swift</automated>
    <automated>git diff --check -- BeautyDemo/BeautyDemoTests/CameraBeautyPipelineTests.swift BeautyDemo/BeautyDemoTests/ImageEditorPipelineTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: camera reset test covers pending work, latest snapshot, warning/current status, in-flight count, dropped-frame count, drop reason, and idle state.
    - Source assertion: still-image recovery test covers failure preservation and later valid-input recovery.
    - CLI output: focused xcodebuild command passes, or exits with the known Metal Toolchain blocker and that blocker is recorded honestly without claiming Demo simulator pass evidence.
    - Source assertion: no test or comment claims GPU transient texture/cache reset for caches that do not exist.
  </acceptance_criteria>
  <done>Demo pipeline reset and still-image selection-failure recovery are covered by focused tests or blocker-honest command evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Camera frame -> Demo pipeline | Test CVPixelBuffer frames enter app-side realtime queueing/backpressure logic. |
| Photo picker data -> ImageEditorPipeline | Untrusted/invalid selected data enters still-image decoding and recovery path. |
| Xcodebuild failure -> evidence summary | Local toolchain errors are summarized for committed planning evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-06 | Tampering | `CameraBeautyPipeline` backpressure | mitigate | Add source-level regression proving stale pending frames are dropped and latest frame wins per D-15. |
| T-23-07 | Denial of Service | Realtime pending queue | mitigate | Assert bounded in-flight/pending behavior and dropped-frame accounting under burst input. |
| T-23-08 | Spoofing/Repudiation | Demo xcodebuild blocker | mitigate | If xcodebuild exits 65 for missing Metal Toolchain, record blocker class and no pass claim per D-08 and D-16. |
| T-23-09 | Information Disclosure | Still-image failure copy | mitigate | Assert recovery copy avoids raw paths, raw framework errors, image bytes, and diagnostics per D-13. |
| T-23-10 | Tampering | Reset behavior | mitigate | Assert reset clears app-side transient queue/counters/status without claiming nonexistent GPU cache reset per D-10. |
| T-23-SC | Tampering | Package installs | accept | No package-manager install is planned. |
</threat_model>

<verification>
Run the focused Demo xcodebuild commands exactly as listed. If the local Metal Toolchain blocker remains, record the blocker in the summary and rely on source assertions plus rerun protocol; do not claim Demo simulator pass evidence.
</verification>

<success_criteria>
- PERF-02 has a focused latest-frame-wins and dropped-frame stress regression.
- PERF-03 has Demo reset and still-image recovery evidence.
- PERF-04 secondary Demo evidence is either passed or blocker-honest.
- PERF-05 is preserved through raw path/error leakage assertions.
</success_criteria>

<output>
Create `.planning/phases/23-performance-and-reliability-gates/23-02-SUMMARY.md` when done.
</output>
