---
phase: 45-public-contract-and-observed-face-support
plan: "02"
type: execute
wave: 2
depends_on: ["45-01"]
files_modified:
  - BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift
  - BeautySDK/Tests/BeautyResourcesTests/BeautyResourceCatalogTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
autonomous: true
requirements: [FACE-07, FACE-08, FACE-09, FACE-12]
must_haves:
  truths:
    - "D-01/D-02: exactly faceContourSmooth, templeFullness, cheekboneSlim, and chinTaper are independent positive-only public Float fields; finite values clamp to 0...1 and every non-finite value becomes zero."
    - "D-03/D-04: the model is exactly 52 stored fields (51 numeric plus filterId), source defaults and missing keys are zero, legacy 48-key JSON is neutral, and unequal new values round-trip without aliasing."
    - "D-03/D-17: historical literal 31/33 payloads stay unchanged, the reconstructed 38-key payload removes ten eye plus four face keys, and all five bundled preset files remain byte-identical while decoding the four values as zero."
    - "D-20: all four new fields at zero leave the existing face, eye, nose, and mouth resolver plan exactly unchanged; nonzero provider eligibility/routing is absent in Phase 45."
    - "FACE-07/FACE-08/FACE-09/FACE-12 edge resolution: each initializer, decode, encode, and normalized-copy call produces an independent request-local value with no cache/shared state, and normalization never mutates the caller's source value."
  artifacts:
    - path: BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift
      provides: "Exact 52-field public scalar and Codable/source compatibility contract"
    - path: BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift
      provides: "Normalization, inventory, legacy 48/38/33/31, unequal round-trip, and non-mutating copy evidence"
    - path: BeautySDK/Tests/BeautyResourcesTests/BeautyResourceCatalogTests.swift
      provides: "Bundled-preset missing-key neutrality"
    - path: BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
      provides: "Explicit-zero face/eye/nose/mouth neutral-plan regression and no premature routing evidence"
  key_links:
    - from: BeautyParameters initializer
      to: CodingKeys/decode/normalized
      via: "the same four names forwarded exactly once through every manual lifecycle seam"
      pattern: "faceContourSmooth|templeFullness|cheekboneSlim|chinTaper"
    - from: bundled preset JSON
      to: BeautyResourceCatalog decode
      via: "missing keys use decodeFloatIfPresent and yield zero without editing resources"
      pattern: "decodeFloatIfPresent"
---

<objective>
Deliver the exact 48-to-52 public model contract without activating any new geometry behavior.

Purpose: Let hosts express four independent contour intents while keeping source, Codable, preset, and shipped output compatibility exact.
Output: Four normalized stored fields and focused compatibility/neutrality evidence.
</objective>

## Artifacts this phase produces

- Public `BeautyParameters.faceContourSmooth`, `.templeFullness`, `.cheekboneSlim`, and `.chinTaper` properties, coding keys, defaulted initializer labels, clamped assignments, decode calls, and `normalized()` forwarding.
- New FACE-07/08/09/12-focused tests in the three existing test owners; no new file or public geometry type.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<read_first>
AGENTS.md; PLANS.md; .planning/PROJECT.md; .planning/ROADMAP.md; .planning/REQUIREMENTS.md; .planning/STATE.md; .planning/phases/45-public-contract-and-observed-face-support/45-CONTEXT.md; .planning/phases/45-public-contract-and-observed-face-support/45-RESEARCH.md; .planning/phases/45-public-contract-and-observed-face-support/45-PATTERNS.md; 45-01-SUMMARY.md; BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift; BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift; BeautySDK/Tests/BeautyResourcesTests/BeautyResourceCatalogTests.swift; BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
</read_first>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/45-public-contract-and-observed-face-support/45-CONTEXT.md
@.planning/phases/45-public-contract-and-observed-face-support/45-RESEARCH.md
@.planning/phases/45-public-contract-and-observed-face-support/45-PATTERNS.md
@BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift
@BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift
@BeautySDK/Tests/BeautyResourcesTests/BeautyResourceCatalogTests.swift
@BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift

<interfaces>
`BeautyParameters` manually owns every stored property, CodingKey, defaulted initializer label, clamp assignment, decoder argument, and normalized-copy argument. Use `clampUnit` and `decodeFloatIfPresent`; preserve signed `chinLength` on `clampSigned`. `BeautyEffectResolver.resolve(parameters:faceGeometry:)` and `.fixture` provide the existing full-domain plan equality seam, but the four new fields must not be added to requiresFaceGeometry, effective strengths, caps, providers, or routing in this plan.
</interfaces>
</context>

## Plan-local source coverage

| Source | Coverage |
| --- | --- |
| GOAL | Establishes compatibility-safe public semantics for all four contour controls. |
| REQ | FACE-07, FACE-08, FACE-09, and FACE-12 each get independent storage/range/default/round-trip evidence. |
| RESEARCH | Applies the seven-site scalar lifecycle, current test counts, legacy payload reconstruction, preset hashes, and no-routing boundary. |
| CONTEXT | D-01 through D-04, D-17, and D-20 are implemented directly. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 45-02-01: Add four independent normalized public fields</name>
  <files>BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift, BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift</files>
  <behavior>
    - Each new field maps negative to 0, keeps a distinct in-range value, clamps overflow to 1, and maps NaN/+infinity/-infinity to 0.
    - Direct mutable assignment followed by `normalized()` reapplies the rules without mutating the source.
    - Defaults and omitted source initializer labels are zero; `chinTaper` never changes signed `chinLength`.
  </behavior>
  <action>Per D-01/D-02/D-03/D-04/D-17, first add FACE-labeled table tests, then wire exactly `faceContourSmooth`, `templeFullness`, `cheekboneSlim`, and `chinTaper` through stored properties, CodingKeys, defaulted initializer labels, clampUnit assignments, `init(from:)` missing-key decode, and `normalized()` forwarding. Place them with the existing face-shape fields, preserve every existing label/order/value, and keep `chinLength` signed. Update live reflected/encoded inventory assertions from 48 to 52 but do not touch historical literal fixture counts. Do not add aliases, setters, caps, provider fields, resolver cases, facade methods, or public support types.</action>
  <acceptance_criteria>The four fields compile and normalize independently, `normalized()` is non-mutating, exact stored/key inventory is 52, source omission compiles to four zeros, and signed chinLength remains independent.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyParametersTests &amp;&amp; git diff --check</automated></verify>
  <done>FACE-07/08/09/12 have exact public range/default/storage semantics through every manual model seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 45-02-02: Prove legacy, preset, independence, and shipped-domain neutrality</name>
  <files>BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift, BeautySDK/Tests/BeautyResourcesTests/BeautyResourceCatalogTests.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift</files>
  <behavior>
    - A complete 52-key payload with four unequal values round-trips exactly; removing the four keys creates exactly 48 keys and decodes four zeros.
    - Historical 31/33 payload fixtures keep their literal counts; the legacy-38 reconstruction removes all ten eye and four face additions and remains exactly 38.
    - Five bundled presets omit the keys, keep their fixed bytes/hashes, and decode four zeros.
    - Existing simultaneous faceSlim/eyeSize/noseSlim/mouthSize behavior produces the same plan when all four additions are explicitly zero as when omitted.
  </behavior>
  <action>Per D-03/D-04/D-17/D-20, extend core tests with exact Mirror/JSON 52 counts, unequal values, legacy-48 removal, legacy-38 fourteen-key removal, and explicit historical-count guards. Extend `BeautyResourceCatalogTests` to assert the exact five preset IDs and four zero values without editing any preset file; the Plan 01 checker owns byte/hash proof. Add a resolver regression comparing an existing eligible face+eye+nose+mouth request against the same request with the four new fields explicitly set to zero, asserting plan strengths/domains/warnings/metrics/control points remain equal. Also assert the new nonzero fields are not yet part of `requiresFaceGeometry`; Phase 46 owns routing. Keep current provider/cap/effective-strength sources unchanged.</action>
  <acceptance_criteria>52-key independence, 48-key neutral decode, unchanged 38/33/31 fixtures, five neutral presets, and full shipped-domain zero neutrality all pass with no routing/cap/provider source edit.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyParametersTests &amp;&amp; swift test --package-path BeautySDK --filter BeautyResourcesTests.BeautyResourceCatalogTests &amp;&amp; swift test --package-path BeautySDK --filter BeautyEffectsTests.BeautyEffectResolverTests &amp;&amp; git diff --check</automated></verify>
  <done>The exact public compatibility contract is executable and cannot be mistaken for Phase 46 provider readiness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Host Float/JSON → BeautyParameters | Untrusted numeric and serialized inputs enter the public model. |
| Public scalar model → existing resolver | New intent must stay neutral until downstream routing is explicitly implemented. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-45-05 | Tampering | BeautyParameters construction/decode | mitigate | Table-test negative/overflow/non-finite values and re-normalize mutable copies through clampUnit. |
| T-45-06 | Spoofing | independent field storage | mitigate | Unequal 52-key round-trip, exact labels/count, and chinTaper/chinLength separation reject aliasing. |
| T-45-07 | Denial of Service | legacy/preset decode | mitigate | Preserve existing bounded Codable/resource paths and missing-key zero behavior; add no parser or resource. |
| T-45-08 | Elevation of Privilege | premature resolver activation | mitigate | Assert no new requiresFaceGeometry/effective/provider routing and compare shipped-domain plans at explicit zero. |
| T-45-SC | Tampering | package/dependency graph | mitigate | No install or manifest edit; Plan 05 live checker blocks drift. |
</threat_model>

<verification>
Run the three focused suites after their tasks, then the full SwiftPM suite at wave completion. Confirm `git diff --name-only` contains no preset JSON, package manifest, provider, resolver production, renderer, facade, or Demo source.
</verification>

<success_criteria>
Hosts receive exactly four independent positive-only scalars in a 52-field model; legacy source/JSON/presets and all shipped face/eye/nose/mouth behavior remain neutral, with no Phase 46+ activation.
</success_criteria>

<output>Create `45-02-SUMMARY.md` with exact inventory/payload/preset counts, focused/full test counts, neutral-plan evidence, and explicit no-routing/no-provider scope.</output>
