---
phase: 45-public-contract-and-observed-face-support
plan: "03"
type: execute
wave: 2
depends_on: ["45-01"]
files_modified:
  - BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift
  - BeautySDK/Tests/BeautyDetectionTests/VisionFaceDetectorTests.swift
  - BeautySDK/Tests/BeautyDetectionTests/FaceObservationMappingTests.swift
autonomous: true
requirements: [SUPP-01, SUPP-02, SUPP-04]
must_haves:
  truths:
    - "D-05/D-06: the existing VNDetectFaceLandmarksRequest copies actual faceContour and medianLine points, composes each face-local point into Vision image space, and sends it through the one per-request CoordinateMapper exactly once."
    - "D-07: contour adjacency is preserved and only whole-array reversal may establish mapper-axis left-to-right contour traversal or top-to-bottom median direction across .up/.right/.left/.down and input mirroring."
    - "D-09/D-10/D-11: each region is preflighted and mapped independently; malformed/oversized contour cannot erase valid median, malformed/oversized median cannot erase valid contour, and either cannot erase the selected face or shipped sibling work."
    - "D-13/D-14: copied support is request-local/package-only and diagnostics/default-provider tests expose only availability/counts, never point values, bounds, samples, region descriptions, or framework objects."
    - "SUPP-01 edge resolution: one immutable provider payload is mapped exactly once with no retry/second request/cache; interruption retains no region data."
    - "SUPP-02 edge resolution at the mapping boundary: invalid regions fail independently and deterministically while valid siblings are mapped once and preserved."
    - "SUPP-04 edge resolution: consecutive and parallel detector invocations share no observed-face payload or mutable support state."
  artifacts:
    - path: BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift
      provides: "Actual Vision faceContour/medianLine capture, region-local preflight/mapping, and mapper-axis canonical reversal"
    - path: BeautySDK/Tests/BeautyDetectionTests/VisionFaceDetectorTests.swift
      provides: "Injected/default-provider actual-region, independent absence, oversize, and redaction evidence"
    - path: BeautySDK/Tests/BeautyDetectionTests/FaceObservationMappingTests.swift
      provides: "All-orientation/mirror/reversed-order and exact mapping-boundary evidence"
  key_links:
    - from: VNFaceLandmarks2D.faceContour/medianLine
      to: VisionDetectionObservation.observedFaceSupport
      via: "immediate CoordinatePoint copy in the existing default provider"
      pattern: "faceContour|medianLine"
    - from: VisionDetectionObservation.observedFaceSupport
      to: BeautyFaceObservation.observedFaceSupport
      via: "independent bounded region mapping through the call-local CoordinateMapper"
      pattern: "mapObservation"
    - from: mapped bounds anchors
      to: canonical region arrays
      via: "endpoint projection and whole-array reversal only"
      pattern: "reversed"
---

<objective>
Capture and canonicalize actual Vision face contour and median-line evidence at the existing detection boundary.

Purpose: Supply honest request-local observed geometry while preventing one malformed optional region, orientation change, or mirror flag from corrupting the selected face or another region.
Output: One-time mapped optional support plus deterministic injection/default-provider tests.
</objective>

## Artifacts this phase produces

- New `VisionDetectionObservation.observedFaceSupport` defaulted carrier.
- Existing default provider extraction of `VNFaceLandmarks2D.faceContour` and `.medianLine` into `BeautyObservedFaceSupport`.
- New private region preflight/mapping and reversal-only canonicalization helpers inside `VisionFaceDetector` (exact helper names may follow current `mapPoints`/`deriveEyeOrder` style).
- SUPP-01/02/04 mapping and lifecycle tests in the existing detection suites.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<read_first>
AGENTS.md; PLANS.md; .planning/PROJECT.md; .planning/ROADMAP.md; .planning/REQUIREMENTS.md; .planning/STATE.md; .planning/phases/45-public-contract-and-observed-face-support/45-CONTEXT.md; .planning/phases/45-public-contract-and-observed-face-support/45-RESEARCH.md; .planning/phases/45-public-contract-and-observed-face-support/45-PATTERNS.md; 45-01-SUMMARY.md; BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift; BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift; BeautySDK/Sources/BeautyDetection/CoordinateSpace.swift; BeautySDK/Tests/BeautyDetectionTests/VisionFaceDetectorTests.swift; BeautySDK/Tests/BeautyDetectionTests/FaceObservationMappingTests.swift
</read_first>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/45-public-contract-and-observed-face-support/45-CONTEXT.md
@.planning/phases/45-public-contract-and-observed-face-support/45-RESEARCH.md
@.planning/phases/45-public-contract-and-observed-face-support/45-PATTERNS.md
@BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift
@BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift
@BeautySDK/Tests/BeautyDetectionTests/VisionFaceDetectorTests.swift
@BeautySDK/Tests/BeautyDetectionTests/FaceObservationMappingTests.swift

<interfaces>
Plan 01 supplies `BeautyObservedFaceSupport(contour: [CoordinatePoint]?, medianLine: [CoordinatePoint]?)` and `BeautyFaceObservation.observedFaceSupport`. Extend existing `VisionDetectionObservation` with the same optional envelope and a defaulted initializer label. `mapObservation(_:mapper:)` already maps bounds and eyes; `mapPoints(_:in:with:)` demonstrates face-local composition. Do not reuse its throwing whole-observation behavior for face regions. `CoordinateMapper` remains the sole orientation/input-mirror conversion; preview mirroring must not change image-normalized output.
</interfaces>
</context>

## Plan-local source coverage

| Source | Coverage |
| --- | --- |
| GOAL | Supplies actual observed contour/median data with honest orientation and malformed-input behavior. |
| REQ | SUPP-01 gets actual one-time Vision mapping; SUPP-02 gets canonical/rejection behavior; SUPP-04 gets request-local redaction/lifetime evidence. |
| RESEARCH | Implements the one-request/one-mapper, whole-array-reversal, independent-region, aggregate-real-fixture pattern. |
| CONTEXT | D-05, D-06, D-07, D-09, D-10, D-11, D-13, D-14, D-18, and D-20 are covered. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 45-03-01: Capture and map actual face contour and median line once</name>
  <files>BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift, BeautySDK/Tests/BeautyDetectionTests/VisionFaceDetectorTests.swift</files>
  <behavior>
    - The default request copies actual faceContour and medianLine normalizedPoints once and retains no VNFaceLandmarkRegion2D.
    - Injected contour/median points are composed with visionBounds and mapped to image-normalized coordinates once per valid point.
    - Nil, valid, malformed, and oversized regions are handled independently; invalid shared bounds still produce the existing observation-level mapping failure.
    - Six committed portrait fixtures report at least one complete support through aggregate booleans/counts only.
  </behavior>
  <action>Per D-05/D-06/D-09/D-10/D-11/D-13/D-14/D-18, add a defaulted `observedFaceSupport` label to `VisionDetectionObservation`; extend the existing `landmarks(from:)` payload and single `VNDetectFaceLandmarksRequest` provider to copy actual faceContour/medianLine normalized points immediately. In `mapObservation`, validate shared bounds once, then preflight and map the two optional arrays independently: require non-empty finite closed-unit raw points and reject counts above the Plan 01/research ceilings before mapping to bound work; compose face-local coordinates into Vision image-normalized coordinates and call the existing mapper once per accepted point. A region error becomes nil for that region, not a thrown whole-observation failure; invalid shared bounds retain the existing `.mappingFailed` behavior. Preserve an explicit envelope when one region survives, never reconstruct either region from bounds, and add no second request/cache/retry/provider. Add tests for contour-only, median-only, both, neither, each malformed/oversized independently, shared-bounds failure, exact mapped points, absence of retries/shared state, and aggregate-only availability over the six portrait fixtures.</action>
  <acceptance_criteria>The current request produces copied actual support, all accepted points are finite image-normalized results of exactly one composition+map, malformed optional regions are isolated, selected observations survive region failure, and no raw value appears in summaries or test diagnostics.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyDetectionTests.VisionFaceDetectorTests &amp;&amp; git diff --check</automated></verify>
  <done>SUPP-01 actual-region capture and request-local independent mapping exist without a new detector, request, dependency, or retention path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 45-03-02: Lock canonical direction across winding, orientation, and mirroring</name>
  <files>BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift, BeautySDK/Tests/BeautyDetectionTests/FaceObservationMappingTests.swift</files>
  <behavior>
    - Forward and reversed contour inputs produce an identical left-to-right canonical array by whole-array reversal only.
    - Forward and reversed median inputs produce an identical top-to-bottom canonical array by whole-array reversal only.
    - `.up`, `.right`, `.left`, `.down` crossed with inputMirrored false/true preserve canonical arrays; preview mirroring has no effect.
    - Direction-degenerate endpoints, just-outside/non-finite points, and oversized arrays fail only their region; exact 0 and 1 raw coordinates are accepted.
  </behavior>
  <action>Per D-06/D-07/D-09/D-10/D-18, derive face-local right and down axes by mapping bounds anchors through the same call-local mapper. Use the right-axis projection of contour `(last-first)` and the down-axis projection of median `(last-first)` only to retain or reverse the entire array; reject non-finite/near-zero projection at the research epsilon and never sort by X, angle, index distance, or anatomical labels. Add the full 4-orientation × 2-input-mirror matrix for forward/reversed contour and median, plus preview-mirror invariance, exact closed-unit edges, outside/non-finite/degenerate direction, adjacency preservation, and independent region failure. Assert consecutive detector calls with opposite metadata do not leak prior arrays or orientation state.</action>
  <acceptance_criteria>All 8 metadata combinations and both source windings converge on deterministic adjacency-preserving arrays, boundary/malformed cases fail region-locally, and repeated calls are stateless.</acceptance_criteria>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyDetectionTests.FaceObservationMappingTests &amp;&amp; swift test --package-path BeautySDK --filter BeautyDetectionTests.VisionFaceDetectorTests &amp;&amp; git diff --check</automated></verify>
  <done>SUPP-02 canonical mapping is orientation/mirror stable and cannot invert or reorder the open paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Vision/injected landmark arrays → detector mapping | Counts and coordinates are untrusted before bounded conversion. |
| Metadata/orientation → canonical traversal | Orientation and input mirror can invert semantics if axes are inferred incorrectly. |
| Detector request → observation lifetime | Raw framework regions and mapped points must not escape the current call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-45-09 | Tampering / DoS | raw contour/median arrays | mitigate | Preflight finite/unit/non-empty/fixed ceilings before mapping and reject each region locally. |
| T-45-10 | Tampering | canonical orientation | mitigate | Mapper-derived right/down axes, reversal only, full orientation/mirror/degenerate tests. |
| T-45-11 | Information Disclosure | default provider/summary | mitigate | Copy points out of Vision immediately; assert aggregate-only results and no framework/coordinate diagnostics. |
| T-45-12 | Repudiation | exactly-once mapping | mitigate | One request and one call-local mapper path; injected exact-point and repeated-call stateless tests. |
| T-45-SC | Tampering | alternate detector/model | mitigate | No new request/dependency/model/network path; Plan 05 live gate blocks additions. |
</threat_model>

<verification>
Run both focused detection suites after their tasks, then full SwiftPM at wave completion. Confirm production diff is limited to the existing detector file and does not add cache, persistence, logs, a new request, target, dependency, or public API.
</verification>

<success_criteria>
Actual faceContour/medianLine evidence is copied, mapped once, canonicalized without adjacency loss, isolated on malformed input, request-local, and aggregate-only across every supported metadata combination.
</success_criteria>

<output>Create `45-03-SUMMARY.md` with exact test counts, orientation/mirror matrix, region-local cases, aggregate fixture availability, and explicit no-cache/no-second-request/no-raw-diagnostic evidence.</output>
