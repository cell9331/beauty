---
phase: 47-public-facade-face-output-evidence
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - BeautySDK/Sources/BeautyExampleRenderer/main.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift
  - BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift
autonomous: true
requirements: [OUT-01, OUT-03]
must_haves:
  truths:
    - "The public renderer contains exactly 59 ordered cases: 55 preserved cases plus four isolated Phase 47 face cases at provisional 0.25."
    - "Every new renderer case contains exactly one matching public scalar and still reaches the one existing BeautyEngine.processResult call through a BeautySDK-only import boundary."
    - "All four new requests preserve extent and aggregate redaction on a representative no-face fixture."
    - "Missing and malformed observed contour fixtures traverse the public facade, remove all four dependent fields, and preserve an eligible shipped face sibling without exposing support data."
  artifacts:
    - path: BeautySDK/Sources/BeautyExampleRenderer/main.swift
      provides: "four isolated public-facade face renderer cases"
    - path: BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift
      provides: "exact 59-case inventory, one-field source contract, and four no-face requests"
    - path: BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift
      provides: "aggregate missing/malformed observed-contour testing fixtures"
    - path: BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift
      provides: "public missing/malformed support and shipped-sibling continuation evidence"
  key_links:
    - from: four new RenderCase entries
      to: BeautyEngine.processResult
      via: "existing shared renderer loop"
      pattern: "faceContourSmooth_0p25|templeFullness_0p25|cheekboneSlim_0p25|chinTaper_0p25"
    - from: SDKTestingFaceDetectionFixture
      to: BeautyFaceGeometryAdapter
      via: "existing VisionFaceDetector observation mapping"
      pattern: "missingObservedFaceContour|malformedObservedFaceContour"
---

<objective>
Freeze the exact public renderer/source matrix and representative public degradation behavior before generated image acceptance.

Purpose: OUT-01 and the facade portion of OUT-03 must be executable before a strict helper can treat saved output as trustworthy evidence.
Output: Four isolated renderer cases plus focused source, no-face, missing-contour, malformed-contour, sibling-continuation, and redaction tests.
</objective>

<context>
@AGENTS.md
@PLANS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/47-public-facade-face-output-evidence/47-CONTEXT.md
@.planning/phases/47-public-facade-face-output-evidence/47-RESEARCH.md
@.planning/phases/47-public-facade-face-output-evidence/47-PATTERNS.md
@.planning/phases/47-public-facade-face-output-evidence/47-VALIDATION.md
@.planning/phases/46-independent-contour-and-chin-geometry/46-VERIFICATION.md
@BeautySDK/Sources/BeautyExampleRenderer/main.swift
@BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift
@BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift
@BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 47-01-01: Add exact isolated renderer cases and public source/no-face contracts</name>
  <files>BeautySDK/Sources/BeautyExampleRenderer/main.swift, BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift</files>
  <action>Add exactly four `RenderCase` entries beside the existing face-shape group with IDs `faceContourSmooth_0p25`, `templeFullness_0p25`, `cheekboneSlim_0p25`, and `chinTaper_0p25`; each constructs only its matching `BeautyParameters` field at `0.25`. Preserve all existing 55 IDs, values, ordering relative to one another, imports, fixture discovery, output naming, and the single `engine.processResult` call. First extend the XCTest ordered inventory and add a table-driven OUT-01 source contract that asserts one matching initializer label, no other face field, no alias/combo/internal import/support carrier, exactly 59 unique IDs, and no deferred double-chin/hairline case. Add a four-request no-face test following the Phase 43 pattern: preserved extent, `.noFace`, `[.noFaceDetected]`, zero face/used-face counts, geometry-required metric, fixed no-face warning, and aggregate redaction with the four field names plus contour/median/apex/source/target/provider/path terms prohibited.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --disable-sandbox --filter BeautyCoreTests.BeautyRendererOutputRegressionTests &amp;&amp; test "$(rg -n 'RenderCase\\(' BeautySDK/Sources/BeautyExampleRenderer/main.swift | wc -l | tr -d ' ')" -eq 59 &amp;&amp; test "$(rg -n 'engine\\.processResult\\(' BeautySDK/Sources/BeautyExampleRenderer/main.swift | wc -l | tr -d ' ')" -eq 1 &amp;&amp; git diff --check</automated>
  </verify>
  <done>The renderer and XCTest agree on exactly 59 public cases, each new case is isolated at 0.25, and all four no-face requests preserve extent with aggregate-only degradation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 47-01-02: Add missing/malformed observed-contour public degradation fixtures</name>
  <files>BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift, BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift</files>
  <action>Add testing-only enum cases `missingObservedFaceContour` and `malformedObservedFaceContour`. The first returns the existing usable face and complete shipped landmarks with `observedFaceSupport` absent; the second returns complete shipped landmarks with a bounded malformed/invalid observed contour that the production detector/adapter rejects and no valid centerline authorization. Do not expose a public geometry carrier, Codable state, cache, persistence, or raw description. Add a table-driven public-facade test for both fixtures and all four new fields, always pairing the requested new field with an eligible `faceSlim` sibling and processing a sibling-only baseline through an equivalent fresh provider. Assert one detector call per request, preserved extent, usable aggregate detection, `.faceShape` active from the sibling, new effective field zero, positive aggregate geometry evidence from the sibling, and equality with the sibling-only baseline for final domains, warnings, metrics, point count, and rendered bytes so dependent work contributes no final evidence. Assert no raw/derived support tokens in public results. Pin the unchanged package manifest with its Phase 46 git-blob hash and use scoped public/Codable/persistence/network/model/resource scans; do not rerun the Phase 46 live checker because its historical contract intentionally forbids the renderer additions owned by this phase.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --disable-sandbox --filter BeautyCoreTests.BeautyEngineGeometryFacadeTests &amp;&amp; test "$(git hash-object BeautySDK/Package.swift)" = "6f03b078816ad1f7a426e3f70d4f57503f3152e9" &amp;&amp; git diff --check</automated>
  </verify>
  <done>Missing and malformed observed contour fail all four new fields closed through the public facade while the shipped sibling continues and diagnostics remain aggregate-only.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation |
|---|---|---:|---|---|---|
| T-47-01 | Tampering / Spoofing | high | renderer inventory | mitigate | Exact ordered 59-ID XCTest, one-field snippets, duplicate rejection, and preserved existing IDs. |
| T-47-02 | Elevation of Privilege | high | public facade boundary | mitigate | `BeautySDK`-only import scan, one shared facade call, and internal/provider/alias prohibitions. |
| T-47-03 | Information Disclosure | high | no-face and degraded results | mitigate | Fixed aggregate assertions and forbidden support/provider/path terms across warnings, metrics, and reasons. |
| T-47-04 | Spoofing / Tampering | high | missing/malformed support | mitigate | Production mapper/adapter traversal, proxy-only prevention, new-field zero, and shipped-sibling continuation evidence. |
| T-47-SC | Tampering | medium | dependencies/package | mitigate | No package/install/target/resource change; the Phase 46 manifest git-blob hash remains pinned directly. |
</threat_model>

<success_criteria>
OUT-01 source behavior and representative public-degradation portions of OUT-03 pass without production provider/cap/pipeline changes.
</success_criteria>

<output>Create `47-01-SUMMARY.md`.</output>
