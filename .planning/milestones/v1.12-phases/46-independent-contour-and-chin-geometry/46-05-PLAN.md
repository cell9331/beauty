---
phase: 46-independent-contour-and-chin-geometry
plan: "05"
type: execute
wave: 5
depends_on: ["46-04"]
files_modified:
  - BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
  - BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift
  - BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift
autonomous: true
requirements: [GEOM-01, GEOM-02, GEOM-03, GEOM-04]
must_haves:
  truths:
    - "D-09/D-11/D-13: all four values traverse geometry triggering, cap, requested/reusable/freshness/zero/scale helpers, face-domain activation, provider-empty removal, aggregate evidence, and existing unified dispatch with no new public API or pass."
    - "D-12: face/chin/eye/nose/mouth emissions sanitize before conflict, then re-evaluate from one retained unscaled baseline after each shared scale; exactly 37 monotone removal passes are possible and no removed field can revive."
    - "D-13: final effective strengths, total, count, scale, weakened count, active/skipped domains, warnings, metrics, geometryPointCount, and dispatched points are all derived from the same final emitting field set; each provider point is accounted exactly once."
    - "D-14/D-18: no-face/stale new fields are zero; eligible reused new fields receive exact non-eye 0.5 before provider eligibility; missing contour/centerline removes only dependent work and safe/shipped siblings continue."
    - "D-15/D-19/D-21/D-22: deterministic `.usableFace` carries private asymmetric raw contour/median input, makes each isolated facade request route once, and preserves aggregate-only diagnostics and shipped zero-new-field behavior."
  artifacts:
    - path: BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
      provides: "Complete four-field lifecycle, pre/post provider sanitization, final emission-owned activity and point accounting"
    - path: BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift
      provides: "Exact 37-field scale/total/nonzero inventory"
    - path: BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift
      provides: "Deterministic asymmetric observed contour/median fixture on the existing testing SPI"
  key_links:
    - from: BeautyParameters four fields
      to: BeautyEffectiveStrengths
      via: "requiresFaceGeometry plus capUnit and freshness helpers"
      pattern: "faceContourSmooth|templeFullness|cheekboneSlim|chinTaper"
    - from: final named emissions
      to: GeometryConflictResolver and BeautyEffectPlan
      via: "preflight sanitization, retained-baseline convergence, final recomputation, and exact provider-local point counts"
      pattern: "fieldEmissions|0..<37"
    - from: SDKTestingFaceDetectionFixture.usableFace
      to: FaceGeometry.observedFaceSupport
      via: "existing Vision mapping and Phase 45 adapter; no new public geometry type"
      pattern: "BeautyObservedFaceSupport"
---

<objective>
Integrate the four named providers through the existing resolver, 37-field conflict ledger, aggregate accounting, deterministic facade fixture, and unified warp route.

Purpose: Make provider eligibility the single retained-set authority from public intent to final points while preserving freshness, privacy, siblings, and one-pass rendering.
Output: Green Wave 0 resolver/conflict/degradation/pipeline/facade suites and complete GEOM-01..04 internal routing.
</objective>

## Artifacts this phase produces

- New resolver helper `zeroNewFaceGeometryStrengths(_:)`.
- New resolver locals `hadRequestedFaceValues`, `faceProvider`, `chinProvider`, `finalFaceEmissions`, and `finalChinEmissions`.
- Expanded `BeautyEffectResolver.resolveGeometryConflict` signature accepting face/chin/eye/nose/mouth providers and exact `0..<37` convergence.
- Expanded `GeometryConflictResolver` scale, total, and nonzero-count inventories for the four new effective fields.
- New private testing-support constants `phase46ObservedContour` and `phase46ObservedMedianLine`.
- Existing `SDKTestingFaceDetectionFixture.usableFace` gains a `BeautyObservedFaceSupport` payload; no new public/SPI case, facade method, result type, target, or path is created.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-independent-contour-and-chin-geometry/46-CONTEXT.md
@.planning/phases/46-independent-contour-and-chin-geometry/46-RESEARCH.md
@.planning/phases/46-independent-contour-and-chin-geometry/46-PATTERNS.md
@.planning/phases/46-independent-contour-and-chin-geometry/46-VALIDATION.md
@46-01-SUMMARY.md
@46-02-SUMMARY.md
@46-03-SUMMARY.md
@46-04-SUMMARY.md
@BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
@BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift
@BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift

<interfaces>
Plan 04 provides four effective/cap symbols plus `FaceShapeWarpFieldEmissions` and `ChinWarpFieldEmissions`. Existing resolver order is normalize/cap, freshness, provider preflight, domain resolution, shared conflict, final provider point evidence. Existing `resolveGeometryConflict` evaluates provider emissions at scaled strengths but sanitizes the retained unscaled baseline. `GeometryConflictResolver` owns three separate manual field inventories. `VisionDetectionObservation.observedFaceSupport` accepts package-only `BeautyObservedFaceSupport(contour:medianLine:)`; Phase 45 maps and validates that payload into `FaceGeometry.observedFaceSupport`.
</interfaces>
</context>

## Deferred beyond Phase 46

- Do not add example-renderer cases, image decoders, ROI thresholds, output files, gallery code, or visual/naturalness claims; Phase 47 owns them.
- Do not rename provisional caps as final, add exhaustive nine-field/37-field matrices, change product ledger rows, promote branch status, or claim final security/release closeout; Phase 48 owns them.

<tasks>

<task type="auto" tdd="true">
  <name>Task 46-05-01: Route, cap, freshness-scale, and preflight all four fields</name>
  <files>BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift</files>
  <read_first>AGENTS.md; PLANS.md; DESIGN.md; RELIABILITY.md; SECURITY.md; .planning/phases/46-independent-contour-and-chin-geometry/46-CONTEXT.md; .planning/phases/46-independent-contour-and-chin-geometry/46-RESEARCH.md; .planning/phases/46-independent-contour-and-chin-geometry/46-PATTERNS.md; 46-02-SUMMARY.md; 46-04-SUMMARY.md; BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift; BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift; BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift; BeautySDK/Sources/BeautyEffects/Warp/FaceShapeWarpProvider.swift; BeautySDK/Sources/BeautyEffects/Warp/ChinWarpProvider.swift; BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift</read_first>
  <behavior>
    - Four normalized nonzero values trigger geometry, cap independently at 0.25, and explicit zeros preserve the entire shipped plan.
    - Eligible reused values scale to 0.125 before conflict/provider evaluation; no-face and stale zero only the four new values while preserving current shipped semantics.
    - Contour-only eligibility keeps three fields; centerline failure removes taper; nil observed support removes all four without borrowing the legacy proxy.
    - Face/chin named emissions sanitize requested strengths before domain activity or conflict accounting; an emitting shipped sibling keeps faceShape active.
  </behavior>
  <action>Per D-07/D-09/D-10/D-11/D-13/D-14, add the four fields to `requiresFaceGeometry`, capUnit assignment, `hasReusableNonEyeGeometryValues`, exact 0.5 scaling, requested-face capture, and the new-field zero helper. Capture `hadRequestedFaceValues` before no-face/stale/provider removal so degradation remains observable, but derive final activity only from final emissions. For no face or stale, call `zeroNewFaceGeometryStrengths` without changing the existing five shipped face/chin values; for reused, scale the four with every non-eye geometry value and then re-check their own support through provider preflight. Instantiate face/chin providers beside eye/nose/mouth, preflight their field emissions first, and sanitize strengths in stable face→chin→eye→nose→mouth order. Replace stale `hasFaceShapeValues` decisions with requested-versus-final logic: final faceShape is active only when final face+chin points are nonempty, otherwise the requested domain is skipped with the existing generic redacted warning. Do not add field-specific warning/metric keys or messages.</action>
  <acceptance_criteria>All manual lifecycle seams contain each new field exactly once; explicit zero remains neutral; cap/reuse/no-face/stale/support matrices match Wave 0; provider-empty fields are zero before conflict and domain membership; warnings/metrics remain generic aggregates.</acceptance_criteria>
  <verify>
    <automated>swift test --package-path BeautySDK --disable-sandbox --filter BeautyEffectsTests.BeautyEffectResolverTests &amp;&amp; git diff --check</automated>
  </verify>
  <done>Public intent reaches independently eligible provisional effective values, and unsupported work is removed before any aggregate evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 46-05-02: Converge the exact 37-field retained set and account final emissions once</name>
  <files>BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift, BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift</files>
  <read_first>AGENTS.md; PLANS.md; DESIGN.md; RELIABILITY.md; .planning/phases/46-independent-contour-and-chin-geometry/46-CONTEXT.md; .planning/phases/46-independent-contour-and-chin-geometry/46-RESEARCH.md; 46-02-SUMMARY.md; 46-03-SUMMARY.md; 46-04-SUMMARY.md; BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift; BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift; BeautySDK/Sources/BeautyEffects/Render/BeautyGeometryEffectPipeline.swift; BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift; BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift; BeautySDK/Tests/BeautyEffectsTests/BeautyGeometryEffectPipelineTests.swift</read_first>
  <behavior>
    - Scaling, total, and count each enumerate exactly 37 fields and produce exact 11.70/37/1÷11.70 evidence for the full eligible baseline.
    - Preflight face/chin/eye/nose/mouth emissions remove empty work before the first total; every post-scale pass evaluates all providers against scaled strengths and sanitizes only the retained unscaled baseline.
    - Loop bound is exactly 37, next masks are subsets, termination occurs on equality, and the fallback resolves only the final retained baseline.
    - Final face/chin/eye/nose/mouth emissions are recomputed once; geometryPointCount sums each provider's final points once, and pipeline dispatch from the same final strengths yields exactly that concatenation.
  </behavior>
  <action>Per D-09/D-11/D-12/D-13, add each new positive field once to `GeometryConflictResolver` scaling, `geometryTotal`, and nonzero count, retaining `abs` only for signed legacy fields. Expand `resolveGeometryConflict` to accept face and chin providers and change the one exact loop to `for _ in 0..<37`; on every pass evaluate face→chin→eye→nose→mouth emissions using `resolution.strengths`, call each `sanitizing` against the current `nextBaseline`, and never reconstruct a removed value from the original request. After convergence, recompute all five final provider emissions once from final strengths. Replace the current face-domain call to unified `BeautyGeometryEffectPipeline.controlPoints`—which includes sibling eye/nose/mouth points—with `finalFaceEmissions.points.count + finalChinEmissions.points.count`; use each final eye/nose/mouth emission result once for its domain and add each provider count once. Keep `BeautyGeometryEffectPipeline` unchanged and verify its output from the same final strengths equals the counted concatenation. Preserve one combined warning and aggregate `weakenedCount`/`geometryStrengthScale` only when the retained total exceeds the threshold.</action>
  <acceptance_criteria>Exact 37/11.70 arithmetic passes; source contains one 37 loop and no 28 loop; provider-empty pre/post conflict cases cannot re-enter; final strengths and named arrays agree; domain/metric point counts equal the existing unified pipeline array with no duplication.</acceptance_criteria>
  <verify>
    <automated>swift test --package-path BeautySDK --disable-sandbox --filter BeautyEffectsTests.GeometryConflictResolverTests &amp;&amp; swift test --package-path BeautySDK --disable-sandbox --filter BeautyEffectsTests.CombinedEffectSafetyTests &amp;&amp; swift test --package-path BeautySDK --disable-sandbox --filter BeautyEffectsTests.BeautyGeometryEffectPipelineTests &amp;&amp; git diff --check</automated>
  </verify>
  <done>All 37 geometry fields converge monotonically on one provider-eligible set and every final point/effective/accounting channel agrees exactly once.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 46-05-03: Supply deterministic observed support and turn facade/degradation routes GREEN</name>
  <files>BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift</files>
  <read_first>AGENTS.md; PLANS.md; SECURITY.md; RELIABILITY.md; .planning/phases/45-public-contract-and-observed-face-support/45-VERIFICATION.md; .planning/phases/46-independent-contour-and-chin-geometry/46-CONTEXT.md; .planning/phases/46-independent-contour-and-chin-geometry/46-RESEARCH.md; 46-03-SUMMARY.md; BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift; BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift; BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift; BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift; BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift</read_first>
  <behavior>
    - Existing `.usableFace` supplies an asymmetric 11-point raw contour and 3-point median that Phase 45 maps/validates into contourEligible+centerlineEligible support with a deterministic apex.
    - Each isolated new public field invokes detection once, preserves extent, produces positive aggregate geometry points, and keeps raw/derived geometry absent from public diagnostics.
    - Existing shipped faceSlim and both chinLength directions keep their exact zero-new-field route/point behavior.
  </behavior>
  <action>Per D-15/D-16/D-18/D-19/D-21/D-22, add only the two private constants listed in Artifacts and attach `BeautyObservedFaceSupport` to the existing `.usableFace` `VisionDetectionObservation`. Express points in the observation's face-local normalized coordinate space so the existing bounds composition, mapper, and Phase 45 validator—not a test-only bypass—produce the intended canonical image-normalized asymmetric contour and median. Use 11 contour samples with distinct upper/middle/apex-adjacent positions and three top-to-bottom median samples; keep every raw value finite and within 0...1 and retain the existing stableID/confidence/area/bounds/landmark groups. Do not add a fixture enum case, public support/result type, mapper shortcut, adapter bypass, second detector request, cache, persistence, or diagnostic field. Run the facade and representative degradation suites after Tasks 01/02 so every Wave 0 route becomes GREEN and shipped fixture behavior stays exact.</action>
  <acceptance_criteria>The existing fixture maps through production boundaries into complete observed semantic support; four isolated facade routes and redaction pass; shipped face/chin zero-new-field regression passes; no public/SPI inventory or detector request count changes.</acceptance_criteria>
  <verify>
    <automated>swift test --package-path BeautySDK --disable-sandbox --filter BeautyCoreTests.BeautyEngineGeometryFacadeTests &amp;&amp; swift test --package-path BeautySDK --disable-sandbox --filter BeautyEffectsTests.MissingLandmarkDegradationTests &amp;&amp; git diff --check</automated>
  </verify>
  <done>The deterministic public-facade and representative degradation contracts are GREEN through the real observed-support mapping/adapter route.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Capped/freshness-adjusted fields → shared conflict ledger | Only provider-emitting retained fields may affect bounded arithmetic or final evidence. |
| Testing SPI raw observation → production mapper/adapter/facade | Fixture coordinates remain private and must traverse the same validation boundary as real Vision support. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-46-12 | Denial of Service | high | resolver convergence | mitigate | Exact `0..<37`, subset-only retained baseline, equality termination, source-level test, and focused combined suite in Task 46-05-02. |
| T-46-13 | Tampering / Repudiation | high | resolver/conflict accounting | mitigate | Face/chin preflight plus all-provider post-scale sanitization, exact 11.70/37 arithmetic, and provider-to-metric point equality in Tasks 01/02. |
| T-46-14 | Information Disclosure | high | testing SPI/public facade | mitigate | Existing production mapper/adapter route, aggregate-only facade assertions, expanded forbidden-token scan, and focused facade command in Task 03. |
| T-46-15 | Tampering | high | freshness/support transitions | mitigate | Exact no-face/stale zero, eligible reused 0.5, field-local support failure, and fresh-reused-stale-fresh suite in Tasks 01/03. |
| T-46-SC | Tampering | medium | package/dependencies | mitigate | No install/manifest change; Phase 01 checker live mode blocks drift. |
</threat_model>

<verification>
- Resolver, conflict, combined, pipeline, degradation, and facade focused suites pass.
- `rg -n "for _ in 0..<" BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift` shows exactly one `0..<37`.
- `git diff --check` passes and only the three planned source files change.
</verification>

<success_criteria>
- All four fields route from public scalar to final unified-warp points.
- Provider-empty work is excluded before and after conflict and from every aggregate channel.
- Deterministic observed-support facade evidence is positive and redacted.
</success_criteria>

<output>
Create `.planning/phases/46-independent-contour-and-chin-geometry/46-05-SUMMARY.md` when done.
</output>
