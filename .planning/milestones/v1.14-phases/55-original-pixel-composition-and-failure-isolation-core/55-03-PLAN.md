---
phase: 55-original-pixel-composition-and-failure-isolation-core
plan: "03"
type: execute
wave: 2
depends_on: [55-02]
files_modified:
  - BeautySDK/Sources/BeautyEffects/Render/BeautyLocalRetouchComposition.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautyLocalRetouchCompositionTests.swift
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, COMP-04, COMP-05]
security_standard: "OWASP ASVS Level 1"
block_on: HIGH
must_haves:
  truths:
    - "D-08/D-55-08 and D-05/D-55-05/COMP-02/05: composition is integer-defined, deterministic, order-independent, and every channel blend reads immutable canonical RGB rather than a prior contribution/output."
    - "D-09/D-55-09 and D-12/D-55-12/COMP-03: final soft Q16 weight is clamped then re-intersected with hard containment; zero/outside work is unowned and every outside-union byte plus all alpha remain canonical."
    - "D-11/D-55-11 and D-15/D-55-15/COMP-04: two or more accepted units at one pixel increment one collision-pixel aggregate, preserve canonical source at that pixel, and keep every noncolliding sibling pixel."
    - "D-07/D-55-07, D-10/D-55-10, D-13/D-55-13 through D-18/D-55-18/COMP-01/04/05: invalid units abstain locally, duplicate/token failures isolate, opaque region/subregion failures preserve accepted siblings, and valid-invalid-valid owns no stale state."
    - "D-19/D-55-19 and D-20/D-55-20: the result exposes only the exact six aggregate counts and makes no anatomy, output-digest, performance, device, or product claim."
  artifacts:
    - path: BeautySDK/Sources/BeautyEffects/Render/BeautyLocalRetouchComposition.swift
      provides: Deterministic sparse multiway original-pixel composition, collision suppression, result carrier, and aggregate summary
      contains: "func compose"
    - path: BeautySDK/Tests/BeautyEffectsTests/BeautyLocalRetouchCompositionTests.swift
      provides: Complete GREEN literal-byte, fusion, collision, failure, and lifecycle mechanics matrix
  key_links:
    - from: BeautySDK/Sources/BeautyEffects/Render/BeautyLocalRetouchComposition.swift
      to: BeautySDK/Sources/BeautyCore/Models/BeautyCanonicalStillImage.swift
      via: output begins from source rgba8Data and reconstructs only a changed canonical RGBA8 value with identical normalized metadata
      pattern: "rgba8Data|BeautyCanonicalStillImage"
    - from: BeautySDK/Tests/BeautyEffectsTests/BeautyLocalRetouchCompositionTests.swift
      to: BeautySDK/Sources/BeautyEffects/Render/BeautyLocalRetouchComposition.swift
      via: literal standalone/merge/fused/permutation/failure oracles independent of production helpers
      pattern: "standalone|fused|collision|validInvalidValid"
  prohibitions:
    - statement: "Sequential local feedback, floating/device-dependent blending, unordered reduction, max/strength/anatomy/array-order priority, or pre-feather-only clipping is forbidden."
      status: resolved
      verification: byte-oracle-and-source-checker
    - statement: "Collision pixels, zero-weight pixels, outside-union pixels, and alpha bytes must never receive a local write."
      status: resolved
      verification: literal-byte-matrix
---

<objective>
Complete the deterministic original-pixel composer, collision-to-source semantics, aggregate-only result, and smallest-unit failure isolation.

Purpose: Turn the validated source/preflight contracts into one order-independent request-local composition result whose every write is attributable to exactly one accepted unit and immutable original bytes.
Output: Complete `compose(_:)`, exact result/summary values, and a fully GREEN unit-level COMP-01..05 byte/failure matrix.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/55-original-pixel-composition-and-failure-isolation-core/55-CONTEXT.md
@.planning/phases/55-original-pixel-composition-and-failure-isolation-core/55-RESEARCH.md
@.planning/phases/55-original-pixel-composition-and-failure-isolation-core/55-PATTERNS.md
@.planning/phases/55-original-pixel-composition-and-failure-isolation-core/55-VALIDATION.md
@.planning/phases/55-original-pixel-composition-and-failure-isolation-core/55-02-SUMMARY.md
@.codex/skills/spike-findings-beauty/references/still-image-integration.md

<interfaces>
Complete these package-only contracts created by Plan 55-02:
- `BeautyLocalRetouchCompositionSummary` with exactly `acceptedUnitCount`, `rejectedUnitCount`, `ownedPixelCount`, `changedPixelCount`, `changedOutsideUnionPixelCount`, and `collisionPixelCount`.
- `BeautyLocalRetouchCompositionResult` with the composed `BeautyCanonicalStillImage` and summary; no public/SPI description, digest, proposal, token, or anatomy.
- `BeautyLocalRetouchCompositionOwner.compose(_:) throws -> BeautyLocalRetouchCompositionResult`.
- Exact channel formula: `((source * (65_536 - weight)) + (target * weight) + 32_768) / 65_536` using UInt64 after clamping weight to 65_536.
</interfaces>
</context>

<artifacts_this_phase_produces>
- `BeautyLocalRetouchCompositionSummary` exact six-count allowlist.
- `BeautyLocalRetouchCompositionResult` containing only the canonical result value plus summary at package scope.
- `BeautyLocalRetouchCompositionOwner.compose(_:)` and private checked/preflight/blend/multiway-reduction helpers.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 55-03-01: Compose uniquely owned pixels once from canonical RGB with hard re-clipping</name>
  <read_first>
    BeautySDK/Sources/BeautyEffects/Render/BeautyLocalRetouchComposition.swift
    BeautySDK/Sources/BeautyCore/Models/BeautyCanonicalStillImage.swift
    BeautySDK/Tests/BeautyEffectsTests/BeautyLocalRetouchCompositionTests.swift
    .planning/phases/55-original-pixel-composition-and-failure-isolation-core/55-RESEARCH.md
    .planning/phases/55-original-pixel-composition-and-failure-isolation-core/55-VALIDATION.md
    .codex/skills/spike-findings-beauty/references/still-image-integration.md
  </read_first>
  <files>BeautySDK/Sources/BeautyEffects/Render/BeautyLocalRetouchComposition.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyLocalRetouchCompositionTests.swift</files>
  <action>Implement the noncollision path of `compose(_:)`. Preflight all units before ownership; for each valid proposal compute `effectiveWeightQ16 = isInsideHardEnvelope ? min(softWeightQ16, 65_536) : 0`, drop zero effective claims before ownership, and sort remaining claims by pixel index using an explicit stable deterministic comparator. Initialize mutable output from the captured canonical `rgba8Data` exactly once. For a uniquely owned index, read source RGB only from the immutable canonical data and compute each channel with UInt64 round-half-up `source*(65_536-w) + target*w + 32_768`, divided by 65_536; use reporting-overflow guards for index/channel math, preserve source alpha without writing it, and never read the mutable output as source. Define summary semantics exactly: accepted units are structurally valid with at least one effective claim; invalid or effective-empty units are rejected/abstained; `ownedPixelCount` counts unique post-collision owners; `changedPixelCount` counts unique owned pixels whose final RGB differs; `changedOutsideUnionPixelCount` is mechanically zero because writes occur only at unique-owner indices. Return the exact original canonical value rather than reconstructing storage when no RGB byte changed; otherwise construct one checked `BeautyCanonicalStillImage` with the same dimensions, rowBytes, and normalized metadata. Turn the Q16 endpoint/midpoint/clamp/rounding, alpha, hard-false, zero-weight, outside-union, standalone, explicit-merge, fused-disjoint, reversed, and permutation tests GREEN without weakening literal equality.</action>
  <verify>
    <automated>CLANG_MODULE_CACHE_PATH=/private/tmp/beauty-phase55-clang-module-cache swift test --package-path BeautySDK --filter 'BeautyLocalRetouchCompositionTests/(testQ16LiteralBlendAndAlpha|testHardReclipZeroWeightAndOutsideUnionIdentity|testStandaloneMergedFusedAndPermutedOutputs)' &amp;&amp; python3 .planning/phases/55-original-pixel-composition-and-failure-isolation-core/check_phase55_composition_boundaries.py --composition &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Q16 0/midpoint/full/oversized literals and round-half-up bytes match independently authored expectations exactly on every RGB channel.
    - Hard-false and zero-weight proposals are unowned, not collision candidates, and leave source bytes/counts unchanged; all alpha and outside-union bytes are exact.
    - All permutations of three disjoint units produce identical fused bytes and summary matching independent standalone/merge literals.
    - The source contains no float blend, mutable-output source read, device color conversion, unordered ownership reduction, or reconstruct-on-no-change behavior.
  </acceptance_criteria>
  <done>Validation row `55-03-01` is green for exact hard-reclipped Q16 original-pixel composition, alpha/outside identity, and stable order-independent disjoint fusion.</done>
</task>

<task type="auto">
  <name>Task 55-03-02: Suppress collisions pixel-locally and isolate every invalid or absent opaque unit</name>
  <read_first>
    BeautySDK/Sources/BeautyEffects/Render/BeautyLocalRetouchComposition.swift
    BeautySDK/Tests/BeautyEffectsTests/BeautyLocalRetouchCompositionTests.swift
    .planning/phases/55-original-pixel-composition-and-failure-isolation-core/55-THREAT-INVENTORY.json
    .planning/phases/55-original-pixel-composition-and-failure-isolation-core/check_phase55_composition_boundaries.py
    SECURITY.md
    RELIABILITY.md
  </read_first>
  <files>BeautySDK/Sources/BeautyEffects/Render/BeautyLocalRetouchComposition.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyLocalRetouchCompositionTests.swift</files>
  <action>Complete the sorted multiway ownership reduction across at most eight preflighted units. At each smallest current pixel index, exactly one claim follows the canonical-source blend path; two or more distinct accepted units advance every cursor at that index, increment `collisionPixelCount` once for that pixel, perform no write, and do not increment owned/changed/outside counts. Never reject the whole unit for a cross-unit collision; adjacent noncolliding claims from every accepted unit still compose. Preserve Plan 55-02 local abstention for source/owner/token/count/index/offset/raw-duplicate/effective-empty failures, with all repeated occurrences of one duplicated token rejected and every unrelated unique valid unit retained. Finish `BeautyLocalRetouchCompositionSummary` as an immutable six-field package value with no reflection/description/raw reason surface. Turn the complete literal unit suite GREEN, including two/three-owner source retention, adjacent unique work, duplicate-before-filter, duplicate token, foreign source, empty input, opaque A-only/pair/B-only/C-only/future-band failures, byte-identical unaffected siblings, and valid-invalid-valid output/summary nonretention. The conceptual failure matrix may document teeth/whole-sclera/left/right only in test names/comments mapped to COMP-01/05; production source remains feature-neutral.</action>
  <verify>
    <automated>CLANG_MODULE_CACHE_PATH=/private/tmp/beauty-phase55-clang-module-cache swift test --package-path BeautySDK --filter BeautyLocalRetouchCompositionTests &amp;&amp; python3 .planning/phases/55-original-pixel-composition-and-failure-isolation-core/check_phase55_composition_boundaries.py --privacy &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Two and three owners at one index each yield exactly one collision count, exact source RGBA, and retained adjacent unique sibling changes independent of unit order.
    - Every malformed/foreign/duplicate/effective-empty unit is an isolated abstention; opaque whole-region and left/right conceptual failures preserve all unaffected standalone bytes.
    - Empty and valid-invalid-valid calls retain no earlier unit, token, claim, output, collision, or summary state.
    - Summary shape is exactly six aggregate integers and public/SPI/Codable/log/description scans reveal no mechanics or digest.
  </acceptance_criteria>
  <done>Validation row `55-03-02` is green: collision-to-source is pixel-local, every unit failure is isolated, all literal fusion/failure oracles pass, and diagnostics remain aggregate-only.</done>
</task>

</tasks>

<threat_model>
## ASVS Level 1 Threat Mapping

`security_standard: OWASP ASVS Level 1`; `block_on: HIGH`. The complete unit suite and checker composition/privacy modes must make all algorithmic and disclosure mitigations green. Any failed, skipped, missing, count-only, or unclassified HIGH result blocks the plan.

| Threat ID | STRIDE | Severity | Disposition | Mitigation plan |
|---|---|---:|---|---|
| T-55-01 | Spoofing / Tampering | HIGH | mitigate | retain exact-source preflight and foreign byte-equal rejection in complete suite |
| T-55-02 | Tampering / Denial of Service | HIGH | mitigate | checked sparse cursors/index/channel arithmetic and bounded output allocation |
| T-55-03 | Tampering | HIGH | mitigate | raw duplicate/token rejection plus all unit permutations exact |
| T-55-04 | Tampering | HIGH | mitigate | sorted two/three-owner collision-to-source, one count per pixel, no priority branch |
| T-55-05 | Information Disclosure | HIGH | mitigate | exact six-count package summary and public/SPI/Codable/log/digest privacy mode |
| T-55-06 | Tampering / Information Disclosure | HIGH | mitigate | owner-local immutable call state and valid-invalid-valid zero-retention tests |
| T-55-07 | Elevation of Privilege / Tampering | HIGH | mitigate | production source stays opaque/feature-neutral and checker preserves exact-empty closed-gate scope |
</threat_model>

<verification>
The full focused `BeautyLocalRetouchCompositionTests` suite, checker composition/privacy modes, and diff hygiene pass with literal bytes and exact summaries. Full SwiftPM and Demo remain final-only in Task 55-05-01.
</verification>

<success_criteria>
The package core produces deterministic canonical output from original pixels only, with post-filter hard containment, exact single ownership, collision-to-source, aggregate-only diagnostics, and smallest-unit failure isolation across all COMP-01..05 mechanics.
</success_criteria>

<output>
Create `.planning/phases/55-original-pixel-composition-and-failure-isolation-core/55-03-SUMMARY.md` when done.
</output>
