---
phase: 53-canonical-still-image-contract-and-private-request-foundatio
plan: "02"
type: execute
wave: 1
depends_on: [53-01]
files_modified:
  - BeautySDK/Sources/BeautyCore/Models/BeautyCanonicalStillImage.swift
  - BeautySDK/Sources/BeautySDK/BeautyStillImageCanonicalizer.swift
  - BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyCanonicalStillImageTests.swift
  - DESIGN.md
  - SECURITY.md
  - RELIABILITY.md
  - PRODUCT_SENSE.md
  - PLANS.md
autonomous: true
requirements: [PATH-02, PATH-03]
must_haves:
  truths:
    - "D-05/D-08: one accepted local still input owns one zero-origin, up-oriented, explicit sRGB RGBA8 allocation and normalized .up/not-mirrored metadata shared by downstream consumers."
    - "D-06/D-07: nonopaque, malformed-orientation, unknown/non-RGB/extended-range, invalid-extent, overflow, and over-ceiling inputs return a fixed BeautyError before Vision/support work."
    - "D-16: decoded CIImage facts are enforced honestly; no encoded-byte, gain-map, HDR, or container-validation claim is made."
    - "D-17: the smallest dependency-safe layout is executable in `BeautyCore` plus the still-image facade only: one package-only immutable carrier and one canonicalizer, with no new target or reversed import."
    - "D-18: observable rejection reuses payload-free `BeautyError.invalidInput` / `.unsupportedPixelFormat`; a new error exists only if a tested distinct caller action cannot be expressed by those cases."
  artifacts:
    - path: BeautySDK/Sources/BeautyCore/Models/BeautyCanonicalStillImage.swift
      provides: Package-only immutable owned canonical raster
    - path: BeautySDK/Sources/BeautySDK/BeautyStillImageCanonicalizer.swift
      provides: Validate/orient/color-manage/render-once/opacity boundary
  key_links:
    - from: BeautySDK/Sources/BeautySDK/BeautyStillImageCanonicalizer.swift
      to: BeautySDK/Sources/BeautyCore/Models/BeautyCanonicalStillImage.swift
      via: one checked RGBA8 allocation
      pattern: "BeautyCanonicalStillImage"
---

<objective>
Implement the single-owner canonical still-image boundary specified by Wave 0.

Purpose: Make accepted local-retouch inputs deterministic and reject unsafe inputs before Vision without changing legacy inactive behavior.
Output: `BeautyCanonicalStillImage`, `BeautyStillImageCanonicalizer`, green canonical tests, and owner-contract updates.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-canonical-still-image-contract-and-private-request-foundatio/53-CONTEXT.md
@.planning/phases/53-canonical-still-image-contract-and-private-request-foundatio/53-RESEARCH.md
@.planning/phases/53-canonical-still-image-contract-and-private-request-foundatio/53-PATTERNS.md
@.planning/phases/53-canonical-still-image-contract-and-private-request-foundatio/53-01-SUMMARY.md
</context>

<tasks>
<task id="53-02-01" type="auto" tdd="true">
  <name>Task 1: Add the immutable canonical raster carrier</name>
  <read_first>
    BeautySDK/Sources/BeautyCore/Models/BeautyInputMetadata.swift
    BeautySDK/Sources/BeautyCore/Models/BeautyError.swift
    BeautySDK/Tests/BeautyCoreTests/BeautyCanonicalStillImageTests.swift
    .planning/phases/53-canonical-still-image-contract-and-private-request-foundatio/53-RESEARCH.md
    DESIGN.md
    PLANS.md
  </read_first>
  <files>BeautySDK/Sources/BeautyCore/Models/BeautyCanonicalStillImage.swift, BeautySDK/Tests/BeautyCoreTests/BeautyCanonicalStillImageTests.swift, DESIGN.md, PLANS.md</files>
  <behavior>
    - Carrier is package-only, immutable, non-Codable, Sendable where ownership permits, and exposes no byte-bearing diagnostic representation.
    - Width/height/rowBytes/data count are exact and checked; its CIImage view uses the same owned bytes, `.RGBA8`, explicit sRGB, and zero origin.
  </behavior>
  <action>Create `BeautyCanonicalStillImage` in BeautyCore per D-05 and D-17's smallest dependency-safe file layout. Store one owned RGBA8 backing plus exact dimensions/rowBytes and normalized metadata; construct all views from that backing. Do not add a target, import Vision/Effects/SDK, expose public/SPI bytes, or create device-RGB or a second canonical copy. Turn the carrier-prefixed cases in `BeautyCanonicalStillImageTests` green, then record the verified D-17 contract and command in DESIGN.md and PLANS.md.</action>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyCanonicalStillImageTests &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>
    - Source contains `package` carrier ownership, `.RGBA8`, explicit sRGB, checked row/total byte arithmetic, and no `public`, `Codable`, `CustomStringConvertible`, or device-RGB byte surface.
    - Focused tests pass exact size/rowBytes/zero-origin/same-backing assertions.
    - DESIGN.md and PLANS.md record D-05 ownership and verification.
  </acceptance_criteria>
  <done>D-17 is implemented by exactly one package-only BeautyCore carrier file with one owned backing and dependency-safe imports; focused carrier assertions and `git diff --check` pass; DESIGN.md and PLANS.md record the verified layout.</done>
</task>

<task id="53-02-02" type="auto" tdd="true">
  <name>Task 2: Implement validate-orient-color-manage-render-once rejection ordering</name>
  <read_first>
    BeautySDK/Sources/BeautySDK/BeautyEngine.swift
    BeautySDK/Sources/BeautyCore/Models/BeautyConfiguration.swift
    BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift
    BeautySDK/Tests/BeautyCoreTests/BeautyCanonicalStillImageTests.swift
    SECURITY.md
    RELIABILITY.md
    PRODUCT_SENSE.md
    PLANS.md
  </read_first>
  <files>BeautySDK/Sources/BeautySDK/BeautyStillImageCanonicalizer.swift, BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift, BeautySDK/Tests/BeautyCoreTests/BeautyCanonicalStillImageTests.swift, SECURITY.md, RELIABILITY.md, PRODUCT_SENSE.md, PLANS.md</files>
  <behavior>
    - Preflight finite positive decoded extent and configured pixel ceiling before checked integer conversion/allocation; consume orientation and input mirroring exactly once.
    - Reject invalid orientation, nil/non-RGB/extended-range color before render; render once through a reused explicit-sRGB CIContext; reject any alpha byte !=255 before returning the carrier.
    - Return only `.invalidInput` or `.unsupportedPixelFormat` unless a narrowly additive payload-free error has a distinct caller action.
  </behavior>
  <action>Implement `BeautyStillImageCanonicalizer` per D-06/D-07/D-08/D-16 and D-18. Use `CIImage.oriented(forExifOrientation:)`, consume `isInputMirrored`, translate integral bounds to zero origin, recheck dimensions/overflow, render directly into the carrier backing with `.RGBA8` and explicit sRGB, then scan alpha without forcing/compositing it. Reuse `CIContext`, but keep pixels request-owned. Reuse payload-free `BeautyError.invalidInput` / `.unsupportedPixelFormat`; add a typed error only if a focused test proves a distinct stable caller action, and never attach metadata, paths, dimensions, pixels, or support. Add only opaque aggregate SPI counters/summaries needed by tests. Finish every PATH-02/PATH-03 case, including exact/one-over limits and the encoded-input observability nonclaim. Update SECURITY, RELIABILITY, PRODUCT_SENSE, and PLANS with the executable D-18 error choice and exact verification.</action>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyCanonicalStillImageTests &amp;&amp; python3 .planning/phases/53-canonical-still-image-contract-and-private-request-foundatio/check_still_image_foundation_boundaries.py --self-test &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>
    - All canonical tests pass, including eight orientations/mirror variants, sRGB/P3 conversion, invalid size/orientation/color/range, exact-limit/one-over, overflow, and alpha rejection.
    - Detector/support counters remain zero for every rejected input and errors contain no metadata, path, dimensions, pixels, or support.
    - Docs explicitly reject transparent/HDR/gain-map/extended-range support and encoded-byte observability overclaim.
  </acceptance_criteria>
  <done>D-18's payload-free typed rejection contract and D-06/D-07/D-08/D-16 canonicalization ordering are green in `BeautyCanonicalStillImageTests`; checker self-tests and `git diff --check` pass; all four owner documents and PLANS.md record the exact error/nonclaim boundary.</done>
</task>
</tasks>

<threat_model>
## ASVS Level 1 Completion Gate

`security_standard: OWASP ASVS Level 1`; `block_on: HIGH`. Automated mitigation verification is named and mandatory: `swift test --package-path BeautySDK --filter BeautyCanonicalStillImageTests`, `python3 .planning/phases/53-canonical-still-image-contract-and-private-request-foundatio/check_still_image_foundation_boundaries.py --self-test`, and `git diff --check`. Any failed, skipped, not-run, or otherwise unverified HIGH mitigation forbids task completion, plan completion, and `53-02-SUMMARY.md` publication.

| Threat ID | STRIDE | Severity | Disposition | Verifiable mitigation |
|---|---|---|---|---|
| T-53-01 | DoS | HIGH | mitigate | configured ceiling plus checked row/total multiplication before allocation; exact/one-over/overflow tests |
| T-53-02 | Tampering | HIGH | mitigate | reject any alpha !=255 before returning canonical carrier; no composite/force-alpha path |
| T-53-03 | Tampering | HIGH | mitigate | reject unknown/non-RGB/extended-range and render with explicit sRGB RGBA8 |
| T-53-04 | Information Disclosure | HIGH | mitigate | non-public/non-Codable carrier, fixed errors, no byte diagnostics, checker scans |
| T-53-05 | Tampering | HIGH | mitigate | carrier is request value with no cache/static/engine pixel field |
| T-53-06 | Elevation of Privilege | HIGH | mitigate | canonicalizer is not wired to pixel-buffer/realtime in this plan |
| T-53-SC | Tampering | LOW | accept | no package install or dependency change |
</threat_model>

## Artifacts this phase produces

- New package-only symbol/file `BeautyCanonicalStillImage` / `BeautyCanonicalStillImage.swift`.
- New package-internal symbol/file `BeautyStillImageCanonicalizer` / `BeautyStillImageCanonicalizer.swift`.
- Any new SPI is limited to opaque invocation counters/summaries; list its exact name in `53-02-SUMMARY.md`.

<verification>
Run the canonical suite after each task, checker self-tests after Task 2, and `git diff --check` after both.
</verification>
<success_criteria>
PATH-02/PATH-03 and D-05 through D-08/D-16 are green with one opaque canonical allocation, typed fail-before-Vision behavior, and explicit observability/nonclaim contracts.
</success_criteria>
<output>Create `.planning/phases/53-canonical-still-image-contract-and-private-request-foundatio/53-02-SUMMARY.md` when done.</output>
