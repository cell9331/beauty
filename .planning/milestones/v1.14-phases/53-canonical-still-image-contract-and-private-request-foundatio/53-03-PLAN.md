---
phase: 53-canonical-still-image-contract-and-private-request-foundatio
plan: "03"
type: execute
wave: 2
depends_on: [53-02]
files_modified:
  - BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift
  - BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift
  - BeautySDK/Tests/BeautyDetectionTests/StillImageRequestSupportTests.swift
  - DESIGN.md
  - SECURITY.md
  - RELIABILITY.md
  - PLANS.md
autonomous: true
requirements: [PATH-04]
must_haves:
  truths:
    - "D-09: one active still request canonicalizes once, performs one existing VNDetectFaceLandmarksRequest and one mapping boundary, then shares one selected-face request context."
    - "D-10/D-11/D-12: actual eye/brow/face plus new actual lip support is immutable, package-only, request-local, aggregate-only in diagnostics, and cannot survive valid-invalid-valid, no-face, reset, or independent parallel values."
    - "D-17: actual outer/inner lip support extends the existing Detection carriers in place; no new target, proxy geometry, second request, or dependency reversal is introduced."
  artifacts:
    - path: BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift
      provides: New BeautyObservedLipSupport and observation attachment
  key_links:
    - from: BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift
      to: BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift
      via: actual outerLips/innerLips copied and mapped in existing request
      pattern: "observedLipSupport"
  flagged_assumptions:
    - "Same-engine concurrency and cooperative cancellation remain unclaimed under TD-013; tests prove value/request ownership without asserting the mutable engine is concurrently safe."
---

<objective>
Capture actual request-local lip support in the existing Vision request and mapping boundary.

Purpose: Give later admitted providers honest outer/inner lip provenance without a proxy, second request, retained support, or public surface.
Output: Actual observed lip support, green support/detector/mapping tests, and synchronized Detection-owned contracts.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-canonical-still-image-contract-and-private-request-foundatio/53-CONTEXT.md
@.planning/phases/53-canonical-still-image-contract-and-private-request-foundatio/53-RESEARCH.md
@.planning/phases/53-canonical-still-image-contract-and-private-request-foundatio/53-PATTERNS.md
@.planning/phases/53-canonical-still-image-contract-and-private-request-foundatio/53-02-SUMMARY.md
</context>

<tasks>
<task id="53-03-01" type="auto" tdd="true">
  <name>Task 1: Capture and map actual lip support in the existing Vision request</name>
  <read_first>
    BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift
    BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift
    BeautySDK/Tests/BeautyDetectionTests/VisionFaceDetectorTests.swift
    BeautySDK/Tests/BeautyDetectionTests/FaceObservationMappingTests.swift
    BeautySDK/Tests/BeautyDetectionTests/StillImageRequestSupportTests.swift
    DESIGN.md
    SECURITY.md
    RELIABILITY.md
    PLANS.md
  </read_first>
  <files>BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift, BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift, BeautySDK/Tests/BeautyDetectionTests/StillImageRequestSupportTests.swift, DESIGN.md, SECURITY.md, RELIABILITY.md, PLANS.md</files>
  <behavior>
    - Actual Vision outerLips and innerLips coordinate values are copied in the existing landmarks request, independently preflighted, mapped once, and attached as optional request evidence.
    - Empty/oversized/non-finite/out-of-unit support fails only that lip region; valid sibling eye/brow/face/lip evidence and selected face remain intact.
    - Stable provider order and ties are preserved; no synthetic `FaceGeometry` lip proxy, sorting, retry, cache, or second request is used.
  </behavior>
  <action>Add package-only immutable non-Codable `BeautyObservedLipSupport` with independent `outer`/`inner` optionals, append defaulted optional properties to `VisionDetectionObservation` and `BeautyFaceObservation`, and extend their descriptions/mirrors with counts only. In `VisionFaceDetector.landmarks` copy actual outer/inner Vision values from the existing `VNDetectFaceLandmarksRequest`; in `mapObservation` preflight at a fixed research-backed ceiling and map accepted points once through the existing mapper. This is D-17's smallest in-place Detection extension: no target, request, proxy, cache, or outward dependency is added. Turn `StillImageRequestSupportTests` green for D-09/D-10/D-11/D-12, including the exact PATH04-BOUNDARY/EMPTY/PRECISION test names, malformed sibling isolation, trace/order, privacy allowlists, valid-invalid-valid, no-face, and independent task-group values. Update DESIGN, SECURITY, RELIABILITY, and PLANS with their distinct owners and the PATH04-CONCURRENCY nonclaim.</action>
  <verify><automated>swift test --package-path BeautySDK --filter 'StillImageRequestSupportTests|VisionFaceDetectorTests|FaceObservationMappingTests' &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>
    - Exactly one `VNDetectFaceLandmarksRequest` remains in production source; no lip-specific request or proxy is introduced.
    - Outer/inner absence and rejection are independent, mapping counts equal accepted point counts, and diagnostics contain counts only.
    - All three focused detection suites pass; docs and PLANS record D-09 through D-12 and the concurrency residual.
  </acceptance_criteria>
  <done>D-09 through D-12 and D-17 are green through the exact existing Vision request/mapping path; outer/inner failures remain independent and aggregate-only; the focused Detection command and `git diff --check` pass; owner docs and PLANS.md record the concurrency nonclaim.</done>
</task>

</tasks>

<threat_model>
## ASVS Level 1 Completion Gate

`security_standard: OWASP ASVS Level 1`; `block_on: HIGH`. Automated mitigation verification is `swift test --package-path BeautySDK --filter 'StillImageRequestSupportTests|VisionFaceDetectorTests|FaceObservationMappingTests'` plus `git diff --check`. Any failed, skipped, not-run, or otherwise unverified HIGH mitigation forbids task completion, plan completion, and `53-03-SUMMARY.md` publication.

| Threat ID | STRIDE | Severity | Disposition | Verifiable mitigation |
|---|---|---|---|---|
| T-53-04 | Information Disclosure | HIGH | mitigate | package-only non-Codable support/context and aggregate description/mirror/checker scans |
| T-53-05 | Tampering | HIGH | mitigate | stack-local context plus valid-invalid-valid/no-face/reset/parallel-value tests |
| T-53-SC | Tampering | LOW | accept | no dependency install or Package.swift change |
</threat_model>

## Artifacts this phase produces

- New package-only symbol `BeautyObservedLipSupport` in existing `BeautyFaceObservation.swift`.
- No testing SPI, raw support/bytes, candidate name, second Vision request, or proxy lip geometry is new.

<verification>Run the focused combined Detection filter and `git diff --check`; this per-task check targets less than 30 seconds on an incremental build.</verification>
<success_criteria>PATH-04 and D-09 through D-12/D-17 are green through the one existing detector/mapping route with actual request-local lip support, aggregate-only diagnostics, and the PATH04-CONCURRENCY nonclaim.</success_criteria>
<output>Create `.planning/phases/53-canonical-still-image-contract-and-private-request-foundatio/53-03-SUMMARY.md` when done.</output>
