---
phase: 53-canonical-still-image-contract-and-private-request-foundatio
plan: "01"
type: tdd
wave: 0
depends_on: []
files_modified:
  - BeautySDK/Tests/BeautyCoreTests/BeautyCanonicalStillImageTests.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyEngineLocalRetouchFoundationTests.swift
  - BeautySDK/Tests/BeautyDetectionTests/StillImageRequestSupportTests.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift
  - BeautySDK/Tests/BeautyResourcesTests/BeautyResourceCatalogTests.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift
  - .planning/phases/53-canonical-still-image-contract-and-private-request-foundatio/check_still_image_foundation_boundaries.py
  - PLANS.md
autonomous: true
requirements: [PATH-01, PATH-02, PATH-03, PATH-04, PATH-05, PATH-06, PATH-07]
must_haves:
  truths:
    - "PATH-02/PATH-03 manual review is resolved by executable RED specifications: one opaque zero-origin up-oriented sRGB RGBA8 carrier is shared by identity, and every invalid extent/orientation/color/range/alpha case stops before Vision."
    - "PATH-04 boundary coverage is exact: inactive demand invokes canonicalization/detection/mapping zero times; one or several injected private demands invoke each boundary exactly once; one-over-count evidence fails."
    - "PATH-04 empty and ordering coverage is explicit: no-face and nil lip support remain empty, valid-invalid-valid does not reuse support, and the trace order is canonicalize -> detect/select -> map -> request context -> render."
    - "PATH-04 precision coverage uses exact integer-count assertions and overflow-shaped inputs; no rounded counter or allocation arithmetic is accepted."
    - "PATH-06 adjacency/empty/ordering coverage locks zero and missing-key neutrality, exact 59 stored/encoded fields, and the stable five-preset inventory."
    - "PATH-07 adjacency/empty/ordering coverage locks the admission checklist at exact 0 and 1 bounds and append-only stored/Codable order without admitting a Phase 53 candidate."
    - "D-19 is executable: deterministic in-memory images, opaque counted collaborators, and mutation-tested source checks prove exactly-once work and request isolation without tracked portrait media or raw support SPI."
  artifacts:
    - path: BeautySDK/Tests/BeautyCoreTests/BeautyCanonicalStillImageTests.swift
      provides: Wave 0 canonical input and fail-before-Vision specification
    - path: BeautySDK/Tests/BeautyCoreTests/BeautyEngineLocalRetouchFoundationTests.swift
      provides: Wave 0 facade, safe-continuation, exactly-once, and pixel-buffer-isolation specification
    - path: BeautySDK/Tests/BeautyDetectionTests/StillImageRequestSupportTests.swift
      provides: Wave 0 actual-lip provenance, mapping, privacy, and request-lifetime specification
    - path: .planning/phases/53-canonical-still-image-contract-and-private-request-foundatio/check_still_image_foundation_boundaries.py
      provides: Fail-closed source/privacy/compatibility checker plus exact 16-row edge manifest and 13-automated/3-flagged equality gate
  key_links:
    - from: BeautySDK/Tests/BeautyCoreTests/BeautyEngineLocalRetouchFoundationTests.swift
      to: BeautySDK/Sources/BeautySDK/BeautyEngine.swift
      via: existing public process and processResult overloads only
      pattern: "process(Result)?\\("
    - from: BeautySDK/Tests/BeautyDetectionTests/StillImageRequestSupportTests.swift
      to: BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift
      via: counted observation provider and mapped support
      pattern: "invocationCount|observedLipSupport"
  flagged_assumptions:
    - "PATH01-CONCURRENCY: FLAGGED ASSUMPTION — same-BeautyEngine parallel safety and cooperative interruption/cancellation are unresolved under TD-013; Phase 53 proves independent request values and no retained support only, and makes no engine-concurrency claim."
    - "PATH04-CONCURRENCY: FLAGGED ASSUMPTION — independent detector/request values must not cross payloads, but the mutable selected-face policy prevents a same-engine concurrency guarantee in this phase."
    - "PATH05-CONCURRENCY: FLAGGED ASSUMPTION — pixel-buffer calls must have zero local-foundation invocations; same-engine parallel execution remains outside the claim rather than being auto-dismissed."
  prohibitions:
    - statement: "MUST NOT expose or persist canonical pixels, raw landmarks, lip/teeth geometry, pupils, sclera/eyelid support, masks, vein-like descriptors, or stable portrait signatures through public/SPI/Codable state, diagnostics, files, or network paths."
      status: unverified
      flagged: true
    - statement: "MUST NOT accept transparent input by silently compositing, forcing alpha opaque, preserving alpha without a background contract, or otherwise letting background choice tamper with Vision interpretation."
      status: unverified
      flagged: true
    - statement: "MUST NOT activate v1.14 local retouch from realtime/pixel-buffer routes or add a candidate field, CodingKey, preset key, provider, renderer case, or inert route before an independent Phase 54 admission decision."
      status: unverified
      flagged: true
---

<objective>
Create the complete Wave 0 RED contract for the feature-neutral still-image foundation before any production owner changes.

Purpose: Turn D-01 through D-19 and PATH-01 through PATH-07 into deterministic tests/checks, including all 16 specless edge items and three descriptor-less safety/value prohibitions.
Output: Three new XCTest suites, exact compatibility extensions, one fail-closed boundary checker, and a traceable RED entry in PLANS.md.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/53-canonical-still-image-contract-and-private-request-foundatio/53-CONTEXT.md
@.planning/phases/53-canonical-still-image-contract-and-private-request-foundatio/53-RESEARCH.md
@.planning/phases/53-canonical-still-image-contract-and-private-request-foundatio/53-VALIDATION.md
@.planning/phases/53-canonical-still-image-contract-and-private-request-foundatio/53-PATTERNS.md
@.codex/skills/spike-findings-beauty/references/still-image-integration.md
</context>

<tasks>

<task id="53-01-01" type="auto" tdd="true">
  <name>Task 1: Specify canonical raster validation and fail-closed source boundaries</name>
  <read_first>
    BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift
    BeautySDK/Sources/BeautySDK/BeautyEngine.swift
    BeautySDK/Sources/BeautyCore/Models/BeautyError.swift
    .planning/phases/53-canonical-still-image-contract-and-private-request-foundatio/53-RESEARCH.md
    .planning/phases/53-canonical-still-image-contract-and-private-request-foundatio/53-VALIDATION.md
    .codex/skills/spike-findings-beauty/references/still-image-integration.md
    PLANS.md
  </read_first>
  <files>BeautySDK/Tests/BeautyCoreTests/BeautyCanonicalStillImageTests.swift, .planning/phases/53-canonical-still-image-contract-and-private-request-foundatio/check_still_image_foundation_boundaries.py, PLANS.md</files>
  <behavior>
    - All eight EXIF orientations plus input-mirror variants normalize to identical zero-origin RGBA8 bytes for lossless synthetic inputs; Display-P3 is converted to sRGB without demanding cross-profile landmark/mask topology identity.
    - Empty/non-finite/fractional-invalid/overflow/one-over-ceiling extents, malformed orientation, nil/gray/CMYK/unknown/extended-range color, and any alpha byte below 255 return an allowlisted payload-free BeautyError before detector or support counters increment.
    - The checker fails on public/SPI/Codable raw support, candidate API/provider/renderer inventory, pixel-buffer local calls, device RGB on the active route, unclassified matches, missing paths, or command errors.
  </behavior>
  <action>Create `BeautyCanonicalStillImageTests` with tiny asymmetric in-memory images and counted no-op collaborators per D-19. Encode D-05 through D-08, D-16, PATH-02, PATH-03, the PATH-02/PATH-03 manual classifications, PATH-04 precision boundaries, and exact-limit/one-over/checked-multiplication cases. Create the standard-library checker with `--self-test` mutation cases named `PATH02-UNCLASSIFIED` and `PATH03-UNCLASSIFIED`, plus a live mode that is expected to fail until Plans 02-05 exist. Its machine-readable edge manifest must contain exactly the 16 IDs in this plan's Edge Coverage Matrix, classify exactly 13 as automated and three as flagged assumptions, reject missing/duplicate/extra IDs, and prove `16 = automated + flagged` with no silent drop. It must classify `rg` exit 0/1/>1 explicitly and scan active source only. Record the Wave 0 RED state and exact commands in PLANS.md; do not add production types.</action>
  <verify>
    <automated>python3 .planning/phases/53-canonical-still-image-contract-and-private-request-foundatio/check_still_image_foundation_boundaries.py --self-test &amp;&amp; ! swift test --package-path BeautySDK --filter BeautyCanonicalStillImageTests</automated>
  </verify>
  <acceptance_criteria>
    - `python3 .../check_still_image_foundation_boundaries.py --self-test` exits 0 and reports every clean and mutation case.
    - `swift test --package-path BeautySDK --filter BeautyCanonicalStillImageTests` exits nonzero because the planned production contract is absent, not because of malformed test syntax unrelated to the contract.
    - The test source names all eight `CGImagePropertyOrientation` values, exact/one-over ceiling cases, unknown/non-RGB/extended-range inputs, partial and zero alpha, and detector invocation count zero on rejection.
    - PLANS.md contains the Wave 0 RED command/result and D-05/D-06/D-07/D-08/D-16 trace.
  </acceptance_criteria>
  <done>D-19's deterministic canonical-input test seam exists without tracked media; the checker self-test proves both unclassified mutations and the exact 16 = 13 automated + 3 flagged edge inventory; the canonical suite is RED only for absent Phase 53 production behavior; PLANS.md records the result.</done>
</task>

<task id="53-01-02" type="auto" tdd="true">
  <name>Task 2: Specify public-facade admission, request ordering, and realtime isolation</name>
  <read_first>
    BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift
    BeautySDK/Tests/BeautyCoreTests/BeautyEngineMetadataCompatibilityTests.swift
    BeautySDK/Sources/BeautySDK/BeautyEngine.swift
    BeautySDK/Sources/BeautySDK/BeautyEngineGeometryDetection.swift
    BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift
    BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
    .planning/phases/53-canonical-still-image-contract-and-private-request-foundatio/53-CONTEXT.md
    PLANS.md
  </read_first>
  <files>BeautySDK/Tests/BeautyCoreTests/BeautyEngineLocalRetouchFoundationTests.swift, PLANS.md</files>
  <behavior>
    - Both existing public CIImage overloads reach a package/testing-only injected admitted-local route; production admission is the exact empty admitted inventory and no parallel public facade appears.
    - One injected request records canonicalize, detect/select, map/context, render exactly once and in order; zero demand records zero local invocations and multiple private demands still record one shared request.
    - No-face/missing support keeps unrelated shipped face-agnostic color work; invalid canonical input throws before Vision.
    - Pixel-buffer/realtime calls and reset record zero canonicalizer/local-context/provider invocations and preserve shipped output/summary behavior.
  </behavior>
  <action>Create `BeautyEngineLocalRetouchFoundationTests` through `@_spi(Testing) import BeautySDK`, using D-19's opaque admission/counter fixtures rather than candidate names or raw pixels. Encode D-01 through D-04, D-09 through D-12, PATH-01, PATH-04, PATH-05, exact 0/1 request counts, multi-demand adjacency, no-face/null support, ordering, valid-invalid-valid recovery, and independent-engine value isolation. Name and preserve the three nonclaims exactly as PATH01-CONCURRENCY, PATH04-CONCURRENCY, and PATH05-CONCURRENCY; do not convert them into passed concurrency claims. Update PLANS.md with this RED suite and the three flagged assumptions.</action>
  <verify>
    <automated>! swift test --package-path BeautySDK --filter BeautyEngineLocalRetouchFoundationTests &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - The suite compiles far enough to fail only on missing Phase 53 production/test seams and contains assertions for both existing CIImage facade entries.
    - Static test assertions prove the pixel-buffer overload has no canonicalizer/request-context entry and reset has no local-support state to clear.
    - The expected trace is exactly `canonicalize,detectAndMap,makeRequestContext,render`; duplicate or reordered events fail.
    - PLANS.md records D-01/D-02/D-03/D-04/D-09/D-10/D-11/D-12 and EDGE-PATH-01/04/05 as explicit RED/flagged inputs.
  </acceptance_criteria>
  <done>D-19's opaque facade seam drives both existing CIImage entries; exactly-once/order/recovery/pixel-buffer RED assertions fail only for the missing foundation; all three concurrency IDs remain explicit flagged assumptions; PLANS.md records the result.</done>
</task>

<task id="53-01-03" type="auto" tdd="true">
  <name>Task 3: Specify observed lip support and exact legacy admission compatibility</name>
  <read_first>
    BeautySDK/Tests/BeautyDetectionTests/VisionFaceDetectorTests.swift
    BeautySDK/Tests/BeautyDetectionTests/FaceObservationMappingTests.swift
    BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift
    BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift
    BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift
    BeautySDK/Tests/BeautyResourcesTests/BeautyResourceCatalogTests.swift
    BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift
    BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift
    PLANS.md
  </read_first>
  <files>BeautySDK/Tests/BeautyDetectionTests/StillImageRequestSupportTests.swift, BeautySDK/Tests/BeautyCoreTests/BeautyParametersTests.swift, BeautySDK/Tests/BeautyResourcesTests/BeautyResourceCatalogTests.swift, BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift, PLANS.md</files>
  <behavior>
    - Actual outer/inner Vision lip paths are independently bounded and mapped in the existing request; invalid inner support preserves valid outer support and no synthetic face-box lip geometry authorizes local retouch.
    - Descriptions, debug descriptions, dumps, and mirrors expose only approved counts/availability; request values remain independent across valid-invalid-valid and task-group value tests.
    - Phase 53 remains exactly 59 stored/encoded fields, 58 numeric plus filterId, five bundled presets, legacy 52-key and older payload neutrality, no candidate field/key/provider/renderer case, and byte-neutral no-local facade output.
    - The reusable admission checklist states that any later admitted field is independent, positive-only Float, finite-normalized 0...1, default/missing zero, trailing appended, and exact-inventory tested.
  </behavior>
  <action>Create `StillImageRequestSupportTests` for D-09 through D-12 using D-19's synthetic face/eyebrow request-local analogs, including empty/single/two lip regions, exact point-count edges, stable provider order/ties, exactly-once map accounting, malformed sibling isolation, aggregate reflection allowlists, and independent parallel detector values without a same-engine concurrency claim. Extend the three existing compatibility suites for D-13 through D-15 and PATH-06/PATH-07: keep counts at 59/five, keep all legacy/no-local bytes neutral, add a source/inventory assertion that no Phase 53 candidate/provider/renderer token exists, and encode the later-admission checklist without adding a field. Use the exact test/check names from the Edge Coverage Matrix so every automated edge row has one owner. Update PLANS.md with RED/green baseline results.</action>
  <verify>
    <automated>! swift test --package-path BeautySDK --filter StillImageRequestSupportTests &amp;&amp; swift test --package-path BeautySDK --filter BeautyParametersTests &amp;&amp; swift test --package-path BeautySDK --filter BeautyResourceCatalogTests &amp;&amp; swift test --package-path BeautySDK --filter BeautyRendererOutputRegressionTests &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - The new support suite is RED only on absent observed-lip/request-local behavior; the three legacy compatibility suites are green at exact 59/five/no-candidate/no-local values.
    - Tests cover PATH-04 boundary/adjacency/empty/ordering/precision plus PATH-06 and PATH-07 adjacency/empty/ordering without silently dismissing any probe row.
    - No candidate field name, provider, renderer case, preset key, or production admission route is introduced.
    - PLANS.md records D-13/D-14/D-15 and the exact compatibility command results.
  </acceptance_criteria>
  <done>D-19's synthetic support fixtures cover the exact named PATH04/PATH06/PATH07 automated edge rows; the new support suite is RED only on absent observed-lip behavior; exact 59-field/five-preset/no-candidate/no-local compatibility remains green; PLANS.md records the results.</done>
</task>

</tasks>

<edge_coverage>
## Exact Specless Edge Coverage Matrix

The checker manifest and this table are a closed set: **16 total = 13 automated + 3 flagged assumptions**. Missing, duplicate, extra, or reclassified IDs fail `check_still_image_foundation_boundaries.py --self-test`; no row may be silently dropped.

| Stable edge ID | Classification | Exact automated test/check or flagged assumption |
|---|---|---|
| PATH01-CONCURRENCY | FLAGGED | Same-engine concurrency/cancellation remains unclaimed under TD-013; checker requires this exact flagged row. |
| PATH02-UNCLASSIFIED | AUTOMATED | `check_still_image_foundation_boundaries.py --self-test` mutation `PATH02-UNCLASSIFIED` must fail closed. |
| PATH03-UNCLASSIFIED | AUTOMATED | `check_still_image_foundation_boundaries.py --self-test` mutation `PATH03-UNCLASSIFIED` must fail closed. |
| PATH04-BOUNDARY | AUTOMATED | `StillImageRequestSupportTests.testRequestMappingBoundaryCountsAreExact`. |
| PATH04-ADJACENCY | AUTOMATED | `BeautyEngineLocalRetouchFoundationTests.testZeroOneAndMultiplePrivateDemandsShareOneRequest`. |
| PATH04-EMPTY | AUTOMATED | `StillImageRequestSupportTests.testNoFaceAndMissingLipsRemainEmpty`. |
| PATH04-ORDERING | AUTOMATED | `BeautyEngineLocalRetouchFoundationTests.testCanonicalizeDetectMapContextRenderOrder`. |
| PATH04-PRECISION | AUTOMATED | `StillImageRequestSupportTests.testMappingCountPrecisionAndOverflowAreExact`. |
| PATH04-CONCURRENCY | FLAGGED | Independent request values are tested; same-engine concurrent selected-face behavior remains unclaimed under TD-013. |
| PATH05-CONCURRENCY | FLAGGED | Pixel-buffer zero-local-work is tested; same-engine cross-overload concurrency remains unclaimed under TD-013. |
| PATH06-ADJACENCY | AUTOMATED | `BeautyParametersTests.testPhase53LegacyInventoryAdjacencyAt58NumericPlusFilterID`. |
| PATH06-EMPTY | AUTOMATED | `BeautyParametersTests.testPhase53MissingKeysAndZeroDefaultsRemainNeutral`. |
| PATH06-ORDERING | AUTOMATED | `BeautyParametersTests.testPhase53StoredAndCodingKeyOrderRemainsLegacy59`. |
| PATH07-ADJACENCY | AUTOMATED | `BeautyParametersTests.testPhase53AdmissionBoundsAreExactAtZeroAndOne`. |
| PATH07-EMPTY | AUTOMATED | `BeautyParametersTests.testPhase53NoCandidateInventory`. |
| PATH07-ORDERING | AUTOMATED | `BeautyParametersTests.testPhase53FutureAdmissionChecklistRequiresTrailingAppendOrder`. |
</edge_coverage>

<threat_model>
## ASVS Level 1 Completion Gate

`security_standard: OWASP ASVS Level 1`; `block_on: HIGH`. The named automated mitigation commands are `python3 .planning/phases/53-canonical-still-image-contract-and-private-request-foundatio/check_still_image_foundation_boundaries.py --self-test`, `swift test --package-path BeautySDK --filter BeautyCanonicalStillImageTests`, `swift test --package-path BeautySDK --filter BeautyEngineLocalRetouchFoundationTests`, and `swift test --package-path BeautySDK --filter StillImageRequestSupportTests`. In this RED plan, a HIGH item is verified only when its test reaches the intended missing-production assertion and the mutation checker passes; malformed, unrelated, failed-to-run, or unclassified results are unverified. Any failed or unverified HIGH item forbids task completion, plan completion, and `53-01-SUMMARY.md` publication. This plan specifies mitigations but does not claim their GREEN closure.

## Trust Boundaries

| Boundary | Description |
|---|---|
| caller CIImage/metadata -> canonicalizer | Untrusted extent, orientation, profile/range, and alpha cross before allocation/Vision |
| Vision regions -> mapped request support | Biometric-adjacent untrusted coordinates cross into package-only request values |
| still facade -> pixel-buffer/realtime facade | Local-retouch activation must remain structurally one-way |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Verifiable mitigation |
|---|---|---|---|---|---|
| T-53-01 | Denial of Service | HIGH | canonical allocation | mitigate | RED tests require finite positive extent, configured ceiling, and checked width*4*height before allocation |
| T-53-02 | Tampering | HIGH | alpha/background | mitigate | RED tests reject every rendered alpha byte != 255 before detector count changes |
| T-53-03 | Tampering | HIGH | color semantics | mitigate | RED tests reject nil/non-RGB/extended-range and require explicit sRGB RGBA8 output |
| T-53-04 | Information Disclosure | HIGH | support/diagnostics | mitigate | checker and reflection tests reject raw support/bytes/public/SPI/Codable/persistence/network disclosure |
| T-53-05 | Tampering | HIGH | request lifecycle | mitigate | valid-invalid-valid and independent value matrices reject stale or crossed support |
| T-53-06 | Elevation of Privilege | HIGH | pixel-buffer/realtime activation | mitigate | facade counters and checker require zero local calls outside still-image routing |
| T-53-SC | Tampering | LOW | package supply chain | accept | no npm/pip/cargo or Swift package dependency install occurs; Package.swift dependency inventory stays unchanged |
</threat_model>

## Artifacts this phase produces

- New test class/file `BeautyCanonicalStillImageTests` in `BeautyCanonicalStillImageTests.swift`.
- New test class/file `BeautyEngineLocalRetouchFoundationTests` in `BeautyEngineLocalRetouchFoundationTests.swift`.
- New test class/file `StillImageRequestSupportTests` in `StillImageRequestSupportTests.swift`.
- New checker file `check_still_image_foundation_boundaries.py` with `--self-test` and live modes.
- No production symbol, public candidate field, provider, renderer case, or external-service coverage matrix is created by this plan.

<verification>
Wave 0 is complete when checker self-tests pass, all three new suites fail for the intended missing contracts, exact 59-field/five-preset compatibility remains green, and `git diff --check` passes.
</verification>

<success_criteria>
Every Validation Strategy Wave 0 gap exists as executable code; all 16 specless edge items equal the authored covered predicates plus three explicit concurrency flags; all three kept prohibitions are descriptor-less flagged-unverified; no production behavior changes.
</success_criteria>

<output>
Create `.planning/phases/53-canonical-still-image-contract-and-private-request-foundatio/53-01-SUMMARY.md` when done.
</output>
