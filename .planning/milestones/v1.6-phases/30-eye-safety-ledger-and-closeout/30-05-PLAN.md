---
phase: 30-eye-safety-ledger-and-closeout
plan: "05"
type: execute
wave: 5
depends_on:
  - 30-04
files_modified:
  - DESIGN.md
  - SECURITY.md
  - RELIABILITY.md
  - PRODUCT_SENSE.md
  - .planning/phases/30-eye-safety-ledger-and-closeout/30-SECURITY.md
autonomous: true
requirements:
  - EYE-04
  - EYE-05
  - EYE-06
  - EYE-07
  - EYE-08
  - DOC-01
must_haves:
  truths:
    - "DESIGN.md independently owns exact eye ranges, caps, abnormal-input behavior, both-eyes requirement, and missing/reused/stale zero-and-skip semantics per D-01 through D-15."
    - "RELIABILITY.md independently records the eye-only reused/stale skip exception, preserves 0.5 reused reduction for face shape/nose/mouth, and names all three stable category reason codes per D-07 through D-12."
    - "PRODUCT_SENSE.md independently defines acceptance for exactly four implemented subtools while branch, device, commercial, parity, visual quality, and readiness remain unclaimed per D-19 through D-21."
    - "Root and Phase 30 security records independently cite redaction, public/SPI, import, network, commercial/VIP, generated-artifact, and atomic-promotion evidence with zero open threats per D-11, D-17, and D-18."
  artifacts:
    - path: "DESIGN.md"
      provides: "Authoritative eye input, cap, freshness, and degradation contract"
    - path: "RELIABILITY.md"
      provides: "Authoritative eye-specific reuse/stale exception and reason codes"
    - path: "PRODUCT_SENSE.md"
      provides: "Four-row acceptance and explicit non-claims"
    - path: "SECURITY.md"
      provides: "Authoritative Phase 30 privacy and active-source boundary record"
    - path: ".planning/phases/30-eye-safety-ledger-and-closeout/30-SECURITY.md"
      provides: "Verified threat register extended with promotion/root-contract audit"
      contains: "threats_open: 0"
  key_links:
    - from: "SECURITY.md"
      to: ".planning/phases/30-eye-safety-ledger-and-closeout/30-SECURITY.md"
      via: "root invariant links the detailed verified threat register"
      pattern: "30-SECURITY"
    - from: "DESIGN.md"
      to: ".planning/phases/30-eye-safety-ledger-and-closeout/30-EYE-SAFETY-EVIDENCE.md"
      via: "design contract cites observed cap/degradation evidence"
      pattern: "30-EYE-SAFETY-EVIDENCE"
---

<objective>
Synchronize the four owning root contracts and append the atomic-promotion audit to Phase 30 security after the blueprint gate passes.

Purpose: separate contract ownership from the atomic blueprint patch and from global quality/GSD/work-ledger closeout.
Output: five independently verified security/design/reliability/product artifacts.
</objective>

## Artifacts this phase produces

- Updated `DESIGN.md` with exact public semantics, caps, both-eyes requirement, and eye-only skip behavior.
- Updated `RELIABILITY.md` with the eye-only reused/stale exception and exact reason codes.
- Updated `PRODUCT_SENSE.md` with four-row acceptance and conservative non-claims.
- Updated root `SECURITY.md` with input/redaction/boundary/artifact evidence.
- Updated `30-SECURITY.md` retaining `status: verified` and `threats_open: 0` after atomic/contract audit.
- No quality, project, requirement, roadmap, state, work-ledger, validation, architecture, frontend, example README, gallery code, source, test, or generated artifact change.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/phases/30-eye-safety-ledger-and-closeout/30-CONTEXT.md
@.planning/phases/30-eye-safety-ledger-and-closeout/30-EYE-SAFETY-EVIDENCE.md
@.planning/phases/30-eye-safety-ledger-and-closeout/30-REVIEW.md
@.planning/phases/30-eye-safety-ledger-and-closeout/30-SECURITY.md
@.planning/phases/30-eye-safety-ledger-and-closeout/30-VERIFICATION.md
@.planning/phases/30-eye-safety-ledger-and-closeout/30-VALIDATION.md
@.planning/phases/30-eye-safety-ledger-and-closeout/30-04-SUMMARY.md
@docs/meitu-function-blueprint/SHAPE_FEATURE_LEDGER.md
@docs/meitu-function-blueprint/FEATURE_MATRIX.md
@docs/meitu-function-blueprint/features/beauty-shaping/eyes/README.md
@DESIGN.md
@SECURITY.md
@RELIABILITY.md
@PRODUCT_SENSE.md
@ARCHITECTURE.md
@FRONTEND.md
</context>

<tasks>

<task type="auto">
  <name>Task 30-05-01: Update design, reliability, and product acceptance contracts</name>
  <files>DESIGN.md, RELIABILITY.md, PRODUCT_SENSE.md</files>
  <read_first>
    - `DESIGN.md`
    - `RELIABILITY.md`
    - `PRODUCT_SENSE.md`
    - `.planning/phases/30-eye-safety-ledger-and-closeout/30-EYE-SAFETY-EVIDENCE.md`
    - `.planning/phases/30-eye-safety-ledger-and-closeout/30-VERIFICATION.md`
    - `docs/meitu-function-blueprint/SHAPE_FEATURE_LEDGER.md`
    - `docs/meitu-function-blueprint/FEATURE_MATRIX.md`
  </read_first>
  <action>After confirming Plan 30-04 promoted exactly four rows and kept the branch partial, update only each owning contract. In `DESIGN.md`, add or replace one exact `### Phase 30 Eye Safety Contract` section that records `eyeSize`/`eyeTailLift` `[0,1]`, `eyeDistance`/`eyeYPosition` `[-1,1]`, caps `0.45/0.30/0.25/0.30`, negative and non-finite semantics, both-eyes-required behavior, missing/reused/stale all-four-zero/domain-skip evidence, and a `30-EYE-SAFETY-EVIDENCE.md` link. Replace the exact stale authoritative row `| Eyes | `eyeSize`, `eyeDistance`, `eyeYPosition`, `eyeTailLift` | mixed |`; do not leave it beside the new contract. In `RELIABILITY.md`, add or replace one exact `### Phase 30 Eye Freshness Contract` section with the eye-only reused/stale skip exception while face shape/nose/mouth retain `0.5` reuse reduction, all three stable category-only reasons, aggregate metrics, and the Phase 30 evidence link. Replace the exact stale sentence `Missing landmark groups skip only dependent domains, reused landmarks reduce geometry briefly, and stale landmarks disable strong geometry with warning/metric evidence.` rather than appending a contradiction. In `PRODUCT_SENSE.md`, add or replace one exact `### 7.14 Phase 30 Eye Slice Acceptance` section with verifiable acceptance for exactly `大小`, `上下`, `眼距`, and `眼尾上扬`, branch partial, future tools, explicit device/commercial/parity/visual-quality/readiness/whole-branch non-claims, and the Phase 30 evidence link. State invariants and evidence links only; do not duplicate evidence tables per D-21. Verify only these exact heading-to-next-heading sections, not the whole historical documents.</action>
  <verify>
    <automated>awk '$0 == "### Phase 30 Eye Safety Contract" { if (seen) exit 2; seen=1; copy=1 } copy && $0 != "### Phase 30 Eye Safety Contract" && /^#{1,3} / { copy=0 } copy { print } END { if (seen != 1) exit 3 }' DESIGN.md > /tmp/phase30-design-section.out || exit $?; for token in 'eyeSize.*\[0, ?1\]' 'eyeTailLift.*\[0, ?1\]' 'eyeDistance.*\[-1, ?1\]' 'eyeYPosition.*\[-1, ?1\]' '0\.45' '0\.30' '0\.25' 'NaN' 'infinity' 'missing' 'reused' 'stale'; do rg -n "$token" /tmp/phase30-design-section.out || exit 1; done</automated>
    <automated>awk '$0 == "### Phase 30 Eye Safety Contract" { if (seen) exit 2; seen=1; copy=1 } copy && $0 != "### Phase 30 Eye Safety Contract" && /^#{1,3} / { copy=0 } copy { print } END { if (seen != 1) exit 3 }' DESIGN.md > /tmp/phase30-design-evidence.out || exit $?; rg -n '30-EYE-SAFETY-EVIDENCE\.md' /tmp/phase30-design-evidence.out</automated>
    <automated>if rg -n -F '| Eyes | `eyeSize`, `eyeDistance`, `eyeYPosition`, `eyeTailLift` | mixed |' DESIGN.md > /tmp/phase30-stale-design.out; then sed -n '1,40p' /tmp/phase30-stale-design.out; exit 1; else scan_status=$?; case "$scan_status" in 1) : ;; *) exit "$scan_status" ;; esac; fi</automated>
    <automated>awk '$0 == "### Phase 30 Eye Freshness Contract" { if (seen) exit 2; seen=1; copy=1 } copy && $0 != "### Phase 30 Eye Freshness Contract" && /^#{1,3} / { copy=0 } copy { print } END { if (seen != 1) exit 3 }' RELIABILITY.md > /tmp/phase30-reliability-section.out || exit $?; for token in 'eye_inputs_missing' 'eye_geometry_reused_skipped' 'eye_geometry_stale_skipped' 'face shape' 'nose' 'mouth' '0\.5' 'aggregate'; do rg -n "$token" /tmp/phase30-reliability-section.out || exit 1; done</automated>
    <automated>awk '$0 == "### Phase 30 Eye Freshness Contract" { if (seen) exit 2; seen=1; copy=1 } copy && $0 != "### Phase 30 Eye Freshness Contract" && /^#{1,3} / { copy=0 } copy { print } END { if (seen != 1) exit 3 }' RELIABILITY.md > /tmp/phase30-reliability-evidence.out || exit $?; rg -n '30-EYE-SAFETY-EVIDENCE\.md' /tmp/phase30-reliability-evidence.out</automated>
    <automated>if rg -n -F 'Missing landmark groups skip only dependent domains, reused landmarks reduce geometry briefly, and stale landmarks disable strong geometry with warning/metric evidence.' RELIABILITY.md > /tmp/phase30-stale-reliability.out; then sed -n '1,40p' /tmp/phase30-stale-reliability.out; exit 1; else scan_status=$?; case "$scan_status" in 1) : ;; *) exit "$scan_status" ;; esac; fi</automated>
    <automated>awk '$0 == "### 7.14 Phase 30 Eye Slice Acceptance" { if (seen) exit 2; seen=1; copy=1 } copy && $0 != "### 7.14 Phase 30 Eye Slice Acceptance" && /^#{1,3} / { copy=0 } copy { print } END { if (seen != 1) exit 3 }' PRODUCT_SENSE.md > /tmp/phase30-product-section.out || exit $?; for token in '大小' '上下' '眼距' '眼尾上扬' '眼睛.*partial' 'future' 'device' 'commercial' 'parity' 'readiness'; do rg -ni "$token" /tmp/phase30-product-section.out || exit 1; done</automated>
    <automated>awk '$0 == "### 7.14 Phase 30 Eye Slice Acceptance" { if (seen) exit 2; seen=1; copy=1 } copy && $0 != "### 7.14 Phase 30 Eye Slice Acceptance" && /^#{1,3} / { copy=0 } copy { print } END { if (seen != 1) exit 3 }' PRODUCT_SENSE.md > /tmp/phase30-product-evidence.out || exit $?; rg -n '30-EYE-SAFETY-EVIDENCE\.md' /tmp/phase30-product-evidence.out</automated>
    <automated>git diff --name-only -- DESIGN.md | rg -x 'DESIGN.md'</automated>
    <automated>git diff --name-only -- RELIABILITY.md | rg -x 'RELIABILITY.md'</automated>
    <automated>git diff --name-only -- PRODUCT_SENSE.md | rg -x 'PRODUCT_SENSE.md'</automated>
    <automated>awk '$0 == "### Phase 30 Eye Safety Contract" { if (seen) exit 2; seen=1; copy=1 } copy && $0 != "### Phase 30 Eye Safety Contract" && /^#{1,3} / { copy=0 } copy { print } END { if (seen != 1) exit 3 }' DESIGN.md > /tmp/phase30-design-no-overclaim.out || exit $?; if rg -n 'all reused geometry.*skip|commercial quality achieved|production quality|release[- ]ready|shipping ready|device parity passed|matches Meitu|full Meitu parity achieved|whole[- ]branch.*眼睛.*implemented|眼睛.*branch.*implemented' /tmp/phase30-design-no-overclaim.out > /tmp/phase30-design-overclaims.out; then sed -n '1,80p' /tmp/phase30-design-overclaims.out; exit 1; else scan_status=$?; case "$scan_status" in 1) : ;; *) exit "$scan_status" ;; esac; fi</automated>
    <automated>awk '$0 == "### Phase 30 Eye Freshness Contract" { if (seen) exit 2; seen=1; copy=1 } copy && $0 != "### Phase 30 Eye Freshness Contract" && /^#{1,3} / { copy=0 } copy { print } END { if (seen != 1) exit 3 }' RELIABILITY.md > /tmp/phase30-reliability-no-overclaim.out || exit $?; if rg -n 'all reused geometry.*skip|commercial quality achieved|production quality|release[- ]ready|shipping ready|device parity passed|matches Meitu|full Meitu parity achieved|whole[- ]branch.*眼睛.*implemented|眼睛.*branch.*implemented' /tmp/phase30-reliability-no-overclaim.out > /tmp/phase30-reliability-overclaims.out; then sed -n '1,80p' /tmp/phase30-reliability-overclaims.out; exit 1; else scan_status=$?; case "$scan_status" in 1) : ;; *) exit "$scan_status" ;; esac; fi</automated>
    <automated>awk '$0 == "### 7.14 Phase 30 Eye Slice Acceptance" { if (seen) exit 2; seen=1; copy=1 } copy && $0 != "### 7.14 Phase 30 Eye Slice Acceptance" && /^#{1,3} / { copy=0 } copy { print } END { if (seen != 1) exit 3 }' PRODUCT_SENSE.md > /tmp/phase30-product-no-overclaim.out || exit $?; if rg -n 'commercial quality achieved|production quality|release[- ]ready|shipping ready|device parity passed|matches Meitu|full Meitu parity achieved|whole[- ]branch.*眼睛.*implemented|眼睛.*branch.*implemented' /tmp/phase30-product-no-overclaim.out > /tmp/phase30-product-overclaims.out; then sed -n '1,80p' /tmp/phase30-product-overclaims.out; exit 1; else scan_status=$?; case "$scan_status" in 1) : ;; *) exit "$scan_status" ;; esac; fi</automated>
    <automated>git diff --check -- DESIGN.md RELIABILITY.md PRODUCT_SENSE.md</automated>
  </verify>
  <acceptance_criteria>
    - `DESIGN.md` independently owns every exact range, cap, abnormal-input, both-eyes, and skip invariant with an evidence link.
    - `RELIABILITY.md` independently owns the eye-only exception, preserves non-eye 0.5 reuse, and lists all three category codes.
    - `PRODUCT_SENSE.md` independently owns four-row acceptance, partial/future wording, and all required non-claims.
    - Each required file is changed and passes its own no-overclaim plus diff-hygiene gate.
  </acceptance_criteria>
  <done>Design, reliability, and product contracts independently describe the same scoped eye outcome.</done>
</task>

<task type="auto">
  <name>Task 30-05-02: Update root security and append the verified promotion audit</name>
  <files>SECURITY.md, .planning/phases/30-eye-safety-ledger-and-closeout/30-SECURITY.md</files>
  <read_first>
    - `SECURITY.md`
    - `.planning/phases/30-eye-safety-ledger-and-closeout/30-SECURITY.md`
    - `.planning/phases/30-eye-safety-ledger-and-closeout/30-EYE-SAFETY-EVIDENCE.md`
    - `.planning/phases/30-eye-safety-ledger-and-closeout/30-REVIEW.md`
    - `docs/meitu-function-blueprint/SHAPE_FEATURE_LEDGER.md`
    - `docs/meitu-function-blueprint/FEATURE_MATRIX.md`
  </read_first>
  <action>Add or replace one exact `### Phase 30 Eye Safety Boundary Evidence` section in root `SECURITY.md` with Phase 30 positive-only/signed/non-finite validation, exact category-only warning messages/codes, aggregate metrics, explicit active roots, multiline public/SPI raw-geometry scan including BeautyCore and BeautyRender, forbidden imports, network/cloud, StoreKit/entitlement gates, exact two-occurrence `vipChip` allowlist with zero unclassified matches, 31-field inventory, ignored/generated-artifact guard, and explicit links to `30-EYE-SAFETY-EVIDENCE.md` and detailed phase security. Add or replace one exact `## Promotion and Root-Contract Audit` section in `30-SECURITY.md` with the successful atomic four-row guard, branch partial result, per-file blueprint no-overclaim results, root-contract synchronization results, and an explicit Phase 30 evidence link. Retain exact `status: verified` and `threats_open: 0`; do not finalize overall phase verification or GSD ledgers here per D-17, D-18, and D-21. Verify each new section only through the next heading of equal or higher level.</action>
  <verify>
    <automated>awk '$0 == "### Phase 30 Eye Safety Boundary Evidence" { if (seen) exit 2; seen=1; copy=1 } copy && $0 != "### Phase 30 Eye Safety Boundary Evidence" && /^#{1,3} / { copy=0 } copy { print } END { if (seen != 1) exit 3 }' SECURITY.md > /tmp/phase30-root-security-section.out || exit $?; for token in 'EYE-07' 'BeautyCore' 'BeautyRender' 'FaceGeometry' 'WarpControlPoint' 'CGPoint' 'CGRect' 'internal import' 'network' 'StoreKit' 'VIP-COMMERCIAL-ALLOW-01' 'VIP-COMMERCIAL-ALLOW-02' 'unclassified_matches: 0' '31' 'generated'; do rg -ni "$token" /tmp/phase30-root-security-section.out || exit 1; done</automated>
    <automated>awk '$0 == "### Phase 30 Eye Safety Boundary Evidence" { if (seen) exit 2; seen=1; copy=1 } copy && $0 != "### Phase 30 Eye Safety Boundary Evidence" && /^#{1,3} / { copy=0 } copy { print } END { if (seen != 1) exit 3 }' SECURITY.md > /tmp/phase30-root-security-evidence.out || exit $?; rg -n '30-EYE-SAFETY-EVIDENCE\.md' /tmp/phase30-root-security-evidence.out; rg -n '30-SECURITY\.md' /tmp/phase30-root-security-evidence.out</automated>
    <automated>for token in '^status: verified$' '^threats_open: 0$'; do rg -n "$token" .planning/phases/30-eye-safety-ledger-and-closeout/30-SECURITY.md || exit 1; done; awk '$0 == "## Promotion and Root-Contract Audit" { if (seen) exit 2; seen=1; copy=1 } copy && $0 != "## Promotion and Root-Contract Audit" && /^#{1,2} / { copy=0 } copy { print } END { if (seen != 1) exit 3 }' .planning/phases/30-eye-safety-ledger-and-closeout/30-SECURITY.md > /tmp/phase30-security-audit-section.out || exit $?; for token in 'atomic' '大小' '上下' '眼距' '眼尾上扬' 'branch.*partial' 'root contract' 'no-overclaim' 'T-30-01' 'T-30-08' 'T-30-SC'; do rg -ni "$token" /tmp/phase30-security-audit-section.out || exit 1; done</automated>
    <automated>awk '$0 == "## Promotion and Root-Contract Audit" { if (seen) exit 2; seen=1; copy=1 } copy && $0 != "## Promotion and Root-Contract Audit" && /^#{1,2} / { copy=0 } copy { print } END { if (seen != 1) exit 3 }' .planning/phases/30-eye-safety-ledger-and-closeout/30-SECURITY.md > /tmp/phase30-security-audit-evidence.out || exit $?; rg -n '30-EYE-SAFETY-EVIDENCE\.md' /tmp/phase30-security-audit-evidence.out</automated>
    <automated>git diff --name-only -- SECURITY.md | rg -x 'SECURITY.md'</automated>
    <automated>git diff --name-only -- .planning/phases/30-eye-safety-ledger-and-closeout/30-SECURITY.md | rg -x '.planning/phases/30-eye-safety-ledger-and-closeout/30-SECURITY.md'</automated>
    <automated>awk '$0 == "### Phase 30 Eye Safety Boundary Evidence" { if (seen) exit 2; seen=1; copy=1 } copy && $0 != "### Phase 30 Eye Safety Boundary Evidence" && /^#{1,3} / { copy=0 } copy { print } END { if (seen != 1) exit 3 }' SECURITY.md > /tmp/phase30-root-security-privacy.out || exit $?; if rg -n '/Users/|/private/var|NSError|AVError|rawPresetJson:|raw JSON:|image bytes:|landmarks=\[|landmarkCoordinates:|rawLandmark:|VNFaceObservation\(|boundingBox:|controlPoint\(|SIMD[0-9]?\(' /tmp/phase30-root-security-privacy.out > /tmp/phase30-root-security-privacy-failures.out; then sed -n '1,80p' /tmp/phase30-root-security-privacy-failures.out; exit 1; else scan_status=$?; case "$scan_status" in 1) : ;; *) exit "$scan_status" ;; esac; fi</automated>
    <automated>awk '$0 == "## Promotion and Root-Contract Audit" { if (seen) exit 2; seen=1; copy=1 } copy && $0 != "## Promotion and Root-Contract Audit" && /^#{1,2} / { copy=0 } copy { print } END { if (seen != 1) exit 3 }' .planning/phases/30-eye-safety-ledger-and-closeout/30-SECURITY.md > /tmp/phase30-security-audit-privacy.out || exit $?; if rg -n '/Users/|/private/var|NSError|AVError|rawPresetJson:|raw JSON:|image bytes:|landmarks=\[|landmarkCoordinates:|rawLandmark:|VNFaceObservation\(|boundingBox:|controlPoint\(|SIMD[0-9]?\(' /tmp/phase30-security-audit-privacy.out > /tmp/phase30-security-audit-privacy-failures.out; then sed -n '1,80p' /tmp/phase30-security-audit-privacy-failures.out; exit 1; else scan_status=$?; case "$scan_status" in 1) : ;; *) exit "$scan_status" ;; esac; fi</automated>
    <automated>awk '$0 == "### Phase 30 Eye Safety Boundary Evidence" { if (seen) exit 2; seen=1; copy=1 } copy && $0 != "### Phase 30 Eye Safety Boundary Evidence" && /^#{1,3} / { copy=0 } copy { print } END { if (seen != 1) exit 3 }' SECURITY.md > /tmp/phase30-root-security-no-overclaim.out || exit $?; if rg -n 'commercial quality achieved|production quality|release[- ]ready|shipping ready|device parity passed|matches Meitu|full Meitu parity achieved|whole[- ]branch.*眼睛.*implemented|眼睛.*branch.*implemented' /tmp/phase30-root-security-no-overclaim.out > /tmp/phase30-root-security-overclaims.out; then sed -n '1,80p' /tmp/phase30-root-security-overclaims.out; exit 1; else scan_status=$?; case "$scan_status" in 1) : ;; *) exit "$scan_status" ;; esac; fi</automated>
    <automated>awk '$0 == "## Promotion and Root-Contract Audit" { if (seen) exit 2; seen=1; copy=1 } copy && $0 != "## Promotion and Root-Contract Audit" && /^#{1,2} / { copy=0 } copy { print } END { if (seen != 1) exit 3 }' .planning/phases/30-eye-safety-ledger-and-closeout/30-SECURITY.md > /tmp/phase30-security-audit-no-overclaim.out || exit $?; if rg -n 'commercial quality achieved|production quality|release[- ]ready|shipping ready|device parity passed|matches Meitu|full Meitu parity achieved|whole[- ]branch.*眼睛.*implemented|眼睛.*branch.*implemented' /tmp/phase30-security-audit-no-overclaim.out > /tmp/phase30-security-audit-overclaims.out; then sed -n '1,80p' /tmp/phase30-security-audit-overclaims.out; exit 1; else scan_status=$?; case "$scan_status" in 1) : ;; *) exit "$scan_status" ;; esac; fi</automated>
    <automated>if git diff -- ARCHITECTURE.md FRONTEND.md | rg -n '^[-+]'; then exit 1; fi</automated>
    <automated>git diff --check -- SECURITY.md .planning/phases/30-eye-safety-ledger-and-closeout/30-SECURITY.md</automated>
  </verify>
  <acceptance_criteria>
    - Root security independently records every EYE-07 root/category/classification and links detailed evidence.
    - Phase security independently records atomic promotion, branch partial, blueprint/root contract, and no-overclaim outcomes while remaining verified with zero open threats.
    - Both files are changed, contain no raw payload/local path, and pass scoped diff hygiene.
  </acceptance_criteria>
  <done>Security contracts close the promotion/root-contract threat audit without prematurely finalizing global ledgers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Blueprint outcome -> root contracts | Each owning contract receives only its invariant and evidence link, not copied raw evidence. |
| Active-source classifications -> security docs | Exact roots, candidate counts, allowlist IDs, and zero-unclassified results must remain auditable. |
| Scoped row completion -> product language | Four implemented rows must remain distinct from whole-branch/product readiness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Task-level mitigation and verification gate |
|-----------|----------|-----------|-------------|---------------------------------------------|
| T-30-01 | Tampering / Denial of Service | Input contract | mitigate | Task 30-05-01 independently verifies exact ranges/caps/non-finite behavior in DESIGN. |
| T-30-02 | Tampering | Freshness and missing eyes | mitigate | Task 30-05-01 records zero/skip plus non-eye 0.5 reuse preservation. |
| T-30-03 | Information Disclosure | Warning/metric/docs | mitigate | Both tasks allow category codes, aggregate facts, and links only; raw-payload scans are per file. |
| T-30-04 | Information Disclosure / Elevation of Privilege | Raw geometry/imports | mitigate | Task 30-05-02 independently records all roots and scan forms including BeautyCore/BeautyRender. |
| T-30-05 | Elevation of Privilege | Network/commercial | mitigate | Task 30-05-02 records API gates and exact VIP allowlist IDs with zero unclassified. |
| T-30-06 | Information Disclosure / Repudiation | Generated artifacts | mitigate | Root security records ignored/untracked artifacts only; no files are generated here. |
| T-30-07 | Spoofing / Repudiation | Promotion audit | mitigate | Phase security appends exact four-row and branch-partial outcomes after Plan 30-04. |
| T-30-08 | Spoofing / Repudiation | Overclaim | mitigate | Task 30-05-01 runs independent no-overclaim gates on each root contract. |
| T-30-SC | Tampering | Package-manager installs | mitigate | Markdown-only contract synchronization uses no install or external service. |
</threat_model>

<verification>
Overall Plan 30-05 checks:

- Each root contract is independently checked for its owning invariant and evidence link.
- Root and phase security independently record complete boundary/classification and atomic-promotion facts.
- All five required files are changed, pass per-file privacy/no-overclaim gates, and pass diff hygiene.
- `ARCHITECTURE.md` and `FRONTEND.md` remain unchanged.
</verification>

<success_criteria>
Plan 30-05 succeeds when design, reliability, product, and security contracts independently agree on the scoped evidence-backed outcome and the verified threat register records atomic promotion with zero open threats.
</success_criteria>

<output>
Create `.planning/phases/30-eye-safety-ledger-and-closeout/30-05-SUMMARY.md` when done.
</output>
