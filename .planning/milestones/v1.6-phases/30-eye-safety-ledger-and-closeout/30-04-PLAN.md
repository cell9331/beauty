---
phase: 30-eye-safety-ledger-and-closeout
plan: "04"
type: execute
wave: 4
depends_on:
  - 30-03
files_modified:
  - docs/meitu-function-blueprint/SHAPE_FEATURE_LEDGER.md
  - docs/meitu-function-blueprint/FEATURE_MATRIX.md
  - docs/meitu-function-blueprint/features/beauty-shaping/eyes/README.md
  - docs/meitu-function-blueprint/features/beauty-shaping/README.md
  - docs/meitu-function-blueprint/EXAMPLE_IMAGE_VALIDATION.md
autonomous: true
requirements:
  - EYE-08
  - DOC-01
must_haves:
  truths:
    - "Only after the passed Phase 30 evidence, clean review, verified security, promotion-ready verification, complete Wave 3 validation, Phase 29 regression, and classified EYE-07 gates pass do `大小`, `上下`, `眼距`, and `眼尾上扬` move from `partial` to `implemented` together per D-18 and D-19."
    - "No other `眼睛` row becomes implemented, and `Beauty shaping | 眼睛` remains exactly `partial` with future tools explicit per D-20."
    - "Each of the five blueprint files independently states its owning invariant and cites Phase 29 renderer evidence plus Phase 30 safety evidence without whole-branch, device, commercial, parity, quality, or readiness overclaim per D-16, D-20, and D-21."
  artifacts:
    - path: "docs/meitu-function-blueprint/SHAPE_FEATURE_LEDGER.md"
      provides: "Exactly four atomic evidence-backed implemented eye rows"
      contains: "| `眼睛` | 大小 | implemented |"
    - path: "docs/meitu-function-blueprint/FEATURE_MATRIX.md"
      provides: "Branch-level eye status remains partial"
      contains: "| Beauty shaping | 眼睛 | partial |"
    - path: "docs/meitu-function-blueprint/features/beauty-shaping/eyes/README.md"
      provides: "Eye-specific semantics, degradation, privacy, evidence, and future gaps"
    - path: "docs/meitu-function-blueprint/features/beauty-shaping/README.md"
      provides: "Family-level four-subtool-versus-partial-branch wording"
    - path: "docs/meitu-function-blueprint/EXAMPLE_IMAGE_VALIDATION.md"
      provides: "Unchanged 23/7/161/36 renderer contract with Phase 30 closeout links"
  key_links:
    - from: "docs/meitu-function-blueprint/SHAPE_FEATURE_LEDGER.md"
      to: ".planning/phases/30-eye-safety-ledger-and-closeout/30-EYE-SAFETY-EVIDENCE.md"
      via: "every promoted row cites safety/degradation/boundary evidence"
      pattern: "30-EYE-SAFETY-EVIDENCE"
    - from: "docs/meitu-function-blueprint/FEATURE_MATRIX.md"
      to: "docs/meitu-function-blueprint/SHAPE_FEATURE_LEDGER.md"
      via: "branch partial status points to exactly four implemented second-level rows"
      pattern: "眼睛.*partial"
---

<objective>
Promote exactly four eye rows atomically, then synchronize only the five blueprint owners that describe those rows and their unchanged renderer evidence.

Purpose: satisfy EYE-08 and the blueprint portion of DOC-01 without mixing the atomic edit with root-contract or global-ledger hotspots.
Output: five independently verified blueprint documents with four implemented subtools and branch-level `眼睛` still partial.
</objective>

## Artifacts this phase produces

- Exactly four implemented `眼睛` rows: `大小`, `上下`, `眼距`, and `眼尾上扬`.
- Exact branch row `Beauty shaping | 眼睛 | partial` retained.
- Eyes README and beauty-shaping README that independently distinguish four implemented subtools from the partial branch and future tools.
- `EXAMPLE_IMAGE_VALIDATION.md` retaining exactly 23 cases, 7 fixtures, 161 outputs, and 36/36 comparisons while linking Phase 30 closeout evidence.
- No root contract, quality ledger, GSD ledger, Swift source, test, renderer, helper, gallery code, generated PNG, package, target, or Demo file is modified.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/phases/30-eye-safety-ledger-and-closeout/30-CONTEXT.md
@.planning/phases/30-eye-safety-ledger-and-closeout/30-EYE-SAFETY-EVIDENCE.md
@.planning/phases/30-eye-safety-ledger-and-closeout/30-REVIEW.md
@.planning/phases/30-eye-safety-ledger-and-closeout/30-SECURITY.md
@.planning/phases/30-eye-safety-ledger-and-closeout/30-VERIFICATION.md
@.planning/phases/30-eye-safety-ledger-and-closeout/30-VALIDATION.md
@.planning/phases/30-eye-safety-ledger-and-closeout/30-03-SUMMARY.md
@.planning/phases/29-eye-renderer-output-evidence/29-EYE-RENDERER-EVIDENCE.md
@.planning/phases/29-eye-renderer-output-evidence/29-VERIFICATION.md
@docs/meitu-function-blueprint/SHAPE_FEATURE_LEDGER.md
@docs/meitu-function-blueprint/FEATURE_MATRIX.md
@docs/meitu-function-blueprint/features/beauty-shaping/eyes/README.md
@docs/meitu-function-blueprint/features/beauty-shaping/README.md
@docs/meitu-function-blueprint/EXAMPLE_IMAGE_VALIDATION.md
@docs/meitu-function-blueprint/shared/IMPLEMENTATION_PRINCIPLES.md
</context>

<tasks>

<task type="auto">
  <name>Task 30-04-01: Gate and atomically promote only the four scoped rows</name>
  <files>docs/meitu-function-blueprint/SHAPE_FEATURE_LEDGER.md, docs/meitu-function-blueprint/FEATURE_MATRIX.md</files>
  <read_first>
    - `.planning/phases/30-eye-safety-ledger-and-closeout/30-EYE-SAFETY-EVIDENCE.md`
    - `.planning/phases/30-eye-safety-ledger-and-closeout/30-REVIEW.md`
    - `.planning/phases/30-eye-safety-ledger-and-closeout/30-SECURITY.md`
    - `.planning/phases/30-eye-safety-ledger-and-closeout/30-VERIFICATION.md`
    - `.planning/phases/30-eye-safety-ledger-and-closeout/30-VALIDATION.md`
    - `docs/meitu-function-blueprint/SHAPE_FEATURE_LEDGER.md`
    - `docs/meitu-function-blueprint/FEATURE_MATRIX.md`
  </read_first>
  <action>Before the first status edit, independently require: evidence `status: passed` with EYE-04 through EYE-07, exact 161/161 and 36/36 regression, zero public-geometry candidates, both VIP allowlist IDs, and `unclassified_matches: 0`; review `status: clean`; security `status: verified` and `threats_open: 0`; verification `status: promotion-ready` with EYE-04 through EYE-07 passed and EYE-08/DOC-01 pending; validation rows through 30-03-02 passed and every 30-04 through 30-07 row pending. On any failure, do not edit either file and confirm all four scoped ledger rows remain `partial` per D-18 and D-19.

After every prerequisite passes, change the four `SHAPE_FEATURE_LEDGER.md` rows `大小`, `上下`, `眼距`, and `眼尾上扬` from `partial` to `implemented` in one patch. Each exact row must cite its exact existing public field, Phase 29 renderer evidence, and `30-EYE-SAFETY-EVIDENCE.md`; no other eye row changes. Update only the exact `眼睛` branch row in `FEATURE_MATRIX.md`: retain exact `partial`, name the four implemented subtools and remaining future gaps, and link `30-EYE-SAFETY-EVIDENCE.md` in that row so the evidence check cannot be satisfied by another branch or historical note. Do not imply branch completion per D-19 and D-20.</action>
  <verify>
    <automated>for token in '^status: passed$' 'EYE-04' 'EYE-05' 'EYE-06' 'EYE-07' '161/161' '36/36' 'public_geometry_candidates: 0' 'VIP-COMMERCIAL-ALLOW-01' 'VIP-COMMERCIAL-ALLOW-02' 'unclassified_matches: 0'; do rg -n "$token" .planning/phases/30-eye-safety-ledger-and-closeout/30-EYE-SAFETY-EVIDENCE.md || exit 1; done</automated>
    <automated>rg -n '^status: clean$' .planning/phases/30-eye-safety-ledger-and-closeout/30-REVIEW.md</automated>
    <automated>for token in '^status: verified$' '^threats_open: 0$'; do rg -n "$token" .planning/phases/30-eye-safety-ledger-and-closeout/30-SECURITY.md || exit 1; done</automated>
    <automated>for token in '^status: promotion-ready$' 'EYE-04.*passed' 'EYE-05.*passed' 'EYE-06.*passed' 'EYE-07.*passed' 'EYE-08.*pending' 'DOC-01.*pending'; do rg -n "$token" .planning/phases/30-eye-safety-ledger-and-closeout/30-VERIFICATION.md || exit 1; done</automated>
    <automated>for token in '^status: in_progress$' '30-03-02.*passed' '30-04-01.*pending' '30-04-02.*pending' '30-05-01.*pending' '30-05-02.*pending' '30-06-01.*pending' '30-06-02.*pending' '30-07-01.*pending' '30-07-02.*pending'; do rg -n "$token" .planning/phases/30-eye-safety-ledger-and-closeout/30-VALIDATION.md || exit 1; done</automated>
    <automated>test "$(awk -F'|' '$2 ~ /`眼睛`/ && $4 ~ /implemented/ {gsub(/^[ `\t]+|[ `\t]+$/, "", $3); print $3}' docs/meitu-function-blueprint/SHAPE_FEATURE_LEDGER.md | paste -sd, -)" = '大小,上下,眼距,眼尾上扬'</automated>
    <automated>for pair in '大小:eyeSize' '上下:eyeYPosition' '眼距:eyeDistance' '眼尾上扬:eyeTailLift'; do label=${pair%%:*}; field=${pair#*:}; awk -F'|' -v label="$label" -v field="$field" '$2 ~ /`眼睛`/ { tool=$3; status=$4; gsub(/^[ `\t]+|[ `\t]+$/, "", tool); gsub(/^[ `\t]+|[ `\t]+$/, "", status); if (tool == label && status == "implemented" && index($0, field) > 0) hits++ } END { if (hits != 1) exit 1 }' docs/meitu-function-blueprint/SHAPE_FEATURE_LEDGER.md || exit 1; done</automated>
    <automated>for label in 大小 上下 眼距 眼尾上扬; do awk -F'|' -v label="$label" '$2 ~ /`眼睛`/ { tool=$3; gsub(/^[ `\t]+|[ `\t]+$/, "", tool); if (tool == label && index($0, "29-EYE-RENDERER-EVIDENCE.md") > 0 && index($0, "30-EYE-SAFETY-EVIDENCE.md") > 0) hits++ } END { if (hits != 1) exit 1 }' docs/meitu-function-blueprint/SHAPE_FEATURE_LEDGER.md || exit 1; done</automated>
    <automated>rg -n '^\| Beauty shaping \| 眼睛 \| partial \|.*大小.*上下.*眼距.*眼尾上扬.*future' docs/meitu-function-blueprint/FEATURE_MATRIX.md</automated>
    <automated>rg -n '^\| Beauty shaping \| 眼睛 \| partial \|.*30-EYE-SAFETY-EVIDENCE\.md' docs/meitu-function-blueprint/FEATURE_MATRIX.md</automated>
    <automated>git diff --name-only -- docs/meitu-function-blueprint/SHAPE_FEATURE_LEDGER.md | rg -x 'docs/meitu-function-blueprint/SHAPE_FEATURE_LEDGER.md'</automated>
    <automated>git diff --name-only -- docs/meitu-function-blueprint/FEATURE_MATRIX.md | rg -x 'docs/meitu-function-blueprint/FEATURE_MATRIX.md'</automated>
    <automated>awk -F'|' '$2 ~ /`眼睛`/ && $4 ~ /implemented/ { print }' docs/meitu-function-blueprint/SHAPE_FEATURE_LEDGER.md > /tmp/phase30-ledger-promoted-rows.out || exit $?; if rg -n 'commercial quality achieved|production quality|release[- ]ready|shipping ready|device parity passed|matches Meitu|full Meitu parity achieved|whole[- ]branch.*眼睛.*implemented|眼睛.*branch.*implemented' /tmp/phase30-ledger-promoted-rows.out > /tmp/phase30-ledger-overclaims.out; then sed -n '1,120p' /tmp/phase30-ledger-overclaims.out; exit 1; else scan_status=$?; case "$scan_status" in 1) : ;; *) exit "$scan_status" ;; esac; fi</automated>
    <automated>rg -n '^\| Beauty shaping \| 眼睛 \| partial \|' docs/meitu-function-blueprint/FEATURE_MATRIX.md > /tmp/phase30-feature-eye-row.out; test "$(wc -l < /tmp/phase30-feature-eye-row.out | tr -d ' ')" -eq 1; if rg -n 'commercial quality achieved|production quality|release[- ]ready|shipping ready|device parity passed|matches Meitu|full Meitu parity achieved|whole[- ]branch.*眼睛.*implemented|眼睛.*branch.*implemented' /tmp/phase30-feature-eye-row.out > /tmp/phase30-feature-overclaims.out; then sed -n '1,120p' /tmp/phase30-feature-overclaims.out; exit 1; else scan_status=$?; case "$scan_status" in 1) : ;; *) exit "$scan_status" ;; esac; fi</automated>
    <automated>git diff --check -- docs/meitu-function-blueprint/SHAPE_FEATURE_LEDGER.md docs/meitu-function-blueprint/FEATURE_MATRIX.md</automated>
  </verify>
  <acceptance_criteria>
    - Every pre-promotion artifact and boundary/classification fact passes before the atomic patch.
    - The complete implemented-eye-row list is exactly `大小,上下,眼距,眼尾上扬`; each row independently links both evidence artifacts.
    - `FEATURE_MATRIX.md` independently retains the exact partial branch row, names the four implemented subtools, and names future gaps.
    - Both files are changed, neither contains an overclaim, and scoped diff hygiene passes.
  </acceptance_criteria>
  <done>The authoritative ledger promotes the four rows together while the branch matrix stays partial.</done>
</task>

<task type="auto">
  <name>Task 30-04-02: Synchronize the two branch READMEs and renderer-validation contract</name>
  <files>docs/meitu-function-blueprint/features/beauty-shaping/eyes/README.md, docs/meitu-function-blueprint/features/beauty-shaping/README.md, docs/meitu-function-blueprint/EXAMPLE_IMAGE_VALIDATION.md</files>
  <read_first>
    - `docs/meitu-function-blueprint/SHAPE_FEATURE_LEDGER.md`
    - `docs/meitu-function-blueprint/FEATURE_MATRIX.md`
    - `docs/meitu-function-blueprint/features/beauty-shaping/eyes/README.md`
    - `docs/meitu-function-blueprint/features/beauty-shaping/README.md`
    - `docs/meitu-function-blueprint/EXAMPLE_IMAGE_VALIDATION.md`
    - `.planning/phases/30-eye-safety-ledger-and-closeout/30-EYE-SAFETY-EVIDENCE.md`
    - `.planning/phases/29-eye-renderer-output-evidence/29-EYE-RENDERER-EVIDENCE.md`
  </read_first>
  <action>After Task 30-04-01's exact row and branch guards pass, add or replace one exact `## Phase 30 Existing-Parameter Closeout` section in the eyes README with `eyeSize`/`eyeTailLift` positive-only `[0,1]`, `eyeDistance`/`eyeYPosition` signed `[-1,1]`, exact caps `0.45/0.30/0.25/0.30`, both-eyes-required behavior, missing/reused/stale full-eye skip, the three stable category-only reason codes, aggregate-only metadata, private raw geometry, the four implemented rows, branch partial, future tools, and explicit Phase 29 plus `30-EYE-SAFETY-EVIDENCE.md` links. Add or replace one exact `## Phase 30 Eye Evidence` section in the beauty-shaping README that independently says four eye subtools are implemented while the family/eye branch remains partial and links the Phase 30 evidence. Add or replace one exact `## Phase 30 Eye Safety Closeout Evidence` section in `EXAMPLE_IMAGE_VALIDATION.md` with Phase 30 evidence/verification links while independently retaining 23 renderer cases, 7 fixtures, 161 outputs, 36/36 comparisons, ignored output/gallery artifacts, unchanged helper, and no required gallery rerun per D-16, D-20, and D-21. Verification must extract each exact heading through the next heading of equal or higher level so Phase 28/29 history cannot satisfy a Phase 30 check.</action>
  <verify>
    <automated>awk '$0 == "## Phase 30 Existing-Parameter Closeout" { if (seen) exit 2; seen=1; copy=1 } copy && $0 != "## Phase 30 Existing-Parameter Closeout" && /^#{1,2} / { copy=0 } copy { print } END { if (seen != 1) exit 3 }' docs/meitu-function-blueprint/features/beauty-shaping/eyes/README.md > /tmp/phase30-eyes-readme-section.out || exit $?; for token in 'eyeSize.*0\.45' 'eyeTailLift.*0\.30' 'eyeDistance.*0\.30' 'eyeYPosition.*0\.25' 'eye_inputs_missing' 'eye_geometry_reused_skipped' 'eye_geometry_stale_skipped' '大小' '上下' '眼距' '眼尾上扬' 'branch.*partial'; do rg -n "$token" /tmp/phase30-eyes-readme-section.out || exit 1; done</automated>
    <automated>awk '$0 == "## Phase 30 Existing-Parameter Closeout" { if (seen) exit 2; seen=1; copy=1 } copy && $0 != "## Phase 30 Existing-Parameter Closeout" && /^#{1,2} / { copy=0 } copy { print } END { if (seen != 1) exit 3 }' docs/meitu-function-blueprint/features/beauty-shaping/eyes/README.md > /tmp/phase30-eyes-readme-evidence.out || exit $?; rg -n '29-EYE-RENDERER-EVIDENCE\.md' /tmp/phase30-eyes-readme-evidence.out; rg -n '30-EYE-SAFETY-EVIDENCE\.md' /tmp/phase30-eyes-readme-evidence.out</automated>
    <automated>awk '$0 == "## Phase 30 Eye Evidence" { if (seen) exit 2; seen=1; copy=1 } copy && $0 != "## Phase 30 Eye Evidence" && /^#{1,2} / { copy=0 } copy { print } END { if (seen != 1) exit 3 }' docs/meitu-function-blueprint/features/beauty-shaping/README.md > /tmp/phase30-beauty-readme-section.out || exit $?; for token in '大小' '上下' '眼距' '眼尾上扬' '眼睛.*partial'; do rg -n "$token" /tmp/phase30-beauty-readme-section.out || exit 1; done</automated>
    <automated>awk '$0 == "## Phase 30 Eye Evidence" { if (seen) exit 2; seen=1; copy=1 } copy && $0 != "## Phase 30 Eye Evidence" && /^#{1,2} / { copy=0 } copy { print } END { if (seen != 1) exit 3 }' docs/meitu-function-blueprint/features/beauty-shaping/README.md > /tmp/phase30-beauty-readme-evidence.out || exit $?; rg -n '29-EYE-RENDERER-EVIDENCE\.md' /tmp/phase30-beauty-readme-evidence.out; rg -n '30-EYE-SAFETY-EVIDENCE\.md' /tmp/phase30-beauty-readme-evidence.out</automated>
    <automated>awk '$0 == "## Phase 30 Eye Safety Closeout Evidence" { if (seen) exit 2; seen=1; copy=1 } copy && $0 != "## Phase 30 Eye Safety Closeout Evidence" && /^#{1,2} / { copy=0 } copy { print } END { if (seen != 1) exit 3 }' docs/meitu-function-blueprint/EXAMPLE_IMAGE_VALIDATION.md > /tmp/phase30-example-validation-section.out || exit $?; for token in '23' '7 fixtures' '161/161' '36/36' 'example-images/output' 'example-images/gallery' 'ignored' 'check_eye_renderer_outputs\.py'; do rg -n "$token" /tmp/phase30-example-validation-section.out || exit 1; done</automated>
    <automated>awk '$0 == "## Phase 30 Eye Safety Closeout Evidence" { if (seen) exit 2; seen=1; copy=1 } copy && $0 != "## Phase 30 Eye Safety Closeout Evidence" && /^#{1,2} / { copy=0 } copy { print } END { if (seen != 1) exit 3 }' docs/meitu-function-blueprint/EXAMPLE_IMAGE_VALIDATION.md > /tmp/phase30-example-validation-evidence.out || exit $?; rg -n '30-EYE-SAFETY-EVIDENCE\.md' /tmp/phase30-example-validation-evidence.out; rg -n '30-VERIFICATION\.md' /tmp/phase30-example-validation-evidence.out</automated>
    <automated>git diff --name-only -- docs/meitu-function-blueprint/features/beauty-shaping/eyes/README.md | rg -x 'docs/meitu-function-blueprint/features/beauty-shaping/eyes/README.md'</automated>
    <automated>git diff --name-only -- docs/meitu-function-blueprint/features/beauty-shaping/README.md | rg -x 'docs/meitu-function-blueprint/features/beauty-shaping/README.md'</automated>
    <automated>git diff --name-only -- docs/meitu-function-blueprint/EXAMPLE_IMAGE_VALIDATION.md | rg -x 'docs/meitu-function-blueprint/EXAMPLE_IMAGE_VALIDATION.md'</automated>
    <automated>awk '$0 == "## Phase 30 Existing-Parameter Closeout" { if (seen) exit 2; seen=1; copy=1 } copy && $0 != "## Phase 30 Existing-Parameter Closeout" && /^#{1,2} / { copy=0 } copy { print } END { if (seen != 1) exit 3 }' docs/meitu-function-blueprint/features/beauty-shaping/eyes/README.md > /tmp/phase30-eyes-readme-no-overclaim.out || exit $?; if rg -n 'commercial quality achieved|production quality|release[- ]ready|shipping ready|device parity passed|matches Meitu|full Meitu parity achieved|whole[- ]branch.*眼睛.*implemented|眼睛.*branch.*implemented' /tmp/phase30-eyes-readme-no-overclaim.out > /tmp/phase30-eyes-readme-overclaims.out; then sed -n '1,120p' /tmp/phase30-eyes-readme-overclaims.out; exit 1; else scan_status=$?; case "$scan_status" in 1) : ;; *) exit "$scan_status" ;; esac; fi</automated>
    <automated>awk '$0 == "## Phase 30 Eye Evidence" { if (seen) exit 2; seen=1; copy=1 } copy && $0 != "## Phase 30 Eye Evidence" && /^#{1,2} / { copy=0 } copy { print } END { if (seen != 1) exit 3 }' docs/meitu-function-blueprint/features/beauty-shaping/README.md > /tmp/phase30-beauty-readme-no-overclaim.out || exit $?; if rg -n 'commercial quality achieved|production quality|release[- ]ready|shipping ready|device parity passed|matches Meitu|full Meitu parity achieved|whole[- ]branch.*眼睛.*implemented|眼睛.*branch.*implemented' /tmp/phase30-beauty-readme-no-overclaim.out > /tmp/phase30-beauty-readme-overclaims.out; then sed -n '1,120p' /tmp/phase30-beauty-readme-overclaims.out; exit 1; else scan_status=$?; case "$scan_status" in 1) : ;; *) exit "$scan_status" ;; esac; fi</automated>
    <automated>awk '$0 == "## Phase 30 Eye Safety Closeout Evidence" { if (seen) exit 2; seen=1; copy=1 } copy && $0 != "## Phase 30 Eye Safety Closeout Evidence" && /^#{1,2} / { copy=0 } copy { print } END { if (seen != 1) exit 3 }' docs/meitu-function-blueprint/EXAMPLE_IMAGE_VALIDATION.md > /tmp/phase30-example-validation-no-overclaim.out || exit $?; if rg -n 'commercial quality achieved|production quality|release[- ]ready|shipping ready|device parity passed|matches Meitu|full Meitu parity achieved|whole[- ]branch.*眼睛.*implemented|眼睛.*branch.*implemented' /tmp/phase30-example-validation-no-overclaim.out > /tmp/phase30-example-validation-overclaims.out; then sed -n '1,120p' /tmp/phase30-example-validation-overclaims.out; exit 1; else scan_status=$?; case "$scan_status" in 1) : ;; *) exit "$scan_status" ;; esac; fi</automated>
    <automated>git diff --check -- docs/meitu-function-blueprint/features/beauty-shaping/eyes/README.md docs/meitu-function-blueprint/features/beauty-shaping/README.md docs/meitu-function-blueprint/EXAMPLE_IMAGE_VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Each README independently distinguishes four implemented subtools from the partial branch and future tools and cites Phase 29 plus Phase 30 evidence.
    - The eyes README independently owns the exact ranges, caps, three degradation codes, both-eyes requirement, aggregate-only observability, and private geometry boundary.
    - `EXAMPLE_IMAGE_VALIDATION.md` independently retains 23 cases, 7 fixtures, 161/161, 36/36, ignored artifacts, and unchanged helper/gallery behavior.
    - All three required files are changed, contain no overclaim, and pass scoped diff hygiene.
  </acceptance_criteria>
  <done>All five blueprint owners agree on four implemented rows, branch partial status, and unchanged renderer evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Promotion-ready evidence -> authoritative feature ledger | Every evidence, review, security, boundary, renderer, and validation prerequisite must pass before the single four-row patch. |
| Four implemented rows -> branch documentation | Scoped status must not become a whole-eye-branch, device, commercial, parity, quality, or readiness claim. |
| Detailed evidence -> blueprint docs | Only invariants, relative links, counts, stable codes, aggregate metrics, and limitations may cross. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Task-level mitigation and verification gate |
|-----------|----------|-----------|-------------|---------------------------------------------|
| T-30-01 | Tampering / Denial of Service | Invalid eye input | mitigate | Task 30-04-01 requires passed EYE-04 evidence before any row edit. |
| T-30-02 | Tampering | Missing/reused/stale eyes | mitigate | Task 30-04-01 requires passed EYE-05 evidence; Task 30-04-02 records exact skip semantics. |
| T-30-03 | Information Disclosure | Blueprint warning/privacy wording | mitigate | Task 30-04-02 allows stable codes and aggregate facts only and preserves private geometry wording. |
| T-30-04 | Information Disclosure / Elevation of Privilege | Raw geometry/import boundary | mitigate | Task 30-04-01 requires EYE-07, zero public candidates, and classified VIP evidence. |
| T-30-05 | Elevation of Privilege | Network/commercial scope | mitigate | Task 30-04-01 blocks on EYE-07; both tasks run independent no-overclaim checks. |
| T-30-06 | Information Disclosure / Repudiation | Generated artifacts | mitigate | Task 30-04-02 retains ignored output/gallery and unchanged-helper facts only. |
| T-30-07 | Spoofing / Repudiation | False partial promotion | mitigate | Task 30-04-01 compares the complete implemented-eye list to four exact names after one atomic patch. |
| T-30-08 | Spoofing / Repudiation | Whole-branch overclaim | mitigate | Both tasks verify branch `partial` and scan every file independently. |
| T-30-SC | Tampering | Package-manager installs | mitigate | No package, service, source, test, renderer, or generator change is allowed. |
</threat_model>

<verification>
Overall Plan 30-04 checks:

- Every pre-promotion artifact and EYE-07 classification passes before the ledger edit.
- The implemented-eye list is exactly four locked names and branch status remains partial.
- Each of the five files independently contains its owning evidence/invariant and is present in the task diff.
- Each file independently passes no-overclaim checks; scoped diff hygiene passes.
</verification>

<success_criteria>
Plan 30-04 succeeds when the atomic four-row promotion is proven from already-passed evidence and all five blueprint owners independently retain branch-partial, privacy, ignored-artifact, renderer-count, and non-claim boundaries.
</success_criteria>

<output>
Create `.planning/phases/30-eye-safety-ledger-and-closeout/30-04-SUMMARY.md` when done.
</output>
