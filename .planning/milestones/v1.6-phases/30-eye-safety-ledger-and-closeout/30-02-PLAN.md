---
phase: 30-eye-safety-ledger-and-closeout
plan: "02"
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
  - BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift
autonomous: true
requirements:
  - EYE-05
  - EYE-06
must_haves:
  truths:
    - "Missing either eye group skips the entire eye domain, zeros all four eye strengths, generates no eye points, and emits code `eye_inputs_missing` with exact category-only message `Eye effects skipped: inputs incomplete.`; usable nose/mouth plus safe color/filter domains continue per D-10, D-11, D-12, and D-15."
    - "Reused and stale geometry skip eyes with codes/messages `eye_geometry_reused_skipped` / `Eye effects skipped: inputs reused.` and `eye_geometry_stale_skipped` / `Eye effects skipped: inputs stale.`, while reused face-shape/nose/mouth still use the existing 0.5 reduction and `geometry_stale_reduced` evidence per D-07, D-08, D-09, and D-11."
    - "The public facade preserves output extent, reports no-face detection with aggregate redacted metadata, and continues color/filter processing for an eye request per D-12."
    - "All six Phase 29 visible eye directions plus one all-eye multi-domain case cross the existing conflict resolver, preserve applicable signs, and weaken below their normal capped strengths per D-13 and D-14."
  artifacts:
    - path: "BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift"
      provides: "Eye-only reused/stale skip policy, zeroing helper, distinct warning reasons, and unchanged non-eye reuse reduction"
      contains: "eye_geometry_reused_skipped"
    - path: "BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift"
      provides: "Either-eye missing plus reused/stale zero/no-point/domain-continuation matrix"
      contains: "testReusedEyeGeometrySkipsEyesZerosStrengthsAndPreservesNonEyeReuseReduction"
    - path: "BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift"
      provides: "Eye-specific public no-face extent, safe-domain, summary, warning, and redaction evidence"
      contains: "testEyeNoFaceRequestPreservesExtentSafeDomainsAndRedactedMetadata"
    - path: "BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift"
      provides: "Six per-behavior normal-vs-combined cases and one all-eye aggregate case"
      contains: "testEYE06EachVisibleEyeBehaviorWeakensWithFaceShapeAndPreservesDirection"
  key_links:
    - from: "BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift"
      to: "BeautySDK/Sources/BeautyEffects/Warp/EyeWarpProvider.swift"
      via: "requested-eye intent survives freshness zeroing; provider receives only fresh complete eye geometry"
      pattern: "hasRequestedEyeValues|zeroEyeStrengths|EyeWarpProvider"
    - from: "BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift"
      to: "BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift"
      via: "faceSlim + faceSmall forces the existing combined-geometry scale below one"
      pattern: "combined_geometry_weakened|beauty.effects.geometryStrengthScale"
---

<objective>
Implement and prove the eye-specific missing/reused/stale degradation policy and the complete combined-geometry weakening matrix.

Purpose: close EYE-05 and EYE-06 at provider/resolver/public-facade layers while keeping the stricter reuse rule confined to eyes.
Output: domain-specific resolver behavior, shared either-eye fixtures/tests, a public no-face facade test, and seven combined weakening cases.
</objective>

## Artifacts this phase produces

- Private resolver local `hasRequestedEyeValues` that records pre-zero eye intent.
- Private resolver helper `zeroEyeStrengths(_:)`.
- Private resolver helper `scaleReusableNonEyeGeometryStrengths(_:by:)` replacing the global eye-inclusive reuse scaler.
- Stable warning code `eye_geometry_reused_skipped` with category-only text.
- Stable warning code `eye_geometry_stale_skipped` with category-only text.
- Existing warning code `eye_inputs_missing` retained for either-eye missing geometry.
- Exact public warning messages locked as `Eye effects skipped: inputs incomplete.`, `Eye effects skipped: inputs reused.`, and `Eye effects skipped: inputs stale.`; none exposes left/right side or raw geometry terms.
- Existing aggregate metric `beauty.effects.skippedEyeDomains` retained with exact value `1`; no raw eye-side or coordinate metric is added.
- Shared test fixture `FaceGeometry.missingRightEye`; `missingLeftEye` remains complete outside its missing eye group.
- XCTest `EyeWarpProviderTests.testMissingEitherEyeInputReturnsStableSkipReasonWithoutPoints`.
- XCTest `MissingLandmarkDegradationTests.testMissingEitherEyeGroupSkipsEyesZerosStrengthsAndKeepsOtherDomainsActive`.
- XCTest `MissingLandmarkDegradationTests.testReusedEyeGeometrySkipsEyesZerosStrengthsAndPreservesNonEyeReuseReduction`.
- XCTest `MissingLandmarkDegradationTests.testStaleEyeGeometrySkipsEyesZerosStrengthsWithDistinctReason`.
- XCTest `BeautyEngineGeometryFacadeTests.testEyeNoFaceRequestPreservesExtentSafeDomainsAndRedactedMetadata`.
- XCTest `CombinedEffectSafetyTests.testEYE06EachVisibleEyeBehaviorWeakensWithFaceShapeAndPreservesDirection`.
- XCTest `CombinedEffectSafetyTests.testEYE06AllEyeMultiDomainCaseEmitsStableWeakeningEvidence`.
- No new public field, raw geometry result, provider, freshness enum, renderer case, Demo UI, generated PNG, package, or target is created.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/30-eye-safety-ledger-and-closeout/30-CONTEXT.md
@.planning/phases/30-eye-safety-ledger-and-closeout/30-RESEARCH.md
@.planning/phases/30-eye-safety-ledger-and-closeout/30-VALIDATION.md
@.planning/phases/30-eye-safety-ledger-and-closeout/30-PATTERNS.md
@.planning/phases/30-eye-safety-ledger-and-closeout/30-01-SUMMARY.md
@BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
@BeautySDK/Sources/BeautyEffects/Warp/EyeWarpProvider.swift
@BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift
@BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift
@BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
@BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift
@BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift
@BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift
@BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift
@DESIGN.md
@SECURITY.md
@RELIABILITY.md
</context>

<tasks>

<task type="auto">
  <name>Task 30-02-01: Skip and zero missing, reused, and stale eye geometry without changing non-eye reuse</name>
  <files>BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift, BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift, BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift, BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift</files>
  <read_first>
    - `BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift`
    - `BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift`
    - `BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift`
    - `BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift`
    - `BeautySDK/Sources/BeautyEffects/Warp/EyeWarpProvider.swift`
    - `BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift`
    - `BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift`
    - `.planning/phases/30-eye-safety-ledger-and-closeout/30-CONTEXT.md`
    - `.planning/phases/30-eye-safety-ledger-and-closeout/30-RESEARCH.md`
    - `SECURITY.md`
    - `RELIABILITY.md`
  </read_first>
  <action>Refactor only the freshness/eye-domain seam in `BeautyEffectResolver.resolve(...)`: capture `hasRequestedEyeValues` immediately after capping and before zeroing; replace `scaleGeometryStrengths` with `scaleReusableNonEyeGeometryStrengths` that scales face shape, nose, and mouth but never eyes; for `.reused` requested eyes call `zeroEyeStrengths`, mark `.eyes` skipped/not active, set `beauty.effects.skippedEyeDomains` to `1`, generate no eye control points, and append exact code/message `eye_geometry_reused_skipped` / `Eye effects skipped: inputs reused.`; for `.stale` do the same with `eye_geometry_stale_skipped` / `Eye effects skipped: inputs stale.`; for a fresh provider result with either eye group missing, call the same zero helper and retain exact code/message `eye_inputs_missing` / `Eye effects skipped: inputs incomplete.`. Mixed reused requests must also retain `geometry_stale_reduced`, `beauty.effects.reusedGeometryScale == 0.5`, and reduced active face-shape/nose/mouth behavior. These three literal messages are the complete public payload: do not interpolate an eye side or any runtime data per D-07 through D-11.

In the shared `FaceGeometry` extension, add `missingRightEye` and make both one-eye fixtures complete for contour, the other eye, nose, and outer lips. Replace the provider's single missing-left test with `testMissingEitherEyeInputReturnsStableSkipReasonWithoutPoints`. Add/replace focused degradation tests with the exact names listed in this plan: loop over left/right missing fixtures with all four eye fields plus nose, mouth, brightness, and filter; assert eyes skipped/not active, all four eye strengths zero, no eye points, exact skip count `1`, distinct exact code and exact message, usable non-eye/safe domains continue, and all metadata passes `assertRedacted`. Add and call a test helper named `assertNoEyeSideOrRawGeometryDisclosure` for every missing/reused/stale plan; it must inspect every warning message, metric key, and string metric value and assert lowercased text contains none of `left`, `right`, `eye side`, `landmark`, `coordinate`, `bounding`, `bounds`, `control point`, `path`, `image bytes`, `raw`, `VNFaceObservation`, `NSError`, or `AVError`. Do not rely on the existing `assertRedacted` alone because it does not reject eye-side disclosure. Rewrite both legacy reused-eye-active assertions (`testReusedLandmarksReduceEyeAndNoseGeometry` and reused assertions in `testPERF03NoFaceMissingStaleAndReusedGeometryRemainRedactedAndDegraded`) so no contradictory expectation remains. Do not change reused behavior for face shape, nose, mouth, the freshness enum, or `EyeWarpProvider` production source per D-09, D-10, D-11, and D-15.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.EyeWarpProviderTests</automated>
    <automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.MissingLandmarkDegradationTests</automated>
    <automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.GeometryConflictResolverTests</automated>
    <automated>rg -n "hasRequestedEyeValues|zeroEyeStrengths|scaleReusableNonEyeGeometryStrengths|eye_geometry_reused_skipped|eye_geometry_stale_skipped|missingRightEye|testMissingEitherEyeInputReturnsStableSkipReasonWithoutPoints|testMissingEitherEyeGroupSkipsEyesZerosStrengthsAndKeepsOtherDomainsActive|testReusedEyeGeometrySkipsEyesZerosStrengthsAndPreservesNonEyeReuseReduction|testStaleEyeGeometrySkipsEyesZerosStrengthsWithDistinctReason" BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift</automated>
    <automated>if rg -n "XCTAssertTrue\(reused\.activeDomains\.contains\(\.eyes\)\)|func testReusedLandmarksReduceEyeAndNoseGeometry" BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift; then exit 1; fi</automated>
    <automated>for message in 'Eye effects skipped: inputs incomplete.' 'Eye effects skipped: inputs reused.' 'Eye effects skipped: inputs stale.'; do rg -n -F "$message" BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift || exit 1; rg -n -F "$message" BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift || exit 1; done</automated>
    <automated>rg -n 'func assertNoEyeSideOrRawGeometryDisclosure' BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift; test "$(rg -c 'assertNoEyeSideOrRawGeometryDisclosure\(' BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift)" -ge 4</automated>
    <automated>rg -n 'left|right|eye side|landmark|coordinate|bounding|bounds|control point|path|image bytes|raw|VNFaceObservation|NSError|AVError' BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift</automated>
    <automated>git diff --check -- BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - Missing-left and missing-right tests both assert zero points, `eye_inputs_missing`, four zero effective strengths, `.eyes` skipped/not active, exact skip metric 1, and continuation of nose/mouth/color/filter domains.
    - Reused-eye tests assert exact reused-eye reason, four zeros, no eye point metric for eye-only input, while mixed face-shape/nose/mouth remain active at the existing 0.5 reuse policy with `geometry_stale_reduced`.
    - Stale-eye tests assert exact stale-eye reason, four zeros, `.eyes` skipped/not active, and no eye point metric for eye-only input.
    - Every missing/reused/stale case asserts its exact code and exact category-only message; explicit forbidden-term assertions reject left/right eye-side and raw geometry disclosure in every new warning/metadata result instead of relying only on `assertRedacted`.
    - No legacy test still asserts that reused eyes are active.
    - All three focused test commands and scoped `git diff --check` exit 0.
  </acceptance_criteria>
  <done>Eye geometry degrades by distinct safe reasons, while every non-eye reuse contract remains unchanged.</done>
</task>

<task type="auto">
  <name>Task 30-02-02: Add public-facade no-face eye degradation evidence</name>
  <files>BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift</files>
  <read_first>
    - `BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift`
    - `BeautySDK/Sources/BeautySDK/BeautyEngine.swift`
    - `BeautySDK/Sources/BeautySDK/BeautyEngineGeometryDetection.swift`
    - `BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift`
    - `BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift`
    - `.planning/phases/30-eye-safety-ledger-and-closeout/30-CONTEXT.md`
    - `.planning/phases/29-eye-renderer-output-evidence/29-VERIFICATION.md`
    - `SECURITY.md`
    - `RELIABILITY.md`
  </read_first>
  <action>Add `testEyeNoFaceRequestPreservesExtentSafeDomainsAndRedactedMetadata` to `BeautyEngineGeometryFacadeTests` using `SDKTestingFaceDetectionProvider([.noFace])`, the existing test image, `eyeSize: 0.35`, `brightness: 0.2`, and built-in `soft_clean` filter intensity `0.5`. Assert detector invocation count `1`; output extent exactly equals input extent; detection availability `.noFace`, reasons exactly `[.noFaceDetected]`, face count and used-face count `0`; `beauty.detection.geometryRequired == 1`; `beauty.effects.activeCount >= 2`; no `beauty.effects.geometryPointCount`; warning code `face_effects_skipped_no_face`; and the existing `assertRedacted(result)` passes. Add and call a facade-test helper named `assertNoEyeSideOrRawGeometryDisclosure` that inspects every warning message, metadata key, and string metadata value in this public result and explicitly rejects lowercased `left`, `right`, `eye side`, `landmark`, `coordinate`, `bounding`, `bounds`, `control point`, `path`, `image bytes`, and `raw`; this is required because the shared helper does not cover eye-side words. Keep internal four-strength and freshness details in Task 30-02-01 tests; do not expose raw geometry, a new public degradation enum, raw detector objects, or extra public result fields merely to observe them per D-11 and D-12.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyEngineGeometryFacadeTests</automated>
    <automated>rg -n "testEyeNoFaceRequestPreservesExtentSafeDomainsAndRedactedMetadata|SDKTestingFaceDetectionProvider\(\[\.noFace\]\)|face_effects_skipped_no_face|geometryPointCount" BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift</automated>
    <automated>rg -n 'func assertNoEyeSideOrRawGeometryDisclosure' BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift; test "$(rg -c 'assertNoEyeSideOrRawGeometryDisclosure\(' BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift)" -ge 2</automated>
    <automated>rg -n 'left|right|eye side|landmark|coordinate|bounding|bounds|control point|path|image bytes|raw' BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift</automated>
    <automated>git diff --check -- BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - The named eye-specific facade test contains exact assertions for one detector invocation, identical extent, no-face availability/reason, zero used faces, geometry-required 1, safe-domain active count at least 2, no geometry-point metric, generic no-face warning, existing redaction, and explicit rejection of eye-side/raw geometry terms in every public warning/metadata string.
    - No production facade source or public result type is modified by this task.
    - Focused facade tests and scoped `git diff --check` exit 0.
  </acceptance_criteria>
  <done>Public EYE-05 evidence proves safe no-face output without exposing internal geometry state.</done>
</task>

<task type="auto">
  <name>Task 30-02-03: Prove per-behavior and all-eye combined weakening</name>
  <files>BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift</files>
  <read_first>
    - `BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift`
    - `BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift`
    - `BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift`
    - `BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift`
    - `BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift`
    - `.planning/phases/29-eye-renderer-output-evidence/29-EYE-RENDERER-EVIDENCE.md`
    - `.planning/phases/30-eye-safety-ledger-and-closeout/30-CONTEXT.md`
    - `.planning/phases/30-eye-safety-ledger-and-closeout/30-RESEARCH.md`
  </read_first>
  <action>Add `testEYE06EachVisibleEyeBehaviorWeakensWithFaceShapeAndPreservesDirection` as a six-case table mirroring the Phase 29 renderer inventory: size `+1`, distance `+1`, distance `-1`, Y `+1`, Y `-1`, and tail `+1`. For each case compare a normal fresh-geometry plan to a combined fresh-geometry plan that adds `faceSlim: 1` and `faceSmall: 1` so capped face-shape total `1.05` crosses the conflict threshold. Assert `.eyes` and `.faceShape` active, normal value equals its exact EYE-04 cap/sign, combined absolute value is greater than zero and strictly below normal, signed direction is unchanged where applicable, `combined_geometry_weakened` exists, `beauty.effects.weakenedCount > 0`, and `beauty.effects.geometryStrengthScale < 1` per D-13.

Add `testEYE06AllEyeMultiDomainCaseEmitsStableWeakeningEvidence` using all four eye fields with signed values `eyeDistance: -1` and `eyeYPosition: 1`, plus `faceSlim: 1` and `noseSlim: 1`. Assert `.eyes`, `.faceShape`, and `.nose` active; distance remains negative and Y positive after weakening; exact `beauty.effects.weakenedCount == 6`; stable combined warning; scale below one; geometry points present; and redacted metadata. Exercise the existing `GeometryConflictResolver`; do not add a second conflict resolver or change production math unless a focused test demonstrates a defect, in which case stop and record the blocker because that would expand the planned file set per D-14 and D-15.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.CombinedEffectSafetyTests</automated>
    <automated>rg -n "testEYE06EachVisibleEyeBehaviorWeakensWithFaceShapeAndPreservesDirection|testEYE06AllEyeMultiDomainCaseEmitsStableWeakeningEvidence|combined_geometry_weakened|beauty\.effects\.weakenedCount|beauty\.effects\.geometryStrengthScale" BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift</automated>
    <automated>git diff --exit-code -- BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift</automated>
    <automated>git diff --check -- BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - The per-behavior test executes exactly six named cases corresponding to all Phase 29 visible directions and compares normal versus combined plans.
    - Every case asserts nonzero additional weakening, stable combined warning, weakened count greater than zero, scale below one, and sign preservation.
    - The all-eye case asserts exactly six weakened fields, three active geometry domains, signed distance/Y preservation, a geometry-point metric, and redacted metadata.
    - `GeometryConflictResolver.swift` is unchanged; focused combined tests and scoped `git diff --check` exit 0.
  </acceptance_criteria>
  <done>EYE-06 has behavior-specific and aggregate combined-geometry evidence without a new algorithm.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Face geometry freshness/landmark groups -> resolver | Reused, stale, or one-eye-incomplete data must never produce eye geometry; non-eye reuse behavior must not be broadened. |
| Resolver/provider -> public facade metadata | Only stable category warnings and aggregate metrics may cross; no eye side or raw geometry payload may leak. |
| Multi-domain strengths -> conflict resolver | Combined weakening must preserve signed behavior and never silently bypass the safety threshold. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Task-level mitigation and verification gate |
|-----------|----------|-----------|-------------|---------------------------------------------|
| T-30-01 | Tampering / Denial of Service | Invalid/non-finite or wrong-sign input | mitigate | Plan 30-01 focused tests own normalization/cap safety; all Plan 30-02 suites consume those normalized semantics. |
| T-30-02 | Tampering | Missing/reused/stale eye geometry | mitigate | Task 30-02-01 adds distinct skip reasons, four-zero/no-point assertions, either-eye fixtures, and non-eye reuse regression gates. |
| T-30-03 | Information Disclosure | Warning/metric/facade metadata | mitigate | Tasks 30-02-01/02 lock three exact category-only messages and explicitly reject left/right/eye-side plus raw-geometry terms in every new warning and public metadata string; `assertRedacted` remains an additional guard. |
| T-30-04 | Information Disclosure / Elevation of Privilege | Raw public/SPI geometry and forbidden imports | mitigate | Task 30-03-02 owns zero-tolerance source/import scans; Task 30-02-02 explicitly forbids new public observation fields. |
| T-30-05 | Elevation of Privilege | Network/cloud or commercial entitlement scope | mitigate | Task 30-03-02 owns API-shaped hard gates; no service, StoreKit path, or package is added in this plan. |
| T-30-06 | Information Disclosure / Repudiation | Generated renderer/gallery artifacts | mitigate | Task 30-03-01 owns the unchanged Phase 29 helper and zero tracked-artifact gate; this plan creates tests only. |
| T-30-07 | Spoofing / Repudiation | False atomic row promotion | mitigate | Task 30-04-01 owns evidence-first atomic promotion; this plan does not modify status ledgers. |
| T-30-08 | Spoofing / Repudiation | Whole-branch or product-readiness overclaim | mitigate | Task 30-04-02 owns branch-partial/no-overclaim gates; EYE-05/06 tests cannot independently justify promotion. |
| T-30-SC | Tampering | Package-manager installs | mitigate | Every task uses existing SwiftPM/XCTest/git/rg only; npm, pip, cargo, new packages, network services, and entitlement SDKs are prohibited. |
</threat_model>

<verification>
Overall Plan 30-02 checks:

- Focused provider, missing-landmark, conflict-resolver, public-facade, and combined-safety suites pass.
- Static source assertions find the two stable eye freshness warnings, zero/non-eye-scale helpers, named fixtures, and exact tests.
- Legacy reused-eye-active assertions are absent.
- `GeometryConflictResolver.swift`, the public facade, renderer, Demo, and ledger files remain unchanged.
- Scoped `git diff --check` passes.
</verification>

<success_criteria>
Plan 30-02 succeeds when missing either eye, reused eyes, and stale eyes cannot create an active eye domain or nonzero eye strengths/points; the public no-face route remains extent-preserving/redacted with safe domains active; every visible eye direction is weakened safely in combined geometry; and non-eye reuse remains unchanged.
</success_criteria>

<output>
Create `.planning/phases/30-eye-safety-ledger-and-closeout/30-02-SUMMARY.md` when done.
</output>
