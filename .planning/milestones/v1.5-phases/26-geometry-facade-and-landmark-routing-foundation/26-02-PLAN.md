---
phase: 26-geometry-facade-and-landmark-routing-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "26-01"
files_modified:
  - BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift
  - BeautySDK/Sources/BeautySDK/BeautyEngine.swift
  - BeautySDK/Sources/BeautySDK/BeautyEngineGeometryDetection.swift
  - BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyEngineMetadataCompatibilityTests.swift
autonomous: true
requirements:
  - GEO-01
  - GEO-02
user_setup: []
must_haves:
  truths:
    - "Public `BeautyEngine.processResult(image:metadata:parameters:)` runs detection only for geometry-triggering still-image work, per D-01."
    - "No usable face, low-confidence face, missing landmarks, and detector failures degrade and continue with safe face-agnostic work, per D-02."
    - "No-geometry paths preserve `.notRun` and disabled tracking preserves `.disabled`, per D-03."
    - "Facade tests can deterministically simulate usable face, no-face, low-confidence, missing-landmark, and detector-failure states through SPI-only fixtures, per D-04 and D-11."
    - "Public result evidence proves routing intent without raw geometry, per D-09, D-13, D-14, and D-15."
  artifacts:
    - path: "BeautySDK/Sources/BeautySDK/BeautyEngineGeometryDetection.swift"
      provides: "Still-image geometry detection gate and routing helper"
      contains: "requiresFaceGeometry"
    - path: "BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift"
      provides: "SPI-only deterministic facade detector fixtures"
      contains: "SDKTestingFaceDetectionFixture"
    - path: "BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift"
      provides: "Public facade geometry activation/degradation tests"
    - path: "BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift"
      provides: "Package-only detector seam usable by the facade"
  key_links:
    - from: "BeautySDK/Sources/BeautySDK/BeautyEngine.swift"
      to: "BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift"
      via: "requiresFaceGeometry and selectedFaceObservation resolver overload"
      pattern: "requiresFaceGeometry"
    - from: "BeautySDK/Sources/BeautySDK/BeautyEngine.swift"
      to: "BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift"
      via: "package detector call for geometry-triggered still images"
      pattern: "detect"
---

<objective>
Wire the public still-image facade through the package-only detector and selected-face resolver route created in Plan 26-01.

Purpose: satisfy GEO-01 and GEO-02 by proving public `BeautyEngine.processResult(image:metadata:parameters:)` can activate geometry planning from detection while preserving no-detection compatibility and raw-geometry privacy.
Output: facade gating/routing code, SPI-only deterministic test fixtures, and public-facade tests.
</objective>

## Artifacts This Phase Produces

- `BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift` with `BeautyFaceGeometryAdapter` from Plan 26-01.
- `BeautyEffectResolver.requiresFaceGeometry(parameters:)` and `BeautyEffectResolver.resolve(parameters:selectedFaceObservation:)` from Plan 26-01.
- Package-only access for `VisionFaceDetector` and the narrow detection result/fixture types needed by the facade.
- `BeautySDK/Sources/BeautySDK/BeautyEngineGeometryDetection.swift` with still-image detection gating and selected-face routing helpers.
- `BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift` with `@_spi(Testing)` deterministic fixture symbols such as `SDKTestingFaceDetectionFixture`, `SDKTestingFaceDetectionProvider`, and `BeautyEngine` SPI initializer; fixtures expose states, counts, and reason codes, not raw landmarks or coordinates.
- `BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift` covering geometry-triggered detection, no-detection compatibility, disabled tracking, no-face, low-confidence, missing-landmark, detector failure, selected-face routing, and redaction.
- Existing `BeautyEngineMetadataCompatibilityTests` updated only where names/assertions mention pre-detector assumptions.
- Phase 26 evidence docs are produced by Plan 26-03 after Plans 26-01 and 26-02 pass.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-geometry-facade-and-landmark-routing-foundation/26-CONTEXT.md
@.planning/phases/26-geometry-facade-and-landmark-routing-foundation/26-RESEARCH.md
@.planning/phases/26-geometry-facade-and-landmark-routing-foundation/26-VALIDATION.md
@.planning/phases/26-geometry-facade-and-landmark-routing-foundation/26-PATTERNS.md
@.planning/phases/26-geometry-facade-and-landmark-routing-foundation/26-01-SUMMARY.md
@ARCHITECTURE.md
@DESIGN.md
@SECURITY.md
@RELIABILITY.md
@PRODUCT_SENSE.md
@QUALITY_SCORE.md
@PLANS.md
@BeautySDK/Sources/BeautySDK/BeautyEngine.swift
@BeautySDK/Sources/BeautySDK/BeautySDK.swift
@BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift
@BeautySDK/Sources/BeautyDetection/FaceSelectionPolicy.swift
@BeautySDK/Sources/BeautyCore/Models/BeautyDetectionSummary.swift
@BeautySDK/Sources/BeautyCore/Models/BeautyResult.swift
@BeautySDK/Tests/BeautyCoreTests/BeautyEngineTests.swift
@BeautySDK/Tests/BeautyCoreTests/BeautyEngineMetadataCompatibilityTests.swift
@BeautySDK/Tests/BeautyDetectionTests/VisionFaceDetectorTests.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 26-02-01: Add the SPI-only facade detector seam</name>
  <files>BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift, BeautySDK/Sources/BeautySDK/BeautyEngine.swift, BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift, BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift</files>
  <read_first>
    - BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift
    - BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift
    - BeautySDK/Sources/BeautySDK/BeautyEngine.swift
    - BeautySDK/Sources/BeautySDK/BeautySDK.swift
    - BeautySDK/Tests/BeautyCoreTests/BeautyEngineTests.swift
    - BeautySDK/Tests/BeautyDetectionTests/VisionFaceDetectorTests.swift
    - .planning/phases/26-geometry-facade-and-landmark-routing-foundation/26-CONTEXT.md
    - .planning/phases/26-geometry-facade-and-landmark-routing-foundation/26-PATTERNS.md
    - SECURITY.md
    - ARCHITECTURE.md
  </read_first>
  <behavior>
    - SPI tests can inject usable face, no-face, low-confidence, missing-landmark, detector-unavailable, and detection-timeout states through `BeautySDK` only.
    - The SPI seam can count detector invocations so no-geometry requests prove detection did not run.
    - The SPI seam does not expose `VisionDetectionObservation`, `BeautyFaceObservation`, landmark groups, coordinates, bounds, `FaceGeometry`, provider types, or control points as public API.
  </behavior>
  <action>Change `VisionFaceDetector`, `VisionFaceDetectionResult`, `VisionDetectionObservation`, `VisionFaceDetector.Failure`, its observation provider type, initializer, `detect`, and `resetTracking` only to `package` access where the facade needs them; keep Vision-facing and observation types out of `BeautySDK.swift` exports, per D-04 and D-06. Add `BeautyEngineTestingSupport.swift` with `@_spi(Testing)` fixture-level API that represents deterministic detector states by enum/counted provider only; the fixture mapper may create package `VisionDetectionObservation` values internally but must not expose raw landmarks, bounding boxes, coordinates, `VNFaceObservation`, or `FaceGeometry` to public or SPI callers. Add or update a `BeautyEngine` SPI initializer that accepts this fixture provider for tests while the public initializer remains unchanged.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyEngineGeometryFacadeTests</automated>
    <automated>swift test --package-path BeautySDK --filter BeautyDetectionTests.VisionFaceDetectorTests</automated>
    <automated>rg -n "public .*VisionDetectionObservation|public .*VisionFaceDetector|public .*BeautyFaceObservation|@_spi\\(Testing\\).*VisionDetectionObservation|@_spi\\(Testing\\).*BeautyFaceObservation|@_spi\\(Testing\\).*FaceGeometry" BeautySDK/Sources/BeautySDK BeautySDK/Sources/BeautyDetection; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - Public API remains unchanged; deterministic facade tests use only `@_spi(Testing) import BeautySDK`, covering D-04 and D-11.
    - SPI fixtures expose state names/counting only, not raw geometry payloads, covering D-06, D-13, and GEO-02.
    - Existing detector tests still pass after package access changes.
  </acceptance_criteria>
  <done>The facade has a deterministic, SPI-only detector seam and package-only detector access without public raw geometry exposure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 26-02-02: Gate still-image detection and route selected faces through BeautyEngine</name>
  <files>BeautySDK/Sources/BeautySDK/BeautyEngine.swift, BeautySDK/Sources/BeautySDK/BeautyEngineGeometryDetection.swift, BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift, BeautySDK/Tests/BeautyCoreTests/BeautyEngineMetadataCompatibilityTests.swift</files>
  <read_first>
    - BeautySDK/Sources/BeautySDK/BeautyEngine.swift
    - BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
    - BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift
    - BeautySDK/Sources/BeautyCore/Models/BeautyDetectionSummary.swift
    - BeautySDK/Tests/BeautyCoreTests/BeautyEngineMetadataCompatibilityTests.swift
    - BeautySDK/Tests/BeautyCoreTests/BeautyEngineTests.swift
    - .planning/phases/26-geometry-facade-and-landmark-routing-foundation/26-CONTEXT.md
    - .planning/phases/26-geometry-facade-and-landmark-routing-foundation/26-VALIDATION.md
    - RELIABILITY.md
    - PRODUCT_SENSE.md
  </read_first>
  <behavior>
    - Geometry-triggering still-image parameters run detection once, route `observations.first` into resolver planning, and return `.usable` or degraded detection summaries.
    - No-op, color-only, filter-only, and basic-skin-only still-image requests do not call the detector and keep `.notRun` when tracking is enabled.
    - `enableFaceTracking == false` returns `.disabled` and avoids detector work while geometry-triggered face-dependent domains degrade safely.
    - No-face, low-confidence, missing-landmark, detector-unavailable, and timeout states return output with safe color/filter work active, face-dependent domains skipped, redacted warnings, and numeric aggregate metrics.
  </behavior>
  <action>In `BeautyEngine.processResult(image:metadata:parameters:)`, validate image extent and `BeautySDKResources.validate(parameters:)` before any detection work, then use `BeautyEffectResolver.requiresFaceGeometry(parameters:)` to decide whether still-image detection is needed, per D-01. If geometry is not required, preserve the current resolver overload and `initialDetectionSummary` behavior exactly, per D-03. If geometry is required and face tracking is enabled, call the package `VisionFaceDetector` with the request metadata, image extent, and configuration; route only `result.observations.first` into `BeautyEffectResolver.resolve(parameters:selectedFaceObservation:)`, per D-05 and D-09. If geometry is required but no usable selected face exists, pass nil through the selected-face resolver entry so face-dependent domains degrade and safe color/filter work continues, per D-02 and D-08. If face tracking is disabled, do not call the detector; return `.disabled` and the explicit no-face resolver behavior for geometry-triggered requests. Keep `processResult(pixelBuffer:metadata:parameters:)` behavior unchanged in Phase 26. Do not add `BeautyExampleRenderer` cases, generated PNG claims, Demo UI work, or public raw geometry API, per D-10, D-12, and D-13.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyEngineGeometryFacadeTests</automated>
    <automated>swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyEngineMetadataCompatibilityTests</automated>
    <automated>rg -n "VNFaceObservation|boundingBox|controlPoint|/private/var|NSError|AVError|rawPresetJson|raw JSON|image bytes|SIMD|landmarks=|landmarkCoordinates|rawLandmark" BeautySDK/Sources/BeautyCore BeautySDK/Sources/BeautySDK; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - GEO-01 is demonstrated through public `BeautyEngine.processResult(image:metadata:parameters:)` tests, not only internal effects tests.
    - No-geometry compatibility, disabled tracking, and detector-call count assertions cover D-01 and D-03.
    - Geometry-triggered degradation states return processed output, redacted `BeautyDetectionSummary`, warnings, and numeric metrics without raw geometry leakage, covering D-02, D-13, D-14, and GEO-02.
    - The pixel-buffer metadata compatibility tests still pass and no realtime/API expansion is introduced.
  </acceptance_criteria>
  <done>Public still-image processing activates geometry intent only when required, routes one selected face into resolver planning, and preserves compatibility/redaction boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host app -> `BeautyEngine.processResult(image:metadata:parameters:)` | Untrusted image metadata and parameters enter the public facade. |
| Facade -> Detection | The facade conditionally invokes package-only detector code. |
| Facade -> Effects | Selected package-only observation crosses into resolver planning from Plan 26-01. |
| SPI tests -> Facade | Test-only fixture states enter the engine without becoming public API. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02-01 | Denial of Service | Still-image detection gate | mitigate | Gate detection on `BeautyEffectResolver.requiresFaceGeometry(parameters:)`; add detector-call-count tests proving no-op/color/filter/basic-skin paths do not invoke detection. |
| T-26-02-02 | Information Disclosure | Detector failures and public summaries | mitigate | Map detector errors to `BeautyDetectionSummary` reason codes only; tests cover detector-unavailable and timeout without raw framework errors or paths. |
| T-26-02-03 | Information Disclosure | Public `BeautyResult` warnings and metrics | mitigate | Assert warnings/metric keys/detection reasons exclude raw landmark, bounding, coordinate, control-point, path, raw framework error, raw JSON, and image-byte tokens. |
| T-26-02-04 | Elevation of Privilege | SPI/test detector seam | mitigate | Expose only fixture states and counted providers under `@_spi(Testing)`; scan for SPI aliases of raw detector observations, `FaceGeometry`, and provider types. |
| T-26-02-05 | Integrity | Phase scope boundaries | mitigate | Tests and scans forbid Demo UI changes, renderer geometry cases, saved-output claims, and `SHAPE_FEATURE_LEDGER.md` implementation status edits in this phase. |
</threat_model>

<verification>
Plan-level checks:
- `swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyEngineGeometryFacadeTests`
- `swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyEngineMetadataCompatibilityTests`
- `swift test --package-path BeautySDK --filter BeautyDetectionTests.VisionFaceDetectorTests`
- Public/SPI raw geometry export scan over `BeautySDK/Sources/BeautySDK` and `BeautySDK/Sources/BeautyDetection`
- `git diff --check -- BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift BeautySDK/Sources/BeautySDK/BeautyEngine.swift BeautySDK/Sources/BeautySDK/BeautyEngineGeometryDetection.swift BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift BeautySDK/Tests/BeautyCoreTests/BeautyEngineMetadataCompatibilityTests.swift`
</verification>

<success_criteria>
GEO-01 and GEO-02 are satisfied at the facade layer: public still-image processing activates geometry intent from detection only when required, routes one selected face into resolver planning, degrades safely when detection is unavailable or incomplete, and exposes only redacted summaries/warnings/metrics.
</success_criteria>

<output>
Create `.planning/phases/26-geometry-facade-and-landmark-routing-foundation/26-02-SUMMARY.md` when done.
</output>
