---
phase: 28-face-shape-slice-completion-and-documentation-closeout
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift
autonomous: true
requirements:
  - FACE-01
  - FACE-02
  - FACE-03
  - FACE-04
  - FACE-05
  - FACE-06
user_setup: []
must_haves:
  truths:
    - "Focused tests prove caps and normal behavior for `faceSlim`, `faceSmall`, `faceVShape`, `jawSlim`, and signed `chinLength` per D-06, D-08, and D-09."
    - "Focused tests prove no-face and missing-contour degradation plus combined weakening without requiring renderer PNGs for every degradation variant per D-09 and D-10."
    - "`下颌角` and alias-backed `下颌线` share `jawSlim` safety/degradation evidence per D-01, D-02, D-03, and D-04."
  artifacts:
    - path: "BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift"
      provides: "Provider-level caps, contour, and signed chin evidence for scoped face-shape parameters"
    - path: "BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift"
      provides: "No-face, cap, weakening, and redacted metric evidence for combined plans"
    - path: "BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift"
      provides: "Face-shape conflict resolution and redacted combined weakening evidence"
  key_links:
    - from: "BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift"
      to: "BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift"
      via: "cap assertions for `faceSlim`, `faceSmall`, `faceVShape`, `jawSlim`, and `chinLength`"
      pattern: "BeautySafetyCaps\\.(faceSlim|faceSmall|faceVShape|jawSlim|chinLength)"
    - from: "BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift"
      to: "BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift"
      via: "no-face and combined parameter resolution"
      pattern: "BeautyEffectResolver\\.resolve"
---

<objective>
Close focused safety, degradation, cap, signed-chin, combined-weakening, and redaction evidence for the scoped `脸型` parameters.

Purpose: satisfy the non-renderer evidence bar for FACE-01 through FACE-06 while keeping degradation evidence in deterministic XCTest/scans as locked by D-09 and D-10.
Output: focused SwiftPM tests and scans that prove behavior does not leak raw geometry or overstate `下颌线`.
</objective>

## Artifacts This Phase Produces

- Provider test coverage in `FaceShapeWarpProviderTests` for `faceSlim`, `faceSmall`, `faceVShape`, `jawSlim`, positive `chinLength`, negative `chinLength`, caps, missing contour, and alias-shared jaw evidence.
- Combined safety test coverage in `CombinedEffectSafetyTests` for no-face degradation, cap warnings, combined weakening, redacted warning/metric metadata, and scoped face-shape parameter participation.
- Resolver test coverage in `GeometryConflictResolverTests` for high face-shape strengths, `chinLength` signed cap behavior, `jawSlim` alias evidence, and no raw geometry metadata.
- Focused raw-leak scan command over active Swift source and touched tests.
- Focused hidden-public-API expansion scan command over public SDK source and tests.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/28-face-shape-slice-completion-and-documentation-closeout/28-CONTEXT.md
@.planning/phases/28-face-shape-slice-completion-and-documentation-closeout/28-RESEARCH.md
@.planning/phases/28-face-shape-slice-completion-and-documentation-closeout/28-VALIDATION.md
@.planning/phases/28-face-shape-slice-completion-and-documentation-closeout/28-PATTERNS.md
@BeautySDK/Sources/BeautyEffects/Warp/FaceShapeWarpProvider.swift
@BeautySDK/Sources/BeautyEffects/Warp/ChinWarpProvider.swift
@BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
@BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift
@BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift
@BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift
@BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift
@SECURITY.md
@RELIABILITY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 28-02-01: Strengthen per-parameter provider, cap, and alias tests</name>
  <files>BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift</files>
  <read_first>
    - `BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift`
    - `BeautySDK/Sources/BeautyEffects/Warp/FaceShapeWarpProvider.swift`
    - `BeautySDK/Sources/BeautyEffects/Warp/ChinWarpProvider.swift`
    - `BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift`
    - `.planning/phases/28-face-shape-slice-completion-and-documentation-closeout/28-CONTEXT.md`
  </read_first>
  <behavior>
    - Test 1: Existing `faceSlim`, `faceSmall`, `faceVShape`, `jawSlim`, and `chinLength` provider tests still pass with cap assertions.
    - Test 2: A new or strengthened assertion proves `jawSlim` is the only provider evidence used for both `下颌角` and alias-backed `下颌线`; no `jawLine` provider or parameter is referenced.
    - Test 3: Signed `chinLength` asserts positive and negative target movement and both strengths are capped by `BeautySafetyCaps.chinLength`.
    - Test 4: Missing contour returns no face-shape points and `missing_face_contour` without raw point payloads.
  </behavior>
  <action>Review `FaceShapeWarpProviderTests` against D-01 through D-09. Keep or strengthen existing tests so they explicitly cover `faceSlim`, `faceSmall`, `faceVShape`, `jawSlim`, positive `chinLength`, negative `chinLength`, cap assertions, deterministic output, and missing-contour degradation. Add a named test such as `testJawSlimEvidenceCoversJawAngleAndAliasBackedJawline` if current assertions do not make the alias evidence explicit. Use English `JawlineAlias` naming in the test and assert the source contains no `jawLine` or separate jawline provider token; the Chinese alias wording is recorded in docs and verification by Plan 28-04 per D-01 through D-04. Do not modify production providers unless a test exposes a real implementation defect; Phase 28 is not a new algorithm phase per D-05 and D-12.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.FaceShapeWarpProviderTests</automated>
    <automated>rg -n "jawLine|JawLine|faceLine|FaceLine|下颌线" BeautySDK/Sources/BeautyEffects BeautySDK/Sources/BeautySDK BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift; test $? -eq 1</automated>
    <automated>rg -n "BeautySafetyCaps\\.(faceSlim|faceSmall|faceVShape|jawSlim|chinLength)|missing_face_contour|chinLength: -1|chinLength: 1" BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift</automated>
    <automated>git diff --check -- BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - Focused provider tests cover all five distinct SDK face-shape parameters and both `chinLength` signs.
    - `jawSlim` is the only jawline-related provider evidence and is explicitly tied to both `下颌角` and alias-backed `下颌线`.
    - Missing contour degradation produces no points and no raw geometry payloads.
    - No production public API or distinct jawline algorithm is added.
  </acceptance_criteria>
  <done>Provider-level Phase 28 behavior, caps, signed chin, and alias evidence are explicit and passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 28-02-02: Strengthen resolver no-face, combined weakening, and redaction evidence</name>
  <files>BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift, BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift</files>
  <read_first>
    - `BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift`
    - `BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift`
    - `BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift`
    - `BeautySDK/Sources/BeautyEffects/Planning/GeometryConflictResolver.swift`
    - `BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift`
    - `SECURITY.md`
    - `RELIABILITY.md`
  </read_first>
  <behavior>
    - Test 1: No-face resolution with scoped face-shape parameters skips `.faceShape`, preserves safe non-face domains, and emits `face_effects_skipped_no_face`.
    - Test 2: High combined strengths cap `faceSlim`, `faceSmall`, `faceVShape`, `jawSlim`, and signed `chinLength`, and produce `combined_geometry_weakened`.
    - Test 3: Warning and metric metadata contain only redacted codes and aggregate metric keys.
    - Test 4: Active source and touched tests expose no public/SPI raw geometry, bounding box, landmark coordinate, Vision object, local path, raw framework error, raw JSON, image bytes, or `SIMD` payload.
  </behavior>
  <action>Update `CombinedEffectSafetyTests` and `GeometryConflictResolverTests` only where current assertions do not explicitly meet D-09. Ensure no-face degradation covers `BeautyParameters(faceSlim: 1, faceSmall: 1, faceVShape: 1, jawSlim: 1, chinLength: 1)` with `faceGeometry: nil`, skipped `.faceShape`, warning code `face_effects_skipped_no_face`, and redacted metadata. Ensure combined weakening and cap evidence includes all five distinct face-shape parameters and verifies `beauty.effects.cappedCount`, `beauty.effects.weakenedCount`, and `beauty.effects.geometryStrengthScale` without raw coordinate details. Add or strengthen redaction assertions using the existing forbidden token style. Do not add renderer PNG degradation variants for every tool, because D-10 assigns those to focused XCTest/scans rather than per-tool no-face output.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.CombinedEffectSafetyTests</automated>
    <automated>swift test --package-path BeautySDK --filter BeautyEffectsTests.GeometryConflictResolverTests</automated>
    <automated>rg -n "face_effects_skipped_no_face|combined_geometry_weakened|beauty\\.effects\\.(cappedCount|weakenedCount|geometryStrengthScale)|faceSmall|faceVShape|jawSlim|chinLength" BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift</automated>
    <automated>rg -n "public .*FaceGeometry|public .*BeautyFaceObservation|public .*landmark|public .*bounding|@_spi.*FaceGeometry|@_spi.*BeautyFaceObservation|VNFaceObservation\\(|boundingBox:|controlPoint\\(|landmarkCoordinates:|rawLandmark:|/private/var|NSError|AVError|raw JSON:|image bytes:|SIMD[0-9]?\\(" BeautySDK/Sources/BeautySDK BeautySDK/Sources/BeautyDetection BeautySDK/Sources/BeautyEffects BeautySDK/Tests/BeautyEffectsTests; test $? -eq 1</automated>
    <automated>git diff --check -- BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - Resolver and conflict tests cover caps, no-face degradation, signed `chinLength`, combined weakening, and redaction for the scoped Phase 28 face-shape parameters.
    - Tests and active source expose no public raw geometry, local path, raw framework error, raw JSON, image bytes, or raw point payload.
    - No renderer degradation matrix is added beyond the existing shared no-face fixture path.
    - No hidden public API expansion, Demo import, or entitlement/pro behavior appears.
  </acceptance_criteria>
  <done>Focused safety/degradation/redaction tests are strong enough to support Phase 28 evidence closeout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public parameters -> internal resolver | Untrusted caller-controlled strengths are normalized, capped, and degraded internally. |
| Internal geometry -> public warnings/metrics | Resolver warnings and metrics may surface through public results and must remain redacted. |
| Alias documentation -> SDK behavior | `下颌线` is a documentation/status alias of `jawSlim`, not a new behavior surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02-01 | Information Disclosure | Resolver/provider tests and active source | mitigate | Raw-leak scans over public SDK, detection/effects source, and touched tests forbid raw geometry, Vision objects, paths, raw JSON, image bytes, and point payloads. |
| T-28-02-02 | Spoofing / Repudiation | Safety/degradation evidence | mitigate | Focused XCTest assertions name caps, no-face degradation, signed `chinLength`, combined weakening, and redacted metric keys instead of relying on visual claims. |
| T-28-02-06 | Spoofing / Repudiation | Renderer/status evidence dependency | mitigate | Any status evidence this plan cites must rely on `check_face_shape_renderer_outputs.py` reporting `30/30` top-region geometry-vs-`geometryBaseline_noop` comparisons above the watermark band, not full watermarked PNG differences. |
| T-28-02-03 | Information Disclosure / Repudiation | Generated PNG artifacts | mitigate | This plan creates no PNGs; generated-output policy is still verified in Plan 28-01 and final Plan 28-03/28-04 gates. |
| T-28-02-04 | Tampering | Hidden public API expansion | mitigate | Tests and scans forbid `jawLine`, separate jawline providers, public raw geometry exports, Demo behavior, and entitlement/pro tokens. |
| T-28-02-05 | Spoofing / Repudiation | Test names and comments | mitigate | Tests may describe scoped evidence and alias sharing only; no UI, commercial quality, device parity, broad Meitu parity, or release-readiness claims are allowed. |
| T-28-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install is allowed; verification uses existing SwiftPM tests and `rg` scans only. |
</threat_model>

<verification>
Overall Plan 28-02 checks:

- `swift test --package-path BeautySDK --filter BeautyEffectsTests.FaceShapeWarpProviderTests`
- `swift test --package-path BeautySDK --filter BeautyEffectsTests.CombinedEffectSafetyTests`
- `swift test --package-path BeautySDK --filter BeautyEffectsTests.GeometryConflictResolverTests`
- Alias, hidden-public-API, active-source raw-leak, and scoped `git diff --check` scans.
</verification>

<success_criteria>
Plan 28-02 succeeds when focused XCTest evidence covers caps, missing contour/no-face degradation, signed `chinLength`, combined weakening, redaction, no raw geometry leakage, and `jawSlim` alias sharing for `下颌角` plus `下颌线`.
</success_criteria>

<output>
Create `.planning/phases/28-face-shape-slice-completion-and-documentation-closeout/28-02-SUMMARY.md` when done.
</output>
