---
phase: 27-geometry-render-output-and-verification-harness
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift
  - BeautySDK/Sources/BeautySDK/BeautyEngineGeometryDetection.swift
  - BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift
  - BeautySDK/Tests/BeautyDetectionTests/VisionFaceDetectorTests.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift
autonomous: true
requirements:
  - GEO-03
  - GEO-04
requirements_addressed:
  - GEO-03
  - GEO-04
user_setup: []
must_haves:
  truths:
    - "Existing example-image fixtures are tried through real public-facade still-image detection before any fallback path, per D-03 and D-04."
    - "Geometry-triggering still-image requests can return usable, no-face, partial, skipped, disabled, or notRun summaries without raw geometry exposure, per D-13 and D-17."
    - "Detection evidence remains redacted to summaries, warning codes, reason codes, counts, and aggregate metrics only, per D-09 and D-17."
  artifacts:
    - path: "BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift"
      provides: "Still-image Vision observation provider that accepts CIImage input without public raw geometry output"
      contains: "VisionFaceDetectionInput"
    - path: "BeautySDK/Sources/BeautySDK/BeautyEngineGeometryDetection.swift"
      provides: "Still-image geometry route that can pass image input into detection"
      contains: "resolveStillImageGeometry"
    - path: "BeautySDK/Tests/BeautyDetectionTests/VisionFaceDetectorTests.swift"
      provides: "Real still-image detection and redacted degradation tests"
    - path: "BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift"
      provides: "Facade-level real fixture probe and redaction coverage"
  key_links:
    - from: "BeautySDK/Sources/BeautySDK/BeautyEngineGeometryDetection.swift"
      to: "BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift"
      via: "detect(image:metadata:imageExtent:previewExtent:configuration:)"
      pattern: "detect\\(image:"
    - from: "BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift"
      to: "example-images/input"
      via: "real public-facade fixture probe"
      pattern: "e[1-5]\\.png"
---

<objective>
Make the Phase 27 primary proof path possible by letting the public still-image facade try real example-image detection before renderer output evidence is generated.

Purpose: satisfy the D-03/D-04 real-facade-first gate for GEO-03 and preserve GEO-04 redacted degradation behavior when real detection produces no-face, partial, skipped, disabled, or usable summaries.
Output: a package-internal still-image Vision input seam, updated SPI fixtures, and focused tests proving real fixture probing and redaction boundaries.
</objective>

## Artifacts This Phase Produces

- `VisionFaceDetectionInput` in `BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift`, package-only, carrying `BeautyInputMetadata`, `imageExtent`, and optional still-image data for detection.
- `VisionFaceDetector.detect(image:metadata:imageExtent:previewExtent:configuration:)`, package-only, used by the still-image facade path.
- Updated `VisionFaceDetector.detect(metadata:imageExtent:previewExtent:configuration:)` compatibility overload that delegates with no image and preserves existing test behavior.
- Updated `VisionFaceDetector.defaultObservationProvider` that uses `VNDetectFaceLandmarksRequest` for CIImage-backed still images and emits `VisionDetectionObservation` values with bounds and landmark-group availability only.
- Updated `SDKTestingFaceDetectionProvider.makeObservationProvider()` closure signature with no new public raw geometry, Vision observation, bounds, or landmark API.
- Test method `VisionFaceDetectorTests.testDefaultStillImageProviderReturnsRedactedNoFaceForNoFaceFixture`.
- Test method `VisionFaceDetectorTests.testDefaultStillImageProviderMapsUsableFaceWithoutRawPayload`.
- Test method `BeautyEngineGeometryFacadeTests.testExistingExampleImageFixtureProducesUsableFaceForGeometryCase`.
- Test method `BeautyEngineGeometryFacadeTests.testRealDetectionMetadataStaysRedactedForGeometryCase`.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/27-geometry-render-output-and-verification-harness/27-CONTEXT.md
@.planning/phases/27-geometry-render-output-and-verification-harness/27-RESEARCH.md
@.planning/phases/27-geometry-render-output-and-verification-harness/27-VALIDATION.md
@.planning/phases/27-geometry-render-output-and-verification-harness/27-PATTERNS.md
@ARCHITECTURE.md
@DESIGN.md
@SECURITY.md
@RELIABILITY.md
@PRODUCT_SENSE.md
@QUALITY_SCORE.md
@BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift
@BeautySDK/Sources/BeautySDK/BeautyEngineGeometryDetection.swift
@BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift
@BeautySDK/Tests/BeautyDetectionTests/VisionFaceDetectorTests.swift
@BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift
@example-images/input
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 27-01-01: Add the still-image Vision input seam for real fixture detection</name>
  <files>BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift, BeautySDK/Sources/BeautySDK/BeautyEngineGeometryDetection.swift, BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift, BeautySDK/Tests/BeautyDetectionTests/VisionFaceDetectorTests.swift</files>
  <read_first>
    - `BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift`
    - `BeautySDK/Sources/BeautySDK/BeautyEngineGeometryDetection.swift`
    - `BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift`
    - `BeautySDK/Tests/BeautyDetectionTests/VisionFaceDetectorTests.swift`
    - `DESIGN.md` sections 4.7, 5.1, and 6
    - `SECURITY.md` sections 9 and Phase 26 geometry facade evidence
  </read_first>
  <behavior>
    - Test 1: CIImage-backed no-face fixture returns `.noFace` with `.noFaceDetected` and zero selected observations.
    - Test 2: CIImage-backed portrait fixture can return `.usable` with counts only and no raw Vision, bounds, coordinate, or landmark payload in public strings.
    - Test 3: Existing SPI fixtures still produce usable, no-face, low-confidence, missing-landmark, unavailable, and timeout states.
  </behavior>
  <action>Add package-only `VisionFaceDetectionInput` and update `VisionFaceDetector.ObservationProvider` so the default provider receives still-image data for D-03. Add `detect(image:metadata:imageExtent:previewExtent:configuration:)` while keeping the existing metadata-only detect overload for compatibility. Implement `defaultObservationProvider` with `VNDetectFaceLandmarksRequest` over the supplied CIImage and map each `VNFaceObservation` to `VisionDetectionObservation` using confidence, normalized area, `CoordinateRect` bounds, and landmark-group availability only; do not store or expose raw landmark points, Vision objects, local paths, raw framework errors, or image bytes per D-17. Update `BeautyEngineGeometryDetection.resolveStillImageGeometry` to call the image-aware detect entry, and update the SPI fixture provider closure to accept the new input object without expanding public API per D-04.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter BeautyDetectionTests.VisionFaceDetectorTests</automated>
    <automated>swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyEngineGeometryFacadeTests/testGeometryTriggeredDetectionDegradesAndKeepsSafeDomainsActive</automated>
    <automated>rg -n "public .*VisionDetectionObservation|public .*VisionFaceDetector|public .*BeautyFaceObservation|@_spi\\(Testing\\).*VisionDetectionObservation|@_spi\\(Testing\\).*BeautyFaceObservation|@_spi\\(Testing\\).*FaceGeometry" BeautySDK/Sources/BeautySDK BeautySDK/Sources/BeautyDetection BeautySDK/Sources/BeautyEffects; test $? -eq 1</automated>
    <automated>git diff --check -- BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift BeautySDK/Sources/BeautySDK/BeautyEngineGeometryDetection.swift BeautySDK/Sources/BeautySDK/BeautyEngineTestingSupport.swift BeautySDK/Tests/BeautyDetectionTests/VisionFaceDetectorTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - Source contains package-only `VisionFaceDetectionInput` and `detect(image:metadata:imageExtent:previewExtent:configuration:)`; no `public` or `@_spi(Testing)` raw Vision observation, raw face observation, `FaceGeometry`, or landmark payload export is added.
    - Default still-image detection returns redacted summary state from CIImage input and preserves `.disabled`, `.noFace`, `.partial`, `.lowConfidence`, `.skipped`, and `.usable` semantics.
    - SPI fixture tests still pass after the observation provider signature change.
    - Active public/SPI export scan returns no matches.
  </acceptance_criteria>
  <done>Real still-image detection is available to the public facade path with only redacted summaries and package-internal observations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 27-01-02: Prove existing portrait fixtures are tried through the public facade</name>
  <files>BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift</files>
  <read_first>
    - `BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift`
    - `BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift`
    - `.planning/phases/27-geometry-render-output-and-verification-harness/27-CONTEXT.md`
    - `example-images/input/e1.png` through `example-images/input/e5.png`
  </read_first>
  <behavior>
    - Test 1: With `BeautyEngine(configuration: .default)`, at least one existing portrait fixture `e1.png` through `e5.png` returns `.usable`, `usedFaceCount == 1`, and `beauty.detection.geometryRequired == 1` for the exact combined face-shape parameters.
    - Test 2: The real-facade fixture probe uses only `BeautySDK` public initialization and does not use `SDKTestingFaceDetectionProvider`.
    - Test 3: The collected warnings, metrics keys, and detection reasons from the real probe contain none of the forbidden raw geometry or path tokens from D-17.
  </behavior>
  <action>Add `testExistingExampleImageFixtureProducesUsableFaceForGeometryCase` and `testRealDetectionMetadataStaysRedactedForGeometryCase` to `BeautyEngineGeometryFacadeTests`. The combined face-shape parameters must be exactly `faceSlim: 0.35`, `faceSmall: 0.30`, `faceVShape: 0.35`, `jawSlim: 0.30`, and `chinLength: 0.20` per D-06. The test may iterate `e1.png` through `e5.png` and pass when at least one fixture produces usable public-facade detection; if none do, fix the default still-image provider instead of replacing this primary proof path with SPI-only evidence per D-03 and D-04.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyEngineGeometryFacadeTests/testExistingExampleImageFixtureProducesUsableFaceForGeometryCase</automated>
    <automated>swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyEngineGeometryFacadeTests/testRealDetectionMetadataStaysRedactedForGeometryCase</automated>
    <automated>rg -n "SDKTestingFaceDetectionProvider" BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift | rg -n "testExistingExampleImageFixtureProducesUsableFaceForGeometryCase|testRealDetectionMetadataStaysRedactedForGeometryCase"; test $? -eq 1</automated>
    <automated>git diff --check -- BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - At least one existing `example-images/input/e*.png` portrait fixture reaches `.usable` through a default `BeautyEngine` for the D-06 face-shape combination.
    - The real-facade fixture probe records only availability, reason codes, counts, warning codes/messages, and aggregate metric keys; it does not expose coordinates, landmarks, bounding boxes, control points, raw Vision/framework errors, raw JSON, local paths, or image bytes.
    - GEO-03 primary proof remains real-facade-first; no fallback verifier is created in this plan.
  </acceptance_criteria>
  <done>The existing portrait fixtures have a command-backed public-facade detection probe for the Phase 27 geometry case.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CIImage fixture -> Vision detection | Local image data enters Vision; only mapped package observations and public redacted summaries may leave detection. |
| BeautyDetection -> BeautySDK facade | Package observation values cross into facade routing; public results must remain geometry-free. |
| Test evidence -> repository Markdown | Command output and assertions may be copied into evidence; raw payloads and local paths must be excluded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01-01 | Information Disclosure | `VisionFaceDetector.defaultObservationProvider` | mitigate | Map Vision observations to confidence, count, normalized area, bounds for internal mapping, and landmark-group availability only; add redaction assertions and public/SPI export scan. |
| T-27-01-02 | Information Disclosure | `BeautyEngineGeometryFacadeTests` real fixture probe | mitigate | Assert forbidden tokens for coordinates, landmarks, bounding boxes, control points, Vision/framework errors, raw JSON, local paths, and image bytes are absent from warnings, metrics, and summary reasons per D-17. |
| T-27-01-03 | Tampering | Public/SPI SDK boundary | mitigate | Run public/SPI raw geometry export scan and keep all new detection symbols package-only. |
| T-27-01-04 | Repudiation | Real fixture detection evidence | mitigate | Require the test to use default `BeautyEngine` and existing `e1` through `e5` fixtures, not `SDKTestingFaceDetectionProvider`, so D-03 evidence cannot be relabeled SPI-only proof. |
| T-27-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install is allowed in this plan; use SwiftPM, XCTest, Apple frameworks, `rg`, and git only. |
</threat_model>

<verification>
Overall Plan 27-01 checks:

- `swift test --package-path BeautySDK --filter BeautyDetectionTests.VisionFaceDetectorTests`
- `swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyEngineGeometryFacadeTests`
- Public/SPI raw geometry export scan over `BeautySDK/Sources/BeautySDK`, `BeautySDK/Sources/BeautyDetection`, and `BeautySDK/Sources/BeautyEffects`.
- Active-source raw-leak scan over public facade/Core and test evidence strings for raw Vision objects, coordinates, landmarks, bounds, control points, raw framework errors, local paths, raw JSON, and image bytes.
- Scoped `git diff --check` for all files in `files_modified`.
</verification>

<success_criteria>
Plan 27-01 succeeds when real still-image detection can be exercised by the public facade against the existing portrait fixtures, while every public result and evidence string remains redacted and no fallback verifier or public raw geometry API is added.
</success_criteria>

<output>
Create `.planning/phases/27-geometry-render-output-and-verification-harness/27-01-SUMMARY.md` when done.
</output>
