---
phase: 36-public-facade-output-evidence
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - BeautySDK/Sources/BeautyExampleRenderer/main.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift
autonomous: true
requirements: [NOSE-07, NOSE-09]
must_haves:
  truths:
    - "The renderer has exactly 36 discovered public-facade cases after adding only noseRootNarrowing_0p25 and noseTipLift_0p25 at exact 0.25."
    - "Each of seven nose cases contains exactly one of six public nose fields; aliases, combo cases, package-internal geometry imports, and second render paths remain absent."
    - "Representative no-face facade evidence for each new isolated field preserves extent, reports no face and zero used faces, retains aggregate-only metrics/warnings, and stays redacted."
  artifacts:
    - path: BeautySDK/Sources/BeautyExampleRenderer/main.swift
      provides: two isolated public cases
    - path: BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift
      provides: exact inventory, isolation, facade boundary, and no-face regressions
  key_links:
    - from: BeautySDK/Sources/BeautyExampleRenderer/main.swift
      to: BeautyEngine.processResult
      via: the existing shared public facade loop
      pattern: "noseRootNarrowing_0p25|noseTipLift_0p25"
---

<objective>
Extend the public-facade renderer contract with exactly the two Phase 36 cases and lock representative no-face degradation before generating artifacts.
</objective>

## Artifacts this phase produces

- `RenderCase` entries `noseRootNarrowing_0p25` and `noseTipLift_0p25`.
- Requirement-named XCTest methods/helpers in the existing renderer regression suite; no new production type, dependency, target, raw geometry API, or generated file.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/REQUIREMENTS.md
@.planning/phases/36-public-facade-output-evidence/36-CONTEXT.md
@.planning/phases/36-public-facade-output-evidence/36-RESEARCH.md
@.planning/phases/36-public-facade-output-evidence/36-PATTERNS.md
@BeautySDK/Sources/BeautyExampleRenderer/main.swift
@BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift
</context>

<tasks>
<task type="auto">
  <name>Task 36-01-01: Add and freeze the exact two isolated renderer cases</name>
  <files>BeautySDK/Sources/BeautyExampleRenderer/main.swift, BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift</files>
  <read_first>
    - `AGENTS.md` and `PLANS.md`
    - `.planning/REQUIREMENTS.md` — NOSE-07 exact contract
    - `36-CONTEXT.md`, `36-RESEARCH.md`, and `36-PATTERNS.md` — locked IDs, values, facade boundary, and stale `noseRoot` substring hazard
    - `BeautySDK/Sources/BeautyExampleRenderer/main.swift` — modified source and actual 34-case baseline
    - `BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift` — modified exact-inventory owner
  </read_first>
  <action>Add exactly `RenderCase(id: "noseRootNarrowing_0p25", displayName: "noseRootNarrowing 0.25", parameters: BeautyParameters(noseRootNarrowing: 0.25))` and `RenderCase(id: "noseTipLift_0p25", displayName: "noseTipLift 0.25", parameters: BeautyParameters(noseTipLift: 0.25))` beside existing nose cases. Keep the single `BeautyEngine.processResult` loop and `import BeautySDK` boundary. Update the regression suite's ordered inventory from observed source to exactly 36. Define exact seven nose cases across six public field labels (signed tip size has two cases), assert one field per case and the two exact 0.25 values, and replace broad `noseRoot` substring rejection with exact alias/case/label guards that allow `noseRootNarrowing`. Reject combo, `noseRoot`, `noseLift`, `shanGen`, and `tiSheng` aliases without touching package-internal source.</action>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyRendererOutputRegressionTests &amp;&amp; test "$(rg -n 'RenderCase\(' BeautySDK/Sources/BeautyExampleRenderer/main.swift | wc -l | tr -d ' ')" -eq 36 &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>
    - Renderer discovery and XCTest both report exactly 36 case entries, with each new ID exactly once.
    - Each new snippet has only its matching public field at exact `0.25`; seven nose cases collectively cover six fields without aliases or combos.
    - Renderer retains only the public `BeautySDK` import and shared facade processing path.
  </acceptance_criteria>
  <done>The source-level public renderer contract is exact and independently testable.</done>
</task>

<task type="auto">
  <name>Task 36-01-02: Lock representative no-face public-facade behavior</name>
  <files>BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift</files>
  <read_first>
    - `36-CONTEXT.md` — representative no-face and Phase 37 boundary
    - `36-RESEARCH.md` — exact result assertions and redaction requirement
    - `BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift` — modified suite and existing face-shape no-face analog
    - `BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift` — existing redacted public result idioms
  </read_first>
  <action>Extend the existing no-face facade test pattern with a loop over `BeautyParameters(noseRootNarrowing: 0.25)` and `BeautyParameters(noseTipLift: 0.25)` using the committed no-face fixture/detector arrangement. For each isolated request assert output extent equals input, availability `.noFace`, reasons `[.noFaceDetected]`, `beauty.detection.geometryRequired == 1`, used-face count zero, the established skipped-no-face category warning, and existing redaction helper success. Do not add missing/stale/reused/provider-empty or all-six-field coverage owned by Phase 37.</action>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyRendererOutputRegressionTests &amp;&amp; git diff --check -- BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift</automated></verify>
  <acceptance_criteria>
    - Both isolated parameters execute nonzero focused XCTest evidence and preserve extent with `.noFace`/`.noFaceDetected` and zero used faces.
    - Diagnostics remain category/aggregate-only and pass the existing redaction assertion.
    - No production geometry, cap, dependency, Demo, ledger, gallery, or generated artifact changes occur.
  </acceptance_criteria>
  <done>Representative no-face facade behavior is locked without expanding into Phase 37 safety scope.</done>
</task>
</tasks>

<threat_model>
  <scope>ASVS L1 public-facade and diagnostic boundary.</scope>
  <blocking_rule>Any unresolved HIGH threat blocks this plan and its summary.</blocking_rule>
  <assets>Exact public case inventory, isolated field identity, raw-geometry privacy, extent preservation, and redacted diagnostics.</assets>
  <trust_boundaries>Fixture images and public parameters enter BeautyEngine; only watermarked PNGs and aggregate public results leave.</trust_boundaries>
  <threats>Alias/combo cases could fake independence; internal imports could bypass the facade; no-face output could expose geometry or unsafe diagnostics.</threats>
  <mitigations>Exact case/field assertions, import/path scans, shared facade loop, extent/summary/warning/metric/redaction XCTest evidence.</mitigations>
  <residual_risk>Saved-output visibility and final cap/six-field safety remain Plans 36-02 and Phase 37.</residual_risk>
</threat_model>

<verification>Run the focused renderer suite, exact 36-case/source scans, facade-import scan, and `git diff --check`; record observed counts in `36-01-SUMMARY.md`.</verification>
<success_criteria>NOSE-07's source contract and NOSE-09's representative facade no-face contract pass with no HIGH threat or Phase 37 scope drift.</success_criteria>
<output>Create `.planning/phases/36-public-facade-output-evidence/36-01-SUMMARY.md`.</output>
