---
phase: 35-public-contract-and-independent-geometry
plan: "03"
type: execute
wave: 3
depends_on:
  - 35-01
  - 35-02
files_modified:
  - BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
  - BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift
autonomous: true
requirements:
  - NOSE-03
  - NOSE-06
must_haves:
  truths:
    - "Each new field independently triggers requiresFaceGeometry, caps to exactly 0.25, activates the nose domain on valid supports, contributes nonzero aggregate geometryPointCount, and produces only redacted warnings/metrics."
    - "Both fields are present in every relevant resolver manual list: cap assignment/capped count, reusable-value detection, nose activation, zeroing, exact reused 0.5 scaling, provider support sanitization, and provider dispatch."
    - "Missing/stale/isolated provider-empty geometry zeros the affected new strengths; reused valid geometry yields exactly 0.125 for each from cap 0.25 times scale 0.5; safe color/filter domains continue."
    - "Both fields participate in GeometryConflictResolver scaling, positive totals, weakened counts, and representative cross-domain weakening without moving the current conflict call sites or claiming Phase 37 once-only behavior."
    - "The public still-image facade invokes detection once for each isolated field, preserves extent, reports usable one-face aggregate evidence, and exposes no support/landmark/control-point payload."
  artifacts:
    - path: "BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift"
      provides: "All routing/cap/freshness/activation/zeroing/scaling/support-sanitization seams for both new fields"
      contains: "noseRootNarrowing"
    - path: "BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift"
      provides: "Conflict scale, total, and weakened-count participation"
      contains: "noseTipLift"
    - path: "BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift"
      provides: "Isolated geometry-required, cap/count, activation, negative-no-op, and redaction evidence"
    - path: "BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift"
      provides: "Missing/stale/reused/provider-empty field-specific degradation evidence"
    - path: "BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift"
      provides: "Direct conflict total/scale/count evidence"
    - path: "BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift"
      provides: "Representative public-parameter cross-domain weakening evidence"
    - path: "BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift"
      provides: "Public facade detector routing and aggregate-only evidence for each isolated new field"
  key_links:
    - from: "BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift"
      to: "BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift"
      via: "normalized public fields are independently capped and routed"
      pattern: "normalized.noseRootNarrowing|normalized.noseTipLift"
    - from: "BeautySDK/Sources/BeautyEffects/Warp/NoseWarpProvider.swift"
      to: "BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift"
      via: "supportAvailability zeros only unsupported new effective fields before aggregate provider dispatch"
      pattern: "supportAvailability"
    - from: "BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift"
      to: "BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift"
      via: "requiresFaceGeometry drives the existing public detector route and aggregate metrics"
      pattern: "geometryRequired|geometryPointCount"
    - from: "BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift"
      to: "BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift"
      via: "representative face/eye/mouth combinations weaken each new nose value without zeroing"
      pattern: "combined_geometry_weakened"
---

<objective>
Route both new values through the existing resolver, degradation, conflict, and public-facade seams with field-specific fail-closed support handling and aggregate-only evidence.

Purpose: turn the public contract and independent provider vectors into complete Phase 35 behavior while preserving current conflict call placement and later-phase boundaries.
Output: complete manual enumerations, focused degradation/conflict tests, and two isolated public-facade routing cases.
</objective>

## Artifacts this phase produces

- Resolver cap assignments for `noseRootNarrowing` and `noseTipLift` using exact `0.25` constants and normal `cappedCount` accounting.
- Both fields added to `requiresFaceGeometry(parameters:)`, `hasReusableNonEyeGeometryValues`, the nose `anyNonZero` guard, `zeroNoseStrengths(_:)`, and `scaleReusableNonEyeGeometryStrengths(_:by:)`.
- Field-specific support sanitization in the nose branch using `NoseWarpProvider.supportAvailability(for:)`: invalid root/tip support zeros only its matching effective strength before provider aggregation, with no legacy alias/fallback.
- Conflict resolver scale assignments, positive-value `geometryTotal(_:)` entries, and `nonZeroFaceShapeFieldCount(_:)` entries for both fields.
- Requirement-named or Phase-35-named tests in the seven existing suite files; no new runtime or test file.
- No new facade signature, raw geometry/result field, conflict call-site move, renderer/gallery output, Demo change, final cap claim, exhaustive once-only claim, ledger promotion, or branch completion.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-public-contract-and-independent-geometry/35-CONTEXT.md
@.planning/phases/35-public-contract-and-independent-geometry/35-RESEARCH.md
@.planning/phases/35-public-contract-and-independent-geometry/35-PATTERNS.md
@.planning/phases/35-public-contract-and-independent-geometry/35-VALIDATION.md
@.planning/phases/35-public-contract-and-independent-geometry/35-01-SUMMARY.md
@.planning/phases/35-public-contract-and-independent-geometry/35-02-SUMMARY.md
@BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
@BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift
@BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
@BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
@BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift
@BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift
@BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 35-03-01: Complete resolver routing, field-specific fail-closed degradation, and focused integration evidence</name>
  <files>BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift, BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift</files>
  <read_first>
    - `AGENTS.md`
    - `.planning/REQUIREMENTS.md` — NOSE-03 and NOSE-06 propagation/fail-closed contract
    - `.planning/phases/35-public-contract-and-independent-geometry/35-CONTEXT.md` — provisional cap, reuse, diagnostics, and Phase 37 boundaries
    - `.planning/phases/35-public-contract-and-independent-geometry/35-RESEARCH.md` — exhaustive resolver hotspot list and validation matrix
    - `.planning/phases/35-public-contract-and-independent-geometry/35-PATTERNS.md` — exact cap/zero/reuse/support-empty test patterns
    - `BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift` — every modified resolver list and current nose branch
    - `BeautySDK/Sources/BeautyEffects/Warp/NoseWarpProvider.swift` — support availability and provider behavior from Plan 35-02
    - `BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift` — effective fields from Plan 35-01
    - `BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift` — exact provisional caps from Plan 35-01
    - `BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift` — modified cap/route suite
    - `BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift` — modified freshness/provider-empty suite
    - `BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift` — read-only valid/malformed fixture source of truth
  </read_first>
  <action>Add `normalized.noseRootNarrowing` and `normalized.noseTipLift` to `requiresFaceGeometry(parameters:)`. Assign effective values with `capUnit(..., cap: BeautySafetyCaps.&lt;field&gt;, cappedCount: &amp;cappedCount)` next to the four existing nose assignments. Add both strengths to `hasReusableNonEyeGeometryValues`, the nose-domain `anyNonZero` guard, `zeroNoseStrengths(_:)`, and `scaleReusableNonEyeGeometryStrengths(_:by:)` so reused valid geometry applies exactly `0.5` and missing/stale paths zero them.

Inside the valid non-stale nose branch, instantiate one `NoseWarpProvider`, obtain `supportAvailability(for: faceGeometry)`, and before `makeControlPoints` set `strengths.noseRootNarrowing = 0` only when it was requested and `rootNarrowing == false`, and likewise set `strengths.noseTipLift = 0` only when requested and `tipLift == false`. Do not copy support points, expose per-point data, use legacy field values as fallbacks, or zero a valid sibling/legacy nose field. Then invoke the same provider with the sanitized strengths. If no valid requested nose strength emits points, preserve the established `.nose` skip, `beauty.effects.skippedNoseDomains = 1`, `nose_inputs_missing`, and complete nose zeroing. If legacy or sibling points remain, the domain may remain active but the unsupported new effective strength must stay exact zero; diagnostics remain category/aggregate-only. Preserve existing stale/no-face behavior and do not move either `GeometryConflictResolver` call site.

Extend `BeautyEffectResolverTests` so each isolated new field: makes `requiresFaceGeometry == true`; caps from public `1` to exact `0.25`; activates `.nose` on `.fixture`; yields `cappedCount == 1`, a cap warning, no conflict warning, and positive `geometryPointCount`. Extend the positive-only negative-no-op table so `-1` normalizes/effectively resolves to zero, does not require geometry, and neither activates nor skips `.nose`. Keep all redaction assertions.

Extend `MissingLandmarkDegradationTests`: include both fields in `assertNoseStrengthsAreZero`; add both at `1` to missing/stale/reused nose scenarios; assert stale/missing exact zero and reused exact `0.125` for each plus metric `0.5`. Add isolated provider-empty cases using malformed root/tip fixtures: affected new strength becomes zero, `.nose` is skipped, `skippedNoseDomains == 1`, and only `nose_inputs_missing` category evidence appears while brightness/filter continue. Add mixed invalid-new-plus-valid-legacy cases proving the invalid new effective field is zero, the valid legacy field remains positive and `.nose` active, and no bridge/tip-size control point is borrowed to represent the failed field. Do not expand to the exhaustive all-six matrix owned by Phase 37.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter BeautyEffectResolverTests &amp;&amp; swift test --package-path BeautySDK --filter MissingLandmarkDegradationTests</automated>
    <automated>for symbol in requiresFaceGeometry hasReusableNonEyeGeometryValues zeroNoseStrengths scaleReusableNonEyeGeometryStrengths supportAvailability; do rg -n "$symbol" BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift || exit 1; done; test "$(rg -o 'noseRootNarrowing' BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift | wc -l | tr -d ' ')" -ge 6; test "$(rg -o 'noseTipLift' BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift | wc -l | tr -d ' ')" -ge 6</automated>
    <automated>git diff --check -- BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - Either isolated new field requires geometry, caps to `0.25`, activates `.nose`, produces aggregate points, and remains redacted; negative input is a silent no-op.
    - Both fields are present in every named resolver list with no duplicate normalization/cap implementation.
    - Missing/stale geometry zeros both fields, and reused valid geometry retains each at exact `0.125` with exact scale metric `0.5`.
    - Isolated invalid support skips/zeros the affected request and safe domains continue; mixed invalid-new/valid-legacy retains valid legacy output but the failed new effective value is zero.
    - Existing resolver call placement, legacy nose behavior, warnings, and aggregate metric names remain compatible.
  </acceptance_criteria>
  <done>Resolver behavior is complete, field-specific, fail-closed, and compatible across cap, route, freshness, and provider-empty seams.</done>
</task>

<task type="auto">
  <name>Task 35-03-02: Extend conflict totals/counts/scaling and prove representative cross-domain weakening</name>
  <files>BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift, BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift, BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift</files>
  <read_first>
    - `AGENTS.md`
    - `.planning/REQUIREMENTS.md` — NOSE-03 conflict propagation requirement
    - `.planning/phases/35-public-contract-and-independent-geometry/35-CONTEXT.md` — preserve current call placement; Phase 37 owns exhaustive once-only behavior
    - `.planning/phases/35-public-contract-and-independent-geometry/35-RESEARCH.md` — exact conflict omissions and representative test pattern
    - `.planning/phases/35-public-contract-and-independent-geometry/35-PATTERNS.md` — scaling/total/count lists and expected integration seam
    - `BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift` — modified scale, `geometryTotal`, and `nonZeroFaceShapeFieldCount`
    - `BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift` — read-only current conflict invocation placement
    - `BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift` — modified direct resolver helper and local total mirror
    - `BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift` — modified representative public-parameter integration suite
  </read_first>
  <action>In `GeometryConflictResolver.resolve`, multiply both new strengths by the computed `scale` beside the existing nose fields. Add both as positive-only values to `geometryTotal(_:)` and `nonZeroFaceShapeFieldCount(_:)`. Do not wrap them in `abs` unnecessarily, change `totalThreshold`, move conflict resolution into the isolated nose block, consolidate the existing face/mouth passes, or change warning/metric keys.

Extend `GeometryConflictResolverTests.strengths(...)` with defaulted `noseRootNarrowing` and `noseTipLift` inputs capped to their exact constants; add both to the private test `geometryTotal` mirror. Include both nonzero in the all-field metadata test and assert the exact weakened-count increase caused by the two new independent fields (derive and assert the full expected count from the complete helper input rather than only `&gt; 0`). Add a direct isolated-strength resolver test with a deliberately low `totalThreshold` if needed to prove each new field is scaled, remains positive, and contributes to total/count.

Extend the existing `testNOSE06EveryNoseFieldWeakensWithFaceEyeMouthAndPreservesDirection` table (rename to current Phase 35/NOSE-03 wording without erasing legacy coverage) with `BeautyParameters(noseRootNarrowing: 1)` expecting `0.25` and `BeautyParameters(noseTipLift: 1)` expecting `0.25`. For each, compare isolated versus the existing faceSlim+eyeSize+mouthSize combination: combined value stays positive/nonzero, is strictly less than isolated, emits `combined_geometry_weakened`, has aggregate scale below `1`, and passes redaction. Treat this as representative participation only; do not assert a final once-only scale/count across all six nose fields.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter BeautySafetyCapsTests &amp;&amp; swift test --package-path BeautySDK --filter GeometryConflictResolverTests &amp;&amp; swift test --package-path BeautySDK --filter CombinedEffectSafetyTests</automated>
    <automated>for field in noseRootNarrowing noseTipLift; do test "$(rg -o "$field" BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift | wc -l | tr -d ' ')" -eq 3 || exit 1; done</automated>
    <automated>git diff --check -- BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - Each new value appears exactly once in conflict scale assignment, total calculation, and nonzero-field count.
    - Direct resolver evidence proves each value contributes to scale and count while staying positive.
    - Representative public-parameter integration proves isolated `0.25` becomes positive but smaller in the established face/eye/mouth conflict seam, with redacted warning/metrics.
    - Current resolver conflict invocation locations and threshold remain unchanged; no Phase 37 once-only/exhaustive claim is made.
  </acceptance_criteria>
  <done>Both new fields participate in the current conflict model without broadening this phase into final conflict architecture.</done>
</task>

<task type="auto">
  <name>Task 35-03-03: Prove isolated public-facade routing with aggregate redacted evidence</name>
  <files>BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift</files>
  <read_first>
    - `AGENTS.md`
    - `.planning/REQUIREMENTS.md` — NOSE-03 public facade and raw-geometry boundary
    - `.planning/phases/35-public-contract-and-independent-geometry/35-CONTEXT.md` — isolated facade assertions and Phase 36 renderer boundary
    - `.planning/phases/35-public-contract-and-independent-geometry/35-RESEARCH.md` — existing SPI fixture seam and exact aggregate assertions
    - `.planning/phases/35-public-contract-and-independent-geometry/35-PATTERNS.md` — fresh detector-per-case and redaction pattern
    - `BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift` — sole modified public-facade suite
    - `BeautySDK/Sources/BeautySDK/BeautyEngineGeometryDetection.swift` — read-only established routing seam; production edit is not expected
    - `BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift` — completed `requiresFaceGeometry` and nose route
    - `BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift` — valid explicit supports from Plan 35-02
  </read_first>
  <action>Add one table-driven test with two isolated cases: `BeautyParameters(noseRootNarrowing: 1)` and `BeautyParameters(noseTipLift: 1)`. For each case construct a fresh `SDKTestingFaceDetectionProvider([.usableFace])` and `BeautyEngine` so invocation accounting cannot bleed between iterations; call the existing public `processResult(image:metadata:parameters:)`. Assert invocation count exactly `1`, output extent unchanged, detection availability `.usable`, face count `1`, used face count `1`, `beauty.detection.geometryRequired == 1`, `beauty.detection.faceCount == 1`, `beauty.detection.usedFaceCount == 1`, and `beauty.effects.geometryPointCount &gt; 0`. Reuse `assertRedacted(result)` and explicitly assert no result warning, metric key, or detection reason contains `noseRoot`, `noseTip`, `support`, `coordinate`, `landmark`, `controlPoint`, `SIMD`, or bounds payload wording. Do not expose active domains, effective strengths, raw supports, points, or provider types through `BeautyResult`; do not edit `BeautyEngineGeometryDetection.swift`; do not compare pixels or add renderer/gallery files because Phase 36 owns output evidence.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter BeautyEngineGeometryFacadeTests</automated>
    <automated>test -z "$(git diff --name-only -- BeautySDK/Sources/BeautySDK/BeautyEngineGeometryDetection.swift)"</automated>
    <automated>git diff --check -- BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - Each isolated field invokes the detector exactly once and routes one usable selected face through the established facade.
    - Each result preserves extent and exposes only numeric aggregate detection/effect metrics with positive aggregate geometry point count.
    - Existing redaction plus the explicit support/root/tip/coordinate scan passes.
    - No facade production signature, public result field, renderer/gallery case, or pixel-difference claim is added.
  </acceptance_criteria>
  <done>The public facade proves both independent routes without widening the public/raw-geometry surface.</done>
</task>

</tasks>

<threat_model>
  <scope>ASVS Level 1 applies to this plan's resolver inputs, geometry-support decisions, conflict math, diagnostics, and public-facade outputs.</scope>
  <blocking_rule>Any unresolved high-severity threat blocks task completion and prevents `35-03-SUMMARY.md` from claiming the plan complete.</blocking_rule>
  <assets>Correct manual resolver propagation, exact cap/reuse/conflict math, safe fail-closed behavior, facade compatibility, raw geometry privacy, and aggregate diagnostic integrity.</assets>
  <trust_boundaries>Normalized public values cross into internal effective strengths; optional/malformed `FaceGeometry` crosses provider availability; provider results cross into domain activation; package-only observations cross the public engine while only redacted summaries/warnings/metrics may return.</trust_boundaries>
  <threats>
    - A field omitted from one manual list could trigger detection but escape caps, zeroing, reuse, activation, conflict scaling, or metrics.
    - Invalid support plus valid legacy points could leave a nonzero unsupported effective strength or alias fallback output.
    - Non-finite values or repeated conflict passes could produce unsafe math; moving call sites could create Phase 37 dependency drift.
    - Facade tests or diagnostics could expose supports/landmarks/control points, and scope could drift into renderer/gallery/ledger work.
  </threats>
  <mitigations>Enumerate every named hotspot; sanitize support availability per field; assert exact `0.25` cap and `0.125` reused values; cover isolated/mixed provider-empty behavior; extend direct and representative conflict tests while preserving call sites; use fresh detector fixtures and aggregate-only facade assertions; retain redaction scans and later full boundary scans.</mitigations>
  <residual_risk>Phase 35 proves representative conflict participation under current call placement, not exhaustive once-only weakening. Phase 37 must lock all-six degradation, final caps, exhaustive conflict behavior, and promotion; Phase 36 must prove visual output.</residual_risk>
</threat_model>

<verification>
Overall Plan 35-03 verification:

- Run focused `BeautyEffectResolverTests`, `MissingLandmarkDegradationTests`, `GeometryConflictResolverTests`, `CombinedEffectSafetyTests`, and `BeautyEngineGeometryFacadeTests` in task order.
- Structurally confirm both fields occur in every resolver hotspot and exactly three conflict locations (scale, total, count).
- Confirm public-facade production code remains unchanged and diagnostics contain no raw support/geometry payload.
- Run scoped `git diff --check`; Plan 35-04 owns the full SwiftPM and repository boundary gate.
</verification>

<success_criteria>
Plan 35-03 succeeds when either isolated field triggers the public geometry route, caps/activates with nonzero aggregate points, degrades to zero for missing/stale/unsupported inputs, reuses at exact `0.125`, participates in current conflict scaling/counts, preserves safe-domain continuation, returns only redacted aggregate facade evidence without changing conflict placement or later-phase scope, and ASVS Level 1 has no unresolved high-severity threat.
</success_criteria>

<output>
Create `.planning/phases/35-public-contract-and-independent-geometry/35-03-SUMMARY.md` when done.
</output>
