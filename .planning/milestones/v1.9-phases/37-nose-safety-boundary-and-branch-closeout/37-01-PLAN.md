---
phase: 37-nose-safety-boundary-and-branch-closeout
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - BeautySDK/Tests/BeautyEffectsTests/BeautySafetyCapsTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/NoseWarpProviderTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift
  - BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
  - BeautySDK/Sources/BeautyEffects/Warp/NoseWarpProvider.swift
autonomous: true
requirements: [NOSE-10, NOSE-11]
must_haves:
  truths:
    - "D-01..D-04: both new positive-only public fields retain 0...1 input semantics and final exact 0.25 effective caps with exact count, warning, and aggregate-metric evidence."
    - "D-05..D-12: each of the six nose fields is independently inert or eligible across zero, no-face, missing support, provider-empty, stale, reused 0.5, and sequential transitions; no prior or sibling emission can keep unsupported work alive."
    - "D-02: exact 0.25 is a conservative SDK safety cap, not subjective naturalness, device, commercial, or production-quality evidence."
    - "D-03: positive-only/default/non-finite semantics, capped_parameter_count, one aggregate warning, and redaction are asserted exactly."
    - "D-06: zero input for every field is inert without domain, count, point, or false missing-input evidence."
    - "D-08: legacy, upper-root-pair, and lower-tip supports fail independently at the narrowest field boundary while eligible siblings continue."
    - "D-09: provider-empty removes that field from baseline, strengths, domain evidence, total, count, scale, and dispatch even when a sibling emits."
    - "D-10: reused exact values are 0.175, 0.175, signed ±0.15, 0.15, 0.125, and 0.125 before independent conflict weakening."
    - "D-11: fresh→reused→stale and valid→missing/provider-empty calls prove no prior strengths or vectors carry forward."
    - "Diagnostics expose categories and aggregate bounded values only; no geometry, field-specific failure payload, path, image, or detector object crosses the facade."
  artifacts:
    - path: BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
      provides: exact final-cap/normalization/count/warning/metric table
    - path: BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
      provides: six-field degradation and transition matrix
    - path: BeautySDK/Tests/BeautyEffectsTests/NoseWarpProviderTests.swift
      provides: per-field emission eligibility and sibling-isolation proof
    - path: BeautySDK/Tests/BeautyCoreTests/BeautyEngineGeometryFacadeTests.swift
      provides: all-six public no-face extent, safe-domain, and redaction evidence
  key_links:
    - from: BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
      to: BeautySDK/Sources/BeautyEffects/Warp/NoseWarpProvider.swift
      via: effective strengths are retained only when their own field emission is nonempty
      pattern: "fieldEmissions|sanitizing|zeroNoseStrengths|scaleReusableNonEyeGeometryStrengths"
---

<objective>Lock the final exact caps and requirement-named exhaustive six-field safety matrix without redesigning behavior that already satisfies the contract.</objective>

## Artifacts this phase produces

- Test-only descriptor/table helpers for the exact cap and six-field safety matrices. No new public symbol or production file is required; production edits are permitted only when a locked test exposes a defect.

<execution_context>@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md
@PLANS.md
@.planning/phases/37-nose-safety-boundary-and-branch-closeout/37-CONTEXT.md
@.planning/phases/37-nose-safety-boundary-and-branch-closeout/37-RESEARCH.md
@.planning/phases/37-nose-safety-boundary-and-branch-closeout/37-PATTERNS.md
@.planning/phases/37-nose-safety-boundary-and-branch-closeout/37-VALIDATION.md</context>

<tasks>
<task type="auto">
  <name>Task 37-01-01: Lock exact 0.25 cap semantics and aggregate evidence</name>
  <files>BeautySafetyCapsTests.swift, BeautyEffectResolverTests.swift</files>
  <read_first>
    - `BeautySafetyCaps.swift`, `BeautyParameters.swift`, and `BeautyEffectResolver.swift` — exact public normalization/cap/count owners
    - `BeautySafetyCapsTests.swift` and `BeautyEffectResolverTests.swift` — modified analog tests
    - `37-CONTEXT.md` D-01..D-04 and `37-RESEARCH.md` final-cap gap
  </read_first>
  <action>Add a requirement-named table for `noseRootNarrowing` and `noseTipLift` that covers zero, exact public/effective `0.25`, finite public overflow (for example `1.0`) resolving to effective `0.25`, negative input resolving silently to zero, and `NaN`, `+infinity`, and `-infinity` resolving to zero. Assert exact normalized/effective values, `.nose` activation only for eligible nonzero work, exact `capped_parameter_count` (zero at input 0.25, one above the effective cap), exactly one existing aggregate capped warning when count is nonzero, expected metric keys, and redaction. Keep `BeautySafetyCaps` constants at exact `0.25`; change production code only if a row fails the locked semantics.</action>
  <verify><automated>swift test --package-path BeautySDK --filter BeautySafetyCapsTests &amp;&amp; swift test --package-path BeautySDK --filter BeautyEffectResolverTests &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>
    - Both constants and every finite/non-finite row assert exact values, not only upper bounds.
    - Exact-cap input does not increment the cap count; public overflow increments it once and produces one aggregate warning.
    - Negative/non-finite positive-only values remain silent zero no-ops with no false missing-input warning.
  </acceptance_criteria>
  <done>NOSE-10 has exact executable cap/normalization/count/warning/metric evidence.</done>
</task>

<task type="auto">
  <name>Task 37-01-02: Add exhaustive per-field degradation, emission, and transition coverage</name>
  <files>NoseWarpProviderTests.swift, MissingLandmarkDegradationTests.swift, FaceShapeWarpProviderTests.swift, BeautyEffectResolver.swift, NoseWarpProvider.swift</files>
  <read_first>
    - `NoseWarpProvider.swift` and `NoseWarpProviderTests.swift` — six explicit emission arrays and sanitization seam
    - `BeautyEffectResolver.swift` and `MissingLandmarkDegradationTests.swift` — zero/stale/reuse/preflight/convergence behavior and current fixtures
    - `FaceShapeWarpProviderTests.swift` — shared `FaceGeometry` fixture ownership
    - `37-CONTEXT.md` D-05..D-12 and research recommended descriptor
  </read_first>
  <action>Create a typed six-field descriptor (plus separate positive/negative `noseTipSize` rows where direction matters) that maps request/effective/emission key paths, exact cap/reused value, valid geometry, and the narrowest missing/provider-empty fixture. Prove per row: zero is inert without `.nose`, vector, cap/weaken count, or misleading warning; missing support and provider-empty zero only the unsupported field, remove it from final eligibility, and allow valid siblings/safe domains; stale zeros all six; reused yields exact `0.175`, `0.175`, signed `±0.15`, `0.15`, `0.125`, `0.125` before independent conflict weakening. Add consecutive fresh→reused→stale and valid→missing/provider-empty calls and compare every returned effective/emission set so no previous vector survives. Extend fixtures only when an existing narrow fixture cannot express one field. Repair resolver/provider production code only when these locked cases fail; preserve package-internal supports and monotonic removal.</action>
  <verify><automated>swift test --package-path BeautySDK --filter NoseWarpProviderTests &amp;&amp; swift test --package-path BeautySDK --filter MissingLandmarkDegradationTests &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>
    - All six fields and both signed-tip directions appear in the descriptor-driven evidence.
    - Every unsupported/provider-empty field has zero final strength and an empty own emission array even when a sibling emits.
    - Reuse values equal the six locked numbers; stale and transition destinations contain no prior strengths/vectors.
    - Warnings/metrics remain category and aggregate only.
  </acceptance_criteria>
  <done>NOSE-11 is covered at resolver/provider seams for every field and transition.</done>
</task>

<task type="auto">
  <name>Task 37-01-03: Close the all-six public no-face facade seam</name>
  <files>BeautyEngineGeometryFacadeTests.swift</files>
  <read_first>
    - `BeautyEngineGeometryFacadeTests.swift` — current legacy-four no-face test and redaction helpers
    - `BeautyEngine.swift` and `BeautyEngineGeometryDetection.swift` — read-only public facade boundary
    - `37-CONTEXT.md` D-07/D-12 and Phase 36 no-face evidence
  </read_first>
  <action>Expand or supplement the current nose no-face request so all six fields are nonzero (with a signed `noseTipSize` direction), safe color/filter work also runs, and the result proves unchanged extent, `.noFace`/`.noFaceDetected`, zero used faces, no nose vectors/effective strength, safe-domain continuation, stable aggregate warning/metric categories, and forbidden geometry/provider/path/image payload absence. Do not expose internal supports to make the test easier.</action>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyEngineGeometryFacadeTests &amp;&amp; swift test --package-path BeautySDK --filter MissingLandmarkDegradationTests &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>
    - The public-facade no-face test explicitly requests all six fields and preserves image extent.
    - No nose work survives and safe independent domains continue with redacted evidence.
  </acceptance_criteria>
  <done>The facade layer agrees with the exhaustive internal six-field fail-closed contract.</done>
</task>
</tasks>

<threat_model><scope>ASVS L1 input normalization, sensitive geometry, state-transition, and diagnostic boundaries.</scope><blocking_rule>Any unresolved HIGH finding blocks this plan and all later promotion.</blocking_rule><assets>Public compatibility, bounded strengths, private supports/control points, redacted diagnostics, deterministic current-call outputs.</assets><trust_boundaries>Untrusted scalar and detected geometry enter public normalization then package-internal resolver/provider seams.</trust_boundaries><threats>Non-finite/negative bypass, unsupported field surviving through a sibling, stale vector carryover, asymmetric output, raw geometry leakage.</threats><mitigations>Exact tables, per-field emissions, independent result comparisons, fail-closed support fixtures, facade redaction assertions.</mitigations><verification>Focused nonzero XCTest suites and diff hygiene.</verification><residual_risk>Exact multi-domain convergence and active-source closeout remain Plans 37-02/03.</residual_risk></threat_model>
<verification>Run all five affected focused suites after tasks; no product ledger may change in this plan.</verification>
<success_criteria>NOSE-10 and NOSE-11 have exact, field-complete, automated evidence with no public geometry or premature promotion.</success_criteria>
<output>Create `37-01-SUMMARY.md` with actual commands/counts, any failure-driven source edits, and promotion explicitly pending.</output>
