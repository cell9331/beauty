---
phase: 37-nose-safety-boundary-and-branch-closeout
plan: "02"
type: execute
wave: 2
depends_on: [37-01]
files_modified:
  - BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/NoseWarpProviderTests.swift
  - BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
  - BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift
autonomous: true
requirements: [NOSE-11, NOSE-12]
must_haves:
  truths:
    - "D-13..D-15: every retained face/eye/mouth/six-field nose value is counted and scaled exactly once from one converged retained baseline; removed provider-empty work contributes zero times."
    - "D-14: final retained total, weakened count, scale, combined warning, effective strengths, and emissions are asserted against the same provider-eligible set."
    - "Both signed noseTipSize directions and every previously shipped face/eye/nose/mouth direction/degradation policy remain intact."
    - "Final total, weakened count, scale, warning multiplicity, effective strengths, and provider emissions describe the same retained set without publishing a new geometry-total metric."
  artifacts:
    - path: BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift
      provides: direction-complete exact combined matrix
    - path: BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
      provides: integrated all-six retained/removed emission-equality fixture
    - path: BeautySDK/Tests/BeautyEffectsTests/GeometryConflictResolverTests.swift
      provides: exact private total/count/scale arithmetic evidence
  key_links:
    - from: BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
      to: BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift
      via: bounded monotonic retained baseline is the sole input to final conflict evidence
      pattern: "retainedBaseline|0..<9|geometryStrengthScale|weakenedCount"
---

<objective>Prove exact once-only multi-domain weakening and final provider/effective convergence for the complete six-field nose inventory.</objective>

## Artifacts this phase produces

- Test-local exact geometry-total helpers and integrated retained/removed fixture helpers. No new public metric or production API.

<execution_context>@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md
@.planning/phases/37-nose-safety-boundary-and-branch-closeout/37-CONTEXT.md
@.planning/phases/37-nose-safety-boundary-and-branch-closeout/37-RESEARCH.md
@.planning/phases/37-nose-safety-boundary-and-branch-closeout/37-PATTERNS.md
@.planning/phases/37-nose-safety-boundary-and-branch-closeout/37-01-SUMMARY.md</context>

<tasks>
<task type="auto">
  <name>Task 37-02-01: Replace relational combined checks with exact direction-complete arithmetic</name>
  <files>CombinedEffectSafetyTests.swift, GeometryConflictResolverTests.swift</files>
  <read_first>
    - Both modified test files and `GeometryConflictResolver.swift`
    - `BeautyEffectResolver.swift` bounded convergence implementation
    - Phase 35 nose/mouth review-fix evidence and `37-CONTEXT.md` D-13..D-15
  </read_first>
  <action>Extend the existing seven-row nose matrix (five positive-only fields plus positive/negative signed tip size) with representative active face, eye, and mouth geometry. Mirror `geometryTotal` test-locally using positive magnitudes and `abs` for signed values, and assert exact retained unscaled total, exact `scale = 1 / total` when total exceeds the existing threshold, exact weakened count, exactly one `combined_geometry_weakened` warning, exact scaled effective strengths, and preserved signs. Add lower-level exact all-six assertions in `GeometryConflictResolverTests`. Do not add a public total metric or an extra scaling pass.</action>
  <verify><automated>swift test --package-path BeautySDK --filter CombinedEffectSafetyTests &amp;&amp; swift test --package-path BeautySDK --filter GeometryConflictResolverTests &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>
    - Seven direction rows assert exact arithmetic, warning multiplicity, and sign rather than only reduced magnitude.
    - All six fields contribute once at the lower-level seam; signed inputs use absolute magnitude only for the total.
    - Existing face/eye/mouth behavior remains covered by the focused suite.
  </acceptance_criteria>
  <done>The normal retained-set combined matrix locks exact once-only behavior.</done>
</task>

<task type="auto">
  <name>Task 37-02-02: Prove integrated provider-empty convergence and regress shared geometry</name>
  <files>MissingLandmarkDegradationTests.swift, NoseWarpProviderTests.swift, BeautyEffectResolver.swift, GeometryConflictResolver.swift</files>
  <read_first>
    - Plan 37-01 summary and descriptor helpers
    - `MissingLandmarkDegradationTests.swift` Phase 35 threshold-crossing helper
    - `NoseWarpProvider.swift` field emissions and both resolver source files
    - `MouthWarpProviderTests.swift` as read-only shared-convergence regression owner
  </read_first>
  <action>Add an integrated case with all six nose fields, representative face/eye/mouth work, at least one provider-empty nose field, and at least one emitting nose sibling. Assert the removed field is absent from the retained unscaled total, weakened count, final scale, final effective strengths, active-domain evidence when no sibling remains, and final dispatch; assert every retained field is counted/scaled once, emits nonempty vectors, and `fieldEmissions(...).sanitizing(finalStrengths) == finalStrengths`. Exercise both signed tip directions across the integrated matrix. If a locked assertion fails, repair only the existing monotonic at-most-nine-removal convergence; never re-add fields or add a second conflict pass. Rerun nose, mouth, resolver, degradation, combined, and conflict suites.</action>
  <verify><automated>swift test --package-path BeautySDK --filter NoseWarpProviderTests &amp;&amp; swift test --package-path BeautySDK --filter MouthWarpProviderTests &amp;&amp; swift test --package-path BeautySDK --filter MissingLandmarkDegradationTests &amp;&amp; swift test --package-path BeautySDK --filter BeautyEffectResolverTests &amp;&amp; swift test --package-path BeautySDK --filter CombinedEffectSafetyTests &amp;&amp; swift test --package-path BeautySDK --filter GeometryConflictResolverTests &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>
    - Provider-empty work contributes zero times; supported siblings remain active and emit.
    - Final exact total/count/scale/effective/emission assertions all refer to one converged retained set.
    - Both tip signs and all shared mouth/nose convergence regressions pass with zero failures.
  </acceptance_criteria>
  <done>NOSE-12 and the final NOSE-11 convergence seam are command-backed.</done>
</task>
</tasks>

<threat_model><scope>ASVS L1 algorithmic abuse, double-scaling, signed-direction, and provider-eligibility boundaries.</scope><blocking_rule>Any unresolved HIGH finding or effective/emission divergence blocks Plan 37-03 and promotion.</blocking_rule><assets>Bounded geometry strength, deterministic totals/counts/scale, signed behavior, final emitted vectors.</assets><trust_boundaries>Normalized public values cross provider eligibility and conflict resolution before internal rendering.</trust_boundaries><threats>Double weakening, counting empty work, sign loss, non-convergent masks, sibling-supported leakage.</threats><mitigations>Exact arithmetic, bounded monotonic loop, retained-set equality, provider emission assertions, shared mouth/nose regressions.</mitigations><verification>Six focused nonzero suites and exact expected values.</verification><residual_risk>Full/output/security scans and documentation promotion remain later plans.</residual_risk></threat_model>
<verification>All focused geometry suites must pass after any production edit; Plans 37-01/02 must leave all product ledgers unpromoted.</verification>
<success_criteria>Each retained geometry field is scaled once, each removed field zero times, all shipped signs/policies survive, and final evidence converges exactly.</success_criteria>
<output>Create `37-02-SUMMARY.md` with exact totals/counts/scales and any source repair.</output>
