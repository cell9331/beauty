# Phase 40 Research: Mouth Geometry Safety and Ledger Closeout

**Researched:** 2026-07-14
**Scope:** MOUTH-12 through MOUTH-16 and DOC-01

## Finding

Phase 38 already installed the runtime seams required by Phase 40: exact provisional `0.25` caps, eight mouth effective strengths, per-field provider emissions/sanitization, exact `0.5` reusable-geometry scaling, per-support local-lip eligibility, and a monotonic resolver loop bounded at fourteen nose/mouth removals. Phase 39 added direct public-facade output evidence. The remaining work is to freeze those behaviors as final with exhaustive requirement-named tests, add a fail-closed active-source/promotion checker, run fresh evidence/review/security/validation, then promote documentation atomically.

## Existing Evidence to Reuse

- `BeautyEffectResolverTests` covers the five new fields at cap through the resolver, signed directions, capped warning/count, and negative positive-only no-ops.
- `MouthWarpProviderTests` covers eight independent emission arrays, malformed outer support, missing/invalid upper/lower/inner support, tiny provider-empty work, invalid bounds, and aggregate redacted skip reasons.
- `MissingLandmarkDegradationTests` covers local-sibling continuation, reused `0.125`, stale/missing/no-face removal, and provider-empty final evidence.
- `CombinedEffectSafetyTests` covers all thirteen signed/positive mouth direction rows with face/eye/six-nose work, but the Phase 40 contract needs exact totals/counts/scales rather than relational checks.
- `GeometryConflictResolverTests` covers exact five-new-field arithmetic and large mixed-domain counts; it needs a complete eight-mouth/six-nose retained-set case.
- Phase 39 verification records 260/260 full tests, 308/308 decoded outputs, 96/96 portrait pairs, 8/8 no-face pairs, and ignored/untracked gallery containment.
- Archived Phase 37 provides the closest safety/boundary/promotion workflow and must be adapted without copying its completed nose-branch status claims.

## Gaps

1. Rename/freeze the five `0.25` constants as final and cover exact-cap input separately from public overflow so capped-count semantics are indisputable.
2. Extend degradation/transition evidence from the five new fields to the complete eight-field mouth set, including lip-color independence and fresh/reused/stale/no-face transitions.
3. Replace relational combined assertions with exact one-baseline arithmetic and provider-emission equality for face, eye, all six nose, and all eight mouth fields.
4. Add a self-tested Phase 40 boundary checker with pre-promotion and allow-promotion modes covering source/API/import/dependency/network/commercial/compatibility/classification/artifact/owner/lifecycle boundaries.
5. Synchronize all current owners and promote exactly five rows, while explicitly retaining `白牙` future and branch `嘴唇` partial.

## Recommended Plan

- Plan 40-01: exact final caps plus exhaustive eight-field degradation/facade transitions.
- Plan 40-02: exact multi-domain convergence, fourteen-removal/provider-empty agreement.
- Plan 40-03: fail-closed boundary checker, fresh runtime/output evidence, review, security, validation.
- Plan 40-04: atomic five-row promotion and all-owner closeout, final verification and audit handoff.

## Risks and Controls

- **Premature promotion:** product/branch status files are read-only until Plans 01-03 are green.
- **Support over-removal:** outer support and local upper/lower/inner support are independent; tests must preserve eligible siblings.
- **Double weakening:** exact totals and emission equality must use one retained baseline and one scale.
- **False security confidence:** scans must distinguish clean no-match, classified matches, command errors, scope errors, and artifact failures.
- **Overclaim:** no milestone audit/archive/tag/cleanup or Demo/device/commercial/performance/packaging/shipping/launch success in Phase 40.

