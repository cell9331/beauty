---
phase: 38-public-contract-and-lip-support-geometry
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift
  - BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift
  - BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift
  - BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift
  - BeautySDK/Tests/BeautyDetectionTests/VisionFaceDetectorTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift
autonomous: true
requirements:
  - MOUTH-04
must_haves:
  truths:
    - "Vision inner-lip availability is recorded separately from outer lips and is not added to the globally required geometry set."
    - "A face with every required group except innerLips remains usable; missing inner lips cannot disable shipped mouth geometry or whole-mouth support."
    - "FaceGeometry adds package-internal default-empty upperLips, lowerLips, and innerLips supports while the existing outerLips proxy remains exactly unchanged."
    - "The adapter builds finite, normalized, face-bounded, deterministic, distinct whole/upper/lower/inner supports only from their owning availability groups."
    - "No raw Vision landmark region or support array crosses the public/SPI facade or diagnostics."
  artifacts:
    - path: "BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift"
      provides: "Independent package-only innerLips availability case with unchanged required group set"
      contains: "case innerLips"
    - path: "BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift"
      provides: "Vision innerLips point-count availability mapping"
      contains: "landmarks.innerLips"
    - path: "BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift"
      provides: "Default-empty package-internal upper/lower/inner support storage"
      contains: "upperLips"
    - path: "BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift"
      provides: "Deterministic group-gated lip proxy construction without changing outerLips"
      contains: "innerLips(in: bounds)"
    - path: "BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift"
      provides: "Exact adapter/support and shared valid/malformed fixture evidence"
  key_links:
    - from: "BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift"
      to: "BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift"
      via: "optional Vision innerLips presence becomes one available group but not a required group"
      pattern: "innerLips"
    - from: "BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift"
      to: "BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift"
      via: "coarse outer/inner availability gates deterministic package-only supports"
      pattern: "availableGroups"
    - from: "BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift"
      to: "BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift"
      via: "adapter output initializes explicit FaceGeometry support arrays"
      pattern: "upperLips|lowerLips|innerLips"
---

<objective>
Create the independent package-only lip-support seam required by local M-lip/plump geometry while preserving all shipped outer-lip and global face-usability behavior.

Purpose: make inner availability explicit without making it globally required, and provide deterministic upper/lower/inner proxy supports with strict structural evidence.
Output: coarse inner availability, default-empty private supports, group-gated adapter construction, shared fixtures, and focused tests.
</objective>

## Artifacts this phase produces

- `BeautyLandmarkGroup.innerLips` and Vision point-count availability mapping.
- Unchanged global required geometry containing `outerLips` but not `innerLips`.
- Package-internal `FaceGeometry.upperLips`, `.lowerLips`, and `.innerLips` with default-empty initializer arguments.
- Deterministic adapter builders for explicit upper/lower/inner supports while preserving exact legacy `outerLips` coordinates/order.
- Shared valid, missing, insufficient, duplicate, non-finite, reused, and stale support fixtures for later provider/resolver plans.
- No public raw geometry, actual Vision point persistence, new dependency, renderer, Demo, or product-status change.

## Atomic Commit Intent

- Task `38-02-01`: one atomic commit for coarse inner availability plus focused detector tests.
- Task `38-02-02`: one atomic commit for private supports, adapter builders, fixtures, and adapter/support tests.
- Stage only task-owned files after focused checks pass; preserve unrelated worktree changes.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-public-contract-and-lip-support-geometry/38-CONTEXT.md
@.planning/phases/38-public-contract-and-lip-support-geometry/38-RESEARCH.md
@.planning/phases/38-public-contract-and-lip-support-geometry/38-PATTERNS.md
@.planning/phases/38-public-contract-and-lip-support-geometry/38-VALIDATION.md
@BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift
@BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift
@BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift
@BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift
@BeautySDK/Tests/BeautyDetectionTests/VisionFaceDetectorTests.swift
@BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 38-02-01: Record inner-lip availability without making it globally required</name>
  <files>BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift, BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift, BeautySDK/Tests/BeautyDetectionTests/VisionFaceDetectorTests.swift</files>
  <read_first>
    - `AGENTS.md`, `PLANS.md`, and `.planning/REQUIREMENTS.md` — workflow and MOUTH-04 contract
    - `38-CONTEXT.md` — locked outer-sufficient/inner-optional decision
    - `38-RESEARCH.md` — coarse availability architecture and raw-geometry boundary
    - `38-PATTERNS.md` — exact enum/required-set/Vision mapping analogs
    - `BeautyFaceObservation.swift` — modified group and required-geometry owner
    - `VisionFaceDetector.swift` — modified Vision availability mapping
    - `VisionFaceDetectorTests.swift` — modified usability/redaction suite
  </read_first>
  <action>Add `case innerLips` to `BeautyLandmarkGroup` beside `outerLips`. Do not add it to `requiredGeometryGroups`; keep contour, both eyes, nose, and outer lips as the exact required set. Preserve `.complete` via `Set(allCases)` so complete observations include the new optional group automatically.

In `VisionFaceDetector.landmarks(from:)`, mirror the outer-lip mapping: insert `.innerLips` only when `landmarks.innerLips?.pointCount` is positive. Do not retain region points, add a public type, or place raw framework objects in observations/diagnostics.

Extend `VisionFaceDetectorTests` with injected observations whose available groups remove only `.innerLips`; assert `hasRequiredGeometry == true`, detection remains `.usable`, the observation is selected, and public counts remain coarse/redacted. Add an explicit missing-outer control that remains `.partial` so the required-set boundary is locked. If a real-fixture assertion is added, it may assert no raw payload but must not make platform-dependent inner availability the only proof. Run focused tests and create the task's atomic commit from only the three listed files.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter VisionFaceDetectorTests</automated>
    <automated>rg -n 'case innerLips|requiredGeometryGroups|outerLips' BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift &amp;&amp; rg -n 'landmarks\.innerLips\?\.pointCount|groups\.insert\(\.innerLips\)' BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift</automated>
    <automated>if sed -n '/requiredGeometryGroups/,/]/p' BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift | rg 'innerLips'; then exit 1; else status=$?; test "$status" -eq 1; fi</automated>
    <automated>git diff --check -- BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift BeautySDK/Sources/BeautyDetection/VisionFaceDetector.swift BeautySDK/Tests/BeautyDetectionTests/VisionFaceDetectorTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - `.innerLips` exists as a separate package-only availability group and Vision mapping records only presence.
    - Removing only inner lips leaves an injected face globally usable and selected.
    - Removing outer lips still fails the required-geometry check.
    - No raw region points, framework payloads, coordinates, or public/SPI geometry surface is added.
  </acceptance_criteria>
  <done>The detector exposes independent coarse inner-lip availability while preserving outer-only global usability and redaction.</done>
</task>

<task type="auto">
  <name>Task 38-02-02: Add deterministic default-empty upper/lower/inner supports and fixtures</name>
  <files>BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift, BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift, BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — MOUTH-04 finite/bounded/deterministic/no-public-geometry contract
    - `38-CONTEXT.md`, `38-RESEARCH.md`, and `38-PATTERNS.md` — explicit support ownership and unchanged-outer invariant
    - `WarpControlPoint.swift` — modified `FaceGeometry` owner and nose support analog
    - `BeautyFaceGeometryAdapter.swift` — modified proxy builder and exact existing outer-lip coordinates
    - `FaceShapeWarpProviderTests.swift` — modified adapter test and shared fixture owner
    - Current `MouthWarpProvider.swift` / `NoseWarpProvider.swift` — read-only upcoming consumer/validation shapes
  </read_first>
  <action>Add package-internal `upperLips`, `lowerLips`, and `innerLips` arrays to `FaceGeometry` after `outerLips`, with default-empty initializer arguments before `freshness`. Preserve every existing field and the exact `outerLips` API/initializer behavior.

In `BeautyFaceGeometryAdapter`, leave the existing `outerLips(in:)` point sequence byte-for-byte unchanged. Add deterministic private builders using `point(bounds:x:y:)`: upper and lower arrays must be distinct local outer-surface subsets populated only when `.outerLips` is available; inner support must represent a nondegenerate opening and be populated only when `.innerLips` is available. Choose conservative proxy coordinates per the discretionary contract, but lock stable ordering, sufficient cardinality for later peak/plump work, finiteness, normalized bounds, face bounds, and no duplicates. `makeGeometry` must create: complete -> all supports; missing outer -> outer/upper/lower empty regardless of inner; missing inner -> outer/upper/lower retained and inner empty.

Extend the existing adapter test to assert exact unchanged legacy outer points and exact new support points for a known face bound. Assert deterministic repeated mapping, finite normalized/face-bounded coordinates, and no duplicates. Extend shared `.fixture`, `.missingMouth`, `.reused`, and `.stale` to forward new supports. Add named variants for missing upper/lower/inner and non-finite, duplicate-only, and insufficient support arrays needed by Plan 38-03/04, without changing unrelated legacy geometry. Prove older source-style `FaceGeometry` construction still compiles due defaults. Run focused tests and create an atomic commit from the three listed files.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter FaceShapeWarpProviderTests</automated>
    <automated>for field in upperLips lowerLips innerLips; do rg -n "let $field: \[SIMD2&lt;Float&gt;\]|$field: \[SIMD2&lt;Float&gt;\] = \[\]" BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift || exit 1; rg -n "$field: landmarks\.contains|private static func $field" BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift || exit 1; done</automated>
    <automated>outer="$(sed -n '/private static func outerLips/,/^    }/p' BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift)"; test "$(printf '%s\n' "$outer" | rg -c 'point\(bounds')" -eq 8; for token in 'x: 0.30, y: 0.76' 'x: 0.40, y: 0.70' 'x: 0.50, y: 0.68' 'x: 0.60, y: 0.70' 'x: 0.70, y: 0.76' 'x: 0.60, y: 0.82' 'x: 0.50, y: 0.84' 'x: 0.40, y: 0.82'; do printf '%s\n' "$outer" | rg -F "$token" || exit 1; done</automated>
    <automated>git diff --check -- BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift BeautySDK/Sources/BeautyEffects/Planning/BeautyFaceGeometryAdapter.swift BeautySDK/Tests/BeautyEffectsTests/FaceShapeWarpProviderTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - `FaceGeometry` owns three package-internal default-empty support arrays and older construction remains source-compatible.
    - Existing adapter outer-lip coordinates/order are unchanged and explicitly regression-tested.
    - Complete, missing-outer, and missing-inner observations produce the exact group-dependent support state.
    - New supports are finite, normalized, face-bounded, deterministic, distinct, structurally sufficient, and never public/SPI.
    - Named valid/malformed fixtures exist for direct provider/resolver tests without weakening unrelated geometry.
  </acceptance_criteria>
  <done>MOUTH-04 has an explicit, deterministic, fail-closed package-only support seam ready for independent provider work.</done>
</task>

</tasks>

<threat_model>
  <scope>ASVS Level 1 applies to Vision-derived availability, package-only biometric-adjacent support data, malformed support construction, public/SPI boundaries, and diagnostic redaction.</scope>
  <blocking_rule>Any unresolved high-severity raw-geometry exposure, required-group regression, non-finite support, or legacy outer-lip drift blocks completion.</blocking_rule>
  <assets>Face usability, raw landmark privacy, deterministic support integrity, shipped mouth behavior, source compatibility, and evidence honesty.</assets>
  <trust_boundaries>Optional Vision region presence crosses into coarse package availability; availability and face bounds cross into private proxy supports consumed by effects.</trust_boundaries>
  <threats>
    - Making inner lips globally required can reject valid outer-only faces and regress shipped behavior.
    - Retaining raw Vision points or exposing support arrays can leak biometric-adjacent geometry.
    - Non-finite, duplicate, out-of-face, insufficient, or nondeterministic proxies can poison provider math.
    - Changing legacy outer-lip points can invalidate archived/shipped mouth evidence even when new fields are zero.
    - Test fixtures can silently omit new supports and create unrelated regressions.
  </threats>
  <mitigations>Exact required-set negative scan; injected outer-only usability test; package-only/default-empty arrays; explicit finite/bounds/distinct adapter tests; exact legacy outer coordinate regression; fixture forwarding; public/SPI/raw-diagnostic scans in Plan 38-04.</mitigations>
  <residual_risk>Provider vector naturalness and final support-dependent degradation are proven in Plans 38-03/04 and output/final-safety Phases 39/40.</residual_risk>
</threat_model>

<verification>
Run focused detector and adapter/fixture suites, exact optional-group/default-array scans, unchanged legacy outer-lip diff checks, and scoped `git diff --check`. Both task commits must remain atomic and contain no unrelated files.
</verification>

<success_criteria>
Plan 38-02 succeeds when inner availability is independently recorded but optional globally, deterministic package-only upper/lower/inner supports are source-compatible and group-gated, legacy outer support is unchanged, malformed fixture coverage exists, focused tests pass, and no raw geometry or later-phase scope crosses the boundary.
</success_criteria>

<output>
Create `.planning/phases/38-public-contract-and-lip-support-geometry/38-02-SUMMARY.md` when done, recording observed test counts, exact support/outer invariants, and both atomic commit hashes.
</output>
