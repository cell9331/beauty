---
phase: 39-public-facade-mouth-geometry-output-evidence
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - BeautySDK/Sources/BeautyExampleRenderer/main.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift
autonomous: true
requirements: [MOUTH-09, MOUTH-11]
must_haves:
  truths:
    - "The renderer has exactly 44 ordered public-facade cases after adding only the eight isolated Phase 39 mouth cases at provisional 0.25."
    - "Each of fourteen mouth/lip cases contains exactly one of nine public mouth/lip fields; aliases, combined cases, internal imports, and second render paths remain absent."
    - "Representative no-face facade evidence for all eight new cases preserves extent, reports no face and zero used faces, and remains aggregate-only and redacted."
  artifacts:
    - path: BeautySDK/Sources/BeautyExampleRenderer/main.swift
      provides: eight isolated public-facade mouth geometry cases
    - path: BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift
      provides: exact inventory, isolation, facade boundary, and no-face regressions
  key_links:
    - from: BeautySDK/Sources/BeautyExampleRenderer/main.swift
      to: BeautyEngine.processResult
      via: the existing shared public facade loop
      pattern: "mouthYPosition_|mouthTilt_|mouthXPosition_|lipPeakDefinition_|lipPlump_"
---

<objective>
Extend the public-facade renderer with exactly eight isolated mouth geometry cases and lock their source/no-face contract before generating artifacts.
</objective>

## Artifacts this phase produces

- `RenderCase` entries `mouthYPosition_plus0p25`, `mouthYPosition_minus0p25`, `mouthTilt_plus0p25`, `mouthTilt_minus0p25`, `mouthXPosition_plus0p25`, `mouthXPosition_minus0p25`, `lipPeakDefinition_0p25`, and `lipPlump_0p25`.
- Phase-39-named XCTest methods/helpers in the existing renderer regression suite; no new production geometry type, dependency, target, public raw geometry API, or generated file.

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-public-facade-mouth-geometry-output-evidence/39-CONTEXT.md
@.planning/phases/39-public-facade-mouth-geometry-output-evidence/39-RESEARCH.md
@.planning/phases/39-public-facade-mouth-geometry-output-evidence/39-PATTERNS.md
@BeautySDK/Sources/BeautyExampleRenderer/main.swift
@BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift
</context>

<tasks>
<task type="auto">
  <name>Task 39-01-01: Add and freeze the exact eight isolated renderer cases</name>
  <files>BeautySDK/Sources/BeautyExampleRenderer/main.swift, BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift</files>
  <read_first>
    - `AGENTS.md` and `PLANS.md`
    - `.planning/REQUIREMENTS.md` — MOUTH-09 exact contract
    - `39-CONTEXT.md`, `39-RESEARCH.md`, and `39-PATTERNS.md` — locked IDs, signs, provisional values, aliases, and facade boundary
    - `BeautySDK/Sources/BeautyExampleRenderer/main.swift` — modified source and actual 36-case baseline
    - `BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift` — modified exact-inventory owner and stale Phase 33 mouth guard
  </read_first>
  <action>Add exactly the eight `RenderCase` values named in the artifacts section, using display names `mouthYPosition ±0.25`, `mouthTilt ±0.25`, `mouthXPosition ±0.25`, `lipPeakDefinition 0.25`, and `lipPlump 0.25` and the matching single-label public `BeautyParameters` initializer. Keep the single shared `BeautyEngine.processResult` loop and `import BeautySDK` boundary. Update the ordered regression inventory to exactly 44. Replace the stale six-case mouth prohibition with an exact fourteen-case contract across `mouthSize`, `mouthWidth`, `smile`, `mouthYPosition`, `mouthTilt`, `mouthXPosition`, `lipPeakDefinition`, `lipPlump`, and `lipColor`; assert one matching field per case, exact signs/0.25 values for new cases, and reject exact combo/M-lip alias/teeth-whitening cases without rejecting canonical names.</action>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyRendererOutputRegressionTests &amp;&amp; test "$(rg -n 'RenderCase\(' BeautySDK/Sources/BeautyExampleRenderer/main.swift | wc -l | tr -d ' ')" -eq 44 &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>
    - Renderer and XCTest discovery both report exactly 44 case entries, with each new ID exactly once.
    - Every new snippet contains only its matching public field at exact signed/unsigned `0.25`; fourteen mouth/lip cases collectively cover nine fields without alias or combo construction.
    - Renderer retains only the public `BeautySDK` Beauty import and one shared public-facade processing path.
  </acceptance_criteria>
  <done>The public renderer source contract for MOUTH-09 is exact and independently testable.</done>
</task>

<task type="auto">
  <name>Task 39-01-02: Lock representative no-face public-facade behavior for all eight cases</name>
  <files>BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift</files>
  <read_first>
    - `39-CONTEXT.md` and `39-RESEARCH.md` — representative no-face assertions and Phase 40 boundary
    - `BeautyRendererOutputRegressionTests.swift` — modified suite and Phase 36 no-face analog
    - `BeautyEngineGeometryFacadeTests.swift` — existing aggregate/redacted public result idioms
  </read_first>
  <action>Extend the existing no-face facade test pattern with an exact loop over the six signed `±0.25` requests plus `lipPeakDefinition: 0.25` and `lipPlump: 0.25`. For every isolated request assert output extent equals input, availability `.noFace`, reasons `[.noFaceDetected]`, geometry-required metric `1`, face and used-face counts zero, established `face_effects_skipped_no_face` warning, and redaction success. Reject disclosure of the five field names plus support/coordinate/landmark/control-point/provider tokens in public warnings, metrics, and detection reasons. Do not add missing/stale/reused/provider-empty or all-eight-field transitions owned by Phase 40.</action>
  <verify><automated>swift test --package-path BeautySDK --filter BeautyRendererOutputRegressionTests &amp;&amp; git diff --check -- BeautySDK/Tests/BeautyCoreTests/BeautyRendererOutputRegressionTests.swift</automated></verify>
  <acceptance_criteria>
    - All eight isolated requests execute nonzero focused XCTest evidence and preserve extent with `.noFace`/`.noFaceDetected` and zero used faces.
    - Diagnostics remain category/aggregate-only and pass existing plus Phase 39 field-disclosure assertions.
    - No production geometry, cap, resolver, dependency, Demo, promotion ledger, gallery, or generated artifact changes occur.
  </acceptance_criteria>
  <done>Representative no-face facade behavior is locked without expanding into Phase 40 safety scope.</done>
</task>
</tasks>

<threat_model>
  <scope>ASVS L1 public-facade and diagnostic boundary.</scope>
  <blocking_rule>Any unresolved HIGH threat blocks this plan and its summary.</blocking_rule>
  <assets>Exact public case inventory, isolated field identity, raw-geometry privacy, extent preservation, and redacted diagnostics.</assets>
  <trust_boundaries>Fixture images and public parameters enter `BeautyEngine`; only watermarked PNGs and aggregate public results leave.</trust_boundaries>
  <threats>Alias/combo cases could fake independence, internal imports could bypass the facade, and no-face output could expose geometry or unsafe diagnostics.</threats>
  <mitigations>Exact case/field assertions, import/path scans, shared facade loop, extent/summary/warning/metric/redaction XCTest evidence.</mitigations>
  <residual_risk>Decoded visibility, final caps, and exhaustive eight-field safety remain Plans 39-02 and Phase 40.</residual_risk>
</threat_model>

<verification>Run the focused renderer suite, exact 44-case/source scans, facade-import scan, and `git diff --check`; record observed counts in `39-01-SUMMARY.md`.</verification>
<success_criteria>MOUTH-09's source contract and MOUTH-11's representative facade no-face contract pass with no HIGH threat or Phase 40 drift.</success_criteria>
<output>Create `.planning/phases/39-public-facade-mouth-geometry-output-evidence/39-01-SUMMARY.md`.</output>
