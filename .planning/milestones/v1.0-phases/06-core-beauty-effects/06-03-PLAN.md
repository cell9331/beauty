---
phase: "06-core-beauty-effects"
plan: "06-03"
status: "pending"
type: "execute"
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - "BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift"
  - "BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift"
  - "BeautySDK/Sources/BeautyEffects/Warp/EyeWarpProvider.swift"
  - "BeautySDK/Sources/BeautyEffects/Warp/NoseWarpProvider.swift"
  - "BeautySDK/Sources/BeautyEffects/Warp/LandmarkGeometryHelper.swift"
  - "BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift"
  - "BeautySDK/Sources/BeautyEffects/Render/BeautyGeometryEffectPipeline.swift"
  - "BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift"
  - "BeautySDK/Tests/BeautyEffectsTests/NoseWarpProviderTests.swift"
  - "BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift"
  - "BeautySDK/Tests/BeautyCoreTests/BeautyEngineTests.swift"
autonomous: true
requirements: ["EFFECT-05", "EFFECT-06", "EFFECT-09"]
requirements_addressed: ["EFFECT-05", "EFFECT-06", "EFFECT-09"]
must_haves:
  truths:
    - "D-01: eye and nose controls must produce deterministic fixture-visible or provider-level MVP evidence."
    - "D-02: eye and nose output must stay plainly visible but conservative."
    - "D-03: automated fixture/provider evidence is required before Demo smoke."
    - "D-05 and D-06: public slider max values remain accepted, while eye and nose effective strengths use tested safety caps from `docs/06_beauty_parameters_spec.md`."
    - "D-07: eye and nose geometry must participate in combined geometry weakening when overlapping controls compound."
    - "D-08: cap, skip, stale, and weakening evidence must appear through `BeautyResult.warnings` or metrics."
    - "D-10: missing eyes skip only eye effects, and missing nose skips only nose effects; unrelated safe effects continue."
    - "D-11: reused landmarks may drive reduced eye/nose geometry only inside the allowed reuse window; stale landmarks disable strong geometry."
    - "D-12: Demo sliders remain enabled and existing detection status/debug surfaces carry degradation feedback."
    - "D-17: exact provider names, point thresholds, metrics keys, and fixture coordinates may vary but must be deterministic."
    - "D-18: eye and nose logic belongs in `BeautyEffects` and must not introduce UI dependencies inside SDK targets."
  artifacts:
    - "`EyeWarpProvider` covering `eyeSize`, `eyeDistance`, `eyeYPosition`, and `eyeTailLift`."
    - "`NoseWarpProvider` covering `noseSlim`, `noseWingSlim`, `noseTipSize`, and `noseBridge`."
    - "Missing-landmark degradation tests proving targeted skip behavior."
    - "Geometry metrics and warnings for caps, stale/reused reductions, and skipped eye/nose domains."
  key_links:
    - "`BeautyFaceLandmarks.leftEye/rightEye` -> eye provider -> capped eye control points."
    - "`BeautyFaceLandmarks.nose` -> nose provider -> capped nose control points."
    - "Missing eye or nose landmarks -> skipped affected domain -> unrelated domains continue."
---

# Plan 06-03: Implement Eye and Nose MVP Controls With Coordinate Fixtures

## Phase Goal

**As a** Demo user, **I want to** tune eye and nose controls, **so that** Phase 6 covers the most recognizable facial-feature edits with deterministic, capped, and safely degrading SDK behavior.

<objective>
Add eye and nose geometry providers on top of the shared Phase 6 warp infrastructure.

Purpose: EFFECT-05 and EFFECT-06 require SDK-backed eye and nose behavior, while EFFECT-09 requires default no-op, naturalness caps, combined safety, and missing-landmark degradation.
Output: Eye and nose providers, targeted missing-landmark skip tests, cap and stale/reused warning evidence, and deterministic fixture evidence for eye and nose effect activation.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@ARCHITECTURE.md
@DESIGN.md
@SECURITY.md
@RELIABILITY.md
@PRODUCT_SENSE.md
@QUALITY_SCORE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-core-beauty-effects/06-CONTEXT.md
@.planning/phases/06-core-beauty-effects/06-RESEARCH.md
@.planning/phases/06-core-beauty-effects/06-VALIDATION.md
@.planning/phases/06-core-beauty-effects/06-PATTERNS.md
@.planning/phases/06-core-beauty-effects/06-01-PLAN.md
@.planning/phases/06-core-beauty-effects/06-02-PLAN.md
@docs/06_beauty_parameters_spec.md
@docs/07_face_landmarks_coordinate_system.md
@docs/08_metal_render_pipeline_design.md
@docs/09_algorithm_effects_implementation.md
@BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift
@BeautySDK/Sources/BeautyDetection/CoordinateMapper.swift
@BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift
@BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift
@BeautySDK/Sources/BeautyEffects/Warp/LandmarkGeometryHelper.swift
@BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift
@BeautySDK/Sources/BeautyEffects/Render/BeautyGeometryEffectPipeline.swift
</context>

## Artifacts this phase produces

- `EyeWarpProvider`: internal provider for `eyeSize`, `eyeDistance`, `eyeYPosition`, and `eyeTailLift`.
- `NoseWarpProvider`: internal provider for `noseSlim`, `noseWingSlim`, `noseTipSize`, and `noseBridge`.
- Tests proving caps: `eyeSize 0.45`, `eyeDistance 0.30`, `eyeYPosition 0.25`, `eyeTailLift 0.30`, `noseSlim 0.35`, `noseWingSlim 0.35`, `noseTipSize 0.30`, and `noseBridge 0.30`.
- Targeted degradation warnings such as `eye_landmarks_missing`, `nose_landmarks_missing`, and `geometry_stale_reduced`, or final equivalent stable codes.
- Resolver and engine fixture evidence that eye and nose domains activate independently and skip independently.

## Source Coverage Audit

| Source | ID | Feature / Requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | - | Eye and nose controls with caps | 06-03 | COVERED | Providers build on plan 06-02 warp primitives. |
| REQ | EFFECT-05 | Eye size, distance, vertical position, tail lift | 06-03 | COVERED | Provider tests cover all fields. |
| REQ | EFFECT-06 | Nose slim, wing slim, tip size, bridge | 06-03 | COVERED | Provider tests cover all fields. |
| REQ | EFFECT-09 | Caps, combined safety, missing landmarks | 06-03 | COVERED | Cap, stale, skip, and metrics tests. |
| CONTEXT | D-01 D-02 D-03 | Visible/conservative/tested evidence | 06-03 | COVERED | Provider and fixture evidence. |
| CONTEXT | D-05 D-06 D-07 D-08 | Safety caps, combined weakening, metadata | 06-03 | COVERED | One cap source and warning/metric evidence. |
| CONTEXT | D-10 D-11 D-12 | Missing, stale, and UI degradation rules | 06-03 | COVERED | Targeted skip and no Demo slider disabling. |
| CONTEXT | D-17 D-18 | Naming and boundary discretion | 06-03 | COVERED | Internal `BeautyEffects` ownership. |

<tasks>

<task id="06-03-01" type="auto" tdd="true">
  <name>Task 1: Add capped eye geometry provider and degradation tests</name>
  <files>
    BeautySDK/Sources/BeautyEffects/Warp/EyeWarpProvider.swift
    BeautySDK/Sources/BeautyEffects/Warp/LandmarkGeometryHelper.swift
    BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
    BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift
    BeautySDK/Tests/BeautyEffectsTests/EyeWarpProviderTests.swift
    BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
  </files>
  <read_first>
    .planning/phases/06-core-beauty-effects/06-CONTEXT.md
    .planning/phases/06-core-beauty-effects/06-RESEARCH.md
    .planning/phases/06-core-beauty-effects/06-PATTERNS.md
    docs/06_beauty_parameters_spec.md
    docs/09_algorithm_effects_implementation.md
    BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift
    BeautySDK/Sources/BeautyEffects/Warp/WarpControlPoint.swift
    BeautySDK/Sources/BeautyEffects/Warp/FaceShapeWarpProvider.swift
    BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift
    BeautySDK/Tests/BeautyEffectsTests/BeautySafetyCapsTests.swift
  </read_first>
  <behavior>
    - `eyeSize` creates symmetric radial or outward control points around both eyes when both eye landmark groups are present.
    - `eyeDistance` moves the left and right eye regions apart or together along the horizontal axis while preserving conservative strength.
    - `eyeYPosition` moves both eye regions vertically in the same direction.
    - `eyeTailLift` lifts outer eye-tail points without moving nose or mouth landmarks.
    - Missing left or right eye landmarks skip only eye geometry and emit a redacted warning/metric.
    - Reused landmarks reduce effective eye geometry; stale landmarks skip strong eye geometry.
  </behavior>
  <action>Implement `EyeWarpProvider` using normalized eye landmarks from `BeautyFaceLandmarks.leftEye` and `rightEye`. Reuse `LandmarkGeometryHelper` for centers, bounds, symmetry checks, and vector movement. Add exact safety cap constants for `eyeSize`, `eyeDistance`, `eyeYPosition`, and `eyeTailLift` in `BeautySafetyCaps` if plan 06-01 did not already add them. Update `BeautyEffectResolver` to activate the `eyes` domain, include capped effective strengths, skip only the eye domain when either eye group is unavailable, and return stable warning and metric evidence for capped, missing, reused, and stale cases.</action>
  <acceptance_criteria>
    - `EyeWarpProviderTests` assert `eyeSize: 1` creates control points around both eyes and effective strength is no greater than `0.45`.
    - Tests assert `eyeDistance: 1` moves eye centers outward and effective strength is no greater than `0.30`.
    - Tests assert `eyeYPosition: 1` moves both eyes vertically and effective strength is no greater than `0.25`.
    - Tests assert `eyeTailLift: 1` moves outer-tail points upward and effective strength is no greater than `0.30`.
    - `MissingLandmarkDegradationTests` assert missing one eye group skips only `eyes`; color/filter and available non-eye domains remain active.
    - Tests assert warnings and metrics do not include raw coordinates, bounding boxes, Vision types, local file paths, or image byte dumps.
  </acceptance_criteria>
  <verify>
    <automated>CLANG_MODULE_CACHE_PATH=/private/tmp/beauty-clang-module-cache DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path BeautySDK --filter EyeWarpProviderTests</automated>
    <automated>CLANG_MODULE_CACHE_PATH=/private/tmp/beauty-clang-module-cache DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path BeautySDK --filter MissingLandmarkDegradationTests</automated>
    <automated>rg -n "eyeSize|eyeDistance|eyeYPosition|eyeTailLift|eye_landmarks_missing" BeautySDK/Sources/BeautyEffects BeautySDK/Tests/BeautyEffectsTests</automated>
  </verify>
  <done>Eye controls produce deterministic capped provider output and targeted missing-eye degradation.</done>
</task>

<task id="06-03-02" type="auto" tdd="true">
  <name>Task 2: Add capped nose geometry provider and engine fixture evidence</name>
  <files>
    BeautySDK/Sources/BeautyEffects/Warp/NoseWarpProvider.swift
    BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift
    BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
    BeautySDK/Sources/BeautyEffects/Render/BeautyGeometryEffectPipeline.swift
    BeautySDK/Tests/BeautyEffectsTests/NoseWarpProviderTests.swift
    BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
    BeautySDK/Tests/BeautyCoreTests/BeautyEngineTests.swift
  </files>
  <read_first>
    .planning/phases/06-core-beauty-effects/06-CONTEXT.md
    .planning/phases/06-core-beauty-effects/06-VALIDATION.md
    .planning/phases/06-core-beauty-effects/06-PATTERNS.md
    docs/06_beauty_parameters_spec.md
    docs/09_algorithm_effects_implementation.md
    BeautySDK/Sources/BeautyDetection/BeautyFaceObservation.swift
    BeautySDK/Sources/BeautyEffects/Warp/EyeWarpProvider.swift
    BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift
    BeautySDK/Sources/BeautyEffects/Render/BeautyGeometryEffectPipeline.swift
    BeautySDK/Tests/BeautyCoreTests/BeautyEngineTests.swift
  </read_first>
  <behavior>
    - `noseSlim` moves nose-side points toward the nose center.
    - `noseWingSlim` affects lower nose/wing points without moving eye landmarks.
    - `noseTipSize` affects the nose-tip region conservatively.
    - `noseBridge` affects upper bridge points conservatively and does not leak raw geometry.
    - Missing nose landmarks skip only the nose domain and keep unrelated safe effects active.
    - Combined face, eye, and nose geometry goes through the same conflict resolver before output.
  </behavior>
  <action>Implement `NoseWarpProvider` using normalized `BeautyFaceLandmarks.nose` points. Generate deterministic control points for slim, wing slim, tip size, and bridge behavior from fixture landmarks. Update `GeometryConflictResolver` so nose points participate in compound strength reduction when face-shape or eye controls overlap. Update engine/effect fixture tests to prove a non-zero nose control produces deterministic changed geometry output or render-plan evidence, while missing nose landmarks skip only the nose domain.</action>
  <acceptance_criteria>
    - `NoseWarpProviderTests` assert `noseSlim: 1` creates side points moving toward the nose center and effective strength is no greater than `0.35`.
    - Tests assert `noseWingSlim: 1` creates lower nose points and effective strength is no greater than `0.35`.
    - Tests assert `noseTipSize: 1` creates tip-region points and effective strength is no greater than `0.30`.
    - Tests assert `noseBridge: 1` creates upper bridge points and effective strength is no greater than `0.30`.
    - `MissingLandmarkDegradationTests` assert missing nose landmarks skip only `nose`; eye and color/filter domains remain active when their inputs are valid.
    - Engine or effect fixture tests assert non-zero nose controls produce changed output or changed render-plan evidence and include redacted warning/metric evidence when capped.
  </acceptance_criteria>
  <verify>
    <automated>CLANG_MODULE_CACHE_PATH=/private/tmp/beauty-clang-module-cache DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path BeautySDK --filter NoseWarpProviderTests</automated>
    <automated>CLANG_MODULE_CACHE_PATH=/private/tmp/beauty-clang-module-cache DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path BeautySDK --filter MissingLandmarkDegradationTests</automated>
    <automated>CLANG_MODULE_CACHE_PATH=/private/tmp/beauty-clang-module-cache DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path BeautySDK --filter BeautyEngineTests</automated>
    <automated>rg -n "noseSlim|noseWingSlim|noseTipSize|noseBridge|nose_landmarks_missing" BeautySDK/Sources/BeautyEffects BeautySDK/Tests/BeautyEffectsTests</automated>
  </verify>
  <done>Nose controls produce deterministic capped provider output and targeted missing-nose degradation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Eye/nose landmarks -> internal providers | Landmark geometry must remain internal and never become host-visible API. |
| Public feature sliders -> effective eye/nose geometry | High public values must be capped before control points are produced. |
| Partial landmark failure -> render domains | Missing eye or nose inputs must skip only affected domains and preserve safe unrelated effects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-06-03-01 | Information Disclosure | Eye and nose provider metadata | mitigate | Keep coordinates internal and scan warning/metric strings for raw geometry, Vision types, and paths. |
| T-06-03-02 | Tampering | Eye/nose strength | mitigate | Apply exact cap constants and conflict resolver before output. |
| T-06-03-03 | Denial of Service | Missing eye or nose groups | mitigate | Return skipped domain status and continue unrelated safe domains. |
| T-06-03-04 | Repudiation | Feature effectiveness claims | mitigate | Add deterministic provider and engine/effect fixture assertions for every eye and nose field. |
| T-06-03-SC | Tampering | Package installation surface | accept | No external package, downloaded model, or remote image-processing dependency is introduced. |
</threat_model>

<verification>
- `CLANG_MODULE_CACHE_PATH=/private/tmp/beauty-clang-module-cache DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path BeautySDK --filter EyeWarpProviderTests`
- `CLANG_MODULE_CACHE_PATH=/private/tmp/beauty-clang-module-cache DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path BeautySDK --filter NoseWarpProviderTests`
- `CLANG_MODULE_CACHE_PATH=/private/tmp/beauty-clang-module-cache DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path BeautySDK --filter MissingLandmarkDegradationTests`
- `CLANG_MODULE_CACHE_PATH=/private/tmp/beauty-clang-module-cache DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path BeautySDK --filter BeautyEngineTests`
- `rg -n "public .*EyeWarpProvider|public .*NoseWarpProvider|public .*landmark|public .*bounding" BeautySDK/Sources/BeautySDK BeautySDK/Sources/BeautyCore`
</verification>

<success_criteria>
- EFFECT-05 maps every eye field to tested SDK provider behavior.
- EFFECT-06 maps every nose field to tested SDK provider behavior.
- EFFECT-09 has cap, combined weakening, missing-landmark skip, stale/reused, warning, and metric evidence for eye and nose domains.
- Demo controls and categories remain unchanged; slider-disabled or warning-row UI is not introduced.
</success_criteria>

<output>
Create `.planning/phases/06-core-beauty-effects/06-03-SUMMARY.md` when done.
</output>
