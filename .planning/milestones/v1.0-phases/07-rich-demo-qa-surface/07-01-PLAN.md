---
phase: "07-rich-demo-qa-surface"
plan: "07-01"
status: "pending"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "BeautyDemo/BeautyDemo/State/BeautyParameterStore.swift"
  - "BeautyDemo/BeautyDemo/State/ParameterJSONCoding.swift"
  - "BeautyDemo/BeautyDemo/Editor/EditorShellView.swift"
  - "BeautyDemo/BeautyDemo/Editor/ParameterJSONSheetView.swift"
  - "BeautyDemo/BeautyDemoTests/BeautyParameterStoreTests.swift"
  - "BeautyDemo/BeautyDemoTests/ParameterJSONCodingTests.swift"
  - "BeautyDemo/BeautyDemoTests/BeautyDemoViewStateTests.swift"
  - "BeautyDemo/BeautyDemoTests/InputPipelinePrivacyTests.swift"
autonomous: true
requirements: ["DEMO-06"]
requirements_addressed: ["DEMO-06"]
must_haves:
  truths:
    - "D-01: parameter JSON import/export is copy/paste sheet UI only, with no file picker, share sheet, saved preset, or persistence scope."
    - "D-02: JSON payload uses `schemaVersion: 1` plus `parameters` based on `BeautyParameters: Codable`."
    - "D-03: import decodes and validates into preview state before Apply can mutate `BeautyParameterStore`."
    - "D-04: invalid JSON, unsupported schema, oversized JSON, invalid parameters, and unknown filter ID leave current parameters, selected filter, selected preset, compare state, and sliders unchanged."
    - "D-05: export emits only deterministic `schemaVersion` plus `parameters` and excludes timestamps, build metadata, preset labels, source labels, detection summaries, debug metrics, paths, and logs."
    - "D-06 and D-07: single reset and reset all return to SDK zero/default values, with reset all clearing filter, preset, and imported-source state."
    - "D-08 and D-09: imported JSON applies as a custom/imported snapshot, deselects preset chips, and any manual slider or filter edit returns source state to custom."
    - "D-10: existing reset labels may stay unchanged, but tests must prove imported JSON follows the same source-clearing and reset rules."
    - "D-22 and D-23: concrete type and test names may vary, but JSON, source, and test behavior must remain deterministic and facade-only."
  artifacts:
    - "Demo-side `ParameterJSONEnvelope` or equivalent with `schemaVersion` and `parameters` only."
    - "Demo-side `ParameterJSONCoding` or equivalent with 64 KB size limit, deterministic export, preview decode, friendly errors, and facade validation."
    - "`BeautyParameterSource` or equivalent source state covering custom, preset, and imported snapshots."
    - "`BeautyParameterStore.applyImportedParameters(_:)` or equivalent store method that synchronizes sliders, filter, selected preset, and source."
    - "A preview-surface `Parameter JSON` entry and copy/paste sheet with Import and Export modes."
    - "Focused XCTest coverage for JSON round-trip, failed-import non-mutation, unknown filter IDs, and reset/source semantics."
  key_links:
    - "JSON sheet pasted text -> `ParameterJSONCoding.previewImport` -> `BeautySDKResources.validate(parameters:)` -> preview state -> explicit Apply -> `BeautyParameterStore`."
    - "`BeautyParameterStore.parametersSnapshot` -> deterministic JSON export -> import preview -> same `BeautyParameters` value after apply."
    - "Preset/import/manual/reset transitions -> selected chip/filter state -> SDK zero/default reset behavior."
---

# Plan 07-01: Add Preset, Reset, and Parameter JSON Workflows

## Phase Goal

**As a** Demo QA user, **I want to** copy, paste, preview, apply, reset, and round-trip parameter snapshots safely, **so that** `DEMO-06` is covered without turning the Demo into a file manager or custom-preset product.

<objective>
Implement Phase 7 parameter JSON and source/reset semantics in the Demo.

Purpose: Phase 7 must make preset/reset/JSON workflows deterministic and testable through the public `BeautySDK` facade while preserving current Demo state when import fails.
Output: Demo-side JSON envelope/coding state, imported/preset/custom source semantics in `BeautyParameterStore`, a copy/paste JSON sheet, and focused XCTest/privacy evidence.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@ARCHITECTURE.md
@DESIGN.md
@FRONTEND.md
@SECURITY.md
@RELIABILITY.md
@PRODUCT_SENSE.md
@QUALITY_SCORE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-rich-demo-qa-surface/07-CONTEXT.md
@.planning/phases/07-rich-demo-qa-surface/07-RESEARCH.md
@.planning/phases/07-rich-demo-qa-surface/07-VALIDATION.md
@.planning/phases/07-rich-demo-qa-surface/07-UI-SPEC.md
@.planning/phases/07-rich-demo-qa-surface/07-PATTERNS.md
@BeautyDemo/BeautyDemo/State/BeautyParameterStore.swift
@BeautyDemo/BeautyDemo/Editor/EditorShellView.swift
@BeautyDemo/BeautyDemo/Panel/BeautyControlDescriptor.swift
@BeautyDemo/BeautyDemo/Panel/BeautyPanelView.swift
@BeautyDemo/BeautyDemo/Panel/BeautyResourcePickerModels.swift
@BeautyDemo/BeautyDemoTests/BeautyParameterStoreTests.swift
@BeautyDemo/BeautyDemoTests/BeautyDemoViewStateTests.swift
@BeautyDemo/BeautyDemoTests/InputPipelinePrivacyTests.swift
@BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift
@BeautySDK/Sources/BeautyCore/Models/BeautyPreset.swift
@BeautySDK/Sources/BeautySDK/BeautySDKResources.swift
</context>

## Artifacts this phase produces

- `ParameterJSONEnvelope`: Demo-side envelope with `schemaVersion: Int` and `parameters: BeautyParameters`.
- `ParameterJSONCoding`: Demo-side coder with `supportedSchemaVersion = 1`, `maxPayloadBytes = 65536`, deterministic export, import preview, and facade-backed filter validation.
- `ParameterJSONImportState`: value state for empty/editing, preview, and failed import modes.
- `ParameterJSONImportError` or equivalent: redacted cases for invalid JSON, unsupported schema, oversized payload, validation failure, and unavailable filter.
- `BeautyParameterSource`: `custom`, `preset(id:)`, and `imported` or equivalent source state.
- `BeautyParameterStore.applyImportedParameters(_:)`: imported apply path that syncs all display values, filter selection, parameter snapshot, selected preset, and source.
- `ParameterJSONSheetView` or equivalent SwiftUI sheet using exact copy from `07-UI-SPEC.md`.

## Source Coverage Audit

| Source | ID | Feature / Requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | - | Preset/reset/JSON workflows | 07-01 | COVERED | Store, sheet, and tests. |
| REQ | DEMO-06 | Preset selection, single reset, reset all, JSON import/export | 07-01 | COVERED | Primary plan scope. |
| CONTEXT | D-01 | Copy/paste JSON sheet only | 07-01 | COVERED | No file/share/persistence. |
| CONTEXT | D-02 | Versioned envelope | 07-01 | COVERED | `schemaVersion` plus `parameters`. |
| CONTEXT | D-03 D-04 | Preview-before-apply and failed import non-mutation | 07-01 | COVERED | Import state and store tests. |
| CONTEXT | D-05 | Minimal deterministic export | 07-01 | COVERED | Export tests and privacy scan. |
| CONTEXT | D-06 D-07 | Reset semantics | 07-01 | COVERED | Store tests. |
| CONTEXT | D-08 D-09 D-10 | Imported/custom/preset source clearing and reset-label coverage | 07-01 | COVERED | Source enum and store tests. |
| CONTEXT | D-22 D-23 | Names/test organization discretionary | 07-01 | COVERED | Concrete examples, not mandatory names. |
| UI-SPEC | - | JSON copy, sheet interaction, 64 KB limit, accessibility | 07-01 | COVERED | Sheet state and tests. |
| RESEARCH | - | Use `BeautySDKResources.validate(parameters:)` | 07-01 | COVERED | Public facade validation. |

<tasks>

<task id="07-01-01" type="auto" tdd="true">
  <name>Task 1: Add deterministic parameter JSON coding</name>
  <files>
    BeautyDemo/BeautyDemo/State/ParameterJSONCoding.swift
    BeautyDemo/BeautyDemoTests/ParameterJSONCodingTests.swift
    BeautyDemo/BeautyDemoTests/InputPipelinePrivacyTests.swift
  </files>
  <read_first>
    .planning/phases/07-rich-demo-qa-surface/07-CONTEXT.md
    .planning/phases/07-rich-demo-qa-surface/07-RESEARCH.md
    .planning/phases/07-rich-demo-qa-surface/07-VALIDATION.md
    .planning/phases/07-rich-demo-qa-surface/07-UI-SPEC.md
    .planning/phases/07-rich-demo-qa-surface/07-PATTERNS.md
    BeautySDK/Sources/BeautyCore/Models/BeautyParameters.swift
    BeautySDK/Sources/BeautyCore/Models/BeautyPreset.swift
    BeautySDK/Sources/BeautySDK/BeautySDKResources.swift
    BeautyDemo/BeautyDemoTests/InputPipelinePrivacyTests.swift
  </read_first>
  <behavior>
    - Export serializes `ParameterJSONEnvelope(schemaVersion: 1, parameters: currentSnapshot)`.
    - Export output is deterministic with sorted keys and includes no keys except `schemaVersion` and `parameters`.
    - Import rejects payloads larger than 65,536 UTF-8 bytes before decoding.
    - Import rejects unsupported `schemaVersion` before presenting preview state.
    - Import validates decoded parameters through `BeautySDKResources.validate(parameters:)` before preview or Apply.
    - Unknown `filterId` values fail as unavailable-filter import errors, not as paths or URLs.
    - Error values expose stable redacted user copy only; they do not store raw payload text.
  </behavior>
  <action>Create a Demo-side JSON coder in `BeautyDemo/BeautyDemo/State/ParameterJSONCoding.swift`. Define an envelope with exact keys `schemaVersion` and `parameters`, supported schema version `1`, and `maxPayloadBytes = 65536`. Add export using `JSONEncoder` with `.sortedKeys` and no timestamp/build/source/debug fields. Add import preview that checks byte size, decodes the envelope, rejects unsupported schema, validates the candidate through `BeautySDKResources.validate(parameters:)`, and returns a preview result without mutating `BeautyParameterStore`. Map failures to copy from `07-UI-SPEC.md`: invalid JSON, unsupported schema, unknown filter, oversized JSON, and current-settings-unchanged language. Add tests for deterministic export, exact top-level keys, round-trip equality, oversized rejection, unsupported schema rejection, invalid JSON rejection, unknown `filterId`, and no raw payload retention.</action>
  <acceptance_criteria>
    - `ParameterJSONCodingTests` assert exported normalized JSON has top-level keys exactly `parameters` and `schemaVersion`.
    - Tests assert two exports of the same `BeautyParameters` value produce identical strings.
    - Tests assert export then preview-import returns the same `BeautyParameters` value after `BeautySDKResources.validate(parameters:)`.
    - Tests assert a 65,537-byte payload fails before decode with oversized copy `Parameter JSON is too large. Paste a smaller parameter payload.`
    - Tests assert `schemaVersion: 2` fails with unsupported schema copy.
    - Tests assert malformed JSON fails with invalid JSON copy and no raw JSON echo.
    - Tests assert `parameters.filterId = "missing_filter"` fails before preview/apply with unknown filter copy and does not interpret the ID as a path or URL.
    - `InputPipelinePrivacyTests` scans the new JSON code for `URLSession`, `http://`, `https://`, `upload`, `DocumentPicker`, `fileImporter`, `fileExporter`, `rawPresetJson`, `/private/var`, and `NSError` in active surfaces.
  </acceptance_criteria>
  <verify>
    <automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project BeautyDemo/BeautyDemo.xcodeproj -scheme BeautyDemo -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' test -only-testing:BeautyDemoTests/ParameterJSONCodingTests -only-testing:BeautyDemoTests/InputPipelinePrivacyTests</automated>
    <automated>rg -n "URLSession|http://|https://|upload|DocumentPicker|fileImporter|fileExporter|rawPresetJson|/private/var|NSError" BeautyDemo/BeautyDemo/State BeautyDemo/BeautyDemo/Editor BeautyDemo/BeautyDemoTests</automated>
  </verify>
  <done>JSON import/export has deterministic, size-limited, facade-validated, redacted model coverage.</done>
</task>

<task id="07-01-02" type="auto" tdd="true">
  <name>Task 2: Add imported/preset/custom source and reset semantics</name>
  <files>
    BeautyDemo/BeautyDemo/State/BeautyParameterStore.swift
    BeautyDemo/BeautyDemoTests/BeautyParameterStoreTests.swift
  </files>
  <read_first>
    .planning/phases/07-rich-demo-qa-surface/07-CONTEXT.md
    .planning/phases/07-rich-demo-qa-surface/07-RESEARCH.md
    .planning/phases/07-rich-demo-qa-surface/07-VALIDATION.md
    .planning/phases/07-rich-demo-qa-surface/07-UI-SPEC.md
    .planning/phases/07-rich-demo-qa-surface/07-PATTERNS.md
    BeautyDemo/BeautyDemo/State/BeautyParameterStore.swift
    BeautyDemo/BeautyDemo/Panel/BeautyControlDescriptor.swift
    BeautyDemo/BeautyDemo/Panel/BeautyResourcePickerModels.swift
    BeautyDemo/BeautyDemoTests/BeautyParameterStoreTests.swift
  </read_first>
  <behavior>
    - Initial parameter source is custom.
    - Applying a preset sets source to preset ID and keeps `selectedPresetId == preset.id`.
    - Applying imported JSON sets source to imported and clears `selectedPresetId`.
    - Manual slider change sets source to custom and clears `selectedPresetId`.
    - Manual filter selection sets source to custom and clears `selectedPresetId`.
    - Single reset sets source to custom and only resets that control to SDK zero/default.
    - Reset all sets source to custom, clears selected filter, clears selected preset, and returns `parametersSnapshot == BeautyParameters()`.
  </behavior>
  <action>Add a small `BeautyParameterSource: Equatable, Sendable` model in `BeautyParameterStore.swift`, with cases equivalent to `custom`, `preset(id: String)`, and `imported`. Add `@Published private(set) var parameterSource` initialized to custom. Refactor private parameter application so `applyPreset(_:)` and `applyImportedParameters(_:)` can synchronize all display values and selected filter without accidentally flipping source state during intermediate setter calls. Add `applyImportedParameters(_ parameters: BeautyParameters)` and ensure it writes normalized display values, selected filter, filter intensity, clears selected preset, sets imported source, and keeps status idle. Update `setDisplayValue`, `selectFilter`, `reset`, and `resetAll` to set source custom exactly when user-driven changes occur. Preserve existing reset labels and normal quiet status.</action>
  <acceptance_criteria>
    - Tests assert new store starts with source custom, no selected preset, no selected filter, and `parametersSnapshot == BeautyParameters()`.
    - Tests assert applying each built-in preset sets source preset with the preset ID and selected preset ID.
    - Tests assert imported parameters with `skinSmoothing = 0.42`, `brightness = -0.2`, `filterId = "soft_clean"`, and `filterIntensity = 0.35` sync display values `42`, `-20`, selected filter `"soft_clean"`, and source imported.
    - Tests assert imported apply clears any previously selected preset ID.
    - Tests assert manual slider edit after preset or import changes source to custom and leaves unrelated display values unchanged.
    - Tests assert manual filter change after preset or import changes source to custom.
    - Tests assert single reset after import resets only the target control to `0`, keeps unrelated imported values, keeps existing `Reset {Control Label}` copy, and sets source custom.
    - Tests assert reset all after preset or import clears filter, clears preset, sets source custom, and restores `BeautyParameters()`.
  </acceptance_criteria>
  <verify>
    <automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project BeautyDemo/BeautyDemo.xcodeproj -scheme BeautyDemo -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' test -only-testing:BeautyDemoTests/BeautyParameterStoreTests</automated>
  </verify>
  <done>Preset, imported, custom, single reset, reset all, and filter transitions are explicit and covered.</done>
</task>

<task id="07-01-03" type="auto" tdd="true">
  <name>Task 3: Wire the preview-surface Parameter JSON sheet</name>
  <files>
    BeautyDemo/BeautyDemo/Editor/EditorShellView.swift
    BeautyDemo/BeautyDemo/Editor/ParameterJSONSheetView.swift
    BeautyDemo/BeautyDemo/State/ParameterJSONCoding.swift
    BeautyDemo/BeautyDemo/State/BeautyParameterStore.swift
    BeautyDemo/BeautyDemoTests/BeautyDemoViewStateTests.swift
    BeautyDemo/BeautyDemoTests/ParameterJSONCodingTests.swift
    BeautyDemo/BeautyDemoTests/InputPipelinePrivacyTests.swift
  </files>
  <read_first>
    .planning/phases/07-rich-demo-qa-surface/07-CONTEXT.md
    .planning/phases/07-rich-demo-qa-surface/07-RESEARCH.md
    .planning/phases/07-rich-demo-qa-surface/07-VALIDATION.md
    .planning/phases/07-rich-demo-qa-surface/07-UI-SPEC.md
    .planning/phases/07-rich-demo-qa-surface/07-PATTERNS.md
    BeautyDemo/BeautyDemo/Editor/EditorShellView.swift
    BeautyDemo/BeautyDemo/Editor/ImageEditorPipeline.swift
    BeautyDemo/BeautyDemo/Camera/CameraBeautyPipeline.swift
    BeautyDemo/BeautyDemo/Panel/BeautyPanelView.swift
    BeautyDemo/BeautyDemoTests/BeautyDemoViewStateTests.swift
    BeautyDemo/BeautyDemoTests/InputPipelinePrivacyTests.swift
  </read_first>
  <behavior>
    - One preview toolbar entry labeled `Parameter JSON` opens the JSON sheet.
    - Sheet presentation uses one enum-driven sheet state such as `.parameterJSON`, not parallel booleans.
    - Sheet title is `Parameter JSON` and modes are `Import` and `Export`.
    - Import entry uses `Paste parameter JSON`, `Preview Parameter JSON`, and `Apply Imported Parameters`.
    - Export entry uses `Copy Parameter JSON` and exports from `parameterStore.parametersSnapshot`.
    - Apply is the only JSON action that mutates parameters.
    - Dismiss, cancel, invalid preview, and mode switching leave current parameters unchanged.
    - Applying valid imported JSON reprocesses the latest photo with new parameters when a photo input exists; camera uses the new snapshot on subsequent frames.
  </behavior>
  <action>Add a compact preview toolbar around the existing compare button without changing `Show Before` or `Show After`. Introduce a single sheet enum in `EditorShellView`, for example `EditorSheet: Identifiable { case parameterJSON }`, and present `ParameterJSONSheetView` from it. Build the sheet from local SwiftUI controls using 16 px padding, 8 px row gaps, 13 px labels, 20 px heading, 44 px minimum button targets, and the approved blue accent for primary actions. Wire export to `ParameterJSONCoding.export(parameters: parameterStore.parametersSnapshot)`. Wire import preview to the coder result and wire Apply to `parameterStore.applyImportedParameters(candidate)`. After Apply, if `selectedInputMode == .photo`, call `imageEditorPipeline.reprocessLatest(parameters: parameterStore.parametersSnapshot)`; do not create a new image/file import path. Keep JSON actions available as a QA utility while keeping compare/debug preview controls display-only.</action>
  <acceptance_criteria>
    - `BeautyDemoViewStateTests` or equivalent state tests assert preview toolbar actions include `Parameter JSON` and preserve compare labels `Show Before` / `Show After`.
    - Tests assert the sheet copy contains `Parameter JSON`, `Import`, `Export`, `Paste parameter JSON`, `Preview Parameter JSON`, `Apply Imported Parameters`, and `Copy Parameter JSON`.
    - Tests assert invalid preview result copy includes `Current settings stay unchanged.`
    - Tests assert Apply is unavailable until a valid preview candidate exists.
    - Tests assert applying a valid candidate calls the imported apply path and clears selected preset source.
    - Privacy scan asserts active JSON sheet code does not contain file importer/exporter, share sheet, network/upload, raw path, raw framework error, or raw debug dump scope.
  </acceptance_criteria>
  <verify>
    <automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project BeautyDemo/BeautyDemo.xcodeproj -scheme BeautyDemo -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' test -only-testing:BeautyDemoTests/BeautyDemoViewStateTests -only-testing:BeautyDemoTests/ParameterJSONCodingTests -only-testing:BeautyDemoTests/InputPipelinePrivacyTests</automated>
    <automated>rg -n "Parameter JSON|Preview Parameter JSON|Apply Imported Parameters|Copy Parameter JSON|Current settings stay unchanged" BeautyDemo/BeautyDemo BeautyDemo/BeautyDemoTests</automated>
  </verify>
  <done>The Demo exposes copy/paste parameter JSON as a preview-before-apply sheet with deterministic state and no file/network scope creep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Pasted JSON -> Demo parser | User-provided JSON is untrusted and must be size-limited, schema-checked, decoded, and facade-validated before preview/apply. |
| JSON preview -> current parameter store | Preview state must not mutate current parameters until explicit Apply. |
| Exported parameters -> clipboard/manual copy | Export must contain only parameter preference data and no image, detection, debug, path, source, timestamp, or build metadata. |
| Resource IDs -> facade validation | `filterId` is an identifier only; it must not be interpreted as a path, URL, or dynamic resource. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-07-01-01 | Tampering | JSON import apply | mitigate | Decode to preview first; Apply is the only mutating action; failed import tests assert store snapshots remain unchanged. |
| T-07-01-02 | Information Disclosure | JSON export and errors | mitigate | Export only `schemaVersion` and `parameters`; error copy does not echo raw JSON, paths, framework strings, or debug metrics. |
| T-07-01-03 | Denial of Service | Pasted JSON size | mitigate | Reject payloads over 65,536 UTF-8 bytes before decoding. |
| T-07-01-04 | Spoofing/Tampering | Unknown filter IDs | mitigate | Validate through `BeautySDKResources.validate(parameters:)` and reject unavailable filters before preview/apply. |
| T-07-01-SC | Supply Chain | UI dependencies | accept | Native SwiftUI only; no registry, remote component, or package dependency is introduced. |
</threat_model>

<verification>
- `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project BeautyDemo/BeautyDemo.xcodeproj -scheme BeautyDemo -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' test -only-testing:BeautyDemoTests/BeautyParameterStoreTests -only-testing:BeautyDemoTests/ParameterJSONCodingTests -only-testing:BeautyDemoTests/BeautyDemoViewStateTests -only-testing:BeautyDemoTests/InputPipelinePrivacyTests`
- `rg -n "import Beauty(Core|Detection|Effects|Render|Resources)" BeautyDemo/BeautyDemo BeautyDemo/BeautyDemoTests`
- `rg -n "URLSession|http://|https://|upload|DocumentPicker|fileImporter|fileExporter|rawPresetJson|/private/var|NSError" BeautyDemo/BeautyDemo/State BeautyDemo/BeautyDemo/Editor BeautyDemo/BeautyDemoTests`
</verification>

<success_criteria>
- `DEMO-06` workflows are implemented through copy/paste JSON, built-in preset selection, single reset, and reset all.
- JSON import previews before Apply and every failure path leaves current parameters unchanged.
- Export is deterministic and minimal.
- Preset/import/custom source transitions are explicit and tested.
- Demo remains facade-only and local-first.
</success_criteria>

<output>
After completion, create `.planning/phases/07-rich-demo-qa-surface/07-01-SUMMARY.md`.
</output>
