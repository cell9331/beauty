---
phase: 74-cpu-gpu-parity-and-sdk-only-closeout
plan: 03
type: execute
wave: 2
depends_on:
  - 74-01
files_modified:
  - BeautySDK/Tests/BeautyEffectsTests/BeautyBackendDeterminismParityTests.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyBackendSelectionConcurrencyTests.swift
autonomous: true
requirements:
  - PARITY-03
must_haves:
  truths:
    - "Repeated identical generated requests return byte-identical finite output and identical bounded diagnostics for every available backend."
    - "Bounded concurrent and interleaved CPU/GPU requests retain request-local policy and do not leak prior-request state into later output."
    - "Unavailable Metal is an explicit terminal classification that never enters the GPU parity denominator or lends success to a CPU result."
  artifacts:
    - path: "BeautySDK/Tests/BeautyEffectsTests/BeautyBackendDeterminismParityTests.swift"
      provides: "Repeated-request and bounded concurrent backend output determinism coverage"
      contains: "BeautyBackendDeterminismParityTests"
    - path: "BeautySDK/Tests/BeautyCoreTests/BeautyBackendSelectionConcurrencyTests.swift"
      provides: "Public configuration request-local policy and explicit unavailable-host separation coverage"
      contains: "BeautyBackendSelectionConcurrencyTests"
  key_links:
    - from: "BeautySDK/Tests/BeautyEffectsTests/BeautyBackendDeterminismParityTests.swift"
      to: "BeautySDK/Sources/BeautyEffects/Backend/BeautyCPUBackend.swift"
      via: "repeated generated request comparison against the CPU oracle"
      pattern: "BeautyCPUBackend"
    - from: "BeautySDK/Tests/BeautyCoreTests/BeautyBackendSelectionConcurrencyTests.swift"
      to: "BeautySDK/Sources/BeautySDK/BeautyBackendFactory.swift"
      via: "closed public configuration policy and injected terminal Metal factory"
      pattern: "BeautyBackendFactory"
---

<objective>
Prove deterministic, finite, request-local backend behavior under repetition,
bounded concurrency, and explicit unavailable-host classification.

Purpose: Numeric parity from one serial request is insufficient if state or policy
leaks across requests; configuration evidence must also stay separate from GPU
success when Metal is unavailable.
Output: Generated backend determinism tests and public selection/concurrency tests.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@BeautySDK/Tests/BeautyEffectsTests/BeautyBackendParityFixtureFactory.swift
@BeautySDK/Tests/BeautyCoreTests/BeautyEngineBackendRoutingTests.swift
@BeautySDK/Tests/BeautyCoreTests/CPUReferenceDeterminismTests.swift
@BeautySDK/Sources/BeautySDK/BeautyBackendFactory.swift
@BeautySDK/Sources/BeautySDK/BeautyEngine.swift
@BeautySDK/Sources/BeautyEffects/Backend/BeautyBackendContract.swift
@BeautySDK/Sources/BeautyRender/BeautyMetalRuntime.swift

Use bounded XCTest expectations, actors/locks, or task groups; never use arbitrary
sleeps, global caches, process-wide mutable backend state, or device/UI lifecycle.
The CPU remains the oracle. An unavailable GPU construction must be asserted as
typed `.metalUnavailable` and excluded from parity success, not converted to a
skip or CPU fallback.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add repeated and concurrent available-backend determinism tests</name>
  <files>BeautySDK/Tests/BeautyEffectsTests/BeautyBackendDeterminismParityTests.swift</files>
  <action>Render a matrix of generated neutral, color, geometry, and composed-retouch requests repeatedly through `BeautyCPUBackend` and, when available, one `BeautyMetalBackend`. Assert byte-identical output, identical dimensions/extent/alpha/metadata, identical bounded diagnostic aggregates, finite values, and zero active resources after each request. Add a bounded `withTaskGroup`/actor test that interleaves distinct generated requests and CPU/Metal policies, collects results by request ID, and compares each result only with its own serial baseline; assert no previous request's bytes, extent, or diagnostics appear in another request. Keep the test available-host branch explicit and aggregate-only, with no `XCTSkip` and no unbounded concurrency.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter 'BeautyEffectsTests.BeautyBackendDeterminismParityTests'</automated>
    <automated>swift test --package-path BeautySDK --filter 'BeautyEffectsTests.BeautyBackendDeterminismParityTests|BeautyCoreTests.CPUReferenceDeterminismTests'</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>Repeated serial requests and bounded mixed-backend concurrency are deterministic, finite, request-local, and resource-clean for each available backend, with CPU retained as the reference.</done>
</task>

<task type="auto">
  <name>Task 2: Prove public request-local selection and unavailable separation</name>
  <files>BeautySDK/Tests/BeautyCoreTests/BeautyBackendSelectionConcurrencyTests.swift</files>
  <action>Exercise `BeautyConfiguration(renderBackend: .cpu/.gpu)` through the package-only factory and public engine injection seam with bounded interleaved still-image and pixel-buffer requests, asserting every request carries its immutable selected policy and no CPU/GPU executor call is substituted, retried, or shared across engines. Add a terminal factory case that throws `.metalUnavailable`, assert the typed error and zero successful GPU output, and keep a distinct aggregate marker path for available versus unavailable host classification. Do not merge unavailable into the generated parity denominator, do not let it claim CPU parity, and do not add a public testing hook or alter the two-case configuration contract.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter 'BeautyCoreTests.BeautyBackendSelectionConcurrencyTests'</automated>
    <automated>swift test --package-path BeautySDK --filter 'BeautyCoreTests.BeautyEngineBackendRoutingTests|BeautyCoreTests.BeautyBackendSelectionConcurrencyTests'</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>Public selection remains immutable and request-local under bounded interleaving, while typed unavailable Metal is terminal and explicitly excluded from GPU parity success.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Configuration → executor selection | Public `.cpu`/`.gpu` policy selects exactly one request-local executor. |
| Concurrent request → aggregate evidence | Multiple requests may run concurrently, but only request-ID keyed bounded results are compared. |
| Host availability → parity accounting | Available and unavailable classifications are separate trust outcomes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-74-07 | Tampering | repeated/concurrent backend state | mitigate | Compare each request to its serial baseline, require exact diagnostics/metadata, and assert resource cleanup. |
| T-74-08 | Spoofing | unavailable-host classification | mitigate | Assert typed `.metalUnavailable`, zero GPU success, and separate availability counters; never treat unavailability as a parity pass. |
| T-74-09 | Denial of Service | concurrent test scheduling | mitigate | Bound task count, fixture size, and expectations; avoid arbitrary sleeps and process-wide state. |
| T-74-SC | Tampering | package installs | accept | No package installation or external dependency is introduced. |
</threat_model>

<verification>
Run both new focused suites and existing routing/determinism tests. Verify the
available/unavailable branches are explicit and that the tests contain no sleeps,
file/media access, raw durable output, or CPU fallback path.
</verification>

<success_criteria>
Identical requests are deterministic and finite for each available backend,
bounded concurrency is request-local, and unavailable Metal remains a typed,
separate non-success classification.
</success_criteria>

<output>
Create `.planning/phases/74-cpu-gpu-parity-and-sdk-only-closeout/74-03-SUMMARY.md` when done.
</output>
