---
phase: 73-public-backend-configuration-and-fail-closed-availability
plan: 04
type: execute
wave: 4
depends_on:
  - 73-03
files_modified:
  - ARCHITECTURE.md
  - DESIGN.md
  - SECURITY.md
  - RELIABILITY.md
  - PRODUCT_SENSE.md
  - QUALITY_SCORE.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/STACK.md
  - .planning/codebase/TESTING.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - PLANS.md
autonomous: true
requirements:
  - CONFIG-01
  - CONFIG-02
must_haves:
  truths:
    - "Current owner documents agree that CPU is the permanent reference, GPU is explicitly selectable, unavailable GPU is typed fail-closed, and UI/Demo/device/release claims remain excluded."
    - "Phase 73 planning ledgers contain the actual gate/test evidence and point to Phase 74 parity work without rewriting historical Phase 70–72 evidence."
  artifacts:
    - path: "ARCHITECTURE.md"
      provides: "Current public backend ownership and target dependency contract"
    - path: "DESIGN.md"
      provides: "Configuration field/default and fail-closed policy contract"
    - path: "SECURITY.md"
      provides: "Configuration/Metal capability trust boundary and no-fallback mitigation"
    - path: "RELIABILITY.md"
      provides: "Typed unavailable behavior and request-local selection/recovery contract"
    - path: "PRODUCT_SENSE.md"
      provides: "SDK integrator journey and explicit CPU/GPU acceptance boundary"
    - path: "QUALITY_SCORE.md"
      provides: "Measured Phase 73 test/gate evidence and current inventory"
    - path: ".planning/ROADMAP.md"
      provides: "Four-plan Phase 73 plan list and completed progress row"
  key_links:
    - from: "ARCHITECTURE.md"
      to: "BeautySDK/Sources/BeautySDK/BeautyBackendFactory.swift"
      via: "documented ownership of public policy to package executor selection"
      pattern: "BeautyBackendFactory"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/73-public-backend-configuration-and-fail-closed-availability/73-03-SUMMARY.md"
      via: "CONFIG-01/CONFIG-02 evidence traceability"
      pattern: "CONFIG-0[12]"
---

<objective>
Synchronize the current SDK owners and planning ledgers after the Phase 73 gate has passed.

Purpose: Make the new public contract durable and unambiguous while preserving historical evidence and keeping parity/closeout in Phase 74.
Output: Updated owner documents, codebase maps, project ledgers, and phase completion traceability.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@PLANS.md
@ARCHITECTURE.md
@DESIGN.md
@SECURITY.md
@RELIABILITY.md
@PRODUCT_SENSE.md
@QUALITY_SCORE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STACK.md
@.planning/codebase/TESTING.md

Only current owner sections may change; phase-qualified historical records remain intact. The full wrapper counts must come from Plan 03's fresh run, not estimates.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Synchronize current SDK contract owners</name>
  <files>ARCHITECTURE.md, DESIGN.md, SECURITY.md, RELIABILITY.md, PRODUCT_SENSE.md, QUALITY_SCORE.md, .planning/codebase/ARCHITECTURE.md, .planning/codebase/STACK.md, .planning/codebase/TESTING.md</files>
  <action>Update only current owner/map sections to state that BeautyCore owns the public BeautyRenderBackend/configuration field; BeautySDK owns immutable factory selection and policy propagation; CPU remains the permanent reference; explicit GPU construction uses the package Metal runtime; missing/legacy configuration defaults to CPU; .metalUnavailable is terminal with no CPU fallback; and package-only injection is test-only. Update the configuration inventory to 11 fields while preserving the 61 parameter/five preset/74 renderer inventories. Record the Phase 73 gate ordering and the measured focused/full counts from Plan 03. Keep historical Phase 70–72 statements phase-qualified and retain all exclusions for UI/Demo, simulator/device, performance, commercial, packaging, shipping, launch, and release readiness.</action>
  <verify>
    <automated>git diff --check &amp;&amp; bash scripts/check-sdk-only-boundary.sh --post-archive</automated>
  </verify>
  <done>All nine current contract owners/maps describe the implemented selector and fail-closed policy without stale current claims or historical evidence rewrites.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize Phase 73 planning and requirements ledgers</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, PLANS.md</files>
  <action>Update the current milestone/project position, mark CONFIG-01 and CONFIG-02 complete only against the Plan 03 full wrapper and public GPU integration evidence, list all four Phase 73 plans with their actual status, and point the next position to Phase 74 generated parity/closeout. Record the exact focused/live/full aggregate counts, explicit metal_available/metal_unavailable classifications, and zero-failure/zero-skip policy without persisting raw output. Preserve historical milestone ledgers and all SDK-only/non-release nonclaims. Do not mark Phase 74 requirements complete or claim CPU/GPU parity from configuration evidence alone.</action>
  <verify>
    <automated>bash scripts/run-no-skip-swiftpm.sh &amp;&amp; git diff --check</automated>
  </verify>
  <done>Planning and requirement ledgers are internally consistent, Phase 73 has traceable CONFIG-01/CONFIG-02 evidence, and the final full no-skip wrapper still passes after documentation changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Implementation → current owner docs | Contract text must reflect the tested public selector and terminal availability behavior. |
| Current ledgers → historical evidence | Phase-qualified updates must not rewrite immutable prior-phase claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-73-11 | Spoofing | current vs historical owner records | mitigate | Update only current sections, keep Phase 70–72 evidence phase-qualified, and cross-link CONFIG-01/02 to fresh summaries. |
| T-73-12 | Tampering | requirements/state ledgers | mitigate | Mark completion only after the full wrapper and public GPU integration pass; preserve Phase 74 as pending. |
| T-73-13 | Information Disclosure | durable docs/ledgers | mitigate | Persist aggregate counts and error classifications only; exclude pixels, support, paths, framework objects, and fixture locators. |
| T-73-SC | Tampering | package installs | accept | No package installation or external dependency is introduced by this plan. |
</threat_model>

<verification>
Run the full bash scripts/run-no-skip-swiftpm.sh after owner and ledger edits, then run bash scripts/check-sdk-only-boundary.sh --post-archive and git diff --check.
</verification>

<success_criteria>
Phase 73 closes CONFIG-01 and CONFIG-02 with current owners/ledgers synchronized to measured evidence, while Phase 74 parity and all excluded UI/device/release scopes remain pending or excluded.
</success_criteria>

<output>
Create .planning/phases/73-public-backend-configuration-and-fail-closed-availability/73-04-SUMMARY.md when done.
</output>
