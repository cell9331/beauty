---
phase: 73-public-backend-configuration-and-fail-closed-availability
plan: 03
type: execute
wave: 3
depends_on:
  - 73-02
files_modified:
  - scripts/check-backend-configuration.sh
  - scripts/run-no-skip-swiftpm.sh
autonomous: true
requirements:
  - CONFIG-01
  - CONFIG-02
must_haves:
  truths:
    - "The archive-first SDK-owned gate validates public backend configuration before the full SwiftPM child and rejects selector/default/fallback drift through mutation checks."
    - "The mandatory no-skip wrapper runs configuration, runtime, CPU-reference, consumer, and SwiftPM checks with zero failures and zero unexpected skips."
    - "The gate connects the public GPU integration case with explicit available/unavailable Metal classification rather than treating an unavailable host as a skip."
  artifacts:
    - path: "scripts/check-backend-configuration.sh"
      provides: "Static, mutation, focused-test, and aggregate availability gate for Phase 73"
      contains: "BeautyRenderBackend"
    - path: "scripts/run-no-skip-swiftpm.sh"
      provides: "Archive-first ordering with the Phase 73 configuration gate"
      contains: "check-backend-configuration.sh"
  key_links:
    - from: "scripts/run-no-skip-swiftpm.sh"
      to: "scripts/check-backend-configuration.sh"
      via: "exactly-once archive-first invocation before consumer/oracle/opt-ins/child"
      pattern: "check-backend-configuration.sh"
    - from: "scripts/check-backend-configuration.sh"
      to: "BeautySDK/Tests/BeautyCoreTests/BeautyEngineBackendRoutingTests.swift"
      via: "focused SwiftPM routing and public GPU integration coverage"
      pattern: "BeautyEngineBackendRoutingTests"
    - from: "scripts/check-backend-configuration.sh"
      to: "scripts/check-metal-runtime.sh"
      via: "separate live metal_available/metal_unavailable classification"
      pattern: "check-metal-runtime.sh"
---

<objective>
Add the archive-first SDK-owned preflight for Phase 73 public backend configuration and wire it into the mandatory no-skip pipeline.

Purpose: Make selector/default/fallback drift fail before the full child run, while recording public GPU integration and Metal availability as explicit aggregate outcomes.
Output: A standalone configuration gate and exactly-once no-skip integration; owner-document synchronization is isolated in Plan 04.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@PLANS.md
@scripts/check-backend-neutral-contract.sh
@scripts/check-metal-runtime.sh
@scripts/check-metal-feature-passes.sh
@scripts/run-no-skip-swiftpm.sh
@BeautySDK/Tests/BeautyCoreTests/BeautyConfigurationTests.swift
@BeautySDK/Tests/BeautyCoreTests/BeautyEngineBackendRoutingTests.swift

The gate must follow existing bounded-output/mutation-test conventions. It is not a replacement for the full no-skip closeout: the complete bash scripts/run-no-skip-swiftpm.sh run remains a required final verification after this gate is integrated.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement static and mutation checks for public backend policy</name>
  <files>scripts/check-backend-configuration.sh</files>
  <action>Create an executable SDK-owned gate with bounded logs and regular-file checks. Static validation must require exactly the two public backend cases, the defaulted renderBackend field and missing-key decode, the factory closed CPU/GPU switch, explicit BeautyBackendRequest policy propagation on all three engine routes, package-only injection boundaries, unchanged 61-parameter/five-preset/74-renderer inventories, and no UI/Demo/device/network dependency. Add self-test mutations that remove the selector/default/policy markers, replace the GPU branch with CPU, and introduce fallback/retry; each mutation must cause the static validator to reject the temporary tree. Keep output aggregate-only and do not inspect or persist raster/support/path data.</action>
  <verify>
    <automated>bash scripts/check-backend-configuration.sh --self-test</automated>
  </verify>
  <done>The static validator and all mutation cases pass, with public selector ownership and no-fallback policy enforced before any SwiftPM child execution.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate live focused coverage and the full no-skip gate</name>
  <files>scripts/check-backend-configuration.sh, scripts/run-no-skip-swiftpm.sh</files>
  <action>Make live configuration validation run the focused BeautyConfigurationTests and BeautyEngineBackendRoutingTests suites with nonzero execution, zero failures, and zero skips, including the public BeautyEngine(configuration: .gpu) integration case. Invoke the existing check-metal-runtime.sh preflight or its bounded availability command so metal_available and metal_unavailable remain separate classifications; an unavailable host must be an explicit .metalUnavailable outcome, never a skip or CPU success. Integrate this gate exactly once into run-no-skip-swiftpm.sh after the Metal feature gate and before consumer/CPU-oracle/opt-in/full-child stages. Then run the complete bash scripts/run-no-skip-swiftpm.sh and record its actual aggregate counts for the next documentation plan; do not claim closeout from self-test-only output.</action>
  <verify>
    <automated>bash scripts/check-backend-configuration.sh &amp;&amp; bash scripts/run-no-skip-swiftpm.sh</automated>
  </verify>
  <done>Live configuration and runtime classifications pass, the no-skip wrapper invokes the new gate exactly once in archive-first order, and the full mandatory wrapper completes with zero failures and zero unexpected skips.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Repository source → configuration gate | Mutable implementation is checked for selector, policy, and fallback drift before execution. |
| Gate → SwiftPM child | Only bounded aggregate counts and availability classifications cross into durable validation output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-73-08 | Tampering | configuration static/mutation gate | mitigate | Reject missing/defaulted selectors, CPU substitution in GPU branches, omitted request policy, and fallback/retry mutations before tests run. |
| T-73-09 | Denial of Service | mandatory no-skip wrapper | mitigate | Require nonzero focused execution, zero failures, zero skips, bounded transcripts, and exact once-only gate ordering; run the full wrapper before claiming completion. |
| T-73-10 | Information Disclosure | gate output | mitigate | Persist only aggregate counts, error codes, and availability classifications; prohibit pixels, support, paths, framework objects, and fixture locators. |
| T-73-SC | Tampering | package installs | accept | No package installation or external dependency is introduced by this plan. |
</threat_model>

<verification>
Run bash scripts/check-backend-configuration.sh --self-test, the live configuration gate, and the complete bash scripts/run-no-skip-swiftpm.sh; also run git diff --check. The full wrapper must pass with zero failures and zero unexpected skips.
</verification>

<success_criteria>
The Phase 73 configuration gate is mutation-tested, its public GPU integration and unavailable-host behavior are explicit, and the archive-first full SwiftPM/SDK-owned wrapper passes before documentation/ledger closeout.
</success_criteria>

<output>
Create .planning/phases/73-public-backend-configuration-and-fail-closed-availability/73-03-SUMMARY.md when done.
</output>
