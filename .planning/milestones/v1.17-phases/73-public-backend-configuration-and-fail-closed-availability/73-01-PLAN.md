---
phase: 73-public-backend-configuration-and-fail-closed-availability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BeautySDK/Sources/BeautyCore/Models/BeautyConfiguration.swift
  - BeautySDK/Tests/BeautyCoreTests/BeautyConfigurationTests.swift
autonomous: true
requirements:
  - CONFIG-01
must_haves:
  truths:
    - "Public configuration exposes exactly the selectable .cpu and .gpu render backends."
    - "New configurations and configurations decoded without the new key select .cpu."
    - "The new backend field is Codable/Equatable/Sendable without changing BeautyParameters, preset, or renderer-case schemas."
  artifacts:
    - path: "BeautySDK/Sources/BeautyCore/Models/BeautyConfiguration.swift"
      provides: "Public BeautyRenderBackend enum and compatibility-safe renderBackend configuration field"
      contains: "public var renderBackend: BeautyRenderBackend"
    - path: "BeautySDK/Tests/BeautyCoreTests/BeautyConfigurationTests.swift"
      provides: "Generated configuration raw-value, default, Codable, legacy, invalid-value, and schema regression coverage"
  key_links:
    - from: "BeautySDK/Sources/BeautyCore/Models/BeautyConfiguration.swift"
      to: "BeautyConfiguration.init(from:)"
      via: "decodeIfPresent(BeautyRenderBackend.self, forKey: .renderBackend) ?? .cpu"
      pattern: "decodeIfPresent.*renderBackend"
    - from: "BeautySDK/Sources/BeautyCore/Models/BeautyConfiguration.swift"
      to: "BeautyConfiguration.init"
      via: "trailing defaulted public initializer argument"
      pattern: "renderBackend: BeautyRenderBackend = .cpu"
---

<objective>
Define the public, compatibility-safe backend configuration contract required by CONFIG-01.

Purpose: Give SDK integrators an explicit .cpu/.gpu policy while preserving existing source calls, legacy configuration decoding, and the independent BeautyParameters/preset inventory.
Output: A public BeautyRenderBackend value type, a defaulted BeautyConfiguration.renderBackend field, and deterministic SwiftPM regression coverage.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@PLANS.md
@DESIGN.md
@ARCHITECTURE.md
@BeautySDK/Sources/BeautyCore/Models/BeautyConfiguration.swift
@BeautySDK/Tests/BeautyCoreTests/BeautyConfigurationTests.swift

Phase 70–72 deliberately kept public selection absent; Phase 73 is the first phase authorized to add it. The field is execution policy only: do not add it to BeautyParameters, preset JSON, resource manifests, or renderer cases. Existing BeautyConfiguration decoding already has explicit missing-key defaults for input limits; preserve that pattern for the new field.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the public CPU/GPU configuration contract</name>
  <files>BeautySDK/Sources/BeautyCore/Models/BeautyConfiguration.swift</files>
  <behavior>
    - BeautyRenderBackend.cpu.rawValue is "cpu" and .gpu.rawValue is "gpu".
    - BeautyConfiguration() and BeautyConfiguration.default have renderBackend == .cpu.
    - BeautyConfiguration(renderBackend: .gpu) preserves .gpu through initialization and Codable round-trip.
    - Decoding a configuration whose renderBackend key is absent yields .cpu; an unknown backend string remains a decoding error.
  </behavior>
  <action>Define public enum BeautyRenderBackend: String, Codable, Equatable, Sendable with exactly the .cpu and .gpu cases in the BeautyCore model layer. Add public var renderBackend: BeautyRenderBackend as the final stored configuration field, append renderBackend: BeautyRenderBackend = .cpu to the existing public initializer so all existing source call sites remain valid, and initialize it without normalization. Extend the explicit CodingKeys/decoder path so the key is decoded with decodeIfPresent(... ) ?? .cpu; retain the existing required/optional behavior of every prior key and let unknown enum raw values fail decoding. Do not hand-edit BeautyParameters, preset resources, or renderer inventories.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyConfigurationTests</automated>
  </verify>
  <done>The package builds, the public enum has exactly two cases, default/source/Codable behavior is CPU-compatible, and no parameter/preset schema is changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock configuration compatibility and schema boundaries</name>
  <files>BeautySDK/Tests/BeautyCoreTests/BeautyConfigurationTests.swift</files>
  <behavior>
    - Current encoded configurations contain renderBackend with the selected raw value.
    - A legacy JSON object with renderBackend, maximumInputByteCount, and maximumInputPixelCount removed decodes with all three defaults, including .cpu.
    - An explicit .gpu JSON value round-trips and remains Sendable.
    - An invalid renderBackend raw value is rejected rather than silently mapped to CPU.
    - Encoded BeautyParameters and the bundled preset/renderer inventories contain no backend selection field or new effect entry.
  </behavior>
  <action>Extend the existing BeautyConfigurationTests table without adding tracked media or UI fixtures. Assert raw values, default/explicit initialization, JSON round-trip, missing-key legacy decoding, invalid-value rejection, and sendability. Add a focused schema assertion that backend selection is absent from BeautyParameters encoding and that the existing 61-field/five-preset/74-renderer compatibility counts remain unchanged; use existing SDK-owned resource/test helpers rather than duplicating inventory logic. Keep all observations aggregate-only.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter BeautyCoreTests.BeautyConfigurationTests</automated>
  </verify>
  <done>The focused configuration suite executes with zero failures and zero skips, and it proves both the new configuration contract and the unchanged beauty-parameter/preset/renderer boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Host configuration JSON → BeautyConfiguration | Untrusted or stale persisted configuration crosses the public Codable boundary. |
| Host source call → public initializer | Integrator-controlled enum/field values enter SDK execution policy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-73-01 | Tampering | BeautyConfiguration.init(from:) | mitigate | Decode only the closed BeautyRenderBackend enum; default only an absent key to .cpu, and reject unknown raw values. |
| T-73-02 | Elevation of Privilege | BeautyParameters/preset boundary | mitigate | Test and gate that backend policy is not persisted in beauty strengths, preset values, or effect inventory. |
| T-73-03 | Information Disclosure | configuration tests and Codable values | mitigate | Persist only the two public backend tokens; no image, support, path, or diagnostic data enters the contract. |
| T-73-SC | Tampering | package installs | accept | No package installation or external dependency is introduced by this plan. |
</threat_model>

<verification>
Run the focused configuration suite and git diff --check after both tasks. Confirm the only production contract change is the public configuration field/enum and that BeautyParameters, resources, and renderer inventories are unchanged.
</verification>

<success_criteria>
CONFIG-01 is satisfied at the model boundary: .cpu/.gpu are the only public values, default and missing legacy keys are .cpu, explicit .gpu survives Codable, invalid values fail, and all prior source/schema compatibility checks remain green.
</success_criteria>

<output>
Create .planning/phases/73-public-backend-configuration-and-fail-closed-availability/73-01-SUMMARY.md when done.
</output>
