---
phase: 71-sdk-owned-metal-runtime
plan: "04"
type: execute
wave: 4
depends_on: [71-03]
files_modified:
  - PLANS.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [METAL-01]
must_haves:
  truths:
    - "METAL-01 is marked complete only after runtime/backend tests, static mutation preflight, archive-first boundary checks, and the mandatory no-skip SwiftPM wrapper pass with positive execution and zero failures/unexpected skips."
    - "Planning ledgers trace Phase 71's four plans and METAL-01 exactly, report aggregate availability/resource evidence without raw data, and preserve CPU reference plus Phase 72/73/74 ownership."
    - "Project and state closeout explicitly exclude public backend selection, feature-pass parity, new algorithms, UI/Demo lifecycle, simulator/physical-device testing, performance, commercial, packaging, shipping, launch, and release-readiness claims."
  artifacts:
    - path: "PLANS.md"
      provides: "Completed Phase 71 ledger with measured runtime/preflight evidence and nonclaims"
      contains: "METAL-01"
    - path: ".planning/REQUIREMENTS.md"
      provides: "METAL-01 completion and exact Phase 71 traceability"
      contains: "METAL-01"
    - path: ".planning/ROADMAP.md"
      provides: "Four-plan Phase 71 list and completed status"
      contains: "71-04-PLAN.md"
    - path: ".planning/STATE.md"
      provides: "Phase transition state after Phase 71 closeout"
      contains: "Phase 72"
    - path: ".planning/PROJECT.md"
      provides: "Current v1.17 runtime state and preserved nonclaims"
      contains: "SDK-Owned Metal Runtime"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "PLANS.md"
      via: "METAL-01 status and aggregate verification evidence"
      pattern: "METAL-01"
    - from: ".planning/ROADMAP.md"
      to: ".planning/STATE.md"
      via: "four-plan completion and transition to Phase 72"
      pattern: "Phase 72"
    - from: "PLANS.md"
      to: ".planning/PROJECT.md"
      via: "same measured runtime availability/cleanup result and nonclaims"
      pattern: "metal_(available|unavailable)"
---

<objective>
Record the measured Phase-71 closeout and transition planning state to the
Metal feature-pass phase without overclaiming public selection or device work.

Purpose: METAL-01 completion must be traceable to executable evidence, not to
the presence of source files. The ledgers must preserve the exact handoff to
Phase 72 feature passes, Phase 73 public configuration, and Phase 74 parity and
SDK-only closeout.

Output: Synchronized PLANS/project/requirements/roadmap/state records and an
explicit Phase 71 source-coverage audit.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/71-sdk-owned-metal-runtime/71-01-PLAN.md
@.planning/phases/71-sdk-owned-metal-runtime/71-02-PLAN.md
@.planning/phases/71-sdk-owned-metal-runtime/71-03-PLAN.md
@.planning/phases/70-backend-neutral-contract-and-cpu-reference/70-VERIFICATION.md
@ARCHITECTURE.md
@DESIGN.md
@SECURITY.md
@RELIABILITY.md
@QUALITY_SCORE.md
@scripts/check-metal-runtime.sh
@scripts/run-no-skip-swiftpm.sh

<interfaces>
Phase 71 has one roadmap requirement, METAL-01. Plans 71-01 through 71-03
produce the runtime, internal executor, static gate, and synchronized owners.
This plan may mark METAL-01 complete only after the full archive-first wrapper
and all declared focused/static checks pass. Aggregate counts and the explicit
`metal_available`/`metal_unavailable` classification are durable; raw bytes,
Metal objects, paths, child transcripts, and host identifiers are not.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run final Phase 71 verification and update traceability ledgers</name>
  <read_first>scripts/check-metal-runtime.sh; scripts/run-no-skip-swiftpm.sh; scripts/check-sdk-only-boundary.sh; ARCHITECTURE.md; DESIGN.md; SECURITY.md; RELIABILITY.md; QUALITY_SCORE.md; PLANS.md; .planning/PROJECT.md; .planning/REQUIREMENTS.md; .planning/ROADMAP.md; .planning/STATE.md; .planning/phases/71-sdk-owned-metal-runtime/71-01-SUMMARY.md; .planning/phases/71-sdk-owned-metal-runtime/71-02-SUMMARY.md; .planning/phases/71-sdk-owned-metal-runtime/71-03-SUMMARY.md</read_first>
  <files>.planning/REQUIREMENTS.md; .planning/ROADMAP.md; .planning/STATE.md</files>
  <action>Run `bash scripts/check-metal-runtime.sh --self-test`, normal Metal preflight, the post-archive SDK-only boundary, focused backend/runtime suites, generated CPU/backend compatibility checks, and the complete `bash scripts/run-no-skip-swiftpm.sh` archive-first wrapper. Require nonzero focused/full execution, zero failures, zero unexpected skips, all existing opt-ins exactly once, and separate aggregate `metal_available` or `metal_unavailable` reporting; an unavailable host must pass only its explicit fail-closed path and cannot satisfy available-GPU success/parity. After all checks pass, mark METAL-01 complete in REQUIREMENTS with exactly Phase 71 ownership, update ROADMAP to four completed plans and Phase 72 as next, and update STATE to the completed Phase 71 position. Record only command names, bounded test counts, resource-cleanup/terminal-error status, and availability classification; do not write raw child transcripts, pixel/texture bytes, support, paths, host/device identifiers, or any public selector/feature-parity/device claim.</action>
  <acceptance_criteria>Requirements, roadmap, and state agree on METAL-01 completion, four plans, measured aggregate verification, and transition to Phase 72; no requirement is duplicated or marked complete from an unavailable-host branch alone.</acceptance_criteria>
  <verify>
    <automated>bash scripts/check-metal-runtime.sh --self-test</automated>
    <automated>bash scripts/check-metal-runtime.sh</automated>
    <automated>bash scripts/check-sdk-only-boundary.sh --post-archive</automated>
    <automated>bash scripts/run-no-skip-swiftpm.sh --self-test</automated>
    <automated>bash scripts/run-no-skip-swiftpm.sh</automated>
    <automated>rg -n -C 4 'METAL-01|Phase 71|Phase 72|71-0[1-4]-PLAN\.md|metal_(available|unavailable)' .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>Final gates pass with explicit availability accounting, and requirements/roadmap/state report the measured Phase 71 closeout and Phase 72 handoff without overclaim.</done>
</task>

<task type="auto">
  <name>Task 2: Record aggregate Phase 71 project and plan ledger closeout</name>
  <read_first>PLANS.md; .planning/PROJECT.md; .planning/REQUIREMENTS.md; .planning/ROADMAP.md; .planning/STATE.md; .planning/phases/71-sdk-owned-metal-runtime/71-01-PLAN.md; .planning/phases/71-sdk-owned-metal-runtime/71-02-PLAN.md; .planning/phases/71-sdk-owned-metal-runtime/71-03-PLAN.md; scripts/check-metal-runtime.sh</read_first>
  <files>PLANS.md; .planning/PROJECT.md</files>
  <action>Append a completed Phase-71 ledger entry to PLANS and synchronize the current milestone PROJECT state with the exact runtime ownership delivered: package-only device/queue/pipeline setup, bounded request-local textures/command synchronization, explicit unavailable-host failure, internal `.metal` boundary execution, no CPU fallback, and mutation-tested archive-first preflight. Include the four plan IDs, requirement METAL-01, aggregate focused/full test and preflight results from Task 1, and the next-phase ownership split (Phase 72 feature passes, Phase 73 public `.cpu`/`.gpu` configuration, Phase 74 parity/closeout). Preserve historical entries and explicitly state that no new algorithm, public selector, UI/Demo lifecycle, simulator/physical-device test, performance, commercial, packaging, shipping, launch, or release-readiness claim follows. Apply the mandatory source audit below: GOAL is covered by Plans 71-01..04; REQ METAL-01 is covered by all four plan frontmatters and completion ledger; RESEARCH is intentionally absent because research is disabled by project configuration and user `--auto`, so no external research item exists; CONTEXT is intentionally absent, so there are no locked decisions or deferred ideas to implement. Do not add raw host output or sensitive runtime data.</action>
  <acceptance_criteria>PLANS and PROJECT contain one aggregate-only Phase-71 completion record consistent with REQUIREMENTS/ROADMAP/STATE, the source audit accounts for GOAL/REQ/RESEARCH/CONTEXT with no unplanned item, and all future-phase/nonclaim boundaries remain explicit.</acceptance_criteria>
  <verify>
    <automated>rg -n -C 8 'Phase 71|SDK-Owned Metal Runtime|METAL-01|71-01|71-02|71-03|71-04|metal_(available|unavailable)|Phase 72|Phase 73|Phase 74' PLANS.md .planning/PROJECT.md</automated>
    <automated>if rg -n '(/private/|/Users/|raw (pixel|mask)|landmark array|fixture locator|child transcript|host identifier|MTLTexture.*description)' PLANS.md .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md; then exit 1; else exit 0; fi</automated>
    <automated>python3 - <<'PY'
from pathlib import Path
required = ["71-01-PLAN.md", "71-02-PLAN.md", "71-03-PLAN.md", "71-04-PLAN.md", "METAL-01"]
text = "\n".join(Path(path).read_text(encoding="utf-8") for path in ("PLANS.md", ".planning/PROJECT.md", ".planning/REQUIREMENTS.md", ".planning/ROADMAP.md", ".planning/STATE.md"))
missing = [item for item in required if item not in text]
raise SystemExit(f"missing ledger token(s): {missing}" if missing else 0)
PY</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>Project and plan ledgers preserve a measured, aggregate-only Phase 71 record with full source coverage and an explicit, non-overclaiming handoff to later v1.17 phases.</done>
</task>

</tasks>

<source_audit>
## Multi-Source Coverage Audit

| Source | Items | Plan coverage | Status |
| --- | --- | --- | --- |
| GOAL (`ROADMAP.md`) | SDK-owned bounded Metal device, queue, textures, synchronization, and resource lifetime without app/UI lifecycle or cross-request leakage | 71-01 runtime; 71-02 boundary executor; 71-03 gate/owners; 71-04 measured closeout | COVERED |
| REQ (`REQUIREMENTS.md`) | METAL-01 | Every plan frontmatter and completion ledger | COVERED |
| RESEARCH (`RESEARCH.md`) | No phase research artifact; research is disabled by project configuration and the user requested `--auto` | N/A — no source items exist | EXCLUDED, NOT A GAP |
| CONTEXT (`CONTEXT.md`) | No phase context artifact; no locked decisions, discretion notes, or deferred ideas exist for this phase | N/A — no source items exist | EXCLUDED, NOT A GAP |

No source item is unplanned. Later-phase METAL-02..04, CONFIG-01..02,
PARITY-01..03, and CLOSE-01..02 are roadmap-scoped exclusions, not Phase-71
gaps; no deferred idea is implemented here.
</source_audit>

<threat_model>
Security posture: OWASP ASVS Level 1 applies. Any HIGH finding is blocking and
must be mitigated before this plan can complete.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Verification commands → ledgers | Only successful bounded aggregate checks may authorize requirement completion. |
| Availability classification → requirement status | Unavailable-host status must not be interpreted as GPU success. |
| Plan/source audit → future execution | Every GOAL/REQ source item must map to an executable plan; absent research/context must remain explicit. |
| Runtime evidence → durable records | Raw output, host identity, framework detail, and child transcripts cannot persist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-71-13 | Tampering | Requirements/roadmap status | mitigate | Update completion only after the complete archive-first wrapper and explicit availability accounting pass. |
| T-71-14 | Information Disclosure | PLANS/PROJECT/STATE records | mitigate | Persist fixed IDs, counts, status, and nonclaims only; scan for paths/raw/host data before closeout. |
| T-71-15 | Repudiation | Source coverage audit | mitigate | Record GOAL, REQ, RESEARCH, and CONTEXT disposition explicitly and verify all plan/requirement tokens. |
| T-71-16 | Scope/Elevation | Later-phase handoff | mitigate | Keep feature passes, public selector, parity, device, and release claims assigned to Phases 72–74. |
| T-71-SC | Tampering | Package installs | accept | No package-manager install or dependency change occurs in this plan. |
</threat_model>

<verification>
Run the final Metal preflight, SDK-only boundary, archive-first no-skip wrapper,
ledger/source-audit scans, and `git diff --check`. Requirement completion is
valid only when the wrapper reports zero failures/unexpected skips and a
separate unavailable-host result where no Metal device exists.
</verification>

<success_criteria>
- METAL-01 is traceably complete against all four source types and four plan
  artifacts, based on executable aggregate evidence.
- Phase 71 transitions cleanly to Phase 72 while preserving CPU reference,
  package-only scope, no public selector, no new algorithms, and no device or
  release-readiness claims.
</success_criteria>

<artifacts>
- Aggregate PLANS/PROJECT/REQUIREMENTS/ROADMAP/STATE closeout with exact
  METAL-01 traceability and source audit.
</artifacts>

<output>
Create `.planning/phases/71-sdk-owned-metal-runtime/71-04-SUMMARY.md` when done.
</output>
