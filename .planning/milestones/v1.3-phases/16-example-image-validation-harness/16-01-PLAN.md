---
phase: "16-example-image-validation-harness"
plan: "16-01"
status: "pending"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "BeautySDK/Package.swift"
  - "BeautySDK/Sources/BeautyExampleRenderer/main.swift"
  - "example-images/out/"
autonomous: false
requirements: ["PREP-01", "PREP-02", "PREP-03"]
requirements_addressed: ["PREP-01", "PREP-02", "PREP-03"]
must_haves:
  truths:
    - "D-01 and D-03: Phase 16 formalizes the already-prepared renderer path; do not expand capabilities except for minimal build/run blockers."
    - "D-04: PREP-01 through PREP-04 completion must be based on commands rerun during Phase 16 execution."
    - "D-05 and D-06: the required verification set is build, one representative renderer run, and `file` dimension proof for `skinWhitening_0p50`."
    - "D-07: visual inspection is factual only: output is non-empty, watermark is readable, and bottom watermark does not cover the face."
    - "D-08: build, run, or dimension-check failure blocks Phase 16 completion."
    - "D-09 through D-12: outputs stay under ignored `example-images/out/`; representative output is `e2__skinWhitening_0p50.png` and must match `example-images/input/e2.png` dimensions."
    - "D-13: geometry-heavy branches do not count as visually complete in Phase 16."
    - "D-14: existing providers/tests may exist, but saved image output still needs face detection plus geometry render image-output integration."
    - "D-15: Phase 19 owns geometry saved-output status and verification for face/facial-feature shaping branches."
    - "D-16: future geometry output evidence must use public `BeautySDK` facade processing, current input fixtures, same-dimension output, and watermarking through this renderer path."
    - "D-17 and D-19: use only current fixtures `e1.png` through `e5.png`, with short stable IDs for future additions."
    - "D-18: input images may remain in the repository, while generated outputs remain ignored."
  artifacts:
    - "Fresh SwiftPM build evidence for `BeautyExampleRenderer`."
    - "Fresh representative local outputs under ignored `example-images/out/`."
    - "Command evidence for `example-images/input/e2.png` and `example-images/out/e2__skinWhitening_0p50.png` dimensions."
    - "Short factual manual observation for the representative watermark."
---

# Plan 16-01: Verify BeautyExampleRenderer Executable and Output Path

<objective>
Re-run the minimal Phase 16 command evidence proving `BeautyExampleRenderer` builds, processes current fixtures through the public `BeautySDK` facade, and writes same-dimension, watermarked PNG outputs under ignored `example-images/out/`.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@ARCHITECTURE.md
@DESIGN.md
@SECURITY.md
@RELIABILITY.md
@PRODUCT_SENSE.md
@QUALITY_SCORE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-example-image-validation-harness/16-CONTEXT.md
@.planning/phases/16-example-image-validation-harness/16-RESEARCH.md
@.planning/phases/16-example-image-validation-harness/16-VALIDATION.md
@.planning/phases/16-example-image-validation-harness/16-PATTERNS.md
@BeautySDK/Package.swift
@BeautySDK/Sources/BeautyExampleRenderer/main.swift
@docs/meitu-function-blueprint/EXAMPLE_IMAGE_VALIDATION.md
@docs/meitu-function-blueprint/MODULES.md
@.gitignore
</context>

## Artifacts this phase produces

- Fresh local renderer output files under ignored `example-images/out/`, including `example-images/out/e2__skinWhitening_0p50.png`.
- Summary evidence recording the build command, renderer run command, `file` dimension output, ignored-output check, facade-only import scan, and factual watermark observation.
- No new public SDK symbols, no new render cases, no new fixtures, no new SwiftUI screens, and no committed PNG evidence.

## Source Coverage Audit

| Source | ID | Feature / Requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| REQ | PREP-01 | SwiftPM executable loads fixtures through public facade and saves PNGs | 16-01 | COVERED | Build and run command evidence. |
| REQ | PREP-02 | Filenames include source, parameter, strength; output dimensions match input | 16-01 | COVERED | `e2__skinWhitening_0p50.png` and `file` proof. |
| REQ | PREP-03 | Watermark readable and not over face | 16-01 | COVERED | Manual factual observation. |
| CONTEXT | D-01, D-03 | Formalize existing path without scope expansion | 16-01 | COVERED | Product changes only for blockers. |
| CONTEXT | D-04, D-05, D-06, D-08 | Fresh minimal command evidence blocks completion on failure | 16-01 | COVERED | Build/run/file gates. |
| CONTEXT | D-07 | Factual visual inspection only | 16-01 | COVERED | No beauty-quality claims. |
| CONTEXT | D-09, D-10, D-11, D-12 | Ignored output policy and representative output | 16-01 | COVERED | `git check-ignore` and output name. |
| CONTEXT | D-13, D-14, D-16 | Geometry limitation is not closed here | 16-01 | COVERED | No geometry completion claim. |
| CONTEXT | D-17, D-18, D-19 | Current five fixture inputs only | 16-01 | COVERED | No fixture expansion. |

<threat_model>
T-16-01 Internal SDK dependency creep: Blocked by scanning `BeautySDK/Sources/BeautyExampleRenderer` for `import BeautyCore`, `import BeautyDetection`, `import BeautyRender`, `import BeautyEffects`, `import BeautyResources`, `import SwiftUI`, and `import UIKit`.
T-16-02 Committed generated image evidence: Blocked by `git check-ignore example-images/out/e2__skinWhitening_0p50.png` and by keeping `example-images/out/` local only.
T-16-03 False visual-quality claim: Blocked by limiting human observation to non-empty output, readable watermark, and bottom watermark placement.
T-16-04 Scope expansion into algorithms or fixtures: Blocked by banning new cases, fixtures, formats, and beauty behavior unless directly required to fix a build/run blocker.
</threat_model>

<tasks>

<task id="16-01-01" type="auto">
  <name>Task 1: Build the example renderer executable</name>
  <files>
    BeautySDK/Package.swift
    BeautySDK/Sources/BeautyExampleRenderer/main.swift
  </files>
  <read_first>
    AGENTS.md
    PLANS.md
    .planning/phases/16-example-image-validation-harness/16-CONTEXT.md
    .planning/phases/16-example-image-validation-harness/16-RESEARCH.md
    .planning/phases/16-example-image-validation-harness/16-VALIDATION.md
    BeautySDK/Package.swift
    BeautySDK/Sources/BeautyExampleRenderer/main.swift
    docs/meitu-function-blueprint/MODULES.md
  </read_first>
  <action>Run `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift build --package-path BeautySDK --product BeautyExampleRenderer`. If it passes, do not change product code. If it fails because of a direct package or renderer blocker, make only the smallest fix in `BeautySDK/Package.swift` or `BeautySDK/Sources/BeautyExampleRenderer/main.swift`, keep the executable target dependent only on `BeautySDK`, rerun the build, and record the blocker and fix in the summary.</action>
  <acceptance_criteria>
    - Build command exits 0: `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift build --package-path BeautySDK --product BeautyExampleRenderer`.
    - `BeautySDK/Package.swift` contains `.executable(name: "BeautyExampleRenderer", targets: ["BeautyExampleRenderer"])`.
    - `BeautySDK/Package.swift` executable target `BeautyExampleRenderer` depends on `["BeautySDK"]`.
    - `BeautySDK/Sources/BeautyExampleRenderer/main.swift` contains `import BeautySDK`.
    - `BeautySDK/Sources/BeautyExampleRenderer/main.swift` contains no `import BeautyCore`, `import BeautyDetection`, `import BeautyRender`, `import BeautyEffects`, `import BeautyResources`, `import SwiftUI`, or `import UIKit`.
  </acceptance_criteria>
  <verify>
    <automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift build --package-path BeautySDK --product BeautyExampleRenderer</automated>
    <automated>rg -n "executable\\(name: \"BeautyExampleRenderer\"|name: \"BeautyExampleRenderer\"|dependencies: \\[\"BeautySDK\"\\]" BeautySDK/Package.swift</automated>
    <automated>rg -n "import BeautySDK" BeautySDK/Sources/BeautyExampleRenderer/main.swift</automated>
    <automated>! rg -n "import Beauty(Core|Detection|Render|Effects|Resources)|import SwiftUI|import UIKit" BeautySDK/Sources/BeautyExampleRenderer</automated>
  </verify>
  <done>`BeautyExampleRenderer` builds as a facade-only SwiftPM executable.</done>
</task>

<task id="16-01-02" type="auto">
  <name>Task 2: Run the representative renderer case</name>
  <files>
    example-images/out/
  </files>
  <read_first>
    .planning/phases/16-example-image-validation-harness/16-CONTEXT.md
    .planning/phases/16-example-image-validation-harness/16-RESEARCH.md
    .planning/phases/16-example-image-validation-harness/16-VALIDATION.md
    BeautySDK/Sources/BeautyExampleRenderer/main.swift
    docs/meitu-function-blueprint/EXAMPLE_IMAGE_VALIDATION.md
    .gitignore
    example-images/input/e1.png
    example-images/input/e2.png
    example-images/input/e3.png
    example-images/input/e4.png
    example-images/input/e5.png
  </read_first>
  <action>Run `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift run --package-path BeautySDK BeautyExampleRenderer --input example-images/input --output example-images/out --case skinWhitening_0p50`. Existing same-name files in `example-images/out/` may be overwritten. Do not add timestamped output directories, extra fixtures, extra cases, or alternate formats. Confirm that `example-images/out/e2__skinWhitening_0p50.png` exists and that `git check-ignore example-images/out/e2__skinWhitening_0p50.png` exits 0.</action>
  <acceptance_criteria>
    - Renderer command exits 0 for `--case skinWhitening_0p50`.
    - Command output includes a `wrote .../example-images/out/e2__skinWhitening_0p50.png` line or the output file exists after the run.
    - `test -f example-images/out/e2__skinWhitening_0p50.png` exits 0.
    - `git check-ignore example-images/out/e2__skinWhitening_0p50.png` exits 0.
    - `BeautySDK/Sources/BeautyExampleRenderer/main.swift` still contains render case ID `skinWhitening_0p50` and display name `skinWhitening 0.50`.
  </acceptance_criteria>
  <verify>
    <automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift run --package-path BeautySDK BeautyExampleRenderer --input example-images/input --output example-images/out --case skinWhitening_0p50</automated>
    <automated>test -f example-images/out/e2__skinWhitening_0p50.png</automated>
    <automated>git check-ignore example-images/out/e2__skinWhitening_0p50.png</automated>
    <automated>rg -n "skinWhitening_0p50|skinWhitening 0\\.50" BeautySDK/Sources/BeautyExampleRenderer/main.swift</automated>
  </verify>
  <done>Representative local output exists under the ignored output directory.</done>
</task>

<task id="16-01-03" type="manual">
  <name>Task 3: Prove representative dimensions and watermark placement</name>
  <files>
    example-images/out/e2__skinWhitening_0p50.png
  </files>
  <read_first>
    .planning/phases/16-example-image-validation-harness/16-CONTEXT.md
    .planning/phases/16-example-image-validation-harness/16-VALIDATION.md
    docs/meitu-function-blueprint/EXAMPLE_IMAGE_VALIDATION.md
    example-images/input/e2.png
    example-images/out/e2__skinWhitening_0p50.png
  </read_first>
  <action>Run `file example-images/input/e2.png example-images/out/e2__skinWhitening_0p50.png` and require both lines to report PNG image data with `576 x 1024`. Open `example-images/out/e2__skinWhitening_0p50.png` locally and record only this factual observation in the summary: output is non-empty, the watermark text is readable, and the bottom watermark does not cover the face. Do not make subjective claims about beauty quality, naturalness, or production readiness.</action>
  <acceptance_criteria>
    - `file example-images/input/e2.png example-images/out/e2__skinWhitening_0p50.png` reports `example-images/input/e2.png: PNG image data, 576 x 1024`.
    - The same `file` command reports `example-images/out/e2__skinWhitening_0p50.png: PNG image data, 576 x 1024`.
    - Summary records the factual observation string: `output is non-empty; watermark is readable; bottom watermark does not cover the face`.
    - Summary avoids subjective visual-quality, release-readiness, and geometry-completion claims.
  </acceptance_criteria>
  <verify>
    <automated>file example-images/input/e2.png example-images/out/e2__skinWhitening_0p50.png</automated>
    <manual>Open `example-images/out/e2__skinWhitening_0p50.png` and record: `output is non-empty; watermark is readable; bottom watermark does not cover the face`.</manual>
  </verify>
  <done>Representative output dimensions and watermark placement are recorded as Phase 16 evidence.</done>
</task>

</tasks>

## Verification

- `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift build --package-path BeautySDK --product BeautyExampleRenderer`
- `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift run --package-path BeautySDK BeautyExampleRenderer --input example-images/input --output example-images/out --case skinWhitening_0p50`
- `file example-images/input/e2.png example-images/out/e2__skinWhitening_0p50.png`
- `git check-ignore example-images/out/e2__skinWhitening_0p50.png`
- `! rg -n "import Beauty(Core|Detection|Render|Effects|Resources)|import SwiftUI|import UIKit" BeautySDK/Sources/BeautyExampleRenderer`

## Success Criteria

- `BeautyExampleRenderer` builds as a SwiftPM executable product.
- Representative fixture processing through the public `BeautySDK` facade writes PNG outputs to ignored `example-images/out/`.
- Representative output `e2__skinWhitening_0p50.png` exists and has the same `576 x 1024` dimensions as `example-images/input/e2.png`.
- Factual watermark observation is recorded without subjective visual quality or geometry-completion claims.
