---
phase: 19-beauty-shaping-core-modules
plan: 03
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - BeautySDK/Sources/BeautyEffects/Warp/MouthWarpProvider.swift
  - BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift
  - BeautySDK/Tests/BeautyEffectsTests/MouthWarpProviderTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift
  - BeautySDK/Tests/BeautyEffectsTests/LipColorEffectTests.swift
autonomous: true
requirements:
  - BSHAPE-02
  - BSHAPE-03
must_haves:
  truths:
    - "D-01/D-02/D-03: Mouth/lip/resolver/degradation hardening remains inside `BeautySDK` sources/tests and uses SwiftPM verification, with no Demo or SwiftUI work."
    - "D-04/D-05/D-06: Resolver/provider evidence remains internal evidence and does not wire public facade saved-image geometry output."
    - "D-07/D-08/D-09: Production code uses only existing public mouth/lip fields and adds no `BeautyParameters` fields, coding keys, initializer arguments, or normalized copies."
    - "D-13: `lipColor` remains visible color evidence for a subtool, but the broader `嘴唇` branch remains `partial`."
    - "D-15/D-16: No pose-aware 3D sculpt, eyebrow geometry, texture synthesis, resource overlays, or public eyebrow parameters are introduced."
    - "D-17/D-18/D-19/D-20: Focused resolver/degradation tests and redaction scans prove safety without requiring renderer geometry cases."
  artifacts:
    - path: "BeautySDK/Tests/BeautyEffectsTests/MouthWarpProviderTests.swift"
      provides: "Mouth-provider evidence for `mouthSize`, `mouthWidth`, and `smile`."
    - path: "BeautySDK/Tests/BeautyEffectsTests/LipColorEffectTests.swift"
      provides: "Visible lip-color color-pipeline evidence without full lips-branch completion."
    - path: "BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift"
      provides: "Missing, stale, reused, and redacted degradation evidence for shaping branches."
    - path: "BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift"
      provides: "Resolver activation, skip, warning, and metric evidence."
  key_links:
    - from: ".planning/phases/19-beauty-shaping-core-modules/19-SHAPING-AUDIT.md"
      to: "BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift"
      via: "fixed mouth/lip/degradation/redaction evidence matrix for Plan 19-03"
      pattern: "mouthSize|mouthWidth|smile|lipColor|geometry_stale|inputs_missing"
    - from: "BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift"
      to: "BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift"
      via: "domain-specific skippedDomains/warnings/metrics assertions"
      pattern: "skippedDomains|geometry_stale|inputs_missing"
---

<objective>
Strengthen mouth, lip-color, resolver degradation, and redacted diagnostic evidence behind SDK boundaries using only existing public parameters and existing degradation patterns.

Purpose: This plan closes the mouth/lip/resolver portion of BSHAPE-02 without public API drift, Demo/UI scope creep, renderer geometry cases, or false saved-image claims.
Output: Focused source/test hardening in `BeautyEffects` resolver/mouth provider and `BeautyEffectsTests`.
</objective>

<execution_context>
@/Users/yakangwang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yakangwang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PLANS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-beauty-shaping-core-modules/19-CONTEXT.md
@.planning/phases/19-beauty-shaping-core-modules/19-RESEARCH.md
@.planning/phases/19-beauty-shaping-core-modules/19-VALIDATION.md
@.planning/phases/19-beauty-shaping-core-modules/19-PATTERNS.md
@.planning/phases/19-beauty-shaping-core-modules/19-SHAPING-AUDIT.md
</context>

## Source Coverage Audit

| Source | Items | Coverage |
| --- | --- | --- |
| GOAL | Implement or prepare promoted face/facial-feature shaping logic behind SDK boundaries, with honest geometry-output status. | This plan hardens the mouth/lip/resolver/degradation slice; Plan 19-02 covers face/chin/proportion, eye, nose, and conflict providers; Plan 19-04/19-05 handle final verification and closeout. |
| REQ | BSHAPE-02, BSHAPE-03 | BSHAPE-02 is addressed through mouth/lip/resolver/degradation evidence; BSHAPE-03 is preserved by keeping mouth geometry partial and saved-image geometry output unclaimed. |
| RESEARCH | Use existing `BeautyEffects` mouth provider, `BeautyEffectResolver`, degradation warnings/metrics, and XCTest. | Tasks target those exact files and add no external packages, no new public API, no UI, and no renderer geometry cases. |
| CONTEXT | D-01 through D-20 | D-01 through D-20 are cited in frontmatter and actions; deferred public facade geometry output, advanced shaping parameters, 3D sculpt, and eyebrows stay out of scope. |

## Artifacts this phase produces

- Updated or newly added XCTest methods in `MouthWarpProviderTests`, `MissingLandmarkDegradationTests`, `BeautyEffectResolverTests`, `CombinedEffectSafetyTests`, and `LipColorEffectTests`.
- Source-level hardening in `MouthWarpProvider` or `BeautyEffectResolver` only when the concrete mouth/resolver assertions require it.
- No new public `BeautyParameters` fields, no SwiftUI/Demo files, no `BeautyExampleRenderer` geometry cases, no public facade geometry saved-image output, and no new public SDK symbols are produced by this plan.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden mouth-provider and lip-color evidence</name>
  <files>BeautySDK/Sources/BeautyEffects/Warp/MouthWarpProvider.swift, BeautySDK/Tests/BeautyEffectsTests/MouthWarpProviderTests.swift, BeautySDK/Tests/BeautyEffectsTests/LipColorEffectTests.swift</files>
  <read_first>
    - `.planning/phases/19-beauty-shaping-core-modules/19-SHAPING-AUDIT.md`
    - `.planning/phases/19-beauty-shaping-core-modules/19-CONTEXT.md`
    - `.planning/phases/19-beauty-shaping-core-modules/19-PATTERNS.md`
    - `BeautySDK/Sources/BeautyEffects/Warp/MouthWarpProvider.swift`
    - `BeautySDK/Sources/BeautyEffects/Planning/BeautySafetyCaps.swift`
    - `BeautySDK/Sources/BeautyEffects/Render/BeautyColorEffectPipeline.swift`
    - `BeautySDK/Tests/BeautyEffectsTests/MouthWarpProviderTests.swift`
    - `BeautySDK/Tests/BeautyEffectsTests/LipColorEffectTests.swift`
  </read_first>
  <behavior>
    - `mouthSize`, `mouthWidth`, and `smile` produce deterministic, clamped mouth control points when mouth landmarks exist and skip with `mouth_inputs_missing` when lip inputs are absent.
    - Signed mouth size/width behavior remains explicit and cap-aware.
    - `lipColor` remains visible color evidence for a subtool, but the broader `嘴唇` branch remains partial because mouth geometry still lacks public facade saved-image output.
  </behavior>
  <action>Write or keep the concrete Phase 19 assertions for mouth geometry and lip-color evidence, then adjust `MouthWarpProvider` only when those assertions require a source correction. Preserve D-13 partial status and D-07 field boundaries; `lipColor` may remain visible color evidence but must not make the full lips branch `implemented`. Do not add M-lip, teeth, mouth position/tilt/left-right controls, public facade geometry output, renderer geometry cases, or UI state per D-01 through D-09 and D-13/D-20.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter MouthWarpProviderTests</automated>
    <automated>swift test --package-path BeautySDK --filter LipColorEffectTests</automated>
    <automated>git diff --check -- BeautySDK/Sources/BeautyEffects/Warp/MouthWarpProvider.swift BeautySDK/Tests/BeautyEffectsTests/MouthWarpProviderTests.swift BeautySDK/Tests/BeautyEffectsTests/LipColorEffectTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - Mouth geometry tests cover valid landmarks, missing mouth inputs, signed size/width behavior, and caps.
    - Lip-color tests still prove visible color evidence without claiming full mouth/lips branch completion.
    - No public mouth or lip controls beyond `mouthSize`, `mouthWidth`, `smile`, and `lipColor` appear in `BeautyParameters`.
  </acceptance_criteria>
  <done>Mouth and lip-color evidence is strengthened while the lips branch remains `partial`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden resolver degradation and redaction evidence</name>
  <files>BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift, BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift, BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift, BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift</files>
  <read_first>
    - `.planning/phases/19-beauty-shaping-core-modules/19-SHAPING-AUDIT.md`
    - `.planning/phases/19-beauty-shaping-core-modules/19-CONTEXT.md`
    - `.planning/phases/19-beauty-shaping-core-modules/19-PATTERNS.md`
    - `BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift`
    - `BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift`
    - `BeautySDK/Sources/BeautyEffects/Warp/GeometryConflictResolver.swift`
    - `BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift`
    - `BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift`
    - `BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift`
  </read_first>
  <behavior>
    - Missing eye, nose, or mouth inputs skip only affected domains while unaffected safe domains remain active.
    - Stale or reused geometry emits stable redacted warning codes and metrics while reducing or skipping unsafe geometry output.
    - Warning and metric metadata never exposes landmarks, control points, bounding boxes, raw Vision objects, local paths, image bytes, `SIMD`, or point arrays.
  </behavior>
  <action>Write or keep the concrete Phase 19 assertions for resolver skip behavior, stale/reused geometry degradation, and redacted diagnostics, then adjust `BeautyEffectResolver` only when those assertions require a source correction. Preserve D-18 focused suite coverage and D-19 redaction requirements. Do not expose geometry internals in warnings/metrics, do not change public facade geometry output, and do not add public API or UI scope.</action>
  <verify>
    <automated>swift test --package-path BeautySDK --filter MissingLandmarkDegradationTests</automated>
    <automated>swift test --package-path BeautySDK --filter BeautyEffectResolverTests</automated>
    <automated>swift test --package-path BeautySDK --filter CombinedEffectSafetyTests</automated>
    <automated>! rg -n 'landmark|control point|controlPoint|bounding|VNFaceObservation|/private/var|image bytes|SIMD|\\[0\\.' BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift</automated>
    <automated>git diff --check -- BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - Resolver/degradation tests cover missing, stale, and reused geometry without disabling unrelated safe domains.
    - Resolver and combined tests scan warnings/metrics for sensitive diagnostic terms and fail if raw geometry data is exposed.
    - The public facade still has no saved-image geometry output path.
  </acceptance_criteria>
  <done>Resolver, degradation, and combined redaction evidence is strengthened while geometry-heavy branches remain below `implemented`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Public host parameters to `BeautyEffects` internals | Only D-07 mouth/lip fields may affect Phase 19 mouth/lip behavior. |
| Internal landmarks/control points to warnings/metrics | Diagnostics must stay redacted even when tests strengthen geometry evidence. |
| Resolver evidence to public renderer | Internal resolver/control-point evidence must not be treated as saved-image output. |
| SDK implementation to Demo/UI | No SwiftUI, Demo route, category rail, tool panel, or app interaction state changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-19-03-01 | Repudiation | Tests and summaries | mitigate | Test names and summaries must say mouth provider/resolver evidence supports `partial`; Plan 19-04 scans branch docs for beauty-shaping `implemented` overclaims. |
| T-19-03-02 | Tampering | `BeautyParameters` | mitigate | This plan does not list `BeautyParameters.swift` in `files_modified`; Plan 19-05 verifies no D-08/D-09 fields were added. |
| T-19-03-03 | Elevation of Privilege / Scope Creep | `BeautyDemo`, SwiftUI | mitigate | This plan only lists `BeautySDK/Sources/BeautyEffects` and `BeautySDK/Tests/BeautyEffectsTests`; no Demo/UI file is modified. |
| T-19-03-04 | Information Disclosure | `BeautyEffectResolver`, tests | mitigate | Add/keep redaction assertions in `MissingLandmarkDegradationTests`, `BeautyEffectResolverTests`, and `CombinedEffectSafetyTests`; scan warning/metric text for landmarks, control points, bounding boxes, raw Vision objects, paths, and image bytes. |
| T-19-03-05 | Spoofing / Repudiation | `BeautyExampleRenderer`, public facade output | mitigate | This plan does not modify `BeautyExampleRenderer` or `BeautyEngine.processResult(image:)`; Plan 19-05 verifies no geometry renderer cases or saved-image geometry claims were added. |
| T-19-03-SC | Tampering | Package installs | accept | No npm/pip/cargo/SPM dependency install is part of this plan; SwiftPM uses existing local package targets. |
</threat_model>

<verification>
Plan-level verification:
- `swift test --package-path BeautySDK --filter MouthWarpProviderTests`
- `swift test --package-path BeautySDK --filter MissingLandmarkDegradationTests`
- `swift test --package-path BeautySDK --filter BeautyEffectResolverTests`
- `swift test --package-path BeautySDK --filter CombinedEffectSafetyTests`
- `swift test --package-path BeautySDK --filter LipColorEffectTests`
- `! rg -n 'landmark|control point|controlPoint|bounding|VNFaceObservation|/private/var|image bytes|SIMD|\\[0\\.' BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectPlan.swift`
- `git diff --check -- BeautySDK/Sources/BeautyEffects/Warp/MouthWarpProvider.swift BeautySDK/Sources/BeautyEffects/Planning/BeautyEffectResolver.swift BeautySDK/Tests/BeautyEffectsTests/MouthWarpProviderTests.swift BeautySDK/Tests/BeautyEffectsTests/MissingLandmarkDegradationTests.swift BeautySDK/Tests/BeautyEffectsTests/BeautyEffectResolverTests.swift BeautySDK/Tests/BeautyEffectsTests/CombinedEffectSafetyTests.swift BeautySDK/Tests/BeautyEffectsTests/LipColorEffectTests.swift`
</verification>

<success_criteria>
- BSHAPE-02 mouth/lip/resolver/degradation/redaction evidence is stronger.
- BSHAPE-03 is preserved: `lipColor` evidence does not make the full lips branch public facade saved-image complete.
- Existing public mouth/lip parameters are the only fields used.
- No SwiftUI/Demo scope, public facade geometry output, renderer geometry cases, 3D sculpt, eyebrow work, or external package install is introduced.
</success_criteria>

<output>
Create `.planning/phases/19-beauty-shaping-core-modules/19-03-SUMMARY.md` when done.
</output>
